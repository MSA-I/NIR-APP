# Plan: קליטה נכנסת (מייל + וואטסאפ), חוזה עמודות הדוח וייצוא מתוזמן, ומעבד החיוב

_נעול דרך claudex-loop — Claude + משה, 31.08.2026._

**המפרט המלא הוא [`docs/plans/2026-08-31-inbound-intake-and-billing.md`](docs/plans/2026-08-31-inbound-intake-and-billing.md)** (982 שורות).
המסמך הזה הוא **שכבת ההכרעה מעליו**: מה תוקן במפרט, מה הוכרע, ומה השתנה בהיקף.
במקום סתירה — **המסמך הזה גובר.**

מחקר הספקים: [`docs/research/2026-08-31-inbound-intake-claudex-research.md`](docs/research/2026-08-31-inbound-intake-claudex-research.md).

**מצב: `PLANNED / NOT_IMPLEMENTED / NOT_DEPLOYED`.** לא נכתב קוד מוצר.

| | |
|---|---|
| base commit | `417775b9dd9e3e138ee4fc3adc0889d70295dcff` |
| ענף | `claude/inbound-intake-billing-a0d0c4` |
| מיגרציה הבאה בתור | **⚠ לא מוקצית מראש — ראו „הקצאת מספרים" להלן.** `0268`–`0270` **תפוסים** |
| סוויטת SQL הבאה בתור | **⚠ לא מוקצית מראש.** `p94`, `p95` **תפוסים** |
| ראש ledger בייצור | `0242` — **ורולאאוט של `0243`→`0267` מוכן וממתין** |
| החלטות בעלים | `#309`–`#318` — **כולן הוכרעו** 31.08.2026 |

### הקצאת מספרים — כלל, לא ערך

**‏`0268`, `0269`, `0270`, `p94` ו-`p95` תפוסים כולם** על ענפים אחרים
(`0268_countdown_and_expectations_flags`, ‏`0269_launch_offer_intents`,
‏`0270_launch_window_extension`, ‏`p94_launch_window_extension`, ‏`p95_launch_offer_intent`) — נמדד
ב-31.08.2026 בסריקת `git ls-tree` על כל ה-refs. המפרט והגרסה הראשונה של המסמך הזה הקצו בדיוק את
המספרים האלה, וזו **בדיוק מחלקת ההתנגשות** שהקומיטים `417775b9`, ‏`ba228fea` ו-`4d8904b1` ניקו.

**הכלל:** מספר מיגרציה וסוויטה **אינו נקבע במסמך תכנון.** הוא נקבע **בסריקה של כל ה-refs ברגע
המימוש**, מיד לפני כתיבת הקובץ. המסמך הזה מפנה למיגרציות **בשם לוגי** (‏`M-INGEST`, ‏`M-WA`,
‏`M-MAIL`, ‏`M-COLUMNS`, ‏`M-BILLING-LINK`, ‏`M-ENABLE`), לא במספר.

---

## Goal

לפתוח שני ערוצי קליטה שבהם מסמך נכנס למערכת **בלי שאדם העלה אותו** — מייל וּוואטסאפ — ולעשות זאת בלי
לייצר את מחלקת הכשל שהמוצר קיים כדי למנוע: מסמך של עסק אחד שנוחת אצל עסק אחר. במקביל, להעביר לשרת את
חוזה עמודות הדוח כדי שיוכל להפיק דוח לבד, לבנות את הייצוא המתוזמן, ולהשלים את חוליית השיוך החסרה
במעבד החיוב — **בלי להדליק גבייה.**

## Key decisions & tradeoffs

### הכרעות הבעלים (31.08.2026) — נכתבות ל-`OPEN-DECISIONS.md` כחלק מהמימוש

| # | ההכרעה | הערה |
|---|---|---|
| **#309** | **כתובת ייחודית לכל ארגון, קידומת אקראית לגמרי** (`k7m2x9q4@in.inplace.digital`). השיוך נגזר **רק** מהכתובת שאנחנו הקצינו. `From` **אינו** מפתח שיוך בשום מצב | נבחר על בסיס מחקר: Xero, Hubdoc ו-Dext — כל מי שמטפל בחשבונית שספק שולח — משתמשים בצורה הזו. המודל של Ramp/Expensify (כתובת אחת + שולח מאומת) **נשבר מבנית** אצלנו, כי ספק אחד משרת הרבה דיירים |
| **#310** | **חתימה נכשלה ⇒ נספר בלבד, אפס אחסון.** חתימה עברה ושיוך נכשל ⇒ **dead letter ברמת פלטפורמה, `org_id` null**, קריא ל-Platform Admin. **אין החזרה לשולח** | כמומלץ |
| **#311** | **`#241` נפתח — למדיה בלבד.** תמונה/PDF שהגיעו למספר של הדייר נקלטים כמסמך `inbox`. **טקסט אינו מפעיל אוטומציה ואינו נענה** | כמומלץ |
| **#312** | **כל שולח נקלט, ושולח שאינו ספק מוכר מסומן** „שולח לא מוכר" | כמומלץ |
| **#313** | **חלופה ב׳** — לא הקטלוג אלא **חוזה העמודות בלבד**, ‏Platform Admin בלבד. `exportTemplates.ts` **נשאר מקור התוויות**. שומר `check:report-columns` מפיל את השער על דריפט לשני הכיוונים | כמומלץ. **⚠ הצורה עודכנה בסבב 1:** לא הרחבה של הענף ב-`0126`, אלא **אובייקט גלובלי ומגורסה לפי `export_key`, נפרד מגרסאות התבנית של הדייר** — אלה אושרו ואינן ניתנות לשינוי, ודרישת עמודות עליהן הייתה שוברת אישור או עדכון של תבנית קיימת |
| **#314** | **⚠ מוותרים על תנאי (א) של `#259`.** הייצוא המתוזמן נבנה **בלי** מדידה מוקדמת של דוח איטי | **נגד ההמלצה.** ההמלצה הייתה לא לוותר; הבעלים הכריע לוותר במפורש. `#259` נסגר על מדידה, ולכן הפתיחה הזו היא **היפוך הכרעה** ונרשמת ככזו, לא כהרחבה |
| **#315** | **שניהם** — הדוח מופיע כארטיפקט במוצר **וגם** נשלח במייל | **נגד ההמלצה** (שהייתה: ארטיפקט בלבד). ההמלצה נשענה על כך שמסירה במייל חסומה — **והחסימה כבר לא קיימת** (ראו „תיקון 2" להלן), ולכן ההכרעה מבוססת |
| **#316** | **כן** — נבנה כותב יחיד ל-`provider_customer_id` | כמומלץ. **⚠ הצורה עודכנה בסבב 1:** המסלול הראשי הוא **„כוונת checkout"** — קישור שנכתב מתשובה מאומתת של הספק, לפני שאירוע יכול להתחרות. ‏`platform_link_billing_customer` נשאר **רק כשבירת-זכוכית מאומתת**, כי הקלדה ידנית שגויה קושרת לקוח אמיתי לארגון הלא-נכון — וכל אירוע חתום עתידי משנה מאז את הדייר הזה |
| **#317** | **(א) 30 יום** ל-dead letter של מייל לא-משויך, ואז מחיקה קשיחה. **(ב) המדיה נמחקת אצל Twilio מיד** אחרי קליטה מוצלחת, עם ראיה | כמומלץ. 30 יום גם תואם את תקופת השמירה של Resend עצמה |
| **חיוב** | **בונים עד הדלת, לא מדליקים.** אין חשבון Paddle מאומת. נבנה הכותב (בצורת „כוונת checkout"). **דף התשלום מותנה ב-sandbox** (ח-9) ואינו נכתב בלעדיו. ‏`M-ENABLE` **נכתבת ואינה נמזגת**, ‏**ולא ממזהי sandbox.** מפת המחירים **נשארת ריקה** | הבעלים ביקש להתקדם לגבייה ובחר במסלול הזה. **תלות חדשה בבעלים: פתיחת sandbox** |

### תיקונים למפרט — נמדדו ב-worktree הזה, גוברים על המפרט

**תיקון 1 — הקרקע אינה יציבה, וזה נמדד פעמיים באותה שיחה.** `main` לא זז מ-`417775b9`, אבל מספר
עצי העבודה הפעילים היה **18 בתחילת הסשן ו-22 בסופו**. **המספר אינו מצב — הוא חותם זמן**, ואין
להסתמך עליו: ‏preflight המימוש **חייב סריקה טרייה** של worktrees, ‏refs ועבודה לא-מקומטת, בדיוק
כפי שהקצאת המספרים דורשת. בעץ הראשי יש עבודה לא-מקומטת — כולל
`docs/ROLLOUT-0243-0267-20260831.md` ו-`scripts/rollout-apply.ps1`. **הרולאאוט טרם רץ** (הבעלים אישר),
אך הוא מוכן. אין לתכנן על בסיס „הריפו שקט".

**תיקון 2 — ⚠ `DEBT §25` ותיאור המפרט לגבי הדומיין שגויים.** המפרט מסמן את כל גל המייל כחסום כי
`inplace.digital` „לא נרכש ולא אומת". **המדידה אומרת אחרת.**
`artifacts/domain-cutover/EMAIL-ENABLEMENT-20260824.md` (24.08.2026) מתעד:

- דומיין **מאומת** ב-Resend (`e7f315d9…27c2`, `eu-west-1`)
- **DKIM, SPF ו-DMARC — שלושתם `verified` ו-`PASS`**
- **מסירה יוצאת חיה** — `delivered`, נקרא ב-Gmail „show original"
- Supabase Auth SMTP על `smtp.resend.com:465`, שולח `no-reply@inplace.digital`
- תקרת מיילים הועלתה **מ-2 ל-100 לשעה**

**המשמעות ל-PR-10:** הוא מתכווץ מ„לרכוש ולאמת דומיין" ל**„להוסיף רשומת MX לתת-דומיין אחד"**.

**ובמקומו — אילוץ חדש שהמפרט לא יכול היה להכיר.** קבלת דואר בשורש הדומיין **כבר תפוסה על ידי
Cloudflare Email Routing** (`enabled=true`/`status=ready`, catch-all מושבת), ושליחה יושבת על
`send.inplace.digital`. **אי אפשר להפנות את השורש ל-Resend Inbound** — שני מקבלי דואר על שם אחד
מתנגשים, ושתי רשומות SPF על אותו שם מבטלות זו את זו (הקובץ מתעד שזו בדיוק הסיבה שרשומות NameCheap
נמחקו). **הקליטה חייבת לשבת על תת-דומיין שלישי ונפרד** — `in.inplace.digital` — שזו גם ההמלצה
המפורשת בתיעוד של Resend. **הצורה שהמפרט הציע שורדת; מה שמשתנה הוא שהיא הפכה מהעדפה לחובה.**

**מה שנשאר פתוח מ-`§25`, ובהבחנה שסבב 1 חייב:** יש **שני סודות נפרדים, לא אחד.**
‏`RESEND_WEBHOOK_SECRET` שייך ל-`email-webhook` ה**יוצא** הקיים, ומצבו **חייב להימדד מחדש** (המסמך
מ-24.08 אומר שאינו קיים — ואותו מסמך מיושן, שכן הוא אומר גם ש-`email-webhook` אינו קיים והוא כן
קיים ב-`417775b9`). **‏`email-inbound` מקבל endpoint נפרד וסוד משלו**, מפני ש-Resend חותמת בסוד
ייחודי לכל endpoint. **אין שימוש חוזר בסוד היוצא לדלת הנכנסת**, ואין תלות בין מדידת האחד ליצירת
השני — שתי המשימות מקבילות.

**תיקון 3 — מפתח השיוך בוואטסאפ אינו „קיים אך הפוך".** המפרט (סעיף 2.2) מתאר את
`providerSenderId = from || to` (`whatsapp-webhook/core.ts:217`) כמפתח שיוך שצריך להפוך. **המדידה:**
ענף הנכנס מחזיר מוקדם ב-`core.ts:204` ו**אינו מחשב מפתח שיוך כלל**. העבודה זהה — לכתוב מפתח מ-`To` —
אבל הניסוח „הפוך" מטעה, וסעיף B של המפרט כבר מנסח את הדרישה נכון.

**תיקון 4 — מספור.** `#306` תפוס (`OPEN-DECISIONS.md:680`, שחרור `legal_hold`), הגבוה בקובץ הוא
`#308`. ההכרעות החדשות הן `#309`–`#317` כפי שהמפרט הציע, ‏**+`#318` שנפתחה ונענתה בסבב 1**.

**תיקון 5 — אתר כתיבה נוסף ל-`provider_customer_id`.** המפרט נוקב ב-`p54:80-83` בלבד; המדידה מוצאת
גם `p71_billing_provider_event_processing.sql:217`. שניהם בדיקות, ולכן המסקנה („אין כותב במוצר")
עומדת — אבל הסוויטה של PR-14 חייבת לדעת על שניהם.

### ממצאי מחקר — סיכונים שהמפרט לא נשא

**ס-1 — תקרת הקצב של Resend היא 10 בקשות בשנייה לכל החשבון, לא לכל דייר.**
כל שליפת צרופה עוברת שם, משותפת לכל הדיירים. המפרט מגדיר תקרה פר-ארגון ופר-כתובת במסד, ו**אין בו
backpressure מול התקרה המשותפת**. הצפה אצל דייר אחד מרעיבה את שליפת הצרופות של כל השאר.
**נדרש:** תור/ריטריי עם כיבוד `429`, ומדידה של הקצב בפועל לפני שפותחים ליותר מדייר אחד.

**ס-2 — פער גודל בוואטסאפ: Twilio מקבלת 16MB, אנחנו חוסמים ב-10MB.**
`file_size_limit = 10485760` נמדד ב-`0045`. **זו כבר לא הערכה — זו עובדה מדודה בשני הצדדים.**
חשבונית של 12MB תתקבל אצל הספק ותידחה אצלנו, והלקוח יראה „נשלח" בלי שהמסמך יופיע.
המפרט סימן את מחלקת הכשל הזו למייל (ט-1) ו**לא לוואטסאפ**. **נדרש:** החלטה מפורשת — לדחות עם הודעה
קריאה, או להעלות את התקרה. שתיקה כאן היא מסמך שנעלם.

**ס-3 — ⚠ חוסם: ייתכן ש-`received_for` ריק בדואר ישיר.**
התיעוד של Resend אומר שהשדה נגזר מסעיף `for` של כותרת `Received`, ובדוגמה הרשמית הוא **שונה**
מ-`to` (`to: [delivered@resend.dev]` מול `received_for: [forwarded@example.com]`). **לא אומת** שהוא
מאוכלס בדואר שנשלח **ישירות** אלינו. אם הוא ריק שם — כלל השיוך של המפרט אינו משייך דבר, ו**100%
מהמיילים נדחים**. **חובה לאמת מול payload חי לפני שנכתב קוד PR-8.** ההכרעה של `#309` על הצורה תקפה
בכל מקרה; מה שתלוי בבדיקה הוא המימוש.

**ס-4 — נורמליזציה של `whatsapp:`.** `From`/`To` נושאים תחילית `whatsapp:<E.164>`. המפרט אינו קובע
באיזו צורה `whatsapp_connections.provider_sender_id` מאוחסן. **חייב להיקבע במפורש ולהיאכף בשני
הכיוונים**, אחרת השיוך נכשל בשקט.

**ס-5 — שדות Twilio שהמפרט לא הכיר:** `ProfileName`, `WaId`, `Forwarded`, `FrequentlyForwarded`.
`Forwarded` רלוונטי ישירות — חשבונית שספק **העביר** הלאה היא מקרה אמיתי.

### תשובות שנסגרו במחקר (ט-1..ט-4 של המפרט)

| # | מצב |
|---|---|
| ט-1 | **חלקי.** שמירה: **30 יום** (Free/Pro/Scale). **גודל מייל וגודל צרופה — אינם מתועדים.** לא נוחשו. נותרו פתוחים ל-PR-8 |
| ט-2 | **נסגר לטובה.** Svix עם `svix-id`/`svix-timestamp`/`svix-signature`, **וסוד ייחודי לכל endpoint**. החתימה היא תכונת ה-endpoint ולא של סוג האירוע ⇒ `email-webhook/core.ts` נצרך **כמות שהוא**. לאמת על ה-payload החי הראשון |
| ט-3 | **נסגר לטובה.** endpoint נרשם עם תת-קבוצה נבחרת של סוגי אירוע; `email.received` ניתן לבחירה. יחד עם הסוד הפר-endpoint ⇒ **פונקציית `email-inbound` נפרדת עם סוד משלה היא הצורה הנכונה** |
| ט-4 | **נסגר.** `NumMedia`/`MediaUrl{N}`/`MediaContentType{N}` אושרו, ‏+ ס-4 ו-ס-5 לעיל |

## תיקוני סבב 1 — ממצאי Codex שהתקבלו

הביקורת החזירה `REVISE` עם 18 ממצאים. **ארבעה נבדקו ידנית וכולם אושרו**, ‏17 התקבלו במלואם ואחד
(‏#15) התקבל בשינוי. הפירוט המלא והדיספוזיציות ב-`PLAN-REVIEW-LOG.md`. השישה שמשנים את **הצורה**:

**ת-1 — הקצאת המספרים.** ראו למעלה.

**ת-2 — ⚠ הדלת לשרת גדולה בהרבה ממה שהמפרט תיאר, וזה השינוי המרכזי.**
המפרט מציע לשחרר שתי נעילות. **המדידה מראה שזה לא מספיק:** ‏`document_scan_jobs.requested_by` הוא
‏`not null` **בעצמו** (`0136:100`, עם FK), והתובע ב-`0246:480` מצמיד
`and j.requested_by = d.uploaded_by`. כלומר תמונה **אינה יכולה להיכנס לסריקה** ומסמך אחר **נתקע
אחרי החילוץ**. ‏**מסקנה: לא „לשחרר נעילות" אלא לבנות חוזה שחקן-מכונה מהמעלה הראשונה** —
‏`documents`, ‏`document_scan_jobs`, ‏`document_processing_jobs`, התובעים, השחזור, העיבוד החוזר,
המכסות והיומן — **ולהוכיח מסע מלא של PDF ושל תמונה** בלי להמציא אדם.

**ת-3 — ⚠ `ssrf.ts` אינו יכול להוריד קובץ.** `ssrf.ts:490-491` מצהיר: „‏The body is intentionally
absent", והמימוש מחזיר `new Response(null, …)`. **המיטיגציה המרכזית של המפרט ל-SSRF פשוט לא עובדת
למקרה הזה.** ‏„אפס יבואנים" היה צריך להיות הרמז. **נדרש `guardedDownload` חדש** — זרימה חסומה עם
תקרת בתים, timeout, ‏hosts מדויקים, ‏redirect ידני, וזיהוי MIME ברמת הבתים — **או** שחזור כתובת
הספק מזהים חתומים (`AccountSid`/`MessageSid`/`MediaSid`) במקום להשתמש ב-URL שהגיע ב-payload.

**ת-4 — ⚠ כלל סדר-החתימה אינו ניתן ליישום מילולי ב-Twilio.** ‏HMAC של Twilio מחושב מעל **פרמטרים
מפורסרים** ודורש לבחור credential מועמד מ-`To` שאינו מהימן — ולכן `whatsapp-webhook/index.ts:52-72`
**בהכרח** מפרסר ומחפש לפני האימות, עם נימוק כתוב. **הכלל „raw → verify → parse" נשאר נכון ל-Resend
ול-Paddle ואינו נכון ל-Twilio.** הניסוח מוחלף בסדר **פר-ספק**: לשמור raw · לפרסר בצורה חסרת-אובדן רק
את מה שחתום ואת מפתח המועמד · חיפוש credential **נטול תופעות לוואי** · לאמת · ורק אז פרסור עסקי
וכתיבה.

**ת-5 — שיוך אינו נגזר מ-`org_id` שמגיע כארגומנט.** ‏`service_role` הוא קורא בעל הרשאה גלובלית,
ולכן פונקציה שמקבלת `org_id`, נתיב ו-MIME ערומים אינה מאמתת שיוך — היא **סומכת** עליו.
‏**‏RPC צורכת „תביעת קליטה" נעולה** (שורת אירוע/צרופה), **גוזרת `org_id` מרשומת הניתוב במסד**,
ומאמתת מחדש bucket, ‏object id, ‏גרסה/eTag, ‏MIME ונתיב `{org_id}/` **באטומיות**.
בנוסף: **תביעה לפני כל I/O רשת** עם lease/attempt, זהות ייחודית של הודעה+צרופה, נתיב דטרמיניסטי
ו-`upsert:false`, וסיום אטומי או ניקוי יתום.

**ת-6 — היקף ארגוני.** מסמך נכנס בלי מעלה **אין לו מקור scope**, ו-`unit_id` ריק הופך אותו
לכלל-ארגוני — כלומר גלוי בין ישויות משפטיות אחיות. **כל כתובת/חיבור נכנס נקשר ל-scope קליטה מפורש**,
‏`unit_id` נגזר בשרת, ובדיקת סירוב בין אחים היא תנאי קבלה.

**שאר הממצאים שהתקבלו** — מגבילי קצב שרצים אחרי ההורדה ועוקפים את מכסות המנוי · הגברת-כתיבה
בלתי-מאומתת על כל חתימה פסולה · בקרות הכתובת חסרות אכיפה ב-SQL ו**סתירה פנימית** (סוד שאסור בלוג
מול כתיבת הכתובת הגולמית ל-`audit_logs`) · ‏UI מקור-המסמך **אינו ניתן למימוש** מטבלה ב-`private` ·
מוכנות Twilio ומחיקה שאינה עמידה לכשל · חוזה העמודות מול **גרסאות תבנית שאושרו ואינן ניתנות
לשינוי** · קישור חיוב ידני כחור שיוך · דגלים שאינם עוצרים קליטה · rollback שאינו מנקז עבודה בתנועה ·
נראוּת תפעולית שאין לה משטח. **ס-5 אינו סיכון אלא רשימת שדות** — הוסב לניתוח (גבולות PII, מה בשליטת
תוקף, סמנטיקת העברה).

## Approach

הסדר של המפרט (סעיף 7) נשמר, עם השינויים שההכרעות והביקורת חייבו.

0. **תנאי-קדם.** ‏**חוסמים את גל 0:** ‏(א) ‏preflight טרי — סריקת worktrees/refs/עבודה לא-מקומטת
   והקצאת מספרים · ‏(ב) **`guardedDownload`** (ת-3) · ‏(ג) **רישום `#309`–`#318`
   ב-`OPEN-DECISIONS.md`.** הרישום הקנוני מסתיים היום ב-`#308`, ושתיים מההכרעות **הופכות** את `#241`
   ואת `#259` — כלומר עד שהן נכתבות, הרישום הקנוני עדיין אומר „לא", ואסור שקוד מוצר יקדים אותו.
   ‏**‏ס-3 אינו חוסם את גל 0** — הוא נוגע לשיוך מייל בלבד וחוסם את `M-MAIL` בלבד; חסימת עבודה
   אוניברסלית על הוכחה שהיא ספציפית למייל הייתה שרשור מלאכותי.
   ‏**⚠ והסדר של ס-3 עצמו היה מעגלי (סבב 3):** אימות `received_for` חי דורש MX, ‏endpoint וסוד —
   וכולם היו ב-PR-10, **אחרי** `M-MAIL`. הפתרון: **גשוש מקדים, נפרד מהרולאאוט** — תת-דומיין קליטה
   מנוהל של Resend (`<id>.resend.app`, מתועד) או endpoint אבחון זמני בחוזה הסופי, **בלי לגעת
   ב-DNS של `inplace.digital`.** הגשוש שומר **רק** את נוכחות/היעדר השדה וספירת הכתובות בו —
   ‏**אפס תוכן מייל, אפס כתובות אמיתיות.** הפעלת ה-MX הסופית נשארת ברולאאוט.
1. **גל 0 — חוזה שחקן-מכונה, לא „שחרור נעילות".** ‏`M-INGEST` לפי ת-2 ות-5: הדלת, התביעה הנעולה,
   גזירת `org_id` מהמסד, ‏scope לפי ת-6, ‏ledger וסופר דחיות מצטבר. ‏→ ‏`_shared/inbound-intake.ts`
   בלי צרכנים. **תנאי יציאה: מסע מלא של PDF ושל תמונה, מקצה לקצה, בלי אדם.**
2. **PR-3 — רשת הביטחון, במקביל ולפני הכול.** שלושת `core.test.ts` נכנסים לשער המקומי ול-CI,
   ושני ה-webhooks החסרים נכנסים ל-`$functionJwt`. אינו תלוי בכלום.
3. **גל 1 — וואטסאפ.** `M-WA` ‏→ ענף הנכנס ‏→ ‏UI. כולל **ס-2** (הכרעת פער הגודל) ו**ס-4**
   (נורמליזציית התחילית).
4. **גל 2 — מייל.** **תחילה ס-3** — אימות `received_for` מול payload חי; בלעדיו PR-8 אינו מתחיל.
   ‏`M-MAIL` ‏→ ‏`email-inbound` ‏→ ‏UI. ‏PR-10 מצטמצם ל-MX על `in.inplace.digital` בלבד,
   **בלי לגעת בשתי מערכות הדואר הקיימות**, ‏+ מדידת מצב `RESEND_WEBHOOK_SECRET`.
5. **גל 3 — דוחות.** `M-COLUMNS` — **חוזה עמודות גלובלי ומגורסה לפי `export_key`, נפרד מגרסאות
   התבנית של הדייר** שאושרו ואינן ניתנות לשינוי (`0126`), ומוצמד למחוללי השורות בשרת בפועל כולל
   סמנטיקת מטבע — לא רק ל-`EXPORT_DEFINITIONS`. ‏→ ‏`check:report-columns`.
   ‏**‏PR-13 (הייצוא המתוזמן) פתוח** — `#318` הוכרעה וחוזה התזמון כתוב שם במלואו.
6. **גל 4 — חיוב.** ‏`M-BILLING-LINK`, **בצורת „כוונת checkout" ולא קישור ידני:** כוונה חד-פעמית
   קשורה ל-`org_id`, והקישור נכתב **מתשובה מאומתת של הספק** לפני שאירוע יכול להתחרות. קישור ידני
   נשאר **רק כשבירת-זכוכית מאומתת** עם מסלול מבוקר לשיוך-מחדש של dead letters.
   ‏→ ‏checkout ‏→ ‏`M-ENABLE` **נכתבת, נסקרת, ואינה נמזגת.**

## Toolchain

**אין.** סריקת המלאי מצאה התאמות דומיין (`resend-automation`, `twilio-communications`,
`supabase-automation`, `postgres-best-practices` בצד Claude; ‏`postgres-best-practices`, `postgresql`
בצד Codex), והבעלים הכריע **לא לטעון אף אחד מהם**: הריפו קובע דפוסים משלו שגוברים על עצה גנרית,
וסקיל אוטומציה כללי הוא מקור לקוד שאינו תואם את החוקה.

## Assumptions

מהלדג'ר שאושר (14 סעיפים, אחת-אחת). המקורות בסוגריים נמדדו ב-worktree הזה ב-`417775b9`.

1. base commit זהה למפרט — `git rev-parse HEAD`.
2. שמונה מטענות המפרט הכבדות נבדקו עצמאית וכולן עמדו.
3. לא נכתב קוד מוצר עד כה.
4. מעבד החיוב קיים במלואו; חסר **כותב** ל-`provider_customer_id` (`grep` על `supabase/`+`src/`).
5. שלושת `core.test.ts` של ה-webhooks **אינם רצים באף שער** (`grep` ב-`scripts/` ו-`.github/`).
6. `$functionJwt` מכילה 12 שמות, בלי `email-webhook` ובלי `whatsapp-webhook` (`:1134-1147`).
7. מספור `#309`–`#317` (הבעלים ויתר על ההכרעה; נקבע על בסיס `OPEN-DECISIONS.md`).
8. ייצור על `0242`, העץ על `0267`, **והרולאאוט טרם רץ** (אישור הבעלים).
9. **בוטלה ותוקנה** — ראו „תיקון 2".
10. **בוטלה ותוקנה** — ראו „תיקון 3".
11. `MAX_ROUNDS=5` לביקורת Codex.
12. אין טעינת סקילים חיצוניים.
13. הפיצול ל-PRs מאושר.
14. **בוטלה** — הבעלים בחר להתקדם לגבייה, במסלול „עד הדלת".

## Risks / open questions

- **ת-2 הוא הסיכון הגדול בתוכנית** — חוזה שחקן-מכונה חוצה שבעה משטחים. אינו מתפצל, ואינו „שחרור
  שתי נעילות" כפי שהמפרט תיאר.
- **ת-3 חוסם את שני ערוצי הקליטה** — אין היום קוד שיכול להוריד קובץ חיצוני בבטחה.
- **ס-3 חוסם את גל 2** עד לאימות מול payload חי.
- **‏Paddle sandbox חוסם את checkout** — החסם היחיד שנשאר בידי הבעלים.
- **מגבלי קצב חייבים לרוץ לפני ההורדה**, לא בזמן יצירת המסמך, ולהשתמש במוני המכסה הקיימים
  (`documents.monthly`, ‏`ocr_pages.monthly`) במקום במונה חדש שעוקף אותם.
- **חתימה פסולה לא תייצר שורה** — מדד מצטבר או דגום, אחרת הדלת הפתוחה היא הגברת-כתיבה.
- **גרסאות תבנית שאושרו אינן ניתנות לשינוי** — חוזה העמודות חייב להיות אובייקט נפרד ומגורסה, אחרת
  הוא שובר אישור או עדכון של תבנית קיימת.
- **ט-1 חלקי** — גודל מייל/צרופה מרביים אינם מתועדים. חוסם את גבולות הקליטה ב-PR-8.
- **ס-1** — תקרת 10/שנייה משותפת. נדרשת הכרעת עיצוב לפני יותר מדייר אחד.
- **ס-2** — פער 16MB/10MB בוואטסאפ. נדרשת הכרעה מפורשת.
- **מצב `RESEND_WEBHOOK_SECRET`** לא נמדד. אין לטעון לגביו לשום כיוון.
- **PR-13 נכנס להיקף בלי המדידה ש-`#259` דרש** — סיכון מוצרי מודע, בהכרעת הבעלים (`#314`).
- **checkout חסום ב-sandbox, ואינו נכתב בלעדיו.** הניסוח „ניתן לכתוב, לא ניתן לאמת" נמחק בסבב 2 —
  הוא היה סתירה: בלי sandbox אין מזהי מחיר, אין גרסת API ואין חוזה redirect, ולכן אין **צורה** לכתוב
  אליה, לא רק חוסר אימות. עם sandbox — נכתב **ונאמת** לפי מטריצת ח-9.
- **`0243`–`0267` לא הוחלו על שום מסד.** מודדים `max(version)` חי לפני כל החלה.
- **PR שהבסיס שלו אינו `main` מקבל אפס בדיקות** (`DEBT §65`). השרשרת כאן היא בדיוק המבנה הזה.

## חוזים מחייבים — סבב 2

סבב 1 קיבל שבעה ממצאים **בפרוזה בלבד**, וסבב 2 צדק שפרוזה אינה חוזה: המפרט בן 982 השורות עדיין
נושא את הגרסאות הלא-בטוחות. הסעיף הזה הופך אותם לדרישות. **במקום סתירה, הסעיף הזה גובר על המפרט.**

### ח-1 — `M-INGEST`: חוזה שחקן-המכונה

**מקור.** ‏`documents` ושתי טבלאות העבודה מקבלות `source` מתוך enum סגור
(`browser` · `email` · `whatsapp`), ‏**`not null default 'browser'`, ‏immutable אחרי היצירה.**

**⚠ תוקן בסבב 3 — הניסוח הקודם („בלי ברירת מחדל") היה שגוי והיה מפיל את מסלול הדפדפן.**
שלושה בנאים חיים מכניסים שורות **בלי** העמודה: ‏`0131:139`, ‏`0140:495` (‏`documents`) ו-`0169:234`
(`document_processing_jobs`). ‏`not null` בלי default היה שובר את שלושתם — כלומר **מפר את כלל
הברזל שלפיו מסלול הדפדפן אינו משתנה בשום תו.**

**ולמה ברירת מחדל בטוחה כאן, למרות שבדרך כלל היא מסוכנת:** ‏ה-CHECK הדו-כיווני שלהלן הופך שורת
מכונה שקיבלה `'browser'` בשקט ל**שורה שנדחית** — כי אין לה שחקן. ‏„ברירת מחדל שקטה" אינה יכולה
להסוות מקור, מפני שהיא מתנגשת עם היעדר השחקן.

**‏⚠ ותנאי preflight חוסם שהתגלה בסבב 4 — ה-backfill אינו בטוח מאליו.**
‏`documents.uploaded_by` הוא **nullable מאז `0001:368`** ומעולם לא היה `not null`. לכן ייתכנו
בייצור מסמכים היסטוריים **בלי מעלה**, ו-backfill שמסמן את כולם `'browser'` ואז אוכף
`browser ⇒ actor is not null` **יפיל את המיגרציה בייצור.**
**חובה לספור לפני:** `select count(*) from documents where uploaded_by is null`. **תוצאה שאינה
אפס עוצרת** ודורשת הכרעה מפורשת על מקור היסטורי (ערך `legacy` נפרד, או החרגת שורות שקדמו
למיגרציה מה-CHECK). **לעולם לא להמציא שחקן** ולייחס מסמך ישן לבעלים.

**‏backfill היסטורי ל-`'browser'` נדרש** (בכפוף ל-preflight), והנתיב
הנכנס **מציין `source` מפורשות תמיד** ולעולם אינו נשען על ברירת המחדל.

**שחקן.** ‏`requested_by`/`uploaded_by` הופכים ל-nullable **בשתי** טבלאות העבודה
(`document_processing_jobs`, ‏`document_scan_jobs`) תחת CHECK דו-כיווני:
`source = 'browser'` ⇒ ‏`actor is not null`; ‏`source <> 'browser'` ⇒ ‏`actor is null`.
**אין ייחוס עבודה ל-`owner` של הארגון** — אדם שלא ביקש הוא שקר ביומן.

**התובעים.** ‏`0246:480` (`and j.requested_by = d.uploaded_by`) ו**כל** predicate אחר שמצמיד שחקן
מוחלף בהחלפה מעוגנת — **לא בהצהרה מחדש מהמיגרציה היוצרת.** התנאי החדש: התאמת `source` ו-`org_id`,
ושוויון שחקן **רק** כאשר `source = 'browser'`. **חובה למפות את כל אתרי ההצמדה לפני הכתיבה** —
‏`0136` ו-`0246` הם השניים הידועים, **ולא הוכח שהם היחידים.**

**‏scope.** כל כתובת/חיבור נכנס נושא `intake_unit_id` **`not null`**. ‏`documents.unit_id` נגזר ממנו
בשרת. ‏`unit_id` ריק במסמך נכנס **אסור** — הוא הופך אותו לכלל-ארגוני וחושף אותו בין ישויות אחיות.

**עקביות מקור בין מסמך לעבודה.** שלוש הטבלאות נושאות `source` עצמאי, ולכן **נדרשת שוויוניות
מבנית** — ‏FK מורכב מ-`(org_id, document_id, source)` של כל טבלת עבודה אל `documents`. בלעדיו עבודה
יכולה לטעון `source='email'` על מסמך דפדפן ולעקוף בכך את דרישת השחקן. מבחן מוטציה מוכיח ששורה עם
מקור שאינו תואם **אינה ניתנת לאחסון**.

**התביעה.** ‏`M-INGEST` מגדיר `private.inbound_intake_claims` עם
`(provider, provider_message_id, attachment_id)` **unique**, ‏`state ∈ {claimed, fetching, stored,
ingested, failed, abandoned}`, ‏`lease_until`, ‏`attempt`. **התביעה נכתבת לפני כל I/O רשת.**

**התביעה קשורה מבנית לשיוך — דרך הורה ניתוב אחד.**
‏**⚠ תוקן בסבב 4:** ‏FK ב-PostgreSQL **אינו יכול** להצביע „או על שורת כתובת או על שורת חיבור",
והקפאת `org_id`/`unit`/`source` **לצד** מזהה ניתוב אינה מוכיחה שהם תואמים לאותו ניתוב.

לכן נדרש **הורה קנוני אחד — `private.inbound_routes`** — עם זהות מורכבת
`(route_id, org_id, intake_unit_id, source)`. ‏`org_inbound_addresses` ו-`whatsapp_connections`
מצביעים אליו, והתביעה נושאת **FK מורכב אל ארבעת השדות יחד** — כך שהתאמה היא **מבנית**, לא הבטחה.
השדות **אינם ניתנים לשינוי אחרי יצירת התביעה** (טריגר), ו**שורות ניתוב מבוטלות נשמרות**
(‏`revoked_at`, לא מחיקה) כדי שתביעה היסטורית תמשיך להיפתר.

**‏Lease עם אסימון גידור.** ‏`lease_until` ו-`attempt` לבדם אינם סמכות: עובד מניסיון 1 שפג יכול
לסיים אחרי שניסיון 2 תפס את התביעה. התביעה נושאת **`lease_token` אטום**, וה-RPC **מקבל אותו
ומאמת אותו** תחת נעילת שורה לפני כל מעבר מצב.

**חתימת ה-RPC.**
‏`service_ingest_inbound_document(p_claim_id uuid, p_lease_token uuid, p_object_id uuid, p_object_version text)`
— **ותו לא.** ‏`org_id`, ‏`unit_id`, הנתיב ו-`source` **נגזרים מהתביעה ומרשומת הניתוב**, לא מהקורא.
הפונקציה נועלת את שורת התביעה, מאמתת `lease_token` ו-`state` חיים, ואז מאמתת מחדש באטומיות: הדלי,
קיום האובייקט, התאמת גרסה/eTag, ‏MIME מול `smart_document_mime_allowed`, ותחילית `{org_id}/`.
‏`auth.role() <> 'service_role'` ⇒ ‏`42501`.

**מכסות — מחזור חיים מלא, בשלושה שלבים.** **פנקס הזמנות מפתוח-תביעה** עם
`reserve` → `commit` → `release`. ‏`documents.monthly` נשמר כיחידה אחת.

‏`ocr_pages.monthly` הוא המורכב, **ותוקן בסבב 4:** הזמנת עמוד אחד והתאמה **אחרי** החילוץ הייתה
מאפשרת ל-PDF גדול **לצרוך OCR שכבר שולם עליו** בזמן שלדייר אין קיבולת. הסדר הנכון:
‏(1) הזמנה זמנית של עמוד אחד לפני השליפה · ‏(2) **אחרי ההורדה החסומה ולפני ה-OCR — קביעת מספר
העמודות והזמנת היתרה באטומיות** · ‏(3) קיבולת שאינה מספקת ⇒ **דחייה או השהיה לפני החילוץ**, לא
אחריו · ‏(4) `commit` מול הספירה האמיתית בדפוס `0169:231`.
‏**שחרור מלא** בכשל שליפה, טיפוס או גודל. מבחנים: כשל, ‏replay, פקיעה, ותביעות מקבילות.

**מבחני שלילה חובה:** תביעה של דייר א׳ עם אובייקט של דייר ב׳ ⇒ סירוב · אובייקט שהוחלף בין ההורדה
לקליטה (TOCTOU) ⇒ סירוב · ‏replay של אותה תביעה ⇒ אפס מסמכים נוספים · ‏`unit_id` של אח ⇒ סירוב ·
מסמך `source='email'` עם שחקן ⇒ סירוב.

**תנאי יציאה מגל 0:** מסע מלא **של PDF ושל תמונה**, מקצה לקצה, בלי אדם — כולל סריקה, חילוץ, שחזור
ועיבוד חוזר. ‏`0136`/`0246` מוכיחים שבלי זה תמונה אינה נכנסת לסריקה ומסמך אחר נתקע.

### ח-2 — קצב, מכסות ומניעת הצפה

תקרה **לפני** ההורדה, לא בזמן יצירת המסמך: תקרת גוף ובקשה ב-Edge · **token bucket משותף** ל-Resend
(ס-1) · **הזמנת מכסה פר-דייר לפני שליפת המדיה**, מול המונים הקיימים `documents.monthly`
ו-`ocr_pages.monthly` — **לא מונה חדש שעוקף אותם.** חתימה פסולה **אינה כותבת שורה**: מדד מצטבר או
דגום בלבד, אחרת הדלת הפתוחה היא הגברת-כתיבה בלתי-מאומתת.

### ח-3 — הכתובת הנכנסת

ה-local-part **נוצר בשרת בלבד** ולעולם לא מתקבל כארגומנט · הפורמט והאורך נאכפים ב-CHECK ב-SQL, לא
בהערה · ‏RLS ל-owner בלבד, **בלי DML ישיר** · ‏**ל-`audit_logs` נכתבים מזהה הכתובת וטביעת אצבע
וערך ממוסך — לעולם לא הכתובת המלאה.** זו הסרת הסתירה של המפרט, שדרש גם „סוד שלא בלוג" וגם כתיבת
הכתובת הגולמית ליומן.

### ח-4 — מקור המסמך במסך

‏`private.inbound_intake_events` **אינה נגישה לדפדפן**, ולכן היא לבדה אינה יכולה להזין את
‏`DocumentsInbox.tsx`. נדרשת **הטלה מסוננת ב-`public` עם `org_id` ו-RLS**, הנושאת ערוץ, שולח ממוסך,
פסק „ספק מוכר/לא מוכר", ‏**דגלי `forwarded`/`frequently_forwarded`** (ס-5) ומזהה מסמך — **בלי
payload גולמי של הספק.** בלעדיה ההבטחה „הגיע במייל מ-…" אינה ניתנת למימוש.

**⚠ ותוספת סבב 3 — כשל סופי אינו יוצר מסמך, ולכן אינו נראה בהטלה הזו.** קובץ שנדחה על גודל (ס-2)
או על טיפוס **אין לו `document_id`**, ולכן הטלה שמפתחה הוא מסמך אינה יכולה להציג אותו — וההבטחה
„הלקוח יקבל הודעה קריאה" נשברת בשקט. ‏`ח-8` נותן נראוּת ל-Platform Admin, **לא לדייר.**
**נדרשת הטלת דחיות נפרדת, org-scoped עם RLS, שמפתחה הוא התביעה ולא המסמך** — או נתיב התראה שקול —
לכל כשל קליטה סופי.

### ח-5 — Twilio: מוכנוּת ומחיקה

הפעלה חסומה בהוכחת חשבון/sandbox אמיתי · נדרשת התאמת `AccountSid` ושולח מול חיבור `active` ·
**מחיקת המדיה אצל הספק נכנסת ל-outbox אידמפוטנטי עם retry, dead-letter והתראה.** מחיקה שנכשלת
בשקט משאירה חשבונית של לקוח אצל Twilio ללא הגבלת זמן, ו-`#317` הופך להבטחה שלא נשמרה.

### ח-6 — מה שבאמת עוצר קליטה

הדגלים הם UI בלבד ואינם עוצרים שרת. **‏`revoked_at`/`inbound_enabled` הם בשליטת הדייר ולכן אינם
kill switch של הפלטפורמה.** נדרש **גבול בשליטת פלטפורמה, שנבדק לפני חיפוש credential ולפני שליפת
מדיה, ושאין לו כותב דייר** — בדפוס `billing_provider_boundary`. הרשאות RLS נשארות בלתי-תלויות בדגלים.

### ח-7 — Rollback שמנקז

`cron.unschedule` אינו מבטל ייצוא רץ, מסירה ב-outbox, מייל שיצא או ארטיפקט שנוצר. לכל משטח נדרש
**מצב מפצה**: לעצור תביעות חדשות · לנקז או לבטל עבודה בתנועה · **לשמור קישורי חיוב append-only**
(ניקוי `provider_customer_id` הורס היסטוריית שיוך ומנתק אירועים שכבר שויכו) · לספור ארטיפקטים
ומסירות · ולמחוק **רק** יתומי אחסון שהוכחו כלא-רשומים.

### ח-8 — נראוּת לפני הפעלה

הטלות חסומות ל-Platform Admin לקריאת dead letters · ‏correlation id לאורך התביעה · והתראות על
**גיל התור · `429` משותפים · דחיות גודל/טיפוס · עבודות מכונה תקועות · יתומים · פיגור במחיקה אצל
Twilio.** ‏**אין הפעלה בלי אלה** — dead letter שאיש אינו רואה אינו ledger.

### ח-9 — Paddle: גידור sandbox מול live

מצב הספק הוא **חוזה נאכף בשרת**, לא הגדרה: סודות, ‏client token ומרחב מזהי מחיר **נפרדים לחלוטין** ·
**מזהה sandbox שנכנס לייצור נדחה** · ‏`M-ENABLE` **לא ייכתב ממזהי sandbox** ויחכה למזהים חיים
שהוכחו בנפרד · ומטריצת קבלה מלאה ב-sandbox: checkout, ‏webhook, ‏replay, ‏refund, ‏reconciliation ·
‏**+ הוכחה מבנית ש-`#295` נשמר: מטבע החיוב נגזר ממדינת חיוב מאומתת ולא משפת הממשק.**

### ח-10 — סגירת ס-1..ס-5

| | ההכרעה המחייבת | מה חוסמת |
|---|---|---|
| **ס-1** | ‏token bucket משותף + כיבוד `429` + הזמנה פר-דייר לפני שליפה (ח-2). **מדידת קצב בפועל לפני הפעלה ליותר מדייר אחד** | הפעלת מייל לרב-דיירים |
| **ס-2** | **דחייה מפורשת עם הודעה קריאה לדייר** על קובץ מעל 10MB, ‏+ שורת ledger עם `reason_code`. **אין העלאה שקטה של התקרה** — היא נוגעת בכל מסלולי המסמכים | מימוש וואטסאפ |
| **ס-4** | הצורה הקנונית של `provider_sender_id` נקבעת **מפורשות**, נורמליזציה בכניסה ובכתיבה, ומבחן שמוכיח ש-`whatsapp:+972…` ו-`+972…` **אינם** יוצרים שתי שורות | מימוש וואטסאפ |
| **ס-5** | ‏`ProfileName` הוא **PII של אדם** ואינו נשמר · `WaId` ממוסך כמו המספר · **`Forwarded`/`FrequentlyForwarded` נשמרים כדגל ומוצגים** — חשבונית שהועברה היא ראיה חלשה יותר ואסור שתיראה זהה לישירה | מימוש וואטסאפ |

## הכרעות ותלויות שנפתחו במהלך הביקורת

### ✅ `#318` — הוכרעה 31.08.2026

| | ההכרעה |
|---|---|
| **מי מקבל** | **בעלים ורואה-חשבון בלבד, ורק לכתובת החשבון המאומתת שלהם.** ‏**אין הקלדת כתובת חופשית** — היא הייתה הופכת שגיאת הקלדה למשלוח ספרי העסק לזר |
| **מה המייל מכיל** | **הקובץ עצמו, מצורף.** ‏*נגד ההמלצה* (שהייתה: קישור שבודק הרשאה בלחיצה) |
| **הפסקה** | **המשלוח נעצר מיד** עם ביטול גישה או סיום מנוי, **והבעלים מקבל התראה** שהמשלוח הופסק |

**⚠ תוצאה מחייבת שנובעת מצירוף שתי הבחירות, ונרשמת כדי שלא תהיה הפתעה:** ‏„עוצרים מיד" עוצר
משלוחים **עתידיים** בלבד. **קובץ שכבר יצא יושב בתיבה של מי שעזב, ואין דרך לשלוף אותו בחזרה** —
ביטול גישה אינו מוחק דואר. זהו בדיוק ההפרש שבין צרופה לקישור. ההכרעה עומדת; המגבלה מתועדת,
ו**חייבת להיאמר במסך** שמפעיל את המשלוח, כדי שהבעלים ידע מה הוא מדליק.

**⚠ נגזרות — תוקנו בסבב 4 מול הסכימה בפועל.**

**כתובת הדואר אינה בפרופיל.** ‏`profiles` נושאת `id`/`org_id`/`full_name`/`role`/`phone`/`active`
בלבד (`0001:31-38`) — **אין בה עמודת email.** הכתובת יושבת ב-`auth.users`. לכן: **מאוחסן
`profile_id` בלבד**, וכתובת היעד ומצב האימות שלה **נפתרים בשרת ברגע השליחה** מ-`auth.users`,
לעולם לא מהדפדפן ולא מתוך הגדרת התזמון. **‏⚠ ואין להשתמש ב-`profiles.backup_email` (`0255`)** —
היא כתובת שחזור לבעלים, לא כתובת ההתחברות המאומתת.

**תפקיד אינו היקף.** רואה-חשבון עשוי להיות מורשה לישות משפטית אחת בלבד. בדיקת `role='accountant'`
לבדה הייתה שולחת את ספרי ישות א׳ לרואה-חשבון שמורשה רק לישות ב׳. **בכל שליחה נבדקים חמישה יחד:**
פרופיל `active` · תפקיד זכאי · **היקף בפועל לישות המשפטית של הדוח הזה** · זכאות מנוי בתוקף ·
וכתובת Auth מאומתת. **כשל בהיקף עוצר את המשלוח ומתריע לבעלים.**

**‏⚠ ואיך בודקים היקף בעבודה מתוזמנת — תוקן בסבב 5, וזו הייתה טעות מסוכנת.**
‏`auth_scopes()` היא **חסרת ארגומנטים** ונשענת על `auth.uid()` ו-`auth_org()` (`0054:321-328`).
לעובד cron אין JWT, ולכן היא מחזירה `'{}'` — **ו-`assert_unit_in_scope` אף מקדימה יציאה ומתייחסת
ל„נושא JWT חסר" כאל עבודת שירות מהימנה** (`0054:334-336`). כלומר הבדיקה שכתבתי לא רק שלא הייתה
מגינה — היא **הייתה עוברת בגלל שזו עבודת שרת.**
**הנכון:** פונקציה **שרת-בלבד** `private.scopes_for_user(p_profile_id, p_org_id)` מעל
`user_scope_closure` (`0054:224`, ‏PK ‏`(user_id, org_id)`) — **ארגומנט מפורש, בלי התחזות ל-JWT** —
ולצדה בדיקות `active`/תפקיד. **תנאי קבלה: הוכחת סירוב בין ישויות אחיות**, כלומר נמען המורשה
לישות ב׳ אינו מקבל דוח של ישות א׳.

**חוזה התזמון — ברירות מחדל מוצהרות** (בדפוס `OPEN-DECISIONS`: ברירת מחדל מתועדת, לא ניחוש שקט):
מי מגדיר — **`owner` עם step-up וסיבה** · הדוח — אחד משלושת ה-`export_key` הקיימים בלבד, **בלי
דוח חדש** · היקף — **ישות משפטית אחת מפורשת** לכל תזמון · נמענים — **רשימת `profile_id` מפורשת**,
לא „כל מי שזכאי" · תדירות — **חודשי** · אזור זמן — **Asia/Jerusalem** · אידמפוטנטיות מסירה —
מפתח `(schedule_id, period)` כך ש-retry אינו שולח פעמיים · תקרת צרופה — נבדקת מול תקרת המייל
לפני השליחה, וכשל חורג נופל למסירה בקישור · שמירת ארטיפקט — **30 יום**, זהה ל-`#317` · וכל שליחה
נרשמת ב-`audit_logs` עם הנמען.

**‏⚠ אידמפוטנטיות — שני מפתחות, לא אחד (סבב 5).** ‏`(schedule_id, period)` לבדו אינו מספיק כאשר
לתזמון יש כמה נמענים: כשל חלקי היה מאלץ לבחור בין שליחה כפולה למי שהצליח לבין ויתור על מי שנכשל.
**הפרדה:** ‏**יצירת הארטיפקט** ממופתחת `(schedule_id, period)` — פעם אחת · **המסירה** ממופתחת
‏`(schedule_id, period, recipient_profile_id)` — ‏retry, ‏bounce, השהיה והתאמה **לכל נמען בנפרד**.

**מדיניות bounce, במספרים (סבב 5).** סף: **שלוש** מסירות כושלות **רצופות לאותו נמען**.
מסירה מוצלחת **מאפסת** את המונה. **ההשהיה היא פר-נמען ולעולם לא לכל התזמון** — נמען אחד עם תיבה
מלאה אינו מפסיק את הדוח לשאר. התראה לבעלים **מנוכה-כפילויות** (אחת להשהיה, לא אחת לכישלון),
וההחזרה לפעילות היא **ידנית ומפורשת**, לא אוטומטית אחרי זמן.

### התלות היחידה שנשארה פתוחה — Paddle sandbox

**רקע — למה `#318` נפתחה בכלל.**
הבעלים ויתר על תנאי (א) של `#259` (`#314`) ובחר מסירה כפולה (`#315`). **הביקורת הצביעה על כך
ש-`#259` עסק ב„תור ג'ובים לדוחות איטיים" — לא במסירה יזומה של נתונים כספיים לתיבת דואר.** אלה שתי
שאלות שונות, ולשנייה יש שאלות רשות משלה שמעולם לא נשאלו: **מי מורשה לקבל ספרי עסק במייל, לאיזו
כתובת, ומה קורה כשעובד עוזב** · באיזה תדירות ובאיזה אזור זמן · מה קורה כשהמנוי כבר אינו מזכה ·
כמה זמן נשמר הארטיפקט · ומה חוזה הצרופה.
**‏PR-13 היה חסום עד ש-`#318` נענתה, והיא נענתה.** ‏`#314` ו-`#315` נשארים בתוקף כפי שהוכרעו; מה שנפתח הוא **מה
שהם לא כיסו**, ולא היפוך שלהם.

**‏Paddle sandbox — תלות חדשה בבעלים.** ‏checkout שנכתב בלי חשבון הוא קוד שאי אפשר לאמת: אין מזהי
מחיר אמיתיים, אין גרסת API, ואין חוזה redirect. **‏sandbox של Paddle אינו דורש KYC** ונותן את
שלושתם. ‏**ההצעה: לפתוח sandbox** — וכך ההכרעה „בונים עד הדלת" נשמרת ו**גם** ניתנת לאימות. בלי
sandbox, ‏checkout נדחה.

## Out of scope

נשמר מסעיף 12 של המפרט, **עם שינוי אחד:** ‏`checkout` נכנס להיקף (בכפוף ל-sandbox).

**‏⚠ תיקון סתירה בגרסה הראשונה של המסמך הזה:** היא הכניסה את הייצוא המתוזמן להיקף ובמקביל השאירה
את `report_jobs` מחוץ להיקף. **שני אלה אינם יכולים להתקיים יחד.** הניסוח הנכון: ‏`#314` פתח את
`#259`, ולכן **תור `report_jobs` אינו עוד „מחוץ להיקף" בהגדרה** — אבל הוא גם אינו נבנה, כי PR-13
כולו נפתח עם הכרעת `#318`, וחוזה התזמון שלו כתוב.

נשאר בחוץ: **הדלקת גבייה בפועל** (חשבון, KYC, payout, סוד) · Stripe ו-Morning (`#207`/`#256`) ·
תיוק אוטומטי של מסמך נכנס לחשבונית · מענה על הודעה נכנסת · IMAP/POP לתיבת הלקוח · SMS ו-MMS ·
דוח או שדה חדש · שינוי `user_role` · מסך אינטגרציות (`#98`) ·
תיקון מספרי השורה ב-`INTEGRATION-ARCHITECTURE.md`.

## רשימת מה שאסור לשנות

חמישה-עשר הסעיפים של המפרט (סעיף 15) בתוקף, **עם תיקון אחד מחייב לסעיף 3.**

`org_id` לעולם לא מה-payload — **ובנוסף, לעולם לא מארגומנט של קורא `service_role`; הוא נגזר מרשומת
הניתוב במסד** (ת-5) · `smart_document_source_checksum` הקיימת אינה משתנה · `documents_insert` ומסלול
הדפדפן אינם משתנים בשום תו · נתיב מתחיל ב-`{org_id}/` ונאכף בפונקציה · `billing_provider_boundary`
נשארת בלי פונקציית כתיבה · `service_role` לעולם לא בדפדפן.

**⚠ סעיף 3 של המפרט („raw bytes → אימות חתימה → parse") מוחלף בסדר פר-ספק.** הניסוח האוניברסלי
**אינו ניתן ליישום ב-Twilio** ולכן היה הופך את הכלל לשקר בעץ (ת-4):

| ספק | הסדר המחייב |
|---|---|
| **Resend** (Svix) · **Paddle** | raw bytes → אימות חתימה → parse. ללא חריגה |
| **Twilio** | ‏HMAC מחושב מעל **פרמטרים מפורסרים** ו-credential נבחר מ-`To` שאינו מהימן. לכן: שמירת raw · **פרסור חסר-אובדן של מה שחתום בלבד** · חיפוש credential **נטול תופעות לוואי** (ללא כתיבה, ללא יומן, ללא מונה) · אימות · **ורק אז** פרסור עסקי וכל כתיבה |

**האינווריאנט האמיתי, שנכון לשלושתם:** ‏**אף תופעת לוואי — כתיבה, יומן, מונה, הורדה או מגע בדייר —
לפני שהחתימה אומתה.** פרסור לצורך בחירת מפתח מועמד אינו תופעת לוואי; הוא חייב להיות טהור.
