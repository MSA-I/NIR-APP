node_modules/
dist/
public/fonts/almoni/
.env
supabase/functions/.env
*.local
*.log
.wrangler/
supabase/.temp/
supabase/.branches/
# impeccable live-mode runtime state (journal/annotations/server) — config and design context ARE committed
.impeccable/live/sessions/
.impeccable/live/annotations/
.impeccable/live/server.json

# Local QA runtime state and evidence. These may contain authenticated sessions.
.env.qa
.qa-auth/
.qa-state/
.qa-runs/
playwright-report/
test-results/
.tmp/

# Written by root-level Deno commands. Edge functions that opt into a dedicated, reviewed lockfile
# add an explicit exception below; the repository still does not carry a shared root lockfile.
deno.lock
!supabase/functions/send-feedback/deno.lock
!supabase/functions/recover-document-processing/deno.lock
!supabase/functions/assistant/deno.lock

# Local screenshots from scripts/portal-visual-check.cjs (manual visual verification)
.portal-shots/

# Local screenshots from scripts/subscription-visual-check.cjs (manual visual verification)
.plan-shots/

# Local screenshots from scripts/toast-undo-visual-check.cjs (manual visual verification)
.toast-shots/
