node_modules/
dist/
.env
*.local
*.log
.wrangler/
supabase/.temp/
supabase/.branches/
# impeccable live-mode runtime state (journal/annotations/server) — config and design context ARE committed
.impeccable/live/sessions/
.impeccable/live/annotations/
.impeccable/live/server.json

# Local QA runtime state and evidence. These may contain authenticated sessions.
.env.qa
.qa-auth/
.qa-state/
.qa-runs/
playwright-report/
test-results/

# Per-run gate secrets handed between CI steps by scripts/ci-local-env.mjs. Throwaway values
# for an ephemeral local stack, but they are secrets in shape and must never be committed.
.ci-gate-secrets.json
