node_modules/
dist/
.env
*.local
*.log
.wrangler/
supabase/.temp/
supabase/.branches/
# impeccable live-mode runtime state (journal/annotations/server) — config and design context ARE committed
.impeccable/live/sessions/
.impeccable/live/annotations/
.impeccable/live/server.json

# Local QA runtime state and evidence. These may contain authenticated sessions.
.env.qa
.qa-auth/
.qa-state/
.qa-runs/
playwright-report/
test-results/

# Written by the deno steps of a LOCAL `npm run quality` run. The repository has never carried a
# root deno.lock — the Deno config lives in supabase/functions/interpret-document/deno.json and CI
# resolves without one. Committing it now would change what CI resolves, which is a supply-chain
# decision and not a tidy-up.
deno.lock
