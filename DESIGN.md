---
name: InPlace
description: מערכת procurement-to-payment עברית RTL — B2B פרימיום, Onyx/Wheat/Oceanic + צבע סמנטי
colors:
  # ---- שלד (structural) — "Onyx / Wheat / Oceanic" (T7, 18.08.2026, פלטת רפרנס של הבעלים) ----
  # שלושה עוגנים: ONYX ‏#0A171D (דיו + shell) · WHEAT ‏#FFF6E9 (קרם נקודתי) ·
  # OCEANIC ‏#003F47 (מותג + פעולה). T7.3k: הרקע הגדול הוא אפור ניטרלי — הקרם ירד
  # לנקודות בלבד (sunken, אותיות ה-tooltip, הדגשות עדינות). צבעי הלוגו הסופי נפרדים; ראו §6.
  canvas: "oklch(96.88% 0.0028 128)"            # רקע הגוף — ‏#F4F5F3 (בעלים, T7.3k)
  surface: "oklch(99.2% 0.006 80)"              # כרטיסים, טבלאות, מודאלים, שדות
  surface-sunken: "oklch(95.4% 0.012 80)"       # thead, שדות disabled, רצועות שקטות
  # שתי מדרגות האינטראקציה (19.08.2026). קודם לכן `action-wash` נשא שני תפקידים — גוון-מותג רך
  # וגם "העכבר כאן" — ונמדד 1.07:1 מול הכרטיס, מוגש ב-40% מזה ב-.row-hover. „נבחר” לא היה קיים
  # כטוקן כלל, ולכן האפשרות הפעילה בחיפוש הגלובלי לבשה את מחלקת ה-hover עצמה.
  # 26.08.2026 — הכרעת בעלים: „יש צבע תכלת כזה כשאני עושה HOVER מעל אובייקט … הוא לא קשור בכלל
  # לצבעי המערכת”. שתי המדרגות נחצבו מ-`action-wash` ולכן ירשו את גוון המותג (200/202) בלי לרשת
  # את תפקידו: מצביע שנוחת על שורה אומר „זו”, וזה נאמר בבהירות בלבד. הבהירות (95.6 / 91.5) לא
  # השתנתה — כל מדידות הניגודיות בסעיף הנגישות נמדדו מולה — רק הכרומה ירדה והגוון עבר ל-80,
  # משפחת הקנבס, כך שהמדרגה נקראת כנייר שמעמיק ולא כשטיפה קרירה על נייר חם.
  surface-hover: "oklch(95.6% 0.004 80)"        # ריחוף על שורה/פריט/summary — ‏ΔL 3.6 מהכרטיס
  surface-selected: "oklch(91.5% 0.006 80)"     # אפשרות נבחרת, ניווט פעיל, מצב לחוץ — ‏ΔL 4.1 מ-hover
  line: "oklch(89.5% 0.022 78)"                 # מפרידים מול canvas (לכרטיסים אין גבול — T7)
  line-soft: "oklch(93.5% 0.017 78)"            # מפרידי שורות פנימיים
  # 65%→63% (19.08.2026): ‏1.4.11 דורש 3:1 לגבול שדה, ו-65% נמדד 3.16 מול הכרטיס אך 2.96 מול
  # הקנבס ו-2.83 מול רצועת sunken — שני המשטחים שעליהם שדה יושב לרוב. המסמך ציטט את ערך
  # הכרטיס כאילו היה כללי. ‏63% נמדד 3.42 / 3.20 / 3.06.
  line-strong: "oklch(63% 0.018 78)"            # גבול שדות/כפתור משני — 3.06:1 במקרה הגרוע
  ink: "oklch(19.6% 0.022 229)"                 # כותרות דף, הטקסט החזק ביותר — Onyx
  ink-body: "oklch(28% 0.021 229)"              # טקסט גוף
  ink-mid: "oklch(36.5% 0.021 229)"             # תאי טבלה
  ink-soft: "oklch(43.5% 0.021 229)"            # תוויות, כפתורי ghost
  ink-muted: "oklch(47% 0.02 229)"              # כותרות עמודה, טקסט משני (הועמק ב-T7.3f)
  ink-faint: "oklch(53.5% 0.019 229)"           # placeholder ורמזים — 4.78:1 על canvas
  ink-ghost: "oklch(75.5% 0.014 80)"            # אייקונים כבויים, סימנים דקורטיביים
  # פעולה = מותג אוקיאני; היא מבנית בלבד ולעולם אינה מחליפה סטטוס.
  action: "oklch(33.66% 0.058 209)"              # #003F47 — כפתור ראשי, לינק, גרף
  action-hover: "oklch(29.5% 0.048 210)"
  action-solid: "oklch(39% 0.062 208)"           # צ'יפים מלאים, מילויי בחירה
  action-soft: "oklch(93% 0.022 200)"            # badge ספירה
  action-on-soft: "oklch(32% 0.05 209)"
  action-wash: "oklch(96.6% 0.011 195)"         # גוון-מותג רך: כפתור משני, צ'יפ סינון, פאנל שקט
  action-line: "oklch(62% 0.055 205)"            # גבול שדה/מסגרות דקות — 3.48:1
  focus: "oklch(50% 0.085 207)"                  # טבעת פוקוס — 5.65:1
  # טקסט על משטח מותג/סטטוס מלא. היה המילה `white` ב-24 מקומות — שם של צבע, לא של תפקיד;
  # הבהרה עתידית של המותג הייתה שוברת את כולם בשקט.
  on-solid: "#ffffff"                            # ‏.btn-primary, ‏.btn-danger, ‏toast, ‏puck
  # shell כהה: המסגרת הממותגת של המוצר — Onyx; אזור העבודה נשאר נייר חיטה.
  shell: "oklch(19.57% 0.022 229)"               # #0A171D
  shell-ink: "oklch(97.5% 0.012 80)"
  shell-ink-soft: "oklch(88% 0.014 80)"
  shell-ink-dim: "oklch(73% 0.016 80)"
  # גרפים — צבעי המערכת עצמם (T7.3g, בעלים: "לפי צבעי המערכת"): אוקיאני + שלושה מיקסים
  # אטומים שלו על surface + אוניקס. מונוכרום-סדרתי; הפרדת שכנים בבהירות (חסין CVD),
  # והזהות ממילא לעולם לא על צבע לבד (מקרא, רווחי דונאט, קווקוו, תוויות).
  # הרמפה הזו כבר אינה מגיעה לגרפים — מ-25.08.2026 היא משמשת את aurora ההתחברות בלבד; העמודות
  # עברו ל-bar-high/mid/low שלמטה. (‏chart-3 נשאר גם המקור של `flat` — קו מחיר ללא שינוי.)
  chart-1: "{colors.action}"                    # אוקיאני עמוק ‏#003F47
  chart-2: "oklch(52.84% 0.0501 205)"           # אוקיאני 72% על surface — ‏#477479
  chart-3: "oklch(68.84% 0.0313 202)"           # אוקיאני 48% — ‏#85A1A3
  chart-4: "{colors.shell}"                     # אוניקס — סדרת הניגוד (מקווקווה בקווי)
  chart-5: "oklch(83.11% 0.0153 192)"           # אוקיאני 26% — ‏#BDCBCA
  # ---- פלטה קטגורית — זהות, מה שהרמפה שמעליה אינה יכולה לשאת (24.08.2026) ----
  # הרמפה סדרתית והיא טובה בזה: עמודות לאורך זמן, ‏sparkline, סדרה אחת שסדר הבהירות בה אומר
  # משהו. היא שימשה גם לזהות — קטגוריות בדונאט והשוואת שתי סדרות — וזו עבודה שרמפת גוון
  # יחיד אינה יכולה לעשות, כי ההפרדה היחידה שיש לה לתת היא בהירות. זו מדידה ולא דעה: מול
  # ה-validator כל חמש המדרגות נופלות ברף הכרומה ‏0.10 ‏(0.058 · 0.050 · 0.031 · 0.022 · 0.015),
  # ומתחת למספר הזה גוון מפסיק להיקרא כגוון. חמש המדרגות כאן נבחרו בחיפוש ולא בעין: גוון
  # ובהירות מוטבו יחד כדי למקסם את ה-ΔE הגרוע ביותר בכל הזוגות תחת פרוטן/דויטן, בתוך טווח
  # הבהירות של ה-validator, בכרומה רגועה (‏≤0.145) ומחוץ לתחומי הגוון שהסטטוס מחזיק
  # ‏(alert ‏~15-40 · await ‏~62-80 · done ‏~150-172) כדי שסימן בגרף לעולם לא ייקרא כמצב עסקי.
  # התוצאה: הזוג הגרוע ‏15.3 ‏CVD ו-16.1 בראייה מלאה, מול רפים של ‏8 ו-15. ‏series-1 נשאר
  # במשפחה האוקיאנית של המותג. מחליף מדרגה מריץ מחדש את ה-validator ואת colorLanguage.spec.
  series-1: "oklch(58% 0.105 222)"              # אוקיאני בהיר — הסדרה הראשית
  series-2: "oklch(73% 0.145 108)"              # זהוב-זית — הסדרה הנגדית (מקווקוות)
  series-3: "oklch(46% 0.125 48)"               # אדמה שרופה
  series-4: "oklch(73% 0.125 192)"              # תכלת-ים
  series-5: "oklch(46% 0.145 270)"              # אינדיגו; „אחר” בדונאט תמיד המדרגה האחרונה
  # ---- רמפת עמודות — גודל, בשפת הפלטה הקטגורית (25.08.2026, דיווח בעלים) ----
  # כשהזהות עברה ל-series-* נשארו העמודות החודשיות על chart-1..5, ולוח המגמות הציג **שתי שפות
  # צבע במבט אחד**: דונאט וזוג קווים בגוונים החדשים, ולידם עמודות בטורקיז עמוק, אוניקס ואפרפר.
  # הפתרון אינו „לעשות שימוש חוזר במדרגות הקטגוריות”: גרף עמודות לאורך חודשים מקודד **גודל**,
  # והפלטה הקטגורית בנויה בכוונה בלי סדר גודל — מדרגותיה יושבות בבהירות דומה כדי שאף אחת לא
  # תשתלט, ושימוש בה היה הופך את החודש הגבוה והנמוך לשני גוונים לא-קשורים. לכן זו נשארה
  # **רמפה**, ומה שהשתנה הוא המשפחה שממנה היא נגזרת: שלוש מדרגות בהירות של הגוון של הסדרה
  # הראשית (222). ‏`mid` הוא alias ל-series-1 ולא חזרה על הליטרל. ‏`colorLanguage.spec.ts` מצמיד
  # סדר בהירות, פער ‏≥0.15 בין מדרגות, וגוון זהה בשלושתן — רמפה שמדרגותיה מתכנסות מפסיקה לקודד
  # דירוג בלי להיראות שבורה, ורמפה שגוונה נודד היא פלטה קטגורית שנייה שאיש לא מדד.
  bar-high: "oklch(40% 0.105 222)"              # הגבוה
  bar-mid: "{colors.series-1}"                  # כל השאר
  bar-low: "oklch(80% 0.062 222)"               # הנמוך, וגם דלי אפס
  # זהות אזור — "איפה אני", לעולם לא "מה קרה". ‏T7.3: אוקיאני אחיד (הבעלים פרש את
  # התמצאות-הגוון — הקו הסגול נקרא כצבע בלי משמעות); המנגנון כולו נשאר.
  # 19.08.2026 (בעלים): שלושת השמות אוחדו לאחד. שלושה טוקנים באותו ערך הבטיחו הבחנה
  # לפי תחום שאף מסך אינו מציג; שם הוא טענה. נקודת ההרחבה אם גוונים נפרדים יחזרו.
  section-accent: "oklch(33.66% 0.058 209)"
  chart-grid: "oklch(90% 0.02 78)"
  chart-tick: "oklch(49.5% 0.02 229)"
  chart-label: "oklch(43.5% 0.021 229)"
  star: "amber-400"                             # כוכב מלא. הכוכב הריק הוא line-strong (3:1)
  star-hover: "amber-300"
  # ---- סימני דרגת מנוי — זהות מסחרית, ובמפורש לא משמעות סטטוס שישית (25.08.2026) ----
  # הדרך המתבקשת לצייר זהב היא גלולה בגון ענבר בהיר — וזו בדיוק `await-soft`,
  # שאומרת „הכדור אצל גורם חיצוני”. לכן הזהב כאן הוא מילוי רווי בבהירות בינונית עם אותיות
  # אוניקס — צ׳יפ מתכתי, לא גלולה — כך שההבדל הוא מבנה ולא רק גוון. שם המסלול מודפס
  # תמיד בתוך הסימן, ולכן המשמעות לעולם אינה נשענת על צבע בלבד.
  tier-oceanic: "oklch(76% 0.075 209)"          # גוף הצ׳יפ
  tier-oceanic-soft: "oklch(89% 0.045 209)"     # הקצה המואר
  tier-oceanic-deep: "oklch(61% 0.070 209)"     # הקצה המוצל והגבול
  # ---- שפת הצבעים הסמנטית — העוגנים ללא שינוי ----
  # done=הושלם (emerald) · await=ממתין (amber) · alert=חריגה (rose) · info=מידע (sky) · idle=ניטרלי (slate)
  # כל משמעות חושפת שישה משטחים: wash/line/soft/on-soft/fg/solid — הערכים ב-@theme ב-src/index.css.
  trend-up: "rose-700"                          # התייקרות — תמיד עם חץ
  trend-down: "emerald-700"                     # הוזלה — תמיד עם חץ
typography:
  # `Almoni Neue SupplyFlow` הוא alias טכני היסטורי של CSS @font-face, לא שם המוצר המוצג.
  # שינויו דורש migration תואם בקוד ובנכסי הפונט; ה-rebrand הנוכחי הוא InPlace.
  # Production: Almoni Neue מורשה. Free/CI: Noto Sans Hebrew מקומי. אין תלות ב-Google Fonts.
  headline:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, Arial, ui-sans-serif, system-ui, sans-serif"
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.25
  headline-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1.25
  title:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "17px"
    fontWeight: 600
    lineHeight: 1.4
  body:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.5
  label:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "14px"
    fontWeight: 500
    lineHeight: 1.45
  table-header:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "13px"
    fontWeight: 500
    note: "ללא uppercase וללא tracking — לעברית אין רישיות; היררכיה מגודל/משקל/צבע בלבד"
  micro:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "12px minimum"
    fontWeight: 500
    note: "12px הוא המינימום לכל ניווט, badge, chart label ומידע עסקי"
  kpi:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "28px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-compact:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "20px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-compact-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-hero:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "30px mobile / 34px wide"
    fontWeight: 500
    lineHeight: 1.1
    note: "T7.2 — משקל 500: המספרים הגדולים ברפרנס קלים, והפס יושב חשוף על הקנבס; שמור לדשבורד בלבד"
  mobile-input:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "16px"
    fontWeight: 400
    note: "מונע zoom אוטומטי ב-Safari iOS; במסך רחב השדה חוזר ל-14px"
  num:
    fontFamily: "primary font family"
    fontFeature: "tabular-nums lining-nums"
    note: ".num אינו כופה LTR או Mono; tech-id שמור רק ל-UUID, hash, checksum ו-ID פנימי"
  tech-id:
    fontFamily: "ui-monospace, Cascadia Mono, SFMono-Regular, Consolas, monospace"
    fontWeight: 400
    note: "חריג מוגבל למזהי מכונה אמיתיים; תמיד isolate ובדרך כלל dir=ltr"
rounded:
  control: "0.5rem"    # rounded-lg — כפתורים, שדות
  card: "1.5rem"       # rounded-3xl — כרטיסים ומשטחי עבודה (T7.2 — פינות הרפרנס, בכל האפליקציה)
  modal: "1rem"        # rounded-2xl — מודאלים
  pill: "9999px"       # badges + גלולות הניווט הצפות
  square: "0"          # רצועות עבודה edge-to-edge (border-y + bg-surface) בלבד — פינה ישרה היא הצהרה שהמשטח נוגע בקצה המסך, לא כרטיס
spacing:
  xs: "8px"
  sm: "10px"
  md: "12px"
  lg: "16px"
  xl: "20px"
components:
  button-primary: { backgroundColor: "{colors.action}", textColor: "{colors.on-solid}", rounded: "{rounded.control}", padding: "8px 14px" }
  button-primary-hover: { backgroundColor: "{colors.action-hover}" }
  button-secondary: { backgroundColor: "{colors.action-wash}", textColor: "{colors.ink-mid}", border: "none — T7.1 tonal", rounded: "{rounded.control}", hover: "{colors.action-soft}" }
  button-danger: { backgroundColor: "alert-solid", textColor: "{colors.on-solid}" }
  button-ghost: { textColor: "{colors.ink-soft}", hoverBackground: "{colors.canvas}" }
  input: { backgroundColor: "{colors.surface}", border: "{colors.line-strong}", focusRing: "{colors.focus}", rounded: "{rounded.control}" }
  card: { backgroundColor: "{colors.surface}", border: "none — T7", rounded: "{rounded.card}", shadow: "onyx soft-wide (T7.3f)", note: "הפרדה מצל נראה + הפרש גוון מול canvas + ריווח, לעולם לא ממסגרת כהה" }
  badge-*: { pattern: "bg-{tone}-soft + text-{tone}-on-soft", rounded: "{rounded.pill}", exception: "alert — bg-alert-solid + text-on-solid (הכרעת בעלים 25.08.2026, 6.06:1)" }
  note-*: { pattern: "bg-{tone}-wash + border-{tone}-line + text-{tone}-on-soft" }
---

# Design System: InPlace — "Onyx / Wheat / Oceanic"

## 1. Overview

**Creative North Star: "חדר בקרה שקט" — דיו אוניקס על נייר חיטה, במסגרת אוקיאנית.**

InPlace מטפל בכסף, ולכן סביבת העבודה שלו נשארת רגועה: נייר חיטה חם, דיו אוניקס וספרות
מיושרות בפונט המוצר. המסגרת הכהה (Onyx) והפעולה האוקיאנית (Oceanic) נותנות למוצר זהות ברורה
בלי לצבוע כל משטח. **צבע סטטוס עדיין שמור לטענה עסקית בלבד**; האוקיאני מזהה את המוצר ואת
הפעולה, לא "הצלחה" או "חריגה". התוצאה צריכה להרגיש כמו כלי מקצועי שנעים לעבוד בו שעות,
לא תבנית אדמין ולא מסך סטרילי.

**‏T7 (18.08.2026, פלטת רפרנס של הבעלים):** רה-תמה סטטית לפי ה-Runbook — אותם שמות טוקנים,
ערכים חדשים. בכל מקום שהמסמך הזה אומר "פטרול" הכוונה מעכשיו לערך האוקיאני `#003F47`; ה-shell
הכהה הוא Onyx ‏`#0A171D`; הנייר הוא Wheat ‏`#FFF6E9`. לצד הרה-טינט נקבעו שלושה כללים חדשים:
כרטיסים ללא גבול, גלואו סביבתי שנע עם העכבר, ופריסת רפרנס לדשבורד — כולם מתועדים בסעיפיהם.

**Key Characteristics:**
- שקט כברירת מחדל; אוקיאני = מסגרת/פעולה, וצבעי done/await/alert/info = משמעות עסקית בלבד.
- מספרים משתמשים בפונט הראשי וב־tabular figures: טורי סכומים מתיישרים בלי אופי של טרמינל.
- עברית RTL מלאה: properties לוגיים בלבד; אין uppercase.
- **כרטיסים ללא גבול (T7):** ההפרדה בין משטחים מגיעה מהפרש הגוון מול נייר החיטה, מצל אוקיאני רך
  ומריווח — לעולם לא ממסגרת כהה. הרמה של פיקסל אחד רק בכרטיס לחיץ.
- **אווירת הסמן (T7.3f — הכחול הנע על רקע חיטה סטטי-טהור):** הבעלים סגר את הצורה הסופית:
  האפקט העוקב חי **על הרקע** (כמו ב-T7.3c שאושר) — אבל בלי שום בסיס צבעוני סטטי מתחתיו
  ("הוספת רקע סטטי ועל זה את האפקט"), ובלי spotlight על כרטיסים ("ממש לא רציתי"). ‏body נושא
  נשימת חיטה בלבד (גוון ‏76-78, מלוא גובה המסמך — לקח רצועת-החום נשאר); ‏`.app-glow` ‏(fixed)
  נושא שני כתמים בלבד, שניהם מהפלטה: **הכתם העוקב הוא ‏OCEANIC ‏#003F47 עצמו**
  (‏"הצבע הכחול יהיה רק OCEANIC") וכתם-נגד חיטה-עמוקה שקט. ידוע ומקובל: מיקס-אלפא של אוקיאני
  על חיטה נוטה ירקרק — זה המראה שהבעלים אישר.
  ‏T7.3h: **כתם יחיד** — כתם-הנגד הוסר ("צריך שזה יהיה כדור אחד").

  **‏26.08.2026 — עקיבה ועומק (דיווח בעלים: „שהסמן יעקוב בצורה יותר מדוייקת … ולהכהות טיפה”).**
  ‏*העקיבה:* ה-`transition` ירד ‏0.45s → **0.12s**. הפיגור מעולם לא היה ב-JS — ‏`--glow-x/y`
  מתעדכנים פעם ל-frame — אלא בשורה הזאת; ‏0.7s נקרא כגרדיאנט סטטי, ‏0.45s גרר את הכתם מאחורי
  המצביע כמעט שנייה שלמה. ‏0.12s הוא כשבעה frames: מספיק להחליק את המדרגה, לא מספיק כדי שהכתם
  יימצא במקום שהמצביע אינו בו. אפס אינו בשימוש בכוונה — בלי אינטרפולציה רדיאל בגודל viewport
  קופץ בפיקסלים שלמים ונקרא כרעד.
  ‏*העומק:* ‏15% → **17%, וזו תקרה שנמדדה ולא העדפה.** הכתם הוא רקע אמיתי לטקסט אמיתי (כותרות
  משנה יושבות על הקנבס החשוף, ואריחי הדשבורד שקופים-למחצה מ-T7.1), ולכן הוא מדידת WCAG — וזו
  הסיבה שהטוקן הוקשח ל-47% מלכתחילה. נסרק באפליקציה החיה בציור הערימה על canvas וקריאת הפיקסל:

  | אלפא הכתם | `ink-muted` על הקנבס |
  |---|---|
  | 15% | 4.73 (הערך שנרשם כאן כ-4.75) |
  | **17%** | **4.60** ← כאן |
  | 18% | 4.47 — נכשל AA |
  | 20% | 4.32 — נכשל AA |

  להכהות מעבר לזה הוא אפשרי ואינו השורה הזאת: פירושו להקשיח שוב את `--color-ink-muted`, וזה
  צובע מחדש טקסט משני בכל מסך במוצר — הכרעה בפני עצמה.
- **האורורה של מסך הכניסה — החריג היחיד לצבע נע (19.08.2026):** ‏`.aurora-pane` +
  ‏`src/lib/loginAurora.ts` מרנדרים שדה fBm ‏domain-warped ב-WebGL2 בפאנל הצבע של הכרטיס
  ב-`/login`. **זה החריג היחיד לכלל "צבע נע = כתם הסמן בלבד"**, והוא מוגבל בשלושה גדרות:
  ‏(א) המסך הזה יושב מחוץ ל-`Layout` ואין בו נתונים לייצג, ולכן התנועה אינה יכולה להיקרא
  כסטטוס; ‏(ב) המחזור איטי (~0.03 יחידות/שנייה — שינוי מורגש לוקח כחצי דקה), ותחת
  ‏`prefers-reduced-motion` נצבע פריים אחד ונעצר; ‏(ג) הפלטה נקראת בזמן ריצה מ-`--color-chart-1..5`
  ‏(oklch→sRGB דרך canvas 2D של הדפדפן, ומעורבבת ב-linear light) — אין ולו ערך RGB אחד בקוד.
  **תקרת הרמפה נקבעה במדידה, לא בטעם:** הפיקסל הבהיר ביותר שטקסט לבן יושב עליו לאורך סריקה
  של 12.6 שניות הוא `rgb(68,105,109)` = **6.02:1** מול לבן ו-5.6:1 מול `shell-ink` — ‏AA עם
  מרווח. ‏`shell-ink-soft`/`-dim` **אסורים בפאנל** (3.62:1 ומטה). ‏`.aurora-pane::after` מוסיף
  scrim גרדיאנטי (‏shell ב-32%/42% בקצוות) — גרדיאנט על אלמנט, לא `backdrop-filter`; איסור
  ה-glassmorphism נשמר. בלי WebGL2 (‏jsdom, ‏`--disable-webgl`, דרייבר חסום) הקנבס אינו נצבע
  והגרדיאנט הסטטי של `.aurora-pane` — אותם טוקנים, אותו סדר — הוא כל התמונה.
- **פס ה-hero — הוסר (T7.3i):** הפס הכחול של מרכז הבקרה (‏T7.3d, קצה חד ב-T7.3g) בוטל
  בהכרעת בעלים — החלוקה נקראה "שני החצאים של הצבע", והדשבורד חזר להיות **זהה לכל מסך**:
  קנבס חיטה מלא + כתם הסמן היחיד. ‏`.dash-hero-band`/`.dash-hero-head` נמחקו. אם יחזור אי
  פעם — הלקח המדיד שנשאר: אין מעבר נקי בין גוון ‏209 ל-77 (כל דעיכה = זית), רק קצה חד.
- **הטופ-בר הדסקטופי אפרפר-קריר בכל מסך (T7.3i):** ‏`--color-topbar` ‏(oklch ‏94.5%/0.008/229,
  נגזרת-Onyx בהירה) ב-‏75% + ‏backdrop-blur — הבעלים העדיף את הגוון האפרפר שההדר קיבל מעל
  הפס הכהה על פני הקרם שמעל החיטה, ואיחד: אפרפר תמיד.
- אין נתון מזויף: מדד בלי נתונים מציג `—`, לא `0`.

## 2. Colors

### עקרון מסגרת הפטרול
**`action` הוא צבע המותג המבני.** כפתור ראשי, לינק (`.link`), צ'יפ נבחר ועמודת הגרף העדכנית משתמשים
בפטרול; שאר עמודות הגרף משתמשות ברמפה המבנית `chart-2`–`chart-5`. ה-shell כהה יותר ומשמש מסגרת;
פריט הניווט הפעיל הופך לגלולת נייר בהירה כדי להישאר
מובחן ונגיש. אסור להשתמש בפטרול כתחליף לסטטוס סמנטי.

### שפת הסטטוס — ללא שינוי
ארבע משמעויות + ניטרלי, כל אחת עם שישה משטחים (wash/line/soft/on-soft/fg/solid):
**done** (emerald) הושלם/תקין/שולם · **await** (amber) ממתין לטיפול מצדנו · **alert** (rose)
חריגה/דחוף/הפסד אפשרי + פעולה הרסנית · **info** (sky) הכדור אצל גורם חיצוני · **idle** (slate)
היעדר טענה. דרגות `solid` ו־trend הוכהו ל־700 כדי להבטיח AA עם טקסט לבן/טקסט קטן; המשמעות והגוון
לא השתנו. **חוק ארבע המשמעויות** ו**חוק האות הכתום** בתוקף: משמעות תמיד מלווה טקסט או אייקון.

**מילון אחד לכל הרכיבים (02.08.2026).** ‏`Tone` מ־`src/lib/status.ts` הוא אוצר-המילים היחיד לטון:
‏`badge-*`, ‏`note-*`, ‏`AttentionItem`, וגם **`KpiCard` ו־`Scorecard`/`ScoreTone`**, שעד לסבב הזה
קיבלו שמות פלטה (`slate|green|amber|red|blue`) ומיפו אותם פנימית לאותם טוקנים. השמות הוסרו: ערך
כמו `openBalance ? 'amber' : 'slate'` הוא טענת „ממתין" מול „היעדר טענה", והשם הצבעוני רק הסתיר איזו
מהחמש נטענת. המחלקות שמוצגות בפועל לא השתנו. **אין להחזיר שם פלטה כ־prop של טון.**

### שפת המקום — זהות אזור (18.08.2026)

עד לסבב הזה הצבע נשא **מצב** היטב ו**מקום** בכלל לא: כל מסך נראה זהה, ומי שנכנס למערכת עשרות
פעמים ביום לא קיבל שום רמז לאן הגיע. שלושת תחומי העבודה שהמשתמש היומי נע ביניהם מקבלים עכשיו
מבטא: **מסמכים · רכש · כספים**. הם אינם משמעות חמישית או שישית, והם אינם יכולים להפוך לכזו —
מסיבות מבניות, לא מסיבות של משמעת:

**‏1. מקור אחר.** טון נבחר מהנתונים (`Tone` ב-`status.ts`; חשבונית באיחור → `alert`). מבטא נבחר
מה-URL — ‏`sectionOf(pathname)` ב-`lib/quickActions.ts`, ליד `isRouteFamilyActive` ומאותה משפחת
כללים. **אין prop, אין argument, אין שדה נתונים שבוחר מבטא.** על `/invoices` המבטא הוא „כספים”
בין אם החשבונית מאושרת, פתוחה או חורגת. מצב לא יכול להגיע לאוצר-המילים הזה, ולכן אין צורך לסמוך
על כך שאיש לא ינסה.

**‏2. משטח אחר.** טון הוא **מילוי מתחת לטקסט** (`badge-*`, ‏`note-*`) או הטקסט עצמו (`text-*-fg`).
מבטא אינו אף אחד מהשניים: הוא **סמן ו־rule בן 3px**, ולעולם אין עליו או בתוכו טקסט. אין כלל
מבטא שקובע רקע מתחת ל־text node.

**‏3. טוקנים אחרים.** אף כלל מבטא אינו מפנה ל-`--color-{done,await,alert,info,idle}-*`, ואף
‏`.badge-*`/`.note-*` אינו מפנה ל-`--color-section-*`. שני הקירות נבדקים ב-`lib/quickActions.spec.ts`
מול `src/index.css` עצמו, לא מול הכוונה — ולצדם בדיקה שסורקת את כל ה-tsx ונופלת אם `data-section`
נכתב בקובץ כלשהו מלבד `Layout.tsx` או מביטוי שאינו `sectionOf(...)`. ‏`layoutActiveState.spec.tsx`
מצמיד את סמן הניווט הפעיל עצמו.

**מאיפה הגוונים (עודכן T7.3).** עד T7.1 ‏`--color-section-*` היו כינויים של רמפת הגרפים; ‏T7.1
הקפיא אותם כשלושה ליטרלים מובחני-גוון (275/305/335). **ב-T7.3 הבעלים פרש את התמצאות-הגוון**:
הקו הסגול נקרא כ"צבע בלי משמעות", ושלושת המבטאים הם עכשיו **ליטרל אוקיאני אחיד** — הסמן עדיין
אומר "אתה בתחום עבודה" (וכל האינסטלציה נשארת: נגזר מה-URL בלבד, ‏Layout כותב יחיד, סמל+rule
בלבד, לעולם לא מצב עסקי), אבל הצבע הוא פשוט המותג. מסך בלי תחום עדיין אינו מציג דבר. אם
הבחנת-גוון תחזור אי-פעם — שלושה ליטרלים + מפת ה-spec.
**הזזת רמפת הגרפים אינה צובעת את הניווט** — הצימוד של T6 בוטל, וה-spec מצמיד את הליטרל.

**איפה זה מופיע — מקום אחד, על נייר.** המבטאים מודדים 5.0–5.7:1 מול נייר ורק ~2.2:1 מול
‏`shell`, ולכן פס שמצויר ישירות על הסרגל הכהה הוא קישוט בלתי-נראה. לכן:
- ~~**סמן הניווט הפעיל** (`.section-glyph`)~~ — **הוסר 26.08.2026.** הוא נכתב לסרגל הצד הכהה,
  והוחל רק כאשר `linkCls` נקרא עם `surface === 'shell'`. מאז T7.2 הסרגל הכהה הפך לגלולה צפה
  ואף קורא אינו מעביר `'shell'` — כלומר הכלל לא רונדר מעולם מאז, והמסמך המשיך לתאר אותו כחי
  במשך ארבעה סבבי עיצוב. הגלולה היא טקסט בלבד והמגירה אינה כהה עוד, ולכן לא נשאר אייקון לצבוע.
- **סמן העמוד** — ‏rule של 28×3px מתחת ל-`h1` ‏(`.section-mark`), ב-`PageHeader` וב-`RecordHeader`.
  ‏28px אינם נקראים כמפריד, והם אינם נושאים שום מידע שהכותרת שלידם אינה אומרת במילים — כלומר
  יתירות, לא מידע שנשען על גוון (WCAG 2.1 AA).

**מה שאין לו מבטא, ולמה.** ‏`/dashboard` ומסכי הבקרה (`/alerts`, ‏`/exceptions`, ‏`/expenses`,
‏`/reports`, ‏`/analytics`) מביטים **על פני** שלושת התחומים ואין להם מקום משלהם לסמן; ‏`/settings`,
‏`/onboarding` ו-`/admin` אינם תחומי עבודה. אלה אינם חורים במפה — הם ההצהרה שהמפה עושה.
בהיעדר תחום פשוט אין attribute, ‏`.section-mark` נשאר `display:none`, ולא נשמר עבורו מקום.

**גוון = תחום, סמל = מסך.** שלושה מבטאים ולא עשרים: ‏`/orders`, ‏`/receiving`, ‏`/inventory`
ו-`/suppliers` חולקים את מבטא הרכש, והאייקון והכותרת מבדילים ביניהם. מערכת התמצאות עם עשרים
צבעים היא מבחן זיכרון, לא עזר. אף אחד משני הרמזים אינו עומד לבדו.

**המנגנון:** ‏`Layout.tsx` הוא **הקובץ היחיד** שקובע `data-section` — על פריט הניווט הפעיל ועל
`<main>` של המסך הנוכחי. ‏`index.css` ממפה `[data-section="…"]` ל-`--section-accent` פעם אחת, ושני
הצרכנים קוראים את המשתנה. אין הקסים, אין מחלקה נבנית דינמית, ואין קובץ CSS פר-מסלול.

### Named Rules
**חוק המסגרת הממותגת:** פטרול מותר רק בשלד, בפעולה ובגרפים מבניים; הוא לעולם אינו מקודד מצב עסקי.
**חוק שני אוצרות-המילים:** טון בא מהנתונים ומופיע כמילוי מתחת לטקסט; מבטא אזור בא מה-URL ומופיע
כסמל או כ־rule בלבד. אסור למשטח לדבר בשתי השפות, ואסור לטוקן להיות מושאל בין השתיים.
**חוק צ'יפ הספירה:** ‏`bg-action-soft`/`text-action-on-soft` הוא צ'יפ **הספירה** של המערכת (פס
המסמכים שלא תויקו בסרגל, ‏`activeFilters` ב-DataTable, ‏`TaskLine`). הוא אומר **כמה**, לעולם לא
**כמה דחוף** — ולכן הוא נשאר פטרול ואינו מפר את חוק המסגרת. ספירה שכן טוענת דחיפות לובשת
‏`badge-*` מהמילון, כמו הספירה שעל כותרת „דורש טיפול היום”.
**שכבת ה-primitives — שתיים, ומוצהרות.** המוצר אינו מגדיר פלטה ראשונית משלו. הטוקנים המבניים
(קנבס, משטחים, קווים, דיו, מותג, רמפת גרפים, זהות אזור) נכתבים כ-**oklch מפורש**; משפחות הסטטוס
וכוכבי הדירוג נגזרים מ-**רמפות Tailwind מהמדף** (`--color-emerald-*` וכו'). שתי שכבות ראשוניות,
ומעליהן שכבה סמנטית אחת שהרכיבים צורכים. רכיב אינו נוגע באף אחת מהשתיים ישירות.

**חוק הטוקנים:** אפס מחלקות פלטה גולמיות (`slate-*`, `indigo-*`...) ב-tsx — נאכף בגרפ שב־Runbook להלן.
צבע או צל חדש = טוקן חדש ב־@theme + עדכון המסמך הזה יחד. **גם מחוץ ל-`@theme` באותו קובץ** אין
ליטרל: כלל CSS מגיע לצבע דרך `var()` או `color-mix(… var(--token) …)` בלבד.

**והחוק חוצה את גבול הדפדפן (28.08.2026).** החוברת שהמוצר מייצא והמסמך שהוא מפיק ל-PDF נושאים את
אותה שפה — שורת כותרות ב-`--color-action` עם `--color-on-solid`, כותרת ב-`--color-ink`, קו ב-
`--color-line`. **בלי ליטרל אחד:** ‏`src/lib/workbook.ts` צובע את הטוקן לתוך canvas בגודל 1×1 וקורא
ממנו בתי sRGB, בדיוק המסלול ש-`src/lib/loginAurora.ts` פתח, ולכן `check:tokens` עובר. אין palette
שנייה לקבצים, וכשהמותג מוחלף הקובץ מוחלף איתו. פלטה חסרה (בדיקת יחידה, worker) מייצרת חוברת ללא
צבע — מבנה, רוחבי עמודות ו-RTL נשמרים — ולעולם לא צבע מומצא.

> **החריגה היחידה לחוק הטוקנים — כרטיס המנוי (הכרעת בעלים 27.08.2026, ‏`OPEN-DECISIONS #296`).**
> «צבע הנייר נשאר. המנויים זה הדבר היחיד שיכול לחרוג ממסגרת הצבעים של האפליקציה.» ‏`plan-card.css`
> נושא ערכי צבע מוחלטים ולא טוקנים, וארבעת הפרצופים — **אונקס · נייר קרם · סגול · שחור מבריק** —
> הם עיצוב מאושר, לא דריפט. **אל תנקה אותם בשם החוק הזה.**
>
> **למה זה לא היה יכול לעבור דרך `@theme` בכלל:** הכרטיס מצויר גם באתר הנחיתה, ושני הריפואים
> נותנים לאותם שמות משמעות **הפוכה** — ‏`--color-ink` שם הוא `#fbf6ee` (דיו בהיר לעמוד כהה) וכאן
> הוא `#0A171D`, הדיו הכהה ביותר. קובץ שהיה קורא `var(--color-ink)` היה מרנדר לבן-על-לבן באחד
> מהשניים, ואין סדר ייבוא שמתקן זאת. לכן הוא נושא מרחב שמות משלו, `--pc-*`.
>
> **המקור הוא הנחיתה, לא כאן.** ‏`src/styles/plan-card.css` הוא **תעתיק מילולי** של
> `src/styles.css:1875-2230` בריפו `LANDING-PAGE-NIR`. שינוי במראה הכרטיס נעשה **שם** ומגיע לכאן
> בהעתקה. שימו לב שזה **הפוך מכיוון הטוקנים**: הנחיתה מושכת את הפלטה מכאן דרך
> `scripts/extract-tokens.mjs`, והכרטיס נוסע לכיוון ההפוך.
>
> **«היחיד» נאכף ולא רק נאמר:** ‏`check:tokens` מכיר בדיוק שני גיליונות סגנון תחת `src/` —
> ‏`index.css` ו-`styles/plan-card.css` — ונופל על שלישי, יהיה תוכנו אשר יהיה. חריגה שאיש אינו
> סופר אינה חריגה אלא חור, ו-`DESIGN.md:662` כבר אומר למה: **חריגה שאינה נרשמת נבדקת שוב בכל פעם.**

**חוק המשטח והמצב (19.08.2026):** ‏`canvas` (רקע הגוף) → `surface-sunken` (באר שקטה) →
`surface` (כרטיס) → `surface-hover` (העכבר כאן) → `surface-selected` (זה שנבחר, וגם מצב לחוץ).
**עומק הוא צל, לא משטח שישי** — ובממשק בהיר זו אינה העדפה אלא מדידה: הכרטיס יושב על
‏99.2% בהירות, ולכן מדרגה "מוגבהת" מעליו הייתה חייבת להיות לבן טהור — הפרש 0.8 נקודות,
בלתי-נראה על נייר. הסולם המלא, שבע מדרגות, **וכולן טוקנים**:
`shadow-card → -card-hover → -dashboard → -menu → -toast → -dialog → -fab`.
‏`shadow-{sm,md,lg,xl,2xl,inner}` של Tailwind **אסורים בקוד מוצר** ונאכפים ב-`check:tokens`:
הם שחור ניטרלי, ולכן היו שורדים רה-תמה של המותג — בדיוק מה שכל שאר טוקני הצל נכתבו כדי למנוע.
עד 19.08.2026 המודאל והטוסט — שני המשטחים הגבוהים במוצר — לבשו אותם.
‏`action-wash`/`action-soft` נשארים גוון-מותג רך ואינם מצב.
**חוק ה-`solid`:** ‏`{tone}-solid` הוא **מילוי פקד**. טקסט או אייקון בטון לובשים `{tone}-fg`;
טקסט על משטח מלא לובש `on-solid`. שלושת הערכים זהים היום, ולכן רק spec יכול להחזיק את ההבחנה —
‏`src/lib/colorLanguage.spec.ts`.
**חוק כיוון השינוי:** התייקרות/הוזלה הן `trend-up-fg`/`trend-down-fg` **תמיד עם חץ**, ולעולם לא
`alert`/`done`. שינוי שאין עליו שיפוט עסקי (רכש חודש מול חודש) הוא דיו ניטרלי — `ink-mid`,
כמו ב-`PeriodComparison`. חץ מגמה וטון סטטוס לא חולקים אלמנט.

**‏`PeriodComparison` — אחוז אינו נאמר בלי מה שמולו (31.08.2026).** הדשבורד החזיק **שלוש** השוואות
מגולפות ביד: חודש-עד-היום מול אותם ימים בחודש הקודם (פעמיים), וחודש מול חודש על חשבוניות. כל אחת
גזרה מחדש את אותה אריתמטיקה, כל אחת איתה את טיפול ה-null שלה, וכולן אמרו „מול אותם ימים בחודש
הקודם" — משפט שמנסח **יחס** ולא נוקב באף אחת משתי התקופות. קורא ב-17 בחודש אינו יכול לדעת ממנו אם
הבסיס הוא חודש שלם (הוא אינו) או אותם 17 ימים (הוא כן). ‏`ui.tsx` מחזיק עכשיו רכיב אחד שמקבל את
הבסיס כארגומנט ומדפיס אותו: **‏„‏1–17.8 מול 1–17.7 · הזמנות שנשלחו".**

**ובסיס חסר נאמר בקול.** מכנה `null`, אפס או שלילי אינו מייצר אחוז — והקוד הקודם פשוט השמיט את
הצ׳יפ, מה שנקרא „לא השתנה". עכשיו כתוב **„אין בסיס להשוואה"**. זו אותה חוקה שאוסרת להדפיס `0`
במקום `—`: היעדר בסיס הוא עובדה על **הנתונים**, והסתרתה מאפשרת לקורא להסיק מה שהמספרים אינם תומכים בו.
‏`DeltaChip` נמחק באותו מהלך — המשפט הנגיש שלו נקב **בבסיס קבוע**, נכון לשני קוראיו ושקרי להשוואה
השלישית באותו מסך.
**חוק אי-הסתמכות על גוון:** מ-24.08.2026 הזהות נשענת על `series-1`–`series-5`, שכל זוג בהן
נמדד ‏≥15 ‏ΔE בראייה מלאה ו-≥8 תחת עיוורון צבעים — אבל **גוון השתפר, הוא לא הפך למספיק**:
הסדרה הנגדית בקו עדיין חייבת `dash`, הדונאט עדיין נושא צ׳יפים עם שם ואחוז, והעמודות עדיין
מוסרות את הערכים ב-tooltip וב-ARIA. כל גרף, sparkline או פקד שמקודד משמעות בגוון נושא
גם `aria-label` או תווית — כולל `PriceSparkline` וכוכבי הדירוג.
**חוק החשיפה המדורגת:** בדשבורד הפעולה הבאה, הסכומים הראשיים והמגמות המרכזיות גלויים מיד; פירוט
תפעולי משני נשאר זמין ב־disclosure. אין למחוק נתון כדי להפחית עומס, ואין להסתיר פעולה קריטית מאחורי
hover או בורר תצוגה.

## 3. Typography

**Production UI:** Almoni Neue במשקלים 400/500/600 בלבד, מתוך קובצי webfont מורשים שמסופקים
בזמן build ואינם נשמרים בריפו. **Free/CI:** Noto Sans Hebrew מקומי, ברישיון OFL, באותה היררכיה.
Assistant ו־IBM Plex אינם חלק מחוזה ה־runtime. Noto הוא גם fallback של Almoni, ואחריו Arial ו־system UI.

**מספרים:** `.num` משתמש באותה משפחה ראשית עם `tabular-nums` ו־`lining-nums`. המחלקה אינה כופה
`direction:ltr`, אינה משנה יישור ואינה משתמשת ב־Mono. `.tech-id` הוא החריג היחיד: UUID, hash,
checksum, job/submission ID או קוד פנימי אמיתי. מזהים אטומיים מקבלים `dir="ltr"`; סכומים, כמויות,
אחוזים, תאריכים, טלפונים ומספרי מסמכים נשארים בפונט הראשי.

### Hierarchy
Page title ‏600/24px מובייל ו־26px רחב (`page-title`) · Section title ‏600/17px (`section-title`) ·
Body ‏400/15px/1.5 · Label/control/table body ‏500 או 400/14px · Table header ‏500/13px —
**בלי uppercase ובלי tracking דקורטיבי** · Micro/chart/badge ‏500/12px מינימום · KPI ראשי
‏600/26px מובייל ו־28px רחב · KPI קומפקטי ‏600/20px מובייל ו־24px רחב. אין משקל 700 בממשק;
`strong` ו־`b` נעצרים ב־600 ו־`font-synthesis` כבוי.

בשדות קלט במובייל גודל הטקסט הוא לפחות `1rem`; זהו כלל שימושיות שמונע zoom אוטומטי ב־Safari iOS,
לא שינוי בהיררכיית הטיפוגרפיה של תוכן המערכת.

**חוק הספרות המיושרות:** ערך מספרי מקבל `class="num"` עם tabular figures. המחלקה אינה עוטפת משפט
עברי שלם. במחרוזת מעורבת עוטפים רק את הערך ב־`span.num`; מזהה טכני אטומי מקבל `tech-id` ו־`dir="ltr"`.

**חוק בידוד השמות:** כל שם שהוזן על ידי משתמש (מוצר, ספק, תיאור שורה) ומרונדר inline נעטף
`<bdi>`. שם כמו `מטליות מיקרופייבר 30*30 אר-12ב` מערבב עברית חזקה עם ספרות חלשות וסימנים
ניטרליים, והאלגוריתם הדו-כיווני פותר את הרצפים החלשים מול השכנים באותו inline context — כך
קטעים מתהפכים מעבר לגבול השם. ‏`<bdi>` (בידוד + first-strong מ־UA stylesheet) סוגר את השם
ליחידה אטומית. **במכוון אין מחלקת CSS חדשה:** האלמנט הנטיבי נושא את ההתנהגות בעצמו, ‏className
עובר דרכו, ו־`.num` נשאר למספרים בלבד ואינו עוטף משפט עברי. כשעטיפה באלמנט נוסף משנה עומק DOM
שחוזה נשען עליו (locator של smoke, בדיקת מבנה) — ‏`dir="auto"` על האלמנט הקיים שקול ל־`<bdi>`
‏(בידוד + first-strong) בלי לשנות את העץ. בהקשרי טקסט-בלבד (הודעות WhatsApp, ‏`<option>`) שבהם
markup אסור — ‏`bidiIsolate` מ־`src/lib/format.ts` עוטף ב־FSI/PDI ‏(U+2068/U+2069), התאום
המחרוזתי של `<bdi>`.

## 4. Elevation

העומק רך ועקבי. **מ-T7 אין לכרטיס גבול, ומ-T7.3f הצל הוא ההפרדה** — הכרעת בעלים מפורשת
("צריך להשחיר את הצל שיש מתחת לכל ה-CARDים", 18.08.2026) שהפכה את לחישת-T7.2: על נייר חיטה
בהיר בכל האפליקציה, כרטיס בלי גבול נפרד מהרקע בצל **Onyx רך, רחב ונראה** (‏0.07/0.13 בשתי
שכבות), כמו ברפרנס הכרטיסים. "אין צל שחור גנרי" עדיין חל על צל קשה/צפוף — העומק כאן רחב
ומעודן, לא קו כהה:
- **מונח:** `shadow-card` — שכבת ההפרדה הראשית של כרטיס, לצד הפרש הגוון והריווח.
- **כרטיסי הדשבורד אטומים (T7.3h — קבורת שקיפות-T7.1):** השקיפות+blur של הדשבורד (‏72%
  ואחר-כך ‏86%) הפכה עכורה-ירקרקה ברגע שהכרטיסים ישבו על פס ה-hero הכהה — הבעלים סימן את
  הערפל פעמיים ("עדיין יש את הדבר הזה"). כרטיסי הדשבורד הם עכשיו נייר אטום כמו כל כרטיס;
  את מרכז הבקרה מייחד רק ‏`shadow-dashboard` העמוק מעט. איסור ה-glassmorphism חזר להיות
  בלי חריגים.
- **חריג עומק בשם — dashboard-depth:** בתוך מרכז הבקרה בלבד, כרטיסים מקבלים
  `shadow-dashboard`, צל פטרול מעט עמוק יותר שמפריד בין אזורי ההחלטה. הסלקטור חייב להישאר
  ממוסגר תחת `.dashboard-depth`; אין להחיל את הטוקן על כרטיסים תפעוליים בשאר המערכת.
- **מוזמן:** ‏`.card-link-hover` — גבול `action-line`, ‏`shadow-card-hover` והרמה של 1px בלבד.
- **מרחף:** ‏`--shadow-dialog` למודאל (rounded-2xl) — תמיד עם backdrop ‏`shell/50`;
  ‏`--shadow-toast` לטוסט, קשה מעט פחות ופרוש פחות, כי הוא הודעה חולפת ולא מצב מוחזק.
- **צף קבוע:** ‏`--shadow-fab` — פטרול מעט חזק מ־`--shadow-menu`; שמור ל־puck הצילום המורם בסרגל
  הפעולות במובייל בלבד. (הוא נקרא `fab` כי הוא שירת גם את ה-FAB הדסקטופי, שהוסר ב-09.08.2026.)

**רישום z-index:** בר קבלה 30 · סרגל צד/כותרות/סרגל פעולות מובייל ‏40 · מגירה/מודאל 50 · טוסט 60.

## 5. Components

### Navigation — גלולה צפה (T7.2, הכרעת בעלים לפי הרפרנס)

**בדסקטופ (מ-`lg`) הניווט הוא גלולות צפות על הקנבס** — אין בר: ‏header שקוף דביק
(`bg-canvas/70 backdrop-blur`), ובתוכו שלושה אשכולות — **גלולת לוגו** מתארית בקצה ההתחלה
(‏surface/85, ‏ring-line-soft, סימן + שם המוצר מ-xl), **גלולת הניווט הלבנה** ממורכזת
(‏rounded-full, ‏surface/90, צל-לחישה), ובקצה הסוף — גלולת חיפוש (ה-combobox הקיים), פעמון
ומשוב עגולים ישירות על הרקע, ו**דיסקת אווטאר** (ראשי תיבות על Onyx) שפותחת את תפריט החשבון.

- **פריטי הגלולה הם טקסט בלבד** (כמו ברפרנס). ‏`NAV_SHORT_LABELS` — הקיצורים „הזמנות · קבלה ·
  מסמכים" שנכתבו לשורה הצרה — **נמחק ב-28.08.2026** עם הסידור מחדש: הקישורים הפשוטים היחידים
  בגלולה הם „מרכז הבקרה" ו„הזמנה חדשה", ולשניהם אין צורת קיצור. כל יעד אחר יושב בפאנל קבוצה,
  שם ממילא רונדר השם המלא. מפת קיצורים לקישורים שאינם קיימים היא מערכת תוויות שנייה שממתינה
  לסטות מהקטלוג. השם הקנוני מ-`routePresentation` הוא עכשיו השם היחיד בכל משטח.
- **פריט פעיל = גלולה אוקיאנית קטנה** (`bg-action text-white`) **על המשטחים הבהירים**; במגירת
  ה-Onyx הוא **גלולה בהירה** (`bg-shell-ink text-shell`) — הכרעת בעלים 28.08.2026, "שזה יהיה
  בהיר ולא כחול". הקבוע הוא העיקרון ולא הגוון: הסימון הוא מה שמנוגד הכי חזק לרקע שעליו הוא
  יושב, ואוקיאני על Onyx הם שני כהים במרחק נקודות ספורות. הכחול מוביל; שחור נשאר נדיר
  (T7.3, הכרעת בעלים). **אין קישוט מבטא בגלולה**: קו-התחתון הוסר (נקרא כ"צבע לא מוסבר");
  ‏`.section-mark` חי בכותרות העמודים בלבד, וצרכני ה-accent נשארים השניים המוצמדים.
- **קבוצות הנושא** — „רכש”, „מסמכים”, „כספים”, „בקרה ודוחות” — הן disclosure-dropdowns בתוך
  הגלולה: ‏button ‏aria-expanded שפותח
  **פאנל נייר בהיר** (‏T7.3h, בעלים על תמונה ‏#18: "בהיר עם סימון כחול — להפוך את הצבעים";
  ‏`bg-surface` + ‏shadow-menu + ‏ring דק) עם `navLinks` המלאים בדיו כהה, והפריט הפעיל לובש
  את **הגלולה האוקיאנית** — הכחול הוא הסימון, המשטח בהיר. הפאנלים **תמיד ב-DOM** ומוסתרים
  ב-`hidden`; סגירה בלחיצה-בחוץ / ‏Escape (פוקוס חוזר לטריגר) / מעבר מסך. טריגר של קבוצה
  שמכילה את המסך הפעיל לובש את הגלולה האוקיאנית; דיסקת החשבון אוקיאנית, ותפריט החשבון —
  אותו פאנל נייר. **קבוצת „החשבון” אינה על הבר כלל** — דיסקת האווטאר כבר מחזיקה אותה, והדפסתה
  בשני המקומות באותו מסך היא הכפילות שהסידור מחדש בא להסיר.
  **Onyx חי בשלושה מקומות: כרטיס „משימות לפי תפקיד”, מעטפת המובייל, ומגירת הטלפון.**
- ‏`linkCls`/`navLinks` מקבלים פרמטר משטח (`'pill' | 'panel' | 'shell'`). **ענף ה-`'shell'` חזר**
  ‏(28.08.2026, הכרעת בעלים: "לעשות שהתפריט יהיה בצבע השחור של האפליקציה והמילים בהירות"). הוא
  נמחק ב-26.08 כקוד מת — נכון באותו יום, כי המגירה הייתה נייר בהיר מ-T7.3j ואיש לא העביר
  `'shell'`; עכשיו יש לו קורא אחד ויחיד. הוא **אינו** זהה ל-`'panel'`: פאנלי הדסקטופ ותפריט
  החשבון נשארים נייר בהיר בהכרעה משלהם (‏T7.3h, תמונה ‏#18), ומחלקת משטח אחת לשני הרקעים היא
  בדיוק איך אחד מהם הופך לבלתי-קריא. הפריט הפעיל לובש את הגלולה האוקיאנית בכל משטח.

**מובייל (<`lg`) — השפה של הדסקטופ (T7.3k, תמונות ‏#30-35):** טופ-בר ‏`topbar/75`+blur עם
אייקונים עגולים חשופים בדיו כהה (בלי קופסה — "אתה רואה את ההבדלים בשפה?"); מגירה **אטומה**
‏`shell` — ‏Onyx עם מילים בהירות (`shell-ink`), הכרעת בעלים 28.08.2026 — עם הפריט הפעיל בגלולה
האוקיאנית. **אטומה ולא שקופה** נשאר מ-T7.3k: שקיפות שם ישבה על ה-scrim הכהה ונקראה כחלחל;
סרגל הפעולות התחתון ‏`topbar/75`+blur; ‏scrim המגירה והמודאל ‏`shell/50` ניטרלי — לא אוקיאני.
‏theme-color ב־index.html = ‏OCEANIC ‏`#003F47` (וכך גם ב-`public/manifest.webmanifest`; המסמך
טען כאן Onyx עד 19.08.2026 — הקוד אמר אוקיאני מאז T7).

הטופ־בר במובייל **אינו** נושא מבטא: הכותרת שם יושבת על `shell` הכהה, שבו הרמפה מודדת ~2.2:1. במסך
טלפון סמן העמוד שב־`PageHeader` הוא נושא הרמז, והוא בתוך אזור העבודה הבהיר.

מתחת ל־`lg` מעטפת האפליקציה היא phone mode: כפתור התפריט פותח מגירה מימין עם כל יעדי הניווט המורשים,
וסרגל קיצורי דרך ופעולות תפקידי קבוע בתחתית. הסרגל התחתון הוא משטח היברידי מפורש, לא תחליף
לניווט המלא: הוא מחזיר את הגישה הישירה של המוצר ליצירה, לצילום ולקבלה בלי להסתיר מסכים בתפריט. שם הארגון הוא זהות
הדייר, לצד סימן ושם InPlace כזהות המוצר. המגירה ננעלת ל־focus, נסגרת ב־Escape ומחזירה focus
לטריגר. המעטפת משתמשת ב־`100dvh`, ב־logical properties וב־safe-area; תוכן אינו רשאי להרחיב את
רוחב המסמך או להסתתר מאחורי ניווט, בר משימה או overlay.

**שלושת הקווים מתקפלים ל-X, והמגירה נכנסת בהחלקה (26.08.2026, הכרעת בעלים עם רכיב רפרנס).**
הטריגר אינו `Menu` של lucide אלא שלושה `path` שמסתובבים — ‏315°/45°/135° — מפני שמה שנתבקש הוא
ה**מעבר** בין שני האייקונים, ומעבר מחייב שאלה יהיו אותם שלושה nodes בשני המצבים. המצב נקרא
מ-`aria-expanded` של הכפתור דרך `group-aria-expanded:`, ולא מ-prop שני: התכונה חייבת להיות נכונה
ממילא ל-assistive tech, ולכן אייקון שגוי והכרזה שגויה הם אותו באג במקום שניים.
‏**ההחלקה אינה קישוט.** המגירה היא overlay על כל ה-viewport ומכסה את הכפתור שפתח אותה, ולכן
בטעינה מיידית האנימציה התרחשה מאחורי יריעה אטומה ואיש לא ראה אותה. `drawer-enter` (‏280ms,
‏`translate` פיזי ‏+100% — האפליקציה RTL, ו-`start-0` הוא הקצה הימני) ו-`drawer-scrim` הם החלון
שבו האנימציה נמצאת על המסך. **כניסה בלבד:** React מפרק את התת-עץ בסגירה, ואין יציאה לצייר;
ספריית presence עבור פריים אחד של מגירה אינה מוצדקת. שניהם נכבים תחת `prefers-reduced-motion`.

**כל הקבוצות פתוחות בשני המשטחים** — בסרגל הדסקטופי ובמגירת הטלפון כאחד.

**במגירה הקו הוא כל ההפרדה** (הכרעת בעלים 28.08.2026: "אין צורך בפסי הפרדה לשים עוד טקסט —
הפסי הפרדה מספיקים"). הגרסה הראשונה של ההפרדה ציירה קו **וגם** הדפיסה את שם הקבוצה עליו;
השם ירד. השורות מתחתיו כבר אומרות „רכש” בכך שהן ספקים ומחירונים, ולכן הכותרת חזרה על הרשימה
בגופן קטן ואפור וגבתה שורה לכל קבוצה במשטח עם הכי פחות מקום. שמות הקבוצות **לא נמחקו** — הם
עדיין שמות ה-dropdowns בדסקטופ, שם לטריגר מקופל אין מה לומר מלבד שמו.

**התפריט מציג את מה שהמסלול כולל** (הכרעת בעלים 28.08.2026). יעד נמשך מהתפריט **רק** כשהשרת
אומר, במדידה, שהמסלול אינו כולל אותו: `my_entitlements()` היא ההכרעה היחידה, ו-`measured:false`
הוא פער אצלנו ולא „זכאות אפס". תשובה לא-נמדדת, קריאה שנכשלה או קריאה שעדיין באוויר משאירות את
התפריט כמות שהוא — הכיוון ההפוך מ-`assistant/contracts.ts`, שנכשל סגור, מפני ש„לא" שגוי כאן
מוחק יעדים מהתפריט של לקוח משלם בלי שיוכל להבחין בין זה לבין מוצר שבור.

**הכרעת בעלים 26.08.2026, והיא הופכת את מה שנכתב כאן קודם.** הפסקה הזו תיארה חשיפה מדורגת
בטלפון — „ניהול” ו„בקרה” סגורות כברירת מחדל — והקוד מעולם לא עשה זאת. שלושה מקורות סתרו זה את זה:
המסמך תיאר disclosures, ‏`NavSection.collapsible` נשאר מאוכלס ומכוסה בבדיקה **בלי שום מרנדר**,
ו-`layoutActiveState.spec` אסר את האלמנט `<details>` עצמו ולא רק את המצב הסגור. הבעלים הכריע
לטובת הקוד: **הכול פתוח.**

הנימוק, כדי שלא ייבדק שוב: מגירה היא קטלוג היעדים המלא, ויעד שדורש פתיחת קבוצה לפניו הוא יעד
שהוסתר. ‏21 קישורים הם גלילה אחת רצופה — עלות שנמדדת באצבע — בעוד disclosure סגור הוא ניחוש
לגבי היכן משהו יושב. החשבון נוסע בסוף הרשימה הגלילה ואינו מוצמד לתחתית, כך שגם במסך נמוך היעדים
אינם נחתכים.

### סרגל הטלפון, תג המנוי וקבוצת „המנוי” (דיווח בעלים 25.08.2026)

בכותרת הטלפון יש מקום לשלושה סימנים לצד שם המסך ושם הארגון, לא לארבעה. „שליחת הערה” —
הנדיר שבהם — ירד ל**מגירה**, כשורה עם מילה, ובמקומו עומד **תג דרגת המנוי** — לבעלים בלבד,
כמו המסך שהוא פותח. בדסקטופ אין מחסור במקום וכפתור ההערה נשאר בבר. **המלכודת שבמעבר:**
ההערה מצלמת את המסך לפני שהדיאלוג נפתח, ומתוך המגירה הצילום היה של התפריט — בדיוק הכשל שהסדר
הזה נועד למנוע, בלבוש overlay אחר. **הפתרון הוא הוצאה מהצילום, לא סגירת המגירה:**
הפאנל הוא `role="dialog"` ו-`SKIP_SELECTOR` מדלג עליו ממילא, וה-scrim שמאחוריו נושא `data-no-capture`
כדי שלא יצבע את כל התמונה באפור. הניסיון הראשון — לסגור את המגירה לפני הצילום — נראה נכון והיה
שגוי: הרכיב מרונדר **בתוך** המגירה, ולכן סגירתה פירקה אותו באמצע הלחיצה והדיאלוג לא נפתח כלל.
שער הדפדפן תפס את זה כי הוא **לוחץ** על השורה; הבדיקה המקומית רק ספרה שהיא קיימת.

תג המנוי לובש ארבע חזויות בסולם עולה. **הכרעת בעלים 26.08.2026 — הסולם הוא שלוש מתכות:**
`.plan-badge-free` נייר שקט · `.plan-badge-basic` **כסף** · `.plan-badge-pro` **אוקיאני** ·
`.plan-badge-premium` **אוניקס**, עם הניצוץ הנוסע. `business` חוזר על האוניקס — הוא מעל פרימיום
בסולם, ומדרגה שקטה יותר הייתה נקראת כהורדה.

**הזהב יצא מהסולם לגמרי, ולא ירד מדרגה.** בגלגול ראשון באותו יום הוא עבר מפרימיום לפרו; הבעלים
הביט בתוצאה ואמר שהוא „לא תורם לצבעים". הוא צדק: זה היה הצליל החם היחיד במעטפת של אוניקס / חיטה /
אוקיאני, והוא נקרא כמדבקה שהודבקה על המוצר ולא כמדרגה בתוכו. הפסקה שמעל תיארה בשלב מסוים סולם
שכבר לא היה קיים — שתי הכרעות שנכתבו זו על גבי זו — וזה בדיוק הדפוס שהמסמך הזה קיים כדי למנוע.

שלוש המתכות חולקות גיאומטריה אחת — קצה מואר, גוף, קצה מוצל, על ריצה של 135deg — ולכן הדרגות
נבדלות במתכת בלבד, ולעולם לא במשקל. שלושתן חולקות את אותן שלוש בהירויות (89 / 76 / 61), כי צ׳יפ
שיושב בהיר מהצ׳יפ שמעליו נקרא כ**דבר** פחות ולא כ**מסלול** נמוך יותר. מה שמבדיל הוא הגוון:
כסף 271 (כחלחל-קר, כרומה מתחת ל-0.05) · אוקיאני 209 בכרומה 0.075 — התקרה ששומרת עליו כ**מתכת**
ולא כגוון בהיר של כפתור הפעולה, שהוא אותו גוון ב-L 33.7%.

האוניקס הוא היחיד שמתהפך — מילוי כהה עם אותיות `on-solid` במקום מילוי בהיר עם אותיות אוניקס —
וזו בדיוק הסיבה שהוא נקרא **מעל** השלושה ולא כמתכת רביעית לצידם. הקצה המואר שלו הוא לכן הבהיר
מבין שלושת ה**שלבים שלו**, ו-38.8% אינה בחירת טעם אלא התקרה ששומרת על אותיות לבנות ב-AA לאורך כל
המדרוג (נמדד 9.71:1; ‏56.5% היא הנקודה שבה הן מתחילות ליפול).

הקצה המוצל היה 58% ועלה ל-61%: האותיות יושבות על הנקודה האחרונה של המדרוג, ו-58% נמדד 4.19:1 מול
אוניקס — מתחת ל-AA לטקסט של 12px. ‏61% נמדד 4.50:1 (כסף) ו-4.94:1 (אוקיאני), ועדיין עובר 3:1
כגבול. גופי הצ׳יפ נמדדו 8.45:1 ו-8.71:1 — תאומים, בכוונה.

**הניצוץ יושב על פרימיום, לא על פרו** (הכרעת בעלים, אותו יום). נצנוץ הוא מה שאדם קורא כ„היקר
ביותר", ולתת אותו למדרגה האמצעית השאיר את זו שמעליה בלי דבר לומר על עצמה. על מילוי כהה הוא חוזר
לאחוז נמוך — התנאי שלמענו כויל במקור — במקום ל-70% שנדרשו כדי שייראה בכלל על מילוי בהיר.

אותן ארבע המחלקות חוזרות על כרטיסי המסלולים במסך המנוי, כך שהסימן שלוחצים עליו הוא הסימן שמוצאים
שם. המפה חיה במקום אחד (`planTierClass()` ב-`PlanBadge.tsx`) עם fallback אחד — שני עותקים שנחלקו
על ה-fallback הם בדיוק איך שההבטחה הזו נשברת בשקט.

בכותרת הטלפון התג הוא **קישור**, ולכן גובה המגע יושב על הקישור (`.plan-badge-trigger`, ‏44px)
ולא על הצ׳יפ: הגדלת הצ׳יפ עצמו ל-44px הייתה מייצרת תג שנראה ככפתור.

**סולם המסלולים — רשת בדסקטופ, שורות בטלפון, ורק במסך שבהגדרות (הכרעת בעלים 26.08.2026, עם
תמונת רפרנס).** ההכרעה הזו **מבטלת** את הפסקה שנרשמה מוקדם יותר באותו יום ב-`PLAN_LIST` („רשת
נקראת כמדף של שווים"), ומיישבת את שתי הטעויות של הרשת הראשונה: היא הייתה בכל רוחב, והיא הייתה
בשני המשטחים. עכשיו:
- **`/settings/subscription` — `PLAN_GRID`:** ‏`1` עמודה עד `md`, ‏`2` עד `xl`, ‏**`5` מ-`xl`**.
  ‏`xl` ולא `lg` מפני שהחשבון שפסל את הרשת הראשונה עדיין נכון: ב-64rem חמישה מסלולים הם ~11rem
  כל אחד וכל צ'יפ נשבר. עמוד המנוי הורחב ל-`max-w-7xl` (‏80rem) כדי של-`xl` באמת יהיה מה לחלק.
- **הטלפון לא עוצב מחדש:** רשת בעמודה אחת **היא** הרשימה הערומה. אותם כרטיסים, אותו סדר, אותו
  ‏gap — ולכן אין פריסה שנייה שצריך לתחזק בסנכרון.
- **`/pricing` נשאר `PLAN_LIST`.** הוא השוואה, לא מקום שפועלים בו; והוא גם המשטח שאין בו מחיר
  ‏(#206) ולכן אין לו את מה שהרשת בתמונה בנויה סביבו.
- **`PlanCard` בפריסת `grid` שומר על הסידור המוערם בכל רוחב** — כלומר על מה שהשורה ממילא הופכת
  אליו מתחת ל-`lg` — ומוסיף בדיוק שני דברים שעמודה צריכה ושורה לא: ‏`h-full` ו-`mt-auto` על
  הפעולה, כך שחמישה כפתורים מתחת לכרטיסים באורכי טקסט שונים נוחתים על קו אחד.

המנוי הוא קבוצה משלו במגירה (`'המנוי'`, אחרונה, לא מתקפלת) ומסך משלו ב-`/settings/subscription`.
שלוש הקבוצות שמעליו הן העסק שהדייר מנהל; זה החוזה שלפיו הוא מנהל אותו, וקבוצה בת פריט אחד
היא הצורה הכנה לנושא שיש לו מסך אחד. דיסקלוזר מעל פריט אחד הוא דלת עם מכסה.

הגדרות owner נשארות באזור תחתון נפרד. `NAV_SECTIONS` הוא קטלוג המסלולים וההרשאות, לא תפריט שמציג
תמיד את כל מבנה המערכת. `isRouteFamilyActive` משותף לסרגל, למגירה ולמובייל כדי שפרטי רשומה ישמרו
את הקשרם.

### העוזר של InPlace — משטח בדיקה docked (20.08.2026, הכרעת בעלים B)

העוזר הוא **משטח בדיקה תפעולית**, לא chatbot. במובייל נשאר שם נגיש מלא.
אין רובוט, avatar, שם מודל, gradient או typing theatre.

**`Sparkles` — האיסור בוטל (הכרעת בעלים 25.08.2026, אושרה מחדש 26.08.2026).**
השורה הזו אסרה אותו בשמו, ‏`c5604f5` החליף אותו ב-`ClipboardCheck` כדי לקיים את האיסור, והבעלים
ביטל את הפריט הזה בלבד — אבל המסמך לא עודכן, ולכן הקוד והחוקה סתרו זה את זה במשך שלושה סבבים,
וכל סוכן שנגע במשטח דיווח על הסתירה מחדש. **חריגה שאינה נרשמת נבדקת שוב בכל פעם.**
שאר הפריטים ברשימה עומדים בעינם: הם אוסרים על העוזר להתחזות ל**מישהו**, וכוכב אינו אדם.

**האיסור על שם מודל הורחב למשטח המסמכים (הכרעת בעלים 25.08.2026).** „פרטים טכניים” במסך
הבדיקה החזיק שתי שורות — „מנוע חילוץ” ו„מנוע פירוש” — שהדפיסו ספק ומודל ליד החשבונית
של הלקוח. לקוח של מערכת רכש קונה את הקריאה, לא את המכונה שקראה. **מה שנשאר ומדוע:**
מזהה משימה, טביעת מקור, גרסת חוזה וצמד `prompt`/`schema` — כולם מזהים **שלנו** שמאפשרים לתמיכה
לאתר ריצה אחת מדויקת, ואינם נושאים שם מסחרי של איש. שתי השורות התאחדו לשורה אחת „גרסת עיבוד”:
אחרי הסרת שמות המותג, מה שנותר בכל אחת היה מספר גרסה. הנתונים עצמם ממשיכים להיכתב למסד
ולהיות נגישים למפעיל — הם פשוט חדלים מלהיות מודפסים אצל דייר.

**עודכן 24.08.2026 (הכרעת בעלים):** האיסור על **בועות שיחה בוטל**, והמשטח מציג את השיחה כשרשור —
הודעת המשתמש והודעת העוזר, בזו אחר זו, כמו בשיחת טקסט. מה ש**לא** השתנה: התשובה נשארת כרטיס ראיות
ולא פרוזה בבועה. לכל ממצא נשארים ערך, עדכניות ומקור, בכל מקום בשרשור. הודעת המשתמש היא בועה
(`bg-action` בקצה הלוגי `end`); תשובת העוזר יושבת בקצה `start` ומכילה את `AnswerView` המלא.
**תיקון 26.08.2026 — המסמך תיאר כאן משטח שהקוד מעולם לא צייר.** נכתב `surface-sunken` לתשובה,
ומאז שהשרשור נחת התשובה יושבת על `.card` (`bg-surface`). המדרגה הטונאלית שהמשפט התכוון אליה נשמרה,
אבל **הפוך**: גוף השרשור הוא `surface-sunken` — באר שקטה — והתשובה היא כרטיס בהיר שצף עליה.
זה הכיוון הנכון של השניים: כרטיס ראיות אמור להיראות כמונח על המשטח, לא חקוק בתוכו.
שאר האיסורים בפסקה שמעל עומדים בעינם.

**הכרעת סתירה — 26.08.2026.** שתי שורות במסמך הזה סתרו זו את זו במשך ארבעה סבבים: השורה למעלה
אמרה ש-Oceanic מסמן את הפקודה לפתוח בדיקה, ו-§Navigation אומרת שמקבץ הטופ-בר הוא **אייקונים
חשופים בדיו כהה, בלי קופסה**. הקוד קיים את הראשונה, וכך הטריגר היה דיסקה מלאה במקבץ של סימנים
חשופים — ועל הדסקטופ **שתי** דיסקות מלאות זהות, כי דיסקת החשבון גם היא אוקיאנית.
ההכרעה: **המקבץ מנצח.** ‏Oceanic מסמן את פקודת הבדיקה **בתוך הפאנל** — בכפתור השליחה, בבועת
השאלה, בסימון המקור הפעיל — ולא על הטריגר שבסרגל. הנימוק אינו אחידות לשמה: הבעלים דיווח
‏(26.08.2026, צילום הכותרת בטלפון) „יש מספר צבעים צריך לסדר גם את זה", והמקבץ הזה הוא בדיוק
המקום שבו נספרו חמישה טיפולים חזותיים בשורה אחת.
טיעון נגד ראוי לרישום, כי הוא נכון בפני עצמו: הטריגר הוא הפקד היחיד במקבץ ש**מתחיל** משהו
במקום לפתוח מקום. אם אי-פעם יוחזר לו מילוי — זו הסיבה, והיא צריכה להירשם כחריג בשמו.

**מצב ריק, הצעות וצבע — 26.08.2026 (דיווח בעלים: „הצבעים … שייכים לפלטת הצבעים הישנה”).**
שלושת המשטחים הרכים בפאנל — שני סימני ה-`Sparkles` ורשימת ההצעות — לבשו `action-wash`, שהוא
לבן-קריר (‏oklch 96.6% .011 195), על גוף שרשור שהוא נייר חם (`surface-sunken`, גוון 80). שני
לבנים משני צידי הנייטרל באותו ריבוע: מעט מדי כרומה כדי להיקרא כסימן מותג, מספיק כדי להתנגש
בנייר — וזה מה שנקרא כשריד מפלטה אחרת. **ההכרעה:** הסימנים עוברים ל-Oceanic מלא
(`bg-action`/`text-on-solid`) — המותג במקום אחד ובעוצמה מלאה — וההצעות הופכות ל**כרטיסי נייר**
‏(`bg-surface` + `border-line-soft` + `.row-hover`), כלומר לאותה שפה שכל רשימת בחירה אחרת
במוצר מדברת. **מספר ההצעות עלה משתיים לשש לכל תפקיד**, וכל אחת נבדקה מול `requiredRoles` של
הכלי שיענה עליה: רשימת רו"ח הכילה „כמה כסף ממתין לזיכוי?”, שהוא `get_open_credits` והוא
`["owner","office"]` — כלומר המשטח הציע לרו"ח שאלה שהשרת מסרב לה. **הצעה שמובילה לסירוב גרועה
מהיעדר הצעה**, כי הקורא מפרש את הסירוב כתקלה בעוזר.

צבעי status נשמרים ל־partial, ‏failure ומדידה עסקית בלבד.

מ־`lg` הפאנל docked בקצה הלוגי (`inline-end`, שמאל ב־RTL), ברוחב **27.5rem**, מתחיל מתחת לטופ־בר
ואינו modal: אין backdrop, אין נעילת body ואין focus trap. ‏`#main` שומר **29rem** בקצה הזה, כך
שמסך מקור אמיתי אינו נצבע מתחת לפאנל. פתיחת מקור משאירה question/result גלויים ומסמנת את הקישור
הפעיל ב־`surface-selected`. מתחת ל־`lg` אותו משטח הוא `100dvh` full-screen, modal ונעול focus;
פתיחת מקור עוברת למסך המוצר והטריגר משתנה ל־„חזרה לבדיקה”, מחזיר focus ואת אותו run.

האנטומיה קבועה: כותרת InPlace + „קריאה בלבד”; השאלה שנבדקה; `as_of`; הודעת partial שאינה
מתחזית ל־all-clear; answer blocks; Fact עם ערך ו־freshness; source פנימי; „היקף הבדיקה” בתוויות
מוצר עבריות; משוב איכות; composer. ‏`0` ו־`—` נשארים שונים. כל source, next step ו־fallback
מסוננים מול role/route policy לפני render. ‏History מוצגת רק אחרי Edge reauthorization נוכחי;
אין direct browser read לטקסט השמור.

כל פקד הוא 44px לפחות. במובייל focus כלוא רק בזמן שהמשטח פתוח; בדסקטופ Tab רשאי לעבור בין
הפאנל למסך המקור. ‏Escape, close, source-return, reduced-motion, RTL, bidi ו־200% zoom כפופים
לאותם כללים של מעטפת המוצר.

### סיור מוצר — Spotlight

סיור ה־first-run של הבעלים הוא שכבת הדרכה תפעולית, לא שפה חזותית נוספת. ארבעה shields בגוון
`shell` מחשיכים את כל ה־viewport חוץ מהפקד הפעיל; החור אמיתי ולכן קישור או כפתור בטוח נשארים
לחיצים. טבעת `focus` מעתיקה את ארבע פינות היעד ומרחיבה כל radius במרווח ה־spotlight — גלולה
נשארת גלולה וכפתור `rounded-lg` אינו מקבל מסגרת מרובעת. פופובר `surface` ברדיוס modal ובצל `dialog` נושא כותרת,
הסבר, מונה צעדים ופקדי חזרה/הבא/דילוג. אין glassmorphism, צבע חדש או אנימציה דקורטיבית.

הסיור משתמש רק בעוגני `data-tour-anchor` קבועים ובוחר את העוגן הגלוי כאשר קיימות גרסאות desktop
ומובייל. לחיצה אמיתית מותרת רק לניווט ולפתיחת משטח; שמירה, העלאה, אישור וכתיבה עסקית לעולם אינן
צעד אוטומטי. עוגן חסר עובר למצב recovery עם ניסיון חוזר ודילוג במקום לנעול את המסך. ‏Escape
מסיים את הסיור, כל יעד ופקד הוא 44px לפחות, ו־`prefers-reduced-motion` מבטל את settle הקצר.
הטקסט, labels, מונה הצעדים וכיוון החצים קוראים את `html[lang]`; עברית היא RTL ואנגלית LTR.
`LocaleProvider` הוא בעל מצב השפה היחיד — הסיור צופה ב־attribute הקנוני ואינו מחזיק מתג שפה משלו.

### Buttons
Primary אוקיאני · Secondary **טונאלי ללא מסגרת (T7.1)** — ‏`surface-sunken` עם hover ‏`line-soft`;
המילוי והתווית הם האפורדנס, לא קו-מתאר כהה · Danger ‏`alert-solid` (תמיד מאחורי
ConfirmDialog עם סיבה כשנרשם ביומן) · Ghost ‏`ink-soft`. לכל btn טבעת focus, מעבר 150ms ומשוב
לחיצה של 1px; ‏disabled אינו זז ו־busy נשאר עם ספינר בתוך הכפתור.
כל כפתור, שדה קלט ויעד ניווט או פעולה מרכזי הם בגובה מגע של `44px` לפחות, בכל viewport.

**סקאלת הגדלים (26.08.2026).** ל-`btn` היה גודל אחד, ולמוצר היו צריך שלושה — מה שהתבטא ב-32
דריסות ריפוד עם `!` ב-20 קבצים. שתי מחלקות מודיפייר, מוצהרות **אחרי** הווריאנטים כדי שסדר המקור
יכריע: `btn-sm` (‏`px-2.5 py-1 text-xs`) ו-`btn-icon` (ריבוע 44px, בלי ריפוד אופקי).
**אף אחת מהן אינה נוגעת ב-`min-h-11`** — פקד רשאי להיות שקט יותר, לעולם לא קשה יותר ללחיצה.

**סקאלת האייקונים (26.08.2026).** `ICON` ב-`ui.tsx`: ‏`xs`=13 · `sm`=15 · `md`=17 · `lg`=20 ·
`xl`=22 · `hero`=36. לפני כן היו תשעה ערכים בשימוש ואותו מושג הופיע בשלושה מהם — `Trash2`
ב-13/14/15/22, ‏`Plus` ב-12/14/15/16. זו אינה רשלנות אלא היעדר סולם: כשאין מה לנקוב בשם, כל מחבר
בוחר מספר סביר. שלוש המדרגות הראשונות הן שלושת הערכים הנפוצים ביותר כפי שהם, ולכן 258 מהמופעים
הקיימים נוחתים על מדרגה **בלי לזוז פיקסל**. מעבירים `size={ICON.sm}`, לעולם לא מספר חשוף.

### אישור, ביטול ורעש (30.08.2026)
המבחן לחלון אישור הוא שאלה אחת: **האם אפשר לבטל את הפעולה בלחיצה אחת?** אם כן — החלון הוא מס,
והדפוס הנכון הוא לפעול מיד ולהציע ביטול. אם לא — החלון חובה, וההסבר בתוכו חייב לומר מה בדיוק
לא ניתן יהיה לבטל. הבדיקה נעשתה פעם אחת על 48 החלונות: לשניים בלבד יש היפוך נקי.

**הטוסט נושא פעולה, ולכן הוא כבר לא רק הודעה חולפת.** ‏`toast(message, tone?, action?)` — הפרמטר
השלישי הוא `{ label, onAct }`. שלושה כללים שאינם קוסמטיים: הטקסט הוא ה-live region והכפתור הוא
**אח** שלו ולא צאצא, אחרת קורא-מסך מקריא את תווית הכפתור כחלק מההודעה; טוסט עם פעולה חי כ-8
שניות במקום 4, כי 4 שניות חוטפות פקד מתחת ליד של מי שמנווט במקלדת; והטיימר נעצר כל עוד יש hover
או focus. בלי `action` ההתנהגות זהה לקודם, על כל 345 הקריאות הקיימות.

**המחיר מוצהר:** ביטול הוא פעולה שנייה ביומן, לא מחיקה של הראשונה. היומן מראה השבתה ואחריה
הפעלה — וזו האמת. ביטול שמוחק את העקבה היה שקר גרוע יותר מהחלון שהוסר.

**‏Danger מאחורי ConfirmDialog — הכלל צומצם.** הניסוח הקודם בסעיף Buttons נכתב כשלא הייתה חלופה
לחלון. הוא נשאר בתוקף לפעולה **בלתי הפיכה**; לפעולה הפיכה החלופה היא פעולה מיידית עם ביטול.
הכלל שלא זז: פעולה בלתי הפיכה לעולם אינה מאחורי hover בלבד.

**תיבת סיבה אינה חוסמת** — הכרעה #299. היא מסומנת ברשות, ‏`reasonOr` כותב ליומן משפט שנושא את שם
הפעולה. החריג היחיד הוא שדה שהמוקלד בו הוא **תוכן שאדם אחר קורא** — מה צריך לברר בפתיחת חריג, מה
סגר את המעקב — ושם התווית שואלת את השאלה האמיתית במקום לבקש סיבה.


### DataTable — כולל מובייל
שולחן העבודה של המערכת. דסקטופ: **‏thead = ‏`.table-head` — פס **אוקיאני** עם אותיות בהירות,
בכל טבלה במערכת** (T7.3b קבע פס כהה אחיד; ‏T7.3g, תמונה ‏#15: ‏Onyx→‏`action` — "שנה את הפס
השחור לכחול"; ‏shell-ink-soft על action ‏8.13:1; בהדפסה חוזר לנייר). שורות `divide-line-soft`, ‏hover
‏`.row-hover` (כולל `active:` כאפורדנס מגע), מיון בכפתורים אמיתיים עם aria-sort.
**מובייל (`mobile="cards"`):** מתחת ל-**`lg`** הטבלה מוסתרת ובמקומה רשימת כרטיסים נגישים.
‏**הגבול אוחד ל-`lg` בהכרעת בעלים (09.08.2026), וזה תיקון ולא העדפה:** הטבלה התחלפה ב-`md`
(‏768px) בזמן שהמעטפת — סרגל צד, כותרת טלפון, סרגל פעולות — מתחלפת ב-`lg` (‏1024px). התוצאה הייתה
שבין 768 ל-1023 פיקסלים המשתמש קיבל **את טבלת הדסקטופ בתוך מעטפת הטלפון**, כלומר טבלה עם
`whitespace-nowrap` גלובלי בתוך מסגרת עם מגירה וסרגל תחתון — טאבלט במאונך. שער הדפדפן לא מדד את
הרוחב הזה (הוא בודק 320/360/390/430). שני הגבולות הם `lg` עכשיו, וכל פיצול כרטיסים↔טבלה בקוד
מיושר אליו: ‏`DataTable`, ריכוז ההוצאות ופריטי ההזמנה.
- ‏`Column.priority`: ‏1 כותרת · 2 פרטים (ברירת מחדל) · 3 מוסתר במובייל.
- ‏`Column.mobileLabel`: תווית לפני הערך; ‏`null` = ערך מדבר-בעד-עצמו (badge, כסף).
- ‏`mobileTitle(row)` — שורת כותרת; ‏`mobileTrailing(row)` — ‏badge בקצה; שורה לחיצה = ‏button ‏44px+.
- חיפוש/סינון/עימוד משותפים לשתי התצוגות; מיון אינו נגיש במובייל (v1, מתועד).
- ברירת המחדל היא `mobile="cards"`; ערכים בכרטיס נשברים לשורות ואינם נחתכים ב־ellipsis.
- `mobile="scroll"` שמור לבחירה מפורשת בתצוגות מטריצה/preview צפופות שבהן משמעות העמודות מחייבת זאת.
- במסך החשבוניות הטלפון חושף כ־quick filters רק „הכל”, „התקבלה”, „ממתינה לאישור” ו„דורשת בירור”.
  „בבדיקה” ו„מאושרת” נשארים ב„סינון ותצוגה”; אם אחד מהם פעיל מקישור או מהגיליון, השבב שלו חוזר
  להופיע. אין סטטוס עסקי שנמחק — זו חשיפה מדורגת, לא שינוי מסלול.

בדוח החודשי טבלאות החשבוניות, התשלומים והזיכויים מוצגות ככרטיסי ledger עד `xl`; הטבלאות המלאות
נשמרות לדסקטופ רחב ולהדפסה בלבד. כך אין גלילה אופקית פנימית ב־320/390/768/1024 ואין אובדן נתון.

### ActionMenu — תפריט פעולות שורה
טריגר `MoreVertical` ריבועי בכפתור ghost (‏44px+), נפתח כ־portal ל־body כדי שלא ייחתך ע״י ה־overflow של
DataTable, וממוקם מהקצה הלוגי של הטריגר (RTL-aware). המשטח ישר וללא עיגול פינות: `bg-surface + border-line` עם טוקן
הצללה ייעודי `--shadow-menu` — פטרול עדין בין `shadow-card-hover` לרובד הטוסט/מודאל. ניווט מקלדת
מלא (roles menu/menuitem, חיצים/Home/End/Escape), סגירה בגלילה ובלחיצה בחוץ. פעולה הרסנית =
`text-alert-fg`, גלויה תמיד בתפריט — לעולם לא מוסתרת מאחורי hover (חוק החשיפה המדורגת).
מחובר ל־DataTable דרך `rowActions` — עמודת קצה בדסקטופ, קצה לוגי של הכרטיס במובייל.

### ניווט מובייל, קיצורי דרך ופעולות
מתחת ל־`lg` מגירת ההמבורגר מציגה את כל היעדים המורשים לתפקיד. בנפרד ממנה, סרגל הפעולות המהירות
מחזיר את המסלול הישיר שנבחר למוצר: `owner`/`office` מקבלים חמישה יעדים — „הזמנה חדשה”, „מרכז
הבקרה”, „צילום מסמך”, „קבלת סחורה” ויעד מסמכים מורשה. אצל `owner` היעד הוא „בקרת מסמכים”;
אצל `office` הוא „מסמכים”. **הצילום הוא הפריט השלישי והמרכז הפיזי של הסרגל**
גם ב־390px; ה־puck משתמש ב־logical inset וב־`margin-inline: auto`, לא בהיסט ידני שתלוי בכיוון.
‏„חשבונית חדשה” אינה בסרגל: האפליקציה מקבלת חשבוניות ספק ואינה מנפיקה אותן. `accountant` מקבל
„מרכז הבקרה”, „חשבוניות” ו„תשלומים לביצוע”. **השם המלא, ולא „תשלומים”:** הסרגל של ה-`accountant`
הדפיס „תשלומים” ל-`/pay`, וזה שמו הקנוני של `/payments` — שני מסכים ומילה אחת, אצל התפקיד היחיד
שההבדל ביניהם הוא עבודתו. תוויות הסרגל נגזרות מ-`routePresentation`, שקורא ל-`/pay`
„תשלומים לביצוע” מאז ומתמיד. התפקידים ההיסטוריים `kitchen`/`payer`/`supplier` אינם
חשבונות מוצר ואינם מקבלים סרגל. צילום מסמך פותח את מצלמת/בורר הקבצים הילידיים.

השם הנגיש של הסרגל הוא „קיצורי דרך ופעולות”: הוא מכיל במכוון גם פקודת צילום וגם קישורים תכופים.
ההגדרה הזו מונעת ממנו להתחזות לניווט ראשי, בזמן שהמגירה נשארת קטלוג הניווט המלא.

הסרגל מכבד את `safe-area-inset-bottom`, והמעטפת שומרת עבורו מרווח תוכן לפי **מלוא מעטפת ה־puck**
(גובה הסרגל ועוד 1.5rem), כך שגם הכיפה המורמת אינה מכסה את קצה הרשומה האחרונה.
הסרגל בנוי grid לפי מספר הפעולות ואינו יוצר גלילה אופקית גם ב־320px וב־zoom. כל יעד מגע הוא 44px לפחות, לכל פעולה שם נגיש,
ותווית גלויה נשברת לשתי שורות לכל היותר. הסרגל נשאר מלא גם במסכי יצירה וקבלה; כל מסך כזה מגן על
הנתונים לפני ניווט. בקבלת סחורה בר המשימה נערם מעליו. צילום במסך ממוקד אינו מעביר אוטומטית
למסך בדיקת המסמך. במובייל אין FAB או speed-dial נוסף.

ב־320–359px סימן הבית שבמרכז הטופ־בר מוסתר חזותית כדי שכותרת המסך לא תקרוס; זהות InPlace
נשארת בשורת הטקסט ובמגירה, ויעד הבית המלא נשאר בראש המגירה. שלוש פעולות הטופ־בר מאוחדות למקבץ
אחד, וכל אחת נשארת 44px ושומרת שם נגיש. ב־landscape שגובהו עד 480px גובה הטופ־בר והסרגל מצטמצם,
ה־puck יורד ל־48px והתוויות נשארות על שורה אחת — ללא הקטנת יעד מגע מתחת ל־44px.

טוסטים והודעת עדכון PWA חיים באותה ערימת overlays מעל הסרגל. כאשר `phone-taskbar` קיים, הערימה
מורמת מעליו ואינה מכסה את פעולות השמירה/שליחה; בדיקת הדפדפן מודדת את המלבנים בפיקסלים. ב־`sm`
ומעלה הודעת ה־PWA נצמדת ל־inline-start. (היא נצמדה לשם כדי להתרחק מה־FAB הדסקטופי; ה־FAB הוסר,
ההצמדה נשארה — היא נכונה בזכות עצמה, כי היא מרחיקה את ההודעה מסרגל הפעולות התחתון.)

**ב־`lg` ומעלה אין משטח פעולות מהירות בכלל — ה־speed-dial הוסר בהכרעת בעלים (09.08.2026).**
שלוש סיבות: הוא **שיכפל** פעולה שכל דף כבר נושא בכותרת שלו; הוא הציע „צילום מסמך” — מצלמה — במחשב
שולחני; והוא ישב צמוד לפינה בלי inset והתנגש ברצועת הכותרת. איתו הוסרו הטריגר, תפריט ה־roving-focus,
העברת ה־focus בין שני משטחים, מאזין `matchMedia` שסגר את התפריט בחציית `lg`, ו־`.phone-fab`
ו־`.speed-dial-*` ב־`index.css`. ‏`--shadow-fab` **נשאר** — ה־puck המורם של המצלמה בסרגל המובייל
משתמש בו.

**מה שאסור להסיק מזה:** שהפעולות המהירות מיותרות. במובייל הסרגל התחתון הוא משטח יחיד וישיר והוא לא
נגע. ההסרה נשענת על כך שבדסקטופ הפעולה **כבר על המסך** — ביום שדף יאבד את הפעולה הראשית שלו,
הנימוק הזה נופל.

### PageHeader / RecordHeader / LifecycleStrip

`PageHeader` הוא אנטומיית ברירת המחדל למסך תפעולי: breadcrumbs אופציונליים, `h1`, מטא־מידע
שמוסיף הקשר ופעולות. אין כותרת שיווקית גדולה ואין subtitle חסר משמעות. `RecordHeader` מוסיף זהות
רשומה, סטטוס, עובדות ופעולה ראשית אחת; פעולות משניות נשארות שקטות יותר.

שניהם מרנדרים **סמן אזור** מתחת ל־`h1` — ‏`.section-mark`, ‏rule של 28×3px במבטא התחום. **אין לו
prop**: הוא קורא את `--section-accent`, ש-`Layout` בלבד קובע ורק מן ה-URL, ולכן אין דרך שקורא
יגרום לו לומר „מאושר” או „באיחור”. במסך ללא תחום עבודה הוא `display:none` ואינו תופס מקום.

`LifecycleStrip` מציג רק שלבים אפשריים שהמסך יכול להסיק מהחוזה הקיים. הוא מדגיש מצב נוכחי ו„הפעולה
הבאה”, אך אינו ממציא שלב טכני או פיננסי. מצב טעינת פרטים משתמש ב־`RecordSkeleton`, לא בספינר שמוחק
את צורת המסך.

לרצועה שלושה מצבים אופציונליים שנוספו עם רצועת עיבוד המסמך, וכולם כפופים לאותו כלל:

- **`failed`** — השלב הנוכחי נעצר. הסמן שלו עובר ל־`alert-*` עם `AlertTriangle`, וה־sr-only אומר
  „השלב שנעצר”. השלבים שלפניו נשארים `done-*`: הם באמת הושלמו, וכשלון מאוחר אינו מוחק אותם.
- **`detail`** — שורה אחת מתחת לרצועה שמתארת מה השלב הנוכחי עושה, במילים של הקורא.
  **השורה הזו והבר אינם מופיעים יחד על אותו נתון** (הכרעת בעלים 25.08.2026): כשיש ספירה
  אמיתית — הבר מחליף את המספרים והשורה נשארת ריקה; המשפט העברי ממשיך להימסר
  דרך `aria-valuetext` של הבר, שזה המקום שבו פס דטרמיניסטי אמור לשאת אותו. כשאין ספירה — אין בר,
  ולכן השורה היא היחידה שיכולה לומר מה חסר, והיא חייבת לומר זאת.
- **`progress`** — בר דטרמיניסטי לשלב הנוכחי. **מוצג אך ורק כשיש ספירה אמיתית מהשרת.** אין בר
  ריק ב־0%, אין בר אינדטרמיניסטי בלולאה, ואין אחוז מחושב מזמן שחלף — שלושתם טענות על העבודה
  שהנתונים אינם תומכים בהן, וזו בדיוק „אנימציה שאינה מצב”. בהיעדר ספירה מוצג טקסט שאומר שהמספר
  טרם דווח. המילוי הוא `action` **כמבנה בלבד**: הוא אומר כמה, לא אם זה הצליח.
  ‏`role="progressbar"` נכתב עם `aria-valuenow/valuemax` רק כשהם אמיתיים, ו־`aria-valuetext` נושא
  את המשפט העברי.

הבר של הרצועה סופר עמודים (`עמוד 7 מתוך 27`) ולא אחוז — אחוזים נשארים בתוך „פרטים טכניים”
לפי החלטת מסך הבדיקה. עד 25.08.2026 המשפט הזה גם **נכתב** מתחת לרצועה וגם הוזן לבר;
הבעלים ראה אותו פעמיים במסך אחד והכריע שהבר מחליף את המספרים ולא מתלווה אליהם. הספירה עצמה
לא ירדה מהמוצר: `DocumentStatusBadge` ממשיך להציג אותה במסכים שאין בהם רצועה.

**הרצועה נשברת לשורות במובייל, ואינה נגללת אופקית.** ארבעה שלבים אינם נכנסים ל־390px: המימוש הקודם
היה `flex` ללא `flex-wrap` עם `overflow-x-auto`, ונמדד ב־**169px חתוכים ושני שלבים מתוך ארבעה
גלויים** — בלי צל, בלי חץ, בלי שום סימן שהתהליך ממשיך מעבר לקצה. גלילה שאינה נראית אינה גילוי:
היא מסתירה מהבעלים ששלב „מוכן לבדיקה” בכלל קיים. הרצועה עוברת `flex-wrap` עם `gap-y`, ו־`flex-1`
נשאר מ־`sm` ומעלה בלבד כדי שהדסקטופ לא ישתנה. אחרי: **0 חתוך, 4 מתוך 4 גלויים, 0px גלישה אופקית**.

(הפסקה שעמדה כאן תיארה את עיגון מעטפת ה־speed-dial ל־`inset-block-start`. היא הוסרה עם המשטח.)

שני המצבים מוסתרים במסכים ממוקדים שכבר מחזיקים פעולה ראשית — `/orders/new`, ‏`/invoices/new`, ‏`/receiving/:orderId` — כדי שלא יתחרו בה.

### התראות חדשות
פעמון ריבועי בגודל מגע 44px חי בכותרת ומוביל ישירות ל־`/alerts` — בלי dropdown. תג המספר מציג
רק רשומות `notifications` ש־`read_at` שלהן ריק, ומוגבל חזותית ל־`99+`. פתיחת מסך ההתראות מסמנת
אותן כנקראו רק אחרי שהסריקה החיה הסתיימה בהצלחה; תקלה בטעינה אינה מנקה עבודה שלא נצפתה.
מסך ההתראות הוא תור הפעולות המלא ואינו משכפל את כרטיסי הסיכום העסקי של הדשבורד. הגדרת Push
למכשיר חיה במסך זה בלבד, משום שהוא נגיש גם ל־owner וגם ל־office.

### מסמכי חשבונית
מסמכים ישירים של החשבונית ומסמכי קבלות הסחורה המקושרות מופיעים ברשימה מאוחדת אחת עם מפרידי שורה,
תצוגה מקדימה, מקור ותאריך. אין גלריות מקבילות ואין כרטיס בתוך כרטיס. העלאת PDF/תמונה נשארת פעולה
גלויה; הסרה זמינה רק למסמך הישיר ובהרשאה מתאימה, דרך ActionMenu.

### הזמנה חדשה וריכוז הוצאות
הזמנה חדשה היא תהליך אמיתי בן שני שלבים: בחירת מוצר עם בורר כמות inline, ואז בחירת ספק וסיכום
הספק הזול ביותר לכל מוצר. ההשוואה היא רשימת ledger עם מחיר נבחר, מחיר זול והפרש — לא כרטיסי הצעה.

**פס השלבים — שני רינדורים, ניווט אחד.** רצועת הצ'יפים המילולית צריכה מקום לשלוש תוויות; ברוחב
טלפון היא נשברה לשתי שורות והגלולה `rounded-full` עם `shadow-card` הפכה לאליפסה מטושטשת (דיווח
בעלים). לכן `<nav>` אחד מכיל שני רינדורים מאותו מערך שלבים יחיד — מספר, תווית, כלל זמינות
ומטפל־מעבר: בטלפון (`sm:hidden`) שלוש נקודות `size-11 rounded-full` ממוספרות — פעילה
`bg-action text-on-solid`, האחרות `bg-action-wash text-ink-mid` — עם התווית כ-`sr-only`, ואחריהן
תווית השלב הנוכחי במילים עם `truncate`; מ-`sm` ומעלה (`hidden sm:flex`) רצועת הצ'יפים בגלולה,
ללא שינוי. אין רקע, גלולה או צל ברינדור הטלפון — שלושה יעדי מגע ותווית אינם יכולים להישבר ב-320px.
‏`hidden` הוא `display:none`, ולכן בדיוק רינדור אחד נמצא בעץ הנגישות בכל רגע: אין `aria-hidden`,
אין landmark כפול ואין הקראה של שישה כפתורים. ‏`aria-current="step"` יושב על שניהם.

**בחירה אינה השלמה.** תיבת הבחירה של מוצר לובשת את משפחת הפעולה (`border-action bg-action
text-on-solid`) ולא את משפחת done — שורת המוצר כבר נושאת `bg-surface-selected` („אפשרות שנבחרה”),
וזה אותו ניב של `.chip-filter-active`. המשמעות אינה נשענת על גוון: הסימון מופיע רק כשהמוצר בסל,
‏`aria-pressed` יושב על הכפתור, ה-`aria-label` שונה לפי מצב, ובורר הכמות קיים רק לשורה שנבחרה.
‏`colorLanguage.spec.ts` אוכף שאין טוקן `done-*` ב-`ProductStep.tsx`.

**רדיוס במסך הזה — כלל אחד.** כל דבר שלוחצים עליו מקבל `rounded-lg` (תיבת הבחירה, קבוצת בורר
הכמות — עם `overflow-hidden` כדי שהילדים הריבועיים לא יבצבצו — וכפתור המחיקה של שורה בשלב 2).
רצועות העבודה `border-y … bg-surface` בשלושת השלבים נשארות ישרות, וכך גם כותרת הקבוצה
‏`bg-surface-sunken` בשלב 3: פינה עגולה הייתה הופכת אותן לכרטיסים — תפקיד שכבר שייך
ל-`.card`/`rounded-3xl` — ופותחת מרזבי canvas בקצה הטלפון.

בריכוז הוצאות טבלת הספקים היא המקור הראשי והמדויק. פירוט קטגוריות הוא `<details>` משני וסגור
כברירת מחדל, ומוצהר בו שהסכומים נגזרים ממחירי snapshot של הזמנות מקושרות. אין תרשים donut שמציג
פילוח משוער כאילו היה סכום חשבוניות מדויק.

### Dashboard — פריסת הרפרנס (T7)

מ-`lg` ומעלה הדשבורד הוא **grid של 12 עמודות** לפי רפרנס הבעלים: פס הכסף כשורת גיבורים ברוחב
מלא, מתחתיו כרטיס "דורש טיפול" (6) לצד כרטיס האספקות (3) לצד **כרטיס התפקידים הכהה** (3),
ומתחתיהם כרטיסי המגמות — עמודות (5), ‏donut ‏(4) וטבעת הכיסוי (3), ואז ה-comparison ברוחב
מלא. מתחת ל-`lg` נשארת עמודה אחת עם הכסף ראשון. סדר ה-DOM לא זז —
שער הדפדפן מצמיד את סדר הכותרות — והמיקום הוא `order` בלבד.

**הקפל הראשון במובייל, ולמה „‏order בלבד" הוא כלל ולא סגנון (31.08.2026).** מתחת ל-`lg` שלושת
הדברים שמנהל צריך בעשר שניות יושבים מעל הקפל: **מספר כספי אחד · „דורש טיפול" · „התראות שלא
נקראו"**. גרף אינו ביניהם. כל אזור מצהיר עכשיו **שני** מספרי `order` — אחד למובייל ואחד מ-`lg`
ומעלה — במקום להסתמך על ברירת המחדל `0`: ברגע שאזור אחד מבקש `order-2`, כל מי שנשאר על ברירת
המחדל קופץ **לפניו**, ולא ביניהם.

**„התראות שלא נקראו" נכנס ל-DOM אחרי האספקות ומופיע לפניהן, וזו אינה קפריזה.** ‏`check-browser-smoke.cjs`
ו-`dashboardDensity.spec` מצמידים את `.dash-enter h2` **לפי מיקום** — „דורש טיפול" ראשון,
„אספקות היום ומחר" שני. בלוק שנושא `h2` **ביניהם** היה מפיל שער עובר מסיבה חזותית בלבד. הוספה
אחרי האספקות שומרת על שני האינדקסים, ו-`order` עושה את העבודה החזותית. **מי שיסדר את ה-JSX
„לפי מה שהעין רואה" ישבור את השער** — ולכן יש על זה בדיקה.

**והשם הוא „התראות שלא נקראו", לא „מה חדש מאז הביקור".** ‏`notifications.read_at` הוא קבלה על
**התראה**, לא על **מסך**: מי שפתח את הפעמון אתמול ולא נכנס לכאן לא היה רואה כלום, והתראה בת
שבועיים שלא נקראה הייתה מוצגת כאילו קרתה „מאז הביקור". הכותרת אומרת בדיוק את מה שהנתון יודע.
הבלוק **קורא בלבד** ואינו מסמן „נקרא" — ‏`/alerts` מאשר כי הוא מציג את השורות במלואן; אריח סיכום
שהיה מנקה את הפעמון היה מנקה ספירה של שורות שהקורא לא פתח. **אפס אינו מרונדר**: אין „‏0 חדשות".

**ברכה ככותרת (T7.1).** ‏h1 של הדשבורד הוא „בוקר טוב / צהריים טובים / ערב טוב, {שם פרטי}”
(שעת העסק, המילה הראשונה של `full_name`; שם ריק — כמו בבוטסטרפ אופליין — מחזיר „מרכז הבקרה”
נקי). שם המסך „מרכז הבקרה” אינו זז מהקטלוג: הוא שורת ה-meta מתחת לברכה, תווית הניווט,
וכותרת הדפדפן דרך `routePresentation.ts` — הברכה לעולם אינה מנתקת את ההתמצאות. ברכה היא
תוכן, לא נתון — אין כאן טענת מדידה.

**הכרטיס הכהה — "משימות לפי תפקיד".** משטח ה-Onyx היחיד בדשבורד (`bg-shell`, ‏rounded-3xl):
מספר גיבור של סך המשימות ושש שורות תור עם ספירות. הוא **ההעברה** של נושא התורים מהקיפול
התפעולי, לא שכפול שלו. צ'יפ הספירה שם לובש `bg-shell-ink/10` — הרינדור על-כהה של אותה טענה
ניטרלית ("כמה", לא "כמה דחוף"); צ'יפ הפטרול הבהיר אינו קריא על Onyx.
**מטריצת הנקודות (T7.2):** כל שורת תור נושאת גם רצועת נקודות — נקודה מלאה לכל משימה פתוחה,
עד תקרת 12 ואז "+" — הרינדור של הרפרנס על נתון אמיתי: המספר עצמו מצויר, לא קישוט. הנקודות
‏aria-hidden; הספירה המספרית נשארת המקור הנגיש. בפינת הכרטיס צ'יפ-חץ עגול (`bg-shell-ink/10`)
אל `/alerts` — ה-affordance של הרפרנס, עם יעד אמיתי.

(קפסולות הדופק חיו כאן סבב אחד — T7.3 — והוסרו בהכרעת בעלים: "לא רלוונטי". ‏`.pill-hatched`
נשאר אחריהן ב-index.css כמרקם זמין בלי צרכן, ונמחק ב-19.08.2026: כלל CSS שאיש אינו קורא הוא
טענה שהמערכת עושה משהו שהיא אינה עושה. אם המרקם יידרש שוב, הוא שבע שורות.)

### Dashboard — פס הכסף
שלושת מדדי הכסף = **שלושה כרטיסי stat נפרדים (T7.3c)** — בטלפון הם נערמים כשלושה אריחים
(גוש ממוזג נקרא כבאג — דיווח בעלים), בדסקטופ הם שורת ה-stat-cards של הרפרנס. כל BandStat
הוא `card card-link-hover` משלו. הערך בגודל `kpi-hero` **במשקל 500**.
האנטומיה הקבועה של כל BandStat היא:
אייקון שטוח `ink-muted` (‏T7.2 — בלי קופסת chip) · תווית · ערך סופי מיידי עם tabular figures · הקשר תקופה · delta אופציונלי מול אותם
ימים בחודש הקודם. ל־"נרכש" ול־"שולם" בלבד נוסף ספארקליין קומפקטי של שמונה שבועות, ורק כשהערך
הראשי קיים ולפחות שני שבועות מכילים פעילות אמיתית. "יתרת חשבוניות" מציגה את מספר החשבוניות הפתוחות
בלבד — לעולם לא ספארקליין או מגמה מומצאים. אין טקסט הסבר ארוך בפס; במובייל כל BandStat הוא שורה
קומפקטית וההשוואה נשארת גלויה.

**מיקום הפס: ראשון על המסך בכל רוחב (T7).** בטלפון הוא היה מתחת לשני כרטיסים, כך שהקיפול
הראשון ב־390 לא הכיל אף מספר כספי — סעיף 12 דורש שמנהל שנכנס יראה מצב, לא רק רשימת משימות.
מ-`lg` הוא שורת הגיבורים של grid הרפרנס. המנגנון הוא **CSS `order` בלבד** — סדר ה־DOM אינו זז, גם כדי שקוראי מסך
וטענת סדר הכותרות בשער האיכות ימשיכו לראות את "דורש טיפול" ראשון. מכיוון ש־`order` מזיז רק ילדי
flex/grid, מכל האזורים הוא `flex flex-col gap-5` ולא `space-y-5`.

### Dashboard — טיפול ופירוט תפעולי
כותרת AttentionZone נושאת את הספירה **פעם אחת ובאלמנט אחד**: „דורש טיפול היום” עם `badge-*` של
מספר סוגי הטיפול, והמשבצת בקצה נושאת רק את חשיפת ה־₪ — עובדה אחרת. הפעמון והתג לוקחים את הטון
מהשורות שהם סופרים (`alert` גובר על `await`), ובאפס שורות הפעמון כבה ל-`ink-ghost`: פעמון כתום
קבוע מעל „אין משימות דחופות כרגע” הוא צבע שטוען את היפוכו של המשפט שלידו. יחידת המידה („סוגי
טיפול”) נשארת בשם הנגיש. ‏`Dashboard.tsx` החזיק עותק שלישי של אותה ספירה בשורת ה־`meta` שלו; הוא הוסר.

ב־AttentionZone פעולות `alert`/`await` גלויות תמיד, **בעמודה אחת בכל רוחב (T7)** — הכרטיס יושב
עכשיו באריח של חצי רוחב בגריד הרפרנס, ושתי עמודות של תוויות עבריות ארוכות שברו כל שורה. שורות
לידיעה, מדדים ללא נתונים ומצבים תקינים מאוחדות תחת `<details>` טבעי וסגור. כל קישור ושורת summary הם
יעד מגע של 44px לפחות, עם focus-visible ו־active גלויים.

**גוף הכרטיס לעולם אינו ריק.** שלושה מצבים ממצים: שורות פעולה; אחרת — כשמדד כלשהו אינו ניתן
למדידה — משפט **ניטרלי** („אין משימות דחופות מבין המדדים שנמדדו”), מפני ש־all-clear ירוק הוא טענה
שאיננו רשאים לטעון כשחלק מהתמונה חסר; אחרת המשפט הירוק. ארגון חדש נוחת בדיוק במצב האמצעי — שני
מדדי התשלומים חוזרים `null` מ־`management_dashboard_snapshot` — ולכן הוא היה המצב היחיד שרונדר
כשום דבר.

שלושת אזורי הפירוט — חריגים, התייקרויות ויתרות ספקים — חיים בכרטיס אחד בשם "תמונת מצב
תפעולית" (T7: "משימות לפי תפקיד" עברו לכרטיס הכהה — ראו "פריסת הרפרנס"). כל אזור הוא disclosure
עם count והנתון העסקי המרכזי בשורת ה־summary; פתיחת אחד סוגרת את
האחרים באמצעות `name="dashboard-operations"`. אזור ריק מוצג כשורת all-clear שאינה אינטראקטיבית,
והרשומות והקישורים נשמרים בתוך האזור הפתוח.

### מסוף התפעול — שלד שני, שפה אחת (28.08.2026)

‏`src/operator/` הוא אפליקציה שנייה (‏`operator.html`, ‏bundle נפרד) ולכן היא **אינה** משתמשת
ב-`Layout`: ניווט מודע-תפקיד, רצועת „דורש טיפול" של הדייר, מוני תיבת דואר ופעולות מהירות אינם
אומרים דבר למפעיל חוצה-דיירים, וייבוא שלהם היה גורר את כל שלד הדייר ל-bundle של הקונסולה.
מה שכן משותף הוא **השפה**, כי מוצר שיש בו שתי אפליקציות ולכל אחת כרום משלה נקרא כשני מוצרים.

‏`OperatorShell` מדבר את T7.2 מילה במילה: קנבס חיטה עם כתם הסמן היחיד, ‏header שקוף דביק
‏(`bg-canvas/70 backdrop-blur`) ולא בר, גלולת לוגו מנייר בקצה ההתחלה, קפסולת הניווט הלבנה
‏(`rounded-[1.625rem]`, ‏`bg-surface/90`, ‏`shadow-card`, ‏`ring-line-soft`) עם היעד הפעיל
בגלולה האוקיאנית, ודיסקת חשבון אוקיאנית בקצה הסוף. מתחת ל-`lg` — טופ-בר `topbar/75`+blur עם
אייקונים עגולים חשופים, שלושת הקווים שמתקפלים ל-X, ומגירה **אטומה** ‏`bg-topbar` מקצה ההתחלה
עם `drawer-enter`/`drawer-scrim`, ‏Escape והחזרת פוקוס.

**חלוקת היעדים היא של המגירה, לא של הגלולה (28.08.2026).** תשעת היעדים יושבים בארבע קבוצות —
ללא-שם (מרכז בקרה) · „לקוחות" · „משתמשים והרשאות" · „תפעול המערכת" — והחלוקה מרונדרת **במגירה
בלבד**, עם כותרת `text-xs font-semibold text-ink-muted` וסמל `ICON.md` לכל שורה, בדיוק כמו
‏`sidebar()` של הדייר. הגלולה נשארת טקסט בלבד וללא קבוצות: הנימוק ל-dropdowns בשלד הדייר הוא
ש-19 יעדים אינם נכנסים לשורה, וכאן תשעה כן — disclosure מעל משהו שכבר גלוי הוא דלת עם מכסה.
תוויות ארוכות מקבלות `short` **לגלולה בלבד** (הרשמות · צוות · פלטפורמה · אוטונומיה · מחיקה),
אותו חוזה של `NAV_SHORT_LABELS`, כך שהמגירה וכותרות המסכים אינן יכולות להתנתק ממנה. בתחתית
המגירה בלוק „החשבון" עם הכתובת וההתנתקות — נוסע עם הרשימה ולא מוצמד לתחתית, מאותו נימוק
שמופיע ב-`Layout`.

**שני צרכנים חדשים, שניהם בשם:**
- ‏`useGlowPointer` יצא מ-`Layout` ל-`src/lib/glowPointer.ts`. האטמוספרה היא תכונה של הקנבס של
  המוצר, לא של שלד הדייר; שני עותקים של אפקט **שנמדד** (‏0.12s, ‏17%) הם הדרך שבה שני השלדים
  מתחילים להיפרד קבוע אחד בכל פעם.
- ‏`dashboard-depth` נצרך גם במרכז הבקרה של הקונסולה. הסייג בסעיף 4 עומד בעינו: זהו **מרכז בקרה**,
  לא כרטיס תפעולי, וזה הצרכן השני והאחרון עד להכרעה נוספת.

**מרכז הבקרה של המפעיל נושא את אותה אנטומיה** של מרכז הבקרה של הדייר: פס המצב ראשון בכל רוחב
‏(אותו נימוק של „פס הכסף" — מי שנכנס רואה מצב, לא רק רשימת משימות), ומתחתיו `AttentionZone`
לצד **משטח ה-Onyx היחיד של המסך** — „צוות הפלטפורמה" ב-`bg-shell rounded-3xl shadow-dashboard`,
עם מספר גיבור, שורות תור וצ'יפ ספירה, וצ'יפ-החץ העגול בפינה — אותה צורה של „משימות לפי תפקיד".

### הרכיבים המשותפים — מה שקיים, ולמה חסר הוא סטייה (26.08.2026)

רכיב שחסר אינו „פחות קוד" — הוא **הוראה לכל מסך להמציא לבד**, והמצאה חוזרת היא בדיוק מה שהסריקה
מדדה: 15 מימושים של „זה הנבחר" ב-13 נוסחי מחלקות · 65 מחרוזות שונות של `.card` על 161 שימושים ·
19 העתקות של אותו פאנל שקוע · שני מוני כמות בלי שורת קוד משותפת · ו**אף רכיב טאבים**, ולכן קבוצת
הטאבים היחידה במוצר נבנתה ביד. חמשת אלה נוספו ל-`ui.tsx`:

| רכיב | מחליף | מה שקל לשבור בו |
|---|---|---|
| `Tabs` / `TabPanel` | הטאבים המגולפים ביד בכרטיס הספק | ‏roving tabindex, ו**החצים הפוכים ל-RTL**: ‏ArrowLeft מתקדם, כי הטאב הבא הוא זה שמשמאל |
| `ToggleGroup` | פקדי „אחד מתוך N" | המודיפייר `chip-filter-active` **מוסיף** ולא מחליף. תומך גם בערך יחיד וגם במערך |
| `Stepper` | שני מוני הכמות | חסימה בקצוות היא `disabled`, לא בליעה בהאנדלר. חושף `inputRef` — מיקוד מסריקת ברקוד |
| `Card` | 65 נוסחי `.card` | ‏`as` שומר על `<section aria-labelledby>` / `<li>` / `<Link>`; כרטיס הוא **מראה, לא תפקיד** |
| `SubPanel` | 19 ההעתקות | משטח שקוע בתוך כרטיס — **לא** כרטיס שני; משטח בתוך משטח נקרא כשני אובייקטים |

**שני סייגים שנרשמו כי כמעט הופרו.** ‏`ToggleGroup` צובע את הנבחר בצבע הפעולה האחד, ולכן קבוצה
שהנבחר בה נצבע לפי **משמעות** — שורת קבלה שהיא מלאה / חלקית / חסרה — **חייבת לשמור את מפת הטונים
שלה**; המרה הייתה משטחת חמישה סטטוסים לאחד. ו-`ActionMenu` מציב את התפריט לפי `getBoundingClientRect()`,
שהוא קואורדינטה **פיזית** שכבר נפתרה מול הכיוון; ‏`inset-inline-start` על אלמנט `fixed` ב-RTL מחושב
כ-`right` והיה מזיז את התפריט לקצה ההפוך. שם — ורק שם — `left` הוא הנכון.

### Chips / Notes / Skeletons / EmptyState
badge = ‏soft/on-soft · ‏note = ‏wash/line/on-soft ‏(+idle לניטרלי) · ‏skeleton = ‏`bg-line`,
משקף את צורת התוכן · ‏EmptyState מלמד את המסך. ל-EmptyState שתי מידות: הצורה בגודל עמוד, ו-`compact`
לתת-מקטע בתוך כרטיס או דיאלוג — 128px ריפוד וסמל 36px נכונים כשהריק **הוא** המסך, וכבדים מדי
כשהוא בלוק אחד בתוכו.

**הלואדר הוא צורה אחת (`.luma-spin`, הכרעת בעלים 26.08.2026):** שתי נקודות שהולכות היקף של ריבוע,
חצי הקפה זו מזו, בצבע הפעולה — כי לואדר הוא האפליקציה אומרת שהיא עובדת, ואוקיאני הוא צבע הפעולה.
**אבל שלד עדיף על כל ספינר** כשלמסך יש צורה ידועה: ספינר ממורכז זורק את כותרת הדף ומכווץ את הגובה,
ולכן המסך קופץ כשהנתונים נוחתים. `PageLoader` נשאר לשערי האימות ולאזורים שאין להם צורה לשקף.
תחת `prefers-reduced-motion` הוא **מאט ואינו נעצר** — אותה הכרעה כמו `.animate-spin`: זו אינה
דקורציה אלא הדבר היחיד שאומר שהמערכת חיה, ועצירה נקראת כתקיעה.

**`.note` היא שורת flex, ולכן טקסט חופשי נכנס אליה בעטיפה אחת.** כל ילד של `<Note>` הוא flex item —
כולל כל רצף טקסט גולמי בין שתי תגיות inline, שמתכווץ ל־min-content. פרוזה שנמסרת ישירות נקרעת
לעמודות ברוחב מילה: מסך ההגדרות ב־390px נמדד ב־**766px גובה, רוחב שורה מרבי 55px, 81 מקטעים**;
אותה פרוזה בעטיפה אחת — **266px, 266px, 27**. הכלל: `<Note tone="…"><span className="min-w-0 flex-1">…</span></Note>`.
השורה עצמה נשארת שורה כי היא נושאת את פריסת האייקון / הטקסט / הפעולה בכ־15 מקומות. הכלל נאכף
ב־`src/components/noteProse.spec.ts`, שנופל על כל גוף `Note` שמערבב פרוזה עם אלמנטים.

**ממצא חוסם שחוזר על עצמו נאמר פעם אחת.** רשימת „יש לטפל לפני אישור” מקבצת ממצאים זהים ושומרת את
מספרי השורות כטווחים (`1–8, 10–11, 13–15`). מסמך ייצור שהעלה את אותו ממצא על 20 שורות הציג 25 שורות
רשימה; אחרי הקיבוץ — **5 שורות, 230px**, והממצאים שאינם חזרות אינם נקברים ביניהן. מעל חמישה סוגי
חריגה הזנב מתקפל; החמישה הראשונים נשארים פתוחים, כי הרשימה הזאת היא העבודה.

### Charts
recharts לא קורא CSS vars מ-attributes — ‏`src/lib/theme.ts` ‏(`chartTheme()`) קורא את הטוקנים
פעם אחת ב-getComputedStyle ומזרים מחרוזות צבע אמיתיות (`bar`, ‏`barHigh`, ‏`barLow`,
‏`categorical`, ‏grid/tick/label/trendUp/trendDown/flat). **שתי פלטות, שתי עבודות:**
‏`bar`/`barHigh`/`barLow` הם `bar-mid`/`bar-high`/`bar-low` והם הגודל; ‏`categorical` הוא
`series-1`–`series-5` והוא הזהות; אפס הקסים ב-tsx וצבע סמנטי בגרף רק
כשהוא נושא משמעות עסקית. ‏`flat` (מחיר ללא שינוי) קורא `chart-3` ולא `ink-faint` — קו בגרף שייך
לשכבת הנתונים, ו-`ink-faint` מכוונן לקריאוּת של placeholder; שתי סיבות שינוי מנוגדות על טוקן אחד.

**חוק תפקידי הסדרה (19.08.2026, הכרעת בעלים).** גרף השוואה **אינו** בוחר אינדקסים באתר הקריאה
אלא מקבל אותם מ-`comparisonSeries(primary, counterpart)`. עד להכרעה הזו שני הדשבורדים בחרו לבד
וסטו זה מזה בכיוון הגרוע ביותר: מרכז הבקרה הפריד בין שתי סדרותיו ב-**1.56:1** ונשא מקף, ודשבורד
רו"ח הפריד ב-**4.23:1** ו**ויתר** על המקף. הזיווג הוא **`series-1` + `series-2`** (מ-24.08.2026;
המשפט הזה נשאר תקוע על `chart-1` + `chart-3` עד 25.08 — הפנקס המלא נמצא בסעיף ΔE שלמטה),
וה-`dash` על הסדרה השנייה הוא **חובה בכל מקום שיש בו קו** — ב-`ComparisonLineChart` הוא מה
שמפריד בהדפסה בגווני אפור, בצילום דחוס ותחת ליקוי ראיית-צבע (נמדד בדפדפן: `stroke-dasharray`
‏"7 5" על הסדרה השנייה). ‏`GroupedBarChart` מתעלם מהשדה — לעמודה מלאה אין stroke לקווקוו — ונושא
את הזהות השנייה במקרא הנקודות שמתחת לגרף. ‏`colorLanguage.spec.ts` מצמיד את הזיווג, את המקף ואת
**רף ההפרדה בין סדרה לסדרה** — מדידה שלא הייתה קיימת: הפנקס בדק תמיד צבע גרף מול המשטח, אף פעם
לא מול הצבע שלצידו. הרף עצמו הוא ‏ΔE ולא 3:1 מ-24.08.2026; הנימוק בסעיף ΔE שלמטה.

כל המגמות חיות בלוח חזותי יחיד וללא tabs — ומ-T7.2 הלוח מדבר בשפת הרינדור המלאה של הרפרנסים:
ארבע תצוגות גלויות יחד, כל אחת כרטיס משלה — עמודות חודשיות, ‏donut עבה, אריח **„לתשלום
בשבוע הקרוב”** (סכום גדול + פס פיצול דו-מקטעי, בלי גרף recharts), ו-areas חלקים.
כללי הרינדור (T7.2):
- **‏Tooltip אחד — `DarkTooltip`:** גלולת Onyx עם דיו לבן ונקודת-צבע לסדרה (ה-"Others 12" של
  הרפרנס) בכל הגרפים. אין קופסה לבנה גנרית.
- **עמודות בגוון יחיד**, מעוגלות-ראש, **בלי מספרים על העמודות** — ה-tooltip וה-ARIA נושאים
  את הערכים; קווי עזר אופקיים **רציפים** וחיוורים (`chart-grid`), לא מקווקווים. כותרת כרטיס
  העמודות נושאת שני תאי-סיכום (החודש · חודש קודם, עם ה-±%) בסגנון הרפרנס.
  **‏`SpendBarChart` אינו מקבל `color`** — התיאור הישן של „גיוון בין אריחים” תיאר prop שאינו
  קיים בחתימה. **מ-26.08.2026 כל דלי נושא צבע קטגורי משלו** (`categorical[index]`), לפי הכרעת
  בעלים: „שהצבע בדאשבורד באזור של המגמות בהוצאות רכש שיהיה כמו הצבעים של תמהיל הרכש החודש”.
  ‏**זהו חריג מפורש ל„עמודות בגוון יחיד” שלמעלה, והוא חל על הגרף הזה בלבד.** מה שנמסר תמורתו,
  ואין להסיק ממנו שלא נמסר: עד לתאריך הזה הצבע דירג — הגבוה בכהה ביותר, הנמוך בחיוור ביותר,
  ‏`rankFill` על `bar-high`/`bar-mid`/`bar-low`. הדירוג עדיין נקרא מגובה העמודה, וה-tooltip
  וה-ARIA עדיין נושאים את הסכומים המדויקים; מה שהשתנה הוא שהמילוי מסמן זהות ולא דרגה, בדיוק
  כמו ב-donut שלצידו. ‏`bar-low` נשאר לדלי אפס/שלילי: „לא נמדד” אינו קטגוריה, וגוון מהפלטה
  היה טוען שכן. רקע: עד 25.08.2026 הרמפה נלקחה מ-chart-4/chart-1/chart-5 — רמפה שנשארה מאחור
  אחרי שהזהות עברה לפלטה הקטגורית, וזה מה שהבעלים דיווח עליו כ„הצבעים לא חלים על חישוב הוצאות
  רכש”; מעבר לגוון 222 ב-25.08 לא סגר את הדיווח, ולכן ההכרעה של 26.08.
- **‏areas חלקים בלי גריד ובלי נקודות לאורך הקו:** ‏`type="monotone"`, ‏stroke ‏2.5, מילוי
  גרדיאנט ‏~28%→0, שתי הסדרות רציפות; **כל קו מסתיים בנקודה קטנה** בצבע הסדרה עם צווארון
  ‏`surface` בעובי 2 (26.08.2026, הכרעת בעלים), **ושם הסדרה יושב בדיוק בגובה הנקודה שלו**
  בטוקן דיו (`chart-label`) — לא מקרא ולא צבע הסדרה. הנקודה והשם נולדים מאותו `y` באותו node,
  ולכן נקודה שזזה גוררת את המילה איתה. **כששני קצוות קרובים מכדי להכיל שתי תיבות טקסט
  (`END_LABEL_GAP` = 18px) הערימה נפתחת כלפי מעלה בלבד** — התווית הנמוכה נשארת על הנקודה
  שלה והשאר עולות — מפני שמתחת לקצה הנמוך יושבת שורת התאריכים, ותווית שנדחפת מטה נוחתת על
  תאריך (נמדד ב-/dashboard: „תשלומים” על „19/07”). ‏`endLabels` הוא הפונקציה,
  ‏`chartEndLabels.spec.ts` מצמיד את שלושת המקרים. **מדיניות אפס-חלון-מלא:** דלי בלי שורות בחלון
  שנשלף במלואו הוא ‏₪0 נמדד — הקו רציף; חלון שכולו שקט מציג את ה-empty state, לעולם לא שני
  קווי-אפס שמפוברקים גרף; שבוע שסדרה כלל לא כיסתה נשאר null.
- **‏donut עבה** (‏inner ‏55% / ‏outer ‏92%): סה"כ גדול במרכז; **המקרא הוא צ'יפים מתחת** —
  ‏●צבע + שם + ‏% (ה-₪ המדויק ב-title וב-tooltip), צ'יפ מקושר נשאר Link וה-% בתוכו. ‏slices
  דרך `sliceColor` יחיד (קשתות + צ'יפים). **הצבע נגזר מהשם, לא מהדירוג (24.08.2026):**
  ‏`DONUT_ORDER = [0, 2, 3, 1]` בחר לפי `index`, ו-`index` הוא המקום ברשימה הממוינת לפי סכום —
  כך שחודש שהחליף בין מקום שני לשלישי צבע מחדש את שתי הקשתות, והקורא נאלץ לחזור למקרא כדי
  למצוא את הקטגוריה שאחריה עקב. עכשיו המדרגה נקבעת לפי מיקום השם ברשימת השמות הממוינת, כך
  שצבע הקטגוריה יציב בכל רענון וסינון כל עוד אותה קבוצת קטגוריות על המסך. הוא עדיין יכול
  לזוז כשקטגוריה **אחרת** נכנסת או יוצאת מארבעת הגדולים — לדונאט בן ארבע מדרגות אין מקום
  למיפוי על פני כל הקטלוג, ונקודת השדרוג, אם זה יהפוך לחשוב, היא מדרגה שמורה לכל קטגוריה.
  „אחר” מוצמד למדרגה האחרונה: הוא צבירה ולא ישות, ואסור לו לקחת מקום ממשהו שכן.
- **‏GroupedBarChart** (רו"ח — תשלומים מול חיובי בנק): זוגות עמודות דקות מעוגלות-מלא, מקרא
  נקודות מתחת. הזיווג מגיע מ-`comparisonSeries()` ולא נבחר כאן. חריג דו-גוני מנוסח: זיהוי
  ממקרא+ARIA+פער בהירות, לא מגוון בלבד.
- **‏CoverageRing — הוסר.** הרכיב תואר כאן עד 24.08.2026 ואינו קיים בקוד: החריץ הרביעי בלוח
  הוא אריח „לתשלום בשבוע הקרוב”. הנימוק שהחליף אותו שמור בקוד — טבעת מקודדת חלק
  מתוך שלם, וה„שלם” כאן הוא חלון שאנחנו בחרנו, כך שאחוז ממנו הוא חשבון על הבחירה שלנו.
  **פס הפיצול (‏25.08.2026) אינו הטבעת שחוזרת.** דיווח בעלים: אריח בלי שום גרפיקה, בין שני
  גרפים, נקרא כחריץ שלא הצליח להיטען. ההתנגדות מעולם לא הייתה „בלי גרפיקה” אלא „לא אחוז מחלון
  שבחרנו”, ולפס הזה **יש שלם אמיתי** — הכסף שחייב לזוז, מחולק לחלק שכבר באיחור ולחלק שעדיין לא.
  שני מקטעים (`alert-solid` ו-`bar-mid`), גובה 8px, ‏`aria-hidden` כי שתי השורות שמתחתיו כבר
  נושאות את שני הסכומים ואת שתי הספירות. האנימציה היא **חשיפה ולא גדילה** (`split-bar-reveal`,
  ‏`clip-path` מקצה ה-inline start): הרוחבים הם הנתון, ואנימציה עליהם הייתה מציגה יחסים שגויים
  בדרך ליחס הנכון. תחת `prefers-reduced-motion` הרוחבים נשארים והחשיפה נעצרת.
- **פנקס ההפרדה — ‏ΔE, לא יחס בהירות (24.08.2026).** המדד הקודם היה יחס ניגודיות ‏WCAG, שהוא
  פונקציה של בהירות בלבד — הכלי הנכון כל עוד הרמפה מונוכרומטית ובהירות היא כל מה שיש. בפלטה
  קטגורית הוא הכלי הלא-נכון וגם בלתי-אפשרי: המדרגות יושבות בבהירות דומה **בכוונה**, כדי שאף
  סדרה לא תשתלט, ובתוך טווח הבהירות של ה-validator אין שני צבעים שמגיעים ל-3:1 בכלל. המדד
  שעונה על „האם הקורא מבחין ביניהן” הוא ‏OKLab ‏ΔE, ו-`colorLanguage.spec.ts` מחזיק אותו:
  **‏≥15 בראייה מלאה ו-≥8 תחת פרוטן/דויטן, על כל הזוגות ולא רק על שכנים** — הדונאט מציג ארבעה
  פלחים ו„אחר” יחד, והצ׳יפים יושבים בשורה, כך שכל שתי מדרגות מושוות ישירות.
  **הזוג של `comparisonSeries()`** הוא `series-1` ↔ `series-2`: ‏26.1 בראייה מלאה, ‏23.6 בגרוע
  שב-CVD — החזק ביותר שהפלטה מציעה ששומר גם את הסדרה הראשית במשפחה האוקיאנית.
  שתי המדרגות הבהירות (`series-2`, `series-4`) מודדות מתחת ל-3:1 מול הקנבס. זו אזהרה שנפרעת
  בקידוד משני, והוא כבר קיים וחובה: צ׳יפים עם שם ואחוז, קווקוו על הסדרה הנגדית, מרווח 2px בין
  קשתות, ותיאור ARIA לכל גרף.
- **תחזית — אין, ולא נבנתה.** אין במוצר מסך תזרים חזוי, ולכן אין טוקן `forecast`/`projected`
  ואין סדרה מקווקוות שלישית. פלטת גרפים שגדלה לפני שיש לה צרכן היא בדיוק מה שחוק הטוקנים אוסר.
  נקודת החזרה: היום שבו יופיע מסך תחזית. התוויות, הסכומים,
רשימת הקטגוריות ו־ARIA נושאים את המשמעות; אין מקרא מיותר. ‏Tooltip משתמש ב־`cursor={false}` כדי לא
להציג overlay אפור.

buckets שבועיים נושאים `label`, סכום ומספר רשומות. **מדיניות אפס-חלון-מלא (T7.2, הכרעת בעלים):**
כשהחלון נשלף במלואו (‏fetchAll על כל הטווח), דלי שקיים ואין בו שורות הוא **‏₪0 שנמדד** — הוא מקבל
נקודה/עמודה בגובה אפס והקו נשאר רציף, כמו ברפרנס. זה אינו סותר את "אפס הוא גם טענה" (CLAUDE.md):
כאן הטענה נכונה — נשלף הכול ולא נמצא דבר. שני הסייגים: שבוע שסדרה **כלל לא כיסתה** נשאר null
ואינו מקבל נקודה; וחלון ש**כולו** שקט מציג empty state — שני קווי-אפס שטוחים אינם גרף. כל תרשים מספק
שם נגיש ותיאור מלא של הסדרה והערכים. שלושת הגרפים נכנסים פעם אחת בלבד בחשיפה הראשונה של לוח המגמות
ל־viewport; רענון נתונים אינו מפעיל את האנימציה מחדש. אם `IntersectionObserver` חסר, או תחת
`prefers-reduced-motion`, כל הגרפים מוצגים סטטיים מיד. סדרה ללא פעילות אמיתית מוחלפת ב־empty state.

### Motion budget — דשבורד
אזורי ההחלטה נכנסים פעם אחת כשהשלד מוחלף בנתונים: 350ms, תזוזה של 7px ו־60ms בין אזורים.
ה־stagger **נמנה לפי הסדר הנראה, לא לפי סדר ה־DOM**: כל אזור מצהיר על הצעד שלו בעצמו
(`[--dash-step-mobile:N]` ו־`[--dash-step:N]` ב־`Dashboard.tsx`), וה־media query ב־`index.css`
בוחר איזה מהשניים נספר. הסיבה: פס הכסף מוזז ל־ראש המסך מתחת ל־`lg`, ולכן `:nth-child` היה מאחר
דווקא את הכרטיס הראשון שהעין פוגשת. אזור שלא הצהיר על צעד מקבל 0ms — נראה בסקירה, לא מזיק בייצור.
חותמת "עודכן ב־" מתחלפת ב־fade של 180ms עם `aria-live="polite"`. אנימציית שלושת הגרפים מוגבלת
לכניסה הראשונה של לוח המגמות ל־viewport. אין count-up לסכומים, אין נורית LIVE, אין הבהוב מחזורי ואין אנימציית hover
שמשנה מידע. תחת reduced-motion מבוטלים stagger, draw-in, lift ותנועת מעבר; מצב טעינה פונקציונלי
יכול להישאר כספינר איטי.

### עורך הזמנה וגלריית מסמכים

עורך ההזמנה שומר את אותה היררכיה צפופה: מצב השמירה והפעולה הראשית נמצאים בכותרת, וסיכום האישור
הוא ledger של נתונים עסקיים — לא רשת KPI חדשה. חיסכון חיובי, תוספת עלות וחוסר קו־בסיס נכתבים
במפורש; צבע לבדו אינו נושא את הטענה.

גלריית המסמכים היא טבלה מסוננת בדסקטופ וכרטיסי `DataTable` במובייל. סטטוס התיוק משתמש רק
ב־`badge-await`/`badge-done`, תאריכים וסכומים מקבלים `num`, וכל פעולת צפייה או תיוק נשארת גלויה
למקלדת וביעד מגע של 44px לפחות. לא נוספו טוקנים או שפה חזותית חדשה.

### מסך בדיקת מסמך שהתקבל (`DocumentAssessmentPanel`, 11.08.2026)

זהו המסך שבו אדם לוחץ על כפתור שיוצר חוב. ארבעה כללים בעלי-שם, שכולם נגזרים מכך:

**‏1. "הקובץ נשמר" ו"הנתונים אושרו" הם שני משפטים, לעולם לא אחד.** שני שדות נפרדים מהשרת, שתי
שורות עם שני אייקונים, ובדיקה שנופלת אם אי-פעם ייצרו אותו טקסט. מי שצילם חשבונית ליד המשאית השיג
דבר אחד: הקובץ לא ילך לאיבוד. שום דבר בעסק שלו לא השתנה עדיין.

**‏2. "מה יקרה באישור" מונה גם את מה ש*לא* יקרה.** המשפטים השליליים אינם ריפוד — "לא ישתנו כמויות
שהתקבלו" הוא הדבר המועיל ביותר לקרוא לפני אישור תעודת משלוח. כל משפט מוצמד בבדיקה מול הענף
ב-`apply_reviewed_document` שהוא מתאר: **הבטחה שהשרת לא מקיים גרועה משתיקה, כי האדם קרא אותה.**

**‏3. ערך שאינו ידוע הוא מקף.** ‏`fmtMoneyExact`/`fmtNum` מדפיסים `—` ל-`null`, וזו הסיבה שהם
בשימוש בכל מספר במסך. מחיר בסיס חסר שמודפס כ-₪0.00 הוא **טענה שהספק הסכים לתת אותו במתנה**.

**‏4. "רגה שלא חולצה" אינה מקף, ובוודאי אינה אפס (`ReconciliationStrip`, 31.08.2026).** כלל 3
נשאר בתוקף בכל מקום שבו למספר יש מקום ואין לו ערך. אבל בסולם ההתאמה — סכום שורות, הנחות, סה״כ
לפני מע״מ, מע״מ, מחושב, סה״כ במסמך — מקף עדיין מטיל על הקורא את ההכרעה אם הוא ריק או אפס, וזו
בדיוק ההכרעה שהוא אינו יכול לעשות. לכן `missing_rungs` מהשרת נקרא כרשימה של **שמות**, וכל רגה
כזו כותבת „לא חולץ" מול התווית שלה. ופער שנגזר מהיעדרות אינו פער: כאשר רגה חסרה, שורת הפער
אומרת „לא ניתן לחשב" במילים במקום להדפיס מספר שהמסמך אינו תומך בו.

**אף מספר בסולם אינו מחושב בדפדפן.** ‏`computed_total` ו-`unexplained_gap` מגיעים מ-`0260`,
מעוגלים לפי `minor_units` של המטבע — אותו עיגול שהכריע אם לחסום. חיבור של `header_net + header_vat`
ב-React היה מקור אמת שני לכסף, שיכול לחלוק על ההחלטה עצמה. גם הסימן עובר דרך `fmtMoneyExact`:
מינוס שנכתב ליד מחרוזת-מטבע מתהפך תחת bidi ומתנתק מהספרות שלו — צילום מסך תפס בדיוק את זה.

**סולם אחד לשני מסכים, ולכן התוויות אינן קוראות לרשומה בשם (31.08.2026).** מ-`0261` גם ההתאמה
המשולשת של החשבונית מפרסמת את אותו סולם, ואותו רכיב מצייר את שניהם. לכן „סה״כ במסמך" הפך
ל„סה״כ מוצהר", „מטבע המסמך" ל„מטבע", והמשפט של הסולם השני הפסיק לומר „מהמסמך" — כותרת הכרטיס
היא המקום היחיד שקורא לרשומה בשם, והיא `prop` שהמסך מעביר. **הרצועה בחשבונית אינה מוצגת כלל**
כשהשרת לא החזיר סובלנות ומטבע: „נבדק מול 1" ליד חשבונית שנשפטה אחרת גרוע מהיעדר רצועה, וזה
בדיוק הכשל שמסע פר-המטבע נועד לסיים.

**שני סולמות, וחסר בסולם אחד אינו מבטל את השני.** „חשבון המסמך" הוא סכום סגור שכל רגה שלו קיימת
בכל מסמך היום. „מה ישולם בפועל" — ניכוי במקור ומה שהתקבל בפועל — הוא סולם שני שרגותיו אינן
מחולצות כלל. איחודם היה גורם לכל מסמך במדינה לקרוא „לא ניתן לחשב", כלומר למחוק את התכונה במקום
לבנות אותה.

כפתור האישור **נשאר פעיל** גם כשיש ממצאים חוסמים, עם משפט שמסביר שהשרת יבדוק שוב וינמק. חסימה
בצד הלקוח הייתה מכניסה מקבל-החלטות שני למסלול ומאפשרת לשניים להיפרד. הטבלה היא `DataTable` עם
`mobile="cards"`, ספרות ב-`num`, טוקנים בלבד, ו-properties לוגיים בלבד.

### `PrimaryDecision` — הפעולה הראשית של מסך בדיקה יושבת בזרימה (18.08.2026)

**חוק המקום של ההכרעה: הכפתור שמסיים בדיקה יושב בזרימת המסך, בראש הראיות — ולא מרחף מעליהן.**
‏`PrimaryDecision` ב-`document-review/` הוא המקום היחיד שמממש אותו, וכל ארבעת מסכי הבדיקה עוברים
דרכו.

**מה הוחלף.** הרכיב נקרא `StickyPrimaryAction` והשתיל את הכפתור, בטלפון, לסרגל `.phone-taskbar`
קבוע מעל סרגל הפעולות הגלובלי. ההיסט של אותו סרגל הוא `--mobile-action-bar-size + safe-area + 2rem`
— ה-`2rem` נועדו לפנות את כיפת הצילום המורמת — ולכן הוא נעצר כ-`6rem` מעל קצה המסך **בזמן שתוכן
העמוד ממשיך לגלול בפער שמתחתיו**. משטח `bg-surface` אטום שיש תוכן חי מעליו וגם מתחתיו אינו נקרא
כסרגל תחתון אלא כלוח שהונח באמצע הרשימה (דיווח בעלים, 18.08.2026). התיקון אינו כיוונון של הריחוף:
הכפתור חזר לזרימה.

**למה דווקא בראש.** זו לא החלטה חדשה — זה המקום ששלושה מארבעת המסכים כבר בחרו לעצמם ותיעדו
בהערותיהם („ההכרעה, מעל הראיות המקופלות ולא מתחתיהן”; „כל ההכרעה, מעל השורות ולא מתחתיהן”),
והסרגל הצף דרס אותו. הנימוק הוא חוק תיבת הדואר של החריגים שלהלן: מה שהמכונה סגרה מקופל לשורת
סיכום, ולכן הרשימה שמתחת לכפתור היא **ראיה, לא טופס**. פקד הכרעה שממוקם אחרי ראיה אומר „קרא את
הכול קודם”, וזו בדיוק ההפך מההבטחה של המסך. בחבילה שהמכונה סגרה ההכרעה כולה היא עכשיו: לקרוא את
שורת הספירה וללחוץ — בלי גלילה.

**החריג היחיד, ובשמו:** ‏`DocumentScanPreview`. שם הראיה היא **מושא** ההכרעה ולא פירוט אופציונלי —
המשפט שמעליה מבקש לוודא שהדף שלם וקריא — ולכן הכפתור נשאר אחרי שתי התמונות.

**מה נמחק עם הסרגל:** ה-spacer ש-`createPortal` שתל בסוף `<body>`, ה-`ResizeObserver` שמדד את
הסרגל כדי לקבוע את גובה ה-spacer, וענף ה-`matchMedia` שבחר בין שני רינדורים. שלושתם התקיימו רק כדי
לשרוד את הריחוף. ‏`.phone-taskbar` ב-`index.css` לא שונה והוא שוב של `Receiving.tsx` בלבד — כולל
הכלל שמרים מעליו את ערימת הטוסטים, שממשיך לחול שם ורק שם.

**מה נשאר בתוקף:** רינדור **אחד** של הכפתור בכל רוחב (זוג `lg:hidden` היה משאיר שני צמתים עם אותו
שם נגיש ואותו `data-testid`); משפט ההסבר נוסע עם הכפתור ו**מעליו** — סיבה שנקראת אחרי הלחיצה אינה
סיבה; ובטלפון הכפתור נמתח לרוחב מלא כיעד אגודל אחד, ומ-`lg` חוזר לגודלו הטבעי בקצה הלוגי.

### `Disclosure` — קיפול מונה, וחוק תיבת הדואר של החריגים (18.08.2026)

‏`Disclosure({ title, count, tone, summary, name, onToggle })` ב-`ui.tsx` הוא הרכיב המשותף היחיד
לקיפול. הוא `<details>/<summary>` טבעי — ‏`list-none`, סמן ברירת המחדל מוסתר, יעד מגע `min-h-11`,
שברון שמסתובב ומכבד `motion-reduce`, ותג ספירה שהוא `badge-*` מאוצר המילים של `status.ts`. הוא
נגזר מה-`OperationsDisclosure` המקומי של הדשבורד; זו אותה שפה ויזואלית, לא שנייה.

**חוק תיבת הדואר של החריגים.** מסך בדיקה אינו טופס שבו אדם מאמת מחדש את כל עבודת המכונה. מה
שהמכונה בטוחה בו מתקפל לשורת סיכום אחת עם מספר; מה שדורש הכרעה אנושית נשאר פתוח. ‏42 עמודים
נפתחים כ„39 מסווגים · 3 דורשים בדיקה”, ולא כ-42 עורכים.

שלושה סייגים שאינם ניתנים למשא ומתן:

**‏1. קיפול אינו הסתרה.** ממצא בחומרת `error`/`critical`, טענה על מלאי, שינוי שהמכונה ביצעה,
ביטחון נמוך ופעולה בלתי-הפיכה — נשארים פתוחים. מה שנסגר הוא ראיה ופירוט, לא סיכון.

**‏2. הספירה על שורת ה-summary היא התוכן.** „39 מסווגים” נושא את המידע שהרשימה הייתה נושאת;
בלי המספר הקיפול הוא מחיקה. ‏`<details>` סגור עדיין מרנדר את ילדיו — לרשימה ארוכה מגדרים את
הבנייה על `onToggle`, אחרת נחסך צבע בלבד ולא עבודה.

**‏3. מה שנשלח אינו תלוי במה שנפתח.** אישור מניפסט של חבילה שולח את כל העמודים, כולל המקופלים,
עם אותו `manifest_hash`. קיפול הוא החלטת תצוגה; ‏payload שמשתנה לפי מצב `<details>` הוא באג.

#### קיפול בשליטת הבודק — שתי קבוצות ופקד אחד (חבילת מסמכים, 18.08.2026)

בפאנל החבילה (`DocumentPacketReview`) שתי הקבוצות — „דורשים בדיקה” ו„עמודים מסווגים” — הן **קיפול
מבוקר** מקומי (`SegmentGroup`) ולא `Disclosure`, משתי סיבות שאינן עניין של טעם: קבוצת החריגים חייבת
להיפתח **מעצמה** (‏`<details>` טבעי נפתח רק כשאומרים לו), ושתי הקבוצות חייבות לענות לפקד אחד. אנטומיית
השורה זהה ל-`Disclosure` — שורת summary בגובה 44px, ‏`badge-*` מאוצר המילים של `status.ts`, שברון
שמכבד `motion-reduce` — כלומר אותו קיפול באותה שפה, לא שפה שנייה.

**הפקד: „קיפול כל החלקים” / „פתיחת כל החלקים”**, ליד כותרת „חלקי הקובץ” ולא על אף שורה. הוא
**נוסף** להתנהגות האוטומטית ואינו מחליף אותה: ברירת המחדל נשארת חריגים־קודם, והפקד הוא הדרך של אדם
שמסתכל על 24 חלקים לקפל את כולם בלי לחפש מתג בכל שורה, או לפתוח את כולם כשהוא רוצה לקרוא את כל
תשובת המכונה. הוא נושא `aria-expanded` ומצביע ב-`aria-controls` על שתי הקבוצות, וילדי קבוצה סגורה
אינם נבנים כלל — ‏24 חלקים הם 24 עורכים ו-192 ‏`<option>` שלא נוצרו.

**חילוץ חלקי הוא טענה על הקובץ, לא על חלק.** ‏`source_partial` היה המבחן הראשון בדירוג תשומת הלב,
ולכן כל חלק בכל חבילה סומן כחריג ושום דבר לא התקפל אף פעם — חבילה של 24 חלקים ב-0.99 נפתחה כ-24
עורכים. גם אם הדגל היה מדויק, זו טעות קטגוריה: משפט על כל הקובץ שנדפס על כל שורה אומר את עצמו N
פעמים, מבטל כל קיפול, ועדיין אינו אומר איזה חלק חסר. הוא עכשיו `note-await` **אחד** בראש הפאנל.

**שתי מגבלות, שתי אמירות — ולא משפט אחד.** בראש הפאנל עמדה שורה שאיחדה שני מצבים („החילוץ חלקי או
ארוך מ-20 עמודי OCR”), והבעלים קרא אותה כטענה על **איכות הקריאה** של מסמך שנקרא מושלם. הן אינן
אותו דבר, ומאז `0144` הן גם אינן אותו מספר:
- **כיסוי** — ‏`note-await`: לפחות עמוד אחד לא הניב טקסט. קריאת ה-OCR בתשלום מוגבלת
  ל-`PAID_OCR_PAGE_CAP` עמודים למסמך (`worker/ocr/src/limits.py`), **ולא זזה ב-0144**.
- **אורך** — ‏`note-idle`: הקובץ ארוך מ-`AUTOMATIC_SPLIT_PAGE_CEILING`, תקרת הפיצול האוטומטי
  (`0144`, עלתה מ-20 ל-40). זו אמירה על אורך, לא על קריאה, ולכן היא ניטרלית ולא ממתינה.

שני המספרים מוצהרים **פעם אחת**, זה לצד זה, ב-`document-review/serverLimits.ts` — בדיוק מפני
שהם היו אותו מספר עד היום. הם עותקים לניסוח בלבד: שום החלטה בלקוח אינה נגזרת מהם, ‏`source_partial`
ו-`automatic_eligible` נקראים מהשורה. הסתירה שהתקרה יצרה מודגשת גם בהגדרות: **קובץ סרוק** נקרא עד
‏20 עמודים ולכן סריקה ארוכה מכך נשארת לאישור אדם — התקרה החדשה פותחת PDF עם שכבת טקסט בלבד.

### מסמכי ההסכמה — עמוד קריאה, לא מסך (28.08.2026, הנחיית בעלים)

`/terms` ו-`/privacy` הם **המקום היחיד במוצר שהוא מסמך ולא מסך**, ומ-28.08.2026 הם נראים כמו
העמודים המקבילים באתר השיווק. ההנחיה הייתה מפורשת: „תעדכן את הנראות של התנאי שימוש והמדיניות
לפי הדפים שקיימים בדף הנחיתה".

**מה שיובא הוא מעטפת הקריאה, לא הצבעים.** כל ערך ב-`.legal-doc` הוא טוקן קיים —
`--color-shell` (Onyx), ‏`--color-shell-ink`, ‏`--color-shell-ink-soft`, ‏`--color-shell-ink-dim`,
ושתי המשיחות האוקייניות דרך `color-mix` על `--color-action`. ‏**אין ערך צבע חדש**, ולכן
ריתום מחדש (retheme) מזיז גם את העמוד הזה במקום להשאיר מסמך צבוע ביד.

| החלטה | הערך | למה |
|---|---|---|
| רקע כהה לעמוד של טקסט קטן | `--color-shell` + שתי משיחות | זה אינו מסך עבודה. לא סורקים אותו מול טבלה — פותחים פעם אחת מ-`/login` או מההזמנה, קוראים מקצה לקצה וסוגרים. אותו Onyx שהניווט כבר לובש, כך שהמסמך נקרא כקצה של המוצר ולא כמשטח חמישי |
| רוחב שורה 44rem | `.legal-doc__column` | מידה, לא רוחב מכולה: בגודל הגוף כאן זה נוחת סביב 70 תווים לשורה — הנקודה שבה פסקה משפטית ארוכה מפסיקה לאבד את הקורא בין השורות |
| **אין כרטיס** | הוסר `Card` | כרטיס ממסגר משטח עבודה. כאן אין במה לעבוד |
| הסרגל נדבק ופרוש לרוחב החלון, תוכנו נשאר בעמודה | `.legal-doc__bar` | מוכל בעמודה הוא טשטש רצועה של 44rem עם שתי מדרגות חדות באמצע המסך |
| ‏h1 לכותרת, ‏h2 לסעיפים | `.legal-doc__section h2` | הלוגו הוא סימן המוצר ולא כותרת המסמך. הסעיפים היו `h3` תחת `h1` — דרגה מדולגת |
| ‏`display: flow-root` | `.legal-doc` | ה-margin של הכותרת התחתונה ברח מהמכולה והשאיר רצועת קנבס מתחת למסמך. **צילום מסך תפס את זה; הפריסה הייתה תקינה לכל דבר אחר** |

**מה שלא יובא, במכוון:** ה-lede של אתר השיווק אומר שהנוסח העברי הוא המחייב. ‏`OPEN-DECISIONS #280`
ביטל את המשפט הזה — **שני הנוסחים מחייבים**. העתקת המראה והתוכן יחד הייתה מחזירה בשקט את הקריאה
שהבעלים לא בחר. **אתר השיווק עדיין נושא את המשפט הזה, וזה חוב פתוח** (`DEBT §70`).

## 6. Do's and Don'ts

### Do:
- **Do** כל צבע דרך טוקן; מחלקות המשמעות (`badge-*`, `note-*`, `text-*-fg`) הן ה-API.
- **Do** ‏`class="num"` על כל תא מספרי — המונו הוא חלק מהשפה, לא קישוט.
- **Do** ‏properties לוגיים בלבד; ‏`—` למדד ללא נתונים; ניגודיות ≥4.5:1 (ledger ב־Runbook להלן).
- **Do** מעברים 150–250ms ‏ease-out מכבדי reduced-motion; ‏`active:` לצד `hover:` ברכיבים משותפים.

### Don't:
- **Don't** להשתמש בפטרול כסטטוס או לפזר גוון מותג נוסף; מעטפת המותג נשענת על גוון אחד.
- **Don't** ‏uppercase/tracking על טקסט עברי.
- **Don't** צל שחור/כבד; ‏glassmorphism קר וגנרי (חריג-בעלים T7.1: שקיפות רכה 72%+ בכרטיסי
  הדשבורד בלבד — ראו §4); אנימציה שאינה מצב — אסור בחוקה.
  **חריג אחד, בשמו (הכרעת בעלים 25.08.2026):** הניצוץ שעובר על תג המנוי של **„פרימיום”**
  (`.plan-badge-premium::after` — היה על „פרו” עד 26.08.2026 ועלה למדרגה העליונה באותו יום).
  הוא אינו מדווח על שום שינוי במערכת, וזו בדיוק הסיבה שהוא צריך להירשם
  כאן ולא להיקרא כטעות. החריג חסום כך שיישאר סימן אחד ולא הרגל חדש: אלמנט אחד, מעבר איטי
  אחד (3.2s), ללא מאפיין פריסה, ללא צבע שנושא משמעות, והוא **נעלם לגמרי** תחת `prefers-reduced-motion`
  — המדרוג הסטטי שמתחתיו נושא את התג בלעדיו, וניצוץ קפוא באמצע מעבר נקרא כתקלת רנדור.
  **חריג שני, בשמו (הכרעת בעלים 26.08.2026):** קצה הקשת על פעולת שדרוג המסלול (`.btn-rainbow`,
  מקורו ב-Magic UI). הוא שובר **שני** כללים — הוא מוסיף חמישה גוונים למעטפת שנשענת על אחד, והלולאה
  שלו אינה מדווחת דבר. הוא נרשם כאן לצד הניצוץ מאותה סיבה בדיוק: חריג שכתוב נשאר סימן אחד, וחריג
  שרק נסבל הופך להרגל. הגבול צר: הוא רשאי ללבוש את פעולת השדרוג ותו לא, המשמעות נשארת בתווית,
  והוא **נעצר** תחת `prefers-reduced-motion`. הוא **אינו נעלם** — וההבדל מהניצוץ הוא העניין: הניצוץ
  הוא שכבה מעל תג ששלם בלעדיה, ואילו הקשת **היא הגבול של הכפתור**. מוסרת, הפקד מאבד את קו המתאר
  שלו; קפואה, היא קצה רב-גוני סטטי שנשאר הכפתור הבולט במסך, בלי שדבר זז.
  שתי הערות מימוש שקל לפספס: המקור מקודד `#121213` ו-`rgba(18,18,19,.6)`, ש-`check:tokens` מפיל
  עליהם את הבנייה, ולכן הגוף הוא `--color-shell`; והוא נשלח ב-36px, מתחת לרצפת המגע, ולכן הוא
  יורש את `btn` ואת ה-`min-h-11` שלו במקום לשאת גובה משלו.
- **Don't** להציג count-up, נורית LIVE או מגמה שאין מאחוריה רשומות אמיתיות.
- **Don't** מחלקת פלטה גולמית או הקס ב-tsx — גרפ האכיפה שלהלן חייב להישאר ריק.
- **Don't** ‏emerald/amber/rose/sky מחוץ למשמעותם; אין להמציא משמעות שישית.
- **Don't** ‏`border-left/right` צבעוני כפס-סימון — ‏`note-*` בלבד. (זו גם הסיבה שסמן האזור הוא
  ‏rule תחתון קצר ולא פס בקצה הלוגי של הכרטיס.)
- **Don't** משטח פעולה קבוע שמשאיר רצועת תוכן גוללת בינו לבין סרגל הפעולות התחתון. סרגל תחתון נוגע
  בקצה; משטח שיש תוכן חי מעליו וגם מתחתיו הוא לוח באמצע הרשימה. במסך בדיקה ההכרעה יושבת בזרימה
  ‏(`PrimaryDecision`), ו-`.phone-taskbar` שמור ל-`Receiving.tsx`.
- **Don't** ‏`--color-section-*` כטענה על נתונים, כרקע מתחת לטקסט, כ־prop של רכיב, או על משטח
  ‏`shell` הכהה (~2.2:1). מבטא אזור מגיע מה-URL בלבד ומופיע כסמל או כ־rule בלבד.
- **Don't** להרחיב את מפת האזורים למסלול רק כי הוא חדש. שלושה תחומי עבודה, ולמסך שמביט על פני
  כולם אין מבטא — זו ההצהרה, לא פער.

### Retheme Runbook + אכיפה

**רה-תמה סטטית עתידית = ארבעה משטחים**. נכסי הלוגו אינם עוד חלק מהרה-תמה: ב־21.08.2026 אושר
לוגו סופי בעל פלטת נכס עצמאית, והבעלים בחר במפורש „נכסי לוגו בלבד” ללא repaint של הממשק.

1. **בלוק ה-`@theme` ב-`src/index.css`** — כל ערך צבע בקובץ יושב בתוכו. מחוץ לבלוק צבע מגיע רק
   דרך `var()` או `color-mix(… var(--token) …)`; ‏`check:tokens` נופל אחרת.
2. `index.html` — ‏`<meta name="theme-color">` והפונטים.
3. `public/manifest.webmanifest` — ‏`theme_color` ו-`background_color`.
4. המסמך הזה.

**מערכת הלוגו הסופית נפרדת:** ‏`public/brand/` מכיל lockup, סמל, inverse ו־app icon שנגזרו 1:1
מלוח ה־SVG הקנוני. צבעי הנכס הם dark ‏`#0D222F`, accent ‏`#05394F`, wordmark ‏`#3E3E40`
ו־paper ‏`#FBFBFB`. אין למפות אותם אוטומטית ל־`@theme`; צבעי ה־runtime נשארים Onyx / Wheat /
Oceanic. ‏`public/icons/icon-192.png`, ‏`icon-512.png` ו־`icon-512-maskable.png` הם נגזרות raster
של אותה מערכת. מקור, מטריצת שימוש וכללי הפקה: `brand/identity.md` ו־`brand/implementation-surface.md`.

מעבר שמשנה את *קוטביות* ה־shell (בהיר↔כהה) מחייב בנוסף audit לצרכני `bg-shell` כדי לוודא שהם
משתמשים ב־`shell-ink-*`, כפי שנעשה ב־Login/AcceptInvite בסבב T6.

**ליטרלים ששורדים בכוונה, ואינם דליפה:** ‏`bg-white` בארבעה מקומות — לוח לבן אטום מתחת ללוגו
שהדייר מעלה (`Layout.tsx` ×3, ‏`Settings.tsx`). לוגו עשוי להיות PNG שקוף, ולבן טהור הוא ההחלטה.
כל טקסט על משטח מלא עובר `--color-on-solid`, לא `text-white`.

**האכיפה** — ‏`scripts/check-design-tokens.ts` ‏(`npm run check:tokens`) בשני היקפים:

- **`src/**/*.ts` ו-`*.tsx`** — אפס מחלקות פלטה גולמיות, אפס `#RRGGBB`. ‏`.ts` נכלל מ-19.08.2026
  מפני ש-`src/lib/orderImage.ts` בונה מסמך HTML מעוצב שלם והיה הקובץ היחיד שיכול היה לסגת לליטרל
  בלי רשת. הגרפ:
  ```
  \b(bg|text|border|ring|fill|stroke|divide|outline|decoration|placeholder|accent|caret|shadow)-(slate|gray|zinc|indigo|violet|blue|emerald|green|amber|yellow|orange|rose|red|sky|cyan|teal)-[0-9]{2,3}\b
  ```
- **`src/index.css`** — אין `rgb()`/`hsl()` בשום מקום בקובץ (הפלטה היא oklch; חמשת טוקני הצל
  היו אוניקס/אוקיאני מועתקים ביד בדיוק בסימון הזה), ואין `#hex` או `oklch(` **מחוץ** לבלוק
  ה-`@theme`.

בנוסף, ‏`src/lib/colorLanguage.spec.ts` מצמיד את **המשמעות** במקום שבו הערכים כבר זהים:
‏`text-{tone}-solid` אסור (‏`solid` הוא מילוי פקד), ‏`text-white` אסור, חץ מגמה לעולם לא חולק
אלמנט עם טון סטטוס, ולכל טון יש שישה משטחים ומחלקת `badge-`/`note-`.

**Contrast ledger — נמדד מחדש 19.08.2026** ‏(oklch → sRGB → luminance יחסית → WCAG 2.x).
הפנקס הקודם ציטט hex-ים מ-T7.1 — ‏`surface #fffcf8`, ‏`chart-2 #27767c`, ‏`chart-5 #8a6c41`
ומבטאי אזור `#785e96`/`#59649f`/`#87567c` — שאף אחד מהם אינו טוקן חי מאז T7.3. **הפנקס הזה הוא
של הערכים שבקוד.**

**טקסט (רף 4.5:1) — הכול עובר.**

| חזית | רקע | יחס |
|---|---|---|
| `ink` | canvas / surface | 16.64 / 17.78 |
| `ink-body` | canvas / surface | 13.28 / 14.19 |
| `ink-mid` | surface / surface-sunken | 10.34 / 9.25 |
| `ink-soft` | surface / canvas | 7.71 / 7.21 |
| `ink-muted` | surface / canvas / sunken / topbar-75 | 6.63 / 6.20 / 5.93 / 5.89 |
| `ink-faint` | surface / canvas / surface-hover | 5.02 / **4.70** / 4.55 |
| `ink-faint` | topbar/75 מעל canvas | **4.46 — גבולי, נופל ב-0.04** |
| `action` (לינק) | surface / canvas | 11.39 / 10.65 |
| `on-solid` | action / -hover / -solid | 11.65 / 13.65 / 9.36 |
| `on-solid` | ‏`{tone}-solid` | ‏5.05 (await) עד 10.34 |
| `{tone}-fg` | surface | ‏4.94 (await) עד 10.11 |
| `{tone}-on-soft` | ‏`{tone}-soft` / `{tone}-wash` | 6.41–13.37 / 6.88–14.00 |
| `trend-up-fg` / `trend-down-fg` | surface | 5.92 / 5.24 |
| `shell-ink` / `-soft` / `-dim` | shell | 16.93 / 12.68 / 7.61 |
| `chart-tick` / `chart-label` | surface | 5.95 / 7.71 |
| `shell-ink` / `on-solid` | ‏`chart-2` (‏DarkTooltip) | 4.83 / 5.20 |
| `ink-body` / `ink-muted` | `surface-selected` | 11.40 / 5.32 |

**רכיבי ממשק לא-טקסטואליים (רף 3:1).**

| רכיב | מול | יחס |
|---|---|---|
| `line-strong` (גבול `.input`) | surface / canvas / sunken | 3.42 / 3.20 / 3.06 |
| `action-line` | surface / canvas | 3.48 / 3.25 |
| `focus` | surface / canvas | 5.65 / 5.29 |
| כוכב ריק (`line-strong`) | surface / canvas / sunken | 3.42 / 3.20 / 3.06 |
| סדרת השוואה `series-1` מול `series-2` | זו מול זו — **‏ΔE ולא יחס בהירות** | 26.1 מלאה / 23.6 ‏CVD |
| כל זוג ב-`series-1`–`series-5` | זו מול זו — הזוג הגרוע ביותר | 16.1 מלאה / 15.3 ‏CVD |

**פטורים בעלי-שם:** ‏`ink-ghost` (2.14 מול surface) — ‏disabled וקישוט בלבד, פטור 1.4.3;
‏`chart-grid` (1.32) — קווי רשת, אינם אובייקט מידע; ‏`star` המלא (1.68) — הבחנתו היא **מילוי
מול מתאר**, והמתאר הוא `line-strong` שעובר 3:1; ‏`chart-3`/`chart-5` מול surface (2.69 / 1.63) —
לכל גרף legend, ‏tooltip, ‏ARIA וערכים מספריים, ולכן הצבע אינו "נדרש להבנת התוכן" (1.4.11).

**מגבלה מתועדת:** ‏`line-strong` מול `surface-selected` = 2.75. שדה קלט אינו יושב על שורה
נבחרת באף מסך היום; אם ישב אי-פעם, זו נקודת המדידה. הוא הדין ב-`ink-faint` על
`surface-selected` (4.03) — placeholder על שורה נבחרת אינו קיים ואינו מותר.

**מדרגות המשטח:** ‏surface↔hover 1.104 (‏ΔL 3.6) · hover↔selected 1.13 (‏ΔL 4.1) ·
surface↔selected 1.245 (‏ΔL 7.7). ‏`action-on-soft` על `surface-selected` — 9.77.

**רמפת הגרפים — הפרדת שכנים** (ההפרדה היחידה שיש לרמפה מונוכרומטית):
‏chart-1↔chart-2 2.24 · chart-1↔chart-3 4.23 · chart-1↔chart-4 **1.56** · chart-1↔chart-5 6.97 ·
chart-2↔chart-3 1.89 · chart-2↔chart-4 3.50 · chart-2↔chart-5 3.11 · chart-3↔chart-4 6.61 ·
chart-3↔chart-5 **1.65** · chart-4↔chart-5 10.89.
**המסקנה המדידה: הרמפה מספקת שלושה צעדים מובחנים, לא חמישה** — ‏chart-1 ו-chart-4 הם 1.56
זה מזה, וכך גם chart-3 ו-chart-5 ב-1.65. לכן סדר הדונאט הוא `[0, 2, 3, 1]` ‏(19.08.2026, היה
`[0,1,3,2]` מזמן הרמפה המעורבת של T7.1): בסדר הציור chart-1 → chart-3 → chart-4 → chart-2 →
"אחר"=chart-5 כל צמד שכנים מודד 4.23 · 6.61 · 3.50 · 3.11 — כולם מעל 3:1, בלי לגעת בגוון.
**סדרה רביעית ומעלה חייבת מבדיל שאינו צבע** (`dash`, marker, תבנית) — לא צעד נוסף ברמפה.

‏`glow` (מחושב, לא נמדד חי): בשיא הכתם `ink-muted` ≈5.36 ו-`ink-faint` ≈4.06. הראשון עובר
בפער; השני נוגע **רק** לטקסט שיושב ישירות על ה-body ולא בתוך כרטיס אטום — ‏`.app-glow` הוא
`z-index:-1` מתחת לכל משטח. ‏T7.3f העמיק את `ink-muted` ל-47% בדיוק בגלל המדידה החיה הזו (4.75).

**מבטאי האזור:** שלושתם ליטרל אוקיאני יחיד `#003F47` מאז T7.3 — ‏canvas 10.65 · surface 11.39 ·
sunken ~10.1. הכלל לא השתנה: מבטא מופיע על נייר בלבד (גלולת הניווט הפעיל וסמן העמוד), לעולם
לא על ה-shell הכהה.

**סיכוני תחזוקה:** ‏`badge-${tone}`/`note-${tone}` נבנות דינמית — כל ערך Tone חייב מחלקה ב-index.css
(אין שגיאת build אם חסרה). ‏Tailwind v4: מחלקה מותאמת = ‏@apply על utilities אמיתיים בלבד; קומפוזיציה
דרך ‏@utility (כמו btn/badge/note). טוקני `shadow-card*` הם Onyx רך-רחב (T7.3f) — לא קו קשה.
‏chartTheme() ממוטמן ברמת מודול — theme switch עתידי חייב invalidation.
‏`--color-section-*` הם **ליטרלים קפואים מאז T7.1** — הזזת רמפת הגרפים **אינה** צובעת עוד את
הניווט או את סמני העמוד; ה-spec מצמיד את שלושת הערכים המדויקים ואוסר `var(` בהצהרותיהם. מי
שמבקש לשנות אותם מתווכח עם „מאיפה הגוונים” ב-§2 ומודד מחדש את שורות הפנקס. ‏`data-section`
נקבע ב-`Layout.tsx` בלבד; אם משטח נוסף יצטרך מבטא, הוא מקבל אותו מ-`sectionOf(pathname)` ולא
מ-prop.
