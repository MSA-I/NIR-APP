---
name: SupplyFlow
description: מערכת procurement-to-payment עברית RTL — B2B פרימיום, מסגרת פטרול עמוקה + נייר חם + צבע סמנטי
colors:
  # ---- שלד (structural) — "חדר בקרה פטרול" (T6, 21.07.2026) ----
  canvas: "oklch(96.8% 0.01 85)"                # רקע הגוף — נייר חם
  surface: "oklch(99.2% 0.004 85)"              # כרטיסים, טבלאות, מודאלים, שדות
  surface-sunken: "oklch(95.2% 0.012 85)"       # thead, שדות disabled, רצועות שקטות
  line: "oklch(88.5% 0.014 80)"                 # גבול כרטיס, מפריד מול canvas
  line-soft: "oklch(93.2% 0.011 82)"            # מפרידי שורות פנימיים
  line-strong: "oklch(65% 0.016 78)"            # גבול שדות/כפתור משני — 3.16:1
  ink: "oklch(22% 0.025 205)"                   # כותרות דף, הטקסט החזק ביותר
  ink-body: "oklch(29% 0.022 205)"              # טקסט גוף
  ink-mid: "oklch(37% 0.022 205)"               # תאי טבלה
  ink-soft: "oklch(44% 0.022 205)"              # תוויות, כפתורי ghost
  ink-muted: "oklch(50% 0.021 205)"             # כותרות עמודה, טקסט משני
  ink-faint: "oklch(54% 0.019 205)"             # placeholder ורמזים — 4.57:1 על canvas
  ink-ghost: "oklch(75.5% 0.012 85)"            # אייקונים כבויים, סימנים דקורטיביים
  # פעולה = מותג פטרול; היא מבנית בלבד ולעולם אינה מחליפה סטטוס.
  action: "oklch(43.08% 0.0701 211.43)"          # #0f5a66 — כפתור ראשי, לינק, גרף
  action-hover: "oklch(38.06% 0.0625 212.45)"
  action-solid: "oklch(47.64% 0.0772 210.72)"    # צ'יפים מלאים, מילויי בחירה
  action-soft: "oklch(93.41% 0.0182 205.32)"     # badge ספירה
  action-on-soft: "oklch(38.19% 0.0588 211.94)"
  action-wash: "oklch(96.69% 0.0086 188.11)"    # hover שורה
  action-line: "oklch(64.32% 0.0541 205.4)"      # גבול hover על כרטיס לחיץ
  focus: "oklch(52.98% 0.0911 212.98)"           # טבעת פוקוס — 4.98:1
  # shell כהה: המסגרת הממותגת של המוצר; אזור העבודה נשאר נייר חם.
  shell: "oklch(31.81% 0.0517 213.09)"           # #073942
  shell-ink: "oklch(97% 0.009 85)"
  shell-ink-soft: "oklch(88% 0.012 85)"
  shell-ink-dim: "oklch(73% 0.015 85)"
  shell-heading: "oklch(72% 0.016 85)"
  # גרפים — חמישה גוונים מבניים; צבע סמנטי רק כשיש משמעות עסקית.
  chart-1: "{colors.action}"
  chart-2: "oklch(49% 0.09 245)"                # כחול פלדה
  chart-3: "oklch(52% 0.095 275)"               # אינדיגו־צפחה
  chart-4: "oklch(53% 0.09 305)"                # סגול מעושן
  chart-5: "oklch(52% 0.085 335)"               # mauve מעושן / "אחר"
  # זהות אזור — "איפה אני", לעולם לא "מה קרה". כינויים של הרמפה המבנית, לא גוונים חדשים.
  section-documents: "{colors.chart-4}"          # מסמכים — סגול מעושן
  section-procurement: "{colors.chart-3}"        # רכש — אינדיגו־צפחה
  section-money: "{colors.chart-5}"              # כספים — mauve מעושן
  chart-grid: "oklch(89% 0.014 80)"
  chart-tick: "oklch(50% 0.021 205)"
  chart-label: "oklch(44% 0.022 205)"
  chart-tick-strong: "oklch(37% 0.022 205)"
  star: "amber-400"                             # כוכבי דירוג: קישוט, לא סטטוס
  star-hover: "amber-300"
  # ---- שפת הצבעים הסמנטית — העוגנים ללא שינוי ----
  # done=הושלם (emerald) · await=ממתין (amber) · alert=חריגה (rose) · info=מידע (sky) · idle=ניטרלי (slate)
  # כל משמעות חושפת שישה משטחים: wash/line/soft/on-soft/fg/solid — הערכים ב-@theme ב-src/index.css.
  trend-up: "rose-700"                          # התייקרות — תמיד עם חץ
  trend-down: "emerald-700"                     # הוזלה — תמיד עם חץ
typography:
  # Production: Almoni Neue מורשה. Free/CI: Noto Sans Hebrew מקומי. אין תלות ב-Google Fonts.
  headline:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, Arial, ui-sans-serif, system-ui, sans-serif"
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.25
  headline-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1.25
  title:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "17px"
    fontWeight: 600
    lineHeight: 1.4
  body:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.5
  label:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "14px"
    fontWeight: 500
    lineHeight: 1.45
  table-header:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "13px"
    fontWeight: 500
    note: "ללא uppercase וללא tracking — לעברית אין רישיות; היררכיה מגודל/משקל/צבע בלבד"
  micro:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "12px minimum"
    fontWeight: 500
    note: "12px הוא המינימום לכל ניווט, badge, chart label ומידע עסקי"
  kpi:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "28px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-compact:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "20px"
    fontWeight: 600
    lineHeight: 1.2
  kpi-compact-wide:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.2
  mobile-input:
    fontFamily: "Almoni Neue SupplyFlow, Noto Sans Hebrew, ..."
    fontSize: "16px"
    fontWeight: 400
    note: "מונע zoom אוטומטי ב-Safari iOS; במסך רחב השדה חוזר ל-14px"
  num:
    fontFamily: "primary font family"
    fontFeature: "tabular-nums lining-nums"
    note: ".num אינו כופה LTR או Mono; tech-id שמור רק ל-UUID, hash, checksum ו-ID פנימי"
  tech-id:
    fontFamily: "ui-monospace, Cascadia Mono, SFMono-Regular, Consolas, monospace"
    fontWeight: 400
    note: "חריג מוגבל למזהי מכונה אמיתיים; תמיד isolate ובדרך כלל dir=ltr"
rounded:
  control: "0.5rem"    # rounded-lg — כפתורים, שדות
  card: "0.75rem"      # rounded-xl — כרטיסים ומשטחי עבודה
  modal: "1rem"        # rounded-2xl — מודאלים (מרחפים — נשארים רכים)
  pill: "9999px"       # badges
spacing:
  xs: "8px"
  sm: "10px"
  md: "12px"
  lg: "16px"
  xl: "20px"
components:
  button-primary: { backgroundColor: "{colors.action}", textColor: "#ffffff", rounded: "{rounded.control}", padding: "8px 14px" }
  button-primary-hover: { backgroundColor: "{colors.action-hover}" }
  button-secondary: { backgroundColor: "{colors.surface}", textColor: "{colors.ink-mid}", border: "{colors.line-strong}", rounded: "{rounded.control}" }
  button-danger: { backgroundColor: "alert-solid", textColor: "#ffffff" }
  button-ghost: { textColor: "{colors.ink-soft}", hoverBackground: "{colors.canvas}" }
  input: { backgroundColor: "{colors.surface}", border: "{colors.line-strong}", focusRing: "{colors.focus}", rounded: "{rounded.control}" }
  card: { backgroundColor: "{colors.surface}", border: "{colors.line}", rounded: "{rounded.card}", shadow: "petrol-tinted soft" }
  badge-*: { pattern: "bg-{tone}-soft + text-{tone}-on-soft", rounded: "{rounded.pill}" }
  note-*: { pattern: "bg-{tone}-wash + border-{tone}-line + text-{tone}-on-soft" }
---

# Design System: SupplyFlow — "חדר בקרה פטרול"

## 1. Overview

**Creative North Star: "חדר בקרה שקט" עטוף במסגרת פטרול עמוקה.**

SupplyFlow מטפל בכסף, ולכן סביבת העבודה שלו נשארת רגועה: נייר חם, טקסט כהה וספרות מיושרות
בפונט המוצר. מסגרת הפטרול — סרגל הצד, פעולות, לינקים וגרפים מבניים — נותנת למוצר זהות ברורה בלי לצבוע
כל משטח. **צבע סטטוס עדיין שמור לטענה עסקית בלבד**; הפטרול מזהה את המוצר ואת הפעולה, לא "הצלחה"
או "חריגה". התוצאה צריכה להרגיש כמו כלי מקצועי שנעים לעבוד בו שעות, לא תבנית אדמין ולא מסך סטרילי.

**Key Characteristics:**
- שקט כברירת מחדל; פטרול = מסגרת/פעולה, וצבעי done/await/alert/info = משמעות עסקית בלבד.
- מספרים משתמשים בפונט הראשי וב־tabular figures: טורי סכומים מתיישרים בלי אופי של טרמינל.
- עברית RTL מלאה: properties לוגיים בלבד; אין uppercase.
- עומק רך: צל פטרול עדין בכרטיס במנוחה, והרמה של פיקסל אחד רק בכרטיס לחיץ.
- אין נתון מזויף: מדד בלי נתונים מציג `—`, לא `0`.

## 2. Colors

### עקרון מסגרת הפטרול
**`action` הוא צבע המותג המבני.** כפתור ראשי, לינק (`.link`), צ'יפ נבחר ועמודת הגרף העדכנית משתמשים
בפטרול; שאר עמודות הגרף משתמשות ברמפה המבנית `chart-2`–`chart-5`. ה-shell כהה יותר ומשמש מסגרת;
פריט הניווט הפעיל הופך לגלולת נייר בהירה כדי להישאר
מובחן ונגיש. אסור להשתמש בפטרול כתחליף לסטטוס סמנטי.

### שפת הסטטוס — ללא שינוי
ארבע משמעויות + ניטרלי, כל אחת עם שישה משטחים (wash/line/soft/on-soft/fg/solid):
**done** (emerald) הושלם/תקין/שולם · **await** (amber) ממתין לטיפול מצדנו · **alert** (rose)
חריגה/דחוף/הפסד אפשרי + פעולה הרסנית · **info** (sky) הכדור אצל גורם חיצוני · **idle** (slate)
היעדר טענה. דרגות `solid` ו־trend הוכהו ל־700 כדי להבטיח AA עם טקסט לבן/טקסט קטן; המשמעות והגוון
לא השתנו. **חוק ארבע המשמעויות** ו**חוק האות הכתום** בתוקף: משמעות תמיד מלווה טקסט או אייקון.

**מילון אחד לכל הרכיבים (02.08.2026).** ‏`Tone` מ־`src/lib/status.ts` הוא אוצר-המילים היחיד לטון:
‏`badge-*`, ‏`note-*`, ‏`AttentionItem`, וגם **`KpiCard` ו־`Scorecard`/`ScoreTone`**, שעד לסבב הזה
קיבלו שמות פלטה (`slate|green|amber|red|blue`) ומיפו אותם פנימית לאותם טוקנים. השמות הוסרו: ערך
כמו `openBalance ? 'amber' : 'slate'` הוא טענת „ממתין" מול „היעדר טענה", והשם הצבעוני רק הסתיר איזו
מהחמש נטענת. המחלקות שמוצגות בפועל לא השתנו. **אין להחזיר שם פלטה כ־prop של טון.**

### שפת המקום — זהות אזור (18.08.2026)

עד לסבב הזה הצבע נשא **מצב** היטב ו**מקום** בכלל לא: כל מסך נראה זהה, ומי שנכנס למערכת עשרות
פעמים ביום לא קיבל שום רמז לאן הגיע. שלושת תחומי העבודה שהמשתמש היומי נע ביניהם מקבלים עכשיו
מבטא: **מסמכים · רכש · כספים**. הם אינם משמעות חמישית או שישית, והם אינם יכולים להפוך לכזו —
מסיבות מבניות, לא מסיבות של משמעת:

**‏1. מקור אחר.** טון נבחר מהנתונים (`Tone` ב-`status.ts`; חשבונית באיחור → `alert`). מבטא נבחר
מה-URL — ‏`sectionOf(pathname)` ב-`lib/quickActions.ts`, ליד `isRouteFamilyActive` ומאותה משפחת
כללים. **אין prop, אין argument, אין שדה נתונים שבוחר מבטא.** על `/invoices` המבטא הוא „כספים”
בין אם החשבונית מאושרת, פתוחה או חורגת. מצב לא יכול להגיע לאוצר-המילים הזה, ולכן אין צורך לסמוך
על כך שאיש לא ינסה.

**‏2. משטח אחר.** טון הוא **מילוי מתחת לטקסט** (`badge-*`, ‏`note-*`) או הטקסט עצמו (`text-*-fg`).
מבטא אינו אף אחד מהשניים: הוא **סמן ו־rule בן 3px**, ולעולם אין עליו או בתוכו טקסט. אין כלל
מבטא שקובע רקע מתחת ל־text node.

**‏3. טוקנים אחרים.** אף כלל מבטא אינו מפנה ל-`--color-{done,await,alert,info,idle}-*`, ואף
‏`.badge-*`/`.note-*` אינו מפנה ל-`--color-section-*`. שני הקירות נבדקים ב-`lib/quickActions.spec.ts`
מול `src/index.css` עצמו, לא מול הכוונה — ולצדם בדיקה שסורקת את כל ה-tsx ונופלת אם `data-section`
נכתב בקובץ כלשהו מלבד `Layout.tsx` או מביטוי שאינו `sectionOf(...)`. ‏`layoutActiveState.spec.tsx`
מצמיד את סמן הניווט הפעיל עצמו.

**מאיפה הגוונים.** ‏`--color-section-*` הם **כינויים של הרמפה המבנית** `chart-3/4/5`, לא גוונים
חדשים: הרמפה כבר מוצהרת כלא-סמנטית, הניגודיות שלה מול נייר כבר בפנקס, ורמפה אחת שזזה עדיפה על
שתיים שנפרדות. שני חברי הרמפה האחרים **מוחרגים במפורש**: ‏`chart-1` הוא `action` — פטרול הוא
המסגרת, ואזור שלובש אותו נקרא „צבע הפעולה”; ‏`chart-2` יושב על גוון 245, **אותו גוון בדיוק** של
‏sky/`info` (‏sky-700 נמדד 243), ואזור שלובש אותו ייקרא כטון מידע. שלושת הנותרים יושבים על
275/305/335, וההקצאה ממקסמת את המרחק מהטון שכל תחום מציג הכי הרבה: מסמכים מציגים הכי הרבה `info`
‏(sky 243) ולכן מקבלים 305; כספים מציגים הכי הרבה `await` ‏(amber 49) ו-`done` ‏(emerald 166) ולכן
מקבלים 335; רכש מקבל 275, רחוק מכולם.

**איפה זה מופיע — שני מקומות, שניהם על נייר.** המבטאים מודדים 5.0–5.7:1 מול נייר ורק ~2.2:1 מול
‏`shell`, ולכן פס שמצויר ישירות על הסרגל הכהה הוא קישוט בלתי-נראה. לכן:
- **סמן הניווט הפעיל** — האייקון של הפריט הפעיל בלבד, בתוך גלולת הנייר שלו (`.section-glyph`).
  ‏19 אייקונים צבועים היו הופכים את הסרגל ללוח דוגמאות; אייקון צבוע **אחד** עונה „באיזה חלק של
  העסק אני”, והתווית לידו עונה „באיזה מסך”.
- **סמן העמוד** — ‏rule של 28×3px מתחת ל-`h1` ‏(`.section-mark`), ב-`PageHeader` וב-`RecordHeader`.
  ‏28px אינם נקראים כמפריד, והם אינם נושאים שום מידע שהכותרת שלידם אינה אומרת במילים — כלומר
  יתירות, לא מידע שנשען על גוון (WCAG 2.1 AA).

**מה שאין לו מבטא, ולמה.** ‏`/dashboard` ומסכי הבקרה (`/alerts`, ‏`/exceptions`, ‏`/expenses`,
‏`/reports`, ‏`/analytics`) מביטים **על פני** שלושת התחומים ואין להם מקום משלהם לסמן; ‏`/settings`,
‏`/onboarding` ו-`/admin` אינם תחומי עבודה. אלה אינם חורים במפה — הם ההצהרה שהמפה עושה.
בהיעדר תחום פשוט אין attribute, ‏`.section-mark` נשאר `display:none`, ולא נשמר עבורו מקום.

**גוון = תחום, סמל = מסך.** שלושה מבטאים ולא עשרים: ‏`/orders`, ‏`/receiving`, ‏`/inventory`
ו-`/suppliers` חולקים את מבטא הרכש, והאייקון והכותרת מבדילים ביניהם. מערכת התמצאות עם עשרים
צבעים היא מבחן זיכרון, לא עזר. אף אחד משני הרמזים אינו עומד לבדו.

**המנגנון:** ‏`Layout.tsx` הוא **הקובץ היחיד** שקובע `data-section` — על פריט הניווט הפעיל ועל
`<main>` של המסך הנוכחי. ‏`index.css` ממפה `[data-section="…"]` ל-`--section-accent` פעם אחת, ושני
הצרכנים קוראים את המשתנה. אין הקסים, אין מחלקה נבנית דינמית, ואין קובץ CSS פר-מסלול.

### Named Rules
**חוק המסגרת הממותגת:** פטרול מותר רק בשלד, בפעולה ובגרפים מבניים; הוא לעולם אינו מקודד מצב עסקי.
**חוק שני אוצרות-המילים:** טון בא מהנתונים ומופיע כמילוי מתחת לטקסט; מבטא אזור בא מה-URL ומופיע
כסמל או כ־rule בלבד. אסור למשטח לדבר בשתי השפות, ואסור לטוקן להיות מושאל בין השתיים.
**חוק צ'יפ הספירה:** ‏`bg-action-soft`/`text-action-on-soft` הוא צ'יפ **הספירה** של המערכת (פס
המסמכים שלא תויקו בסרגל, ‏`activeFilters` ב-DataTable, ‏`TaskLine`). הוא אומר **כמה**, לעולם לא
**כמה דחוף** — ולכן הוא נשאר פטרול ואינו מפר את חוק המסגרת. ספירה שכן טוענת דחיפות לובשת
‏`badge-*` מהמילון, כמו הספירה שעל כותרת „דורש טיפול היום”.
**חוק הטוקנים:** אפס מחלקות פלטה גולמיות (`slate-*`, `indigo-*`...) ב-tsx — נאכף בגרפ שב־Runbook להלן.
צבע או צל חדש = טוקן חדש ב־@theme + עדכון המסמך הזה יחד.
**חוק החשיפה המדורגת:** בדשבורד הפעולה הבאה, הסכומים הראשיים והמגמות המרכזיות גלויים מיד; פירוט
תפעולי משני נשאר זמין ב־disclosure. אין למחוק נתון כדי להפחית עומס, ואין להסתיר פעולה קריטית מאחורי
hover או בורר תצוגה.

## 3. Typography

**Production UI:** Almoni Neue במשקלים 400/500/600 בלבד, מתוך קובצי webfont מורשים שמסופקים
בזמן build ואינם נשמרים בריפו. **Free/CI:** Noto Sans Hebrew מקומי, ברישיון OFL, באותה היררכיה.
Assistant ו־IBM Plex אינם חלק מחוזה ה־runtime. Noto הוא גם fallback של Almoni, ואחריו Arial ו־system UI.

**מספרים:** `.num` משתמש באותה משפחה ראשית עם `tabular-nums` ו־`lining-nums`. המחלקה אינה כופה
`direction:ltr`, אינה משנה יישור ואינה משתמשת ב־Mono. `.tech-id` הוא החריג היחיד: UUID, hash,
checksum, job/submission ID או קוד פנימי אמיתי. מזהים אטומיים מקבלים `dir="ltr"`; סכומים, כמויות,
אחוזים, תאריכים, טלפונים ומספרי מסמכים נשארים בפונט הראשי.

### Hierarchy
Page title ‏600/24px מובייל ו־26px רחב (`page-title`) · Section title ‏600/17px (`section-title`) ·
Body ‏400/15px/1.5 · Label/control/table body ‏500 או 400/14px · Table header ‏500/13px —
**בלי uppercase ובלי tracking דקורטיבי** · Micro/chart/badge ‏500/12px מינימום · KPI ראשי
‏600/26px מובייל ו־28px רחב · KPI קומפקטי ‏600/20px מובייל ו־24px רחב. אין משקל 700 בממשק;
`strong` ו־`b` נעצרים ב־600 ו־`font-synthesis` כבוי.

בשדות קלט במובייל גודל הטקסט הוא לפחות `1rem`; זהו כלל שימושיות שמונע zoom אוטומטי ב־Safari iOS,
לא שינוי בהיררכיית הטיפוגרפיה של תוכן המערכת.

**חוק הספרות המיושרות:** ערך מספרי מקבל `class="num"` עם tabular figures. המחלקה אינה עוטפת משפט
עברי שלם. במחרוזת מעורבת עוטפים רק את הערך ב־`span.num`; מזהה טכני אטומי מקבל `tech-id` ו־`dir="ltr"`.

## 4. Elevation

העומק רך, צבוע ועקבי; אין צל שחור גנרי:
- **מונח:** `shadow-card` + ‏hairline חם — מורגש מול canvas בלי להפוך כל אזור לאריח.
- **חריג עומק בשם — dashboard-depth:** בתוך מרכז הבקרה בלבד, כרטיסים מקבלים
  `shadow-dashboard`, צל פטרול מעט עמוק יותר שמפריד בין אזורי ההחלטה. הסלקטור חייב להישאר
  ממוסגר תחת `.dashboard-depth`; אין להחיל את הטוקן על כרטיסים תפעוליים בשאר המערכת.
- **מוזמן:** ‏`.card-link-hover` — גבול `action-line`, ‏`shadow-card-hover` והרמה של 1px בלבד.
- **מרחף:** ‏shadow-xl למודאל (rounded-2xl), ‏shadow-lg לטוסט — תמיד עם backdrop ‏`shell/50` במודאל.
- **צף קבוע:** ‏`--shadow-fab` — פטרול מעט חזק מ־`--shadow-menu`; שמור ל־puck הצילום המורם בסרגל
  הפעולות במובייל בלבד. (הוא נקרא `fab` כי הוא שירת גם את ה-FAB הדסקטופי, שהוסר ב-09.08.2026.)

**רישום z-index:** בר קבלה 30 · סרגל צד/כותרות/סרגל פעולות מובייל ‏40 · מגירה/מודאל 50 · טוסט 60.

## 5. Components

### Navigation — מסגרת פטרול
סרגל צדי, טופ־בר ומגירת מובייל משתמשים ב־`bg-shell` (`#073942`) עם קווים `shell-ink/10`.
**פריט פעיל = גלולת נייר (`bg-shell-ink text-shell`)**; פריט רגיל `shell-ink-soft` עם hover של
`shell-ink/10`. **האייקון של הפריט הפעיל בלבד לובש את מבטא האזור** (`.section-glyph`, ראו „שפת
המקום”): הגלולה היא משטח הנייר היחיד בסרגל, ולכן המקום היחיד שבו מבטא עובר 3:1 שם. אייקוני פריטים
שאינם פעילים נשארים `currentColor`, ופריט ללא תחום עבודה (מרכז הבקרה, בקרה, הגדרות) נשאר ללא מבטא.
‏theme-color ב־index.html = ‏`#073942`.

הטופ־בר במובייל **אינו** נושא מבטא: הכותרת שם יושבת על `shell` הכהה, שבו הרמפה מודדת ~2.2:1. במסך
טלפון סמן העמוד שב־`PageHeader` הוא נושא הרמז, והוא בתוך אזור העבודה הבהיר.

מתחת ל־`lg` מעטפת האפליקציה היא phone mode: כפתור התפריט פותח מגירה מימין עם כל יעדי הניווט המורשים,
וסרגל קיצורי דרך ופעולות תפקידי קבוע בתחתית. הסרגל התחתון הוא משטח היברידי מפורש, לא תחליף
לניווט המלא: הוא מחזיר את הגישה הישירה של המוצר ליצירה, לצילום ולקבלה בלי להסתיר מסכים בתפריט. שם הארגון הוא זהות
הדייר, לצד סימן ושם SupplyFlow כזהות המוצר. המגירה ננעלת ל־focus, נסגרת ב־Escape ומחזירה focus
לטריגר. המעטפת משתמשת ב־`100dvh`, ב־logical properties וב־safe-area; תוכן אינו רשאי להרחיב את
רוחב המסמך או להסתתר מאחורי ניווט, בר משימה או overlay.

**בסרגל הדסקטופי כל הקבוצות פתוחות; במגירת הטלפון החשיפה מדורגת.** „עבודה שוטפת” גלויה תמיד.
„ניהול” ו„בקרה” הן disclosures סגורות כברירת מחדל, והקבוצה שמכילה את המסך הפעיל נפתחת אוטומטית.
כך נשמרים כל היעדים המורשים בלי להפוך 21 קישורי owner לרשימה שטוחה. החשבון נוסע בסוף הרשימה
הגלילה ואינו מוצמד לתחתית, כך שגם במסך נמוך היעדים אינם נחתכים.

הגדרות owner נשארות באזור תחתון נפרד. `NAV_SECTIONS` הוא קטלוג המסלולים וההרשאות, לא תפריט שמציג
תמיד את כל מבנה המערכת. `isRouteFamilyActive` משותף לסרגל, למגירה ולמובייל כדי שפרטי רשומה ישמרו
את הקשרם.

### Buttons
Primary פטרול · Secondary נייר + ‏`line-strong` ביחס 3:1 · Danger ‏`alert-solid` (תמיד מאחורי
ConfirmDialog עם סיבה כשנרשם ביומן) · Ghost ‏`ink-soft`. לכל btn טבעת focus, מעבר 150ms ומשוב
לחיצה של 1px; ‏disabled אינו זז ו־busy נשאר עם ספינר בתוך הכפתור.
כל כפתור, שדה קלט ויעד ניווט או פעולה מרכזי הם בגובה מגע של `44px` לפחות, בכל viewport.

### DataTable — כולל מובייל
שולחן העבודה של המערכת. דסקטופ: thead ‏`surface-sunken`, שורות `divide-line-soft`, ‏hover
‏`.row-hover` (כולל `active:` כאפורדנס מגע), מיון בכפתורים אמיתיים עם aria-sort.
**מובייל (`mobile="cards"`):** מתחת ל-**`lg`** הטבלה מוסתרת ובמקומה רשימת כרטיסים נגישים.
‏**הגבול אוחד ל-`lg` בהכרעת בעלים (09.08.2026), וזה תיקון ולא העדפה:** הטבלה התחלפה ב-`md`
(‏768px) בזמן שהמעטפת — סרגל צד, כותרת טלפון, סרגל פעולות — מתחלפת ב-`lg` (‏1024px). התוצאה הייתה
שבין 768 ל-1023 פיקסלים המשתמש קיבל **את טבלת הדסקטופ בתוך מעטפת הטלפון**, כלומר טבלה עם
`whitespace-nowrap` גלובלי בתוך מסגרת עם מגירה וסרגל תחתון — טאבלט במאונך. שער הדפדפן לא מדד את
הרוחב הזה (הוא בודק 320/360/390/430). שני הגבולות הם `lg` עכשיו, וכל פיצול כרטיסים↔טבלה בקוד
מיושר אליו: ‏`DataTable`, ריכוז ההוצאות ופריטי ההזמנה.
- ‏`Column.priority`: ‏1 כותרת · 2 פרטים (ברירת מחדל) · 3 מוסתר במובייל.
- ‏`Column.mobileLabel`: תווית לפני הערך; ‏`null` = ערך מדבר-בעד-עצמו (badge, כסף).
- ‏`mobileTitle(row)` — שורת כותרת; ‏`mobileTrailing(row)` — ‏badge בקצה; שורה לחיצה = ‏button ‏44px+.
- חיפוש/סינון/עימוד משותפים לשתי התצוגות; מיון אינו נגיש במובייל (v1, מתועד).
- ברירת המחדל היא `mobile="cards"`; ערכים בכרטיס נשברים לשורות ואינם נחתכים ב־ellipsis.
- `mobile="scroll"` שמור לבחירה מפורשת בתצוגות מטריצה/preview צפופות שבהן משמעות העמודות מחייבת זאת.
- במסך החשבוניות הטלפון חושף כ־quick filters רק „הכל”, „התקבלה”, „ממתינה לאישור” ו„דורשת בירור”.
  „בבדיקה” ו„מאושרת” נשארים ב„סינון ותצוגה”; אם אחד מהם פעיל מקישור או מהגיליון, השבב שלו חוזר
  להופיע. אין סטטוס עסקי שנמחק — זו חשיפה מדורגת, לא שינוי מסלול.

בדוח החודשי טבלאות החשבוניות, התשלומים והזיכויים מוצגות ככרטיסי ledger עד `xl`; הטבלאות המלאות
נשמרות לדסקטופ רחב ולהדפסה בלבד. כך אין גלילה אופקית פנימית ב־320/390/768/1024 ואין אובדן נתון.

### ActionMenu — תפריט פעולות שורה
טריגר `MoreVertical` ריבועי בכפתור ghost (‏44px+), נפתח כ־portal ל־body כדי שלא ייחתך ע״י ה־overflow של
DataTable, וממוקם מהקצה הלוגי של הטריגר (RTL-aware). המשטח ישר וללא עיגול פינות: `bg-surface + border-line` עם טוקן
הצללה ייעודי `--shadow-menu` — פטרול עדין בין `shadow-card-hover` לרובד הטוסט/מודאל. ניווט מקלדת
מלא (roles menu/menuitem, חיצים/Home/End/Escape), סגירה בגלילה ובלחיצה בחוץ. פעולה הרסנית =
`text-alert-fg`, גלויה תמיד בתפריט — לעולם לא מוסתרת מאחורי hover (חוק החשיפה המדורגת).
מחובר ל־DataTable דרך `rowActions` — עמודת קצה בדסקטופ, קצה לוגי של הכרטיס במובייל.

### ניווט מובייל, קיצורי דרך ופעולות
מתחת ל־`lg` מגירת ההמבורגר מציגה את כל היעדים המורשים לתפקיד. בנפרד ממנה, סרגל הפעולות המהירות
מחזיר את המסלול הישיר שנבחר למוצר: `owner`/`office` מקבלים חמישה יעדים — „הזמנה חדשה”, „מרכז
הבקרה”, „צילום מסמך”, „קבלת סחורה” ויעד מסמכים מורשה. אצל `owner` היעד הוא „בקרת מסמכים”;
אצל `office` הוא „מסמכים”. **הצילום הוא הפריט השלישי והמרכז הפיזי של הסרגל**
גם ב־390px; ה־puck משתמש ב־logical inset וב־`margin-inline: auto`, לא בהיסט ידני שתלוי בכיוון.
‏„חשבונית חדשה” אינה בסרגל: האפליקציה מקבלת חשבוניות ספק ואינה מנפיקה אותן. `accountant` מקבל
„מרכז הבקרה”, „חשבוניות” ו„תשלומים”. התפקידים ההיסטוריים `kitchen`/`payer`/`supplier` אינם
חשבונות מוצר ואינם מקבלים סרגל. צילום מסמך פותח את מצלמת/בורר הקבצים הילידיים.

השם הנגיש של הסרגל הוא „קיצורי דרך ופעולות”: הוא מכיל במכוון גם פקודת צילום וגם קישורים תכופים.
ההגדרה הזו מונעת ממנו להתחזות לניווט ראשי, בזמן שהמגירה נשארת קטלוג הניווט המלא.

הסרגל מכבד את `safe-area-inset-bottom`, והמעטפת שומרת עבורו מרווח תוכן לפי **מלוא מעטפת ה־puck**
(גובה הסרגל ועוד 1.5rem), כך שגם הכיפה המורמת אינה מכסה את קצה הרשומה האחרונה.
הסרגל בנוי grid לפי מספר הפעולות ואינו יוצר גלילה אופקית גם ב־320px וב־zoom. כל יעד מגע הוא 44px לפחות, לכל פעולה שם נגיש,
ותווית גלויה נשברת לשתי שורות לכל היותר. הסרגל נשאר מלא גם במסכי יצירה וקבלה; כל מסך כזה מגן על
הנתונים לפני ניווט. בקבלת סחורה בר המשימה נערם מעליו. צילום במסך ממוקד אינו מעביר אוטומטית
למסך בדיקת המסמך. במובייל אין FAB או speed-dial נוסף.

ב־320–359px סימן הבית שבמרכז הטופ־בר מוסתר חזותית כדי שכותרת המסך לא תקרוס; זהות SupplyFlow
נשארת בשורת הטקסט ובמגירה, ויעד הבית המלא נשאר בראש המגירה. שלוש פעולות הטופ־בר מאוחדות למקבץ
אחד, וכל אחת נשארת 44px ושומרת שם נגיש. ב־landscape שגובהו עד 480px גובה הטופ־בר והסרגל מצטמצם,
ה־puck יורד ל־48px והתוויות נשארות על שורה אחת — ללא הקטנת יעד מגע מתחת ל־44px.

טוסטים והודעת עדכון PWA חיים באותה ערימת overlays מעל הסרגל. כאשר `phone-taskbar` קיים, הערימה
מורמת מעליו ואינה מכסה את פעולות השמירה/שליחה; בדיקת הדפדפן מודדת את המלבנים בפיקסלים. ב־`sm`
ומעלה הודעת ה־PWA נצמדת ל־inline-start. (היא נצמדה לשם כדי להתרחק מה־FAB הדסקטופי; ה־FAB הוסר,
ההצמדה נשארה — היא נכונה בזכות עצמה, כי היא מרחיקה את ההודעה מסרגל הפעולות התחתון.)

**ב־`lg` ומעלה אין משטח פעולות מהירות בכלל — ה־speed-dial הוסר בהכרעת בעלים (09.08.2026).**
שלוש סיבות: הוא **שיכפל** פעולה שכל דף כבר נושא בכותרת שלו; הוא הציע „צילום מסמך” — מצלמה — במחשב
שולחני; והוא ישב צמוד לפינה בלי inset והתנגש ברצועת הכותרת. איתו הוסרו הטריגר, תפריט ה־roving-focus,
העברת ה־focus בין שני משטחים, מאזין `matchMedia` שסגר את התפריט בחציית `lg`, ו־`.phone-fab`
ו־`.speed-dial-*` ב־`index.css`. ‏`--shadow-fab` **נשאר** — ה־puck המורם של המצלמה בסרגל המובייל
משתמש בו.

**מה שאסור להסיק מזה:** שהפעולות המהירות מיותרות. במובייל הסרגל התחתון הוא משטח יחיד וישיר והוא לא
נגע. ההסרה נשענת על כך שבדסקטופ הפעולה **כבר על המסך** — ביום שדף יאבד את הפעולה הראשית שלו,
הנימוק הזה נופל.

### PageHeader / RecordHeader / LifecycleStrip

`PageHeader` הוא אנטומיית ברירת המחדל למסך תפעולי: breadcrumbs אופציונליים, `h1`, מטא־מידע
שמוסיף הקשר ופעולות. אין כותרת שיווקית גדולה ואין subtitle חסר משמעות. `RecordHeader` מוסיף זהות
רשומה, סטטוס, עובדות ופעולה ראשית אחת; פעולות משניות נשארות שקטות יותר.

שניהם מרנדרים **סמן אזור** מתחת ל־`h1` — ‏`.section-mark`, ‏rule של 28×3px במבטא התחום. **אין לו
prop**: הוא קורא את `--section-accent`, ש-`Layout` בלבד קובע ורק מן ה-URL, ולכן אין דרך שקורא
יגרום לו לומר „מאושר” או „באיחור”. במסך ללא תחום עבודה הוא `display:none` ואינו תופס מקום.

`LifecycleStrip` מציג רק שלבים אפשריים שהמסך יכול להסיק מהחוזה הקיים. הוא מדגיש מצב נוכחי ו„הפעולה
הבאה”, אך אינו ממציא שלב טכני או פיננסי. מצב טעינת פרטים משתמש ב־`RecordSkeleton`, לא בספינר שמוחק
את צורת המסך.

לרצועה שלושה מצבים אופציונליים שנוספו עם רצועת עיבוד המסמך, וכולם כפופים לאותו כלל:

- **`failed`** — השלב הנוכחי נעצר. הסמן שלו עובר ל־`alert-*` עם `AlertTriangle`, וה־sr-only אומר
  „השלב שנעצר”. השלבים שלפניו נשארים `done-*`: הם באמת הושלמו, וכשלון מאוחר אינו מוחק אותם.
- **`detail`** — שורה אחת מתחת לרצועה שמתארת מה השלב הנוכחי עושה, במילים של הקורא.
- **`progress`** — בר דטרמיניסטי לשלב הנוכחי. **מוצג אך ורק כשיש ספירה אמיתית מהשרת.** אין בר
  ריק ב־0%, אין בר אינדטרמיניסטי בלולאה, ואין אחוז מחושב מזמן שחלף — שלושתם טענות על העבודה
  שהנתונים אינם תומכים בהן, וזו בדיוק „אנימציה שאינה מצב”. בהיעדר ספירה מוצג טקסט שאומר שהמספר
  טרם דווח. המילוי הוא `action` **כמבנה בלבד**: הוא אומר כמה, לא אם זה הצליח.
  ‏`role="progressbar"` נכתב עם `aria-valuenow/valuemax` רק כשהם אמיתיים, ו־`aria-valuetext` נושא
  את המשפט העברי.

הבר של הרצועה מציג ספירת עמודים (`עמוד 7 מתוך 27`) ולא אחוז — אחוזים נשארים בתוך „פרטים טכניים”
לפי החלטת מסך הבדיקה.

(הפסקה שעמדה כאן תיארה את עיגון מעטפת ה־speed-dial ל־`inset-block-start`. היא הוסרה עם המשטח.)

שני המצבים מוסתרים במסכים ממוקדים שכבר מחזיקים פעולה ראשית — `/orders/new`, ‏`/invoices/new`, ‏`/receiving/:orderId` — כדי שלא יתחרו בה.

### התראות חדשות
פעמון ריבועי בגודל מגע 44px חי בכותרת ומוביל ישירות ל־`/alerts` — בלי dropdown. תג המספר מציג
רק רשומות `notifications` ש־`read_at` שלהן ריק, ומוגבל חזותית ל־`99+`. פתיחת מסך ההתראות מסמנת
אותן כנקראו רק אחרי שהסריקה החיה הסתיימה בהצלחה; תקלה בטעינה אינה מנקה עבודה שלא נצפתה.
מסך ההתראות הוא תור הפעולות המלא ואינו משכפל את כרטיסי הסיכום העסקי של הדשבורד. הגדרת Push
למכשיר חיה במסך זה בלבד, משום שהוא נגיש גם ל־owner וגם ל־office.

### מסמכי חשבונית
מסמכים ישירים של החשבונית ומסמכי קבלות הסחורה המקושרות מופיעים ברשימה מאוחדת אחת עם מפרידי שורה,
תצוגה מקדימה, מקור ותאריך. אין גלריות מקבילות ואין כרטיס בתוך כרטיס. העלאת PDF/תמונה נשארת פעולה
גלויה; הסרה זמינה רק למסמך הישיר ובהרשאה מתאימה, דרך ActionMenu.

### הזמנה חדשה וריכוז הוצאות
הזמנה חדשה היא תהליך אמיתי בן שני שלבים: בחירת מוצר עם בורר כמות inline, ואז בחירת ספק וסיכום
הספק הזול ביותר לכל מוצר. ההשוואה היא רשימת ledger עם מחיר נבחר, מחיר זול והפרש — לא כרטיסי הצעה.

בריכוז הוצאות טבלת הספקים היא המקור הראשי והמדויק. פירוט קטגוריות הוא `<details>` משני וסגור
כברירת מחדל, ומוצהר בו שהסכומים נגזרים ממחירי snapshot של הזמנות מקושרות. אין תרשים donut שמציג
פילוח משוער כאילו היה סכום חשבוניות מדויק.

### Dashboard — פס הכסף
שלושת מדדי הכסף = **BandStat בתוך `.card` אחד** — מקטעים עם מפרידים לוגיים (`border-s`/`border-t`,
לעולם לא divide-x הפיזי), לא שלושה אריחים זהים. האנטומיה הקבועה של כל BandStat היא:
אייקון סמנטי בתוך chip שקט · תווית · ערך סופי מיידי עם tabular figures · הקשר תקופה · delta אופציונלי מול אותם
ימים בחודש הקודם. ל־"נרכש" ול־"שולם" בלבד נוסף ספארקליין קומפקטי של שמונה שבועות, ורק כשהערך
הראשי קיים ולפחות שני שבועות מכילים פעילות אמיתית. "יתרת חשבוניות" מציגה את מספר החשבוניות הפתוחות
בלבד — לעולם לא ספארקליין או מגמה מומצאים. אין טקסט הסבר ארוך בפס; במובייל כל BandStat הוא שורה
קומפקטית וההשוואה נשארת גלויה.

**מיקום הפס: ראשון על המסך מתחת ל־`lg`, שלישי מ־`lg` ומעלה.** בטלפון הוא היה מתחת לשני כרטיסים,
כך שהקיפול הראשון ב־390 לא הכיל אף מספר כספי — סעיף 12 דורש שמנהל שנכנס יראה מצב, לא רק רשימת
משימות. בדסקטופ שלושת האזורים נכנסים לקיפול בכל מקרה, ולכן שם נשמר הסדר המקורי ש"דורש טיפול" פותח
אותו. השינוי הוא **`order-first lg:order-none` בלבד** — סדר ה־DOM אינו זז, גם כדי שקוראי מסך
וטענת סדר הכותרות בשער האיכות ימשיכו לראות את "דורש טיפול" ראשון. מכיוון ש־`order` מזיז רק ילדי
flex/grid, מכל האזורים הוא `flex flex-col gap-5` ולא `space-y-5`.

### Dashboard — טיפול ופירוט תפעולי
כותרת AttentionZone נושאת את הספירה **פעם אחת ובאלמנט אחד**: „דורש טיפול היום” עם `badge-*` של
מספר סוגי הטיפול, והמשבצת בקצה נושאת רק את חשיפת ה־₪ — עובדה אחרת. הפעמון והתג לוקחים את הטון
מהשורות שהם סופרים (`alert` גובר על `await`), ובאפס שורות הפעמון כבה ל-`ink-ghost`: פעמון כתום
קבוע מעל „אין משימות דחופות כרגע” הוא צבע שטוען את היפוכו של המשפט שלידו. יחידת המידה („סוגי
טיפול”) נשארת בשם הנגיש. ‏`Dashboard.tsx` החזיק עותק שלישי של אותה ספירה בשורת ה־`meta` שלו; הוא הוסר.

ב־AttentionZone פעולות `alert`/`await` גלויות תמיד, בשתי עמודות מדסקטופ ובעמודה אחת במובייל. שורות
לידיעה, מדדים ללא נתונים ומצבים תקינים מאוחדות תחת `<details>` טבעי וסגור. כל קישור ושורת summary הם
יעד מגע של 44px לפחות, עם focus-visible ו־active גלויים.

ארבעת אזורי הפירוט — חריגים, התייקרויות, יתרות ספקים ומשימות — חיים בכרטיס אחד בשם "תמונת מצב
תפעולית". כל אזור הוא disclosure עם count והנתון העסקי המרכזי בשורת ה־summary; פתיחת אחד סוגרת את
האחרים באמצעות `name="dashboard-operations"`. אזור ריק מוצג כשורת all-clear שאינה אינטראקטיבית,
והרשומות והקישורים נשמרים בתוך האזור הפתוח.

### Chips / Notes / Skeletons / EmptyState
badge = ‏soft/on-soft · ‏note = ‏wash/line/on-soft ‏(+idle לניטרלי) · ‏skeleton = ‏`bg-line`,
משקף את צורת התוכן · ‏EmptyState מלמד את המסך.

### Charts
recharts לא קורא CSS vars מ-attributes — ‏`src/lib/theme.ts` ‏(`chartTheme()`) קורא את הטוקנים
פעם אחת ב-getComputedStyle ומזרים מחרוזות צבע אמיתיות (`bar`, ‏`bars`, ‏grid/tick/label/tickStrong/
trendUp/trendDown/flat). ‏`bars` הוא מערך `chart-1`–`chart-5`; אפס הקסים ב-tsx וצבע סמנטי בגרף רק
כשהוא נושא משמעות עסקית.

כל המגמות חיות בלוח חזותי יחיד וללא tabs: שלוש תצוגות גלויות יחד — הוצאות חודשיות בעמודות, תמהיל
קטגוריות בחודש הנוכחי כ־donut, והשוואת רכש מול תשלומים בשמונה השבועות האחרונים בקווים. בגרף החודשי
העמודה העדכנית תמיד `chart-1`, ועמודות סמוכות מקבלות גוונים שונים. בקטגוריות מוצגות ארבע הגדולות
ו־"אחר" שמסכם במדויק את היתר ומקבל תמיד `chart-5`; donut מוצג רק כשהסכום הכולל חיובי, ואחרת מוצג
empty state. קווי הרכש והתשלומים מובחנים גם בדפוס קו או בסמן נקודה, לא בצבע בלבד. התוויות, הסכומים,
רשימת הקטגוריות ו־ARIA נושאים את המשמעות; אין מקרא מיותר. ‏Tooltip משתמש ב־`cursor={false}` כדי לא
להציג overlay אפור.

buckets שבועיים נושאים `label`, סכום ומספר רשומות. bucket חסר מיוצג ב־`total: null`, בעוד אפס מדוד
מותר רק כשקיימות מאחוריו רשומות; נקודה חסרה אינה מקבלת עמודה, נקודה או תווית צבעונית. כל תרשים מספק
שם נגיש ותיאור מלא של הסדרה והערכים. שלושת הגרפים נכנסים פעם אחת בלבד בחשיפה הראשונה של לוח המגמות
ל־viewport; רענון נתונים אינו מפעיל את האנימציה מחדש. אם `IntersectionObserver` חסר, או תחת
`prefers-reduced-motion`, כל הגרפים מוצגים סטטיים מיד. סדרה ללא פעילות אמיתית מוחלפת ב־empty state.

### Motion budget — דשבורד
אזורי ההחלטה נכנסים פעם אחת כשהשלד מוחלף בנתונים: 350ms, תזוזה של 7px ו־60ms בין אזורים.
ה־stagger **נמנה לפי הסדר הנראה, לא לפי סדר ה־DOM**: כל אזור מצהיר על הצעד שלו בעצמו
(`[--dash-step-mobile:N]` ו־`[--dash-step:N]` ב־`Dashboard.tsx`), וה־media query ב־`index.css`
בוחר איזה מהשניים נספר. הסיבה: פס הכסף מוזז ל־ראש המסך מתחת ל־`lg`, ולכן `:nth-child` היה מאחר
דווקא את הכרטיס הראשון שהעין פוגשת. אזור שלא הצהיר על צעד מקבל 0ms — נראה בסקירה, לא מזיק בייצור.
חותמת "עודכן ב־" מתחלפת ב־fade של 180ms עם `aria-live="polite"`. אנימציית שלושת הגרפים מוגבלת
לכניסה הראשונה של לוח המגמות ל־viewport. אין count-up לסכומים, אין נורית LIVE, אין הבהוב מחזורי ואין אנימציית hover
שמשנה מידע. תחת reduced-motion מבוטלים stagger, draw-in, lift ותנועת מעבר; מצב טעינה פונקציונלי
יכול להישאר כספינר איטי.

### עורך הזמנה וגלריית מסמכים

עורך ההזמנה שומר את אותה היררכיה צפופה: מצב השמירה והפעולה הראשית נמצאים בכותרת, וסיכום האישור
הוא ledger של נתונים עסקיים — לא רשת KPI חדשה. חיסכון חיובי, תוספת עלות וחוסר קו־בסיס נכתבים
במפורש; צבע לבדו אינו נושא את הטענה.

גלריית המסמכים היא טבלה מסוננת בדסקטופ וכרטיסי `DataTable` במובייל. סטטוס התיוק משתמש רק
ב־`badge-await`/`badge-done`, תאריכים וסכומים מקבלים `num`, וכל פעולת צפייה או תיוק נשארת גלויה
למקלדת וביעד מגע של 44px לפחות. לא נוספו טוקנים או שפה חזותית חדשה.

### מסך בדיקת מסמך שהתקבל (`DocumentAssessmentPanel`, 11.08.2026)

זהו המסך שבו אדם לוחץ על כפתור שיוצר חוב. שלושה כללים בעלי-שם, שכולם נגזרים מכך:

**‏1. "הקובץ נשמר" ו"הנתונים אושרו" הם שני משפטים, לעולם לא אחד.** שני שדות נפרדים מהשרת, שתי
שורות עם שני אייקונים, ובדיקה שנופלת אם אי-פעם ייצרו אותו טקסט. מי שצילם חשבונית ליד המשאית השיג
דבר אחד: הקובץ לא ילך לאיבוד. שום דבר בעסק שלו לא השתנה עדיין.

**‏2. "מה יקרה באישור" מונה גם את מה ש*לא* יקרה.** המשפטים השליליים אינם ריפוד — "לא ישתנו כמויות
שהתקבלו" הוא הדבר המועיל ביותר לקרוא לפני אישור תעודת משלוח. כל משפט מוצמד בבדיקה מול הענף
ב-`apply_reviewed_document` שהוא מתאר: **הבטחה שהשרת לא מקיים גרועה משתיקה, כי האדם קרא אותה.**

**‏3. ערך שאינו ידוע הוא מקף.** ‏`fmtMoneyExact`/`fmtNum` מדפיסים `—` ל-`null`, וזו הסיבה שהם
בשימוש בכל מספר במסך. מחיר בסיס חסר שמודפס כ-₪0.00 הוא **טענה שהספק הסכים לתת אותו במתנה**.

כפתור האישור **נשאר פעיל** גם כשיש ממצאים חוסמים, עם משפט שמסביר שהשרת יבדוק שוב וינמק. חסימה
בצד הלקוח הייתה מכניסה מקבל-החלטות שני למסלול ומאפשרת לשניים להיפרד. הטבלה היא `DataTable` עם
`mobile="cards"`, ספרות ב-`num`, טוקנים בלבד, ו-properties לוגיים בלבד.

### `Disclosure` — קיפול מונה, וחוק תיבת הדואר של החריגים (18.08.2026)

‏`Disclosure({ title, count, tone, summary, name, onToggle })` ב-`ui.tsx` הוא הרכיב המשותף היחיד
לקיפול. הוא `<details>/<summary>` טבעי — ‏`list-none`, סמן ברירת המחדל מוסתר, יעד מגע `min-h-11`,
שברון שמסתובב ומכבד `motion-reduce`, ותג ספירה שהוא `badge-*` מאוצר המילים של `status.ts`. הוא
נגזר מה-`OperationsDisclosure` המקומי של הדשבורד; זו אותה שפה ויזואלית, לא שנייה.

**חוק תיבת הדואר של החריגים.** מסך בדיקה אינו טופס שבו אדם מאמת מחדש את כל עבודת המכונה. מה
שהמכונה בטוחה בו מתקפל לשורת סיכום אחת עם מספר; מה שדורש הכרעה אנושית נשאר פתוח. ‏42 עמודים
נפתחים כ„39 מסווגים · 3 דורשים בדיקה”, ולא כ-42 עורכים.

שלושה סייגים שאינם ניתנים למשא ומתן:

**‏1. קיפול אינו הסתרה.** ממצא בחומרת `error`/`critical`, טענה על מלאי, שינוי שהמכונה ביצעה,
ביטחון נמוך ופעולה בלתי-הפיכה — נשארים פתוחים. מה שנסגר הוא ראיה ופירוט, לא סיכון.

**‏2. הספירה על שורת ה-summary היא התוכן.** „39 מסווגים” נושא את המידע שהרשימה הייתה נושאת;
בלי המספר הקיפול הוא מחיקה. ‏`<details>` סגור עדיין מרנדר את ילדיו — לרשימה ארוכה מגדרים את
הבנייה על `onToggle`, אחרת נחסך צבע בלבד ולא עבודה.

**‏3. מה שנשלח אינו תלוי במה שנפתח.** אישור מניפסט של חבילה שולח את כל העמודים, כולל המקופלים,
עם אותו `manifest_hash`. קיפול הוא החלטת תצוגה; ‏payload שמשתנה לפי מצב `<details>` הוא באג.

## 6. Do's and Don'ts

### Do:
- **Do** כל צבע דרך טוקן; מחלקות המשמעות (`badge-*`, `note-*`, `text-*-fg`) הן ה-API.
- **Do** ‏`class="num"` על כל תא מספרי — המונו הוא חלק מהשפה, לא קישוט.
- **Do** ‏properties לוגיים בלבד; ‏`—` למדד ללא נתונים; ניגודיות ≥4.5:1 (ledger ב־Runbook להלן).
- **Do** מעברים 150–250ms ‏ease-out מכבדי reduced-motion; ‏`active:` לצד `hover:` ברכיבים משותפים.

### Don't:
- **Don't** להשתמש בפטרול כסטטוס או לפזר גוון מותג נוסף; מעטפת המותג נשענת על גוון אחד.
- **Don't** ‏uppercase/tracking על טקסט עברי.
- **Don't** צל שחור/כבד; ‏glassmorphism; אנימציה שאינה מצב — אסור בחוקה.
- **Don't** להציג count-up, נורית LIVE או מגמה שאין מאחוריה רשומות אמיתיות.
- **Don't** מחלקת פלטה גולמית או הקס ב-tsx — גרפ האכיפה שלהלן חייב להישאר ריק.
- **Don't** ‏emerald/amber/rose/sky מחוץ למשמעותם; אין להמציא משמעות שישית.
- **Don't** ‏`border-left/right` צבעוני כפס-סימון — ‏`note-*` בלבד. (זו גם הסיבה שסמן האזור הוא
  ‏rule תחתון קצר ולא פס בקצה הלוגי של הכרטיס.)
- **Don't** ‏`--color-section-*` כטענה על נתונים, כרקע מתחת לטקסט, כ־prop של רכיב, או על משטח
  ‏`shell` הכהה (~2.2:1). מבטא אזור מגיע מה-URL בלבד ומופיע כסמל או כ־rule בלבד.
- **Don't** להרחיב את מפת האזורים למסלול רק כי הוא חדש. שלושה תחומי עבודה, ולמסך שמביט על פני
  כולם אין מבטא — זו ההצהרה, לא פער.

### Retheme Runbook + אכיפה

**רה-תמה סטטית עתידית = שלושה קבצים:** ‏(1) בלוק ה־@theme ב־`src/index.css` · ‏(2) ‏`index.html`
ל־theme-color/פונטים · ‏(3) המסמך הזה. מעבר שמשנה את *קוטביות* ה־shell (בהיר↔כהה) מחייב בנוסף audit
לצרכני `bg-shell` כדי לוודא שהם משתמשים ב־`shell-ink-*`, כפי שנעשה ב־Login/AcceptInvite בסבב T6.

**גרפ האכיפה** (חייב להחזיר אפס שורות על `src --include='*.tsx'`):
```
\b(bg|text|border|ring|fill|stroke|divide|outline|decoration|placeholder|accent|caret|shadow)-(slate|gray|zinc|indigo|violet|blue|emerald|green|amber|yellow|orange|rose|red|sky|cyan|teal)-[0-9]{2,3}\b
```
וכן `#[0-9a-fA-F]{6}` — אפס הקסים ב-tsx (הגרפים דרך chartTheme).

**Contrast ledger (נמדד 21.07.2026, סקריפט OKLCH→sRGB):**
סדרת הגרפים מול surface (`#fefcf9`), לאחר המרה ל־sRGB: chart-1 ‏`#0f5a66` — ‏7.67:1 ·
chart-2 ‏`#2f6590` — ‏6.07:1 · chart-3 ‏`#59649f` — ‏5.49:1 · chart-4 ‏`#785e96` — ‏5.36:1 ·
chart-5 ‏`#87567c` — ‏5.63:1. כל חמשת הצבעים בתוך gamut של sRGB ועוברים את רף 3:1.

**מבטאי האזור (נמדד 18.08.2026, אותו סקריפט OKLCH→sRGB).** אין ערכים חדשים — אלה כינויים —
אבל המשטחים שמתחתם חדשים, ולכן נמדדו מחדש. הרף הרלוונטי הוא **3:1 (רכיב לא-טקסטואלי)**:
‏section-documents = ‏chart-4 ‏`#785e96` — ‏shell-ink ‏5.03 · ‏canvas ‏5.00 · ‏surface ‏5.36 ·
‏surface-sunken ‏4.77 · **shell ‏2.29 — נכשל, ולכן אסור על המסגרת הכהה**.
‏section-procurement = ‏chart-3 ‏`#59649f` — ‏shell-ink ‏5.15 · ‏canvas ‏5.12 · ‏surface ‏5.49 ·
‏surface-sunken ‏4.88 · **shell ‏2.24 — נכשל**.
‏section-money = ‏chart-5 ‏`#87567c` — ‏shell-ink ‏5.29 · ‏canvas ‏5.25 · ‏surface ‏5.63 ·
‏surface-sunken ‏5.01 · **shell ‏2.18 — נכשל**.
שני המשטחים בשימוש הם `shell-ink` (גלולת פריט הניווט הפעיל) ו-`canvas` (אזור העבודה מתחת לכותרת),
ושניהם ≥5.00:1 — כלומר עוברים גם את רף הטקסט 4.5:1, אף שהמבטא אינו טקסט. ‏`chart-2` נמדד 245
מול ‏sky-700 ‏(`#0369a1`) שנמדד 243 — אותו גוון, ולכן הוא מוחרג מהמבטאים ונשאר סדרת גרף בלבד.
ink/canvas ‏15.68 · ‏ink-body/surface ‏13.68 · ‏ink-mid ‏10.10 · ‏ink-soft ‏7.51 · ‏ink-muted ‏5.81 ·
‏ink-faint/surface ‏4.90 · ‏ink-faint/canvas ‏4.57 · לבן על action ‏7.85 · על action-hover ‏9.75 ·
על action-solid ‏6.43 · ‏focus/surface ‏4.98 · ‏line-strong/surface ‏3.16 · ‏action-line/surface ‏3.17 ·
‏shell-ink/shell ‏11.51 · ‏shell-soft ‏8.74 · ‏shell-dim ‏5.25 · ‏shell-heading ‏5.06.
מילויי הסטטוס בדרגת 700 עם לבן: המינימום הוא await ‏5.05; trend-up/down בדרגת 700 עוברים 5.2.

**סיכוני תחזוקה:** ‏`badge-${tone}`/`note-${tone}` נבנות דינמית — כל ערך Tone חייב מחלקה ב-index.css
(אין שגיאת build אם חסרה). ‏Tailwind v4: מחלקה מותאמת = ‏@apply על utilities אמיתיים בלבד; קומפוזיציה
דרך ‏@utility (כמו btn/badge/note). טוקני `shadow-card*` חייבים להישאר צבועי פטרול ועדינים.
‏chartTheme() ממוטמן ברמת מודול — theme switch עתידי חייב invalidation.
‏`--color-section-*` הם כינויים של `chart-3/4/5`: שינוי ברמפת הגרפים **מצבע גם את הניווט וגם את
סמני העמוד**, וזו צימוד מכוון (רמפה מבנית אחת) ולא תופעת לוואי — מי שמזיז את הרמפה מודד מחדש את
שתי השורות בפנקס. ‏`data-section` נקבע ב-`Layout.tsx` בלבד; אם משטח נוסף יצטרך מבטא, הוא מקבל
אותו מ-`sectionOf(pathname)` ולא מ-prop.
