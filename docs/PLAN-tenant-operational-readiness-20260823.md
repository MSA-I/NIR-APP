# תוכנית — מוכנות תפעולית רב־דיירית

נכתב: 23.08.2026. מחליף בפועל את `SCALE-TO-MANY-TENANTS.md` (‏17.08.2026) כתמונת מצב פעילה.

**מצב:** `DESIGNED_HERE / PARTIALLY_CLOSED / FINAL_PROOF_BLOCKED`

> **השער של הבעלים:** *הוכחת scale הסופית מחכה ל־Report Jobs ולחוזי היסוד.*
> **מסקנת התוכנית:** רוב עבודת המוכנות **כבר נסגרה** ולא תועדה במסמך המקור. מה שנשאר פתוח הוא
> **פריט אחד ממשי** (תור הוגן), **פריט תפעולי אחד** (דומיין מייל), ו**הוכחה** שאכן חוסמת.

---

## 1. `SCALE-TO-MANY-TENANTS.md` מיושן — ארבע טענות נפלו

| טענה במסמך | המצב בפועל | ראיה |
|---|---|---|
| ‏`:30-32` — ‏worker על Docker Desktop בתחנת Windows אחת | **§39 נסגר 17.08.** שני containers על VPS ‏(Ubuntu 24.04, ‏4 vCPU, ‏7.7GB), ‏`--restart unless-stopped`, ‏Docker ‏`enabled` בעלייה, ‏ufw חוסם נכנס פרט ל־SSH | `DEBT-REGISTER.md:203-208`; טקסט Windows נשמר רק בבלוק `<details>` ב־`:225-240` |
| ‏`:32` — אין ניטור | **§43 נסגר 17.08.** ‏`0142` + cron כל 5 דקות כותב `critical` ל־`notifications` לכל בעלים פעיל. שני ספים מדודים: `queued` מעל 20 דקות (‏המתנה לגיטימית גרועה ביותר שנמדדה: 745ש׳), ו־lease שפג מעל 5 דקות. דדופ שעתי. אומת בייצור: אפס שורות בתור בריא, ‏`cron.job_run_details` = `succeeded` | `DEBT-REGISTER.md:241-262` |
| ‏`:97` — אין מסלול הרשמה עצמית ב־`App.tsx` | **`src/App.tsx:257` — `/signup` קיים.** ‏#172 הוכרע 19.08 ומבטל את #12; ממומש ב־`0159` + `supabase/functions/public-signup` + `p55`, עם rate limit **שנספר במסד** ולא בזיכרון פונקציה, טופס שאינו יכול לבחור מסלול/סטטוס/מע״מ, וחשבון בעלים שנפתח **לא מאומת** | `OPEN-DECISIONS.md:474`; ‏#165 (`:458`) — נרשם מדף הנחיתה מקבל Free |
| ‏`:151-154` — ארבע הכרעות בעלים פתוחות | **שלוש הוכרעו:** תמחור (#194–#196, ‏22.08), מי מקים דייר (#172/#165), ‏retention (#261). ‏**הרביעית אינה הכרעה ממוספרת בכלל** | להלן §4 |

**מה שנשאר נכון במסמך:** הלולאה סדרתית לכל container, התור FIFO גלובלי, דומיין המייל לא אומת,
ורשם ה־definer אינו ריק.

**המסמך `SCALE-TO-MANY-TENANTS.md` עדיין untracked ב־git.** יש להכריע אם הוא נכנס לריפו כהיסטוריה
או מוחלף על ידי מסמך זה.

---

## 2. הפריט הפתוח היחיד שהוא עבודת מוצר: תור הוגן פר־דייר

### 2.1 המצב

**הבעלים של פונקציית ה־claim הוא `0129`, לא `0045` ולא `0103`.** נבדקו כל המיגרציות `0130`–`0170`
כולל כל בלוק תיקון־גוף (`0132:342,383,397`; ‏`0133:50…1135`; ‏`0136:1503,1520`; ‏`0137:2266…2519`;
‏`0139:1215`) — **אף אחת אינה נוגעת ב־`claim_document_processing_job`**.

```
0129:56    order by j.priority desc, j.created_at, j.id
```

FIFO גלובלי, בלי כל חלוקה לפי `org_id`. הגדר הפר־דיירית היחידה שקיימת ב־claim היא
`private.organization_write_allowed_fenced(j.org_id)` (`0129:55`), והיא גדר offboarding/השעיה —
לא הוגנות.

**‏`priority` אינו פתרון.** העמודה קיימת (`0045:405`, ברירת מחדל 100) אבל **אף מסלול enqueue אינו
קובע אותה במפורש** (`0169:232-239`, ‏`0162:132`, ‏`0155:409`) — כלומר כל ג'וב בייצור הוא priority 100.
ההוגנות חייבת לבוא מה־`order by`, לא מהעדיפות.

### 2.2 משטח השינוי המינימלי

**חייב להשתנות:**
1. **מיגרציה חדשה** — ‏`create or replace function public.claim_document_processing_job(text, integer)`
   שמשחזרת את כל גוף `0129` כשרק `0129:56` מוחלף בסבב מעגלי לפי `org_id`, ותקרת ג'ובים פעילים
   פר־דייר מקופלת לתוך ה־`where` ב־`0129:51-55` (פרדיקט `count(*)` מתואם — כדי שיישאר **בתוך אותה
   טרנזקציית claim**, כפי שהמסמך דורש).
2. **אינדקס חדש/משונה** — ‏`document_processing_jobs_claim_idx` (`0045:434-436`) אינו נושא `org_id`.
3. **בלוק אימות־מחדש A1/A3/A5** בסוף המיגרציה (תבנית `0129:216-229`). ‏`scripts/check-exemption-pin.ts`
   **מפיל את `npm run build`** על כל מיגרציה אחרי 0057 שחסרה אותו.
4. **רענון רשם ייצוא A6** — ‏**רק אם** התקרה מוסיפה עמודה (תקדים `0141:196-200`, `0143:235-239`).

**אסור שישתנה:**
- ‏`claim_document_processing_job_input` (`0136:1449`) — מאציל, אינו זקוק לעריכה
- ‏`worker.py`, ‏`gateway.py`, ‏`document-processing/index.ts` — ‏**`gateway.py:108-124` אוכף קבוצת
  מפתחות מדויקת בת 14. הוספת מפתח ל־payload של ה־claim שוברת את העובד.**

### 2.3 שומרים — מה נשבר ומה לא

| שומר | נשבר? | הערה |
|---|---|---|
| הצמדות `md5(prosrc)` בבדיקות (`p20:104`, `p41:93`, `p44:270`, `p58:239`) | **לא** | אף אחת אינה מזכירה את `claim_document_processing_job`. אומת בגריפ ממצה |
| הצמדת גוף ב־`private.scope_definer_enforcements` (`0095:186-190,277`) | **לא** | הבודק מדלג על כל פונקציה שיש לה שורת פטור (`0095:270-275`), ול־claim יש אחת מ־`0057:234` (`'service-role-trusted-path'`). ה־`drop`+`create` ב־`0129` שימר את השורה כי טקסט החתימה לא השתנה |
| מונה הפטורים ב־`p9_five_domains.sql` | **רק אם תתווסף פונקציית definer חדשה** | ערך: **89** ב־checkout הראשי (שורה 323), ‏**90** ב־`f0c5cd2` (שורה 331). החלפת הפונקציה הקיימת שומרת על אותה שורה |
| נוכחות בלוק A1/A3/A5 | **כן אם יושמט** | מפיל את `npm run build`, לא את שער ה־SQL |
| ‏A6 סחף עמודות ייצוא | **כן אם תתווסף עמודה** | — |
| טענות התנהגות קיימות | **בסיכון התנהגותי, לא טקסטואלי** | אף אחת אינה גורפת את ה־`order by`. ‏**`p45:391-398` מבצע ארבע תפיסות רצופות באותו ארגון — תקרה מתחת ל־4 תשבור אותו.** לבדוק את ה־fixture לפני בחירת ברירת מחדל |

**תשובה פשוטה: שינוי גוף ה־claim אינו שובר אף הצמדת `md5(prosrc)`.**

### 2.4 סוויטת ההוכחה

- **מספר פנוי:** ‏`p60` ב־checkout הראשי; אבל ב־`f0c5cd2` — הבסיס האמיתי של worktrees הדיירים —
  ‏`p60` ו־`p61` כבר קיימים. **להשתמש ב־`p62`.**
- **צורה:** ‏`begin;` / ‏`rollback;` (תבנית `p59`), לא `dblink` — הוכחת 50־מול־1 אינה זקוקה למרוץ.
- **עוזר טענות:** `pg_temp.pNN_assert` בתקדים `p42:6-13` / `p59:7-14`.
- **תפקיד מוצהר פר־פקודה בתוך הקובץ**, לא בכותרת: `perform set_config('request.jwt.claim.role','service_role', true)` + `set local role service_role` (`smart_document_processing.sql:721-722`).
- **רישום — קובץ אחד בלבד:** שורת `Invoke-SqlTest "supabase\tests\p62_….sql" "<תווית>"` ב־`scripts/check-quality-gates.ps1`
  בתוך אזור הריצה שמתחיל ב־`$qaMutex = Enter-QaMutex` (`:1153`). **אין מה להוסיף ב־`scripts/ci-sql-suites.mjs`** —
  הוא מפרסר את קובץ ה־PowerShell בזמן ריצה (`:18-27,38-62`). תנאי: השורה חייבת להשתמש ב־literals
  במרכאות כפולות **בשורה אחת**; המפרסר זורק על פחות משני ארגומנטים מצוטטים ואינו מטפל במשתנים.

### 2.5 פער שהתגלה אגב — ‏`p61` אינו רץ ב־CI

`supabase/tests/p61_assistant_history_ui.sql` קיים ב־`f0c5cd2` אבל **אינו מופיע כלל ב־`check-quality-gates.ps1`**.
מכיוון ש־`ci-sql-suites.mjs` מפרסר את הקובץ הזה, ‏**`p61` לעולם לא רץ ב־CI.** פריט חוב עצמאי.

---

## 3. קיבולת — המספרים רעועים, לא הארכיטקטורה

**הלולאה סדרתית: מאומת.** ‏`worker/ocr/src/worker.py:789-798` — כל איטרציה קוראת
`process_scan_one` ואז `process_one` **ברצף, בחוט אחד**, וכל אחת תופסת ג'וב אחד וחוסמת עליו
(`:751-755`). אין executor, אין `gather`.
*(הערה: `SCALE:47` מפנה ל־`worker.py:756-763` — הציטוט נדד; שם יושבת היום חתימת `process_scan_one`.)*

**‏`OCR_PAGE_CONCURRENCY` הוא עמודים בתוך ג'וב: מאומת.** ‏`worker/ocr/src/ocr.py:332-343` —
ה־pool מתוכנן מול `len(pages)` של המסמך הנוכחי; הנימוק בקוד (`:335-338`) הוא שעמוד סרוק דורש
שתיים־שלוש קריאות QA עצמאיות. חסום לטווח 1–4 (`ocr.py:102-108`).

**שני הקלטים של המכפיל 16–20 workers רעועים:**

| קלט | ערך | אמינות |
|---|---|---|
| משך ג'וב | 370 שניות | **מדידת ייצור אמיתית של ג'וב אחד בדיוק** — אימות אחרי החלפת image מיושן ב־HEAD. ‏`PROGRESS.md:1108-1109`. ‏**n=1**, לא התפלגות |
| מסמכים ליום לדייר | 10 | **אינו הכרעה ממוספרת.** אין שורה תואמת ב־`OPEN-DECISIONS.md`. זהו מספר להמחשה בתוך `SCALE:59` עצמו |

**מסקנה: אין להרחיב את הבריכה לפי המספרים האלה.** ‏`OCR-WORKER-HOSTING.md:90-92` מתעד שייחוס
מדידה שגוי בדיוק בתאריך הזה עלה שעת חקירה — שתי בריכות שנלחמו על אותם ג'ובים גרמו לתקרת 900
שניות להיראות כמו 300.

---

## 4. הכרעות בעלים — מצב מעודכן

| נושא ב־`SCALE:151-154` | מצב אמיתי |
|---|---|
| **מודל תמחור** | **הוכרע 22.08** — ‏#194 סולם `חינם/בסיס/פרו/פרימיום/ביזנס`; ‏#195 מחירים; ‏#196 **אין קרדיטים, כל היכולות פתוחות בכל מסלול, ההבדל הוא נפח בלבד**. ‏**`DECIDED_FOR_PLANNING / NOT_IMPLEMENTED / NOT_ACTIVATED / NOT_DEPLOYED`** — ‏`OPEN-DECISIONS.md:517-521`: *"מחיר אינו קיים בסכימה"* |
| **מסמכים ליום לדייר** | **אין שורה ממוספרת.** חוסם רק את גודל הבריכה (§3), לא חיוב — המכסות הן פר־תקופה, ואין מגבלה יומית בסכימה |
| **מי מקים דייר** | **הוכרע ושוגר** — ‏#172/#165. השורה במסמך המקור מיושנת |
| **retention לאחר עזיבה** | **הוכרע 22.08** — ‏#261, אין purge עיוור. ‏**המנגנון לא נבנה**, ו‏#261 מפורשות *"אינה מרשה purge כעת"*. §30 נשאר פתוח |

**חסמים אמיתיים לחיוב** (אינם חלק ממוכנות תפעולית, אבל מוצגים למניעת בלבול):
`#194` (‏runtime מכיר 3 מסלולים, ההכרעה מגדירה 5) · `#195` (חשבון Paddle מאומת + קטלוג versioned) ·
`#164` (‏cutover ל־Legacy) · `#124` (החלפת placeholder `מפעילת השירות` לפני השקה בתשלום).

---

## 5. מספרי מיגרציה — פנקס ההקצאה המחייב

**הכרעת בעלים 23.08.2026:** ההקצאה נגזרת מהקוד, ולהלן היא. הקצאה בבלוקים של שישה, ומדידה —
לא ניחוש. **זהו פנקס ההקצאה היחיד; אין מקור שני.**

### 5.1 מיגרציות

| טווח | בעלים | מצב |
|---|---|---|
| `0001`–`0167` | mainline | הוחל. ‏166 קבצים, רציף **פרט ל‏`0072`** |
| `0168`–`0170` | ‏`main` ‏(`f0c5cd2`) | **הוחל בייצור** |
| `0171`–`0176` | `codex/financial-data-contracts-20260823` | קומיט `345bcb1`, לא ממוזג |
| `0177`–`0182` | `codex/document-ocr-automation-20260823` | קומיט `88c3bb3`, לא ממוזג |
| **`0183`** | **Rules Engine** | **פנוי — היעד הנכון** |
| `0184`–`0185` | `agent/subscriptions-db-20260823` | קומיט. ‏`0185_signup_anchored_usage_period` = **התיקון ל‏#242** (§8) |
| `0186` | — | **פנוי** |
| `0187`–`0188` | `agent/subscriptions-provider-20260823` | קומיט |
| `0189` | — | **פנוי** |
| `0190` | `agent/domain-email-20260823` | קומיט |
| `0191`+ | — | **פנוי** |

**‏`0072` הוא פער היסטורי קבוע. אין למחזר אותו לעולם** — מספר מיגרציה הוא מזהה בפנקס, לא משבצת פנויה.

### 5.2 סוויטות SQL

| טווח | בעלים |
|---|---|
| `p59` | portal (mainline) |
| `p60`–`p61` | ‏`main` ‏(`f0c5cd2`) — ‏**`p61` אינו רשום ב־CI**, ראו §2.5 |
| `p62`–`p64` | חוזים פיננסיים |
| `p65`–`p68` | document-ocr |
| **`p69`** | **פנוי — Rules Engine** |
| `p70` | subscriptions-db |
| `p71`–`p72` | תפוסים; `p73`+ פנוי |

*(‏`p8`, ‏`p10`, ‏`p12` הם פערים היסטוריים — אין למחזר.)*

### 5.3 ההתנגשות היחידה שקיימת בפועל, והיא כפולה

| מספר | תובע א׳ | תובע ב׳ |
|---|---|---|
| `0179` | `0179_heic_scan_derivative_provenance.sql` — document-ocr, **קומיט** | `0179_alert_rules_engine.sql` — rules-impl, **קומיט** |
| `p67` | `p67_document_media_contracts.sql` — document-ocr, **קומיט** | `p67_rules_engine.sql` — rules-impl, **קומיט** |

**השינוי הלא־מקומיט ב־`rules-impl` שממספר `0179 → 0183` ו־`p67 → p69` הוא בדיוק התיקון להתנגשות
הזו.** הוא נכון וחייב להיקומט לפני כל מיזוג. ‏`0183` ו־`p69` פנויים בכל הענפים.

### 5.4 שני סיכונים שנשארים

1. **ה־checkout הראשי יושב על `0167`.** מי שייצר "את המיגרציה הבאה" שם יקבל `0168` **ויתנגש
   עם מיגרציה שכבר הוחלה בייצור**. יש למשוך את `f0c5cd2` לפני כל עבודת DB.
2. **אין שום מנגנון ב־`scripts/` שמזהה התנגשות מספרים בין worktrees.** זה איך ש‏`0179` ו‏`p67`
   נתפסו פעמיים בלי שאיש ידע. פריט חוב עצמאי; המסמך הזה הוא כרגע התחליף היחיד.

**בסיס לא תואם:** ‏`f0c5cd2` **אינו אב־קדמון** של ה־checkout הראשי (`35edc3f`); ה־merge-base הוא
`d857d7b`. ‏worktrees הדיירים יושבים על `f0c5cd2`, שכולל `0168`–`0170` ואת `p60`/`p61` —
**שאינם קיימים ב־checkout הראשי כלל.**

### 5.5 סדר מיזוג מחייב

`0171`–`0176` ← `0177`–`0182` ← `0183` ← `0184`–`0185` ← `0187`–`0188` ← `0190`.

הסדר **אינו מוסכמה** — ‏`0172`/`0173`/`0174` מתקנות גופי פונקציות חיות לפי עוגני מחרוזת, ומיזוג
מחוץ לסדר מפיל אותן בזמן `apply`. ראו `PLAN-report-jobs-20260823.md` §2.4.

---

## 6. הוכחת ה־scale הסופית — למה היא חסומה

**"הוכחת scale סופית" אינו מונח מוגדר בריפו.** מה שכן מוגדר:

| רכיב | מקור | מצב |
|---|---|---|
| soak של שבעה ימים בלי מגע יד + ריסטארט מכונה שאינו מפסיק עיבוד | `SCALE:43` | **לא תועד מעולם** |
| סוויטת הוגנות 50־מול־1 | `SCALE:76-77` | **אין תור הוגן** (§2) |
| ‏§2, ‏§4, ‏§5 | — | **אין הגדרת הוכחת סגירה כלל** — פער במסמך המקור |
| איחוד כל שורות מטריצת ה־rollout | `CLAUDE.md:107-114` | שינוי חוצה־משטחים מאחד דרישות ו**אינו** מחזיר אוטומטית את השער הידני (`:116`) |
| checks טריים על ה־SHA | `CLAUDE.md:117` | **"PASS היסטורי לעולם אינו מחליף check טרי"** |

**החסם החמור ביותר — חמישה שערים שאינם ניתנים לאימות ב־CI:**
`check-p0-security.ps1` (‏862 שורות), ‏`check-p0-upgrade.ps1`, ‏`Invoke-PriceListEdgeSmoke`,
`Invoke-OcrEdgeSmoke`, ‏`check-p4-integrated-journey.cjs` — ‏`CLAUDE.md:87`, ‏`DEBT-REGISTER.md:534-539` (§34).

> **שים לב לצורה:** בודק גבולות האמון P0 ו־smoke ה־OCR הם **בדיוק שני המשטחים שנושאים את
> טענת ה־scale** — והם היחידים ש־CI אינו יכול לטעון עליהם דבר. ‏`CLAUDE.md:87`: **"תיק ירוק אינו
> טענה שהם עברו."** אומת בפועל ב־`PROGRESS.md:506-508`, שם הם לא רצו.

---

## 7. מגבלה שאין לה מסלול זול: ריבוי סניפים

‏**89** פונקציות `SECURITY DEFINER` מוצמדות ב־`p9_five_domains.sql:323` (‏`= 90` ב־`f0c5cd2:331`).
‏§7 (`DEBT-REGISTER.md:12-21`): *"ארגון עם יחידות אחיות אינו נתמך כל עוד קיימת פונקציה פטורה.
הגנת `org_units` היא גם גדר תפעולית, לא רק טענת preflight."* הצעד הבא: **ריקון פונקציה־פונקציה**
עם actor, ‏tenant, ‏unit, ‏tables, ‏reason, ‏audit וטסט — *"אין לבצע הצהרה־מחדש גורפת."*

**אם "מאות עסקים" כולל רשתות רב־סניפיות — זו עבודה שאינה נכללת באף הערכה קיימת.**

*פגם טקסט:* הודעת הכשל ב־`p9_five_domains.sql:332` עדיין אומרת `"must stay at 86 rows"` בעוד
הערך הנאכף הוא 89. האכיפה תקינה — ‏`check-exemption-pin.ts:221` גורף את המספר מהטענה, לא מהפרוזה.

---

## 8. שלושה ליקויים חיים שהתגלו אגב

| # | ליקוי | ראיה |
|---|---|---|
| 1 | **קוד סותר הכרעה חיה.** ‏#242 הוכרע 22.08: עוגן תקופת השימוש הוא **תאריך ההרשמה, קבוע**, ו־`private.usage_period()` צריך **להפסיק** ליפול ל־billing period. הקוד החי עושה בדיוק ההפך — מעדיף את `current_period_start` של המנוי. **חוסם חיוב** | `OPEN-DECISIONS.md:592` מול `0155:90` |
| 2 | **‏`p61` אינו רץ ב־CI** | §2.5 |
| 3 | **‏`#177` מפנה למיגרציה `0169` שלא קיימת** ב־checkout הראשי (מקסימום `0167`) | `OPEN-DECISIONS.md:459` |

---

## 9. מה **אין** לבנות

- **דוח שימוש פר־דייר — קיים ורץ.** מחסנית `0154`–`0164` ממוזגת ב־main: ‏`private.usage_events`/`usage_counters`,
  ‏`private.usage_rows(uuid)`, ‏`public.platform_org_usage(org_id)` המחובר ל־`src/operator/CustomerUsage.tsx`,
  ו־`public.organization_usage_snapshot()` — התאום של הדייר, **בלי ארגומנט ארגון בכלל** (`0155:296-298`),
  כדי שלא ניתן לכוון אותו ללקוח אחר. מכסות נאכפות בדלת אחת (`enqueue_document_processing`).
  ‏`SCALE:120` מעריך זאת ב־3–5 ימים; **העבודה בוצעה.** גם ארבעת worktrees ה־`subscriptions-*` ריקים.
- **אין Workflow Engine** — ‏#257 סגור סופית.
- **אין אופטימיזציה ספקולטיבית.** ‏§1 (round trips), ‏§13 (`global_search`), ‏§14 (מסנן כפילויות),
  ‏§15 (`count:'exact'`) תלויים בנפח של **דייר יחיד**, לא במספר הדיירים. הם ייפגעו אצל לקוח **גדול**,
  לא אצל לקוח **נוסף**. ‏#260 דורש מדידה לפני איחוד.
- **אין sharding ואין שכתוב RLS.** הבידוד בנוי ונבדק.

---

## 10. סדר ביצוע מוצע

| # | פריט | חוסם | תלוי ב־ |
|---|---|---|---|
| 1 | **הכרעת מספור מיגרציה וסדר מיזוג** (§5) | כל עבודת DB בכל שלוש התוכניות | בעלים |
| 2 | **תיקון שני ליקויי הכסף** של `PLAN-report-jobs-20260823.md` §2.2 | נכונות הדוח לרו״ח | ‏(1) |
| 3 | **דומיין מייל** — רכישה, ‏SPF/DKIM/DMARC, ‏`INVITE_FROM_EMAIL`/`ORDERS_FROM_EMAIL`, ‏SMTP Auth, ‏smoke מסירה חיצוני | הדייר השני | בעלים. ‏**אפס שורות קוד מוצר** |
| 4 | **תור הוגן + סוויטת `p62`** (§2) | ‏5 דיירים ומעלה | ‏(1) |
| 5 | **‏soak של שבעה ימים** (`SCALE:43`) | סגירת §1 | ‏(4) פרוס |
| 6 | **תיקון `#242`** — עוגן ההרשמה ב־`usage_period()` | חיוב | בעלים |
| 7 | **הרחבת בריכה** | ‏50 דיירים ומעלה | הכרעת מסמכים/יום (§3, §4) |
| 8 | **ריקון רשם ה־definer** | רשתות רב־סניפיות | ‏89 ריקונים, אחד־אחד |

---

## 11. ראיות

`DEBT-REGISTER.md` §1, §7, §13, §14, §15, §25, §30, §34, §39, §43 ·
`OPEN-DECISIONS.md` #124, #164, #165, #172, #177, #194–#196, #242, #257, #260, #261 ·
`CLAUDE.md:87,107-117` · `PROGRESS.md:506-508,1108-1109` ·
`0045:405,434-436` · `0057:234` · `0095:186-190,270-277` · `0103:851` ·
`0129:47-58,51-55,56,216-229` · `0132:37-65` · `0136:1449,1569-1580` · `0141:196-200` · `0143:235-239` ·
`0155:90,296-298` · `0159` · `p9_five_domains.sql:323,332` · `p45:391-398` · `p59:7-14` ·
`scripts/check-quality-gates.ps1:1153` · `scripts/ci-sql-suites.mjs:18-27,38-62` ·
`scripts/check-exemption-pin.ts:221` · `worker/ocr/src/worker.py:751-808,789-798` ·
`worker/ocr/src/ocr.py:102-108,332-343` · `worker/ocr/src/gateway.py:108-124` · `src/App.tsx:257`
