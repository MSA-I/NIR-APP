# דוח כיול סף האוטונומיה — קורפוס אינטרנט, 09.08.2026

**מה נמדד:** ‏`baseline_min_confidence = 0.900` (‏§16/§24 ב-`DEBT-REGISTER`, ‏#109/#119
ב-`OPEN-DECISIONS`) — לראשונה מול ריצות מודל אמיתיות, לא ניחוש ולא mock.

**ההכרעה שנגזרת מהמדידה: ‏0.900 מאושרר.** לא הוזז — אבל מהיום הוא מספר עם מדידה מאחוריו,
לא ניחוש מתועד.

## איך נמדד — ומה בדיוק רץ

- **קורפוס:** הבעלים הכריע (09.08.2026) שהקורפוס נאסף מהאינטרנט ע"י סוכן. ‏25 קבצים נאספו
  (‏`NIR-APP-DOCS/calibration-corpus/20260808/`, ‏manifest עם מקור+SHA-256 לכל קובץ):
  חשבוניות/חשבוניות-קבלה, מחירונים ורשימות מחירים מפוקחות, מכרזי מזון, תעודות זיכוי, טפסים ריקים.
- **ההרצה:** ‏`scripts/calibration/run-calibration.ts` — מייבא את `buildProviderPayload`,
  ‏`createOpenAiProvider`, את ה-SYSTEM_PROMPT ואת סכימת ה-JSON **ישירות מ-`core.ts` של
  ה-Edge Function** (אפס העתקה, אפס סטייה). מודל: ‏`gpt-5.6-terra`, פרומפט: ‏`interpret-document-v8`
  — בדיוק מה שרץ בפרודקשן.
- **מה שונה מפרודקשן, בקול:** ‏(א) חילוץ הטקסט הוא שכבת-הטקסט של ה-PDF (‏pdf.js) ולא ה-OCR worker —
  נאמן ל-PDF דיגיטלי; ‏**7 קבצים סרוקים ללא שכבת טקסט דולגו ונספרו, לא נוחשו.** ‏(ב)
  ‏`supplier_candidates` ריק — אין קטלוג דייר, ולכן נמדד זיהוי שם ספק + ביטחון, לא פתרון id.
- **שיפוט אמת:** כל אחד מ-10 המסמכים שפורשו נקרא ע"י הסוכן המכייל ונרשם ב-`adjudication.json`
  (סוג אמיתי, שם ספק, סכום). **השופט הוא הסוכן, לא בעל העסק** — מוצהר, לא מוסתר. כפילות אחת
  (‏00=01) הוחרגה מהשיפוט.
- **החלטת הכתיבה משוחזרת אחד-לאחד:** שני הביטחונות ≥ סף (‏0076) **וגם** סוג בר-כתיבה
  (‏invoice/price_list — הכותבים היחידים שקיימים). ‏FN נספר רק על סוגים ברי-כתיבה — תיוק מכרז
  לתור הוא המכונה צודקת, לא מפספסת.

## התוצאות (‏N=10 משופטים; ‏7 דולגו ללא שכבת טקסט; ‏1 כפילות)

| סף | נכתב-נכון | ‏FP (נכתב בטעות) | בתור-אך-נכון | בתור-וטעה/לא-בר-כתיבה |
|---|---|---|---|---|
| 0.50–0.80 | 2 | **1** | 1 | 6 |
| **0.85–0.95** | **2** | **0** | 1 | 7 |
| 0.99 | 1 | 0 | 2 | 7 |

- **ה-FP היחיד שנמדד:** דו"ח מחירי ירקות שבועי של משרד החקלאות פורש כ-`price_list` בביטחון
  ‏0.90/0.80. **ב-0.900 הוא נחסם (ביטחון הספק 0.80 מתחת לרצפה); ב-0.80 הוא היה נכתב לספר
  המחירים.** זה בדיוק הכשל היקר ש-§16 הגדיר, והרצפה חסמה אותו.
- **מכרזים והודעות רגולטוריות** (‏4 מסמכים) סווגו `other` בביטחון גבוה — שער הסוג עוצר אותם בלי
  קשר לביטחון. טופס ריק קיבל ביטחון נמוך (‏0.57) ונעצר פעמיים.
- **העלאה ל-0.99** מפילה חשבונית אמיתית שנכתבה נכון (ביטחון ספק 0.95) בלי לחסום אף FP נוסף.
- שתי החשבוניות שנכתבו-נכון ב-0.900: חשבונית אמיתית שפורסמה פומבית (‏240.72 ₪) ומסמך דוגמה
  מלא (‏5,833.86 ₪, סכום ושם ספק דויקו). **הערה:** מסמך הדוגמה נושא סימן מים "טיוטא" — אותנטיות
  היא שכבת בדיקות נפרדת (כפילויות, התאמות), לא תפקיד שכבת הפירוש.

## הדוח הקבוע — ‏reason_code בין גרסאות פרומפט (‏§19)

‏`scripts/calibration/filing-reason-report.sql` — שאילתה אחת (ה-Management API מחזיר רק את
המשפט האחרון — נמדד), רץ מול פרודקשן 09.08.2026:

- ‏filings: ‏v4 — ‏2 ללא סיבה רשומה (רטרואקטיביים, כמתועד ב-§19) + ‏1 ‏`not_an_invoice`;
  ‏v8 — ‏3 ‏`not_an_invoice`.
- הזנב הלא-נמדד (פירושים ללא filing): ‏v2:‏6 · ‏v4:‏1 · ‏v6:‏2 · ‏v7:‏1 · ‏v8:‏4 — סיבתם חיה רק
  בלוגים (המגבלה של §19 בעינה; סגירתה = מיגרציה, לא דוח).

## מה המדידה הזאת לא אומרת — נשאר פתוח בכוונה

1. **הנחת ההעברה מוצהרת:** קורפוס אינטרנט ≠ המסמכים של הדייר החי. המדידה על ~50 מסמכים
   אמיתיים עם הכרעת בעל-העסק נשארת הצעד הבא של §16, כשיהיו.
2. **‏N קטן (10).** רוחב הרצועה הבטוחה [0.85, 0.95] נשען על מסמך-גבול אחד. אין כאן דיוק של
   ספרה שלישית — יש אישור שהסדר-גודל נכון ושהכיוון (רצפה גבוהה) מגן בפועל.
3. **מחירוני ספק אמיתיים** (‏§24): איכות ההתאמה למוצרים (מק"ט/ברקוד) דורשת קטלוג דייר —
   לא נמדדה כאן. שני ה"מחירונים" האמיתיים בקורפוס הם רשימות מפוקחות; המדידה של §24 נשארת
   על 50 המחירונים האמיתיים הבאים.
4. סריקות ללא שכבת טקסט מדולגות — מדידת מסלול ה-OCR המלא דורשת את ה-worker
   (‏docker-compose.ocr.yml) ולא בוצעה.

**שחזור:** ‏`node scripts/calibration/run-calibration.ts run` (אידמפוטנטי — תוצאה קיימת לא
נשלחת שוב למודל) ואז `score`. תוצאות גולמיות: ‏`calibration-corpus/20260808/results/`.
