# מסירה — קמפיין "מסמך ספק שהתקבל"

**נכתב:** 11.08.2026 בערב · **ענף:** `feat/received-supplier-document` · **בסיס השערים:** `ec30570`
**PR:** [#18](https://github.com/MSA-I/NIR-APP/pull/18), פתוח · **23 מיגרציות** (`0104`–`0126`)

**עדכון המשך, 11.08.2026 בלילה:** אחרי `ed0e0dc` תוקנו שתי רגרסיות מקומיות — דגל הפידבק בזרע
והשבת סיסמאות Auth של חשבונות הדמו. ה־PASS-ים של `ec30570` ושל `ed0e0dc` אינם ראיה ל־diff הזה;
מיזוג מחייב ארבעה checks חדשים על ה־SHA שיכלול את העדכון.

> קרא את `CLAUDE.md` ואת `docs/PROGRESS.md` ראשונים, כרגיל. הקובץ הזה מוסיף עליהם דבר אחד בלבד:
> **מה נשאר, ומה כבר הוכח** — כדי שלא תוכיח מחדש דברים שהוכחו, ולא תניח שהוכח מה שלא.

---

## 1. מה כבר ירוק — אל תריץ מחדש בלי סיבה

| מה | איפה | מתי |
|---|---|---|
| `build` — ‏tsc + אחד-עשר `check:*` + **605** בדיקות vitest + vite build | ‏CI, runner נקי | ריצה `31507313769` |
| ‏54 סוויטות SQL + ‏44 זרועות preflight | ‏CI | ריצה `31507313917` |
| ‏35 תרחישי דפדפן | ‏CI | ריצה `31507313917` |
| חוזי Deno + ‏OCR worker | ‏CI | ריצה `31507313917` |
| **`npm run quality` מקומי מלא** — ‏125 שלבים, ‏24.5 דקות, **אפס דילוגים** | מכונת הבעלים | 11.08.2026, ‏exit 0 |

**חמשת הבדיקנים ש-CI אינו מכסה** (‏`CLAUDE.md:156-158`) רצו כולם בריצה המקומית ההיא:

```
P0 security acceptance ....... 279 טענות, exit 0
P0 upgrade path .............. הרצה מחדש של כל שרשרת המיגרציות מ-0019, exit 0
P4 integrated journey ........ 19 שורות audit סמנטי, exit 0
P1B local Edge runtime smoke . 13.0s, exit 0
OCR Edge + provider-mock ..... 14.9s, exit 0
```

**זו אינה טענה נצחית.** היא נכונה ל-`ec30570`. כל קומיט נוסף מחייב לפחות את שער ה-CI מחדש, ואם
הוא נוגע ב-RLS, ב-grants או במדיניות אחסון — גם את `npm run quality` המקומי.

### 1.1 ראיות מקומיות חדשות — עדיין לא אישור מיזוג

- אפשרות „שליחת הערה” והבחירה „לצרף צילום של המסך” נצפו ב־1440px וב־390px. הרשומה נשמרה עם
  PNG של 97,238 bytes, ‏checksum בן 64 ספרות hex ונתיב tenant-scoped.
- ששת חשבונות הדמו החזירו Auth 200 אחרי מסלול ה־update החדש ב־`create-users.ps1`; כניסה דרך
  קיצור המסך נבדקה בפועל עבור owner ו־kitchen.
- build ייצור מקומי סופי עבר עם **607/607** בדיקות אחרי תנאי `import.meta.env.DEV` המפורש
  ב־JSX. ב־`dist` נמצאו אפס מופעים של seed הדמו, כתובות דמו והכותרת „חשבונות דמו מקומיים”,
  ו־host הייצור נמצא בבאנדל.
- הראיות הוויזואליות: `local-demo-login.png`, ‏`local-demo-login-mobile.png`,
  ‏`feedback-screenshot-option.png`, ‏`feedback-screenshot-option-mobile.png` בארטיפקטי משימת Codex.

---

## 2. מה נשאר — שני דברים, לפי סדר

### 2.1 ‏✅ **נסגר 11.08.2026:** צילום מסך הבדיקה (חבילה E)

המסך נפתח בדפדפן אמיתי מול הסטאק המקומי, בחשבון owner, ב־1440px וב־390px. ה־fixture המקורי
נכשל בשער עצמו: הוא הדפיס `SKU-DEMO`, שאינו מזהה מוצר אמיתי בזרע, ולכן המסך לא יכול היה להציג
מחיר בסיס או פער. ניסיון ההרצה החוזרת גם נתקל בצדק ב־`document_interpretations_immutable`.
‏`dcdad84` החליף את ה־placeholder ב־SKU הקנוני של המוצר, נתן ל־fixture מזהים מגרסה חדשה והפך
הרצה חוזרת ל־no-op ששומר את יומן הלמידה הבלתי־משתנה. ה־fixture רץ פעמיים: ההרצה הראשונה יצרה,
והשנייה החזירה `review-screen fixture already exists` ויצאה 0.

**ראיה ויזואלית:** `review-screen-desktop.png`, ‏`review-screen-desktop-viewport.png` ו־
`review-screen-mobile.png` בארטיפקטי משימת Codex. כל שמונת החוזים הממוקדים עברו:

- „הקובץ נשמר" ו„הנתונים עדיין לא אושרו" הם שני משפטים נפרדים;
- הספק וההזמנה מציגים את הראיה שהכריעה בעברית, בלי `by_number`/`by_name`/`by_sku` גולמי;
- שורת `VEG-001` מציגה 3 במסמך, 50 בהזמנה, 0 שהתקבלו, ‏117.00 במסמך, ‏8.50 מוסכם ופער ‏108.50;
- „מה יקרה באישור" כולל גם את שלוש הפעולות וגם את שני משפטי ה„לא";
- ‏`html[dir=rtl]` נשמר, וב־390px אין גלילה אופקית של הדף;
- לא נצפו תגובות HTTP בסטטוס 400 ומעלה בזמן הטעינה.

**מגבלת ה־fixture:** הוא יוצר את רשומות המסמך והפירוש במסד אך אינו מעלה PDF ל־Storage, ולכן אזור
„המסמך המקורי" מציג במכוון שאין תצוגה מאובטחת. שער זה הוכיח את מסך ה־assessment והאישור; הוא
אינו טענה על preview של קובץ אמיתי.

### 2.2 ‏**פריסה** — `main` והייצור על `0103`

מיזוג פורס. 23 המיגרציות כוללות שינויים שאינם הפיכים בקלות:

| | |
|---|---|
| `0111` | ‏`REVOKE` על `execute_emergency_payment_request` |
| `0112` | הרשאת עמודה על `suppliers` — ‏`bank_details` יוצא מ-`authenticated` |
| `0121` | הרחבת הרשאות הסרת מסמך לארבעה תפקידים |
| `0124`/`0125` | ‏ACL של `feedback_notes` ומדיניות דלי האחסון |

```powershell
# הסדר, אחרי המיזוג:
$env:SUPABASE_ACCESS_TOKEN = (Get-Content 'D:\...\NIR-APP-DOCS\NIR-TOKEN-SUPABASE.txt' -Raw).Trim()
.\scripts\db-query.ps1 -SqlFile supabase\migrations\0104_....sql -ProjectRef rkftlbctohswhbbiaqin -AllowProduction
# ... אחת אחרי השנייה, לפי הסדר. לא באצווה: כל מיגרציה מכילה טענות שנועדו לעצור.
```

**‏`0126` מכיל שתי הצבות מעוגנות שקוראות את סימן סוף השורה מהגוף החי.** במסד הייצור סימן סוף
השורה עשוי להיות שונה מהמקומי — הקוד מטפל בזה, אבל **אמת** שהן תפסו (ראה §3.2).

### 2.3 ‏**חוב פתוח** — שלוש שורות ב-`DEBT-REGISTER.md`

| § | מה | הצעד הזול הבא |
|---|---|---|
| **36** | שלושת הייצואים אינם קוראים ל-`resolve_export_report_template`. אפשר להעלות תבנית, לראותה חיה, ולקבל את הייצוא הרגיל | להתחיל מ-`monthlyReport.ts` **בלבד** |
| **37** | שימור עיצוב תא ב-round-trip לא נמדד מול חוברת אמיתית **עם עיצוב** | לבקש חוברת אחת מהבעלים |
| **38** | תנאי `owner`/`metadata` מתים בשלוש מדיניות העלאה אחרות (‏`documents`, ‏`price-submissions`, ‏`supplier-price-documents`) | תרחיש דפדפן אחד שמעלה קובץ אמיתי ל-`documents` |

‏**§38 הוא החשוד המשמעותי מביניהם.** אם העלאות מסמכים מדפדפן נכשלות באותה דרך שהעלאת הצילום
נכשלה — הן נכשלות **בשקט**, כמו שהיא נכשלה.

---

## 3. מלכודות שהסשן הזה שילם עליהן. אל תשלם שוב

### 3.1 מיגרציה בלי סוויטה היא מיגרציה שלא ידוע אם היא רצה

**שתי המיגרציות שנשלחו בסשן הזה בלי סוויטה — `0122` ו-`0123` — שתיהן היו שבורות.**

- ‏`0123`: ‏`attach_export_template_workbook` **מעולם לא רצה** — טריגר האי-שינויוּת של `0047` חוסם כל
  עדכון לשורת גרסה חוץ מזוג האישור. ‏p38 תפסה את זה בריצה הראשונה.
- ‏`0122`: כל העלאת צילום נדחתה, משלוש סיבות בזו אחר זו (§3.3).

השער הכבד בדק שהמיגרציות **הוחלו**. הוא לא בדק שהפקודות **רצות**.

### 3.2 הצבה מעוגנת חייבת לקרוא את סימן סוף השורה מהגוף

במסד הזה `document_export_template_contract_valid` נושא **LF** ו-`guard_document_export_template_version`
נושא **CRLF** — נוצרו מקבצים שנשמרו אחרת על מכונת Windows. עוגן עם `\n` בלבד מתאים על runner של
לינוקס ומחטיא כאן. הדפוס הנכון נמצא ב-`0126` §2 ו-§4:

```sql
v_nl := case when position(chr(13) in v_def) > 0 then chr(13) || chr(10) else chr(10) end;
v_old := replace(v_old, chr(10), v_nl);
```

זו אותה מלכודת של הצמדות `md5(prosrc)` (‏זיכרון `prosrc-hash-crlf-trap`), בכובע אחר.

### 3.3 מדיניות העלאה ל-Storage יכולה לקרוא רק את `name` ו-`bucket_id`

נמדד על storage-api 1.54.1:

- ‏`metadata ->> 'mimetype'` הוא **null** כשמדיניות ה-INSERT רצה. השירות ממלא `metadata` **אחרי**
  יצירת השורה. את הגבלת ה-mime שמים על `allowed_mime_types` של הדלי.
- ‏`owner` ו-`owner_id` **null** באותו רגע, מאותה סיבה.
- ההעלאה היא `insert ... on conflict ... RETURNING *`, ולכן הדלי **חייב מדיניות `SELECT`** או
  שההעלאה נכשלת ב-400. **דלי בלי מדיניות קריאה הוא דלי שאי אפשר לכתוב אליו.**

הדיאגנוזה: ‏`docker logs supabase_storage_supplyflow-p0` — ה-JSON נושא את ה-SQL המדויק תחת
`error.raw.metadata.query`.

### 3.4 סביבת Windows

- ‏**`.ps1` חייב BOM.** בלעדיו PowerShell 5 קורא אותו כ-ANSI וכל תו עברי בנתיב הופך ל-mojibake.
- ‏**צינור של PowerShell לתהליך מקורי משבש UTF-8.** ‏`Get-Content ... | docker exec psql` הפך שמות
  קטגוריות בעברית לאותה מחרוזת `?????` והפיל אילוץ ייחודיות. מ-bash: ‏`docker exec ... psql -f - < file`.
- ‏**`newline=''` בכתיבת קובץ קיים מפייתון.** ‏`newline='\r\n'` על מחרוזת שכבר מכילה CRLF יוצר `\r\r\n`.
- ‏`npm run quality` מקומי דורש `$env:SUPPLYFLOW_ALLOW_LOCAL_QUALITY = '1'`, ולפניו **לעצור את
  שרת ה-dev** (הוא מחזיק חיבור כותב ל-DB). הריצה שתועדה כאן עברה בדיוק כך.
- ‏**‏`deno.lock` בשורש** נוצר מהריצה המקומית. הוא ב-`.gitignore` מ-11.08 — הריפו מעולם לא נשא
  אחד, והוספתו הייתה משנה מה ש-CI פותר. אל תקמט אותו „לסדר".

### 3.4.1 קבצי-זבל משורת הפקודה

הסשן הזה ייצר שוב ושוב קבצים בשמות כמו `'size'`, `head[index]`, `0` — תוצר של ציטוט שבור
ב-one-liners. **לפני כל קומיט: `git status --short | grep '??'`.** ‏`git add -A` עיוור להם.

---

## 4. מה הקמפיין הזה **לא** טוען

- ‏**‏`0126` ואילך לא נבדקו על הייצור.** כל הבדיקות הן מול מסד מקומי שנבנה מאפס.
- ‏**חבילה E לא נראתה בעין** (§2.1).
- ‏**עיצוב תא בתבנית ייצוא לא נמדד** (‏`DEBT §37`).
- ‏**ספי האוטונומיה `0.900` עדיין לא מכוילים** (‏`DEBT §16`, ‏§24, ‏§27). הקמפיין לא מסתמך עליהם:
  כל מסלול חדש בו דורש אישור אדם.

---

## 5. עשר ההכרעות של הבעלים, 11.08.2026 — הן בקוד, לא בתוכנית

הטבלה המלאה ב-`docs/OWNER-QUESTIONS-received-supplier-document.md`. השתיים שהכי קל להפר בטעות:

1. **סיבה אינה חוסמת כפתור, והיומן ממשיך לקבל משפט.** ‏`src/lib/reason.ts`. עריכה שמחזירה
   `disabled={... !reason.trim()}` מחזירה את החקירה; עריכה ששולחת מחרוזת ריקה מפילה ~50 פקודות
   שרת. ‏`reason.spec.ts` מצמיד את שני הצדדים.
2. **מיפוי placeholder מתאים אוטומטית רק בשוויון מדויק.** התאמה „כמעט נכונה" שמה מספר שגוי בתא
   בקובץ שהרו״ח מגיש, בשורה שכתוב עליה „הותאם". ‏`exportTemplates.spec.ts`.
