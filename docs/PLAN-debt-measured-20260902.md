# תוכנית חוב מדודה — 02.09.2026

מחליפה את `docs/PLAN-debt-code-20260902.md`. אותו מסמך לא היה שגוי כשנכתב; הוא נכתב מול
מספור אחר ומול רשם שהתיישן, ובוקר אחד של מדידה שינה את התמונה.

**שים לב: התוכנית הקודמת אינה ב-`main` כלל.** היא קיימת רק בקומיט `3896a5c3` על
‏`rescue/second-agent-docs-20260902`, יחד עם הרשם הממוספר-מחדש שהיא נמדדה מולו. זו הסיבה
שהמספרים שלה אינם תואמים — ולכן גם אין מה „לסמן כמוחלף" כאן; המסמך הזה הוא הראשון מסוגו על `main`.

**הבסיס:** `origin/main` בקומיט `4cfd14ab`. **הייצור:** `0283` מאז 02.09.2026 06:17Z–07:15Z.

> **מה שונה כאן.** כל שורה נושאת **מה נמדד**, לא מה נטען. סעיף שלא נמדד מסומן כך במפורש,
> ואין להסיק ממנו דבר. הכלי: `npm run audit:debt`.

---

## 1. מה השתנה מאז התוכנית הקודמת

**שלוש מהחבילות הישנות אינן קיימות עוד:**

| חבילה ישנה | מה קרה |
|---|---|
| **א׳ — כסף** | ‏`§49`, `§50`, `§29` **היו סגורים כבר** (‏`0172`/`0173`/`0174`, בייצור); `§35` נבנה ונסגר. ‏PR #218 |
| **ג׳ — רולאאוט** | הרולאאוט **בוצע**. אבל ראה §3 למטה — הוא סגר שניים מארבעת הסעיפים, לא את כולם |
| **ד׳ — שערים** | ‏`§65` ו-`§9` נסגרו; שני פריטים שלה כבר היו סגורים. ‏PR #219 |

**ומה שהתגלה שגוי בתוכנית הישנה:**

1. **המספרים אינם תואמים.** התוכנית נמדדה על `rescue/second-agent-docs-20260902`, שבו הרשם
   ממוספר מחדש. ‏**15 סעיפים נושאים מספר שונה ב-`main`**, ושלושה (‏`§96`, `§97`, `§98`)
   אינם קיימים שם כלל. ההמרות שחשוב לזכור:

   | בתוכנית | ב-`main` | מה זה |
   |---|---|---|
   | `§86` | **`§97`** | ‏`my_entitlements()` — כ-900 שאילתות |
   | `§93` | **`§95`** | הצעד האחרון בסיור המוצר |
   | `§94` | **`§85`** | אתגר כתובת הגיבוי |
   | `§95` | **`§86`** | תיבת `support@` שאינה מקבלת דואר |

2. **„חבילה ג׳ נסגרת ברולאאוט" היה לא מדויק.** שניים מארבעת הסעיפים שלה תלויים ב**פריסת
   ה-VPS**, שהרולאאוט **במפורש לא ביצע**.

---

## 2. הכלל שנלמד, והוא חשוב מכל סעיף בודד

חמישה סעיפים הכריזו „הקוד שבור" כשהקוד תוקן. **לכולם הראיה הייתה מספר שורה במיגרציה**,
ובכולם החלפה מעוגנת מאוחרת שכתבה בדיוק את אותה שורה.

> **לפני שמתחילים חבילה: `npm run audit:debt`, ואז מודדים את הסעיפים שבה.**
> דקות מול שעות עבודה על משהו שכבר עובד.

---

## 3. חבילה 1 — פריסת שני שירותי ה-VPS · **המנוף הגבוה ביותר**

**זו אינה כתיבת קוד. זו פעולת תפעול אחת שפותחת שלושה סעיפים בבת אחת.**

| § | מה חסום בו | נמדד |
|---|---|---|
| `§72` | אכיפת סימן המים — **נבנתה 28.08**, „הצעד הבא, ורק הוא: פריסה" | ‏`worker/render` לא נפרס ברולאאוט |
| `§20` | ‏`normalize-with-evidence` — „להריץ את השער, ואז לפרוס את ה-VPS יחד עם המיגרציה" | אותו חסם |
| `§68` | טקסט ב-PDF שאינו ניתן לחיפוש | ‏`#338` קבע שהוא נפרס **עכשיו** ואינו ממתין |

מסמך הרולאאוט אומר זאת בפירוש: „**שני שירותי ה-VPS לא נפרסו**, כמתוכנן: שלוש גרסאות החוזה
לא זזו בשום צד."

**מה נדרש:** לפי מטריצת `CLAUDE.md` — `RENDER_CONTRACT_VERSION` זהה בשני הצדדים, `/health`
שמחזיר את אותה גרסה, והוכחת ייצוא חי אחד. עבור OCR: הוכחת `job_claimed`+`job_completed` ביומן.
**‏`Up` אינו ראיה.**

---

## 4. חבילה 2 — מהירות · החוסם האחרון שהבעלים נקב בו

`#332` נקב בשלושה חוסמים. שניים (`§49`, `§50`) נמדדו כסגורים. **נשאר אחד.**

| § (main) | נמדד 02.09.2026 |
|---|---|
| **`§97`** | ‏`my_entitlements()` עדיין מכילה `cross join lateral` — נמדד על הגוף החי |
| `§1` | ‏`Dashboard.tsx` יורה **13** קריאות נפרדות ל-Supabase |
| `§15` | **13** שימושים ב-`count: 'exact'` ב-`src/` |
| `§13` · `§14` · `§12` | **לא נמדדו.** דורשים קריאה של הגופים החיים |

**מה סוגר:** מדידת latency **לפני ואחרי** על אותו מסד. ‏`scripts/check-browser-smoke.cjs:101`
הוא המקום הטבעי להצמיד סף.

---

## 5. חבילה 3 — הרשאות ואבטחה · גדולה יותר משנראה

| § | נמדד |
|---|---|
| **`§7`** | ‏**93 חריגי `SECURITY DEFINER`** ברשם החי. התוכנית הישנה תיארה „ריקון פונקציה-פונקציה" בלי לומר את המספר |
| `§41` | **שתי** הטבלאות של `0140` — ‏`document_packets`, `document_packet_segments` — עדיין ללא `zz_organization_write_guard` |
| `§56` | ‏`suppliers.max` ו-`storage.bytes` אינם מופיעים במסך השימוש של התפעול |
| `§10` | ‏`p2_invoice_without_order_count` עדיין `security invoker` על הגוף החי |
| `§62` · `§76` · `§77` · `§8` · `§59` | **לא נמדדו** |

‏`§62` (‏MFA) קודם ל-`§76` — אין טעם לחזק step-up לפני שיש לו גורם שני. זה לא השתנה.

---

## 6. חבילה 4 — מסמכים, OCR ואחסון

| § | נמדד |
|---|---|
| `§17` | חריגה אוטומטית עדיין נרשמת כ-`receipt_mismatch` עם `details.code='item_not_ordered'` |
| `§42` | **אף קובץ ב-`src/`** אינו כותב `price_list_automation_scope_decisions`; המצב `eligible` אינו בר-השגה מהמוצר |
| `§64` | טבלת ההגדרות קיימת ו**אין מיגרציה שזורעת אותה** — ההגדרה עדיין נקבעת ביד בייצור |
| `§66` | **אינו חסום ברולאאוט.** דורש מיגרציה חדשה **והכרעת בעלים** על חלון `organization_teardown` |
| `§48` · `§40` · `§27` · `§31` · `§32` · `§22` · `§5` | **לא נמדדו** |

---

## 7. חבילה 5 — מוצר, שפה ומייל

| § | נמדד |
|---|---|
| `§33` | ‏`@supabase/supabase-js` עדיין `"^2.91.1"` — טווח צף. שורה אחת |
| `§53` | הגרף מבחין בכיוון ב**צבע וקו בלבד**; אין נשא צורני |
| `§86` (היה `§95`) | ‏`support@inplace.digital` מתפרסם מ-`src/lib/support.ts` ואינו מקבל דואר |
| `§83` | **הענף שהסעיף מתאר אינו קיים עוד ב-`origin`** — מועמד ל**הסרה**, לא לתיקון |
| `§82` | מצטט `src/content/pages.ts` — **הקובץ אינו קיים**. הסעיף כנראה עדיין תקף; הראיה אינה |
| `§95` (היה `§93`) · `§89` · `§25`/`§85` · `§58` · `§30` · `§61` · `§84`/`§81`/`§80`/`§79` | **לא נמדדו** |

---

## 8. חבילה 6 — מה שנשאר בשערים

| § | נמדד |
|---|---|
| `§34` | **0 מתוך 5** הבדיקנים מורצים ב-CI — הם מופיעים אך ורק ב**הערה** ב-`quality-gate.yml`. דורש runners של Windows |
| `§9` (שארית) | השער קיים ונמדד שהוא נכשל, אבל **אין לו בקרה חיובית קבועה** — חסר hook של `INJECT` |
| `§52` · `§67` · `§78` | כשלים סביבתיים. **לא נמדדו** |

---

## 9. סדר הביצוע המומלץ

```
חבילה 1 (VPS)  ──►  §72 §20 §68 נסגרים
חבילה 2 (מהירות) ──►  החוסם האחרון של #332
        │
        └──►  3 (הרשאות) · 4 (מסמכים) · 5 (מוצר) · 6 (שערים) — לפי סדר שאתה קובע
```

**‏1 ו-2 קודמות** מפני ש-1 היא פעולה אחת שפותחת שלושה סעיפים, ו-2 היא החוסם היחיד שנותר
מבין שלושת אלה שהבעלים נקב בהם בשמם.

**כלל עבודה, ולא השתנה:** חבילה = ענף = ‏PR. אין למזג שתי חבילות לענף אחד.
לפני מספר חדש: `node scripts/next-free-number.mjs`.

---

## 10. מה התוכנית הזאת אינה מבטיחה

- **‏30 מתוך 80 סעיפים נמדדו.** ‏50 לא. כל „לא נמדד" למעלה הוא בדיוק זה — לא טענה שהסעיף
  פתוח, ולא טענה שהוא סגור.
- **אין הערכות זמן.** ההיקף נמדד בסעיפים, בקבצים ובשערים.
- **אין כאן הכרעה עסקית חדשה.** ‏`§66` מסומן במפורש כדורש הכרעת בעלים שטרם ניתנה.
- **‏`§72`, `§20` ו-`§68` אינם „כמעט סגורים".** הם חסומים על פעולת תפעול שלא בוצעה, ואי-ביצועה
  אינו נראה בשום שער.
