# HANDOFF — מערכת רב-מטבעית: הפרומפט לסוכן המבצע

נכתב 28.08.2026 בענף `plan/multi-currency-20260828` (קומיט `101daeb`).
**זה מסמך מסירה, לא תוכנית.** התוכנית עצמה: `docs/PLAN-multi-currency-20260828.md`.

---

## אזהרה שחייבת להיקרא ראשונה: ההכרעות מפוזרות בין שני ענפים שלא מוזגו

| מה | איפה הוא נמצא | קיים ב-`main`? |
|---|---|---|
| `#277` — „מערכת שעובדת באמת בדולר" · `#281` — האנגלית קודם | ענף `claude/add-english-language-system-f43d1e` | **לא** |
| `DEBT §69` — החוב, עם המדידות ההיסטוריות | אותו ענף | **לא** |
| `#284`–`#289` — שש הכרעות הבעלים מ-28.08.2026 | ענף `plan/multi-currency-20260828` | **לא** |
| התוכנית + `GATES.md` | אותו ענף | **לא** |

**המשמעות המעשית:** סוכן שיפתח ענף נקי מ-`main` **לא יראה אף אחת מההכרעות האלה** ולא ידע
שהן קיימות. ענף הביצוע חייב להיפתח מ-`plan/multi-currency-20260828` **אחרי** שהאנגלית מוזגה
ל-`main` — או, אם היא כבר מוזגה, מ-`main` עם merge של ענף התכנון לתוכו.

---

## הפרומפט (להעתקה כמות שהוא)

```text
מימוש מערכת רב-מטבעית ל-InPlace. אתה מבצע תוכנית קיימת — אל תכתוב תוכנית חדשה.

ענף: פתח ענף ביצוע מ-plan/multi-currency-20260828 (קומיט 101daeb).
אל תפתח מ-main: ההכרעות אינן שם. אם עבודת האנגלית כבר מוזגה ל-main, מזג את
main לתוך ענף הביצוע לפני שאתה מתחיל.

לפני שאתה נוגע בקוד, קרא במלואם ובסדר הזה:
1. docs/PLAN-multi-currency-20260828.md — התוכנית. §1 הוא מה שכבר נמדד,
   §2 ההכרעה הטכנית, §3 הכלל שאסור לשבור וכלל הכפייה, §4 שמונה ההכרעות,
   §5 שבע הפאזות.
2. GATES.md — חמישה שערי תכנון שהושגו ו-19 שערי ביצוע עם CHECK ו-EXPECT.
3. docs/OPEN-DECISIONS.md — שש השורות #284 עד #289. אלה הכרעות בעלים
   מ-28.08.2026, לא הצעות.
4. #277 ו-#281 — נמצאים רק בענף האנגלית. קרא אותם ב-
   git show claude/add-english-language-system-f43d1e:docs/OPEN-DECISIONS.md
   וכן DEBT §69 מאותו ענף.
5. CLAUDE.md — חוקת הפרויקט. ARCHITECTURE.md — כללי מודל הנתונים.

ההכרעה שממנה הכול יוצא (#277, בעלים): ספק שמוציא חשבונית בדולר — החשבונית
נקלטת בדולר, נשמרת בדולר, משולמת בדולר, ויתרת אותו ספק מנוהלת בדולר בנפרד
מיתרתו בשקל. אין תצוגה מומרת ואין החלפת סמל. ההכרעה סופית.

הכלל שהוא המבחן של הכול: מסך שמסכם 12,400 ₪ ועוד 3,100 $ למספר אחד מציג
מספר שקרי במסך שמקבלים לפיו החלטות — הפגיעה בסעיף 12 של החוקה. סכום מוגדר
רק בתוך מטבע אחד.

מה שכבר נמדד — אל תמדוד מחדש (הראיות ב-§1 של התוכנית):
- 24 עמודות כסף ב-17 טבלאות שאין להן מטבע. הרשימה המלאה ב-§1.1.
- 22 עמודות numeric(12,2) נוספות שאינן כסף — כמויות, שיעורים, ביטחון.
  אסור לתת להן מטבע. זו נקודת הכשל הראשונה של חיפוש-והחלפה.
- 111 אתרי צבירת כסף ב-20 קובצי מיגרציה, ~23 פונקציות DB נבדלות.
- 31 הפניות ל-p0_*_balance_rows ב-10 קבצים, ועוד 2 בסקריפט שער P4.
- 267 אתרי עיצוב כסף בלקוח ב-46 קבצים לא-spec. 42 שורות עם ₪ ב-22 קבצים.
- 13 קובצי בדיקה נוגעים בכסף.
- 8 קריאות לקוח ל-invoice_balances ב-7 קבצים, 5 ל-supplier_balances.
- ראש המיגרציות ב-main היה 0212 והאנגלית מוסיפה 0213. אמת את המספר הפנוי
  לפני שאתה קובע 0214.

שלוש עובדות שנמדדו ומשנות את הדרך — הן בתוכנית ולא בקוד:
1. כבר יש עמודת מטבע במערכת. 0184 נתן ל-plan_price_catalogues ול-
   organization_billing_periods עמודת currency ליד ה-amount, ו-fmtPlanPrice
   כבר מקבל את המטבע כארגומנט מהשורה. מכליל דפוס קיים, לא ממציא.
2. invoice_balances ו-supplier_balances לא נמחקו ב-0022. הם נמחקו ונוצרו
   מחדש כ-views מעל הפונקציות (0022:462-465), והלקוח קורא את ה-views.
3. לקוח ה-Supabase אינו מוקלד (supabase.ts:44, בלי generic) והשורות נקראות
   דרך cast ידני. שינוי במסד אינו מייצר ולו שגיאת הידור אחת.

מכאן שני כללי הכפייה, והם לב הביצוע:
- ב-TypeScript: מוחקים את החתימה החד-ארגומנטית של fmtMoneyExact/Rounded/
  Compact. 267 שגיאות הידור צפויות, כל אחת מקום שבו אדם מכריע איזה מטבע.
  מדביקים את רשימת השגיאות ב-GATES.md לפני, ו-tsc --noEmit = 0 אחרי.
  משנים גם שמות שדות: balance ⇒ balance_in_currency, open_balance ⇒
  open_balance_in_currency. שם זהה עם משמעות חדשה הוא מה שה-cast מסתיר.
- ב-SQL: מוחקים את השמות הישנים ולא מחליפים גוף. p0_*_balance_rows וה-views
  שלהם נולדים כ-*_by_currency. קורא שלא עודכן נכשל ברעש. drop, לא
  create or replace.

שש הכרעות הבעלים מ-28.08.2026, וכל אחת מהן משנה משהו קונקרטי:
#284 כל מטבע ISO, לא רשימה סגורה. טבלת ייחוס currencies עם minor_units
     (בלי org_id — חריג מתועד כמו plan_price_catalogues), 24 העמודות
     מתרחבות ל-numeric(14,3), והעיגול נגזר מ-minor_units במקום „תמיד 2".
#285 מע״מ לפי מדינת העסק. organizations.country_code (הצעה ראשונית
     מהדפדפן בהרשמה, שינוי רק ב-owner עם סיבה ו-audit), vat_rate נגזר
     ממנה, suppliers.country_code נוסף, ובדיקת שיעור המע״מ של 0099 חלה
     על ספק מקומי בלבד. מסמך של ספק זר נרשם כפי שהודפס.
#286 תשלום דולרי מחשבון שקלי מותר. payments מקבל settlement_amount ו-
     settlement_currency שנשארים NULL במקרה הרגיל. ההקצאה נשארת במטבע
     החוב. השער נגזר בקריאה ואינו נשמר. אין מקור שערים חיצוני.
#287 חוברת הרו״ח: עמודת מטבע תמיד, ופיצול לגיליונות רק בחודש שיש בו יותר
     ממטבע אחד. חודש חד-מטבעי — החוברת זהה למבנה הקיים. שם הגיליון וסדר
     הגיליונות נגזרים מהנתונים בלבד, כי הדוח הסופי חתום ב-hash.
     אין נוסחאות בחוברת: הנטרול הוא הגנת אבטחה.
#288 סובלנות לכל מטבע. הסקלר הישן נקרא כערך של ILS.
#289 חשבון בנק אחד לספק נשאר. מטבע שאינו תואם נעצר בפני אדם.

סדר העבודה — שבע פאזות, ואין לעבור פאזה בלי הראיה שהשער דורש:
0. השומרים: תיקון העיוורון של check:money (רב-שורתי + ארגומנט מחושב),
   ו-scripts/check-currency.mjs עם שלוש טענות. שער שעובר רק היום הוא חסר ערך.
1. מיגרציה אחת: טבלת currencies, 24 עמודות + הרחבת טיפוס, מדינה לארגון
   ולספק, שדות סילוק, backfill ל-ILS, constraints של זהות מטבע בהקצאות.
2. קוראי הכסף: שמות חדשים, group by currency, 31 אתרי SQL אחד-אחד.
3. הלקוח: 267 אתרי ההידור. הפאזה הגדולה, כמחצית מהעבודה.
4. קליטה: 0108 מצטמצם ולא נעלם, apply_reviewed_document כותב מטבע.
5. בנק, תשלומים, חוברת, snapshots.
6. ראיות ורולאאוט.

כללי ברזל:
- אין המרת מטבע בשום מקום ואין מקור שערים חיצוני. מקור חיצוני הוא הרחבת
  גבול אמון כמו DEBT §63, ודורש הכרעת בעלים ו-DPA — לא שורת קוד.
- ראיה לא נוגעים: document_extractions, document_interpretations,
  audit_logs, snapshots חתומים, order_snapshot של הפורטל, bank_transactions.raw.
  המטבע שלהם נקבע בפרשנות בזמן קריאה, לעולם לא בכתיבה חוזרת.
- backfill ל-ILS אינו ניחוש: 0001 מצהיר על שקלים ו-0108 חוסם בפועל כל
  מטבע אחר. אל תשנה את הנימוק הזה — הוא מה שמצדיק את not null.
- מיגרציה אחת, forward-only, עם org_id ו-RLS, הרשאות עמודה מפורשות
  (0213 שכח את זה), A5 — שם טבלה scope-enforced נשלף מטבלת הגדרות ולא
  נכתב כמחרוזת, A6 — tenant_export_registry ו-hash מחדש.
- git stash אסור: ה-worktree חולק את המחסנית עם סוכנים אחרים.
- אין „בוצע" בלי npx tsc --noEmit נקי ו-npm run -s test ירוק.
- שער או בדיקה שנכשלו הם מדידה. שני כשלים רצופים באותו שער — עצור ודווח.
- אל תיתן למשה הערכות זמן בימי-מתכנת. מדוד היקף: כמה קבצים, כמה אתרים,
  כמה עמודות.

אחרי כל משימה — קומיט, הודעה באנגלית בפורמט הריפו (feat(currency): …).
עדכן את GATES.md בכל שער שנסגר, עם הראיה בפועל. שער שננטש נרשם עם ABANDON:
וסיבה — לא נמחק בשקט.

תקשר עם משה בעברית, קרא לו בשמו, והסבר בשפה פשוטה — הוא אינו מתכנת.
שאלת הבהרה — עצור וחכה לתשובה אמיתית; הודעת timeout אינה תשובה.
```

---

## מה שהסוכן הבא **לא** ידע אם לא ייאמר לו

1. **ההכרעות אינן ב-`main`.** ראה הטבלה למעלה. זו נקודת הכשל הראשונה של המסירה הזאת.
2. **‏`#281` חוסם התחלה** — האנגלית קודם. סוכן שיתחיל לפני כן יתנגש בקבצים שהיא נוגעת בהם.
3. **‏`DEBT §69` צריך עדכון אחרי המיזוג** — הטקסט המוכן נמצא ב-§8 של התוכנית ואינו ניתן
   להחלה מענף התכנון, כי `§69` קיים רק בענף האנגלית.
4. **‏`ARCHITECTURE.md` נושא טענה לא מדויקת** על מחיקת ה-views (‏§1.3.1 בתוכנית). מי שיתקן
   אותה יעשה שירות לכל מי שיבוא אחריו.
