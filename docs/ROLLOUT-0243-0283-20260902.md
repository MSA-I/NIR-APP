# רולאאוט `0243`→`0283` — סדר הפעולות

**מצב: בוצע 02.09.2026, ‏06:17Z–07:15Z.** התוצאה המלאה בסוף המסמך, §13. הוכן 02.09.2026;
כל המספרים כאן **נמדדו** מול `rkftlbctohswhbbiaqin` בקריאה בלבד, לא הוערכו. המדידה שקדמה
להכנה: **01.09.2026 21:24Z**; המדידה שקדמה להרצה: **02.09.2026 06:17Z**.

**המסמך הזה מחליף את `ROLLOUT-0243-0267-20260831.md`.** אותו מסמך לא הפך לשגוי — הוא הפך
ל**חלקי**. הוא הוכן ב-31.08, ומאז נכנסו ל-`main` שמונת גלי המיזוג (`wave/0`…`wave/7`,
‏PRs #198–#207) ואחריהם #204, ‏#187 ו-#201. **הייצור לא זז בכלל:** ראש הלדג'ר עדיין `0242`,
‏233 שורות. מה שגדל הוא רק החבילה.

הרולאאוט האחרון שבוצע בפועל (25.08.2026, `690cd5b`) מתועד ב-`PROGRESS.md`. המסמך הזה בנוי על
התקדים ההוא, על המסמך שהוא מחליף, ועל מטריצת הרולאאוט ב-`CLAUDE.md`. אינו ממציא נוהל חדש.

---

## 1. מה נכלל

| משטח | מה | פירוט |
|---|---|---|
| מסד נתונים | **41 מיגרציות**, ‏`0243`→`0283` | §6 |
| ‏Edge | **11 פונקציות** — 3 שמעולם לא נפרסו, 8 שהשתנו | §8 |
| ‏Frontend | חייב לעלות **באותו רולאאוט**, מיד אחרי ה-Edge | §9 |

**לא נכלל — שני שירותי ה-VPS.** שלושת חוזי ה-gateway מסונכרנים בשני הצדדים, נמדד על `main`:

| חוזה | צד ה-worker | צד ה-Edge |
|---|---|---|
| `GATEWAY_CONTRACT_VERSION` | `worker/ocr/src/gateway.py` = `3` | `document-processing/contract.ts` = `3` |
| `SCAN_GATEWAY_CONTRACT_VERSION` | `worker/ocr/src/scan_gateway.py` = `3` | `document-preprocessing/contract.ts` = `3` |
| `RENDER_CONTRACT_VERSION` | `worker/render/src/contract.mjs` = `1` | `render-document/contract.ts` = `1` |

אין העלאת גרסה בשום צד, ולכן **אין פריסה מחדש של ה-VPS**. אם מישהו נוגע בחוזה לפני שהרולאאוט
יוצא — הכלל ב-`CLAUDE.md` חוזר לתוקף מיד, ו-`Up` אינו ראיה: הראיה היא `job_claimed` ביומן.

---

## 2. מה נמדד (01.09.2026 21:24Z, קריאה בלבד)

```bash
node scripts/rollout-preflight.mjs
```

| קריאה | ערך |
|---|---|
| ראש הלדג'ר | `0242`, **233 שורות** — אפס פערים, אפס עודפים |
| פונקציות / טבלאות / מדיניות | 640 / 216 / 170 |
| ‏`SECURITY DEFINER` | 479 |
| ‏CR בגופי פונקציות | **0** |
| ‏`scope_enforcement_violations()` | **ריק** |
| ‏PITR | **כבוי** |
| הגיבוי האחרון | `1538491869` ‏(31.08 22:44:06Z) — **22.7 שעות בשעת המדידה** |

**ספירות עסקיות שחייבות לא לזוז:** ספקים 25 · מוצרים 271 · הזמנות 263 · שורות הזמנה 478 ·
חשבוניות 15 · תשלומים 7 · הקצאות 9 · תנועות בנק 8 · ייבוא בנק 3 · ארגונים 4 · פרופילים 9.

> **הספירות זזות בין יום ליום** (הזמנות היו 259 ב-31.08 ו-263 ב-01.09). זה שימוש אמיתי, לא
> סטייה. **את הבסיס לוקחים טרי ביום הרולאאוט** — `--save rollout-before.json` — ולא מעתיקים
> את המספרים מכאן.

---

## 3. העוגנים — מה שהמסמך הקודם לא יכול היה לטעון, ונמדד עכשיו

המסמך הקודם כתב במפורש שהוא **אינו** טוען שהעוגנים יתאימו, מפני שניסיון לאמת אותם סטטית
מבחוץ נכשל. הפעם השאלה פורקה לשתיים, וזה שינה אותה.

בין `0243` ל-`0283` יש **19 החלפות מעוגנות**. הן אינן שוות בסיכון:

- **14 מהן מעוגנות על גוף שנוצר קודם באותה שרשרת** — או באותו קובץ עצמו, או במיגרציה מוקדמת
  יותר בתוך `0243`–`0283`. הסובסטרט שהן פוגשות בייצור הוא **בדיוק** זה ש-`supabase db reset`
  ב-CI כבר שיחזר בהצלחה, כולל בגלים 5 ו-7. אין שם שאלה פתוחה.
- **5 מעוגנות על גוף שהייצור כבר נושא**, שנכתב במיגרציה `0242` או קודמת. אלה היחידות שאף
  ריצה מעולם לא בדקה, ולכן היחידות ששווה למדוד.

**חמשתן נמדדו חי, בקריאה בלבד. כולן עומדות בתנאי שלהן:**

| מיגרציה | היעד | הגוף בייצור מ- | מה נמדד |
|---|---|---|---|
| `0254` | 33 גופי שומרים + `execute_organization_purge_batch` | שונים | **32/33** עומדים (קיים · `\nbegin\n` בדיוק פעם אחת · לא הוחל קודם). ה-33 — `private.plan_capability_write_guard()` — **אינו קיים בייצור, וזה תקין**: ‏`0252` יוצר אותו קודם באותה ריצה |
| `0258` | `organizations_guard_lifecycle()` | `0098` | קיים · לא הוחל קודם · רשימת ההיתר **פעמיים** כמצופה · `organizations.onboarding_completed_at` עדיין אינו קיים |
| `0263` | `management_dashboard_snapshot(date)` | `0218` | **2/2** עוגנים בגוף החי |
| `0276` | `apply_reviewed_document(uuid,uuid,jsonb,uuid,text)` | `0110` | לא הוחל קודם · עוגן העמודות **פעם אחת** · עוגן הערכים **פעם אחת** |
| `0282` | `accept_invitation(text,text,text)` ו-`(text,text,text,text)` | `0089` | **3/3** עוגנים בגוף החי |

> **‏`0282` היא זו ש-`PROGRESS.md` סימן כמסוכנת ביותר** — „מעולם לא רצה מול מסד נתונים".
> ההערה נכתבה ב-31.08, **לפני** ש-`wave/5` עבר את שער ה-SQL. מאז היא רצה על מסד טרי ב-CI,
> והעוגנים שלה נמדדו מול הגוף החי בייצור. שתי הראיות אינן זהות — מסד טרי אינו ייצור — אבל
> יחד הן מכסות את שני הסובסטרטים שקיימים.

**מה זה עדיין אינו טוען.** עוגן יכול להתאים והמיגרציה עדיין להיכשל על משהו אחר — הרשאה,
תלות, נעילה. וכל 41 המיגרציות עדיין לא רצו **ברצף** מול הסובסטרט הזה. מה שנשלל כאן הוא סיבת
הכשל היחידה שכבר עצרה רולאאוט בפרויקט הזה, והיא נשללה במדידה ולא בהנחה.

---

## 4. הסדר, והנימוק שלו

```
גיבוי טרי  →  מדידת בסיס  →  0243…0283 לפי הסדר  →  postflight
           →  Edge (11)   →  Frontend  →  smoke חי  →  דף הנחיתה
```

**למה המיגרציות לפני הפרונטאנד. נמדד, לא הונח:** כל 150 שמות ה-RPC שהקוד ב-`main` קורא
הושוו מול קטלוג הפונקציות של הייצור. **שמונה אינם קיימים שם:**

| ‏RPC | נוצר ב- |
|---|---|
| `currencies_in_use` | `0243` |
| `get_public_plan_features` | `0246` |
| `my_export_watermark` | `0247` |
| `my_benefit_window` | `0262` |
| `set_invoice_due_date` | `0264` |
| `scheduled_payments_outlook` | `0265` |
| `record_launch_offer_intent` | `0269` |
| `record_my_countdown_event` | `0271` |

**פרונטאנד חדש לפני המיגרציות = שמונה אתרי קריאה שבורים.** אלה בדיוק המסכים, ולא יותר.

**ובכיוון השני — חלון אחד, והוא לא גדל:** ‏`0246` מצמצמת את `get_public_plan_quotas()`
מחמישה מפתחות לשלושה. מסך המנויים שכבר באוויר קורא אותה, ולכן ישנה את מה שהוא מציג. זה לא
קורס, אבל זה חלון שצריך לסגור מיד. **ואף מיגרציה בין `0243` ל-`0283` אינה מוחקת פונקציה** —
נבדק על כל 41 הקבצים — ולכן שום קריאה של ה-bundle החי לא נעלמת תחתיו.

לכן: המיגרציות ראשונות, ה-Edge אחריהן, והפרונטאנד **מיד אחריו**, באותה ישיבה.

---

## 5. שלב 0 — גיבוי ותזמון

**ה-PITR כבוי.** נקודת החזרה היחידה היא הגיבוי הלילתי, שרץ סביב **22:45Z**.

> אם משהו משתבש וצריך שחזור, מאבדים את כל מה שנעשה מאז הגיבוי האחרון — עד 24 שעות. לכן
> **מריצים את הרולאאוט זמן קצר אחרי שגיבוי לילתי הושלם**, ולא באמצע היום. בשתי המדידות
> האחרונות הגיבוי היה בן 21.2 ואז 22.7 שעות — כלומר **על הגבול. אין להתחיל עליו.**

```bash
node scripts/rollout-preflight.mjs --save rollout-before.json
```

רושמים ביומן: מזהה הגיבוי, החותמת שלו, וכל מספרי הבסיס שהסקריפט מדפיס.

**אין להתחיל אם:** הגיבוי האחרון בן יותר מ-24 שעות, ‏`CR bodies` אינו 0, או
`scope_enforcement_violations()` אינו ריק. הסקריפט מסרב בעצמו על שניים מהשלושה.

**בדיקה מקדימה נוספת — רק אם החבילה תורחב מעבר ל-`0283` ותכלול את `0288`.** ‏`0288` מסירה את
‏`text/html` מרשימת טיפוסי המסמך (‏`#346`), והיא **מסרבת להיות מוחלת** כל עוד קיימת שורת מסמך
**חיה** שמצהירה על הטיפוס הזה. מכיוון ש-`db-query.ps1` שולח כל מיגרציה כבקשה אחת ו-`rollout-apply.ps1`
עוצר על שלב שנכשל, גילוי כזה באמצע החבילה הוא עצירה — לכן שואלים לפני, בקריאה בלבד:

```sql
select count(*) filter (where deleted_at is null) as live_html_documents
from public.documents
where lower(coalesce(mime_type, '')) = 'text/html';
```

‏**0 — ממשיכים.** גדול מ-0: מוציאים כל שורה כזו משימוש עם `remove_document(id, reason)` (מחיקה
רכה, המסלול המורשה היחיד) **לפני** תחילת החבילה, ואז מריצים את השאילתה שוב. שורה שכבר פרשה אינה
נספרת ואינה דורשת דבר — `0288` מרחיבה את `p0_documents_mime_check` כך שהיא נשארת ניתנת לעדכון.

---

## 6. שלב 1 — המיגרציות

**שתי עובדות שקובעות את צורת השלב הזה, ושתיהן לא השתנו:**

1. ‏`scripts/db-query.ps1` שולח את הקובץ כבקשה אחת. פוסטגרס מריץ אצווה כזו בטרנזקציה משתמעת
   אחת, ולכן **כל מיגרציה היא הכול-או-כלום**. אין מצב ביניים בתוך קובץ.
2. ‏**`db-query.ps1` אינו כותב שורה בטבלת המיגרציות.** את השורה מוסיפים אחרי כל קובץ, אחרת
   הלדג'ר משקר וההרצה הבאה תנסה להחיל הכול שוב. ‏`rollout-apply.ps1` עושה זאת בעצמו.

```powershell
$env:SUPABASE_ACCESS_TOKEN = (Get-Content 'D:\משה פרוייקטים\פיתוח אתרים\AI\API\NIR-TOKEN-SUPABASE.txt' -Raw).Trim()
.\scripts\rollout-apply.ps1 -From 0243 -To 0283 -ProjectRef rkftlbctohswhbbiaqin -AllowProduction
```

הסקריפט מחיל קובץ, מאמת, כותב את שורת הלדג'ר, וממשיך — **ועוצר על הכשל הראשון בלי לכתוב את
השורה של הקובץ שנכשל.** הרצה חוזרת ממשיכה מהמקום שבו נעצרה.

### הכבדות שבחבילה

‏`0243`–`0267` מפורטות ב-`ROLLOUT-0243-0267-20260831.md` §5 ולא הועתקו לכאן. אלה שנוספו מאז:

| # | שם | הערה |
|---|---|---|
| 0268 | `countdown_and_expectations_flags` | דגלים, נולדים כבויים |
| 0269 | `launch_offer_intents` | טבלה, מדיניות, 2 פונקציות |
| 0270 | `launch_window_extension` | |
| 0271 | `countdown_events` | |
| 0272 | `supplier_document_expectations` | 2 טבלאות, 2 מדיניות |
| 0273 | `expected_document_missing_exception_type` | ערך enum בלבד |
| **0274** | `document_expectation_scanner` | **736 שורות, 7 פונקציות** |
| 0275 | `supplier_logo_path` | נוגעת ב-Storage |
| 0276 | `invoice_due_date_from_interpretation` | עוגן על גוף חי — §3 |
| 0277 | `paddle_sandbox_price_map` | |
| 0278 | `billing_checkout_authorization` | 3 פונקציות |
| **0279** | `a_document_carries_the_actor_that_made_it` | **1,437 שורות, 3 טבלאות, 12 פונקציות, 31 נגיעות Storage. הגדולה בחבילה** |
| 0280 | `a_number_that_can_receive_a_document` | טבלה, 4 פונקציות, 14 נגיעות Storage |
| 0281 | `subscription_activation_email` | טבלה |
| 0282 | `invitation_is_a_proven_address_not_a_password` | עוגן על גוף חי — §3 |
| 0283 | `profile_theme` | סוגר את `DEBT §91` |

### אם זה נעצר באמצע

**זה מצב אפשרי, והוא לא אסון.** המיגרציות המעוגנות קוראות את הפונקציה כפי שהיא חיה, ואם
הטקסט אינו מה שהן מצפות לו הן **זורקות שגיאה ועוצרות**. הן נכשלות בקול, לא בשקט.

1. **לא להריץ שוב „אולי יעבור".** לקרוא את ההודעה במלואה — היא נוקבת בשם המיגרציה, בעוגן
   ובמספר ההופעות שנמצא בפועל.
2. הקבצים שכבר עברו **נשארים** ושורות הלדג'ר שלהם נכונות. אין צורך לשחזר.
3. לתקן את המיגרציה שנכשלה מול הטקסט החי — לא לכפות אותה.
4. להמשיך מאותו מספר.

**שחזור מגיבוי הוא המוצא האחרון**, ועולה עד יממה של נתונים. הוא נדרש רק אם מיגרציה הצליחה
והשאירה מצב שגוי — לא כשהיא נכשלה, כי כשל הוא אטומי.

---

## 7. שלב 2 — postflight

```bash
node scripts/rollout-preflight.mjs --compare
```

| טענה | ציפייה |
|---|---|
| ראש הלדג'ר | `0283`, **274 שורות** (233 + 41) |
| ‏`scope_enforcement_violations()` | ריק, כמו לפני |
| ספירות עסקיות | **זהות ל-`rollout-before.json`** |
| ‏`audit_logs` | עלה במעט בלבד. עלייה גדולה = מיגרציה נגעה בנתונים עסקיים |
| פונקציות / טבלאות / מדיניות | עלו. **ירידה במדיניות = ‏`0252` מחקה ולא יצרה מחדש** |
| ‏`get_public_plan_quotas()` | מחזירה **3** מפתחות: `documents.monthly`, `users.max`, `branches.max` |
| שמונת ה-RPC של §4 | כולם קיימים |

**אם ספירה עסקית זזה — לעצור ולא להמשיך ל-Edge.**

---

## 8. שלב 3 — Edge

**אחת עשרה פונקציות.** שלוש מעולם לא נפרסו, שמונה השתנו מאז שנפרסו:

| פונקציה | מצב היום |
|---|---|
| `render-document` | **מעולם לא נפרסה** |
| `organization-storage-purge` | **מעולם לא נפרסה** |
| `billing-checkout` | **מעולם לא נפרסה** |
| `assistant` | פרוסה בגרסה 18 מ-30.08; הקוד השתנה ב-#204 |
| `billing-webhook` · `email-sender` · `public-signup` · `send-invite` · `upload-organization-logo` · `webhook-verify` · `whatsapp-webhook` | פרוסות, והקוד השתנה |

‏`0254` בנויה סביב `organization-storage-purge`: בלעדיה מחיקת לקוח מבוקרת לא תשלים, וזה בדיוק
הכשל ש-`0254` נכתבה כדי למנוע.

**‏`_shared` השתנה בשלושה קבצים** — `activation-email.ts`, ‏`billing-adapter.ts`, ‏`reply-to.ts`.
כל צרכן שלהם כבר ברשימה, ולכן אין פונקציה נוספת שנגררת.

### הסודות — נמדדו, ואחד מהם חסר באמת

‏34 סודות מוגדרים בייצור. מול מה שאחת עשרה הפונקציות קוראות בפועל:

| סוד חסר | מי קורא | האם חוסם |
|---|---|---|
| `WHATSAPP_WEBHOOK_URL` | `whatsapp-webhook` | **כן.** הקובץ מצהיר עליו כ-„Required environment" — הוא ה-URL הציבורי המדויק שאימות החתימה של Twilio נבנה עליו, ואי אפשר לשחזר אותו מכותרות מאחורי proxy. **להגדיר לפני הפריסה** |
| `REQUIRE_BACKUP_EMAIL` | `public-signup` | **לא.** הקוד קובע במפורש שחוסר הוא „עדיין לא הודלק" ולעולם לא סירוב, כדי ש-deploy ששכח אותו לא ינעל הרשמה. נדלק יחד עם Apple |

כל שאר הסודות שעשר הפונקציות האחרות קוראות — **קיימים**, כולל ששת אלה של `assistant`.

```bash
npx supabase functions deploy <name> --project-ref rkftlbctohswhbbiaqin
```

לפרוס **רק את האחת עשרה**, אחת-אחת, ולאמת קריאה חיה ממוקדת אחרי כל אחת.

> **‏`whatsapp` פרוסה בייצור ואינה קיימת בריפו** — נמדד שוב 01.09.2026. היא לא חלק
> מהרולאאוט ואין לגעת בה כאן. **פונקציה פרוסה שאיש אינו מתחזק היא משטח תקיפה**, וזו הכרעת
> בעלים נפרדת: למחוק, או להחזיר את המקור לריפו.

---

## 9. שלב 4 — Frontend

מיד אחרי ה-Edge, כדי לסגור את החלון של §4.

**הרולאאוט הזה נושא ארבעה קבצים שסוגרים את `DEBT §93`** — האפליקציה מפסיקה להיות משטח
חיפוש: `public/_headers` (‏`X-Robots-Tag: noindex, nofollow` על `/*`), ‏`public/robots.txt`,
ותג `robots` ב-`index.html` וב-`operator.html`. ‏`Cloudflare Pages` מפרסם תיקייה שלמה, ולכן
**אי אפשר להעלות אותם בלי פריסה** — זו הסיבה שהבעלים דחה ב-01.09 את הקיצור של הוספת הכותרת
בלוח הבקרה.

לפי מטריצת הרולאאוט: בנייה עם env של ייצור, סריקת סודות/localhost, פריסה ל-Pages, ואימות
‏**hash parity בשלוש הכתובות** — הייחודית, הקנונית, ו-`app.inplace.digital`.

> הכתובת הקנונית מגישה את הבנייה הקודמת עוד כדקה אחרי העלאה מוצלחת. לבדוק parity
> **בלולאת polling**, לא בקריאה אחת.

**‏`check:paddle-secrets` חייב לעבור על ה-env של הייצור עצמו** ולא רק על המקומי: הוא מפיל
בנייה שמטמיעה את מפתח השרת של Paddle ב-bundle, וזה השינוי היחיד בחבילה שיכול לפרסם מפתח
שמסוגל להחזיר כסף.

אחרי הפריסה: `npm run check:live-seo` אמור לעבור — הוא אדום היום בכוונה.

---

## 10. שלב 5 — smoke חי

שורת „Auth / תפקידים / RLS" במטריצה חלה, כי `0252` משנה מדיניות ו-`0282` משנה את מסלול
פדיון ההזמנה:

- כניסה בשלושת חשבונות הבדיקה (`owner`, `office`, `accountant`) — הסיסמה נקראת בזמן ריצה
  מהקובץ החיצוני, ואינה מודפסת.
- **קריאה בלבד. אפס כתיבות עסקיות.**
- לוודא שהזהויות שפרשו (`kitchen`, ‏`payer`, ‏`supplier`) עדיין חסומות.
- מסכי הבנק, תור התשלומים והחשבוניות המרכזות — הארגונים על `premium`/`legacy`, ולכן
  **אמורים להישאר פתוחים**. אם נסגרו, ‏`0252` נעלה יותר ממה שהוחלט.
- מסך המנויים — שלושת המפתחות החדשים, לא שבור.
- הדשבורד — עולה, ומתייצב אחרי `networkidle`. צילום מוקדם מדי מראה spinner ואינו ראיה.
- **העוזר** — נפרס מחדש בסבב הזה; קריאה חיה אחת שמחזירה תשובה.

---

## 11. שלב 6 — דף הנחיתה

הרולאאוט סוגר את `DEBT §34` בריפו של דף הנחיתה. אחריו:

```bash
cd "D:\משה פרוייקטים\פיתוח אתרים\LANDING-PAGE-NIR"
npm run build
node scripts/check-live-catalogue.mjs
```

**נמדד 02.09.2026, לפני הרולאאוט: אפס `MISMATCH`, ‏12 `UNVERIFIABLE` על פני 96 תאים, ושתי
דמויות `SILENT`.** המספר „2 MISMATCH" שהופיע כאן קודם הועתק מהמסמך של 31.08 והתיישן —
‏`DEBT 33` בריפו של דף הנחיתה (תווית „30% הנחה" מול 16.7% אמיתיים) **נסגר 31.08.2026**,
והמתג אומר עכשיו „חודשיים חינם". הסקריפט מאשר: „the billing control states no percentage".

**השורה הראשונה בפלט מסבירה את כל השאר:**

```
ladder read model: ABSENT — get_public_plan_features() is not on this database
```

‏`get_public_plan_features()` נוצרת ב-`0246`, שנמצאת בתוך החבילה הזו. כלומר 12 ה-`UNVERIFIABLE`
אינם טעות בדף — הדף מפרסם סולם יכולות שהמוצר בייצור **עדיין לא מפרסם**, מפני שהרולאאוט לא רץ.
אחריו הם אמורים להיעלם.

**ושתי ה-`SILENT` נסגרות באותו צעד, מהכיוון ההפוך:** המוצר מפרסם היום `assistant_runs.monthly`
‏(20/40/100/250) ו-`ocr_pages.monthly` ‏(200/400/1500/3750) שהדף אינו מציג. ‏`0246` מצמצמת את
‏`get_public_plan_quotas()` ומוציאה בדיוק את שתיהן, ומכניסה במקומן את `users.max` ו-`branches.max`
— שהן שתיים מהטענות שהדף כבר מציג ואי אפשר לאמת. **אותה מיגרציה סוגרת את שני הצדדים.**

---

## 12. מה המסמך הזה אינו טוען

- **שהמיגרציות רצו ברצף מול הסובסטרט הזה.** לא רצו. מה שנשלל ב-§3 הוא סיבת הכשל היחידה
  שכבר עצרה רולאאוט כאן, וגם היא נשללה על **חמש** מיגרציות ולא על ארבעים ואחת.
- **שנבדקה הרצה יבשה על עותק של הייצור.** לא בוצעה. היא דורשת שחזור גיבוי לפרויקט נפרד,
  והיא ההגנה החזקה ביותר שקיימת. אם רוצים אותה — היא נכנסת בין §5 ל-§6.
- **ש-`npm run quality` רץ.** לא הורץ במסגרת ההכנה הזו.
- **שתיק ה-CI הירוק מכסה את `verify`.** אינו מכסה. ‏`build.yml` מריץ **13 מתוך 27** השערים
  ש-`npm run verify` מריץ, ו-`check:contrast` נכשל על `main` היום בלי שאף PR מראה זאת —
  חמישה טוקני `--color-doc-ink*` שגל 6 הוסיף מעולם לא נכנסו למניפסט שגל 7 הוסיף. שני
  הפגמים רשומים בנפרד ואינם חוסמים את הרולאאוט, אך „ירוק" כאן אינו „הכול נבדק".
- **שהמספרים כאן יהיו נכונים ביום ההרצה.** ראש הלדג'ר, גיל הגיבוי והספירות העסקיות נמדדים
  **מחדש** ב-§5. המסמך אומר מה למדוד ומה חייב להתקיים, לא מה יימצא.

---

## 13. מה קרה בפועל — 02.09.2026

הרולאאוט רץ מקצה לקצה. כל שלב אומת במדידה, והמדידות למטה הן מה שהודפס, לא מה שצופה.

### שלב 0 — גיבוי ותזמון (06:17Z)

`node scripts/rollout-preflight.mjs` החזיר **GO**: ראש הלדג'ר `0242` / 233 שורות,
‏CR בגופי פונקציות 0, ‏`scope_enforcement_violations()` ריק, והגיבוי האחרון
‏**`1548463825` מ-01.09.2026 22:45:58Z — בן 7.5 שעות**, הרבה מתחת לגבול. הבסיס נשמר ב-
`artifacts/rollout-20260902/rollout-before.json`.

### שלב 1 — המיגרציות (06:28Z–06:36Z)

`rollout-apply.ps1 -From 0243 -To 0283 -AllowProduction`. ‏`-WhatIf` דיווח 41 קבצים בטווח
ו-0 כבר בלדג'ר. ההרצה החילה את **כל 41**, כל אחת עם שורת לדג'ר מאומתת, **בלי כשל אחד**.
‏**חמש ההחלפות המעוגנות על גוף חי — `0254`, `0258`, `0263`, `0276`, `0282` — עברו.** מה ש-§3
ניבא במדידה התרחש. יומן מלא: `rollout-0243-0283-20260902-092833.log`.

### שלב 2 — postflight

| טענה | ציפייה | נמדד |
|---|---|---|
| ראש הלדג'ר | `0283` / 274 | **`0283` / 274** |
| ‏`scope_enforcement_violations()` | ריק | **ריק** |
| ‏CR בגופי פונקציות | 0 | **0** |
| ספירות עסקיות | זהות | **11/11 זהות** |
| פונקציות / טבלאות / מדיניות | עולות | **640→721 · 216→233 · 170→175** |
| ‏`SECURITY DEFINER` | עולה | **479→543** |
| ‏`get_public_plan_quotas()` | 3 מפתחות | **3** — `documents.monthly`, `users.max`, `branches.max` |
| שמונת ה-RPC של §4 | כולם קיימים | **8/8** |
| ‏`audit_logs` | עלייה קטנה | **+43**: ‏23 `prelaunch_window_extended` ‏(`0270` עושה את עבודתה) ו-20 `update` |

### שלב 3 — Edge (06:41Z–06:44Z)

אחת עשרה, אחת-אחת, כולן `ACTIVE`: ‏`render-document` ‏**v1**, ‏`organization-storage-purge`
‏**v1**, ‏`billing-checkout` ‏**v1** (שלוש הראשונות אי פעם), ואז `assistant` v18→**19**,
‏`billing-webhook` →**11**, ‏`email-sender` →**15**, ‏`public-signup` →**16**, ‏`send-invite`
→**32**, ‏`upload-organization-logo` →**17**, ‏`webhook-verify` →**11**, ‏`whatsapp-webhook`
→**11**.

**‏`WHATSAPP_WEBHOOK_URL` — הכרעת בעלים 02.09.2026: לפרוס בלי הסוד.** הפונקציה תמשיך להחזיר
`misconfigured` בכל קריאה, בדיוק כמו לפני הרולאאוט; היא אינה מחוברת לשום ספק. **זה נשאר חוב
פתוח**, ולא נסגר כאן. ‏`REQUIRE_BACKUP_EMAIL` נשאר לא מוגדר בכוונה, יחד עם Apple.
‏`whatsapp` (‏v17, אינה בריפו) **לא נגעה**.

### שלב 4 — Frontend

בנייה עם env הייצור בלבד: ‏`.env.local` הוזז הצידה לפני הבנייה והוחזר אחריה, אחרת Vite היה
מטמיע את `http://127.0.0.1:55431` ב-bundle. הסריקה על `dist/` מצאה **מופע אחד** של כתובת
Supabase — `https://rkftlbctohswhbbiaqin.supabase.co` — ואפס `service_role`, אפס
‏`DEMO_PASSWORD_SEED`, אפס מפתח שרת של Paddle. שתי ההתאמות ל-`localhost` הן קוד ספרייה
(רשימת ההיתר של Supabase Realtime ובדיקת קלט של `html2canvas-pro`), לא הגדרה שלנו.
‏`check:paddle-secrets` ו-`check:noindex-posture` עברו.

פריסה: ‏`wrangler pages deploy dist --project-name=supplyflow` → **`bc09e71c`**.
‏**התאמת hash: 138/138 בכל שלוש הכתובות** — הייחודית, הקנונית ו-`app.inplace.digital`.
פיגור ההפצה לא הופיע הפעם: שתי הכתובות התאימו בסבב הראשון.

**‏`DEBT §93` נסגר בחלקו:** ‏`npm run check:live-seo` **עבר** — ‏2 מקורות × 8 נתיבים, כל
תשובה `noindex, nofollow`, ‏`robots.txt` מוגש כ-`text/plain` וזהה בייט-לבייט לקובץ בריפו.
פריט 3 (המעבר ל-`Disallow: /`) נשאר פתוח וממתין לאפס כתובות ב-Search Console.

### שלב 5 — smoke חי, קריאה בלבד

`artifacts/rollout-20260902/live-smoke.cjs`, ראיות ב-`artifacts/rollout-20260902/smoke/`.

- שלושת חשבונות הבדיקה נכנסו. **אפס שגיאות console, ‏אפס `pageerror`, ‏אפס תשובת HTTP ≥400**
  בשלושתם.
- **אפס כתיבות עסקיות.** כל בקשה שאינה GET היא `POST /rest/v1/rpc/<שם קריאה>` — כך PostgREST
  קורא פונקציה — ואף אחת אינה בשם כותב.
- **הזהויות שפרשו נדחו:** ‏`nir@`, ‏`payer@`, ‏`meshek@supplier.demo` נשארו על `/login`.
- ‏`/bank`, ‏`/pay` ו-`/documents/consolidated-invoices` **נשארו פתוחים** — ‏`0252` לא נעלה
  יותר ממה שהוחלט.
- מסך המנויים: **3/3** תוויות המכסה החדשות מוצגות, המסך שלם. צילום: `smoke/subscription.png`.
- הדשבורד עלה והתייצב בשלושת התפקידים, עם נתונים אמיתיים. צילומים: `smoke/dashboard-*.png`.
- **העוזר ענה חי:** ‏`POST /functions/v1/assistant` → **200**, תשובה בעברית עם `run_id`,
  ‏`conversation_id` ובלוקי `claim`.
- ספירת הנתונים העסקיים נמדדה שוב **אחרי** ה-smoke ונשארה זהה ל-`rollout-before.json`.

### שלב 6 — דף הנחיתה

`npm run build` בריפו של דף הנחיתה עבר (30 עמודים, sitemap, ‏`_headers`).
‏`node scripts/check-live-catalogue.mjs` מדד את הדף החי מול הסולם החי:

- **12 ה-`UNVERIFIABLE` נעלמו** — 0 טענות שלא ניתן לאמת. הסולם קריא, וזה מה שהרולאאוט הבטיח.
- **‏`MISMATCH` ה-30% נעלם גם הוא**: בקרת החיוב אינה מצהירה אחוז יותר, ולכן אין מה לבדוק.
- **ובמקומם נחשפו שלוש סתירות אמיתיות במסלול `basic`, בשתי השפות** (‏6 `MISMATCH`):
  ‏`users.max` — הדף אומר 5, המוצר נותן 1; ‏`exports.custom` — הדף אומר כן, המוצר אומר לא;
  ‏`reports.advanced` — הדף אומר כן, המוצר אומר לא. ועוד `SILENT` אחד: המוצר מפרסם
  ‏`org.multi_unit` ולדף אין שורה עבורו.
  **אלה לא נוצרו כאן** — הם היו קיימים ובלתי-נראים כל עוד הסולם לא היה קריא.

**‏[נסגר באותו יום] הכרעת בעלים 02.09.2026: הדף הולך אחרי המוצר.** תוקן ונפרס:
‏`ca89035` — שלושת התאים בשתי המהדורות, ‏„ייצוא Excel" ירד מכרטיס בסיס לפי הכלל שכבר כתוב
ב-`extra.ts`, והערת ההקדמה הפסיקה לקרוא לחמש יכולות חלון ה-30 יום „היכולות של מסלול בסיס".
מדידה חוזרת: **0 `MISMATCH`, ‏0 `UNVERIFIABLE`, ‏1 `SILENT`** (‏`org.multi_unit`, כפילות של
שורת הסניפים). נפרס ל-`inplace.digital`, ו**הטקסט של שתי המהדורות החיות זהה בדיוק ל-`dist`
המאומת**. ‏`DEBT §34` בריפו של דף הנחיתה **נסגר**.

**ומה שהתיקון חשף במוצר עצמו — `DEBT §108` החדש.** מודל הקריאה מפרסם `intro_included: true`
לחמש יכולות בחלון ההיכרות, אבל האכיפה ב-`effective_entitlement` מגיעה אליהן דרך המסלול
‏`basic` — ושם שתיים מהן `false`. **המסך אומר „נפתחו לך חמש" והשרת נותן שלוש.** הכרעת בעלים
פתוחה, ומיגרציה אחת קטנה לכל כיוון.

### מה עדיין אינו נטען

- **‏`npm run quality` לא רץ** במסגרת הרולאאוט הזה, ולא נטען שרץ.
- **‏`check:contrast` עדיין נכשל על `main`** — חמשת טוקני `--color-doc-ink*`. לא נגעתי.
- **שני שירותי ה-VPS לא נפרסו ברולאאוט הזה**, כמתוכנן: שלוש גרסאות החוזה לא זזו בשום צד.
  **בהמשך אותו יום, בעבודה נפרדת, שירות ה-render כן עלה** — §68 נסגר ו-§72 נפרס,
  ו-`docs/VPS-DEPLOYMENT-GUIDE.md` נכתב ממנה. שירות ה-OCR לא נגע באותו יום.
