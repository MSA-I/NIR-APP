# החלטות פתוחות — דורש אישור עסקי

לפי הנחיית האפיון, שאלות עסקיות שלא נענו לא "הומצאו" — מומשה ברירת-מחדל שמסומנת כאן, וניתנת לשינוי בקלות (רובן ערכי קונפיגורציה).

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 1 | **שיעור מע״מ** | 18% (עדכני לישראל 2026); שדות סכום נשמרים פר-חשבונית כך ששינוי עתידי לא ישבור נתונים היסטוריים | הגדרות ← "שיעור מע״מ", או `organizations.vat_rate` |
| 2 | **מי מאשר דרישת תשלום** | מנהל/בעלים (`owner`) וגם מנהל רכש (`office`) יכולים לאשר; המערכת שומרת מי אישר ומתי. הנהלת חשבונות (`accountant`) מבצעת רק לאחר אישור | `transition_payment_request` + מסך הדרישות |
| 3 | **טווח התאמת בנק** | ±7 ימים, סטיית סכום עד 1 ₪ | הגדרות ← עסק, או `organizations.settings` |
| 4 | **ספירת "נרכש היום/שבוע/חודש"** | לפי שווי הזמנות שנשלחו/אושרו/התקבלו (לא טיוטות ולא מבוטלות), במחירי snapshot | `Dashboard.tsx` |
| 5 | **"חיסכון משוער"** | (המחיר היקר ביותר הזמין − המחיר שנבחר) × כמות, על פריטי דרישות רכש שפוצלו החודש | `Dashboard.tsx` |
| 6 | **זיכוי אוטומטי בקבלה** | נפתח אוטומטית (ברירת מחדל דלוקה, ניתנת לכיבוי בכל קבלה) לפי כמות חסרה × מחיר הזמנה | מסך קבלת סחורה |
| 7 | **שליחת הזמנה לספק** | MVP: סטטוס "נשלחה" + תצוגת הדפסה/שיתוף. אין שליחת WhatsApp/מייל אוטומטית (מוגדר עתידי באפיון) | — |
| 8 | **פרטי בנק של ספק** | שדה טקסט חופשי המוצג למבצע ההעברות. אם נדרש מבנה (בנק/סניף/חשבון נפרדים) — פיצול עמודה | `suppliers.bank_details` |
| 9 | **מבנה תדפיס הבנק** | ממופה ידנית בכל ייבוא (תאריך/תיאור/חובה/אסמכתא), תומך CSV ו-Excel; עמודות ברירת מחדל מזוהות אוטומטית | מסך ייבוא |
| 10 | **חשבוניות ללא הזמנה** | מותר (מציאות קיימת) — הפער יעלה בבדיקות וביומן החריגים | — |
| 11 | **מלאי** | שדה `min_stock` ועדכון `received_qty` קיימים בסכימה; מסך מלאי מלא הוגדר עתידי | — |
| 12 | **הוספת משתמשים** | דרך לוח Supabase (Auth) + שורת פרופיל; אין self-signup (מערכת סגורה) | `scripts/create-users.ps1` |
| 13 | **תפקיד `accountant`** | הנהלת חשבונות תפעולית: חשבוניות מאושרות והקשר רכש מינימלי בלבד; ביצוע תשלום מאושר, בנק, זיכויים, דוחות וייצוא. ללא שינוי מוצר, מחירון או הזמנה | `0031` + route guards |
| 14 | **מטבע** | ₪ בלבד ב-MVP | — |

## שלב 2 — SaaS (20.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 15 | **אורך תקופת ניסיון ומה קורה בפקיעתה** | `trial_ends_at` נשמר ו**אינו נאכף בשום מקום** — רק `suspended` חוסם בפועל. המפעיל מזין תאריך ידנית או משאיר ריק. אכיפה אוטומטית דורשת החלטה עסקית | `0006` + מסך `/admin` |
| 16 | **תוקף הזמנת עובד** | 7 ימים. "שליחה מחדש" מנפיקה טוקן חדש ומאפסת את התוקף — הקישור הקודם מת | `organizations.settings.invite_expiry_days` |
| 17 | **אילו תפקידים ניתן להזמין** | **הוכרע מחדש 09.08.2026 (בעלים): כל התפקידים, כולל `supplier`.** מסלול ה-DB קיים מאז `0025` — ‏`invitations.supplier_id`, ‏overload ‏`create_invitation(text,user_role,uuid)` ואילוץ `invitations_supplier_role_check` שמחייב שיוך ספק — אבל הלקוח ופונקציית המייל מעולם לא חוברו אליו, וההערה ב-`invitations.ts` המשיכה לצטט CHECK שכבר לא קיים. חובר 08.08: בורר ספק בטופס ההזמנה, ‏`send-invite` מעביר `p_supplier_id`. **שינוי תפקיד של עובד קיים ל-`supplier` נשאר חסום במסך** — הוא היה דורש `supplier_id` ש-`manage_profile_access` לא ממציא; חשבון ספק נולד כהזמנת ספק, לא כשינוי תפקיד | `0025_supplier_price_portal.sql` + `src/lib/invitations.ts` + `supabase/functions/send-invite` |
| 18 | **איך הבעלים החדש מקבל פרטי כניסה** | סיסמה ראשונית שהמפעיל מגריל, מוצגת פעם אחת למסירה בערוץ מאובטח (שיקוף `create-users.ps1`). לא הומצא ערוץ מייל | `admin-provision` |
| 19 | **סטטוס יעד בהפעלה מחדש** | תמיד `active` — ארגון שהיה `trial` אינו חוזר ל-`trial`. כתוב במפורש בדיאלוג | `Admin.tsx` |
| 20 | **האם סיבת ההשעיה גלויה לדייר** | נכתבת ל-`audit_logs` של הארגון המושעה, ולכן תיקרא ע״י `owner`/`accountant` שלו עם חזרת הגישה. אם ההערה מסחרית — צריך יעד אחר | `Admin.tsx` |
| 21 | **קטגוריות בסיס ללקוח חדש** | 7 קטגוריות ניטרליות ב-`seed.sql:50-62`, מוצעות באשף כ**הצעות ללחיצה בלבד** — הרשימה מתחילה ריקה ושום דבר לא נוסף בשקט. קטגוריות העסק הישן (מזון/שתייה/חד-פעמי) **לא** הועתקו: הן של עסק אירועים | `seed.sql` + `Onboarding.tsx` |
| 22 | **התאמת ספק בייבוא מוצרים** | לפי שם מדויק. מחיר שהספק שלו לא נפתר — נזרק עם הערה גלויה, **והמוצר עדיין נוצר**, כדי שעמודת ספק שגויה לא תאבד את כל הייבוא | `importSheet.ts` |
| 23 | **גבולות ולידציה בייבוא** | 10MB / 5000 שורות לקובץ; מינימום הזמנה ≤ 10M; מחיר יחידה ≤ 1M. **מעקות בטיחות שהומצאו, לא כללים עסקיים** | `importSheet.ts` |
| 24 | **לוגו פר-דייר** | **לא מומש.** "שיישא את זהות הלקוח" מרמז על כך, אבל לא הוגדרו נתיב אחסון (תחת `{org_id}/`), מגבלות גודל, ואיפה מופיע בהדפסה | — |

## סעיף 9 — התראות (20.07.2026)

שלושה ספים שהיו הופכים לניחוש שקט אילו נשארו בקוד בלבד. כולם קבועים בעלי שם ב-`src/lib/alerts.ts`. **#25–27 אושרו ע״י המשתמש 20.07.2026.**

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 25 | **מתי מחיר נחשב "גבוה מהממוצע"** | 15% מעל ממוצע הספקים לאותו מוצר. "מעל הממוצע" מילולית היה מסמן כמחצית מההצעות ומאבד את הערך. **נמדד רק על מוצר עם 2+ ספקים** — למוצר עם ספק יחיד המחיר שלו *הוא* הממוצע | `ABOVE_AVG_MARGIN` |
| 26 | **חלון "ספק העלה מחיר"** | 30 יום אחורה מ-`price_effective_date` | `PRICE_INCREASE_WINDOW_DAYS` |
| 27 | **מתי תשלום "מתקרב לפירעון"** | 7 ימים קדימה. **חל רק על דרישות תשלום שהוזן להן תאריך ידנית** — לחשבונית אין `due_date` ו-`suppliers.payment_terms` הוא טקסט חופשי שאיש אינו מפרסר. ההתראה אומרת זאת במפורש בטקסט שלה, כדי שמנהל לא יקרא אותה כ"הכול מכוסה" | `DUE_SOON_DAYS` |

## הוכרעו 20.07.2026 — סעיף 11

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 28 | **מחיקת מסמך מצורף** | **רכה.** `documents.deleted_at` במיגרציה `0010`; מדיניות ה-DELETE הקשה **הוסרה**, כך שהמסלול ההרסני סגור גם לקריאת API ישירה. טריגר נועל כל עמודה פרט ל-`deleted_at`/`deleted_by` — מדיניות UPDATE מגבילה *שורה*, לא *עמודה*, אותו פער שיצר את פרצת `profiles` לפני `0006`. **הקובץ באחסון נשמר בכוונה:** מחיקת הבייטים תוך שמירת השורה הייתה מאבדת את המסמך והופכת את המחיקה הרכה לחסרת טעם | `0010` + `FileUpload.tsx` |
| 29 | **מוצר ללא ספקים — `0` או `—`** | **`0`.** אפס ספקים הוא עובדה נמדדת ופעילה (המוצר לא ניתן להזמנה), לא היעדר נתונים. הכלל "מדד ללא נתונים מציג `—`" חל על *היעדר מדידה*, ולא על תוצאת מדידה שהיא אפס — הצגת `—` כאן קברה בדיוק את השורות ששוות מבט | `Products.tsx:46` |

### סעיפים 1–6 (21.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 30 | **דירוג ספק — ידני או מחושב** | **ידני 1-5 ע״י הצוות** (`suppliers.rating`). **לא** ציון משוקלל מחושב — קביעת המשקלות בין עמידה-בזמנים/חריגים/זיכויים היא הכרעה עסקית, לא הומצאה | `suppliers.rating` |
| 31 | **חלון מדדי ספק** | 180 יום; מונים "אי פעם" מוצגים לצידם. אספקה חלקית נמדדת לפי הקבלה המושלמת הראשונה (`min(received_at)`) | `supplier_metrics` view (`0012`) |
| 32 | **`expected_date` אינו נכתב בשום מסלול** | עמידה-בזמנים וזמן-אספקה מציגים `—` ("אין תאריך אספקה מוזן"), **לעולם לא `0%`**. הוספת שדה קליטה ב-`NewOrder`/`Orders` היא הכרעה עסקית שלא בוצעה — עד אז המדד לא-null רק על נתוני seed | `NewOrder.tsx`, `Orders.tsx` |
| 33 | **11 מיפויי צבע-לסטטוס לא הוכרעו** | הושארו בצבע הקיים, לא נוחשו: expired · PO.ready/sent/confirmed · receipt.returned · invoice.in_review · export.not_sent · credit.received · pr.approved/sent_for_execution/executed. + טון `idle` (ניטרלי) + 3 סטטוסי `violet` זמניים שדורשים משמעות עסקית | `src/lib/status.ts` |


**נסגרו 21.07.2026 (מסמך `NIR-DEV-CLOSEOUT.md`):** #32 — `expected_date` = תאריך **מבוקש**, נכתב ב-NewOrder ובמודאל האישור. #33 — כל 11 המיפויים הוכרעו והטון `violet` הוסר לגמרי. **החלטת FK חדשה:** `payment_allocations` ו-`bank_allocations` יושרו ל-`ON DELETE RESTRICT` (מיגרציה `0013`) — מחיקה קשה של תשלום נכשלת בקול במקום קסקד שקט, בהתאם לעקרון המחיקה הרכה לרשומות כספיות.
**שתיים מהתראות ניר לא מומשו ואינן מוצגות כאפס:** "מלאי נמוך" (אין כמות במלאי בסכימה — ראה #11) ו"חריגה בתקציב" (אין טבלת תקציב כלל; צד ההוצאה מחושב, היעד חסר והוא קלט עסקי).

**סולמות חומרה — נסגר 21.07 כהפרדה מכוונת:** `exceptions.severity` (`low/medium/high`) הוא enum **מאוחסן** ב-DB; `checks.ts`/`alerts.ts` (`info/warning/critical`) הם ערכים **חולפים** בזיכרון. שני מחזורי-חיים, לא לאחד. שניהם ממופים לאותם טונים לתצוגה. מתועד בהערה ב-`status.ts`.

**חוב ידוע שנוצר בשלב 2:** ערכי enum שאין להם נתונים ב-seed הדמו — `receipt_line_status='returned'`, `credit_status='received'`, `supplier_status='inactive'`, `bank_tx_status='ignored'`, ושלושה סטטוסי דרישת תשלום. הוספת נתונים להם הייתה מזיזה את היתרות הפיננסיות שהדמו מסתמך עליהן. החריגים בדמו משויכים ל-`office` ו-`kitchen` בלבד, כלומר "חריג לכל תפקיד" דק ממה שהצ׳קליסט מרמז.

**נקודות מהאפיון העברי שנשארו לפגישת האפיון:** רשימת 15 הספקים האמיתיים ומה כל אחד מספק, מבנה האקסלים הקיימים של המזכירה והבעלים, זהות צד ג' המבצע ומה בדיוק הוא מקבל, פורמט תדפיס הבנק בפועל, ואילו דוחות בדיוק עוברים לרו״ח בסוף חודש.

## סבב ניר — תפעול, מסמכים ו-Push‏ (21.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 34 | **פילוח הוצאות ב-`/expenses`** | טבלת הספקים היא המקור הראשי והמדויק, לפי סכומי חשבוניות. פירוט הקטגוריות משני, סגור כברירת מחדל, ומוצהר בו במפורש שהוא snapshot של פריטי הזמנות מקושרות — אין donut ואין הצגת אומדן קטגוריות כאילו היה סכום חשבוניות | `src/pages/Expenses.tsx` |
| 35 | **תיוק-מחדש של מסמך מה-inbox** | רק אל `invoice`/`goods_receipt`, ורק ע״י owner/office. ‏`file_document` גוזר ספק ותאריך מהיעד וכותב audit עם סיבה באותה עסקה; עדכון תיוק ישיר נחסם | מיגרציות `0014`/`0019` + `DocumentsInbox.tsx` |
| 36 | **"מחיקה" בתפריטי השורה — פר-ישות** | חשבוניות/ספקים — מחיקה רכה עם סיבה חובה; חשבונית נמחקת רק ב־RPC אטומי ונחסמת אם יש לה קשר תשלום, דרישת תשלום, זיכוי, התאמת בנק או export; הזמנות/דרישות תשלום — ביטול סטטוס עם סיבה; מוצרים — השבתה ב-`active` (לא נוסף `deleted_at`) | `soft_delete_invoice` ב־`0034` + ה־`rowActions` בכל דף |
| 37 | **קהל ה-push וספיו** | Push נשלח ל-owner/office בלבד ורק לאירועי warning/critical: עליית מחיר, חשד לחשבונית כפולה ותשלום שמתקרב לפירעון/עבר אותו. ממצאי info נשארים במסך ההתראות החי. ספי הפירעון זהים לחוזה של `src/lib/alerts.ts` ‏(`DUE_SOON_DAYS=7`, רשימת `PR_ACTIVE`) ומשנים את שני הצדדים יחד | `supabase/functions/send-push` + מיגרציות `0016`–`0017` |
| 38 | **שכפול חשבונית** | מעתיק סכומים, ספק והערות — **לעולם לא את מספר החשבונית**, כדי לא לעקוף את בדיקת הכפילויות | `InvoiceNew` ‏(`?from=`) |
| 39 | **מה מונה פעמון ההתראות ומתי נחשב נקרא** | התג מונה רק רשומות `notifications` של המשתמש עם `read_at IS NULL`. לחיצה מובילה ישירות ל־`/alerts`, בלי dropdown; הרשומות מסומנות כנקראו רק אחרי טעינת הסריקה החיה בהצלחה | `NotificationBell.tsx` + `Alerts.tsx` + מיגרציה `0017` |
| 40 | **תדירות התראת מצב עומד** | פעם אחת בתחילת מחזור warning, פעם נוספת רק בהסלמה ל־critical, ללא חזרה יומית. לאחר שהמצב נפתר ונסגר, הישנות עתידית פותחת מחזור חדש. עליית מחיר היא אירוע בדיד ולכן כל העלאה חדשה היא אירוע חדש | `notification_event_states` + `claim_notification_event` + `send-push` |

## סבב ניר — טיוטות, חיסכון וגלריית מסמכים (22.07.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 41 | **מסך כניסה ושם המסך הראשי** | owner/office ממשיכים לנחות ב־`/dashboard`, ששמו “מרכז הבקרה”; “הזמנה חדשה” היא פריט הניווט הראשון | `Layout.tsx` + `Dashboard.tsx` |
| 42 | **מודל טיוטות** | כמה טיוטות אישיות למשתמש; `/orders/new` ממשיך את האחרונה, `?draft=` פותח מפורשת ו־`?fresh=1` מתחיל חדשה | `0018` + `NewOrder.tsx` |
| 43 | **קו בסיס לחיסכון** | הספק הזול ביותר שמכסה את כל הסל. כשאין ספק כזה מוצג `—`; הפרש שלילי מוצג כתוספת עלות ולא כחיסכון אפס | `orderSavings.ts` |
| 44 | **משמעות “אשר ושלח”** | האישור יוצר הזמנות `ready` באופן אטומי; כל הזמנה עוברת ל־`sent` רק לאחר פתיחת פעולת השליחה הקיימת | `0018` + `Orders.tsx` |
| 45 | **סטטוס בגלריית מסמכים** | סטטוס תיוק אחיד: `inbox`/ללא יעד = לא משויך, וכל יעד עסקי = משויך | `DocumentsInbox.tsx` |
| 46 | **סיווג מסמכים היסטוריים** | סוג נשאר `other` עד סיווג אמיתי; ספק ותאריך ניתנים לגזירה מהישות המקושרת. אין ניחוש בין חשבונית לתעודת משלוח | `0019` |
## P0 — גבולות אמון (22.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 47 | **הגבלת משלוח הזמנות עובד** | 60 שניות בין יצירה/שליחה מחדש ו־20 בקשות משלוח למזמין בכל חלון של 24 שעות. אלה מעקות אבטחה, לא כלל מסחרי מאושר; שני הערכים ניתנים לשינוי פר־דייר ב־`organizations.settings` | `invite_resend_cooldown_seconds`, ‏`invite_send_limit_per_day` + `0020` |
| 48 | **סוג וגודל קובץ מסמך** | החלטת P0 ההיסטורית אפשרה PDF ותמונות בלבד ללא מגבלת גודל עסקית חדשה. החלטה זו הוחלפה עבור pipeline העיבוד החכם ב־#69–#70: ‏HTML נתמך, SVG עדיין חסום, והמגבלה היא 10MB | bucket `documents` + `0022`; הוחלף ב־`0045` וב־#69–#70 |

## P1 — פקודות פיננסיות אטומיות (22.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 49 | **השפעה כספית של פריט פגום או מוחזר בקבלה** | **הוכרע 09.08.2026 (בעלים): אוטומטי מהקבלה.** ‏`damaged`/`returned` פותחים דרישת זיכוי אוטומטית תחת אותה תיבת `p_open_credits` שהאוטומציה של חוסרים כבר משתמשת בה (ברירת המחדל במסך: מסומן). הסכום: **מלוא הכמות הלא-שמישה במחיר ה-snapshot של ההזמנה** — עידון "רק הכמות שלא הוחלפה" נשאר החלטה עתידית; היום הספק חייב על כל מה שהגיע לא-שמיש. מומש ב-`0087` כהזרקה לגוף החי של `save_goods_receipt` (לא הצהרה-מחדש); ‏`p1_financial_commands.sql` טוען את שני הזיכויים (חוסר 40 + החזרה 100) על אותה קבלה | `0087_receipt_credit_automation_and_manual_exceptions.sql` + `Receiving.tsx` + `Credits.tsx` |
| 50 | **שובר שוויון בין הצעות מחיר זהות** | מחיר קודם; בשוויון נבחר `supplier_id` הנמוך בסדר UUID. אותו סדר מיושם בשרת ובדפדפן כדי שהספק המוצג יהיה הספק שנשמר | `0018`/P1 + `NewOrder.tsx` + `orderSavings.ts` |

## P0 — חוזה תפקידים מאוחד (23.07.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 51 | **מה פירוש `office`** | מנהל רכש. `owner` הוא מנהל/בעלים; `accountant` הוא הנהלת חשבונות תפעולית. אין שינוי ב־enum ‏`user_role` | `PRODUCT.md`, ‏`ARCHITECTURE.md`, ‏`status.ts` |
| 52 | **מי מבצע תשלום** | המסלול הרגיל הוא `payer`/`accountant` בלבד. `owner` חסום בו ומקבל RPC חירום נפרד עם `amr.method=password` טרי עד חמש דקות, סיבה, idempotency ו־audit נפרד | `0031_role_capability_contract.sql` |
| 53 | **איך מסירים התאמת בנק** | התאמה לתשלום קיים ניתנת להסרה עם סיבה ו־audit, בלי למחוק את התשלום. אם תנועה מקושרת לכמה תשלומים, כל ההקצאות ננעלות וכל דרישות התשלום המקושרות חוזרות ל־`executed` באותה עסקה. התאמה ישירה לחשבונית כבר צרכה יתרה ולכן נחסמת ודורשת מסלול תיקון כספי נפרד; אין מחיקה או היפוך היסטוריה שקט | `unmatch_bank_transaction` ב־`0034` |
| 54 | **סיבה בפעולות רכש שגרתיות** | אישור הזמנה וקבלת סחורה רגילה אינם דורשים מן המשתמש להקליד הצדקה מלאכותית. הלקוח שולח סיבה מערכתית קבועה ומובנת (`אישור הזמנה`, `שמירת ביניים של קבלת סחורה`, `השלמת קבלת סחורה`) והיא נשמרת ב־audit. פעולות חריגות או רגישות — ביטול, מחיקה רכה, בנק, זיכוי ומסלול תשלום חירום — ממשיכות לדרוש סיבה מפורשת מן המשתמש | `NewOrder.tsx`, ‏`Receiving.tsx`, פקודות P1 |
| 55 | **איך מתעדים שליחה ואישור ספק להזמנה** | אין עוד PATCH ישיר לעמודות lifecycle. הלקוח שולח סיבה מערכתית עסקית ל־`transition_purchase_order_status`; השרת קובע `sent_at`/`confirmed_at`, משמר הערת אישור ותאריך יעד אופציונלי וכותב audit אטומי. ביטול נשאר ב־RPC הנפרד שלו | `0041_reasoned_purchase_order_status.sql`, ‏`Orders.tsx`, ‏`share.ts` |
| 56 | **מה מאשר rollout** | PASS מקומי, גם מלא, אינו אישור deploy. ‏staging דורש אישור נפרד, dry run למיגרציות ו־smoke לכל תפקיד; production דורש אישור נוסף, snapshot או forward-fix, ניטור והוכחה חזותית. בדיקת מנהל של 10 שניות ו־NVDA/200% הן אנושיות ונשארות BLOCKED עד שבוצעו בפועל | `check-quality-gates.ps1`, דוח קבלה `06` |

## עורך ההזמנה — פיצול אוטומטי ושלב הספקים (26.07.2026)

| # | שאלה | ברירת המחדל שמומשה | איפה משנים |
|---|---|---|---|
| 62 | **האם שלב 3 ניתן לדילוג בהזמנה לספק יחיד** | **לא — הסיכום תמיד מוצג.** מסך אחד קבוע לפני יצירת הזמנות עדיף על מסלול שמשתנה לפי מספר הספקים; ספירת ספקים היא בדיוק אחד מהנתונים שהסיכום אמור לאשר. אם יוכרע לדלג — התנאי היחיד הסביר הוא `groups.length === 1 && breachCount === 0` | `SummaryStep` ב־`src/pages/neworder/` |
| 63 | **מה קורה לפינים כשטיוטה נוצרת מחדש מהזמנה קיימת (`?from=<orderId>`)** | **הספק של ההזמנה המקורית נעשה פין מפורש** לכל שורה שההצעה שלו עדיין זמינה — זה שיקוף של ההתנהגות הקיימת, שהציבה `chosenSupplierId = source.supplier_id`. שורה שההצעה שלה נעלמה חוזרת ל־auto. החלופה — הזמנה חוזרת שמתפצלת מחדש לספק אחר בלי לומר — נפסלה כשקטה מדי | `NewOrder.tsx` hydration של `data.source` |

## מינימום הזמנה ופיצול ידני (26.07.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 64 | **רשימת "להזמנה הבאה" — פר משתמש או פר ארגון** | **פר משתמש.** מהדהד את מודל הטיוטות (#42): הדחייה היא כוונת רכש אישית של מי שבנה את ההזמנה, לא מדיניות של העסק. `next_order_items.user_id` נושא `default auth.uid()` ואינו ב־allowlist הכתיבה, כך שדפדפן אינו יכול לזייף בעלות; ה־RLS מסנן `org_id = auth_org() and user_id = auth.uid()`. מעבר לתחולה ארגונית = הסרת תנאי ה־`user_id` משלוש המדיניות והסרת המסנן מהשאילתה | `0044_next_order_items.sql` + `src/lib/nextOrderItems.ts` |
| 65 | **מתי פריט דחוי פוקע** | **90 יום, נאכף בקריאה בלבד.** `listNextOrderItems` מסנן `created_at > now() - interval '90 days'`. אין cron ואין מחיקה — שורה שפגה נעלמת מהתצוגה ונשארת ב־DB, כדי ששום כוונת רכש לא תימחק בשקט וההחלטה תישאר הפיכה. תזכורת שאיש לא נגע בה במשך רבעון היא רעש, לא מידע | `src/lib/nextOrderItems.ts` |
| 66 | **האם `increase_qty` יעגל לגודל אריזה** | **לא בשלב הזה — מעגל למינימום המדויק בלבד.** `supplier_products.package_size` **קיים בסכימה** (`0001_init.sql:113`) וב־`src/lib/types.ts:122`, אך אינו נכתב ואינו נקרא בשום מסלול באפליקציה. עיגול כלפי מעלה לאריזה יגדיל את ההזמנה מעבר למה שהמשתמש ביקש — הפרה ישירה של "לעולם לא חייב להגדיל כמות שאינו צריך" (החלטה 2). כשיהיו נתוני אריזה אמיתיים: `resolutionOptions` מוסיף `packRounded: boolean` ומציג את שתי האופציות זו לצד זו | `resolutionOptions` ב־`src/lib/orderSplit.ts` |

## יישור הסכימה החיה של `supplier_price_submissions` (26.07.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 67 | **מה גורלן של קבלות מדור קודם ללא metadata מלא של הקובץ** | אין ממציאים נתון. `payload_hash`, אם קיים, הוא checksum אמיתי ומקבל את השם `file_checksum`; ‏`file_name`, ‏`storage_path` ו־`submitted_by` נשארים nullable כשלא נשמרו במקור. אילוצי הנתיב וה־checksum נוספו כ־`not valid` ונשארים כך; כתיבות חדשות עדיין חייבות metadata מלא דרך פקודת הקליטה. בזמן האינטרוספקציה היו בייצור 0 קבלות, ולכן לא שוכתבה היסטוריה | `0030_align_supplier_price_submissions.sql` |
| 68 | **איך מונעים חזרה של סחף בין ה־ledger החי לריפו** | ה־ledger החי כבר הכיל מיגרציות `0025`–`0029` שונות מן המספור המקומי. השושלת החיה נשמרה בריפו מילה במילה, גשר היישור קיבל `0030`, מיגרציות התיקון מוספרו מחדש `0031`–`0041`, ו־`0042` מקבעת ACL מפורש לפרופיל; מיגרציות עורך ההזמנה הן `0043`–`0044` ותוכניות נוספות מתחילות ב־`0045`. **פתוח:** להוסיף שער CI שקורא את ledger וסכמת הייצור ומשווה אותם ל־reset נקי, כדי שסחף עתידי ייחסם לפני פריסה ולא בקריסת מסך | `supabase/migrations/0025_*`–`0044_*`, ‏`live_schema_alignment.sql` |

## חוזה עיבוד מסמכים חכם (29.07.2026)

ההחלטות הבאות מחליפות את השאלה הפתוחה ב-#48 עבור pipeline העיבוד. הן מגדירות חוזה קלט
ותשתית בלבד; אינן טוענות שמנוע OCR, worker או UI כבר מומשו.

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 69 | **אילו מקורות נכנסים ל-pipeline** | JPEG, ‏PNG, ‏PDF, ‏XLS, ‏XLSX, ‏CSV, ‏DOC, ‏DOCX, ‏RTF, ‏TXT, ‏HTML ו-ODT. ‏MIME נגזר מה-bytes וה-extension הוא רמז בלבד. קובץ מובנה נקרא קודם ב-parser native; OCR שמור לשכבה חזותית | `0045_smart_document_processing.sql`; worker ב-PLAN-02 |
| 70 | **מהם גבולות הבטיחות** | 10MB לקובץ, 100 עמודי PDF, ‏5,000 שורות spreadsheet, ‏2 מיליון תווי טקסט ו-100MB לאחר decompression. worker רשאי להחמיר אך לא להרחיב בלי migration והחלטה חדשה. מוצפן, פגום או unsupported נכשל בקוד מפורש; אין fallback שקט ל-`other` | `0045` + חוזה worker |
| 71 | **האם חילוץ משנה נתונים עסקיים** | לא. `document_extractions` הוא ledger immutable של הצעה/ראיה; reprocess יוצר job חדש. אין כתיבה אוטומטית לחשבונית, קבלה, זיכוי, מחיר או יתרה, וקליטת מחירון מאושר ממשיכה רק דרך `submit_supplier_price_list` | `0045`; תוכניות 03, 05 ו-06 |
| 72 | **מאין מגיע checksum לפני שה-worker מוריד bytes** | `enqueue_document_processing` גוזרת server-side את `input_checksum` מ-`storage.objects.metadata.eTag` ומנרמלת אותו. ‏`eTag` חסר חוסם enqueue; הלקוח אינו מוסר checksum סמכותי | `0045_smart_document_processing.sql` |
| 73 | **מהו סדר הקואורדינטות של bbox** | כל bbox בחוזה גרסה `1` הוא `[xMin, yMin, xMax, yMax]`, מנורמל ל־`0..1`, עם `xMax >= xMin` ו־`yMax >= yMin` | `0045_smart_document_processing.sql`; `src/lib/types.ts` |
| 74 | **איך נשמר תיקון ידני בלי לשכתב ראיית OCR** | החילוץ והפרשנות נשארים immutable. Annotation חד־פעמי נכתב ל־`document_annotations`; תיקון block/תא נכתב כ־revision append-only ב־`document_review_corrections`. השרת נועל document ואז interpretation/job, גוזר original/before, מאמת target, checksum/contract/current Storage ו־expected revision/text, וכותב audit עם סיבה ללא שכפול הטקסט הגולמי ב־audit | `0049_document_review_mutations.sql` |
| 75 | **איך נקבע סוג המסמך** | **עודכן בהכרעת הבעלים 07.08.2026: אין שלב אישור ידני.** ‏`document_interpretations.payload.document_type` נשאר immutable, והמערכת מעתיקה את הסיווג אוטומטית ל־`documents.document_kind` עם audit. אם הסיווג שגוי, owner/office/kitchen רשאים לבצע **תיקון מנומק** דרך `review_document_type`; התיקון נשמר כ־revision append-only ואינו משכתב את הצעת המודל. ספק רשאי לקרוא הכרעה למחירון שלו אך לא לתקן | `0084_automatic_document_classification.sql`, `DocumentReviewProposals.tsx` |

## העלאת מחירונים מכל מסך, אספקות היום/מחר (04.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 76 | **מהי "אספקה היום/מחר" בכרטיס הדשבורד** | הזמנת רכש בסטטוס `sent`/`confirmed`/`partial` שה-`expected_date` שלה הוא היום או מחר (Asia/Jerusalem). הזמנות פתוחות ללא `expected_date` אינן נספרות — הכרטיס מציג שורת רמז "N הזמנות פתוחות ללא תאריך אספקה" כדי לא לטעון לשלמות. **אין שדה שעת אספקה בסכימה** — שעה אינה מוצגת ואינה מומצאת; הוספת שדה שעה דורשת migration והחלטה עסקית | `Dashboard.tsx` |
| 77 | **זיהוי אוטומטי של הספק ממסמך מחירון** | לא קיים במסלול הניתן לאישור: `reserve_supplier_price_document_upload` כובל ספק לנתיב האחסון בעת ההזמנה (אינווריאנט אבטחה של `0048`), ולכן הספק נבחר בעת ההעלאה — נעול מהקשר (שורת ספק) או נבחר ידנית (מוצרים/מחירונים). ה-OCR עדיין מזהה ספק ומציג התרעה על אי-התאמה במסך הבדיקה. שינוי לזיהוי-תחילה דורש ריכוך חוזה ה-intake — החלטה עסקית-אבטחתית נפרדת | `PriceListUpload.tsx` |
| 78 | **מוצר חדש מתוך מחירון** | **עודכן בהכרעת הבעלים 07.08.2026:** במסלול OCR אוטומטי, שורה שלא הותאמה יכולה ליצור מוצר ומחיר ספק באותה עסקה רק אם הודפסו בה **שם מוצר וגם מק״ט או ברקוד**. שם לעולם אינו מפתח התאמה; התאמה עמומה, שורה בלי מזהה, בלי שם או בלי מחיר תקין ממתינה לאדם. ביטול הקליטה משבית רכה את המוצר החדש ואת קשר הספק. במסלול Excel ובתיקון ידני ההתנהגות המפורשת הקיימת נשארת | `0081_automatic_interpretation_and_price_list_intake.sql`, `PriceListReviewConfirmation.tsx` |

## יסודות Enterprise (04.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 79 | **האם נבנה מצב כהה** | **לא.** אומת שאין מצב כהה בקוד כלל — אפס מופעים של `dark:` / `prefers-color-scheme` / `data-theme` / `.dark` ב-`src`. ‏`DESIGN.md` מגדיר ערכה אחת ("נייר חם" + shell פטרול כהה כמסגרת ניווט בלבד; ה-shell אינו מצב כהה). מסמך ההנחיות הארגוני דרש "לאמת מצב כהה ובהיר", אך הדרישה נכתבה על בסיס הנחה שגויה. כל קריטריון כזה מסומן **N/A מתועד**, לא "עבר". רכיב שמאומץ מ-Radix/shadcn מותאם לטוקנים הקיימים בלבד. מעבר ערכה עתידי = שלושה קבצים (`DESIGN.md:353-355`) **ובנוסף** invalidation ל-`chartTheme()` שממוטמן ברמת מודול | `docs/adr/0002-no-dark-theme.md` |
| 80 | **היכן נשמרות העדפות עמודה בטבלאות עד שקיימת היררכיית סניפים** | ‏`localStorage` פר-משתמש-פר-מסך, **כפתרון זמני מוצהר**. "תצוגות שמורות" אמיתיות מסוקפות לפי ארגון · משתמש · מסך · סניף אופציונלי · פרטי/משותף, ומחייבות audit לשינוי בתצוגה משותפת — ולכן לא נבנו לפני שהיחידה קיימת במסד. **הסכימה מומשה במיגרציה `0058`** (`saved_views` + ‏`save_saved_view`/`delete_saved_view`); ה-UI עדיין על `localStorage` עד גל ה-UI של ריבוי יחידות. עד אז אין להציג את התכונה כיכולת ארגונית, ואין להסתמך עליה בין מכשירים | `docs/adr/0007-server-side-tables.md` + ‏`0058_saved_views.sql` |
| 81 | **מה מותר לעבוד offline** | **רק מסלול קבלת הסחורה.** ‏`public/sw.js` מצהיר במפורש שאין מטמון כי "יתרה מקאש היא יתרה שגויה", וההחלטה הזו נשארת. החריג נפתח משום שקבלת סחורה היא **דיווח על מה שהמכשיר ראה** — עובדה מקומית שהמכשיר הוא מקורה המהימן; אישור תשלום הוא **הכרעה על מצב מרוחק** שהמכשיר אינו יכול לדעת. יתרות, סכומים לתשלום, סטטוס תשלום ופרטי בנק **לעולם אינם נשמרים במטמון**, גם במסלול הקבלה | `docs/OFFLINE-SYNC-DESIGN.md`, `docs/adr/0006-offline-scoped-to-goods-receiving.md` |
| 82 | **כיצד מסווגות טבלאות הקטלוג בהיררכיית הסניפים** | ‏`suppliers`, ‏`products` ו-`supplier_products` נשארים **גלובליים-ארגון** ואינם מקבלים עמודת סניף: ספק הוא צד נגדי משפטי של הארגון, ומחיר נסגר מולו ברמת הארגון. סיווגם כסניפיים היה מפצל את רשם הספקים, מכפיל את המחירון במספר הסניפים ושובר את מנוע השוואת המחירים. הגבלת סניפים לספקים מסוימים, אם תידרש, היא טבלת N:M נפרדת — **הגבלה, לא בעלות**. חשבוניות, תשלומים ותנועות בנק מסווגים לפי **ישות משפטית**; רכש לפי **סניף**; קבלה ומלאי לפי **מחסן** | `docs/ENTERPRISE-SECURITY-MODEL.md` §3 |

## גל 3 — אכיפת סקופ (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 83 | **איך A5 מכסה ~60 פונקציות definer שנוגעות בטבלאות מסוקפות בלי להצהיר את כולן מחדש בגל אחד** | **רשם חריגים + בריח** — הכרעת מתזמר 05.08.2026 (‏PLAN-03 §1.1): הצהרה-מחדש גורפת (~6,000 שורות, 17 מוקשי silent-revert) היא סיכון רגרסיה גדול מהסיכון שהיא מונעת. בגל הזה רומדיאו **שתי פונקציות היתרה בלבד** (תנאי `auth_scopes` בגוף, מ-`0031`); יתר הפונקציות רשומות ב-`private.scope_definer_exemptions` (חתימה · סיבה · גל יעד; ‏59 שורות בזמן `0057`). ‏A5 נכשלת ב-`db reset` על definer חדש לא-מכוסה ולא-רשום. **הבריח:** כל עוד הרשם אינו ריק, ארגון עם יחידות-אחיות מאותו סוג הוא אנומליית preflight חוסמת (`multi_unit_org_with_open_exemptions`) — המצב שבו חריג היה מדליף בפועל אינו בר-השגה. **משנים** ע"י ריקון הרשם: רמדיאציה של פונקציה ⇒ מחיקת שורתה; רשם ריק ⇒ הבריח שותק ויחידות-אחיות מותרות. זה תנאי הכניסה של גל אפשור ריבוי-היחידות | `0057_scope_enforcement.sql` + ‏`p1_preflight.sql` + ‏`p3_org_scope.sql` |
| 84 | **מהי שרשרת היחידות של ארגון חדש ומה משמעות `unit_id` ריק** | שרשרת ברירת-מחדל אחת לכל ארגון, בעברית: ‏root (שם הארגון עצמו) ← ‏legal_entity ‏"ישות משפטית ראשית" ← ‏branch ‏"סניף ראשי" ← ‏warehouse ‏"מחסן ראשי". נוצרת בטריגר `AFTER INSERT ON organizations` ‏(`0054`) וב-backfill ‏(`0056`). כל משתמש מעוגן לשורש ⇒ התנהגות זהה להיום. **‏`unit_id IS NULL` פירושו נראה-ארגונית** — כל שורה מלפני `0055` ומסמכים שטרם תויקו; הרוכב מתיר אותה לכל משתמש בדייר. השמות והמבנה ניתנים לשינוי פר-ארגון בגל ריבוי-היחידות; עד אז אין UI לעריכתם | `0054_org_scope_foundations.sql` + ‏`0056_scope_backfill.sql` |

## גל 4 — feature flags, זהות ו-step-up (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 85 | **‏step-up — מותנה-flag או בלתי-מותנה** | **בלתי-מותנה.** חוק ה-flags (‏SECURITY-MODEL §8) אוסר קריאת מעריך-flag מתוך RPC — ‏step-up מאחורי flag היה סותר אותו. המנגנון חולץ מילולית מ-`execute_emergency_payment_request` (‏`0031:788-805`) לפונקציה בעלת שם — `assert_recent_password_authentication` — על ארבע תכונותיו (‏typeof-array fail-closed · ‏method='password' בלבד · ‏max(timestamp) · תקרת clock-skew ‏+30s), והוצמד בצד השרת לכל המסלולים: ביצוע תשלום (רגיל + חירום-בהאצלה) · ‏`bank_details` של ספק (‏RPC חדש `update_supplier_bank_details` + הסרת `UPDATE (bank_details)` מה-grant העמודתי של `0036`; ‏INSERT ביצירת ספק נשאר) · ‏`manage_profile_access` · ‏`grant_user_scope`/`revoke_user_scope` · ‏`update_identity_provider_settings` · ‏`mark_month_export_sent`. השגיאה נשמרת בית-בית: ‏`fresh_authentication_required`/`42501`. בצד הלקוח הדפוס הקיים ב-`PayerQueue.tsx:104-114` מחולץ ל-`ReauthModal` משותף עם **דילוג-כשטרי חובה** (‏JWT עם ‏amr ‏password טרי מ-5 דקות ⇒ אין מודאל; השרת מאמת ממילא). ‏`record_document_export` **לא** חווט — "ייצוא פיננסי רגיש" הוא הדוח החודשי לרו"ח; יומן ייצוא המסמכים כבר immutable ומאושר-תהליך | `0061_step_up_paths.sql` + ‏`handoff/04-flags-stepup-contract.md` |
| 86 | **מי משנה feature flags** | **המפעיל (platform), לא ה-owner.** הגדרות גלובליות ב-`private.flag_definitions` (כתיבה out-of-band — ‏SQL console / ‏service_role — כמו חברות `platform_admins`); ‏config פר-ארגון נכתב **רק** דרך `platform_set_org_flag` (‏platform_admins + סיבה + audit לארגון היעד). ‏UI ניהול נדחה כמו #80 — סכימה + RPC בלבד. הרחבה עתידית ל-owner אינה דורשת שינוי סכימה — רק RPC מנומק חדש. ‏`organizations.settings` נפסל כמאחסן flags: כתיב-דפדפן (‏`0036:51`) ללא טריגר audit. מגבלת הבריח מוצהרת: מיקוד-יחידה מאוחסן ונבדק מול יחידות ברירת-המחדל; תרגול ריבוי-אחים אמיתי שייך לגל ריבוי-היחידות | `0059_feature_flags.sql` |
| 87 | **טקסונומיית `security_events`** | **מינימלית ופתוחה:** ‏`step_up_success`/`step_up_failure` · ‏`permission_change` · ‏`scope_grant_change` · ‏`sso_config_change` · ‏`org_lifecycle_change` — רק מה שהמערכת מסוגלת להקליט היום; ‏`event_type` הוא ‏text פתוח כדי שהרחבה לא תדרוש מיגרציה. הטבלה Shape-2 — הדפדפן לעולם אינו קורא אותה, והיא אינה מקבלת `audit_row_change` (היא עצמה יומן). ‏`actor_user_id` ללא FK — רשומה פורנזית שורדת מחיקת פרופיל ומתה רק עם הדייר | `0060_org_identity.sql` |
| 88 | **"רמת אבטחת סשן"** | **נגזרת, לא מאוחסנת:** ‏`session_security_level()` → ‏`'password_fresh'`\|`'standard'` מ-AMR בלבד; אפס state חדש. ‏MFA — ‏out of scope מוצהר: אין MFA/TOTP במערכת (‏SECURITY-MODEL §6) ואין `[auth.mfa]` בקונפיג; הוספתו היא החלטת תוכנית-Supabase נפרדת, לא גל זה | `0060_org_identity.sql` |

## גל 4b — מזהי קורלציה (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 89 | **גרעיניות מזהה הקורלציה — פר-בקשה או פר-פעולת-משתמש** | **מזהה פר-בקשה + ‏helper לקיבוץ מפורש, כברירת המחדל המתועדת.** עטיפת ה-`fetch` של `src/lib/supabase.ts` מטביעה `x-correlation-id` = ‏`crypto.randomUUID()` על כל בקשת `/rest/v1` (ורק עליהן — auth/storage/functions לא, בגלל פער ה-preflight מקומי-ענן); פעולה מרובת-בקשות מקבצת במפורש דרך `withCorrelationId(id, fn)`. "פר פעולת משתמש" מלא — עטיפה אוטומטית של כל handler — **נדחה עד שקיים צרכן** שמפיק ממנו ערך (מסך חקירה, קישור Sentry); כשיגיע, ‏`withCorrelationId` הוא כבר הזרוע והמעבר אינו שובר סכימה. בצד השרת המזהה נרשם ל-`audit_logs.correlation_id` דרך DEFAULT עמודתי (`public.request_correlation_id()`, ‏fail-to-NULL) — אפס שינויי חתימות. ‏`traceparent` של supabase-js ‏2.110 רשום כהתנגשות-עתידית ב-INTEGRATION-ARCHITECTURE §5 | `0062_correlation_ids.sql` + ‏`src/lib/supabase.ts` |

## גל 5 — אירועי דומיין ו-outbox (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 90 | **סמנטיקת המסירה של ה-outbox ומספריה** | **‏at-least-once, עם מעקות שהומצאו ותועדו — לא כללים עסקיים:** ‏backoff מעריכי בסיס 1 דקה · מכפיל 4 · תקרה 6 שעות · מקסימום **8 ניסיונות** ⇒ ‏dead-letter. רשומת dead-letter נושאת `failure_reason` ‏NOT NULL ושורדת replay (נחתמת, לא נמחקת). ‏**replay רק בפקודה מנומקת** — `replay_dead_letter_record(id, reason)` (‏service_role, ‏audit עם סיבה) — החזרה אוטומטית ללולאה נפסלה: מיצוי 8 ניסיונות הוא אות שדורש הכרעת אדם. שינוי המספרים = עדכון `fail_integration_outbox_delivery` + השורה הזו יחד | `0064_integration_outbox.sql` |
| 91 | **מהו `idempotency_keys` של האינטגרציות** | **מפתח de-dup יוצא פר ‏(target, event):** דטרמיניסטי (`sf:<event_id>:<target>`), נרשם ב-claim הראשון ונשלח בכותרת `x-idempotency-key` על **כל** ניסיון, כדי שהצד המקבל יוכל לבלוע מסירה כפולה של at-least-once. זה **אינו** מפתח האידמפוטנטיות של פקודות P1 (UUID מהלקוח) — שני מנגנונים נפרדים לשתי שאלות | `0064_integration_outbox.sql` |
| 92 | **גרסאות סכימת אירועים** | ‏`event_type` הוא **text פתוח** (תקדים #87 — הרחבה אינה דורשת מיגרציה); ‏`schema_version` הוא **שלם פר-סוג** שמתחיל ב-1 ועולה כששוברים את חוזה ה-payload של אותו סוג. חוזה ה-payload חי בשורת המפה (`payload_keys`) — שינויו מחייב העלאת גרסה באותה שורה | `0063_domain_events.sql` |
| 93 | **אילו טבלאות אינטגרציה נדחו לגל 7** | ‏`external_references` · ‏`webhook_subscriptions` · ‏`integration_failures` — **במפורש גל 7** (המתאמים). עד אז אין רישום יעדים ⇒ ‏**אפס auto-enqueue** ⇒ ‏outbox ריק בשער **בעיצוב**; זרוע ההרשמה של גל 7 מתועדת ב-`handoff/05-events-contract.md` §6 (‏`private.enqueue_integration_outbox` היא התפר) | `0064_integration_outbox.sql` |
| 94 | **מיפוי שמות הקטלוג אל פעולות ה-audit החיות** | שמות הקטלוג (INTEGRATION-ARCHITECTURE §2) נוסחו לפני שנמדד אוצר ה-actions החי; שתי הכרעות מיפוי בלי מקבילה מדויקת: ‏**`invoice.review_required` = מעבר ל-`review_status='investigation'`** (המצב שאליו `create_invoice` דוגל חשד לכפילות — "דורש בדיקה" בפועל; ‏`in_review` פירושו שמישהו כבר בודק), ‏**`purchase_order.approved` = מעבר ל-`status='ready'`** (‏po_status חי: ‏draft→ready→sent→confirmed — ‏ready הוא שער האישור של `0053`; אין סטטוס `approved`). ‏`payment.executed` נפלט משני הליטרלים (רגיל + חירום); ‏`invoice.created` משני ליטרלי היצירה (רגיל + עקיפת-כפילות). שינוי מיפוי = עריכת שורת מפה, לא קוד | `private.domain_event_map` ‏(`0063`) |

## גל 6b — חידוש הזמנות העלאה (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 95 | **סמנטיקת ה-renew: ‏audit, הזמנה רשומה, הזמנה שפגה** | ‏`renew_supplier_price_document_upload` **אינו נרשם ב-audit** — התקדים הוא reserve עצמו (‏0048), ששניהם הקצאת-זמן טכנית ולא פעולה עסקית; הפעולה העסקית (הרישום) ממשיכה להירשם דרך `enqueue_document_processing`. ‏**הזמנה רשומה ⇒ דחייה בשם** (`document_upload_reservation_registered`), לא הצלחה אידמפוטנטית — הצלחה הייתה אומרת ללקוח "המשך להעלות", וחוק הכסף אוסר להזמין העלאה-מחדש של קובץ שכבר נשמר ונרשם. ‏**הזמנה שפגה אך בתוך תקרת החיים ניתנת ל-renew** (ההחייאה) — זה בדיוק מרוץ-החידוש שחלון-החסד קיים בשבילו; שערי ה-Storage וה-register נשארים סגורים מרגע התפוגה ועד ה-renew | `0065_upload_reservation_renewal.sql` + ‏`handoff/6b-upload-contract.md` |
| 96 | **מספרי הגל — מעקות שהומצאו ותועדו, לא כללים עסקיים** | ‏**תקרת חיים 45 דקות** מ-`created_at` (‏`least(now()+15min, created_at+45min)` בכל renew) — מתחת לתפוגת ה-tus בענן ~1h, כדי שהזמנה לעולם לא תבטיח חלון שה-storage כבר לא יכבד; ‏**חלון-חסד שעה ל-sweep** (‏`expires_at <= now() - interval '1 hour'`) — מחיקה פיזית בלבד נדחית, כדי ש-renew שהפסיד מרוץ ל-reserve של דייר אחר לא יאבד bytes שכבר הועברו. שינוי המספרים = עדכון `0065` (‏renew + ‏sweep), ‏`p6b_upload_reservations.sql`, זרוע ה-preflight ‏#39 והחוזה — יחד | `0065_upload_reservation_renewal.sql` |

## גל 7 — מתאמי אינטגרציה (05.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 97 | **חתימת webhook — מי חותם והיכן חי הסוד** | **ה-DB חותם (Design 2), וה-HMAC הוא מעקה שהומצא ותועד — לא כלל עסקי.** ‏`claim_integration_outbox` הוצהר מחדש ב-`0066` **מההגדרה החיה של `0064`** (בלוק ancestry מפיל reset על גוף שסטה או שכבר הורחב), ומחזיר לכל שורה נתבעת גם: ‏`body` — סריאליזציית `jsonb::text` דטרמיניסטית של מעטפת האירוע; ‏`timestamp` — שניות epoch; ‏`signature` — ‏hex של ‏HMAC-SHA256 על **`body \|\| '.' \|\| timestamp`** עם סוד ה-Vault של המנוי. הסוד לעולם אינו עוזב את Postgres: ה-worker מעתיק לכותרות `x-supplyflow-signature: sha256=<hex>` + ‏`x-supplyflow-timestamp` ושולח את `body` **מילולית** — החתימה מכסה בדיוק את הבייטים שנשלחו. וקטור known-answer מוצמד בשני הצדדים (‏`p7_integration_adapters.sql` מול `extensions.hmac`; ‏`outbox-worker/core.test.ts` מול WebCrypto). יעד לא-רשום / מנוי כבוי / סוד שאינו נפתר ⇒ שורה **בלי** ‏url/חתימה ⇒ מסלול `target_unregistered` של גל 5 נשמר. שינוי פורמט המחרוזת החתומה = עדכון `0066` + ‏`core.ts` + החוזה + שני הווקטורים **יחד** | `0066_integration_adapters.sql` + ‏`handoff/07-adapters-contract.md` |
| 98 | **מסך האינטגרציות** | **נדחה** — תקדים ‏#86/#80 (סכימה + RPCs לפני UI), ו-`INTEGRATION-ARCHITECTURE.md:96` ממילא אוסר מסכי-ספק עד שספק ממומש בפועל; כל שדות המסך (ספק, סנכרון מוצלח אחרון, רשומות ממתינות/כושלות) היו ריקים היום. מה שכן קיים: ‏`read_webhook_subscriptions()` ו-`read_integration_failures()` (בעלים בלבד; לעולם בלי `secret_id` ובלי `raw_error`) — המשטח שהמסך העתידי יקרא בלי שינוי סכימה. הפעלת מנוי היא כבר היום פקודת בעלים מנומקת עם **step-up** (‏`set_webhook_subscription_active`; ‏#85 חל — הזרמת אירועי העסק ל-URL חיצוני היא אותה מחלקת exfiltration כמו `bank_details`) + ‏security_event | `0066` |
| 99 | **הגבול בין `integration_failures` ל-`integration_deliveries`** | ‏**deliveries = תוצאת ה-transport של שורת outbox** — שורה אחת לכל ניסיון מסירה, נכתבת אך ורק ב-RPCs של ה-worker (‏private, ‏0064). ‏**failures = כשלים במישור המתאם/הנכנס שאין להם שורת outbox** — חתימה נכנסת שנדחתה, קונפליקט מיפוי, שגיאת ספק — נרשמים דרך `private.record_integration_failure` (definer נקי-מילים, מדלג בשקט בלי דייר), בלי UI. ‏`raw_error` הוא טקסט ספק גולמי — אותה מחלקת scrubbing כמו Sentry (‏`observability.ts` מוחק breadcrumbs כי מחרוזת שרת יכולה לנקוב בספק או בסכום): הדפדפן לעולם אינו קורא את הטבלה (Shape-2), והקורא המוגן מחזיר **קודים וספירות בלבד** | `0066` |
## גל 8 — קליטה offline וברקוד (06.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 100 | **זמן התצפית של קבלת סחורה — עמודה חדשה או שדה מנומק** | **נכנס ל-`p_reason` המערכתי, לא לחתימה.** הפער אמיתי: קבלה שהוזנה במכשיר ב-08:14 ונשלחה ב-14:02 נרשמת כ-`received_at = now()` (‏`0023:1645`) — כלומר 14:02. זו אותה מחלקת אי-אמת כמו יתרה ממטמון. **אבל** פרמטר חדש ל-`save_goods_receipt` דורש ‏`drop function`+recreate על פקודה פיננסית בת 300 שורות, עדכון שישה אתרי קריאה, והכרעה אם הוא נכנס להשוואת האידמפוטנטיות של ‏`0023:1472-1503` (אם כן — ניסיון חוזר עם שעון אחר הופך לקונפליקט מדומה). לכן: **אין שינוי חתימה.** הזמן שנצפה במכשיר נכתב ל-`audit_logs.reason` **באותה עסקה** של הפקודה, כמחרוזת מובנית — `השלמת קבלת סחורה · נרשמה במכשיר 08:14 · סונכרנה 14:02` (`receiptAuditReason`, ‏`offlineQueue.ts`). **הסעיף מופיע רק כששני השעונים נבדלים:** שליחה מקוונת מיידית היא תצפית וסנכרון באותו מעשה, ולכן הנימוק שלה נשאר מחרוזת הבסיס בדיוק (וזו גם הטענה שהשער הקיים קורא, ‏`check-browser-smoke.cjs:810,815`). **מסלול השדרוג, אם דיווח יצטרך לשאול "מתי האדם ראה את הסחורה" בשאילתה ולא בטקסט:** עמודה `observed_at timestamptz` על `goods_receipts` + פרמטר `p_observed_at`, שמוחרג במפורש מהשוואת האידמפוטנטיות (אחרת retry אחרי שעה = קונפליקט), + ‏backfill מה-reason. אין צורך לשנות את התור — הוא כבר מחזיק את הזמן פר-פעולה | `src/lib/offlineQueue.ts` (‏`receiptAuditReason`) + ‏`src/pages/Receiving.tsx` |
| 101 | **‏precache של קונכיית האפליקציה — עכשיו או בגל נפרד** | **נדחה במפורש לגל נפרד.** התור — הערך האמיתי של הגל — **אינו זקוק ל-service worker**: הוא JS + IndexedDB. ‏precache לעומת זאת מחייב לשכתב את `public/sw.js` שכותרתו מצוטטת מילולית בחמישה מקומות, מפיל את חוזה ה-`controllerchange` (‏`main.tsx:19-28`) שעליו נשען תרחיש `pwaUpdate` ("הטאב לא נמחק"), ומחייב `vite-plugin-pwa`/workbox עם `injectManifest` כדי לא לאבד את מטפלי ה-push (ארבעה תרחישי שער). **המגבלה נאמרת בקול ואינה מוסתרת: טעינה-מחדש במצב לא-מקוון תאבד את קונכיית האפליקציה — הדף לא יעלה. התור ב-IndexedDB שורד סגירת טאב וקריסה, ונשלח בפתיחה הבאה.** זה מכסה את התרחיש האמיתי (הטאב נפתח במשרד, הסיגנל נעלם בדרך למחסן), ואינו משקר על אפס. תרחיש השער נכתב בהתאם: הוא **סוגר את הטאב** במקום לנסות reload לא-מקוון. ‏`workbox-*` נשאר ב"תוספות מתוכננות" ב-`THIRD_PARTY_NOTICES` ולא הותקן · **נסגר 09.08.2026 (חבילה 5): ‏precache של הקונכייה בלבד** — ‏`vite-plugin-pwa` ב-`injectManifest` על `public/sw.js` הקיים (בלי runtime של workbox — רק הזרקת המניפסט בזמן build; לוגיקת ה-cache כתובה ביד, ~50 שורות); ‏API/Auth/Storage/Functions לעולם לא נכנסים ל-cache (`isApiRequest`); ‏push ו-`controllerchange` נשמרו כלשונם; תרחיש שער חדש עושה reload לא-מקוון אמיתי ומוודא שהקונכייה עולה | `vite.config.ts` + `public/sw.js` + `handoff/08-offline-contract.md` §5 |
| 102 | **מה ברקוד עושה בקבלת סחורה** | **מפתח בלבד — מזהה שורה, לעולם לא מנחש כמות.** לא הייתה החלטה כתובה על ברקוד באף מסמך. נקבע: הסורק מזין את שרשרת ההתאמה הקיימת (`matchDeliveryLineProduct`, ‏`model.ts:343-370`) עם שורת תעודה **סינתטית** שנושאת רק `barcode`, כדי ששני המסלולים יחלקו חוק אחד — **וחוק אי-הבהירות שלה נשמר: שני מוצרים שעונים לאותו קוד אינם התאמה.** ‏`ambiguous` מדווח בנפרד מ-`none` כי אלה שתי עובדות שונות על הקטלוג, ובשניהם **אין בחירה מוקדמת** ויש נפילה-אחורה להזנה ידנית. הקוד תמיד **נאמר בשמו** במסך. ‏`flag receiving.barcode` כבוי (ברירת המחדל, ‏`0059:39`) ⇒ **אין סורק כלל** — לא כפתור מושבת ולא דיאלוג ריק — כולל "עוד לא נטען" ו"הרזולבר נכשל" (‏fail-closed, ‏`flags.ts:33-34`). דחיית הרשאת מצלמה = הסבר עברי + הזנה ידנית. **האינדקס ב-`0067` אינו ייחודי בכוונה** — ייחודיות הייתה אוסרת את המצב שחוק אי-הבהירות קיים בשבילו; ה-demo seed נושא צמד מוצרים שחולק ברקוד אחד על אותה הזמנה שניתנת לקבלה, כדי שהמסלול יהיה ניתן להוכחה | `0067_barcode_lookup_index.sql` + ‏`src/components/BarcodeScanner.tsx` |

## גל 9 — העדפות התראה, שער סוגי-החיפוש ומדיניות אישור (06.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 103 | **מה נמסר בגל 9 ומה נדחה — שלוש שכבות, ולמה `0071`–`0072` נשארות ריקות** | **מספר מיגרציה אינו מכסה שיש למלא.** ההקצאה נתנה לגל 9 חמש מיגרציות; נוצלו **שלוש** (`0068`–`0070`), ו-`0071`–`0072` נשארות **מוקצות ובלתי-מנוצלות**. שלוש השכבות: **(א) עובד מקצה-לקצה** — העדפות התראה (`0068`: הצרכן חי ב-`send-push`, המסך חי ב-`PushSettings`, נקודת ההזרקה היא שורה אחת — `0024:96-102`) ושער סוגי-החיפוש (`0069`: `GlobalSearch.tsx:28-34` היה השער היחיד בפני `payer`/`supplier`, וה-grant הוא ל-`authenticated` כולו — תיקון הרשאה אמיתי). **(ב) סכימה + מעריך, אפס צרכנים, מוצהר** — מדיניות אישור (`0070`, תקדים גל 7: ממשקים+mocks ללא יבואנים). **(ג) נדחה במפורש:** ‏**מנוע workflow** — שש מכונות המצב עובדות ומבוקרות, ו-`INTEGRATION-ARCHITECTURE.md:164-172` מבטיח *מתאם Temporal עתידי*, לא מנוע; הפער האמיתי היה מטריצה משוכפלת בדפדפן ונסגר ב-`read_allowed_transitions()` (‏#105). ‏**מנוע חוקים** — אותה שורה בטבלה, ואותו נימוק. ‏**report jobs** — המפרט הוא **מילה אחת** (`ENTERPRISE-FOUNDATION-PLAN.md`, שורת גל 9 לפני התיקון) ואפס מקומות נוספים; שלושה משטחי דיווח נכונים כבר קיימים, והבנייה הייתה דורשת Edge Function חדשה, ‏cron רביעי, ‏XLSX בשרת וצעד Deno — המשטח המומצא הגדול ביותר בקמפיין, למפרט הרזה ביותר. **חוב אגרגציית הדשבורד** — נמדד (34 קריאות `fetchAll`, ~20–25 RPCs, וארבע החלטות עסקיות שיוקפאו) ולכן **גל בפני עצמו**; נמסרת פרוסת-הוכחה אחת. **משנים** ע"י תוכנית שמנמקת צרכן אמיתי לכל תחום שנדחה — לא ע"י מילוי מספר מיגרציה פנוי | `PLAN-10 §1` + ‏`docs/INTEGRATION-ARCHITECTURE.md:164-172` |
| 104 | **האם תצורת מדיניות אישור פר-ארגון יכולה להקל, או רק להצר** | **להצר בלבד — מעולם לא להקל.** לכל `policy_key` יש **בסיס גלובלי** ב-`private.approval_policy_definitions` (‏`baseline_required_approvals`, ‏`baseline_step_up_required`), ותצורת הארגון ב-`public.approval_policy_configurations` חייבת להיות **מחמירה או שווה**: פחות מאשרים מהבסיס, או ביטול step-up שהבסיס דורש, נדחים בשם — `approval_policy_not_tightening`/`22023` — בתוך פקודת הכתיבה היחידה (`platform_set_approval_policy`, ‏platform_admins + סיבה + audit, תקדים `platform_set_org_flag` ו-#86). תצורה שיכולה להקל היא **הענקת הרשאה בתחפושת של קונפיגורציה**; זהו בדיוק ההיגיון שנתן ל-kill switch של ה-flags סמנטיקת "כיבוי בלבד" בלי מקבילה של "הדלקה בכוח" (`0059:29-32`). הבסיסים המושתלים הם **ההתנהגות הקיימת** (מאשר אחד, בלי step-up), כך שהסכימה אינה יכולה לשנות התנהגות בכך שהיא קיימת. ‏**אין FK** מהתצורה להגדרה בכוונה (תקדים `0059:66-68`) — יתום הוא אנומליית preflight (`approval_policy_config_without_definition`), לא cascade שבור. **‏`evaluate_approval_policy()` אינה נקראת מאף מקום בגל הזה**, ואסור שתיקרא מ-RLS או מבדיקת-תפקיד (`ENTERPRISE-SECURITY-MODEL.md:246-251`) — חיווטה לפקודה פיננסית היה מכריע מחדש את **#2** (מי מאשר כסף), שאלה שנשארת פתוחה. שתי טענות מבניות ב-`p9_five_domains.sql` נכשלות ראשונות אם מישהו יחווט אותה | `0070_approval_policies_and_transitions.sql` |
| 105 | **מי מחזיק את מטריצת המעברים — השרת או הדפדפן** | **השרת, ורק השרת.** ארבע מטריצות המצב חיות בפקודות (`set_invoice_review_status` ‏`0023:1926-1931`, ‏`transition_payment_request` ‏`0031:877-883`, ‏`transition_credit_request` ‏`0024:327-331`, ‏`transition_purchase_order_status` ‏`0041:75-81`), והדפדפן החזיק **עותק** של מטריצת החשבונית (`InvoiceDetail.tsx:142-147`) שסטה מהמקור. הפתרון הוא `read_allowed_transitions(entity_type, current_status)` — ‏invoker, מחזירה את הגרף **כדאטה** (אילו סטטוסים הפקודה תקבל מכאן), ובמפורש **אינה** אומרת אם *הקורא* מורשה: הרשאת התפקיד נשארת בפקודה, שם היא כבר מבוקרת. הקבוצה **בלתי-מסודרת בחוזה** — הסדר הוא של הלקוח (`src/lib/status.ts`). ‏**שינוי גלוי אחד, מכוון:** ‏`approved → investigation` **נפתח בממשק**, כי השרת מתיר אותו מאז `0023` (זרוע ה-wildcard: כל סטטוס שאינו `investigation` יכול לעבור *אל* `investigation`) — וזו ההתנהגות הנכונה: גילוי בעיה בחשבונית שאושרה הוא בדיוק הרגע שבו צריך חקירה. הדפדפן פשוט הסתיר מעבר חוקי, מנומק ומבוקר. ‏**שתי הערות כנות שנוסעות עם החוזה:** ‏`payment_request → approved` עובר את הגרף ועדיין עלול להיכשל בתנאי-הקדם של הפקודה (`0031:887-917`); ‏**ביטול הזמנה אינו בגרף** — הוא פקודה נפרדת (`cancel_purchase_order`). המראה מוכחת בשער: ‏`p9_five_domains.sql` **מריץ את ארבע הפקודות האמיתיות על כל זוג סטטוסים מסודר** ומשווה, כך שעריכת מטריצה שתשכח את הקורא נכשלת בשער ולא סוטה בשקט | `0070_approval_policies_and_transitions.sql` + ‏`supabase/tests/p9_five_domains.sql` |

## גל 10 — תיקון הביקורת הסופית (06.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 106 | **האם יצירת ספק חדש עם פרטי בנק היא פעולה רגישה שדורשת סיבה ו-step-up** | **הוכרעה 09.08.2026 (בעלים): כן — אפשרות (ב), ומומשה חזק מהניסוח שלה.** ‏`0088` ביטל גם את grant ה-**INSERT** על `suppliers.bank_details` (המקבילה המדויקת של מה ש-`0061` עשה ל-UPDATE), ולכן האכיפה **בסכימה, לא בלקוח**: אין שום INSERT מהדפדפן שנושא פרטי בנק. יצירת ספק כותבת שורה בלי פרטי בנק, וערך לא-ריק עובר מיד את אותו מסלול מנומק שהשינוי עובר — ‏`update_supplier_bank_details` (‏owner/office + ‏step-up סיסמה טרייה + סיבה חובה + ‏audit). ‏`QuickCreateSupplier` נשאר בלי `bank_details` (הטענה המבנית בספק שלו נשארה). **הוכחות בשער:** ‏`p0_client_dml_acl.sql` (הפריווילגיה נעדרת בשני הכיוונים) · ‏`p1_financial_commands.sql` (‏INSERT עם פרטי בנק נדחה פונקציונלית; יצירה בלי — עוברת) · ‏`supplierBankDetails.spec.tsx` (מסלול היצירה בלקוח: ‏INSERT נקי + ‏RPC עם ה-id החדש והסיבה) | ‏`0088_supplier_bank_details_insert_stepup.sql` + ‏`0061_step_up_paths.sql` + ‏`src/pages/Suppliers.tsx` |

## שילוב QA פיננסי מול סקופ Enterprise (הוכרע 06.08.2026)

החלטות #107–#108 אושרו ב-06.08.2026 ומומשו סטטית ב-`0074_monthly_report_snapshots.sql`
וב-`0073_payment_credit_override.sql` בהתאמה. הן אינן פתוחות עוד; אימות runtime טרם בוצע.
(במקור נרשמו בענף האינטגרציה כ-#106–#107; מוספרו מחדש במיזוג כי גל 10 תפס את #106.)

החלטת המשך לבנק: שיוך `bank_imports` ו-`bank_transactions` לישות משפטית הוא שינוי נפרד. עד שיושם, `0073` אינה מריצה שאילתת בנק ארגונית ואינה מחזירה bit שעלול לחשוף פעילות של ישות אחות; היא מחזירה `similar_bank_transfer_check = unavailable` ומציגה אזהרה כללית שאינה חוסמת ואינה מאשרת את הבקשה.

`0074` משאירה את `monthly_exports` ההיסטורית ללא שינוי ומוסיפה `monthly_report_snapshot_deliveries` כ-ledger המסירה הסמכותי לגרסה נעולה. יצירת snapshot נכשלת סגור אם תנועת בנק בחודש אינה מיוחסת באמצעות allocation מאושר לישות אחת.

החלטת ארכיטקטורה שנותרה: `audit_logs` מסווגת `cross_scope` וארגונית. כתוצאה מכך משתמש audit מורשה שמוגבל לישות אחת עשוי לראות metadata של פעולת snapshot בישות אחות. אין כאן דליפה בין ארגונים; שינוי audit לישות משפטית דורש תכנון רוחבי ואינו נפתר בתוך `0074`.

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 107 | **מהו הסקופ של snapshot חודשי סופי לרו״ח** | **ישות משפטית אחת בכל snapshot.** הארגון נשאר גבול הדייר וה-metadata, אך דיווח חשבונאי, חשבוניות, תשלומים, בנק ו-`monthly_exports` שייכים לישות משפטית לפי מודל האבטחה. `0074` תקבל `unit_id` מסוג `legal_entity`, ‏FK מרוכב עם `org_id`, רוכב A3 וגרסאות ייחודיות לפי `(org_id, unit_id, month, version)`. Owner או accountant רשאים ליצור ולקרוא רק ישות שנמצאת ב-`auth_scopes()` שלהם. אין `profiles.active_unit_id`; הבחירה מפורשת במסך/URL. | `docs/ENTERPRISE-SECURITY-MODEL.md` §3.5; ‏`0074_monthly_report_snapshots.sql` העתידית |
| 108 | **כיצד מטפלים בזיכוי פתוח שאין לו ישות משפטית מוכחת** | **לא מנחשים ולא מחשבים אותו ארגונית.** לפני הפעלת `0073`, זיכוי חשבונית יורש את `invoices.unit_id`; זיכוי קבלה נגזר דרך מחסן→ישות משפטית; רשומת legacy שלא ניתן לייחס דטרמיניסטית חוסמת את שער המיגרציה/ה-preflight ודורשת סיווג ידני מבוקר. לאחר היישור, ה-override המאושר נשאר כמתוכנן: אישור מפורש עם סיבה ואudit, ללא קיזוז או סגירת הזיכוי וללא שינוי סכום הדרישה. | `0073_payment_credit_override.sql` העתידית; ‏`p1_preflight.sql`; ‏`docs/ENTERPRISE-SECURITY-MODEL.md` §3.5 |

## אוטונומיית המסמכים — המתג והמספר (הוכרע ע״י הבעלים 06.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 109 | **מה מותר למערכת לעשות בלי אישור אדם כשהמודל מזהה חשבונית — ומהו הסף** | **הבעלים בחר באפשרות המתירנית מבין השלוש: אוטומציה מלאה מעל סף ביטחון** — המערכת יוצרת את החשבונית ומקשרת אותה להזמנה. **הבקשה כללה גם "אם חסר פריט הוא מעדכן", וזה לא מומש — ראה (ד).** הסיכון (מודל שכותב רשומות כספיות) הוצג לפני הבחירה ואושרר אחריה. **ארבע** ההשלכות שאינן סגנוניות: **(א) ‏0.90 הוא ניחוש מתועד, לא מספר מכויל. הסף לא נמדד מול קורפוס אמיתי — איש לא השווה את ההחלטה האוטומטית להכרעת אדם על גוף מסמכים אמיתי. הצעד הזול לכיול: 50 מסמכים אמיתיים, והשוואת ההחלטה האוטומטית להכרעת האדם.** עד שזה ירוץ, המספר נשאר ניחוש והקוד אומר זאת (`0076` בכותרת, ובתגובת `evaluate_autonomy_policy`). ‏**(ב) ברירת המחדל היא `false` לכל דייר, וזה חלק מהתכולה ולא זהירות** — אפשרות ב' מעניקה סמכות; היא אינה אומרת "לכל דייר קיים, מהיום, בלי שנאמר לו". דייר נדלק **בכוונה**, ע״י מפעיל, עם סיבה, ב-audit. שום מיגרציה אינה מדליקה אותו בשום מקום, וזו **הטענה הראשונה** ב-`p13_document_autonomy_config.sql`, לפני שנוצר ולו fixture אחד. ‏**(ג) זה אינו feature flag, ואסור שיהיה** — ‏SECURITY-MODEL §8: "flag יכול רק לכבות יכולת, לעולם לא להרחיב הרשאה… כך flag שנדלק בטעות אינו יכול לפתוח דלת". מתג שמצבו הדלוק מרשה למודל לכתוב חשבונית **הוא** בדיוק מתג שפותח דלת כשנדלק בטעות (אותו היגיון של #104: קונפיגורציה שיכולה להקל היא הענקת הרשאה בתחפושת). בנוסף: ‏`org_flag_configurations.state` הוא boolean ו-`targeting` הוא rollout בלבד ש**מתעלם ממפתחות לא מוכרים** — סף שהיה מוחבא שם היה נשמר, לא מאומת ולא נקרא; ו-`p4_flags_identity.sql:162-167` מצמידה שאף רוטינה אחרת אינה מזכירה את `resolve_feature_flags`, כך ש-C2 שתקרא את המתג דרך מעריך ה-flags **מפילה טענת שער קיימת**. לכן `0076` בנוי על תקדים `0070` (מדיניות אישור): מדיניות פר-ארגון, בסיס ב-`private`, פקודת מפעיל מנומקת אחת (`platform_set_autonomy_policy` — ‏platform_admins + סיבה + audit לארגון היעד, חוזה `platform_set_org_flag` מילה במילה), ומעריך. ‏**סף אחד, לא שניים** — אותו מספר חל על `document_type_confidence` **וגם** על `supplier.confidence`; שני מספרים בלתי-תלויים היו ניחוש שני בלתי-מכויל, וההחלטה בפועל היא ה-`min` שלהם. **מה שנאסר מבנית — בשתי שכבות, אחרי שהסקירה הראתה ששכבה אחת אינה מספיקה:** ‏`min_confidence` הוא `NOT NULL` עם `CHECK (> 0 and <= 1)` בשם, ולכן **שורה** של "אוטונומיה דלוקה בלי סף" אינה ניתנת לייצוג — אותו idiom של `document_filings_reversal_shape` (#110). **אבל הצרכן קורא תשובה, לא שורה**, ו"דלוק בלי סף" היה עדיין ניתן לייצוג ב**תשובה** דרך הענף הלא-מוגדר (‏`baseline_enabled` דלוק + אין שורת דייר ⇒ ‏`enabled=true, min=NULL`). זה **אינו** נשאר בטוח במורד הזרם: ‏`confidence >= null` נכשל-סגור ב-SQL, אבל אותה השוואה ב-TypeScript אחרי PostgREST היא `0.42 >= null` ⇒ ‏**true** (‏null נכפה ל-0) — כלומר "החל על הכול", בדיוק מה שהסף קיים כדי למנוע. לכן נוספו **שתי** גדרות: ‏`constraint autonomy_definitions_baseline_off check (baseline_enabled = false)` (‏"אין הדלקה בכוח" הופך מהבטחה בהערה לאילוץ), ו-`and c.min_confidence is not null` בתוך המעריך עצמו, כך שהאינווריאנטה נאכפת **במקום שבו היא נצרכת**; וחוק ההחמרה-בלבד (#104 על מספר): דייר רשאי לדרוש **יותר** ביטחון מהרצפה המתועדת ולעולם לא פחות, כי סף שאפשר להוריד היה מאפשר לגלגל ניחוש לא-מאומת כלפי מטה, קריאה מנומקת אחת בכל פעם. **הורדת הרצפה עצמה** = עריכת `private.autonomy_policy_definitions` מחוץ למסלול (כמו חברות `platform_admins` או `kill switch`), ו**אין לעשות זאת לפני שהכיול בסעיף (א) רץ בפועל**. ‏**(ד) "אם חסר פריט הוא מעדכן" לא מומש, וזו סטייה מוצהרת מהבקשה — בשתי שכבות, לא באחת.** ‏**(ד1) ההזמנה אינה נכתבת:** ‏`purchase_order_items.unit_price` הוא snapshot מחירים ברגע ההזמנה (`CLAUDE.md`) ו-`price_history` נגזר ממנו, ולכן הוספת שורה בדיעבד אינה "עדכון" אלא **שכתוב היסטוריה** שגוזר מחדש ניתוחי מחיר וחיסכון שכבר הוצגו למנהל. ‏`apply_document_interpretation` **אינה נוגעת** ב-`purchase_orders` ולא ב-`purchase_order_items`. ‏**(ד2) והפריט גם אינו "נכנס לחשבונית" — התוכנית הבטיחה כאן דבר שאינו ניתן למימוש:** אין בסכימה טבלת שורות-חשבונית כלל (אין `invoice_items`/`invoice_lines`), ולחשבונית יש שדות כותרת בלבד. **התוכנית טעתה, לא המימוש.** מה שקורה בפועל: נפתח **חריג** (`receipt_mismatch`) שכותרתו "התקבל פריט שלא הוזמן — חשבונית N", והפריטים עצמם — ‏`sku`, ‏`barcode`, ‏`description`, ‏`source_row` — יושבים ב-`details.items` יחד עם `order_id`, ‏`order_number` ו-`interpretation_id`. **החריג הוא הייצוג היחיד של הפריט החסר במערכת.** ההתאמה שמזינה אותו שמרנית במכוון: מק"ט או ברקוד בלבד, לעולם לא תיאור חופשי, ושורה בלי מזהה **מדולגת** — התראת שווא מאמנת את המנהל להתעלם. **אם משה רוצה גם את שכתוב ההזמנה** זהו דגל נוסף (`document_autonomy_mutates_orders`) ומיגרציה אחת, **ואין לממש בלי אמירה מפורשת שלו.** תווית החריג המקורבת רשומה כחוב ב-`DEBT-REGISTER` §17 | `0076_document_autonomy_config.sql` · ‏`0077_apply_document_interpretation.sql` (בלוק ה-`insert into public.exceptions`) · ‏`supabase/tests/p13_document_autonomy_config.sql` · ‏`supabase/tests/p14_apply_interpretation.sql` סעיף (l) · ‏`docs/DEBT-REGISTER.md` §16, §17 · **עדכון 09.08.2026 (חבילה 3): הסף ‏0.900 נמדד לראשונה — ריצות מודל אמיתיות על קורפוס אינטרנט בהכרעת בעלים — ואושרר: בתוך הרצועה הבטוחה הנמדדת ‏[0.85, 0.95]; ‏FP יחיד נחסם ברצפה והיה נכנס ב-0.80. ‏`docs/calibration/2026-08-09-internet-corpus.md`. מדידת 50 מסמכי הדייר החי נשארת פתוחה** |

## ארכיון המסמכים — חילוץ ומחיקה (הוכרע ע״י הבעלים 06.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 110 | **האם אדם רשאי להוציא מסמך מהארכיון, ולמחוק ממנו** | **חילוץ — כן, עם סיבה. מחיקה — כן, בלי סיבה.** הכרעת בעלים מפורשת. שתי ההשלכות מבניות ולא סגנוניות: ‏**(א) לחילוץ פקודה משלו** — `rescue_document_from_archive(uuid, text)`, ‏`SECURITY DEFINER`, ‏owner/office, ‏`reason_required` על ריק/רווחים, ומחזירה את המסמך ל-`inbox` (היעד היחיד הקוהרנטי: משם שתי פעולות השיוך הקיימות עובדות ו-`isUnfiled` צודק לגביו). **לא** הוספת שדה סיבה ל-`RefileModal` ולא הסרת `archive ||` משתי פעולות השיוך — זה היה מפנה את הארכיון חזרה אל `file_document`, שהשומר שלה מסרב מבנית לכל שורה שאינה ב-inbox (`0019:167`), והיה הופך את "האם נדרשת סיבה?" לתלוי ב-prop במקום בפעולה שהמשתמש בחר. **הדרישה לסיבה מבנית, לא מותנית**, והיא מגודרת פעמיים: בפקודה (`reason_required`) ובסכימה (`document_filings_reversal_shape` — `reverted_at` בלי סיבה אינו ניתן לייצוג). ‏**(ב) למחיקה אין מנגנון DB חדש** — זו המחיקה הרכה של החלטה #28 (`deleted_at`/`deleted_by`, הקובץ נשמר לביקורת), אותה הצהרה ש-`AttachmentsPanel.tsx:142-144` מריץ. **לא הומצאה מחיקה קשה.** ‏`ConfirmDialog` תומך בשני המסלולים כבר היום (`requireReason` אופציונלי) — נבדק ולא הונח | `0075_document_filing_and_archive.sql` · ‏`supabase/tests/p11_document_filing.sql` · ‏`src/pages/DocumentsInbox.tsx` |
| 111 | **מה מוצג בעמודת "תיוק" ובמסנן שלה במסך הארכיון** | **לא מוצגים כלל.** ‏`isUnfiled()` הוא `entity_type='inbox' or entity_id is null`, ולכן כל שורת ארכיון נקראת "לא משויך" — טענה שגויה על מסמך שהוכרע לגביו שאין לו יעד. **החלופה שנדחתה:** מצב שלישי ב-`isUnfiled` — הוא היה פותח מחדש את החלטה **#45** ("סטטוס תיוק אחיד") בשביל מסך אחד. השאלה הדו-ערכית פשוט אינה נשאלת במקום שבו אין לה תשובה. המסנן מנוטרל **בערך ולא רק בפקד** (`filing` מוכרח `'all'` כשהארכיון דלוק), כי `/inbox` מפנה ל-`?filing=unfiled` והפרמטר שורד ניווט — אחרת הארכיון היה מסונן בשקט בלי פקד שיפתח אותו בחזרה | `src/pages/DocumentsInbox.tsx` · ‏`src/pages/documentsArchiveView.spec.tsx` |

## ביטול שיוך אוטומטי — מה קורה לחריגה שהמערכת פתחה (ברירת מחדל מתועדת 07.08.2026 — טרם הוכרע ע״י הבעלים)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 112 | **כשמבטלים שיוך אוטומטי — מה קורה לחריגה ש-`apply_document_interpretation` פתחה בעקבותיו** | **נסגרת כ-`dismissed`, עם `resolution_note` שמצהירה על מגבלת עצמה. זו ברירת מחדל מתועדת לפי סעיף 3 של המסמך הזה — לא הכרעה שקטה בקוד ולא שיתוק.** שלושת הנימוקים: ‏**(א) חריגה שמצביעה על חשבונית שנמחקה רכה היא בדיוק ה"דורש טיפול" השקרי שסעיף 12 אוסר — וגרוע מרעש**, כי המנהל אינו יכול לפעול עליה כלל: הרשומה שהיא מנקבת אינה קיימת. ‏**(ב) הביטול מכחיש את הפירוש שהוליד את החריגה** — הטענה "התקבל פריט שלא הוזמן" נשענת על קריאה שאדם בדיוק דחה. ‏**(ג) המגבלה נאמרת במפורש, וזו עיקר ההכרעה ולא נימוס: ‏הקריאה של המכונה הוכחשה — המשלוח לא.** ייתכן שהפער מול ההזמנה אמיתי לגמרי. לכן ה-`resolution_note` אומרת שהחריגה נסגרה **מפני שהשיוך האוטומטי בוטל — ולא מפני שהפער נבדק**, מוסיפה שכדי לברר את הפער יש לפתוח חריגה מחדש, ונושאת את מזהה הפעולה האוטומטית כדי שאפשר יהיה לעשות זאת בכוונה. **אסור שהניסוח ירמוז שבוצעה בדיקה** — זו תהיה המכונה מתהדרת בעבודה שאיש לא עשה. **מה ישנה את זה:** אם הבעלים יכריע שחריגה אינה נסגרת לעולם בלי שאדם הסתכל עליה — זהו שינוי של שורה אחת ב-`revert_document_auto_action` (הסרת ה-`update exceptions`) בתוספת עריכת השורה הזו, וזו בדיוק הסיבה שההכרעה יושבת בטבלה ולא בקוד. הנימוק הנגדי המלא, לפרוטוקול: חריגה שנשארת פתוחה שומרת על העיקרון ש**רק אדם סוגר פער כספי**, במחיר של פריט "דורש טיפול" שאיש אינו יכול לטפל בו. **מה שאינו פתוח:** הביטול עצמו — מחיקה רכה בלבד, סיבה חובה, שתי שורות ה-audit נשמרות; ואופן הסגירה — `dismissed` ולא `resolved`, כי `resolved` היה טוען שהבעיה נפתרה | `0077_apply_document_interpretation.sql` §4b · ‏`supabase/tests/p14_apply_interpretation.sql` (‏o3 — הוכחת המוטציה שמראה את החריגה היתומה) · ‏`src/pages/DocumentsInbox.tsx` |

## מבואות סתומים — היכולות שהוקפאו בכוונה בגל G1 (07.08.2026)

`docs/DEAD-ENDS-AUDIT.md` סיווג 22 משימות בניסוח של אדם: **13 חסומות · 6 סיבוב · 3 ישירות.** גל G1 תיקן
את מה שהוא טקסט, קישור, או חיווט של מנגנון שכבר קיים. שש שאלות שנחשפו שם הן **הכרעות עסקיות או אבטחתיות**,
ולכן המסך הופסק לשקר ו**היכולת לא נפתחה** — בדיוק לפי סעיף "אין להמציא תשובות עסקיות" ב-`CLAUDE.md`.
לכל אחת מהן: מה קורה היום, למה לא הוכרע כאן, ומה הצעד היחיד שיפתח אותה.

| # | שאלה | ההכרעה / ברירת המחדל | איפה משנים |
|---|---|---|---|
| 113 | **האם מותר לשלם לספק לפני שהחשבונית הגיעה (מקדמה)** | **לא — נשאר חסום, והמסך אומר זאת.** דרישת תשלום נגזרת אך ורק מהחשבוניות המסומנות (`PaymentRequests.tsx:284`), שני כפתורי השמירה מושבתים על `amount <= 0` (`:427-428`), ו-`create_payment_request` היה זורק `allocation_invalid` (`0023:528`). המסך **הבטיח** "ניתן לשמור דרישה ללא חשבונית" — טענה שקרית בשלוש שכבות, במסך כספי; G1 החליף את המשפט באמת ולא פתח את המסלול. **למה לא הוכרע:** מקדמה לספק היא שאלה על **תהליך העבודה והחשיפה הכספית של העסק** — האם מותר להוציא כסף בלי מסמך, מי מאשר, ואיך נסגר מול החשבונית כשתגיע. שלוש שאלות עסקיות, לא שאלת סכימה. **הצעד שיפתח:** הכרעה של הבעלים, ואז שדה סכום חופשי + הרחבת `p_allocations` להקצאה ריקה + מסלול קיזוז מול החשבונית העתידית — פקודה פיננסית חדשה, לא שינוי UI | `src/pages/PaymentRequests.tsx:409` + `0023_p1_financial_command_boundaries.sql:528` |
| 114 | **האם בעלים ארגוני רשאי לאפס סיסמה של עובד שלו** | **הוכרע 09.08.2026 (בעלים): לא — במקום זאת נבנה "שכחתי סיסמה" בשירות עצמי מבוסס מייל.** בעל ארגון עדיין אינו מאפס סיסמה של עובד אחר (זו נשארת הענקת סמכות חטיפת-חשבון, והבעלים בחר שלא לפתוח אותה); העובד מאפס בעצמו: קישור "שכחתי סיסמה" ב-`Login.tsx` → ‏`/forgot-password` (‏`resetPasswordForEmail`) → קישור במייל → ‏`/reset-password` (‏`updateUser`). התשובה במסך זהה בין כתובת רשומה ללא-רשומה — מסך כניסה אינו מדריך חברים. שסתום המפעיל (`admin-provision` ‏`reset_password`) נשאר כ-fallback. **מגבלת מסירה מוצהרת:** אין דומיין שליחה מאומת (הכרעת בעלים 08.08: "אין דומיין"), ולכן מייל האיפוס יוצא מהמיילר המובנה של Supabase — מוגבל-קצב ולא ממותג. אימות דומיין עתידי משדרג את הערוץ בלי לשנות את המסלול. **דרישת פריסה:** ‏`{origin}/reset-password` חייב להופיע ב-redirect allow-list של Auth בפרויקט המרוחק | `src/pages/ForgotPassword.tsx` + `src/pages/ResetPassword.tsx` + `supabase/config.toml` (allow-list מקומי) |
| 115 | **מה `suppliers.status='inactive'` אומר בפועל** | **הוכרע 09.08.2026 (הבעלים האציל: "תקבע מה שהכי טוב"): ‏"לא להזמין ממנו יותר"** — המשמעות היחידה שמתיישבת עם קליטת חשבונית מספק שהושבת אתמול, האירוע השכיח ביותר אחרי השבתה. **מה מסונן:** דלתות ה**רכש** — ‏`/orders/new` (סונן עוד קודם, ‏`NewOrder.tsx`) והעלאת מחירון (`PriceListUpload.tsx`, נוסף עכשיו). **מה נשאר פתוח בכוונה:** קליטת חשבונית, דרישת תשלום, התאמות בנק, תיקיית מסמכים, אנליטיקה — מסכי כסף והיסטוריה, כדי שניתן יהיה לסגור חשבון פתוח. לרשימת הספקים נוסף מסנן סטטוס, וטקסט העזר תחת הבורר אומר את ההתנהגות במדויק | `src/pages/Suppliers.tsx` + `src/components/PriceListUpload.tsx` + `src/pages/neworder/NewOrder.tsx:206` |
| 116 | **מי רשאי לפתוח חריג ידנית, ומאיפה** | **הוכרע 09.08.2026 (בעלים): מנהל ומנהל רכש בלבד (owner/office).** מומש כפקודה מנומקת אחת — ‏`open_manual_exception(entity_type, entity_id, type, reason)` ‏(`0087`), בתקדים `open_bank_transaction_exception`: סיבה חובה, ‏audit, אידמפוטנטית לכל (ישות, סוג) כל עוד חריג פתוח, ישויות נתמכות `purchase_orders`/`invoices`, ‏`assert_unit_in_scope` בגוף (A5 בסמן, לא בחריג — הרשם נשאר 59). לדפדפן עדיין **אין** ‏`grant insert` על `exceptions` — הפקודה היא הדלת היחידה. **ההשלכות על הממצאים:** ממצא 5 — כפתור "פתיחת חריג לבירור" ב-`Receiving` מוצג ל-owner/office בלבד; ‏kitchen מקבל את המשפט "עדכן את מנהל הרכש". ממצא 11 — ‏`payer` **לא** קיבל את הפקודה; ה-Note הקיים נשאר. ממצא 10 — ‏accountant ממשיך עם `open_bank_transaction_exception` הבנקאית שכבר בידיו | `0087_receipt_credit_automation_and_manual_exceptions.sql` + `src/pages/Receiving.tsx` |
| 117 | **האם `accountant` רשאי לפתוח כרטיס ספק** | **לא — נשאר חסום; במקום זאת פרוסות גרף העוגה בדשבורד שלו נפתחות לחשבוניות אותו ספק.** המסד **מתיר** לו לקרוא יתרות ספק (`p0_supplier_balance_rows()`, ‏`0022:453,:465-466`), אך `/suppliers` ו-`/suppliers/:id` הם `STAFF` בלבד (`App.tsx:228-229`) והחיפוש הגלובלי אינו כולל `supplier` לתפקיד הזה. השאלה "כמה אני חייב לספק הזה" הסתיימה בארבע תוויות בגרף בלי drill-in. **למה לא הוכרע:** כרטיס הספק מכיל מחירים, הזמנות ודירוג — כלומר **הקשר רכש**, שהחלטה #13 הוציאה במפורש מתפקיד ה-`accountant`. פתיחתו היא שינוי **חוזה תפקידים** (`PRODUCT.md:23-30`), לא שינוי UI. **הצעד שיפתח:** הכרעה, ואז או הרחבת ה-Guard, או כרטיס ספק מצומצם-כספית (יתרה, חשבוניות, תשלומים, זיכויים) שהוא מסך חדש ולא הרשאה על מסך קיים | `src/App.tsx:228-229` + `src/pages/dashboards/AccountantDashboard.tsx` |
| 118 | **ברירת המחדל של חודש היעד בהגשת מחירון ספק** | **נשארה החודש הנוכחי (`todayISO().slice(0,7)`); הכפתור נוקב בשם החודש שיוגש.** המשימה השכיחה של ספק היא מחירון ל**חודש הבא**, אך ההגשה חסינת-שינוי בסכימה (`0032:32`) ואין ב-UI ביטול או משיכה — התיקון היחיד הוא הגשה נוספת. החודש הנבחר לא אושר לפני השליחה אלא רק הדהד אחריה (`SupplierPrices.tsx:413`). G1 העביר את שם החודש אל **תווית כפתור השליחה** עצמה, כדי שפעולה בלתי-הפיכה תגיד מה היא עושה. **למה לא הוכרע:** האם ספק מגיש מראש לחודש הבא או מתקן את החודש הרץ — עובדה על **תהליך העבודה מול הספקים של העסק**, ושינוי ברירת המחדל בכיוון הלא נכון מייצר הגשות לחודש שגוי שאי אפשר למשוך. **הצעד שיפתח:** אמירה של הבעלים, ואז שורה אחת ב-`SupplierPrices.tsx:285` (`shiftCalendarMonth(monthKey, +1)`) | `src/pages/SupplierPrices.tsx:285` |

## פירוש אוטומטי וקליטת מחירון לפי שורה (07.08.2026)

| # | שאלה | ההכרעה | איפה |
|---|---|---|---|
| 119 | **האם קליטת מחירון חולקת את מתג אוטונומיית המסמכים** | **לא.** `price_list.intake` הוא מפתח מדיניות נפרד, עם בסיס כבוי וסף התחלתי `0.900` שאינו מכויל. אפשר לפרש מסמכים אוטומטית בלי להעניק למודל סמכות לכתוב מחירים. **הבעלים הפעיל ב־07.08.2026 את המדיניות לדייר החי** כדי שמחירון יעדכן שורות בטוחות וייצור מוצרים מזוהים בלי הקמה ידנית; ברירת המחדל לדיירים אחרים נשארת כבויה | `0081_automatic_interpretation_and_price_list_intake.sql`, `AutonomyPolicyPanel.tsx`, `DEBT-REGISTER` §24 |
| 120 | **באיזו זהות רצה פרשנות שהופעלה בלי דפדפן** | זהות מעלה המסמך בלבד, ורק לאחר התאמה מדויקת בין `documents.uploaded_by`, ‏`jobs.requested_by`, ‏`jobs.interpretation_actor_id`, ‏`interpretations.interpreted_for_user_id`, הארגון והתפקיד הפעיל. טוקן ה-cron אינו שחקן עסקי ואינו מקבל `service_role` בדפדפן | `interpret-document/index.ts`, `0080_server_price_submission_actor.sql` |
| 121 | **מהי תקרת העלות להפעלה האוטומטית** | ברירת המחדל היא **20 התחלות פירוש לארגון בשעה**, לכל היותר משימה אחת לארגון בדקת cron, וניסיון מסירה שלא התחיל מודל רשאי לחזור אחרי חמש דקות. הטווח המבני הוא 1–100; שינוי דורש עדכון שורת הקונפיגורציה הפרטית | `private.document_interpretation_automation_config`, `private.claim_document_interpretation_jobs` ב-`0081` |
| 122 | **מה גורל שורת מחירון שלא הותאמה** | קליטה חלקית: שורות בטוחות נכנסות; עמומות או חסרות נתונים ממתינות עם סיבה. בעקבות הכרעת הבעלים, שורה **שאינה תואמת מוצר קיים** אך נושאת שם + מק״ט או ברקוד יוצרת מוצר חדש ומחיר ספק באותה עסקה. שם אינו משמש להתאמה, ולעולם אין “הראשון מנצח” בכפילות | `private.match_price_list_line`, `apply_price_list_interpretation` ב-`0081`; עדכון #78 |
| 123 | **מה מציג מסך בדיקת מחירון אחרי הפירוש** | תוצאת ההעלאה האחרונה היא המידע הראשי: כמה שורות נקלטו, כמה מוצרים נוצרו ומה עדיין דורש בדיקה, כולל תוצאה לכל שורה. במובייל היא מופיעה לפני הקובץ המקורי. כרטיסי הערות, כללים או ייצוא שאין בהם תוכן אינם מוצגים למחירון. העלאה דרך מסלול המחירון היא הקשר שרת מהימן: כותרת "הצעת מחיר" אינה מחזירה את המשתמש לאישור סוג ידני, אך כל גדרי הספק, המזהה והמחיר נשארים | `0084_automatic_document_classification.sql`, `DocumentReviewWorkspace.tsx`, `PriceListReviewConfirmation.tsx` |

**מה **כן** תוקן בגל G1 ואינו פתוח:** ‏`SupplierSelectField` אומר לתפקיד שאינו רשאי ליצור ספק למי לפנות (במקום
שדה חובה בלי כפתור ובלי מילה), והוא חובר למסך הרביעי — `Bank.tsx` — **אחרי** שהמשפט נוסף ולא לפניו; ה-FAB שומר
את פעולת הצילום בשלושת המסכים שבהם הסתיר את כל הסרגל; ‏"עריכה" ברשימת החשבוניות נקראת "פתיחה" ו-`?from=`
נושא `order`/`receipt`; ‏`OrderDetail` ו-`ReceiptDetail` מציעים את קישור החשבונית המקושרת שהיה קיים בחלון-זמן
אחד בלבד; ‏`/suppliers/:id?edit=bank` פותח את טופס פרטי הבנק ישירות; החוסם של סגירת החודש הוא `Note` קבוע עם
הקישור המשקם במקום toast חולף; ומ-`/products` יש קישור להיסטוריית המחירים.
