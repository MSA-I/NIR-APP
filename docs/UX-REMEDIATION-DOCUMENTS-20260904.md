# תוכנית תיקון — העלאת מסמכים והעוזר

**נוצר:** 04.09.2026 · **מקור:** ביקורת UX בשישה סוכנים על `main@b48a0b36` · **104 ממצאים**
**ענף:** `docs/ux-remediation-documents-assistant` · **סטטוס:** תוכנית בלבד, לא נגעו בקוד
**סבב ביקורת:** ‏1 (Codex, `VERDICT: REVISE`) — הגרסה הזו היא התיקון. יומן: `UX-REMEDIATION-REVIEW-LOG-20260904.md`

---

## 0. שלוש טענות ב-`CLAUDE.md` שהתיישנו — ותוקנו כאן

הגרסה הראשונה של התוכנית ירשה אותן מהחוקה ובנתה עליהן שערי קבלה. **כולן נבדקו ונמצאו לא נכונות היום.**
זה מסמך נפרד לתיקון, אבל הוא חייב להירשם כאן כי הוא משנה את הגדרת „בוצע":

| מה `CLAUDE.md` אומר | מה נמדד ב-04.09.2026 |
|---|---|
| „‏13 מתוך 27 שומרי `npm run verify` אינם רצים ב-CI" | **‏`verify` מריץ 32 תת-פקודות; ‏`build.yml` מריץ את אותם 32 בשמם.** אין פער |
| „‏`check:contrast` נכשל על `main` היום ומעולם לא עבר שם" | **עובר.** ‏exit 0, ‏53 צמדי טקסט, 7 צמדי non-text, 1 חוזה כיוון |
| „‏PR שהבסיס שלו אינו `main` מקבל אפס בדיקות (‏§65)" | **‏§65 נסגר 02.09.2026.** שני ה-workflows מכילים `pull_request:` בלי מסנן ענף, ונוסף שומר שמונע חזרה |

**המסקנה המעשית:** ‏`npm run verify` המקומי ו-CI זהים; `check:contrast` הוא שער **חוסם רגיל**;
אין צורך בהרצת `workflow_dispatch` ידנית לענף. ‏**‏`CLAUDE.md` עצמו צריך תיקון בקומיט נפרד.**

הסתייגות אחת שכן שרדה: ‏`workflow_dispatch` ב-`quality-gate.yml:77-83` **חסר `render=true`**,
ולכן „מריץ הכול" אינו מדויק גם היום.

---

## 1. מה המסמך הזה, ומה הוא לא

תוכנית מימוש נגזרת מביקורת שכבר בוצעה, **שעברה סבב אחד של ביקורת נגדית מול הקוד עצמו**.
ציטוטים שנמצאו שגויים או מוגזמים בסבב הזה — תוקנו במקומם וסומנו.

**מה שהתוכנית לא עושה:** לא מרחיבה מעבר לזרימות המסמכים והעוזר, לא נוגעת בשכבת הנתונים,
ולא מציעה עיצוב מחדש.

**מדידת היקף — לא בזמן.** מאמץ נמדד בקבצים, אתרי קריאה ומפתחות מילון.

### מספרים

| סוג | הבא הפנוי | הערה |
|---|---|---|
| הכרעת בעלים | **`#358`** | ‏`#350`–`#357` תפוסים על `qa/pr01-offboarding` ולא בעץ שלנו |
| סעיף חוב | **`§117`** | |
| מיגרציה | `0315` | נדרשת רק אם D-4 או D-6 יאושרו |

**שתי „הכרעות" מהגרסה הראשונה נמחקו — הן כבר הוכרעו:**
- **תעודת משלוח בלי הזמנה → `#125`** (09.08.2026, בעלים). שלוש דרגות ראיה; אפס או ריבוי → `order_unresolved` והמסמך **ממתין לאדם**. בורר ההזמנות אינו שאלה חדשה — הוא **המימוש של ההכרעה הקיימת**.
- **שמות מוצרים הפוכים → `#231`.** re-import מהמקור, preview, ואישור אדם לכל שינוי. **לא להקצות מספר חדש.**

---

## 2. העובדה שמשנה את סדר העדיפויות

מסכי ההעלאה **אינם הבעיה**: `העלאת מסמך` מציג **7 פקדים**, `העלאת מחירון` מציג **6** — שניהם
בתוך תקציב הׅ־‏~5 של חוק היק.

| מסך | גובה | מסכי גלילה | פקדים | חלוניות | כותרות |
|---|---:|---:|---:|---:|---:|
| אישור מסמך · דסקטופ | 3196px | **3.6** | 32–72 | **24** | **14** |
| אישור מסמך · טלפון | 4081px | **4.8** | 30 | 21 | 14 |
| אישור מחירון · 74 שורות | — | — | **~266** | 14 | — |
| תיקיית המסמכים · 4 מסמכים | 900px | 1.0 | **37** | 6 | 1 |
| מחירונים · טלפון | 2466px | 2.9 | 31 | 17 | 2 |

---

## 3. גרף התלויות — לא שרשרת

הגרסה הראשונה טענה `P1→P10` בטור ו-`P9` עצמאי. **שתי הטענות שגויות.**
‏`P9` חולק את `AnswerView.tsx` עם `P2` ואת `he.ts`/`en.ts` עם `P2`–`P5`, ‏`P8` ו-`P10`;
ואילו `P1` ו-`P4` דווקא **כן** עצמאיים מלבד המילון.

```
                    ┌─────────────────────────────┐
   P1 ──────────────┤  חוזה מילון משותף           │
   P4 ──────────────┤  he.ts / en.ts              │──── P10 (אחרון תמיד)
                    │  נעול לפני P2/P5/P8/P9      │
   P2 ──► P6 ───────┤                             │
    │                └─────────────────────────────┘
    └──► P3 ──► P7        P8 ◄──► P6   (שניהם ב-DocumentReview)

   P9a recovery ─┐
   P9b help+Edge ─┼── חולקים AnswerView ו-he.ts ──► אחרי נעילת חוזה המילון
   P9c feedback  ─┤
   P9d visual    ─┘
```

**כלל אחד:** חוזה המילון (`he.ts` + `en.ts`) נסגר **לפני** שמתחילים P2/P5/P8/P9,
אחרת שתי חבילות מוחקות מפתחות זו של זו ו-`check:orphan-keys` תופס את זה מאוחר מדי.

---

## 4. שערי קבלה

| # | שער | מדידה | ראיה |
|---|---|---|---|
| G1 | אין רגרסיה סטטית | `npm run verify` — **32 תת-פקודות**, זהה ל-CI | פלט מלא |
| G2 | טיפוסים ובאנדל | `npm run build` | פלט |
| G3 | מילון עקבי | `check:i18n`, `check:orphan-keys`, `check:plurals` — **רצים גם ב-CI**; עדיין להריץ מקומית כי הם זולים ומשוב מיידי | פלט |
| G4 | ניגודיות | `check:contrast` — **שער חוסם רגיל, עובר היום** | פלט |
| G5 | הצפיפות ירדה בפועל | ספירת פקדים/חלוניות/בלוקי טקסט בדפדפן, לפני ואחרי | טבלה + צילום **דסקטופ ומובייל** |
| G6 | הזרימה עדיין עוברת | תרחיש: העלאה → סטטוס → אישור, כולל „אין ספק" | צילומים בשתי מידות |
| G7 | שער האינטגרציה | ה-PR מפעיל אותו אוטומטית (‏§65 נסגר). ‏`workflow_dispatch` ידני **רק** כשצריך ריצת כל-הנתיבים — ואז לזכור ש-`render=true` חסר בו | קישור לריצה |
| G8 | **חדש —** הוכחה במסלול השמור | כל עריכה כספית מוכחת דרך המסלול המוגן, **לא** דרך `psql` כתפקיד `postgres` (שעוקף את שומר הכספים ואת RLS) | פלט הרצה |

**שורת רולאאוט** (מטריצת `CLAUDE.md`):
- ‏**P1–P8, P10 — Frontend / נכס ציבורי.** ‏`build` · `verify` · browser. בייצור: בנייה עם env ייצור, סריקת סודות, **`wrangler pages deploy --branch=main`** (בלי הדגל מתקבל preview שמדפיס „Deployment complete" בזמן שהכתובת החיה מגישה את ה-build הקודם — לאמת ב-`canonical_deployment` מול ה-API), התאמת hashes, smoke קנוני, `check:live-seo`.
- ‏**P9b — Frontend + Edge Function.** ‏**תיקון מסבב 1:** ‏`routeAccess.ts` ו-`productHelpRegistry.ts` **מיובאים לתוך פונקציית `assistant`** (`evidence-authorization.ts:8`, ‏`index.ts:74`, ‏`live-evaluation.ts:6`). שינוי בהם מחייב חוזי Deno **ופריסת Edge**, לא רק Pages, ואימות בקריאה חיה ממוקדת. ‏`P9a`/`P9c`/`P9d` נשארים Frontend בלבד.
- **‏D-4 או D-6 אם יאושרו — מוסיפים שורת Migration:** גיבוי schema/data/roles, dry-run + ledger, apply forward-only, postflight.

---

## 5. החבילות

### P1 — תיקוני נכונות · 5 קבצים

| # | מה | איפה | התיקון |
|---|---|---|---|
| 1.1 | קריאה ל-hook של React **בתוך תנאי, בפונקציה שאינה רכיב**. קבלה פגומה → `Invalid hook call…` באנגלית במקום המשפט העברי, בדיוק כשצריך לדעת אם המחירים נשמרו | `PriceListReviewConfirmation.tsx:181` | **מחיקת השורה.** ‏`t` כבר מגיע כפרמטר ומוצל על ידה |
| 1.2 | קוד שגיאה פנימי בסוף משפט עברי: `…1 לא הושלמו. document_registration_transient` | `FileUpload.tsx:865-866` | `errorText(new Error(failures[0].code))` — כמו `DocumentsInbox.tsx:323` ו-`QuickCapture.tsx:89` |
| 1.3 | כל העלאה מהתיקייה **חותמת את היום** כתאריך המסמך, גם בלי לפתוח את השדה. הרשימה מציגה אותו כתאריך המסמך ומדכאת את סימון `(העלאה)` שקיים בדיוק כדי להבחין. מסלול המצלמה שולח `null` ומסומן נכון — **שתי דלתות, שתי טענות סותרות** | `DocumentsInbox.tsx:297`, `:310`, `:829` | אתחול ל-`''` |
| 1.4 | **מנוסח מחדש אחרי סבב 1.** הכפתור מושבת כל עוד יש קובץ שנכשל. ‏**התיקון לניסוח:** סיכום הכשל וכפתור ה-retry **כן נשארים גלויים** (`:986-1000`) — זה לא „מבוי סתום בלי סיבה". מה שבאמת נשבר: **אי אפשר להתחיל batch חדש, ואי אפשר לנטוש retry.** ‏`ניקוי שהסתיימו` מנקה את Upload Center ולא את `uploadSummary` | `FileUpload.tsx:960`, `:986-1000` | **לא להסיר את `disabled` לבדו** — ראה סיכון למטה |
| 1.5 | כפתור המצלמה בטלפון **מפסיק בשקט להיות מצלמה** כשיש קובץ שנכשל. מסומן רק ב-`aria-label`/`title`, והסרגל `lg:hidden` — מגע בלבד, שם `title` לעולם לא מוצג. הכיתוב הגלוי והאייקון נשארים „מצלמה" | `QuickCapture.tsx:167`, `Fab.tsx:86`, `:93-96`, `:106` | כיתוב **גלוי** ואייקון שמשתנים כש-`retryCount > 0` |

**סיכון 1.4 — נמצא בסבב 1, חוסם:** ‏`clientUploadKey` נגזר מאובייקט ה-`File` עצמו
(`FileUpload.tsx:101-116`). בחירה מחדש של אותו קובץ יוצרת `File` חדש ולכן **מפתח חדש** —
כלומר הסרת התנאי לבדה **מייצרת העלאה כפולה**. בנוסף, batch חדש דורס את `retryFiles` הישן.
**התיקון הנכון:** לשמר retries לפי מפתח יציב (לא לפי אובייקט `File`), ורק אז לאפשר batch חדש.

**שער:** G1, G2, ו**הוכחה ריצתית לכל אחד מהחמישה**. ‏1.4 דורש גם הוכחה שאין כפילות.

---

### P2 — צמצום המסך · 9 קבצים

**תיקון מסבב 1:** זו **אינה** „מחיקות טהורות". שלושה פריטים הם refactor עם התנהגות
(‏2.2 ממירה נתיב, ‏2.11 מעבירה state, ‏2.12 מוסיפה disclosure) ודורשים בדיקות התנהגות.

#### 2א — מחיקות אמיתיות

| # | מה | איפה |
|---|---|---|
| 2.1 | כרטיס `מה ישולם בפועל` — שתי שורות מקודדות ל-`value={null} … missing` + `„…שתי הרגות אינן מחולצות."` ‏**אומת:** הקוד עצמו מגן על ההצגה בהערה. ראה **D-1** | `ReconciliationStrip.tsx:219-229` |
| 2.3 | המאפיין `meta` מתחת לכותרת התיקייה — `PageHeader` כבר פותר תיאור אוטומטי (`ui.tsx:305-314`) ואתר הקריאה מעביר גם וגם | `DocumentsInbox.tsx:878` |
| 2.5 | ההודעה הירוקה שמסכמת העלאה — נוכחת גם בהרצה נקייה, מדפיסה `0`, ומתנקה רק בבחירה הבאה | `FileUpload.tsx:986-1005` |
| 2.7 | טלמטריה מכל תג: `· עמוד 7 מתוך 27 · 12 דק׳`. ‏**לוודא שהמצב `עיבוד תקוע` עדיין ניתן לזיהוי בלעדיה** | `DocumentStatusBadge.tsx:33-38` |
| 2.8 | `גרסה: 2` ותג `בקשה חוזרת — ללא כפילות` — הגנה מכפילות אינה תג למשתמש | `PriceListReviewConfirmation.tsx:1180`, `:1183` |
| 2.10 | תג מונה `היקף הבדיקה` על השורה **הסגורה**. הקיפול נשאר | `AnswerView.tsx:378` |

#### 2ב — החלפות, לא מחיקות (תוקן בסבב 1)

| # | מה | הבעיה שנמצאה | מה עושים במקום |
|---|---|---|---|
| 2.4 | רשת שמות השדות הגולמיים (`description`, `unit_price`, `sku`) — עד 50 בעמוד | `item.values` הוא גם **המקור ל-`guessLineName`** (`PriceListReviewConfirmation.tsx:63`); SKU, יחידה וערכים של שורה לא-מותאמת **אינם כולם** מוצגים בשדות הייעודיים | **סיכום עברי קומפקטי** במקום הרשת. לא למחוק את ראיית המקור |
| 2.6 | גודל הקובץ בכל שורת תור | **לא בתים גולמיים** — `UploadCenter.tsx:608` כבר ממיר ל-B/KB/MB | להסיר כי לא ניתן לפעולה, לא כי הוא „גולמי". ובמקביל — התקרה (10MB) חייבת להופיע **לפני** הבחירה, לא רק כדחייה |
| 2.9 | חותמת `נמדד ל־` מתחת לכל עובדה | `Fact.as_of` הוא **חלק מחוזה הראיה** ויכול להשתנות בין עובדות בעתיד | **לא למחוק גלובלית.** לתקן רק את עזרת המוצר, שחותמת `ctx.now()` על תוכן סטטי — להשתמש ב-`entry.updated_at`, או לקבץ חותמות זהות |
| 2.11 | כרטיס `מצב המסמך` | **לא תמיד תג יחיד** — בזמן עבודה חיה הוא מכיל גם `DocumentProcessingProgress` (`DocumentReviewWorkspace.tsx:95-102`); רק ב-`review` הרכיב מחזיר `null` | לפצל לפי state: תג בכותרת בשלב הבדיקה, תג+התקדמות יחד בשלבים החיים. **בדיקות לכל שלב** |
| 2.12 | פרובננס תבנית הייצוא | — | דחייה מאחורי פעולת הייצוא. **מוסיף disclosure חדש** — לא מחיקה |
| 2.2 | מסנן `סטטוס תיוק` | **דורש להמיר `/inbox?filing=unfiled` ל-processing filter** ולעדכן `App.tsx`, ארכיטקטורה ובדיקות | לתכנן כ-refactor, לא כמחיקת שורות |

**שער:** G1–G4, **G5 חובה**. **יעד:** אישור מסמך מ-24 חלוניות ל-**≤20** ומ-127 בלוקי טקסט ל-**≤105**;
תיקיית המסמכים מ-37 פקדים ל-**≤35**.

---

### P3 — פתרון הספק · 6 קבצים

**זו התלונה שהתחילה את הכול.**

#### מה אומת, ומה תוקן בסבב 1

- הכרטיס `הספק` מציג `לא זוהה מהמסמך` / `יותר ממועמד אחד — נדרשת בחירה` ורשימת מועמדים
  כ-`<li>` **שאינם ניתנים ללחיצה** — `DocumentAssessmentPanel.tsx:322-344`. ✔
- טקסט אחר מנחה `…ולזהות את הספק למעלה` — `DocumentLineMapping.tsx:283-287`. ✔
- `supplierId` לקריאה בלבד; ‏`canSubmit` ← `false`; הכפתור `disabled` **בלי רמז**. ✔
- **תיקון:** ‏`assessment.ts:147` הוא **הגדרת TypeScript**, לא „השרת מפרסם". המקור האמיתי הוא
  `0109_document_review_assessment_read.sql:159`. הטענה ש**אין קורא ב-`src/`** — נכונה.
- **תיקון:** ‏`QuickSupplierPicker` **אינו** מנהל את הרשימה בעצמו. ‏`useQuickSupplier`
  (`QuickSupplierPicker.tsx:116`) מקבל רשימה שנשלפה **על ידי הקורא**, וממזג יצירות מקומיות בלבד.
  ⇒ **התוכנית חייבת לכלול שאילתת ספקים tenant-scoped ומצבי loading/error.**

| # | שינוי | הערה |
|---|---|---|
| 3.1 | להרכיב `QuickSupplierPicker` בכרטיס `הספק` + לספק לו שאילתה tenant-scoped | הכתיבה **רק** דרך `apply_reviewed_document`, שמאמת org, supplier, order, scope וסיבה (`0110:341-355`) |
| 3.2 | רשימת המועמדים → בחירה | |
| 3.3 | לקרוא את `supplier_unresolved` ולתת לכפתור המושבת **סיבה מנוסחת** | המקור: `0109:159` |
| 3.4 | בורר הזמנות — **מימוש `#125`, לא הכרעה חדשה** | **חסר בגרסה 1:** חייבים להגדיר eligibility — cancelled / closed / fully-received, ישות משפטית, יחידה ו**מטבע**. לנעול מול אותו חוזה שהשרת בודק |
| 3.5 | מועמד חסר-שם מוצג כ-UUID גולמי → נפילה חזרה אנושית | `DocumentAssessmentPanel.tsx:336` |
| 3.6 | `ספק לא מזוהה` כמצב משלו ברשימה | **חסר בגרסה 1:** לתיקייה **אין `assessment` state** — היא שולפת `documents`, ספקים, processing ו-auto-actions בלבד (`DocumentsInbox.tsx:467-540`). נדרש **read model מרוכז**. ‏**אסור RPC לכל שורה** |
| 3.7 | **הועבר לשלב אבחון, לא מימוש.** הרשימה מציגה `—` בזמן שמסך האישור מכריז `זוהה · שם ספק תואם במדויק` | חקירה read-only **קודם**; רק אחריה עריכת מקור מדויקת נכנסת לתוכנית |
| 3.8 | **מנוסח מחדש.** ‏`apply_reviewed_document` **כן** מעדכן `documents.supplier_id` (`0110:487`) — הטענה „שום דבר לא מתקן" הייתה מוגזמת. הבעיה האמיתית: **עד האישור, הרמז מההעלאה נחשב ראיה סמכותית** ומוצג ככזה | לתכנן provenance לרמז |
| 3.9 | **חדש.** בזיכוי, בחירת ספק ידנית **אינה פותרת** `credit_resolution` לא-פתור | לבדוק במפורש |

**חסם לפני 3.1 — נמצא בסבב 1:** ‏`QuickCreateSupplier` מבצע insert ישיר (`:161`) ונסמך על RLS
נכון, אבל Codex טוען שהטריגר הגנרי כותב audit עם `reason = null` — מה שסותר את כלל הברזל
„כל פעולה רגישה נרשמת **עם סיבה**". **לא אימתתי את הטריגר בעצמי.**
⇒ **צעד ראשון ב-P3: לאמת את הטריגר.** אם הטענה נכונה — להעביר יצירת ספק ל-RPC
tenant-scoped, idempotent ומנומק, **או לא להציע יצירה במסך הזה כלל** ולהסתפק בבחירה.

**שער:** G1, G2, G6, **G8**. תרחיש מלא בדפדפן, כולל יצירת ספק חדש, בשתי מידות מסך.

---

### P4 — הוצאת קונסולת האילוף · 2 קבצים

אחד עשר פקדים במסך הלקוח, אף אחד מהם לא עוזר לאשר חשבונית: שם עמודה גולמי (`supplier_name`)
כטקסט מצויר, שדה **נערך** שכותרתו `מפתח משמעות` וערכו אותו `supplier_name`, שדה `תווית`,
תיבת `סיבת המשוב`, שלושה כפתורים, תג `כלל v3`, והודעה שמכילה את המילה האנגלית `annotation`.

| # | שינוי |
|---|---|
| 4.1 | להסיר את קיפולי `הערות והחלטות` ו-`כללים שהופעלו` (`DocumentReviewProposals.tsx:740-798`) |
| 4.2 | להסיר את שדות העריכה ואת שלושת הכפתורים (`:451-480`, `:545-570`) |
| 4.3 | **תוקן בסבב 1.** „קישור אחד" **לא אפשרי** — `add_document_feedback` דורש `annotation.id`, ובמסמך יכולים להיות כמה annotations וכללים. ⇒ **פעולה קומפקטית לכל הצעה**, או חוזה document-level חדש ומבוקר |
| 4.4 | לנקות `annotation` מהמחרוזת העברית (`he.ts:2566` + `en.ts`) |

**תוקן בסבב 1 — D-3 אינה „העברה":** ‏`/documents/operations` **אינו טוען** annotations,
feedback או learning rules. „להעביר לשם" היא **חבילת query + UI + authorization נפרדת**,
לא צעד בתוך P4. אם לא בונים אותה — P4 היא **מחיקה**, ויש להצהיר על כך.

---

### P5 — כיווץ אוצר המילים · 16 אתרי קריאה, 2 מילונים

**התיקון הגדול ביותר מסבב 1: ארבעה מתוך חמשת המיזוגים שהוצעו הורסים הבחנה אמיתית.**

| # | מיזוג | פסק סבב 1 | מה עושים |
|---|---|---|---|
| 5.1 | שש מילות „בתהליך" → `קורא את המסמך` | **דוחה.** ‏`DocumentProcessingProgress` מפריד **בכוונה** `ממתין בתור` מ-`קריאת המסמך`; ‏`משפר תמונה` אף **מצהיר שטרם בוצע OCR** | לאחד **ניסוחים בתוך כל state**, לא למזג queue / scanning / reading לטענה אחת |
| 5.2 | שלוש תוויות שיוך → `משויך` | מקובל | |
| 5.3 | `סטטוס נטען`, `סטטוס לא זמין`, `הוחלף בניסיון חדש` → שום תג | מקובל | |
| 5.4 | `העיבוד נכשל` + `עיבוד תקוע` → מצב אחד | **דוחה.** ‏`failed` מציג retry; ‏`stuck` **אינו נכלל** ב-row retry ואינו מקבל כפתור במסך הבדיקה. הפעולה **אינה** זהה | **קודם** לבנות recovery אחיד; **רק אז** תווית אחידה |
| 5.5 | שמונה מצבי העלאה → שלושה | **דוחה חלקית — בטיחות כספית.** ‏`הועלה אך לא נרשם` שונה מהותית מ-`נרשם — העיבוד לא החל`: בראשון **אין registry row**, בשני **אסור להעלות שוב** | להחליף ז'רגון במילים אנושיות, **לשמור את ההבחנה ואת פעולת ה-retry המתאימה לכל אחד** |
| 5.6 | לתקן את ההפניה השבורה `„…במרכז תפעול המסמכים"` — טקסט ולא קישור; **אין מסך בשם הזה** (הדף `בקרת מסמכים`); ולבעלים בלבד | **תיקון:** מסך הבדיקה פתוח ל-`owner|office` בלבד (`App.tsx:172`). ‏**רואה החשבון אינו רואה את ההפניה.** הנפגעת היא `office` | |
| 5.7 | לסלק אוצר מילים של תור מהתיקייה | מקובל | |
| 5.8 | למחוק `„מספר המקטעים עדיין לא ידוע."` (`he.ts:5915`) | מקובל | |
| 5.9 | לאחד `שליחה לעיבוד`/`עיבוד מחדש` ו-`הסרה`/`הסרה עם תצוגת השפעה` | **דוחה את השני.** הסרה מהארכיון היא soft delete בלי סיבה; „הסרה עם השפעה" **מבטלת נגזרות, דורשת סיבה, ונחסמת על תלות כספית** | לאחד רק את הזוג הראשון; לשני — **לתאר את ההשלכה כבר בתפריט**, לא לאחד שמות |

**שער:** G1–G4, G6. **חובה:** לוודא שאף מצב שהמשתמש **כן** פועל לפיו לא נבלע.
‏`documentOperationsModel.ts:53` **בכוונה** משתף `documentStatus.needsReview` עם קונסולת הבעלים —
לשמור על הקישור או לנתקו במפורש.

---

### P6 — סדר מסך האישור · **תוקן: 6 קבצים, לא 2, ולא layout בלבד**

הציטוטים עצמם נוגעים ב-`DocumentReviewWorkspace`, `PrimaryDecision`, `DocumentAssessmentPanel`,
`ReconciliationStrip`, `DocumentReviewProposals` ו-`DocumentReview`. **קיפול והסתרת פקדים הם
שינויי interaction ודורשים בדיקות state.**

| # | שינוי | הראיה |
|---|---|---|
| 6.1 | להעלות את `המסמך המקורי` לראש העמוד **בטלפון** (`DocumentReviewWorkspace.tsx:162`, `:176`) | העמוד 4081px; המסמך מתחיל ב-**y=3579**, כלומר 4.2 מסכים אחרי 13 חלוניות. `אישור המסמך` ב-y=1437 ומתחתיו **שבע** חלוניות. **מבקשים לאשר לפני שמראים** |
| 6.2 | משקל ויזואלי להחלטה (`PrimaryDecision.tsx:43-69`) | 69 שורות שמתוכן **26 הערה**; לא מצייר מסגרת, רקע או כותרת. ההחלטה בכרטיס התוכן **היחיד בלי כותרת** — ל-13 האחרים יש |
| 6.3 | לקפל את סולם ההתאמה כשהמספרים מסתדרים | היום נבנה **פתוח** בכל מסמך, בזמן שטבלת השורות — המסוכנת פחות — מקופלת |
| 6.4 | ארבע ההכרזות של „עדיין לא אושר" → אחת | ארבעה ניסוחים בארבעה אוצרות מילים |
| 6.5 | כרטיס `סוג המסמך` → שורה + קישור | כותרת המשנה שלו אומרת בעצמה `„…אין צורך באישור ידני."` |
| 6.6 | לאחד את ההרכבה הכפולה של סביבת העבודה (`DocumentReview.tsx:264-274`, `:289-299`) | רשימת מאפיינים זהה בת 8 שורות פעמיים |

**יעד:** טלפון מ-4.8 מסכים ל-**≤3.0**; דסקטופ מ-3.6 ל-**≤2.5**; `המסמך המקורי` ל-`y < 1200` במובייל.
**‏RTL:** properties לוגיים בלבד.

---

### P7 — עריכת ערכים שנקראו · 2 קבצים · **חוסם כספי שנמצא בסבב 1**

**תוקן:** ‏`ReviewedLineEdit` מכיל **שלושה שדות מספריים ו-`unit` טקסטואלי** (`assessment.ts:393-399`),
לא „ארבעה מספריים". הטבלה מציגה כרגע **רק** `quantity` ו-`unit price` — לא `unit` ולא `line total`.

> ### ⚠ החוסם: זריעת מחירון עם המספר השגוי
>
> `priceSeedRows` (`assessment.ts:490`) קורא `edits[line.line_index]?.product_id` בלבד,
> ואז לוקח `line.normalized_unit_price ?? line.unit_price` — **הוא מתעלם מ-`edits.unit_price`.**
>
> כלומר ברגע שנאפשר עריכה: **החשבונית תירשם במחיר המתוקן, ומחירון הספק ייזרע במחיר
> שהמכונה קראה.** שני מספרים שונים לאותה עסקה, והשגוי הוא זה שנשאר כנקודת ההשוואה העתידית.
>
> **‏P7 לא תיפתח לפני שזה נפתר** — לגזור את הזריעה מה-reviewed proposal המאושר, או להעביר
> את הזריעה לשרת.

| # | שינוי |
|---|---|
| 7.1 | לתקן את `priceSeedRows` שיכבד `edits.unit_price` — **קודם** |
| 7.2 | להגדיר **במפורש** אילו עמודות נוספות נבנות בטבלה ואיך כל ערך נערך |
| 7.3 | **ולידציה, חסר לגמרי בגרסה 1:** פסיק עשרוני, סימן מטבע בקלט, ערך שלילי, ערך ריק, precision. ‏**המטבע נשאר בלתי-ניתן לעריכה**, ו-`unit price`/`line total` חייבים להציג את מטבע המסמך (כלל ברזל: „כל סכום נושא מטבע"). לבדוק `minor_units` |
| 7.4 | הבחנה ויזואלית בין ערך שהמערכת קראה לערך שאדם אישר |

**שער:** G1, G2, **G8**, וסוויטת בדיקות חדשה. כל עריכה נרשמת ב-`audit_logs` **עם סיבה**.

---

### P8 — מוצא מסריקה שנכשלה · **תוקן: לא כשל אחד, שישה**

**התיקון מסבב 1:** קודי הכשל כוללים corrupt, decompressed-size, file-size, resource, timeout
ו-image-too-small (`DocumentScanPreview.tsx:33-43`). **מסגרת מלאה לא מתקנת אף אחד מהם.**
יתרה מזו — כשל גבולות רגיל **כבר מפיק `full_frame_fallback`**.

| # | שינוי |
|---|---|
| 8.1 | **לפצל recovery לפי error code.** להציע „קרא כמו שהוא" **רק** כשהמקור תקין והבעיה גאומטרית |
| 8.2 | לעשות את עורך הפינות אופציונלי ולא כפוי (`:150`) — ההגשה מושבתת (`:135`) עד שארבע נקודות יוצרות מרובע קמור ≥8% מהמסגרת (`useDocumentScanning.ts:59-60`), וההודעה מנסחת זאת בשפת גיאומטריה |
| 8.3 | **תוקן: אינו „עוד כפתור".** אין callback שמחליף קובץ; העלאה חדשה יוצרת **מסמך חדש**. נדרש חוזה replace/supersede בשרת + audit + סיבה, **תוך שמירת המקור הישן immutable**, והחלטה מה קורה למסמך הכושל ולניווט |
| 8.4 | לכווץ את כרטיס האישור לשתי תמונות, משפט אחד וכפתור אחד שאינו נקרא `אישור והמשך לחילוץ` |

השאלה שהמסך שואל — האם הדף שלם וקריא — **כן** ניתנת לענייה משתי התמונות. זה החלק שעובד.

---

### P9 — העוזר · **מפוצל לארבע, ו-P9b נוגע ב-Edge**

| חבילה | תוכן | רולאאוט |
|---|---|---|
| **P9a** recovery | 9.1, 9.9, 9.10 | Frontend |
| **P9b** עזרה + הרשאות | 9.2, 9.3, 9.4, 9.5 | **Frontend + Edge** |
| **P9c** משוב | 9.11 | Frontend |
| **P9d** ויזואלי | 9.6, 9.7, 9.8, 9.12, 9.13, 9.14, 9.15 | Frontend |

| # | שינוי | תיקוני סבב 1 |
|---|---|---|
| 9.1 | recovery אחרי כשלון | **לא P0 ולא מבוי סתום מוחלט.** ה-composer **נשאר פעיל**, ושאלה שהוקלדה נשארת ב-state. **רק** שאלה שנשלחה מהצעה אינה מוחזרת לשדה, וכפתור `בדיקה חדשה` חסר. ⇒ retry ששומר את `submittedQuestion` |
| 9.2 | להוסיף `/documents` לרשימת ההיתרים (`routeAccess.ts:25-60`) | **נוגע ב-Edge** |
| 9.3 | ערך עזרה למקרה „אין ספק" — היום `אין ספק`/`לא זוהה ספק`/`לשייך ספק` אינם מופיעים באף ערך | **נוגע ב-Edge** |
| 9.4 | להרחיב שמונה ערכים מ-`owner` לתפקידי המסלול | **תיקון D-7:** הרחבה גורפת לפי route **אינה מספיקה** — לאשר כל entry מול כל פעולה שהוא מתאר |
| 9.5 | עזרת מוצר מוצגת ככתובות גולמיות עם `נמדד ל־` שנחתם ב-`ctx.now()` על תוכן סטטי | להשתמש ב-`entry.updated_at` |
| 9.6 | היעדר disclosure של AI | **תיקון:** „בדיקה תפעולית מבוססת ראיות · לקריאה בלבד" **אינו** טוען את ההפך מ-AI — הוא תואם גם לתשובה שנוצרה ב-AI. ⇒ **בחירת מוצר**, לא הצהרה שקרית. **דורש הכרעה** |
| 9.7 | הבחנה בין פרוזה לטענה | **תיקון:** ההבחנה **קיימת מבנית** — claim מקבל כרטיס, `<dl>` וקישורי מקור (`AnswerView.tsx:300-335`). ⇒ להגדיר ליקוי מדיד או **להסיר את הממצא** |
| 9.8 | שש הצעות → שלוש + חשיפה | **הכרעת בעלים קיימת** על שש — הפיכתה דורשת הכרעה חדשה |
| 9.9 | „משתמש חוזר לעולם לא רואה" | **תיקון:** שיחה משוחזרת **כן** מציגה `בדיקה חדשה` שמחזיר את ההצעות. ⇒ „ההצעות אינן מופיעות **אוטומטית**" |
| 9.10 | 10 מתוך 19 קודי שגיאה בלי דרך קדימה | מקובל |
| 9.11 | `p_note` מקודד ל-`null` (`client.ts:151-157`) | מקובל |
| 9.12 | החלון העגון צף מעל התוכן; ההערה טוענת ש-`#main` שומר רוחב — **הכלל לא קיים** | מקובל |
| 9.13 | 11 גופי אור, גרדיאנט נודד, טשטוש — סותר את חוקת הפרויקט | **הכרעת בעלים קיימת.** ושינוי שפה ויזואלית מחייב `src/index.css` **ו-`DESIGN.md` יחד** |
| 9.14 | clamp 260px | **תיקון: אינו חיתוך ללא מוצא** — זה disclosure עם „הצג עוד", **לפי הכרעת בעלים מתועדת** ב-`CollapsibleAnswer.tsx:7`. **לא לשנות בלי הכרעה חדשה וראיית שימוש** |
| 9.15 | אייקון undo על פעולה יוצרת; `רשומת עזרה` בממשק | מקובל |

---

### P10 — מעבר ניסוח · **תוקן: לא 2 מילונים בלבד**

‏10.10 משנה במפורש `PriceLists.tsx`; ‏10.9 משנה את מבנה `FileUpload`. ⇒ **לספור קובצי מוצר
ובדיקות, או להוציא את שינויי ה-interaction לחבילה אחרת.**

| # | מה |
|---|---|
| 10.1 | **מילים שאינן עברית:** `רגה`/`רגות` — תרגום מילולי של `rungs` מהערות הקוד (`he.ts:1475`, `:1480`) |
| 10.2 | ז'רגון צנרת בכרזה שכל משתמש רואה: `„החילוץ נשמר, אך שלב הפענוח לא הצליח…OCR מחדש."` — ארבעה מונחים פנימיים במשפט אחד |
| 10.3 | `„…אפשר לנסות לאשר — השרת יבדוק שוב…"` — „השרת" אינו בן שיח |
| 10.4 | שגיאות אחסון מילה במילה: `הקצאת העלאה`, `נתיב ההעלאה`, `רישום המסמך` |
| 10.5 | אוצר קבלות ופירושים; חודש בפורמט `2026-09` גולמי |
| 10.6 | 19 הודעות פירוש, שלוש **בלי כפתור** (`DocumentReview.tsx:249-262`) |
| 10.7 | `„המוצר טרם הגדיר את הכלל העסקי…"` — משפט על צבר הפיתוח, מופנה לבעל עסק |
| 10.8 | הסבר בן 28 מילים בין המשתמש לשדה הקובץ |
| 10.9 | חלון בן כפתור אחד לתמונות iPhone — **שינוי מבנה, לא ניסוח** |
| 10.10 | טקסטים עבריים ישירות בקוד (`PriceLists.tsx:210`, `:211`, `:232`, `:432-435`) בזמן שהמילון מחזיק מפתחות לא מנוצלים — **נוגע בקובץ מוצר** |
| 10.11 | **צומצם.** אחוז הביטחון **כבר תוקן במסך הבדיקה** — `confidenceLabel` מחזיר `זוהה בבירור`/`חלקית`/`לא ודאי` ובדיקות אוסרות אחוזים. הגולמי נשאר **רק במשטח הפיקוח על שיוך אוטומטי** |
| 10.12 | תקרת 10MB לפני הבחירה, לא כדחייה |

---

## 6. הכרעות בעלים

**שתיים נמחקו — כבר הוכרעו.** אחת הודחה. שבע חדשות נוספו מסבב 1.

| # | השאלה | המלצה | חוסם |
|---|---|---|---|
| ~~D-2~~ | תעודת משלוח בלי הזמנה | **הוכרע ב-`#125`.** לצטט ולממש picker | — |
| ~~D-8~~ | שמות מוצרים הפוכים | **הוכרע ב-`#231`** — re-import + preview + אישור אדם | — |
| **D-1** | כרטיס `מה ישולם בפועל` | **הודח מהכרעה עסקית ל-היפוך מתועד.** הקוד מגן על ההצגה בהערה, אז מוחקים **עם נימוק כתוב** — אבל **זה לא חוסם את שאר P2** | 2.1 בלבד |
| **D-3** `#358` | לאן עוברת קונסולת האילוף | ‏`/documents/operations` — **אבל זו חבילה נפרדת** (query+UI+authz), לא צעד ב-P4 | P4.1–4.2 |
| **D-4** `#359` | סריקה שנכשלה | **הבינאריות שגויה.** לפצל לפי error code; „מסגרת מלאה" רלוונטית רק לכשל גאומטרי. להצליב עם `#70` ו-`#247` | P8 |
| **D-5** `#360` | מיזוגי סטטוס | **אינה הכרעה אחת — matrix נפרדת לכל מיזוג.** ‏5.2/5.3/5.7/5.8 מקובלים; ‏5.1/5.4/5.5/5.9 שנויים | P5 |
| **D-6** `#361` | עריכת סכומים שנקראו | **כן** — אבל ההכרעה חייבת לכלול גם **מי מנצח בזריעת המחירון** ואת כללי ה-precision | P7 |
| **D-7** `#362` | הרחבת ערכי עזרה מ-`owner` | **לאשר entry-by-entry**, לא גורף לפי route | P9b |
| **D-9** `#363` | **חדש.** סמכות רמז הספק מההעלאה — האם ניחוש-אדם קודם לקריאת-מכונה? | provenance מפורש; ניחוש מסומן כניחוש | P3.8 |
| **D-10** `#364` | **חדש.** eligibility להזמנה ידנית — cancelled/closed/fully-received/ישות/מטבע | לנעול מול חוזה השרת | P3.4 |
| **D-11** `#365` | **חדש.** בזיכוי, האם ספק ידני פותר `credit_resolution`? | לא, אלא אם הוכרע אחרת | P3.9 |
| **D-12** `#366` | **חדש.** בהעלאה חוזרת אחרי כשל — מה קורה למסמך הישן? | supersede, המקור נשמר immutable | P8.3 |
| **D-13** `#367` | **חדש.** מקור-קודם במובייל — האם המסמך מעל ההחלטה? | כן | P6.1 |
| **D-14** `#368` | **חדש.** ‏disclosure של AI בעוזר | להוסיף | P9d |
| **D-15** `#369` | **חדש.** היפוך שלוש הכרעות בעלים קיימות: שש ההצעות, ה-clamp, והכרטיס הצף + glass | **לא להפוך בלי ראיית שימוש** | P9d |

---

## 7. סיכום היקף — **union, לא סכום**

הגרסה הראשונה חיברה חבילות וקיבלה מספר מנופח. שלושה קבצים חופפים בין P1 ל-P2.

| חבילה | קובצי מוצר | מילון | הכרעה חוסמת | סיכון |
|---|---:|:---:|:---:|:---:|
| P1 נכונות | 5 | — | — | נמוך |
| P2 צמצום | 9 | ✓ | D-1 (2.1 בלבד) | **בינוני** (2ב אינן מחיקות) |
| P3 ספק | 6 | ✓ | D-9, D-10, D-11 | **בינוני-גבוה** |
| P4 קונסולה | 2 | ✓ | D-3 | בינוני |
| P5 אוצר מילים | 16 | ✓✓ | D-5 (matrix) | **בינוני-גבוה** |
| P6 פריסה | **6** | — | D-13 | בינוני |
| P7 עריכה | 2 | — | D-6 | **גבוה** |
| P8 סריקה | 2 | ✓ | D-4, D-12 | **גבוה** |
| P9a–d עוזר | ~10 | ✓ | D-7, D-14, D-15 | בינוני |
| P10 ניסוח | 2 + מוצר | ✓✓ | — | נמוך |

**‏P1 + P2א** — **11 קובצי קוד ייחודיים** (13 עם שני המילונים), הכרעה אחת שנוגעת לפריט אחד.
זו נקודת הפתיחה: חמשת באגי הנכונות ושש המחיקות האמיתיות.

---

## 8. מה התוכנית **לא** מכסה

- **שמות המוצרים ההפוכים** — `#231`, מסלול נפרד.
- **‏21 כשלי עיבוד מ-03.09** ו-**12 מסמכים שממתינים לסריקה** — אינם נובעים מהזרימות כאן.
- **תיקון `CLAUDE.md`** — סעיף 0. קומיט נפרד.
- **‏`workflow_dispatch` חסר `render=true`** (`quality-gate.yml:77-83`) — תיקון נפרד.
- **‏`he.ts` ו-`en.ts` מתוחזקים יחד.** כל שינוי ניסוח הוא **פעמיים**.
