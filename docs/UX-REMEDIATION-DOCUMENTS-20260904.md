# תוכנית תיקון — העלאת מסמכים והעוזר

**נוצר:** 04.09.2026 · **מקור:** ביקורת UX בשישה סוכנים על `main@b48a0b36`
**ענף:** `docs/ux-remediation-documents-assistant` · **סטטוס:** P1 + P2א מומשו ואומתו מקומית; CI/PR טרם נפתחו; P3+ מחוץ להיקף
**סבבי ביקורת נגדית:** ‏5 (Codex; תיקוני סבב 5 התקבלו ואז הבעלים בחר להתחיל בבנייה) · יומן מלא: `UX-REMEDIATION-REVIEW-LOG-20260904.md`

---

## 0. ארבע טענות ב-`CLAUDE.md` שהתיישנו

הגרסה הראשונה ירשה אותן מהחוקה ובנתה עליהן שערי קבלה. **כולן נמדדו ב-04.09.2026.**
**‏`CLAUDE.md` תוקן בקומיט נפרד `f6b7decd`**, אחרי מדידה חוזרת של כל ארבע הטענות.

| מה החוקה אומרת | מה נמדד |
|---|---|
| „‏13 מתוך 27 שומרי `verify` אינם רצים ב-CI" | **‏`verify` מריץ 32; ‏`build.yml` מונה את אותם 32 בשמם** |
| „‏`check:contrast` נכשל על `main` ומעולם לא עבר" | **עובר.** ‏exit 0, ‏53 צמדי טקסט, 7 non-text, 1 חוזה כיוון |
| „‏PR שבסיסו אינו `main` מקבל אפס בדיקות (‏§65)" | **‏§65 נסגר 02.09.2026** — שני ה-workflows עם `pull_request:` בלי מסנן ענף |
| „‏`workflow_dispatch` מריץ את כולם" | **לא.** חסר `render=true` (`quality-gate.yml:77-83`) |

> **הבחנה שנוספה בסבב 2 ומשנה את המשמעות:** ‏**מלאי הפקודות זהה — הביצוע אינו.**
> ‏CI מפעיל את 32 הפקודות דרך path filters; הריצה המקומית מפעילה את כולן תמיד.
> ‏`npm run verify` המקומי הוא עדיין **הרחב מבין השניים בפועל**, גם כשהרשימה זהה.

---

## 1. היקף

תוכנית מימוש נגזרת מביקורת שכבר בוצעה, שעברה **שני** סבבי ביקורת נגדית מול הקוד.
ציטוטים שנמצאו שגויים תוקנו במקומם וסומנו. **מדידת היקף בקבצים ואתרי קריאה, לא בזמן.**

**תיקון מסבב 2 — התוכנית כן נוגעת בשכבת הנתונים.** הגרסאות הקודמות טענו שלא. בפועל:
‏P3.6 דורש read model · תיקון ה-audit ביצירת ספק דורש RPC · P8.3 דורש פקודת supersede ·
ו-D-9/D-10/D-11/D-12 עשויות לשנות חוזי שרת. הסיווג תוקן בסעיף 4.

### מספרים — **לא לתפוס מראש**

| סוג | פנוי (04.09, 16:20) |
|---|---|
| הכרעת בעלים | **`#358`** — **וזה המספר היחיד הידוע כפנוי** |
| סעיף חוב | **`§119`** (היה `§117` בגרסה 1 — התיישן תוך שעתיים) |
| מיגרציה | `0315` |

> **תיקון מסבב 2:** ‏`#359`–`#369` שהופיעו בגרסה 2 **אינם תפוסים** — הכלי אינו סופר מספר
> עד שהוא נכתב ל-`OPEN-DECISIONS.md`. **להריץ `next-number` טרי לפני כל הקצאה**, ולכתוב
> את השורה מיד. ‏`next-number` גם מזהיר כש-`origin` refs מיושנים בגלל fetch חסום.

### הכרעות שכבר קיימות — לא לפתוח מחדש

- **`#125`** (09.08.2026) — איך נבחרת הזמנה לתעודת משלוח: שלוש דרגות ראיה; אפס או ריבוי →
  `order_unresolved` והמסמך **ממתין לאדם**. בורר ההזמנות הוא **המימוש**, לא שאלה.
- **`#231`** — שמות מוצרים הפוכים: re-import מהמקור, preview, אישור אדם לכל שינוי.

---

## 2. העובדה שמשנה את סדר העדיפויות

`העלאת מסמך` = **7 פקדים**. `העלאת מחירון` = **6**. שניהם בתוך תקציב חוק היק.

| מסך | גובה | מסכי גלילה | פקדים | חלוניות | כותרות |
|---|---:|---:|---:|---:|---:|
| אישור מסמך · דסקטופ | 3196px | **3.6** | 32–72 | **24** | **14** |
| אישור מסמך · טלפון | 4081px | **4.8** | 30 | 21 | 14 |
| אישור מחירון · 74 שורות | — | — | **~266** | 14 | — |
| תיקיית המסמכים · 4 מסמכים | 900px | 1.0 | **37** | 6 | 1 |

---

## 3. גרף התלויות

**תוקן בסבב 2:** הגרסה הקודמת ציירה `P8 ↔ P6` — מחזור, לא DAG. הכיוון נקבע:
**`P6 → P8`.** ‏P6 מסדר מחדש את סביבת העבודה; P8 עובד **בתוכה**.

```
   חוזה מילון (he.ts + en.ts) — נסגר לפני כל השאר
   ובעלות שלו כוללת את P1..P10 כולן, לא רק P2/P5/P8/P9
                    │
   P1 ──────────────┤   (עצמאי מלבד המילון)
   P4 ──► P4b?      │   (P4b קיים רק אם D-3 מאשרת העברה)
                    │
   P2 ──► P6 ──► P8 │
    └──► P3 ──► P7  │
                    │
   P9a · P9b · P9c · P9d   (חולקות AnswerView ו-he.ts)
                    │
                    └──► P10 (אחרון תמיד)
```

**תוקן בסבב 2:** ‏P3 ו-P6 **גם הן** נוגעות במילון. חוזה המילון מכסה את כולן, או שכל
עריכות המילון מרוכזות בחבילת integration אחת.

---

## 4. שערי קבלה ורולאאוט

| # | שער | מדידה |
|---|---|---|
| G1 | אין רגרסיה סטטית | `npm run verify` — 32 תת-פקודות. **מלאי זהה ל-CI, ביצוע רחב יותר** (CI מסנן לפי נתיב) |
| G2 | טיפוסים ובאנדל | `npm run build` |
| G3 | מילון עקבי | `check:i18n`, `check:orphan-keys`, `check:plurals` |
| G4 | ניגודיות | `check:contrast` — **שער חוסם רגיל, עובר היום** |
| G5 | הצפיפות ירדה | ספירת פקדים/חלוניות/בלוקי טקסט לפני ואחרי, **דסקטופ ומובייל** |
| G6 | הזרימה עוברת | העלאה → סטטוס → אישור, כולל „אין ספק" |
| G7 | שער האינטגרציה | אוטומטי על ה-PR. ‏`workflow_dispatch` רק לריצת כל-הנתיבים — ואז לזכור ש-`render=true` חסר |
| G8 | הוכחה במסלול השמור | עריכה כספית מוכחת דרך המסלול המוגן, **לא** `psql` כ-`postgres` (עוקף את שומר הכספים ו-RLS) |

### רולאאוט לכל חבילה — **תוקן בסבב 2**

| חבילה | שורת מטריצה |
|---|---|
| P1, P2, P5, P6, P10 | **Frontend / נכס ציבורי** |
| **P3** | **Frontend + Migration/DB** — read model ל-3.6, ו-RPC ליצירת ספק (ראה החסם למטה) |
| **P4** | **מותנה ב-D-17:** per-annotation ⇒ Frontend · document-level ⇒ **Frontend + Migration/DB** |
| **P4b** | קיים רק אם D-3 מאשרת. ‏**להוכיח Frontend-only קודם** — אם הטבלאות ו-RLS הקיימים מספיקים, אין צורך ב-Migration; רק אם הם אינם מספיקים, `/documents/operations` מקבל read model והחבילה הופכת **Frontend + Migration/DB** |
| **P7** | **Frontend.** ‏**נעול** — ראה החוזה ב-P7 |
| **P8** | **Frontend + Migration/DB** — חוזה supersede ל-8.3. ‏**8.1 ננעל ל-Frontend בלבד**: החלופה „קוד שרת חדש" נמחקה, כי קוד scan גאומטרי חדש היה גורר **Worker + Edge + פריסת VPS** והוכחת `job_claimed` |
| **P9b** | **Frontend + Edge** — `routeAccess.ts` ו-`productHelpRegistry.ts` מיובאים לפונקציית `assistant` (`evidence-authorization.ts:8`, `index.ts:74`, `live-evaluation.ts:6`). חוזי Deno **ופריסת Edge**, ואימות בקריאה חיה |
| P9a, P9c, P9d | Frontend |

**Frontend בייצור:** בנייה עם env ייצור, סריקת סודות, **`wrangler pages deploy --branch=main`**
(בלי הדגל מתקבל preview שמדפיס „Deployment complete" בזמן שהכתובת החיה מגישה את ה-build
הקודם — לאמת ב-`canonical_deployment` מול ה-API), התאמת hashes, smoke קנוני, `check:live-seo`.

**Migration בייצור:** גיבוי schema/data/roles, dry-run + ledger, apply forward-only, postflight.
**להשתמש ב-`scripts/rollout-apply.ps1`**, שמחיל מיגרציה **וכותב את ה-ledger** בסדרה —
במקום `db-query.ps1` ידני, שמחיל SQL אך **אינו** רושם ב-`schema_migrations` ומשאיר את
שורת ה-ledger לזיכרון של מי שהריץ.

### שערי קבלה ספציפיים לחבילה — **נוספו בסבב 3**

‏G1–G8 הם רוחביים ואינם מוכיחים את מה שכל חבילה באמת משנה. לכל חבילה טענות
שחייבות **ראיה ריצתית**, ממופות לשער הרוחבי שהן מחדדות. **אף חבילה אינה מדווחת כגמורה
בלי השורות שלה כאן** — כולל P4b, P9c ו-P9d, שנשכחו בגרסה הקודמת ולכן היו מאפשרות
להכריז „בוצע" בלי להוכיח שהערת המשוב נשמרה, שה-glass ירד, או שהקונסולה נשארה owner-only:

| חבילה | טענה שחייבת ראיה | שער |
|---|---|---|
| **P1** | כשלון מכוון → לחיצה מוצלחת על ההעלאה, **ואפס העלאה כפולה** של אותו קובץ | G6 |
| **P1** | הודעת הכשל מציגה משפט עברי, **ואף קוד פנימי אינו מופיע בטקסט הגלוי** | G6 |
| **P1** | העלאה בלי פתיחת השדה המקופל → המסמך **אינו** נושא את תאריך היום | G6 |
| **P2** | ספירת חלוניות/פקדים/בלוקי טקסט לפני ואחרי, **בשתי מידות מסך** | G5 |
| **P2** | `/inbox?filing=unfiled` ממשיך להגיע לאותה תוצאה אחרי ההמרה | G6 |
| **P3** | יצירת ספק כותבת `audit_logs` **עם `reason` שאינו null** — או שהמסלול אינו קיים כלל (לפי D-16) | G8 |
| **P3** | מסמך בלי ספק → בחירה → אישור, **דרך המסלול השמור** ולא כ-`postgres` | G8 |
| **P3** | ‏`supplier_unresolved` **נקרא בפועל** ומופיע במסך — לא רק עובר typecheck | G6 |
| **P4** | משוב על הצעה **עדיין נשמר** אחרי הסרת הפקדים | G6 |
| **P5** | **תוקן בסבב 4 — לא „recovery לכל state".** ‏`processing`/`review`/`assigned`/`completed` **אינם** מצבי recovery, ודרישה גורפת הייתה מוסיפה פעולות מיותרות. הראיה הנדרשת: **מטריצת `state → wait / retry / review / file / no-action`**, עם פעולה **רק** במקום שהמשתמש באמת יכול לפעול | G6 |
| **P5** | `הועלה אך לא נרשם` ו-`נרשם — העיבוד לא החל` **נשארים נפרדים**, עם פעולת retry שונה לכל אחד | G6 |
| **P6** | `המסמך המקורי` ב-`y < 1200` במובייל; „הקובץ נשמר" ו„הנתונים אושרו" **נשארים שני משפטים** | G5 |
| **P7** | עריכת מחיר → אישור → **המחירון נזרע במחיר המנורמל שהשרת החזיר**, לא בזה שהמכונה קראה | G8 |
| **P7** | **מסמך במטבע שאינו ברירת המחדל של הספק** → גם `supplier_products.currency` וגם `price_history.currency` **שווים למטבע המסמך**. זו הראיה ל-7.6 | G8 |
| **P7** | מטריצת הקלט של 7.3 — כל שורה בה מודגמת: `1,234.50` מתקבל · `1,5` נדחה · `₪12.50` מנורמל · מטבע סותר נדחה · שלילי נדחה | G8 |
| **P7** | **נוסף בסבב 5.** שורה עם `baseline_price` קיים → אחרי האישור, `supplier_products.current_price`, המטבע ו-`price_history` **ללא שינוי**, והשורה **לא נשלחה** לייבוא | G8 |
| **P8** | **תוקן בסבב 5 — ששת הקודים אינם הרשימה המלאה.** ‏`SCAN_FAILURE_KEYS` (`DocumentScanPreview.tsx:33-40`) מונה שישה, אבל המסד כותב `status='failed'` גם עם `claim_attempt_limit_exceeded` (`0136:733-738`) ו-`document_deleted` (`0136:741-749`), ועוד קוד worker דרך regex (`0136:1153-1166`) — וכולם נופלים ל-`failureUnknown`. ⇒ השער חייב לכסות **כל `status === 'failed'`**: אף אחד מהם אינו מציג עורך פינות, ולכל קוד **ולמצב unknown** יש פעולה מוגדרת או no-action מפורש | G6 |
| **P8** | העלאה חוזרת → המסמך הישן **supersede**, המקור נשאר קריא | G8 |
| **P9a** | כשלון בשאלה שנשלחה מהצעה → **התאוששות בלי רענון דף** | G6 |
| **P9b** | „המסמך לא מצא ספק, מה עושים?" → תשובה **עם קישור**, בקריאה חיה אחרי פריסת Edge | G6 |
| **P9b** | חותמת העזרה עוברת את סכימת `Fact.as_of` (datetime עם offset) | G1 |
| **P9c** | **נוסף בסבב 4.** הערת המשוב **נשמרת ונקראת חזרה** — לא רק שהשדה מופיע | G6 |
| **P9d** | **נוסף בסבב 4.** ‏`backdrop-filter` וגופי האור **אינם קיימים ב-DOM**; ‏disclosure של AI **מוצג**; והחלטת D-18 על הכרטיס הצף **יושמה או נדחתה במפורש** | G5 |
| **P4b** | **נוסף בסבב 4.** הקונסולה **נשארת owner-only** אחרי ההעברה — כניסה בתפקיד אחר נדחית | G8 |
| **P10** | `check:orphan-keys` נקי בשני המילונים | G3 |

**שערי מסלול-שלילי לחבילות שנוגעות ב-DB — נוספו בסבב 4, ותוקנו בסבב 5.**

הניסוח הקודם החיל ארבע דרישות אחידות על „כל חוזה DB", **כולל read model** — ובכלל זה
`replay אידמפוטנטי` ו-`audit_logs עם reason`. **זו הייתה דרישה שגויה שהייתה מייצרת מימוש
גרוע:** קריאה אינה פעולה כותבת, אין לה מפתח אידמפוטנטיות, **ואסור לייצר שורות audit רק
כדי לעבור שער.** הדרישות מופרדות לפי סוג החוזה:

| סוג חוזה | מה חייב להיות מוכח |
|---|---|
| **Read model** (‏P3.6, ‏P4b אם ייבנה) | בידוד דייר · נראות לפי תפקיד · **אותה תוצאה בקריאה חוזרת** · **אפס כתיבות ואפס audit** |
| **פקודת כתיבה** (‏RPC ליצירת ספק · eligibility בשרת · supersede של P8 · חוזה feedback אם D-17 תבחר document-level) | דחיית תפקיד · דחיית cross-tenant · **replay אידמפוטנטי** · **`audit_logs` עם `reason` שאינו null בהצלחה** |
| **קריאה שנדחתה** | **אפס כתיבות עסקיות ואפס audit** — אלא אם קיים חוזה security-event מפורש |

**‏D-4, D-6, D-9, D-10, D-11, D-12** — **לכל אחת מטריצת רולאאוט משלה**, כי כל אחת עשויה
להוסיף שורת Migration בהתאם לתשובה.

---

## 5. החבילות

### P1 — תיקוני נכונות · 5 קבצים

| # | מה | איפה | תיקון |
|---|---|---|---|
| 1.1 | hook של React **בתוך תנאי, בפונקציה שאינה רכיב**. קבלה פגומה → `Invalid hook call…` באנגלית במקום המשפט העברי, כשצריך לדעת אם המחירים נשמרו | `PriceListReviewConfirmation.tsx:181` | מחיקת השורה |
| 1.2 | קוד שגיאה פנימי בסוף משפט עברי | `FileUpload.tsx:865-866` | `errorText(new Error(failures[0].code))` |
| 1.3 | כל העלאה מהתיקייה **חותמת את היום** כתאריך המסמך ומדכאת את סימון `(העלאה)`. מסלול המצלמה שולח `null` ומסומן נכון — שתי דלתות, שתי טענות סותרות | `DocumentsInbox.tsx:297`, `:310`, `:829` | אתחול ל-`''` |
| 1.4 | אי אפשר להתחיל batch חדש ואי אפשר לנטוש retry | `FileUpload.tsx:960`, `:986-1000`, `:101-116` | ראה למטה |
| 1.5 | כפתור המצלמה מפסיק בשקט להיות מצלמה. מסומן רק ב-`aria-label`/`title`, והסרגל `lg:hidden` — מגע בלבד, שם `title` לעולם לא מוצג | `QuickCapture.tsx:167`, `Fab.tsx:86`, `:93-96`, `:106` | כיתוב **גלוי** ואייקון שמשתנים |

**‏1.4 — תוקן פעמיים.** סבב 1 קבע שהניסוח „מבוי סתום בלי סיבה" מוגזם: סיכום הכשל וכפתור
ה-retry **כן נשארים גלויים**. סבב 2 תיקן את הסיבה הטכנית: ‏`clientUploadKey` **אינו נגזר
מאובייקט ה-`File`** — הוא **UUID אקראי**, וה-resume מאונדקס ב-`WeakMap` לפי **זהות** ה-`File`.
לכן בחירה מחדש של אותו קובץ מאבדת את ה-resume.
**התיקון:** לשמור את המפתח ב**רשומת batch עצמאית**, ולהוסיף **פעולת discard מפורשת** —
בלי אפשרות לנטוש retry, המשתמש בוחר בין להיתקע לבין למחוק מקור ששמור בבטחה.
לכל `storedSafely` נדרש **מצב retention מוגדר**.

**חוזה המימוש שנמדד 05.09.2026:** כל רשומת retry מחזיקה את ה-`File` ואת
`DocumentUploadResume` שלו, כולל `clientUploadKey`, בנפרד מ-batch חדש. ‏discard מוחק רק את
ההצעה לניסיון חוזר מזיכרון הדפדפן; הוא **אינו** קורא למחיקת Storage או למחיקת מסמך. מקור שכבר
נשמר נשאר בדיוק במצב השרת שאליו הגיע, והמסך אומר זאת לפני ה-discard.

---

### P2 — צמצום המסך

**זו אינה „מחיקות טהורות".** מחיקות אמיתיות מופרדות מהחלפות.

#### 2א — מחיקות

| # | מה | איפה |
|---|---|---|
| 2.1 | כרטיס `מה ישולם בפועל` — שתי שורות מקודדות ל-`value={null} … missing` + „…שתי הרגות אינן מחולצות." ‏**חסום ב-R-1** | `ReconciliationStrip.tsx:219-229` |
| 2.3 | המאפיין `meta` — `PageHeader` כבר פותר תיאור אוטומטי ואתר הקריאה מעביר גם וגם | `DocumentsInbox.tsx:878` |
| 2.5 | ההודעה הירוקה — נוכחת גם בהרצה נקייה, מדפיסה `0`, מתנקה רק בבחירה הבאה | `FileUpload.tsx:986-1005` |
| 2.7 | טלמטריה מכל תג: `· עמוד 7 מתוך 27 · 12 דק׳`. **לוודא ש-`עיבוד תקוע` עדיין ניתן לזיהוי** | `DocumentStatusBadge.tsx:33-38` |
| 2.8 | `גרסה: 2` ותג `בקשה חוזרת — ללא כפילות` | `PriceListReviewConfirmation.tsx:1180`, `:1183` |
| 2.10 | תג מונה `היקף הבדיקה` על השורה הסגורה. הקיפול נשאר | `AnswerView.tsx:378` |

**מימוש 05.09.2026:** שש המחיקות הוחלו בלבד. ‏2.1 עדכן יחד את `DESIGN.md`; ‏2.7 משאיר את
`עיבוד תקוע` כתג ואייקון בלי טלמטריית עמוד/גיל בכל שורה; ‏2.8 משאיר את ספירות תוצאת הקליטה;
‏2.10 משאיר את הקיפול ואת תוכנו. ראיית הריצה וה-before/after נמצאות ב-
`artifacts/ux-remediation-p1-p2a-20260904/` והפקודות ב-`docs/ux-remediation-p1-p2a/GATES.md`.

#### 2ב — החלפות

| # | הבעיה שנמצאה בביקורת | מה עושים |
|---|---|---|
| 2.2 | מסנן `סטטוס תיוק` — **דורש להמיר `/inbox?filing=unfiled` ל-processing filter**, ולעדכן `App.tsx` ו**כנראה `documentStatus.ts`** | refactor עם בדיקות התנהגות |
| 2.4 | רשת שמות השדות — אבל `item.values` הוא **גם המקור ל-`guessLineName`** (`:63`), ו-SKU/יחידה/שורה לא-מותאמת אינם כולם בשדות ייעודיים | **סיכום עברי קומפקטי**, לא מחיקה |
| 2.6 | גודל הקובץ — **לא בתים גולמיים**, `UploadCenter.tsx:608` כבר ממיר B/KB/MB | להסיר כי לא ניתן לפעולה; והתקרה (10MB) חייבת להופיע **לפני** הבחירה |
| 2.9 | חותמת `נמדד ל־` — `Fact.as_of` הוא **חלק מחוזה הראיה** | לתקן **רק** את עזרת המוצר (ראה 9.5) |
| 2.11 | כרטיס `מצב המסמך` — **לא תמיד תג יחיד**; בשלבים חיים הוא מכיל גם `DocumentProcessingProgress` (`:95-102`) | לפצל לפי state, בדיקות לכל שלב |
| 2.12 | פרובננס תבנית הייצוא | דחייה מאחורי פעולת הייצוא — **disclosure חדש** |

**יעד:** אישור מסמך מ-24 חלוניות ל-**≤20**, מ-127 בלוקי טקסט ל-**≤105**; תיקייה מ-37 פקדים ל-**≤35**.

---

### P3 — פתרון הספק · **Frontend + Migration/DB**

**התלונה שהתחילה את הכול.**

#### מאומת

- כרטיס `הספק` מציג `לא זוהה מהמסמך` / `יותר ממועמד אחד — נדרשת בחירה` ורשימת מועמדים
  כ-`<li>` **שאינם ניתנים ללחיצה** (`DocumentAssessmentPanel.tsx:322-344`).
- טקסט אחר מנחה `…ולזהות את הספק למעלה` (`DocumentLineMapping.tsx:283-287`).
- `supplierId` לקריאה בלבד · `canSubmit` ← `false` · הכפתור `disabled` **בלי רמז**.
- מקור `supplier_unresolved` הוא **`0109_document_review_assessment_read.sql:159`**;
  `assessment.ts:147` הוא רק הטיפוס. **אין קורא ב-`src/`** — נכון.
- `QuickSupplierPicker` **אינו** שולף את הרשימה; `useQuickSupplier` (`:116`) מקבל אותה
  **מהקורא** וממזג יצירות מקומיות. ⇒ נדרשת שאילתה tenant-scoped ומצבי loading/error.

> ### ⚠ חסם לפני 3.1 — **אומת, לא השערה**
>
> יצירת ספק דרך `QuickCreateSupplier` (`:161`) היא **insert ישיר**. הטריגר `suppliers_audit`
> נוצר ב-`0001_init.sql:444-447`; ההגדרה **האחרונה** של `audit_row_change` היא
> `0059_feature_flags.sql:101-130`, וה-insert בשורות **116-125 אינו כולל `reason`**.
> ‏**נבדק: אף מיגרציה מאוחרת יותר אינה מגדירה מחדש את הפונקציה**, ולעמודה אין default.
>
> ⇒ ‏`reason` הוא בהכרח `null`, בסתירה לכלל הברזל „כל פעולה רגישה נרשמת **עם סיבה**".
>
> **שתי דרכים, ויש לבחור אחת — לא להשאיר את שתיהן פתוחות:**
> **(א)** להעביר יצירת ספק ל-RPC tenant-scoped, idempotent ומנומק ⇒ P3 כולל Migration;
> **(ב)** **לא להציע יצירה במסך הזה** ולהסתפק בבחירה ⇒ P3 נשאר Frontend, **ושער P3
> מוותר על תרחיש היצירה.** ‏`D-16` מכריעה.

| # | שינוי | הערה |
|---|---|---|
| 3.1 | להרכיב `QuickSupplierPicker` + שאילתה tenant-scoped | **בחירת** ספק נכתבת דרך `apply_reviewed_document`, שמאמת org/supplier/order/scope/סיבה (`0110:341-355`). ‏**יצירת** ספק היא write **נפרד** ואינה מכוסה על ידו |
| 3.2 | רשימת המועמדים → בחירה | |
| 3.3 | לקרוא את `supplier_unresolved` ולנסח סיבה לכפתור המושבת | מקור: `0109:159` |
| 3.4 | בורר הזמנות — **מימוש `#125`** | eligibility: cancelled/closed/fully-received, ישות משפטית, יחידה, **מטבע** |
| 3.5 | מועמד חסר-שם מוצג כ-UUID → נפילה חזרה אנושית | `:336` |
| 3.6 | `ספק לא מזוהה` כמצב ברשימה | לתיקייה **אין `assessment` state** (`:467-540`) ⇒ **read model מרוכז. אסור RPC לכל שורה** |
| 3.7 | **אבחון, לא מימוש.** הרשימה מציגה `—` בזמן שהאישור מכריז `זוהה · שם ספק תואם במדויק` | חקירה read-only קודם |
| 3.8 | `apply_reviewed_document` **כן** מעדכן `supplier_id` (`0110:487`). הבעיה: **עד האישור הרמז מההעלאה נחשב ראיה סמכותית** | provenance לרמז |
| 3.9 | בזיכוי, ספק ידני **אינו פותר** `credit_resolution` | לבדוק במפורש |

**‏D-10 אינה ניתנת לאכיפה בבורר בלבד** — `apply_reviewed_document` בודק org/supplier/unit
אך **אינו מסנן** cancelled/closed/fully-received (`0110:347-355`). האכיפה חייבת להיות **בשרת**.

---

### P4 — הוצאת קונסולת האילוף

אחד עשר פקדים במסך הלקוח: שם עמודה גולמי (`supplier_name`) כטקסט מצויר, שדה **נערך**
שכותרתו `מפתח משמעות` וערכו אותו `supplier_name`, `תווית`, `סיבת המשוב`, שלושה כפתורים,
תג `כלל v3`, והמילה האנגלית `annotation` בהודעה עברית.

| # | שינוי |
|---|---|
| 4.1 | להסיר את קיפולי `הערות והחלטות` ו-`כללים שהופעלו` (`:740-798`) |
| 4.2 | להסיר את שדות העריכה ואת שלושת הכפתורים (`:451-480`, `:545-570`) |
| 4.3 | **חייב להיסגר לפני התחלה.** „קישור אחד" **לא אפשרי** — `add_document_feedback` דורש `annotation.id` ובמסמך יכולים להיות כמה. **לבחור אחת:** ‏(א) פעולה קומפקטית **לכל הצעה**; ‏(ב) חוזה document-level חדש ומבוקר. ‏`D-17` |
| 4.4 | לנקות `annotation` מהמחרוזת (`he.ts:2566` + `en.ts`) |

**‏P4b — קיים רק אם D-3 מאשרת העברה.** ‏`/documents/operations` **אינו טוען** annotations,
feedback או learning rules. „העברה" היא **חבילה נפרדת** (query + UI + authorization) עם
היקף ושערים משלה. אם לא בונים אותה — **P4 היא מחיקה, ויש להצהיר על כך.**

---

### P5 — כיווץ אוצר המילים

**ארבעה מתוך המיזוגים שהוצעו הורסים הבחנה אמיתית.**

| # | מיזוג | פסק | מה עושים |
|---|---|---|---|
| 5.1 | שש מילות „בתהליך" → אחת | **נדחה.** ‏`DocumentProcessingProgress` מפריד **בכוונה** `ממתין בתור` מ-`קריאת המסמך`; ‏`משפר תמונה` **מצהיר שטרם בוצע OCR** | לאחד ניסוחים **בתוך כל state** |
| 5.2 | שלוש תוויות שיוך → `משויך` | מקובל | |
| 5.3 | `סטטוס נטען`/`סטטוס לא זמין`/`הוחלף בניסיון חדש` → שום תג | מקובל | |
| 5.4 | `העיבוד נכשל` + `עיבוד תקוע` | **נדחה.** ‏`failed` מציג retry; ‏`stuck` **אינו נכלל** ב-row retry ואינו מקבל כפתור. הפעולה **אינה** זהה | **קודם** recovery אחיד, אחר כך תווית |
| 5.5 | שמונה מצבי העלאה → שלושה | **נדחה — בטיחות כספית.** ‏`הועלה אך לא נרשם` = **אין registry row**; ‏`נרשם — העיבוד לא החל` = **אסור להעלות שוב** | להחליף ז'רגון במילים אנושיות, **לשמור את ההבחנה ואת פעולת ה-retry לכל אחד** |
| 5.6 | ההפניה השבורה `„…במרכז תפעול המסמכים"` — טקסט ולא קישור; **אין מסך בשם הזה**; ולבעלים בלבד | מסך הבדיקה פתוח ל-`owner\|office` (`App.tsx:172`) ⇒ **הנפגעת היא `office`. רואה החשבון אינו רואה זאת** | |
| 5.7 | לסלק אוצר מילים של תור | מקובל | |
| 5.8 | למחוק `„מספר המקטעים עדיין לא ידוע."` | **אינו deletion טהור.** ‏`chunkCountUnknown` הוא ה-fallback **הפעיל** של `DocumentProcessingProgress.tsx:100-109` | **להגדיר ניסוח חלופי באותו state**, לא למחוק את המפתח |
| 5.9 | `שליחה לעיבוד`/`עיבוד מחדש` · `הסרה`/`הסרה עם השפעה` | **הראשון מקובל; השני נדחה** — הסרה מהארכיון היא soft delete בלי סיבה, ואילו „עם השפעה" **מבטלת נגזרות, דורשת סיבה, ונחסמת על תלות כספית** | לאחד רק את הראשון; לשני — לתאר את ההשלכה **כבר בתפריט** |

---

### P6 — סדר מסך האישור · 6 קבצים, **לא layout בלבד**

| # | שינוי | הראיה |
|---|---|---|
| 6.1 | `המסמך המקורי` לראש העמוד **בטלפון** (`DocumentReviewWorkspace.tsx:162`, `:176`) | העמוד 4081px; המסמך ב-**y=3579** — 4.2 מסכים אחרי 13 חלוניות. `אישור המסמך` ב-y=1437 ומתחתיו **שבע** חלוניות. **מבקשים לאשר לפני שמראים** |
| 6.2 | משקל ויזואלי להחלטה | **תוקן בסבב 2 — טיעון ה-LOC נמחק.** הטענה היחידה שנשארת נמדדת ב-DOM: `PrimaryDecision` **אינו מצייר מסגרת, רקע או כותרת**, וההחלטה יושבת בכרטיס התוכן **היחיד בלי כותרת** |
| 6.3 | לקפל את סולם ההתאמה כשהמספרים מסתדרים | **התנאי חייב להיות מדויק:** `gapKnown && !overTolerance && missing_rungs.length === 0`. **חוסר נתון אינו התאמה.** לנעול את מקרי missing/unknown בבדיקות |
| 6.4 | לכווץ את ההכרזות של „עדיין לא אושר" | **אסור למזג „הקובץ נשמר" עם „הנתונים אושרו"** — `storageAndApprovalKeys` (`assessment.ts:382`) מגדיר אותם כ**שני צירים**. לנקוב במקור הקנוני **לכל ציר** ולהסיר רק כפילויות בתוכו |
| 6.5 | כרטיס `סוג המסמך` → שורה + קישור | כותרת המשנה שלו אומרת בעצמה „…אין צורך באישור ידני." |
| 6.6 | לאחד את ההרכבה הכפולה (`DocumentReview.tsx:264-274`, `:289-299`) | רשימת מאפיינים זהה בת 8 שורות פעמיים |

**יעד:** טלפון מ-4.8 מסכים ל-**≤3.0**; דסקטופ מ-3.6 ל-**≤2.5**; `המסמך המקורי` ל-`y < 1200`.
**‏RTL:** properties לוגיים בלבד.

---

### P7 — עריכת ערכים שנקראו · **חסום**

`ReviewedLineEdit` = **שלושה שדות מספריים ו-`unit` טקסטואלי** (`assessment.ts:393-399`).
הטבלה מציגה כרגע **רק** `quantity` ו-`unit price`.

> ### ⚠ החוסם — וגם התיקון הראשון לו היה מסוכן
>
> `priceSeedRows` (`assessment.ts:490`) קורא `edits[...].product_id` בלבד, ואז לוקח
> `line.normalized_unit_price ?? line.unit_price` — **מתעלם מ-`edits.unit_price`.**
> ⇒ החשבונית תירשם במחיר המתוקן, **ומחירון הספק ייזרע במחיר שהמכונה קראה.**
>
> **סבב 2 פסל גם את התיקון הראשון:** להשתמש ב-`edits.unit_price` הגולמי **אינו מספיק**
> כאשר `unit` השתנה — המחיר שצריך להיזרע הוא `normalized_unit_price` **שמחושב מחדש בשרת**.
>
> **התיקון הנכון, ננעל בסבב 3 לדרך אחת:** לזרוע **רק** מה-assessment שהשרת מחזיר **אחרי**
> `apply_reviewed_document`, ואז לקרוא ל-`import_supplier_prices` כרגיל.
>
> **החלופה „להעביר את הזריעה לאותה פקודת שרת" נמחקה** — היא הייתה שוברת חוזה מתועד.
> `DocumentAssessmentPanel.tsx:216-225` קובע במפורש: ‏`import_supplier_prices` **בעלת
> הכתיבה היחידה** ל-`supplier_products` (‏`0023` שלל DML ישיר), אין RPC שמשתרע על השתיים,
> **והסדר הוא מה שהופך את הפיצול לכן** — המסמך נרשם ראשון, ולכן כשלון בזריעה משאיר מסמך
> רשום נכון ומחירון ללא שינוי. הסדר ההפוך היה מזיז מחירים לפי מסמך שאיש לא אישר.

| # | שינוי |
|---|---|
| 7.1 | לתקן את מקור הזריעה — **קודם לכל השאר**. ‏`import_supplier_prices` נשארת בעלת הכתיבה היחידה |
| 7.5 | **נוסף בסבב 3:** parser בזמן ריצה ל-assessment שחוזר מ-`apply_reviewed_document`, **לפני** שמשתמשים בו לזריעת כסף |
| 7.2 | להגדיר במפורש אילו עמודות נוספות נבנות ואיך כל ערך נערך |
| 7.3 | ולידציה — **מטריצה נעולה, לא רשימה**: מפריד אלפים תקין (`1,234.50`) **מתקבל** · פסיק עשרוני עמום (`1,5`) **נדחה** (‏`interpretation_number` מסיר פסיקים, ולכן `1,5` היה הופך ל-15) · סימן מטבע **תואם** מנורמל (`₪12.50`) · מטבע **סותר** נדחה · ערך שלילי נדחה · ערך ריק נדחה · `minor_units` נבדק |
| 7.4 | הבחנה ויזואלית בין ערך שנקרא לערך שאושר |
| **7.7** | **מעקה שאסור לאבד ב-refactor — נמצא בסבב 5.** ‏`priceSeedRows` מדלג על שורה כש-`line.baseline_price != null` (`assessment.ts:497-500`). זה **מעקה כספי**: חשבונית **אינה משכתבת מחיר מוסכם קיים** כדי להסכים עם עצמה. ‏`import_supplier_prices` **כן** יעדכן מחיר קיים אם השורה תישלח אליו, ולכן ה-refactor של 7.1 עלול לאבד את התנאי בשקט. **התנאי נשמר במפורש**, ונבדק בשער |
| **7.6** | **חוסם כספי שני, נמצא בסבב 4 ואומת.** ‏`PriceSeedRow` **אינו נושא `currency`** (`assessment.ts:462-468`), ואילו `import_supplier_prices` מקבל `currency` בקלט ו**נופל חזרה ל-`supplier.default_currency`** כשהקורא לא מצהיר (`0298_one_parser_for_a_price.sql:311-327`). ⇒ **מסמך ב-USD של ספק שברירת המחדל שלו ILS ייזרע כ-ILS** — במספר הנכון ובמטבע השגוי, גם אחרי שתיקנו את 7.1. סותר את כלל הברזל „כל סכום נושא מטבע". **התיקון:** ה-parser של 7.5 מפיק גם `currency`; כל `PriceSeedRow` נושא את מטבע המסמך במפורש |

**שער:** G1, G2, **G8**, וסוויטה חדשה. כל עריכה ב-`audit_logs` **עם סיבה**.

---

### P8 — סריקה שנכשלה · **Frontend + Migration/DB** · **הונחה מחדש בסבב 2**

**הממצא שהפיל את התוכנית המקורית:** ‏`document_not_detected` ממופה ל-**`needs_corners`**,
לא ל-`failed` (`0136_document_scan_preprocessing.sql:1163`), וה-worker **כבר מחזיר
`full_frame_fallback`** כשאין polygon אמין.

⇒ **אף אחד מששת קודי `failed` הקיימים אינו „גאומטרי בלבד".** הם corrupt, decompressed-size,
file-size, resource, timeout ו-image-too-small — **מסגרת מלאה אינה מתקנת אף אחד מהם.**

| # | שינוי |
|---|---|
| 8.1 | **להסיר „קרא כמו שהוא" מ-`failed` לגמרי.** ‏**החלופה „קוד שרת חדש" נמחקה בסבב 3** — קוד scan גאומטרי חדש נוצר ב-worker / scan gateway, ולכן היה גורר חוזה Edge, **פריסת ה-VPS מחדש באותו רולאאוט**, והוכחת `job_claimed` ביומן. זו חבילה אחרת לגמרי |
| 8.2 | **למפות כל error code לפעולה אחת.** ‏corrupt/size/resource/timeout דורשים retry או צילום מחדש — **לא עורך פינות.** עורך הפינות אינו אופציה אוניברסלית |
| 8.3 | **אינו „עוד כפתור".** אין callback שמחליף קובץ; העלאה חדשה יוצרת **מסמך חדש**. נדרש חוזה replace/supersede בשרת + audit + סיבה, **תוך שמירת המקור immutable**, והחלטה מה קורה למסמך הכושל ולניווט. ‏`D-12` |
| 8.4 | לכווץ את כרטיס האישור לשתי תמונות, משפט אחד וכפתור שאינו נקרא `אישור והמשך לחילוץ` |

השאלה שהמסך שואל — האם הדף שלם וקריא — **כן** ניתנת לענייה משתי התמונות. זה החלק שעובד.

---

### P9 — העוזר · ארבע חבילות

**מיפוי החבילות — הוחזר בסבב 3** (הטבלה נמחקה בכתיבה מחדש בזמן שהגרף והרולאאוט
המשיכו להשתמש בשמות):

| חבילה | פריטים | רולאאוט |
|---|---|---|
| **P9a** recovery | 9.1 · 9.9 · 9.10 | Frontend |
| **P9b** עזרה והרשאות | 9.2 · 9.3 · 9.4 · 9.5 | **Frontend + Edge** |
| **P9c** משוב | 9.11 | Frontend |
| **P9d** ויזואלי וניסוח | 9.6 · 9.8 · 9.12 · 9.13 · 9.15 | Frontend |

| # | שינוי | הערה |
|---|---|---|
| 9.1 | recovery אחרי כשלון | **לא מבוי סתום מוחלט** — ה-composer נשאר פעיל ושאלה שהוקלדה נשארת ב-state. **רק** שאלה שנשלחה מהצעה אינה מוחזרת, וכפתור `בדיקה חדשה` חסר ⇒ retry ששומר את `submittedQuestion` |
| 9.2 | `/documents` לרשימת ההיתרים (`routeAccess.ts:25-60`) | **Edge** |
| 9.3 | ערך עזרה ל„אין ספק" — היום `אין ספק`/`לא זוהה ספק`/`לשייך ספק` אינם באף ערך | **Edge** |
| 9.4 | להרחיב שמונה ערכים מ-`owner` | **entry-by-entry מול כל פעולה שהוא מתאר** — לא גורף לפי route |
| 9.5 | עזרת מוצר עם `נמדד ל־` שנחתם ב-`ctx.now()` על תוכן סטטי | **תוקן בסבב 2:** ‏`entry.updated_at` הוא `YYYY-MM-DD` בעוד `Fact.as_of` דורש **datetime עם offset** (`contracts.ts:233`). ⇒ **לנרמל לתאריך-זמן תקין**, לשנות את התווית ל„**עודכן**", ולהוסיף בדיקת schema |
| 9.6 | disclosure של AI | „בדיקה תפעולית מבוססת ראיות · לקריאה בלבד" **אינו** טוען את ההפך — הוא תואם גם לתשובה מ-AI. ⇒ **בחירת מוצר.** ‏`D-14` |
| ~~9.7~~ | ~~הבחנה בין פרוזה לטענה~~ | **הוסר.** ‏`ClaimBlockSchema` **מחייב לפחות fact אחד** (`contracts.ts:439-458`), ו-`AnswerView.tsx:300-335` מציג כרטיס, fact ledger וקישורי מקור. הגוון אינו הסימן היחיד — **אין defect מדיד**, רק השערת usability. יחזור רק עם מבחן הבנה או מדידת גבול חזותי |
| 9.8 | שש הצעות → שלוש + חשיפה | הכרעת בעלים קיימת ⇒ `D-15` |
| 9.9 | „משתמש חוזר לעולם לא רואה" | שיחה משוחזרת **כן** מציגה `בדיקה חדשה` שמחזיר את ההצעות ⇒ „אינן מופיעות **אוטומטית**" |
| 9.10 | קודי שגיאה בלי דרך קדימה | **תוקן בסבב 3 — הטענה „10 מתוך 19" אינה מוכחת וכנראה שגויה.** ‏`FALLBACK_CODES` (`AssistantDialog.tsx:34-46`) מקנה קישורים לתשעה, **וכמה מהעשרה הנותרים כן נוקבים בצעד הבא בטקסט** — למשל `rate_limited`, `question_too_long`, `proposal_expired`. ⇒ **לבנות מטריצה `code → message → affordance → מה נדרש`** לפני שנוגעים במשהו. ספירה גורפת אינה ממצא |
| 9.11 | `p_note` מקודד ל-`null` (`client.ts:151-157`) | מקובל |
| 9.12 | **מפוצל בסבב 2.** ‏(א) ההערה על `#main` היא **stale comment שניתן למחוק מיד**; ‏(ב) האם הכרטיס הצף מכסה תוכן — **החלטת UX** | תיעוד ≠ פריסה |
| 9.13 | 11 גופי אור, גרדיאנט נודד, טשטוש | **אינו הכרעה ואינו שייך ל-D-15.** ‏`CLAUDE.md` אוסר „אנימציות מוגזמות · glassmorphism · שטחים דקורטיביים ריקים" **בלי חריג** ⇒ **יורד.** שינוי בשפה הוויזואלית מחייב `src/index.css` **ו-`DESIGN.md` יחד** |
| ~~9.14~~ | ~~clamp 260px~~ | **הוסר.** ‏disclosure מדוד עם „הצג עוד", **לפי הכרעת בעלים מתועדת** (`CollapsibleAnswer.tsx:7`) |
| 9.15 | אייקון undo על פעולה יוצרת; `רשומת עזרה` בממשק | מקובל |

---

### P10 — מעבר ניסוח · **ניסוח בלבד**

**‏10.9 הוצא מ-P10** — חלון ה-HEIC הוא שינוי התנהגות: צריך להחליט אם הקובץ נשלח אוטומטית,
נעצר, או מקבל acknowledgment inline, **ומה תוצאת הביטול**. חבילה משלו.

| # | מה |
|---|---|
| 10.1 | **מילים שאינן עברית:** `רגה`/`רגות` — תרגום מילולי של `rungs` מהערות הקוד (`he.ts:1475`, `:1480`) |
| 10.2 | ז'רגון צנרת בכרזה שכל משתמש רואה — ארבעה מונחים פנימיים במשפט אחד |
| 10.3 | „…אפשר לנסות לאשר — **השרת** יבדוק שוב…" |
| 10.4 | שגיאות אחסון מילה במילה: `הקצאת העלאה`, `נתיב ההעלאה`, `רישום המסמך` |
| 10.5 | אוצר קבלות ופירושים; חודש בפורמט `2026-09` גולמי |
| 10.6 | 19 הודעות פירוש, שלוש **בלי כפתור** (`DocumentReview.tsx:249-262`) |
| 10.7 | „המוצר טרם הגדיר את הכלל העסקי…" — משפט על צבר הפיתוח, מופנה לבעל עסק |
| 10.8 | הסבר בן 28 מילים בין המשתמש לשדה הקובץ |
| 10.10 | טקסטים עבריים ישירות בקוד (`PriceLists.tsx:210`, `:211`, `:232`, `:432-435`) — **נוגע בקובץ מוצר** |
| 10.11 | **צומצם.** אחוז הביטחון **כבר תוקן במסך הבדיקה** (`confidenceLabel`, ובדיקות אוסרות אחוזים). נשאר **רק במשטח הפיקוח על שיוך אוטומטי** |
| 10.12 | תקרת 10MB לפני הבחירה |

---

## 6. הכרעות ותיעודי-היפוך

### היפוכים מתועדים — **לא שאלות עסקיות**

| # | מה | מה נדרש |
|---|---|---|
| **R-1** | מחיקת כרטיס `מה ישולם בפועל` | ההערה ב-`ReconciliationStrip.tsx:217` היא **נימוק תכנוני שניתן להפוך**, לא הכרעת בעלים. ‏**חוסם רק את 2.1.** לעדכן באותו שינוי גם את **`DESIGN.md:1560`**, שמגן על אותו סולם שני |

### הכרעות בעלים

**להריץ `next-number` טרי לפני כל הקצאה ולכתוב את השורה מיד.** ‏`#358` הוא היחיד הידוע כפנוי.

| # | השאלה | המלצה | חוסם |
|---|---|---|---|
| **D-3** | לאן עוברת קונסולת האילוף | `/documents/operations` — **אבל זו P4b, חבילה נפרדת שאינה קיימת היום** | P4 |
| **D-4** | סריקה שנכשלה | **הבינאריות שגויה.** אף קוד `failed` אינו גאומטרי; לפצל לפי code. להצליב עם `#70`, `#247` | P8 |
| **D-5** | מיזוגי סטטוס | **matrix נפרדת לכל מיזוג.** ‏5.2/5.3/5.7 מקובלים; ‏5.1/5.4/5.5/5.8/5.9 שנויים | P5 |
| **D-6** | עריכת סכומים | **כן** — כולל מי מנצח בזריעת המחירון ו-precision | P7 |
| **D-7** | הרחבת ערכי עזרה | **entry-by-entry** | P9b |
| **D-9** | סמכות רמז הספק מההעלאה | provenance מפורש | P3.8 |
| **D-10** | eligibility להזמנה ידנית | **אכיפה בשרת**, לא רק בבורר | P3.4 |
| **D-11** | ספק ידני בזיכוי | לא פותר `credit_resolution` | P3.9 |
| **D-12** | מה קורה למסמך הישן בהעלאה חוזרת | supersede, המקור immutable | P8.3 |
| **D-13** | מקור-קודם במובייל | כן | P6.1 |
| **D-14** | disclosure של AI | להוסיף | P9d |
| **D-15** | **צומצם בסבב 3 לשאלה אחת: שש ההצעות.** ה-clamp ירד כי 9.14 הוסר; ה-glass **אינו הכרעה** — `CLAUDE.md` אוסר glassmorphism **בלי חריג**, והערת CSS או הכרעת עבר **אינן גוברות על החוקה**. או שהוא יורד, או שהחוקה מתוקנת קודם במפורש | P9d |
| **D-16** | יצירת ספק במסך האישור: RPC מנומק, או לא להציע יצירה כלל | ראה החסם ב-P3 | P3.1 |
| **D-17** | משוב על הצעה: per-annotation, או חוזה document-level. ‏**התשובה קובעת גם את שורת הרולאאוט של P4** | ראה 4.3 | P4 |
| **D-18** | **חדש בסבב 3.** הכרטיס הצף של העוזר מכסה רבע מהמסך שאליו הוא בדיוק הפנה (9.12ב). ‏**מחיקת ההערה המיושנת על `#main` אינה דורשת הכרעה** — רק שינוי הפריסה כן | P9d |

---

## 7. היקף — union אמיתי

| חבילה | קובצי מוצר | מילון | חוסם | סיכון |
|---|---:|:---:|:---:|:---:|
| P1 | 5 | ✓ | — | נמוך |
| P2 | **11** (‏9 + `App.tsx` ו-`documentStatus.ts` מ-2.2) | ✓ | R-1 (2.1 בלבד) | בינוני |
| P3 | 6 + read model + RPC | ✓ | D-9, D-10, D-11, **D-16** | **גבוה** |
| P4 (+P4b) | 2 · **P4b נפרדת** | ✓ | D-3, **D-17** | בינוני |
| P5 | 16 **אתרי קריאה** (לא קבצים) | ✓✓ | D-5 | בינוני-גבוה |
| P6 | 6 | ✓ | D-13 | בינוני |
| P7 | 2 (‏`import_supplier_prices` נשארת בעלת הכתיבה היחידה) | — | D-6 | **גבוה** |
| P8 | 2 + **חוזה supersede בשרת** | ✓ | D-4, D-12 | **גבוה** |
| P9a–d | רשימת paths מדויקת **תיסגר אחרי D-14/D-15/D-18** | ✓ | D-7, D-14, D-15, **D-18** | בינוני |
| P10 | 2 מילונים + `PriceLists.tsx` | ✓✓ | — | נמוך |

**נקודת הפתיחה: `P1 + P2א`** — **8 קובצי קוד ייחודיים** (10 עם שני המילונים).
שלושה מששת קובצי P2א כבר נמצאים ב-P1, ולכן זה union ולא סכום.
חמשת באגי הנכונות ושש המחיקות האמיתיות, בהיפוך מתועד אחד.

---

## 8. מה התוכנית **לא** מכסה

- **שמות מוצרים הפוכים** — `#231`.
- **‏21 כשלי עיבוד מ-03.09** ו-**12 מסמכים שממתינים לסריקה** — אינם נובעים מהזרימות כאן.
- **תיקון `CLAUDE.md`** — סעיף 0. קומיט נפרד.
- **`workflow_dispatch` חסר `render=true`** — תיקון נפרד.
- **חלון ה-HEIC** (10.9 לשעבר) — חבילת התנהגות משלו.
- **‏`he.ts` ו-`en.ts` מתוחזקים יחד.** כל שינוי ניסוח הוא **פעמיים**.
