# רולאאוט `0243`→`0267` — סדר הפעולות

**מצב: לא בוצע.** הוכן 31.08.2026. כל המספרים כאן **נמדדו** מול `rkftlbctohswhbbiaqin`
בקריאה בלבד, לא הוערכו.

הרולאאוט האחרון (25.08.2026, `690cd5b`) מתועד ב-`PROGRESS.md`. המסמך הזה בנוי על התקדים
ההוא ועל מטריצת הרולאאוט ב-`CLAUDE.md`, ולא ממציא נוהל חדש.

---

## 1. מה נכלל

| משטח | מה |
|---|---|
| מסד נתונים | 25 מיגרציות, `0243`→`0267` |
| ‏Edge | ‏`render-document` ו-`organization-storage-purge` — **קיימות בריפו ולא פרוסות** |
| ‏Frontend | חייב לעלות **באותו רולאאוט**. ראה §3 |

**לא נכלל:** ‏`worker/ocr` ו-`worker/render` על ה-VPS. שלושת חוזי ה-gateway מסונכרנים בשני
הצדדים (`3`/`3`, `3`/`3`, `1`/`1`), אין העלאת גרסה, ולכן אין פריסה מחדש. אם מישהו נוגע
בחוזה — הכלל ב-`CLAUDE.md` חוזר לתוקף מיד.

---

## 2. מה כבר אומת (31.08.2026, קריאה בלבד)

| בדיקה | תוצאה | למה זה משנה |
|---|---|---|
| ‏CR בגופי פונקציות | **0 מתוך 640** | זו בדיוק הסיבה שהרולאאוט של `0171`–`0205` **נכשל ב-`0181`**: אז 302 גופים אחסנו `\r\n` והעוגנים חיפשו `\n`. היום המצע נקי, ולכן **אין צורך בהחלה מחוץ לסדר** כפי ש-`0209` חייבה אז |
| ‏`npm run check:anchored-replacements` | **עובר** — 258 מיגרציות נסרקו, 82 קוראות גוף חי, **0 קוראים חדשים בלי נרמול** | הרגל השלישית של אותה הגנה: כל 25 המיגרציות שקוראות גוף פונקציה מסירות `\r` בעצמן, ולכן הן **אינן תלויות** בכך שהמצע נקי. שלוש שכבות: `0209` ניקה את מה שהיה, `db-query.ps1` מנרמל בהחלה, והשומר הזה מפיל מיגרציה שתסתמך על זה |
| ‏`npm run verify` | ‏Knip עובר; כשלי ה-Vitest היו **timeout של worker** תחת עומס, וקובץ שנכשל עבר לבדו ב-9 שניות | סביבתי, לא קוד |
| טבלת המיגרציות | 233 שורות, ראש `0242`, **אפס פערים, אפס עודפים** | אף אחד לא החיל שינוי בלי לרשום. הסדר צפוי |
| האובייקטים של `0243`+ | 9 טבלאות, הפונקציות הציבוריות והעמודות — **כולן חסרות** | שום מיגרציה לא הוחלה חלקית |
| חתימות פונקציות | 52 פונקציות בחבילה, 5 כבר קיימות, **לכולן חתימה זהה** | ‏`create or replace` לא ייכשל על שינוי סוג החזרה |
| ‏`user_role` | **לא נגוע** | כלל הברזל נשמר |
| ‏`scope_enforcement_violations()` | **ריק** | נקודת פתיחה תקינה |
| נתונים קיימים מול התקרות החדשות | 4 ארגונים (3 premium, 1 legacy); ≤3 משתמשים פעילים מול 30; 4 סניפים מול 10 | **שום שורה קיימת לא חורגת.** נעילת היכולות לא תיקח כלום מאף אחד |
| ‏PITR | **כבוי.** ‏7 גיבויים לילתיים, האחרון `1528633287` ‏(30.08 22:46Z) | נקודת השחזור בת עד 24 שעות. ראה §4 |

---

## 3. הסדר, והנימוק שלו

```
גיבוי טרי  →  מדידת בסיס  →  0243…0267 לפי הסדר  →  postflight
           →  Edge  →  Frontend  →  smoke חי  →  דף הנחיתה
```

**למה המיגרציות לפני הפרונטאנד, ולא הפוך.** הקוד שרץ עכשיו ב-`app.inplace.digital` **נבדק**:
הוא קורא `get_public_plan_quotas` ו-`management_dashboard_snapshot` בלבד, ואינו מזכיר את
`my_plan_features`, `get_public_plan_features`, `scheduled_payments_outlook`,
`my_benefit_window` או `set_invoice_due_date`. כלומר:

- **פרונטאנד חדש לפני המיגרציות = מסכים שבורים** (קריאה לפונקציות שאינן קיימות).
- **מיגרציות בלי פרונטאנד = חלון של הצגה חלקית.** ‏`0246` מצמצמת את
  `get_public_plan_quotas()` מחמישה מפתחות לשלושה — יוצאים `ocr_pages.monthly`
  ו-`assistant_runs.monthly`, נכנסים `users.max` ו-`branches.max`. מסך המנויים **שכבר
  באוויר** קורא בדיוק אותה, ולכן ישנה את מה שהוא מציג. זה לא קורס, אבל זה חלון שצריך לסגור
  מיד.

לכן: המיגרציות ראשונות, והפרונטאנד **מיד אחריהן**, באותה ישיבה.

`0263` מוסיפה `periodComparison` ל-`management_dashboard_snapshot` ואינה מסירה דבר; הדשבורד
הפרוס מתעלם ממנה. זו התוספת היחידה שאין בה חלון.

---

## 4. שלב 0 — גיבוי ותזמון

**ה-PITR כבוי.** נקודת החזרה היחידה היא הגיבוי הלילתי, שרץ סביב **22:45Z**.

> **המשמעות בפועל:** אם משהו משתבש וצריך שחזור, מאבדים את כל מה שנעשה מאז הגיבוי האחרון —
> עד 24 שעות. לכן **מריצים את הרולאאוט זמן קצר אחרי שגיבוי לילתי הושלם**, ולא באמצע היום.

```bash
node scripts/rollout-preflight.mjs
```

רושמים ביומן: מזהה הגיבוי, החותמת שלו, וכל מספרי הבסיס שהסקריפט מדפיס.

**אין להתחיל אם:** הגיבוי האחרון בן יותר מ-24 שעות, `CR bodies` אינו 0, או
`scope_enforcement_violations()` אינו ריק.

---

## 5. שלב 1 — המיגרציות

**שתי עובדות שקובעות את צורת השלב הזה:**

1. ‏`scripts/db-query.ps1` שולח את הקובץ כבקשה אחת. פוסטגרס מריץ אצווה כזו בטרנזקציה
   משתמעת אחת, ולכן **כל מיגרציה היא הכול-או-כלום**. אין מצב ביניים בתוך קובץ.
2. ‏**`db-query.ps1` אינו כותב שורה בטבלת המיגרציות.** את השורה מוסיפים ידנית אחרי כל
   קובץ, אחרת הלדג'ר משקר וההרצה הבאה תנסה להחיל הכול שוב.

```powershell
$env:SUPABASE_ACCESS_TOKEN = (Get-Content 'D:\משה פרוייקטים\פיתוח אתרים\AI\API\NIR-TOKEN-SUPABASE.txt' -Raw).Trim()
.\scripts\rollout-apply.ps1 -From 0243 -To 0267 -ProjectRef rkftlbctohswhbbiaqin -AllowProduction
```

הסקריפט מחיל קובץ, מאמת, כותב את שורת הלדג'ר, וממשיך — **ועוצר על הכשל הראשון בלי לכתוב
את השורה של הקובץ שנכשל.** הרצה חוזרת ממשיכה מהמקום שבו נעצרה.

להרצה ידנית, אם צריך קובץ בודד:

```powershell
.\scripts\db-query.ps1 -SqlFile supabase\migrations\0243_currencies_in_use_are_readable.sql -ProjectRef rkftlbctohswhbbiaqin -AllowProduction
```

ואז, ורק אחרי שהיא הצליחה:

```sql
insert into supabase_migrations.schema_migrations (version, name)
values ('0243', 'currencies_in_use_are_readable');
```

### הסדר המלא

| # | שם | הערה |
|---|---|---|
| 0243 | `currencies_in_use_are_readable` | |
| 0244 | `intake_says_when_it_could_not_check` | כתיבה מחדש לפי עוגן |
| 0245 | `every_currency_carries_its_own_thresholds` | |
| **0246** | `plan_capability_ladder` | **הגדולה.** 3 טבלאות, 10 פונקציות, מצמצמת חוזה שהלקוח קורא |
| 0247 | `export_branding_entitlement` | |
| 0248 | `plan_capability_decisions` | |
| 0249 | `platform_user_administration` | 13 פונקציות |
| 0250 | `platform_operator_invitations` | |
| 0251 | `scan_pages_label` | |
| **0252** | `plan_capability_enforcement` | **10 פקודות מדיניות.** הרגיש ביותר |
| 0253 | `profile_locale` | עוגן; אידמפוטנטי במפורש |
| 0254 | `organization_teardown_window` | 3 פונקציות בעוגן |
| 0255 | `owner_backup_email` | |
| 0256 | `document_text_evidence` | |
| 0257 | `legal_hold_release` | |
| 0258 | `the_setup_wizard_can_be_finished` | עוגן |
| 0259 | `invoice_three_way_tolerance_per_currency` | 11 עוגנים |
| 0260 | `document_assessment_publishes_the_ladder` | עוגן על מה ש-0244 ייצר |
| 0261 | `invoice_three_way_publishes_the_ladder` | עוגן על מה ש-0259 ייצר |
| 0262 | `benefit_window_contract` | |
| 0263 | `dashboard_previous_period` | תוספת בלבד |
| 0264 | `invoice_due_date` | |
| 0265 | `scheduled_payments_outlook` | 2 טבלאות, 2 מדיניות |
| 0266 | `scheduled_payments_flag` | דגל, נולד כבוי |
| 0267 | `forecast_cohort_joins_the_teardown_window` | תיקון מיזוג בין 0254 ל-0265 |

### אם זה נעצר באמצע

**זה המצב הצפוי ביותר, והוא לא אסון.** 14 פונקציות נכתבות מחדש בשיטת העוגן: המיגרציה
קוראת את הפונקציה כפי שהיא חיה, ואם הטקסט אינו מה שהיא מצפה לו היא **זורקת שגיאה
ועוצרת**. היא נכשלת בקול, לא בשקט, וזו הסיבה שהיא בנויה כך.

1. **לא להריץ שוב "אולי יעבור".** לקרוא את הודעת השגיאה במלואה — היא נוקבת בשם המיגרציה,
   בעוגן ובמספר ההופעות שנמצא בפועל.
2. הקבצים שכבר עברו **נשארים** ושורות הלדג'ר שלהם נכונות. אין צורך לשחזר.
3. לתקן את המיגרציה שנכשלה מול הטקסט החי — לא לכפות אותה.
4. להמשיך מאותו מספר.

**שחזור מגיבוי הוא המוצא האחרון**, ועולה עד יממה של נתונים. הוא נדרש רק אם מיגרציה הצליחה
והשאירה מצב שגוי — לא כשהיא נכשלה, כי כשל הוא אטומי.

---

## 6. שלב 2 — postflight

```bash
node scripts/rollout-preflight.mjs --compare
```

**מה שחייב להתקיים:**

| טענה | ציפייה |
|---|---|
| ראש הלדג'ר | `0267`, **258 שורות** (233 + 25) |
| ‏`scope_enforcement_violations()` | ריק, כמו לפני |
| ספירות עסקיות | **זהות לחלוטין** — ספקים 25, מוצרים 271, הזמנות 259, חשבוניות 15, תשלומים 7, הקצאות 9 |
| ‏`audit_logs` | עלה במעט בלבד. עלייה גדולה = מיגרציה נגעה בנתונים עסקיים |
| פונקציות / טבלאות / מדיניות | עלו. ירידה במדיניות = ‏`0252` מחקה ולא יצרה מחדש |
| ‏`get_public_plan_features()` | קיימת ומחזירה שורות |
| ‏`get_public_plan_quotas()` | מחזירה **3** מפתחות: `documents.monthly`, `users.max`, `branches.max` |

**אם ספירה עסקית זזה — לעצור ולא להמשיך לפרונטאנד.**

---

## 7. שלב 3 — Edge

```bash
npx supabase functions deploy render-document --project-ref rkftlbctohswhbbiaqin
npx supabase functions deploy organization-storage-purge --project-ref rkftlbctohswhbbiaqin
```

שתיהן קיימות בריפו ואינן פרוסות. ‏`0254` בנויה סביב `organization-storage-purge`: בלעדיה
מחיקת לקוח מבוקרת לא תשלים, וזה בדיוק הכשל ש-`0254` נכתבה כדי למנוע.

לוודא שהסודות שהן צריכות מוגדרים **לפני** הפריסה, ולא אחריה.

> **נמצא אגב הבדיקה ולא נגעתי בו:** פונקציה בשם `whatsapp` **פרוסה בייצור ואינה קיימת
> בריפו**. היא לא חלק מהרולאאוט. שווה בירור נפרד — פונקציה פרוסה שאיש אינו מתחזק היא
> משטח תקיפה.

---

## 8. שלב 4 — Frontend

מיד אחרי ה-Edge, כדי לסגור את החלון של §3.

לפי מטריצת הרולאאוט: בנייה עם env של ייצור, סריקת סודות/localhost, פריסה ל-Pages, ואימות
‏**hash parity בשלוש הכתובות** — הייחודית, הקנונית, ו-`app.inplace.digital`.

> הכתובת הקנונית מגישה את הבנייה הקודמת עוד כדקה אחרי העלאה מוצלחת. לבדוק parity
> **בלולאת polling**, לא בקריאה אחת.

---

## 9. שלב 5 — smoke חי

שורת „Auth / תפקידים / RLS" במטריצה חלה, כי `0252` משנה מדיניות:

- כניסה בשלושת חשבונות הבדיקה (`owner`, `office`, `accountant`) — הסיסמה נקראת בזמן ריצה
  מהקובץ החיצוני, ואינה מודפסת.
- **קריאה בלבד.** אפס כתיבות עסקיות.
- לוודא שהזהויות שפרשו (`kitchen`, `payer`, `supplier`) עדיין חסומות.
- מסכי הבנק, תור התשלומים והחשבוניות המרכזות — הארגונים קיימים על `premium`/`legacy`,
  ולכן **אמורים להישאר פתוחים**. אם נסגרו, ‏`0252` נעלה יותר ממה שהוחלט.
- מסך המנויים — לוודא שהוא מציג את שלושת המפתחות החדשים ולא נשבר.

---

## 10. שלב 6 — דף הנחיתה

הרולאאוט הזה סוגר את `DEBT §34` בריפו של דף הנחיתה. אחריו:

```bash
cd "D:\משה פרוייקטים\פיתוח אתרים\LANDING-PAGE-NIR"
npm run build
node scripts/check-live-catalogue.mjs
```

**לפני הרולאאוט** הוא מדווח 2 `MISMATCH` ו-12 `UNVERIFIABLE` על פני 96 תאים.
**אחריו** 12 ה-`UNVERIFIABLE` אמורים להיעלם.

שתי ה-`MISMATCH` **יישארו**, והן אינן קשורות לרולאאוט: תווית „30% הנחה" מול 16.7% אמיתיים
(`DEBT §33`). זו הכרעת בעלים פתוחה — או שהמחירים השנתיים משתנים, או שהתווית אומרת 17%.

---

## 11. מה המסמך הזה אינו טוען

- **שהעוגנים יתאימו.** שלוש שכבות מגנות על *סיבת* הכשל ההיסטורי (‏CR), והן נבדקו ועוברות.
  אבל עוגן יכול לזוז גם מסיבה אחרת — טקסט שהשתנה. ניסיתי לאמת את העוגנים עצמם סטטית מבחוץ
  ו**הבדיקה שלי טעתה**: כל מיגרציה מצהירה בעצמה כמה הופעות היא מצפה למצוא (‏`0253` מצפה
  לשתיים), ובדיקה חיצונית שדורשת אחת מדווחת כשל שווא. את זה מכריעה רק ההחלה עצמה, שנכשלת
  בקול ועוצרת.
- **שנבדקה הרצה יבשה על עותק של הייצור.** לא בוצעה. היא דורשת שחזור גיבוי לפרויקט נפרד,
  והיא ההגנה החזקה ביותר שיש כאן. אם רוצים אותה — היא נכנסת בין §4 ל-§5.
- **ש-`npm run quality` רץ.** לא הורץ במסגרת ההכנה הזו.
