# PROGRESS — מצב נוכחי

עודכן: 28.08.2026 — **הייצוא נכתב מחדש: אקסל מעוצב ומימין לשמאל, ומחולל PDF אמיתי עם לוגו הדייר.**

> **מה שהבעלים דיווח, ומה שהתברר.** „אין עיצוב בשפה של האתר, אין אקסל מסודר, והעברית נקראת משום
> מה משמאל לימין." שלושת הליקויים נמצאו במדידה, לא בניחוש. **העברית**: שני בונים ברמת המסך —
> `Expenses.tsx` ו-`ProductPurchaseSummary.tsx` — יצרו חוברת בלי `Workbook.Views[0].RTL`. שלושת
> הבונים ב-`src/lib` כן הגדירו אותו, ו-`DEBT §37` אפילו הצהיר „RTL בשלושת הבנאים" — **שני אלה לא
> נספרו מעולם.** זו הפעם השנייה שאותו דגל נשכח (הראשונה: חוברת ה-snapshot הנעולה). **האקסל**:
> אותם שני בונים גם לא נתנו רוחבי עמודות, פורמט כסף או בלוק כותרת. **העיצוב**: ‏SheetJS CE אינה
> כותבת מילוי או גופן כלל, ולכן צבע מותג בחוברת לא היה אפשרי — זה בדיוק החוב ש-`§37` תיעד.
>
> **מה הוכרע (‏#277, בעלים).** ‏(א) תלות חדשה — `exceljs` (MIT) כותבת כל חוברת שהמוצר מייצר;
> ‏SheetJS נשאר לקריאה ולתבנית הדייר. ‏(ב) מחולל PDF אמיתי במקום `window.print()`. ‏(ג) הלוגו של
> הדייר בכל מסמך שנוצר. ‏(ד) המסלול החינמי מייצא עם סימן המים של InPlace, וזה נאמר בתיאור המסלול.
>
> **למה המחולל מצלם ואינו כותב טקסט, וזו לא עצלות.** נמדד: **אף מנוע PDF ב-JavaScript אינו מממש
> את האלגוריתם הדו-כיווני של יוניקוד.** ‏`setR2L` של jsPDF הופך רצף במקום לפתור אותו; ל-PDFKit/
> react-pdf יש ליקוי פתוח מ-2019; ל-pdfmake קריאה פתוחה בשם „עברית משמאל לימין, מילים הפוכות".
> כל שורה בכל טבלה כאן מערבת עברית עם מספר ו-₪ — בדיוק המקרה שכולם שוברים. הדפדפן, לעומתם, כבר
> מסדר את אותה טבלה נכון על המסך. לכן `html2canvas-pro` (שכבר היה בפרויקט, לתמונת ההזמנה
> לוואטסאפ) מצלם את אזור ההדפסה ו-`jspdf` אורז עמודים. **המחיר מוצהר:** הטקסט אינו בר-סימון —
> `DEBT §68`. כפתור „הדפסה" נשאר לצדו ומייצר PDF וקטורי.
>
> **תיאור המנוי לא נכתב ידנית.** ‏`0213` מוסיף הרשאה בוליאנית `exports.unbranded_pdf`, פתוחה בכל
> מסלול וסגורה ב-`free`. כרטיסי המסלולים מציגים כל הרשאה שהקטלוג מחזיר, ולכן השורה מופיעה לבד
> ב-`/pricing` וב-`/settings/subscription` — ואינה יכולה לסטות מההתנהגות, כי זו אותה הרשאה
> שהמחולל קורא. הניסוח הוא ההענקה ולא הקנס, כדי שהרשאה לא-מוצהרת תסרב לפי `0154` — וסירוב פירושו
> הטבעה, שמונעת הטבה במקום לדלוף אותה.
>
> **שלושה באגים נוספים שהמדידה חשפה, ולא היו בדיווח.** ‏(א) **תאריך שזז ביום שלם.** עמודת תאריך
> מגיעה כ-`'2026-08-01'`, ‏`new Date()` קורא אותה כחצות UTC, ו-xlsx שומר סריאל נאיבי — הסריאל
> שנכתב היה `46234.875`, כלומר 31 ביולי 21:00. רואה חשבון היה מוצא את החשבונית הראשונה של אוגוסט
> תחת יולי. עכשיו נכתב הערך הנאיבי ששווה לשעון המקומי. ‏(ב) **הורדה שנופלת בפיירפוקס** —
> `link.click()` על עוגן שלא הוכנס למסמך, ו-`revokeObjectURL` באותו תור. ‏(ג) **שבירת עמוד באמצע
> שורה** — נמצא בצילום הראשון של ה-PDF; שבירת העמוד מוזזת עכשיו לשורת פיקסלים אחידה.
>
> **מה שאומת ויזואלית ב-28.08.2026, מקומית.** החוברת נפתחה ב-**Excel עצמו** (‏COM) והוצאה ל-PDF:
> ‏RTL, שורת כותרות בצבע המותג, כותרת ממוזגת, `10,612.71` ו-`03/06/2026`. ה-PDF של הדוח, של ריכוז
> ההוצאות ושל ההזמנה נצפו ב-Chrome: לוגו הדייר בראש, טבלאות בשפת האתר, שבירת עמוד בין שורות,
> מספור עמודים, ואפס שגיאות קונסול. גרסה עם סימן מים הופקה מול override זמני ואומתה. **מצב הדמו
> המקומי הוחזר לקדמותו** — ההרשאה החורגת נמחקה, הלוגו הוסר, והקובץ שהועלה נמחק מה-bucket.
>
> **מלכודת סביבה ששווה לדעת:** ה-chromium של Playwright על המכונה הזו **אינו רץ** —
> „side-by-side configuration is incorrect". האימות נעשה עם Chrome המותקן דרך `executablePath`.
> ובנוסף, פורט 5199 היה תפוס על ידי `vite` של worktree אחר, כך שדף שנראה עדכני הגיש קוד ישן;
> האימות רץ על 5290.
>
> **סבב שני באותו יום — שלושת הפריטים ש„לא נעשו" הצטמצמו לאחד.**
>
> **‏(א) החשבונית מייצאת.** המחולל מקבל עכשיו **מערך** אלמנטים ולא שורש אחד: הוא מצלם כל בלוק
> בנפרד ומזרים אותם על עמודים — בלוק שאינו נכנס בשארית העמוד פותח עמוד חדש, ובלוק גבוה מעמוד
> נחתך כמו קודם. זה בדיוק מה שמסך החשבונית דרש (שישה אזורי הדפסה, בלי מיכל), והוא מייצא שלושה
> בלוקים: כותרת, אריחי כסף וכרטיס פרטים. ‏`DEBT §71` נסגר.
>
> **‏(ב) תבנית הדייר עברה ל-exceljs.** דווקא המסלול שכל כולו „העיצוב שלך הוא התוצר" רץ על הכותב
> שמוחק מילוי וגופן. עכשיו הוא נמדד: הבדיקה בונה חוברת עם מילוי, גופן לבן מודגש, `numFmt` ורוחב
> עמודה, מעבירה אותה במילוי וקוראת בחזרה — ארבעתם שורדים. הפירסור **נשאר** על SheetJS, כי שם
> יושבות הדחיות (מאקרו שהוסווה, קישור חיצוני) והן מוכחות. ‏`DEBT §70` נסגר.
>
> **‏(ג) סימן המים הוא ה-lockup המלא** — הסמל וגם השם (הכרעה משלימה ב-`#277`). סמל בלבד אומר מי
> הפיק את המסמך רק למי שכבר מכיר אותו.
>
> **שני תיקונים שהסבב הזה חייב, ואינם קוסמטיים.** ‏(1) בדיקת סימן המים הייתה `useQuery` והופעלה
> **בכל טעינה** של חשבונית, הזמנה או דוח — בקשת רשת לעובדה שרלוונטית רק אם לוחצים כפתור. היא
> גם הפילה שמונה בדיקות רכיב, שזה אותו ליקוי מהצד השני. עכשיו היא נשאלת ברגע ההורדה בלבד.
> ‏(2) כותרת ה-`print-only` הכפילה כותרת קיימת בעץ הנגישות — ‏jsdom אינו טוען את גיליון הסגנון,
> ולכן `display:none` לא חל שם והבדיקה מצאה שתי כותרות. היא סומנה `aria-hidden`, וזה גם נכון
> לגופו: היא חזרה על מה שכבר על המסך.
>
> **סבב שלישי — האכיפה נבנתה, והיא גם הפכה את הטקסט לבר-חיפוש.**
>
> הבעלים שאל „איך אוכפים את זה באמת" והתשובה הייתה תמיד אחת: **מסמך שנוצר אצל הלקוח אינו יכול
> להיות אכוף.** לכן `worker/render` — שירות Node עם Chromium שרץ על אותו VPS כמו עובד ה-OCR,
> ומקבל את המסמך מ-`supabase/functions/render-document`.
>
> **ומה שהתברר תוך כדי: זה אותו מהלך שפותר את החוב השני.** ‏Chromium מסדר את העברית נכון — זו
> בדיוק הסיבה שהמחולל בדפדפן צילם מסך — אבל `page.pdf()` פולט **טקסט אמיתי** ולא תמונה. נמדד:
> חילוץ טקסט מהקובץ שהשירות ייצר החזיר `דוח חודשי יוני 2026`, ‏`12,523.00` ו-`חשבונית 1509`.
> ‏`DEBT §68` ו-`§69` נסגרים באותה עבודה.
>
> **אין תבנית שנייה, וזו ההחלטה המרכזית.** השירות פותח את **אותו מסך** שהמשתמש רואה;
> ‏`@media print` ב-`src/index.css` הוא התבנית, והוא כבר מחליט מה יורד, מה מופיע רק על נייר
> ומה ה-`@page`. מסלול `/print` נפרד נשקל ונדחה: הוא היה פריסה שנייה לתחזק, והצימוד הנוכחי
> נכון — מסמך שנראה שגוי בשירות נראה שגוי גם במדפסת.
>
> **מי מחליט על הסימן: הפונקציה, לא הקליינט.** הבקשה מהדפדפן **אינה נושאת `watermark` כלל**.
> הפונקציה פותרת בעצמה את `exports.unbranded_pdf` ומעבירה את ההכרעה לשירות, וההטבעה נעשית שם
> על ה-PDF המוגמר דרך `pdf-lib`. הלקוח לעולם אינו מחזיק עותק לא-מוטבע.
>
> **ההנחה היחידה שלא ניתן היה ליישב מקריאת תיעוד, ולכן נמדדה.** הפונקציה אינה רואה refresh token,
> ולכן היא בונה session מ-**access token בלבד** וכותבת אותו ל-`localStorage` לפני הניווט הראשון.
> נמדד מקומית שזה עובד, ושהמפתח הוא `sb-127-auth-token` — התווית הראשונה של ה-host, כפי שחוזה
> הפונקציה מחשב.
>
> **מה שנמדד מקצה לקצה, מקומית:** מפתח שגוי → `401`; גרסת חוזה שגויה → `409`; נתיב שאינו
> ברשימה → `400`; דוח, דוח מוטבע וחשבונית נוצרו ונצפו — לוגו הדייר, טקסט וקטורי, סימן מים על שני
> העמודים. **באג שנמצא ותוקן תוך כדי:** ההמתנה הייתה ל-selector *נראה*, ו-`.print-only` הוא
> `display:none` על המסך בהגדרה — מסך החשבונית, שמתחיל בכזה, נכשל ב-timeout על עמוד שהיה מוכן.
>
> **גבול אמון שנרשם ולא הוסתר:** השירות מקבל את ה-session של הקורא לשניות שהרינדור נמשך, ולכן הוא
> יכול באותן שניות לקרוא את מה שהקורא יכול. ההגנות: סוד משותף, רשימת נתיבים שנאכפת **בשני
> הצדדים**, ו-context שנהרס אחרי כל בקשה. ‏`docs/INTEGRATION-ARCHITECTURE.md §7`.
>
> **מה שעדיין לא נעשה, ואי אפשר לעשות מכאן: הפריסה.** אין גישה ל-VPS מהריפו — בדיוק כמו
> `worker/ocr`. עד ש-`RENDER_SERVICE_URL`/`RENDER_SERVICE_TOKEN` מוגדרים, הפונקציה עונה
> `renderer_not_configured` והקליינט מייצר את המסמך בעצמו: מצב תקין, שבו סימן המים חוזר להיות
> מיתוג. **גרסת החוזה חייבת לזוז בשני הצדדים באותו רולאאוט** — זה מה שהשבית את עיבוד המסמכים
> בשקט ל-5 ימים ב-24.08; כאן שער CI משווה את שני הליטרלים ו-`/health` מחזיר את הגרסה החיה.
>
> **מה שנשאר פתוח, וזו עכשיו עבודה אחת ולא שלוש:** ‏`DEBT §68` (טקסט לא בר-חיפוש) ו-`§69`
> (סימן מים שאינו נאכף) **נפתרים באותו מהלך** — ייצור המסמך בצד שרת: מסלול `/print/:kind/:id`,
> שירות renderer עם Chromium headless על ה-VPS שכבר מריץ את ה-worker, ופונקציית Edge שמאמתת,
> פותרת את ההרשאה, מטביעה ומחזירה URL חתום. ‏`§69` מפרט את השלבים ואת העלות: שירות חדש עם
> מספר גרסת חוזה משלו ופריסה ל-VPS — חבילה בפני עצמה.

---

עודכן: 26.08.2026 — **הרולאאוט לייצור בוצע. ייצור על `0211`, ‏Edge ו-Pages נפרסו ואומתו חי.**

> **קרא את השורה הזו לפני שאתה מתכנן משהו:** ‏`main` וייצור **מסונכרנים**. ‏`0210` ו-`0211` הוחלו
> על ייצור ב-26.08.2026 בסדר הזה, כל אחת אחרי dry-run ב-`begin/rollback` ועם שורת ledger שנכתבה
> **ידנית** (`db-query.ps1` אינו כותב ל-`schema_migrations`). ‏`max(version)` = `0211`, ‏206 שורות.
> **חלון טרום-ההשקה פעיל בייצור עד 2027-01-01.**
>
> **מה נמדד אחרי ההחלה, לא הונח.** שני הארגונים שנוצרו בחלון נושאים ארבע מדיניות אוטונומיה עם
> `expires_at`, כולן בסף `0.900` ואף אחת מתחתיו; ארגון הדמו הוותיק (`legacy`, מ-16.07) **לא
> נגע** — כצפוי, כי החלון חל על ארגונים שנולדו בתוכו. ‏`org_autonomy_policies` ו-
> `organization_subscriptions` רשומות `not_evidence` ברשם הראיות, כך שההענקה אינה הופכת ארגון
> ריק ל„פעיל" וניקוי חשבונות נטושים אינו משתתק. ספירות הדיירים לא זזו: 3 ארגונים, 8 פרופילים,
> 15 חשבוניות, 7 תשלומים — לפני ואחרי.
>
> **מה נפרס מלבד ה-DB.** שלוש פונקציות Edge בלבד (`admin-provision`, `send-invite`,
> `tenant-export`) — אלה ששינוי המותג נגע בהן; 20 הפונקציות האחרות לא נגעו, ונבדק לפי חותמות
> הזמן. ‏Frontend נבנה מ-`origin/main` ב-worktree נקי עם `.env` הייצור בלבד (**בלי**
> `.env.local`, שמכיל loopback), נסרק לפני פרסום — ה-JWT היחיד ב-bundle הוא `anon`, אין
> `service_role`, וה-client מכוון ל-`rkftlbctohswhbbiaqin` — ונפרס ל-Pages. שני הדומיינים
> מגישים את אותם ה-hashes.
>
> **מה שאומת חי, לא ב-HTTP 200.** ‏`/login` בדסקטופ ובמובייל, כניסת בעלים אמיתית, דשבורד עם
> נתונים (יתרה 15,083 ₪, רצועת „דורש טיפול"), ו-`/settings/subscription` שמציג „לקוח ותיק"
> למסלול `legacy` — כלומר המסך אומר את מה שה-DB אומר. אפס שגיאות קונסול, אפס תשובות ≥400.
> **הדשבורד לוקח כמה שניות להתייצב** — צילום מוקדם תופס spinner, וזו אינה תקלה.
>
> **מה שלא נגע ברולאאוט:** ה-OCR worker. הוא רץ על VPS נפרד, קבצי `worker/ocr/**` לא השתנו
> ושתי גרסאות חוזי ה-gateway לא זזו — ולכן אין פער חוזה ואין צורך בפריסה מחדש.
>
> **מספור ההכרעות: ‏#128 ו-#130 תפסו שניהם את `#275`.** ‏`0211` מטביע `(0211, #275)` בתוך גוף
> ה-SQL של עצמה, ולכן המספר נשאר עם הכרעת אוטונומיית המסמכים. **חלון ההיכרות של המסלול החינמי
> הוא `#276`**, וכל תשע ההפניות אליו ב-`PLAN-subscription-tiers-20260825.md` הוסבו. נוסח שתי
> ההכרעות לא נגעו בו — רק המספר.
>
> **מה מוזג (#129), ומה במפורש לא נעשה.** תג דרגת מנוי בכותרת הטלפון (בעלים בלבד, ארבע חזויות,
> „פרו" עם ניצוץ נוסע שהוא **חריג מתועד** ל„אנימציה שאינה מצב" ב-`DESIGN.md`), „שליחת הערה" ירדה
> למגירה, סרגל הפעולות התחתון יושר, מסך מנוי משלו ב-`/settings/subscription` עם קבוצת „המנוי"
> במגירה, הבר מחליף את מונה העמודים, ושם הספק/מודל ירד מ„פרטים טכניים". **לא נפרס לייצור**
> (הכרעת בעלים: מיזוג בלבד). אין מיגרציה ב-#129.
>
> **התיקון שחשוב יותר מהתכולה: „הפירוש האוטומטי לא רץ בייצור" היה שגוי.** הניתוח נעשה על הריפו
> בלבד. מדידה חיה הראתה שורת קונפיגורציה קיימת, cron פעיל, 28 שיגורים, ומסמך של הבעלים ששוגר
> 46 שניות אחרי ההעלאה ופורש 56 שניות אחר כך. **הפירוש עובד, גם לארגונים חדשים.** מה שחסר היה
> החצי השני — האישור האוטומטי, שכבוי לכל ארגון פרט לדמו. ‏`DEBT §64` מתעד את מה שכן נשאר פגום:
> הריפו אינו כותב את שורת הקונפיגורציה בשום מקום, כך שסביבה חדשה תקבל cron שאינו עושה דבר.
>
> **‏#130 — מה הוא עושה, ומה שהיה צריך להיבנות בשבילו.** ארגון חדש נולד עם ארבע מדיניות
> האוטונומיה דלוקות בסף `0.900` עד 31.12.2026 (`#275`, כולל `document.packet_split` — הבעלים
> קיבל המלצה מפורשת שלא לכלול אותו לפי `#249` ובחר לכלול; ההמלצה רשומה בהכרעה). **להענקה לא
> הייתה דרך להסתיים:** לדגלים יש `targeting.ends_at`, לאוטונומיה לא היה שעון כלל. נוספה עמודת
> `expires_at`, והשעון הוכנס ל-**JOIN** של `private.autonomy_policy_for_org` — כך שהענקה שפגה
> נפתרת **בדיוק** כמו ארגון שלא הוגדר מעולם, בלי מצב חדש ובלי הבטחה שנשברת.
>
> **הבאג שהעיקרון של הבעלים חשף, והוא הממצא החשוב של הסבב.** „הכול חוזר לקדמותו אחרי התאריך"
> נשמע כמו הבהרה והתברר כמדידה: ‏`private.organization_has_business_activity` סופרת כל טבלה עם
> `org_id` שאינה רשומה כ-`not_evidence`. ‏`0210` ו-`0211` כותבים שורות **בלידה**, והשורות
> **אינן נעלמות בתאריך — הן רק מפסיקות לפעול**. כלומר בלי תיקון, כל ארגון שנפתח בחלון היה נשאר
> „לא ריק" לנצח ו**ניקוי חשבונות נטושים היה משתתק** — כשל שנראה כזהירות. שתי הטבלאות סומנו
> `not_evidence`, וזו הסיווג הנכון גם לפני החלון וגם אחריו: **הדייר לעולם אינו כותב בהן שורה.**
>
> **מה שהשערים תפסו, ושווה לדעת לפני שכותבים מיגרציה דומה.** ‏(א) ‏`0182` מחזיק רשם של כל
> פונקציה שרשאית להדליק אוטומטציה, ו-`P68` משווה אותו לכל פונקציה חיה שכותבת ל-`org_autonomy_policies`
> — טריגר חדש **חייב להצהיר על עצמו** שם. ‏(ב) שער `A5` הוא **טקסטואלי**: שם של טבלה מוגנת בהערה
> בתוך גוף פונקציה מפיל את המיגרציה. ‏(ג) שער `A6` תופס עמודה חדשה שסוטה מחוזה ייצוא הדייר —
> מרעננים את ה-hash באותו קובץ שמוסיף את העמודה. ‏(ד) **שבע** סוויטות הניחו שארגון נולד ריק
> (`p13`, ‏`p14`, ‏`p15`, ‏`p16`, ‏`p47`, ושתי `p18`) — אחרי `0211` „כבוי" הוא מצב שבדיקה **בונה**.
>
> **מלכודת סביבה שעלתה סבב שלם:** כתיבת `e'\r'` דרך heredoc ב-shell הפכה את תו הבריחה **לתו
> ירידת-שורה אמיתי** בתוך ה-SQL, והמיגרציה נשברה בפרסינג. השתמש ב-`chr(13)`. זה חשוב במיוחד כאן
> כי `0209` קיימת בדיוק בגלל סופי שורה בגוף פונקציות.
>
> **מה נמדד.** ‏#130: **השער המלא ירוק בכל העבודות** — 96 שלבי SQL, דפדפן, חוזי Deno, ביקורת
> תלויות ([ריצה 32897715501](https://github.com/MSA-I/NIR-APP/actions/runs/32897715501)).
> ‏#127 אחרי ששת התיקונים: ‏SQL, ‏`verify`, ‏`build` וחוזי Deno ירוקים; הדפדפן נכשל פעמיים
> ב**שני מקומות שונים ולא קשורים** (‏502 על `my_subscription`, ואז שני 401 בתרחיש שמדמה כשל
> שרת) — בעוד `main` מריץ את אותו שער ירוק עם **אפס** 502. סווג כתנודתיות סביבתית ונעצר לפי כלל
> האבחון; הראיות על ה-PR.
>
> **מה שהסוכן הבא צריך לדעת:** המיזוג והרולאאוט **בוצעו שניהם**. ‏`0210` **אינו אידמפוטנטי**
> (יוצר טריגר בלי `drop ... if exists`) — הוא הוחל פעם אחת ואין להחילו שוב. **PITR כבוי
> בפרויקט הייצור**: נקודת השחזור היחידה היא הגיבוי היומי, ולכן כל כתיבה ל-DB מכאן היא
> forward-only בפועל. הגיבוי שקדם לרולאאוט הזה: `2026-08-25T22:43:49Z`.

---

עודכן: 25.08.2026 — **חמישה ליקויים מדיווח הבעלים מהמסך החי. ארבעה תוקנו; החמישי התברר כשני
שערים מכוונים ולא כבאג, ותוקן רק במה שהוא באמת היה — מסך שמשקר.**

> **מה שהמשימה התבררה כלא-היותה, פעמיים.** ‏(א) האייקון שהבעלים סימן באדום **אינו העוזר** אלא
> `FeedbackButton` (‏`MessageSquarePlus`) — „שליחת הערה". העוזר הוא ה-`Sparkles` שלידו, ולא זז.
> ‏(ב) „מסך ניהול מנוי" **כבר היה קיים ובנוי היטב** (`OrgSubscriptionPanel`, עם חוזה מלא של
> ‏`#194`/`#201`/`#208`/`#216`–`#225`), קבור בתוך „הגדרות מערכת" מתחת לשיעור המע״מ ולמעלה הלוגו.
> העבודה הייתה **להזיז ולעטוף**, לא לכתוב מאפס.
>
> **תג דרגת המנוי.** בכותרת הטלפון יש מקום לשלושה סימנים לצד שם המסך ושם הארגון, לא לארבעה.
> „שליחת הערה" — הנדיר שבהם — ירדה למגירה כשורה עם מילה, ובמקומה עומד `PlanBadge` (בעלים בלבד,
> לחיצה נוחתת ב-`/settings/subscription`). ארבע חזויות בסולם עולה: `free` נייר שקט · `basic`
> מדרוג מותג רך · `pro` אותו מדרוג עמוק יותר עם **ניצוץ נוסע** · `premium` צ׳יפ זהב;
> ‏`business` חוזר על הזהב, כי הוא מעל פרימיום בסולם. **הזהב אינו גלולת ענבר בכוונה:**
> ‏`await-soft` הוא בדיוק זה, והוא סטטוס — לכן התג הוא מילוי רווי בבהירות בינונית עם אותיות
> אוניקס, כלומר הבדל של **מבנה** ולא של גוון בלבד, ושם המסלול מודפס תמיד בתוכו.
>
> **הניצוץ הוא חריג בשמו, לא סחף.** תנועה מתמדת סותרת „אנימציה שאינה מצב — אסור בחוקה";
> הבעלים הכריע לטובתה, והיא נרשמה ב-`DESIGN.md` עם הגבולות: אלמנט אחד, מעבר אחד של 3.2s, בלי
> מאפיין פריסה, ו**נעלמת לגמרי** תחת `prefers-reduced-motion`. **נמדד, לא הונח:** שני פריימים
> של התג במרווח 900ms **אינם זהים** בריצה רגילה, ו**זהים בייט-לבייט** תחת `reduce`.
>
> **סרגל הפעולות התחתון — שתי סיבות, לא אחת.** נמדד לפני שנגענו, ב-390px: התוויות ישבו בשלושה
> גבהים שונים. ‏(1) הפריט המרכזי היה מעוגן אחרת מארבעת שכניו — ה-puck הוא `absolute` ואינו תורם
> גובה, ולכן העמודה שלו החזיקה תווית בלבד; (2) התוויות נשברות אחרת זו מזו — „הזמנה חדשה" לשתי
> שורות ו„מרכז הבקרה" לאחת — ופריט ממורכז גדל **לשני הכיוונים**. תיקון של אחת מהן בלבד היה משאיר
> את המסך עקום. עכשיו כל החמישה מעוגנים לתחתית וארגז התווית שמור לשתי שורות תמיד. **נמדד אחרי:
> חמש התוויות ב-`top` זהה — 806 — ו-2px מרווח בין ה-puck לתווית המרכזית.**
>
> **מסך המנוי.** ‏`/settings/subscription`, ‏`Guard roles={['owner']}`, וקבוצה משלו במגירה
> („המנוי", אחרונה, לא מתקפלת — דיסקלוזר מעל פריט אחד הוא דלת עם מכסה). הרשימה השטוחה הפכה
> לחמישה כרטיסים בשורה. **מה שמותר להיכתב בכרטיס נקבע במדידה ולא בטעם:** המחיר `—` (הכרעת
> בעלים; השרת ממילא מחזיר `NULL` בלי כתובת חיוב מאומתת, ו-`ARCHITECTURE.md:244` אוסר מחירון
> בקוד) · מכסת המסמכים 20/40/150/375 מ-`get_public_plan_quotas()`, **אותה פונקציה** שדף התמחור
> הציבורי קורא · „ביזנס" — „דברו איתנו" בלי מספר. **ואין טבלת יכולות:** ‏`#274` הוא
> `NOT_IMPLEMENTED`, כל שישה הבוליאנים עדיין `true` לכל מסלול ו-`0184:701` מפיל כל מיגרציה
> שמכבה יכולת — כרטיס שהיה אומר „ייצוא ✗ בחינם" היה מוכר הבדל שאינו נאכף. כפתורי השדרוג
> **מוצגים ומושבתים** עם הסיבה: `#204` אוסר להסתיר, `#217` אוסר לפתוח בלי אירוע שרת חתום.
>
> **הטקסט המעוך.** שתי פסקאות עזר מודבקות בכרטיס הסיסמה; השנייה תיארה מסלול שמתחיל **במסך
> הכניסה** ואין לו קשר לשני השדות שמתחתיו. הוסרה. ה-`meta` של הכותרת הבטיח „מדיניות עבודה"
> שעברה לאפליקציית המפעיל ב-19.08 — תוקן, וגם התיאור בקטלוג המסלולים.
>
> **המסמכים.** ‏(1) אותה מחרוזת (`עמוד 7 מתוך 27`) נשלחה פעמיים ל-`LifecycleStrip` — פעם
> כטקסט ופעם כתווית הבר — והרכיב צייר את שתיהן. הבר מחליף עכשיו את המספרים, והמשפט ממשיך להימסר
> דרך `aria-valuetext`. ‏(2) „פרטים טכניים" הדפיס `openai · gpt-…`; שתי שורות המנוע התאחדו
> ל„גרסת עיבוד". **מה שנשאר במפורש:** מזהה משימה, טביעת מקור, גרסת חוזה ו-`prompt`/`schema` —
> מזהים **שלנו** שתמיכה צריכה, שאינם שם מסחרי של איש. הנתונים לא נמחקו מהמסד.
>
> **החצי החמישי — והמדידה שהפכה את התשובה.** „מסמך בחשבון חדש לא מתפרש
> ומתחשב". **הניתוח הראשון נעשה על הריפו בלבד והוא היה שגוי בחציו.** הריפו אמנם אינו
> כותב את שורת הקונפיגורציה של ה-cron בשום מקום, אבל **בייצור היא קיימת** — הוגדרה ביד בשלב
> כלשהו. קריאה חיה: שורה אחת, cron פעיל, 28 שיגורים, והמסמך שהבעלים העלה ב-16:14 שוגר
> ב-16:15:00 ופורש ב-16:15:56 בלי שאיש נגע בו. **הפירוש האוטומטי עובד, ואין מה להדליק.**
>
> **מה שבאמת חסר הוא החצי השני — „מתחשב".** לארגון הדמו ארבע מדיניות אוטונומיה
> דלוקות ו-15 חשבוניות שנוצרו אוטומטית; לשני הארגונים שנפתחו היום **אין אף שורה**, ולכן
> המסמך מתפרש ואז עוצר ב-`review`. `baseline_enabled` נעול `false` באילוץ בסכימה, וה-seed
> היחיד בריפו מדליק `price_list.intake` לארגון הפיקסט׳ר בלבד.
>
> **מה תוקן בחבילה הזו, ומה לא.** רצועת ההתקדמות נשארה עם הנוסח הניטרלי שלה
> („הקריאה הסתיימה. הפירוש עוד לא התחיל.") — נוסח שמבטיח „מתחיל מעצמו" היה טענה על
> מצב פריסה שהרכיב אינו רואה. הפער האמיתי — שהריפו אינו מידע על ההגדרה הידנית הזו
> — רשום כ-`DEBT §64` עם הצעד הזול הבא (preflight שמכשיל פריסה לסביבה בלי השורה).
> הדלקת האוטונומיה לארגון חדש הוכרעה על ידי הבעלים ויוצאת במיגרציה נפרדת.
>
> **מה נמדד.** ‏`npm run build` ירוק · `npm run verify` ירוק עם **152 קבצים ו-1,541 בדיקות**
> (‏+10 מול הסבב הקודם: `planBadge.spec.tsx` החדש, שני כרטיסי המסלול ובדיקת היעדר שם ספק/מודל).
> אימות ויזואלי מקומי **headed** ב-390px ו-1280px: התג בכותרת, הקבוצה החדשה ושורת ההערה במגירה,
> חמש התוויות ב-`top` זהה, חמשת הכרטיסים, ו„הגדרות מערכת" בלי פאנל המנוי. **מה שלא צולם חי
> ולמה:** ל-stack המקומי **אפס** `document_extractions` ו-`document_interpretations`, וטעינת
> ה-fixture היא כתיבה ל-stack משותף באמצע סשן. שני שינויי המסמכים אומתו ב-DOM המרונדר בבדיקות,
> ובשער הדפדפן של CI נוספה טענה שאוסרת שם ספק/מודל על המסך כולו.

---

עודכן: 25.08.2026 — **ארבעת ה-PR הפתוחים מוזגו ונפרסו. העוזר לא הודלק היום — הוא כבר היה דלוק,
והמסמך שאמר אחרת תוקן.**

> **מה שהמשימה התבררה כלא-היותה.** הבקשה הייתה „התחל רולאאוט, העוזר אמור להיות דלוק בייצור".
> החצי השני כבר היה נכון לפני שנגענו: מדידה חיה לפני כל שינוי מצאה `assistant` **v7 ACTIVE**
> עם `verify_jwt=true`, תשעה סודות `AI_ASSISTANT_*` כולל `AI_ASSISTANT_PRELAUNCH_EXCEPTION`,
> ‏`assistant.ui` ו-`assistant.history` דלוקים לארגון אחד, ו-`assistant_runs` עם שורה אחת
> ‏`succeeded`. **‏`DEBT §63` ב-`main` עדיין הצהיר „לא הופעל, לא נפרס ומעולם לא קרא לספק".**
> התיקון היה כתוב — ויושב על ענף PR ‏#123 שלא מוזג. כלומר הריפו החזיק תשובה הפוכה מהמציאות,
> וזה כבר הטעה סוכן אחד. **המיזוג הוא מה שסגר את הפער הזה, לא הפריסה.**
>
> **התנגשות מספרי הכרעה, ולמה היא לא נפתרה בבחירת צד.** ‏`#121` ו-`#123` הביאו ל-`main` את
> ‏`#272` (חלון דממה בשיחה) ואת `#273` (יישור משטח העוזר); ענף המסלולים החזיק הכרעה **משלו**
> שגם היא מוספרה `273`. שתיהן אמיתיות ואף אחת אינה מבטלת את השנייה — לכן הכרעת המסלולים עברה
> ל-**`#274`**, ושש ההפניות אליה זזו איתה: כותרת מסמך התוכנית, הערת שלב 1, טבלת ההשפעה על
> מסמכים, ושורות `#196` ו-`#199`. **מספר הכרעה שמתפצל לשתי משמעויות הוא הפניה שבורה בכל מקום
> שמצטט אותה**, ולכן זה תוקן בבת אחת ולא הושאר ל„מי שיבחין".
>
> **הפריסה, ומה שהיה כמעט תאונה.** ‏`.env.local` בעותק העבודה מצביע על `http://127.0.0.1:55431`,
> ו-Vite טוען אותו **בכל מצב בנייה** — כולל production. בנייה תמימה הייתה צורבת את כתובת המחסנית
> המקומית לתוך ה-bundle החי. הקובץ הוזז הצידה לזמן הבנייה והוחזר ב-`trap` שרץ גם על כשל.
> הסריקה שאחריה מצאה שלוש התאמות ל„localhost" — **כולן טקסט של ספריות** (‏`gotrue-js` עם
> ‏`http://localhost:9999` כברירת מחדל, רשימת המארחים של supabase-js, ובודק הקידומת
> ‏`sb_secret_` שלה) — ובספירה **זהה לזו שכבר חיה באתר**. הכתובת המקומית עצמה אינה שם.
>
> **מה נמדד אחרי הפריסה, ולא הונח.** ‏`app.inplace.digital` ו-`supplyflow-baq.pages.dev` מגישים
> שניהם את אותו chunk כניסה `index-DrkLbE-l.js`. ‏`/` ו-`/login` אנונימיים החזירו 200 בדסקטופ
> ובמובייל עם **אפס שגיאות קונסול, אפס שגיאות דף ואפס תשובות 400 ומעלה**; כניסת בעלים נחתה
> ב-`/dashboard` בשני הגדלים, וארבעה גרפים נוצרו בשניהם. פונקציית `assistant` עלתה מ-`v7`
> ל-**`v8`**, ‏`verify_jwt` נשאר `true`, ו**אף פונקציה אחרת לא נגעה**. שאלה אמיתית דרך הדפדפן
> החזירה **HTTP 200**, ‏`assistant_runs` עלה מ-1 ל-**2** — `succeeded`, ‏`gpt-5.4-2026-03-05`,
> ‏6,884ms — ו-`audit_logs` מחזיק שתי שורות `assistant_run_recorded`.
>
> **התשובה עצמה היא הראיה הטובה ביותר לכך ש-`#123` חי.** לשאלה „כמה ספקים פעילים יש לי" העוזר
> **סירב**: „אין לי מדד זמין כאן לספירת ספקים פעילים", ומתחתיו, ב**טקסט אדום ולא בפאנל מלא**,
> ההסבר „הבדיקה רצה אך לא הצליחה למדוד את הנתון המבוקש, ולכן אין תשובה — וזה שונה מ'הכול תקין'".
> זה בדיוק הטיפול ש-`#273` קבע ל-`not_measured`, והאייקון בכותרת הוא `sparkles`. שניהם צולמו.
>
> **מה שלא נעשה, במפורש.** **אין מיגרציה באף אחד מארבעת ה-PR** — ולכן אין גיבוי, אין dry-run,
> אין שורת ledger ואין postflight; מטריצת הרולאאוט אינה דורשת אותם למשטח שלא השתנה.
> ‏`check-p0-security.ps1`, ‏`check-p0-upgrade.ps1`, ‏`Invoke-PriceListEdgeSmoke`,
> ‏`Invoke-OcrEdgeSmoke` ו-`check-p4-integrated-journey.cjs` **לא רצו** — הם ידניים ותלויי
> Windows, ותיק ירוק אינו טענה שהם עברו. שערי CI על `ec0aca9` ירוקים אך **כל ה-jobs של המוצר
> דולגו** (הקומיט נוגע במסמכים בלבד); מה שכיסה את העץ הממוזג בפועל הוא ריצה מקומית —
> ‏`npm run build` ו-`npm run verify` עם **151 קבצים ו-1,530 בדיקות**, כולן עברו.
>
> **שלוש מדידות שנרשמות כאן כדי שלא ייקראו כתקין.** ‏(א) חמש בקשות `net::ERR_ABORTED` מופיעות
> בשתי הריצות המחוברות (`notifications`, ‏`documents`, ‏`suppliers`) — אותה ספירה בשני הגדלים,
> בלי שגיאת HTTP ובלי שגיאת קונסול; זה נראה כביטול שאילתות בסגירת ההקשר, **לא נמדד כרגרסיה
> ולא נמדד כתקין**. ‏(ב) מכסת השאלות לעוזר במסלול `free` היא **20** בייצור בזמן ש-`#274` קובע
> **5** — עבודת המסלולים, לא נגענו. ‏(ג) ההיתר של `#271` פג **31.12.2026**; פקיעה מכבה את העוזר
> ואינה פורעת את `§63`.

---

עודכן: 25.08.2026 — **הרשמה פדרטיבית: Google עברה מקצה לקצה מקומית ומודלקת בייצור בצד השרת;
‏Apple בנוי, מכוסה בבדיקות, ומעולם לא רץ. הכפתור המת במסך ההתחברות הוסר.**

> **מה שהמשימה התבררה כלא-היותה.** הבקשה הייתה „כפתור Google לא עובד, ותוסיף Apple". החצי
> הראשון היה בעיה אחרת ממה שנראה: הרשמה עם Google **כבר מומשה** ב-24.08 לפי `#265` — ‏`0205`,
> הענף הפדרטיבי ב-`public-signup`, ‏`adoptExistingUserAsOwner`, ‏`Signup.tsx`. מה שלא עבד היה
> הכפתור במסך ה**התחברות**: ‏`aria-disabled`, בלי `onClick`, עם בדיקה שהצמידה שהוא אינו עושה
> דבר. הנימוק שלו — „המערכת סגורה" לפי `#12` — בוטל על ידי `#172` **תשעה ימים לפני** שהכפתור
> נשלח. ההכרעה החדשה היא `#268`: מסך ההתחברות פותח את המסירה ולעולם אינו מסיים אותה.
>
> **‏Apple לא דרש מיגרציה, וזו תכונה של `0205` ולא מזל.** השומר שם קורא
> `coalesce(private.auth_identity_provider(), 'email') <> 'email'` — הוא כבר סירב לכל זהות שאינה
> סיסמה — ו-`service_identity_has_profile` מעולם לא שאל באיזה ספק מדובר. הכלל נכתב „פדרטיבי",
> לא „Google". הענף ב-`public-signup` נפתח לקבוצת ספקים, ו**הטוקן מכריע ולא גוף הבקשה**: סשן
> שהונפק בידי ספק אחד אינו ניתן לפדיון בענף של אחר (`identity_provider_mismatch`). ‏`#269`.
>
> **הבאג שהכניסה החיה מצאה.** שני המסכים מחזירים את הספק ל-`/signup`. בביקור ראשון זה נכון —
> שם נותנים שם לעסק. **בביקור שני זה המסך הלא נכון:** למשתמש כבר יש ארגון, והמסך קידם אותו
> ב„נשאר רק לתת שם לעסק" ובשדה שם עסק. ‏`public-signup` סירב **נכון** ב-
> `identity_already_has_organization` — אבל רק **אחרי** מילוי הטופס ושליחתו. מה שהמשתמש פגש היה
> dead end בלבוש של צעד ראשון. ‏`Login.tsx` מחזיק את ההפניה הזו מאז ומתמיד; ל-`Signup.tsx` היא
> לא נדרשה כל עוד מייל וסיסמה היו המסלול היחיד, כי אף אחד לא הגיע לשם מחובר.
>
> **מה נמדד, לא הונח.** מול הסטאק המקומי, עם חשבון Google אמיתי: ארגון אחד · פרופיל `owner`
> אחד · `plan_key=free` לפי `#165` · קטגוריית הבסיס `כללי` · `private.product_events` עם
> `signup.completed {"identity":"google"}`. ואחרי התיקון — ניווט ל-`/signup` בזמן שהמשתמש
> מחובר נוחת ב-`/dashboard` בלי שדה שם עסק. ‏`npm run build` ירוק; ‏`npm run verify` ירוק עם
> **1,527 בדיקות ב-151 קבצים**; ‏`_shared/provision.test.ts` **9 עברו**, מהן שתיים חדשות
> ל-`adoptExistingUserAsOwner` שלא היו לו בדיקות כלל. **הן היו בלתי-נראות ל-CI** — הקובץ לא היה
> ברשימת ה-Deno של שער האיכות, וזה תוקן.
>
> **ייצור — מה כן ומה לא.** ‏`#268`/`#269` הוחלו על **הפרויקט המרוחק בלבד**: הספק Google מודלק,
> ‏`client_id`/`secret` מוזנים, ו-`/signup` נוסף ל-`uri_allow_list` בשתי הכתובות
> (`app.inplace.digital`, ‏`supplyflow-baq.pages.dev`). אומת ש-`/auth/v1/authorize?provider=google`
> מפנה לדף הכניסה האמיתי של Google ולא ל-`invalid_client`. לפני ההדלקה נמדד ששלושת השומרים של
> `0205` חיים בייצור. **החזית לא נבנתה ולא נפרסה** — הכפתור אינו מופיע באתר החי, כי הבנייה שם
> אינה מכילה `VITE_GOOGLE_SIGNUP_ENABLED`. ‏`[auth.external.google].enabled` נשאר `false`
> **בריפו** בכוונה: שער האיכות מריץ `supabase start` בכל שינוי ל-`config.toml`, ו-`true` בלי
> הסודות ב-runner נותן ל-GoTrue ‏`client_id` ריק ומפיל jobs שאינם קשורים ל-auth.
>
> **מה שאינו נטען.** ‏Apple הוא `NEVER_EXERCISED` — חלש יותר מ-`PROVIDER_NOT_CONFIGURED` של
> Google. בלי חברות ב-Apple Developer Program אין Service ID ואין מפתח חתימה, והקוד מעולם לא
> דיבר עם Apple. ‏`#270` — מה עושים עם כתובת Private Relay — **נשאר פתוח** ודורש הכרעה לפני
> הדלקה. ‏`public-signup/index.ts` עדיין ללא בדיקת יחידה וללא typecheck ב-CI; שומר התאמת הספק
> החדש נשען על הפער הזה.

עודכן: 25.08.2026 — **העוזר רץ מקצה לקצה מקומית ונשאר כבוי בייצור: אין חברה שתחתום על ה-DPA.
המחיר ירד מכל משטח ציבורי, ו**הרולאאוט בוצע: ייצור עומד על `0209`** (קריאה חיה, 25.08) — אפס
מיגרציות ממתינות.**

> **הכרעת הבעלים על הפעלת העוזר (25.08.2026) — ומה היא שינתה, ומה לא.**
>
> **מה שהשתנה:** הסיבה שבגללה שורת `dpa` חסרה. עד 25.08 נרשם שהבעלים *בחר* לדחות חתימה זמינה,
> וזה לא היה מדויק. ‏DPA נכרת בין **ישות משפטית** לבין `OpenAI OpCo, LLC`, ו**חברה טרם נרשמה** —
> כלומר אין צד מתקשר, ולא נייר שלא הודפס. החתימה נקבעה **לזמן ההשקה**, כשהחברה קיימת, ולפני
> הדייר הראשון. זו בדיוק הדרישה כפי שנוסחה מלכתחילה: „חתימה לפני דייר ראשון", לא „לפני כל
> הפעלה".
>
> **מה שלא השתנה, למרות שהבעלים הורה להפעיל בייצור:** ‏**העוזר כבוי בייצור.**
> ‏`assertProviderGovernance` דורש `VERIFIED` בכל חמש השורות, ו-`dpa` אינה. **אין לפתוח את השער
> בכתיבת `VERIFIED`** — הסטטוס הוא טענה על חוזה קיים, והוא נגזר החוצה ל-`/privacy` ול-`/terms`.
> הדרך הכשרה היחידה היא משטח חריג מפורש בקוד — לפני-השקה בלבד, לארגון של הבעלים בלבד, נרשם
> ב-`audit_logs`, נסגר עם הדייר הראשון — ו**הוא טרם נכתב**. ההוראה מתועדת ב-`DEBT §63`
> וב-`#179` כ-`DECIDED / GATE_REFUSES / NOT_ACTIVATED`, ולא כמשהו שבוצע.
>
> **ומה שכן הוכח, ומבטל טענה שהופיעה כאן קודם.** ‏„המערכת מעולם לא קראה למודל" נכון **לייצור
> בלבד**. ב-24.08.2026, מול הסטאק המקומי, העוזר רץ מקצה לקצה: שש קריאות Edge ב-200, שתי תשובות
> עם כרטיסי ראיות (זיכויים פתוחים · חשבוניות ממתינות) ושרשור ששרד ריענון —
> `NIR-APP-DOCS/release-evidence/20260824-assistant-local/`. מה שחסם קודם לא היה מפתח, דגם או
> ממשל אלא **עשר סכמות כלים עם `required: []`**, שהחזירו `HTTP 400 invalid_function_parameters`
> מהספק והוצגו בדפדפן כ-`assistant_provider_unavailable`. תוקן, ו-`check:assistant-tool-schemas`
> שומר שלא יחזור. **מה שחסר להפעלה הוא רשות, לא יכולת.**
>
> **המחיר, באותה נשימה (`#267`).** שום סכום אינו מגיע יותר למשטח ציבורי: לא ב-`/pricing`
> באפליקציה, לא באתר השיווק, ולא ב-JSON-LD שלו. ‏`#195` ו-`#208` לא בוטלו — המחיר נמסר בתוך
> החשבון, בשלב המעבר למסלול בתשלום, שם כתובת החיוב ידועה. **מוזג ל-`main`, לא נפרס.**

---

24.08.2026 — **העוזר קיבל מכסה, שני read models, registry עזרה וטיוטת ספק. שער ממשל הספק נאכף
בקוד. העוזר נשאר כבוי, וארבע מתוך חמש שורות הראיה נסגרו באותו יום.**

> **מה שהיה שבור, במדויק.** ‏`0164` זרע את `assistant_runs.monthly` במצב "לא ידוע" לכל מסלול —
> המצב הכן כל עוד המספרים לא הוכרעו, ובפועל **העוזר לא היה זמין לאף דייר גם עם הדגל דלוק**.
> ‏`0184` השאיר זאת בכוונה וכתב למה: ‏`#198` קובע את מספרי המצב-היציב, אבל `#209` מציב לפניהם חלון
> ‏30 יום בעוגן **אחר**, וזריעת מספר המצב-היציב לבדו הייתה מקצה בחסר לכל ארגון חדש בחודשו הראשון.
> ‏`0202` סוגר את שני החצאים יחד, מפני שכל חצי לבדו הוא תשובה שגויה.
>
> **שני החלונות חופפים בכוונה.** חלון ההיכרות (`#209`) הוא 30 ימים רצופים מ**אימות המייל הראשון
> של הבעלים**; תקופת ה-usage‏ (`#242`) היא חודשית מרגע ההרשמה, ו-`0202` **אינו נוגע בה**. כמעט לכל
> ארגון ההיכרות חוצה גבול תקופה, ולכן הריצות בתוכה נספרות מ-`assistant_runs` עצמו ולא מהמונה
> הכרוך-בתקופה: איפוס מונה באמצע הקצאה של 50 היה מחלק 50 נוספות. זו גם הסיבה שמגביל הקצב סופר
> שורות ריצה — **מחיקת שיחה מטעמי פרטיות אינה מחזירה מכסה**. המונה ממשיך לזוז בתוך ההיכרות בכל
> זאת, כי ה-snapshot, ‏`platform_org_usage` והקריאות החשבונאיות קוראים אותו, וחודש תנועה שנספר
> כאפס היה הופך את שלושתם לשקר.
>
> **‏`business` נשאר "לא ידוע" בכוונה.** ‏`#198` אומר „מותאם", כלומר override פר-חוזה. מספר
> שמיגרציה תמציא לחוזה שאיש לא קרא הוא **ניחוש עסקי**, ו-`OPEN-DECISIONS:3` אוסר אותו. הוא מסרב
> עד שנכתב override, ו-`p77` מוכיח את שני הכיוונים.
>
> **‏`0203` — הבסיס בשלושה מקרים, ואף אחד אינו ניחוש.** שורת `price_history` בתאריך תחולה שלפני
> תחילת החודש היא הבסיס הסמכותי; אין שורה כזו אבל המחיר כבר היה בתוקף כשהחודש נפתח — זה **אפס
> נמדד**; אין שורה כזו והמחיר הופיע **בתוך** החודש — `לא ניתן למדוד`, מוחרג ולא נספר כאפס, כי אפס
> הוא טענה שהספק החזיק את המחיר ואנחנו לא יודעים זאת. ‏`private.supplier_price_effective_on()`
> מ-`0105` **לא נוצלה מחדש**: ה-fallback שלה ל-`current_price` הוא בדיוק מה שהיה מייצר את האפס
> המזויף. ‏`SECURITY INVOKER`, ולכן RLS הוא הגבול ורואה חשבון מקבל אפס שורות ולא שגיאה שמאשרת
> שהשורות קיימות. **אפס פטורי definer חדשים** — `check:exemptions` עבר עם pin 90.
>
> **הנוסחה נשארה מימוש אחד.** ‏`get_purchase_comparison` מייבא את `compareLine`/`summarizeComparison`
> מ-`src/lib/orderComparison.ts` במקום לשכפל אותן ב-SQL, כך שהמסך והעוזר אינם יכולים לסטות. כל
> השינוי בקובץ ההוא הוא סיומת `.ts` מפורשת בייבוא כדי ש-Deno יפתור אותו — אותו נימוק ש-`orderSplit.ts`
> כבר נותן לעצמו.
>
> **הממצא שהביקורת מצאה לפני המיזוג, והוא נכונות ולא סגנון.** שני הכלים הנפיקו כל עובדה כספית
> כ-`metric.money`. ‏`validateAnswer` מכריע תמיכה לפי kind, subject, unit וערך — כלומר **חיסכון
> ותשלום-עודף על אותו מוצר, באותו מטבע, היו חליפיים למאמת**, ורק התווית הבדילה. תוויות אינן
> ניתנות לבדיקה **בכוונה** (הכלל מ-20.08: בריכת הספרות מגיעה מערכי עובדות בלבד, אחרת ספק בשם
> „ספק 2000" מחלק ספרות חינם). ‏`orderComparison.ts` קיים בדיוק כדי ששני אלה לא יקוזזו, ואוצר
> המילים קיזז אותם בחזרה. עכשיו: ‏`comparison.saved_vs_next`, ‏`comparison.extra_vs_cheapest`,
> ו-`comparison.minimum_breach` כדי שחוסר-למינימום לא ייטען כסכום הסל אצל אותו ספק. **מאותו נימוק
> הפוך** מחיר הבסיס קיבל עובדה משלו: הוא היה כלוא בתווית, כלומר מספר שהעוזר קורא ואסור לו לומר —
> בזמן ש-`#189` דורש שיאמר אותו.
>
> **שער ממשל הספק (`#179`) הפסיק להיות טבלה במסמך.** ‏`governance.ts` מכריע את חמש שורות החובה,
> ו-`parseAssistantConfig` מסרב **לפני הדגם ולפני שאר הכוונונים** — גבול שעולה בלי ראיות אינו מגיע
> לספק כלל. המודול חסר-תלויות בכוונה: שער שנזקק לרשת כדי להכריע אם הרשת מותרת הוא מעגלי, ושער
> שקורא שעון עונה אחרת בשתי מכונות על אותה ראיה. **כל חמש השורות `MISSING` היום** ו-
> `docs/ASSISTANT-ACTIVATION-EVIDENCE.md` אומר זאת במפורש: התנאים המתוארכים של OpenAI אינם בריפו,
> ומשפט סביר במסמך הסכמה אינו ראיה חלשה יותר אלא **הצהרה כוזבת ללקוח**.
>
> **סתירה פנימית שנמצאה ונסגרה.** ‏`ASSISTANT.md §4.1` הצהיר ש"גם runner סינתטי אינו עוקף את חסמי
> הממשל" — **הוא כן עקף**: `live-evaluation.ts` בונה ספק ישירות ולא עובר ב-`parseAssistantConfig`.
> השער נוסף בראש `executeSyntheticLiveEvaluation`, ומקרה הבדיקה מחליף את `fetch` ב-stub זורק, כך
> ששער שיורה רק **אחרי** ניסיון יציאה נכשל ברעש ולא עובר כשגיאת ספק רגילה.
>
> **מדף הפרטיות הוסרה הבטחה שאסור היה להבטיח.** הדף אמר שמסמך שנשלח לפירוש **אינו נשמר** אצל ספק
> המודל. ‏`store: false` היא **בקשת API**, לא התחייבות הספק — ‏`ASSISTANT.md §5.1` אמר זאת באותו
> ריפו בזמן שדף ההסכמה אמר אחרת. ‏`TERMS_VERSION` עלה ל-`2026-08-24`; ההחתמה היא בקבלת הזמנה בלבד
> (`0089`), ולכן **הבאמפ נוגע למוזמנים חדשים ואינו מפריע למשתמש קיים**.
>
> **‏`#192` — אין ניחוש.** ‏`productHelpRegistry.ts` הוא מקור האמת היחיד, כל רשומה נושאת owner,
> ‏version, ‏source ו-`updated_at`, וה-`route` שלה הוא מפתח של `APP_ROUTE_POLICY` ולא מחרוזת — מסך
> שהוסר או תפקיד שזז **מפיל את השומר** במקום לשלוח הוראה מתה. תפקידי רשומה מצרים קהל של מסך ולעולם
> לא מרחיבים. עברית היא locale הבסיס; נושא שקיים רק בתרגום הוא פגם. הצעדים נכתבו **מהמסכים**:
> ‏„ייבוא רב־ספקים מ־Excel", ‏„אישור וייבוא", ‏„מתחת למינימום", ‏„איך המספרים כאן מחושבים" — כולם
> קיימים ככתבם ב-`PriceLists.tsx` ו-`Inventory.tsx`. שאלה שה-registry אינו עונה עליה נענית
> ‏`no_capability`.
>
> **‏`#191` — טיוטה שאינה יכולה לטעון שנשלחה.** בלוק `draft` נכנס לחוזה: התווית היא **קבוע של
> המוצר** ולא שדה של המודל, ‏`fact_ids` חובה, וכל ספרה בגוף מוצמדת לערך של עובדה מצוטטת בדיוק כמו
> טענה. ‏`accountant` **ותפקיד לא-ידוע** נדחים. ‏`scripts/check-assistant-no-send.mjs` סורק את כל
> משטח העוזר ונופל על `נשלח`, על egress חיצוני, על מפתח דגל רביעי ועל כתיבה ל-`purchase_orders`.
> **הוא הופרך לפני שנסמכנו עליו:** ארבע הזרקות אמיתיות, כל אחת הפילה אותו עם קובץ ושורה. המילה
> ‏`נשלח` חיה עכשיו במקום **אחד** — `ASSISTANT_SENT_CLAIM_MARKER` — כי הסירוב שאוסר אותה חייב לנקוב
> בה, ושומר שקורא מחרוזות אינו מבחין בין איסור ליכולת.
>
> **אימות.** ‏`npm run check` ירוק: build, ‏typecheck, שבעת השומרים, **1,502 בדיקות ב-151 קבצים**.
> ‏**208 בדיקות Deno** ירוקות עם lock קפוא ו-`deno check` נקי. תשע סוויטות SQL הורצו מול הסטאק
> המקומי אחרי `0202`/`0203`: ‏`p77`, ‏`p51`, ‏`p56`, ‏`p70`, ‏`p4`, ‏`p52`, ‏`p57`, ‏`p58`, ‏`p61` —
> כולן `passed`. זרוע ה-concurrency ב-`p77` הראתה ריצה אחת נרשמת ו-`assistant_limit_reached`
> לשנייה: **בדיוק אחת עברה**.
>
> **מה שבמפורש לא קרה.** ‏**בייצור המערכת מעולם לא קראה למודל.** לא נפרס דבר — לא Edge, לא חזית,
> ולא מיגרציה לייצור. ‏`assistant.ui` **לא הודלק** לאף ארגון. שני חסמים בלתי-תלויים חוסמים
> הפעלה, ו-`DEBT-REGISTER §63` מפרט את שניהם: **ה-DPA נדחה בהכרעת בעלים 24.08.2026** · אין
> הרשאת בעלים מפורשת להפעלה. ארבע שורות `#179` האחרות — אימון, שימור, לוגים, אזור — נסגרו
> באותו יום מהעמודים הרשמיים המתוארכים של OpenAI. **דחייה מאושרת אינה פותחת את השער:**
> ‏`assertProviderGovernance` דורש `VERIFIED` בכל חמש השורות, ושורה דחויה אינה `VERIFIED`.
> בתצפית בעלים בחשבון: שיתוף לאימון **מכובה**, ובקרת השימור **מכובה** — כלומר שימור ברירת
> המחדל של עד 30 יום חל עלינו, בדיוק כפי שדף הפרטיות אומר.
>
> **תוקן למחרת, 25.08.2026 — ראו את הרשומה בראש הקובץ.** „ה-DPA נדחה" תיאר בחירה שלא הייתה:
> אין ישות משפטית שתחתום, והחתימה נקבעה לזמן ההשקה. הסטטוס לא זז — הסיבה כן. באותה רשומה גם
> ההוראה להפעיל בייצור, ו**למה היא לא בוצעה**.
>
> **תיקון לאותו יום.** הגרסה הראשונה של הפסקה הזו מנתה חסם שלישי — „קובץ הטוקן אינו קיים בנתיב
> הקנוני". **זו הייתה טעות.** ‏`LOCAL-CREDENTIALS-PATH.md` נקרא מעותק עבודה מיושן במקום מהענף
> שנגדו עבדנו; ‏`origin/main` החזיק את הנתיב הנכון (`AI\API\NIR-TOKEN-SUPABASE.txt`) כבר בקומיט
> הבסיס, ואף אמר במפורש שהנתיב הישן אינו קיים יותר. הטוקן נטען ועבד.
>
> **ומה שהקריאה החיה גילתה בפועל:** ראש ה-ledger בייצור הוא **`0170`**. כל המיגרציות מ-`0171`
> ומעלה מוזגו ל-`main` ו**לא הוחלו בייצור** — הגלים של financial, ‏document, ‏billing, ‏domain,
> ‏identity והעוזר. זו עובדת rollout, לא חסם הפעלה. ‏`0202`/`0203` נשארים מספרים בטוחים: הם מעל
> כל מה שקיים ב-Git ומעל ראש ה-ledger החי.

עודכן: 24.08.2026 — **‏`https://app.inplace.digital` חי ומאומת. תצורה בלבד: אין build חדש, אין פריסה חדשה ואין שינוי קוד.**

> **מה חי עכשיו.** האזור `inplace.digital` (‏id `82a4bdef…7b5a`) **‏active** ב-Cloudflare אחרי שה-nameservers
> ברשם הוחלפו ל-`clyde.ns.cloudflare.com`/`rose.ns.cloudflare.com` (‏RDAP `last changed`
> ‏`2026-08-23T22:07:04Z`). ‏`app.inplace.digital` מחובר כ-custom domain לפרויקט ה-Pages **הקיים**
> ‏`supplyflow` — `status=active`, ‏`cert=active` — דרך `CNAME app → supplyflow-baq.pages.dev`
> ‏(proxied). ‏**לא נוצר עותק שני של האפליקציה:** ה-entry chunks בשתי הכתובות זהים
> ‏(`index-Do0SLF1a.js`, ‏`client-DW-RYYJh.js`, ‏`format-BW5Zejev.js`, ‏`recharts-BhAWep5w.js`),
> כלומר שתיהן מגישות את פריסת הייצור `e851dbe8-fcc6-450d-b5b8-d80aace67da0` ‏(`15baeac`).
>
> **למה לא נדרש build.** ה-frontend אינו נושא כתובת בסיס. הלקוח בונה כל קישור מוחלט מ-
> `window.location.origin` — ‏`src/lib/supplierPortal.ts:11` ו-`src/pages/ForgotPassword.tsx:33` —
> ולכן **אותו bundle בדיוק** נכון לשתי הכתובות. אין `VITE_APP_BASE_URL` בחוזה, ולא נוסף כזה.
> הכתובת המוחלטת היחידה היא שרתית: הסוד `APP_BASE_URL`.
>
> **מה שונה בתצורה, ומה נשמר.** ‏`ALLOWED_ORIGINS` **הורחב תוספתית** ל-
> `pages.dev, localhost:5199, app.inplace.digital` — הסדר נשמר כך ש-`allowed[0]` לא זז; מקור לא-מורשה
> עדיין נדחה ואין `*`. ‏`APP_BASE_URL` הועבר ל-`https://app.inplace.digital`. ב-Supabase Auth ה-Site URL
> **היה `http://localhost:3000`** — ברירת המחדל של Supabase, מעולם לא הוצבעה לייצור — והוא עכשיו
> `https://app.inplace.digital`; ה-allowlist **הורחב ולא הוחלף**:
> ‏`…pages.dev/reset-password` **נשאר** לצד `…app.inplace.digital/reset-password`, כדי שקישורי שחזור
> שכבר נשלחו ימשיכו לנחות. אין wildcard.
>
> **‏`supplyflow-baq.pages.dev` נשאר חי** כ-origin לגלגול אחורה; לא נוצרה ממנו הפניה למשתמש.
>
> **ה-apex ו-`www` אינם מוגדרים — במכוון.** סריקת הייבוא של Cloudflare משכה את ברירות המחדל של
> ‏NameCheap, ובהן `A inplace.digital → 192.64.119.114` (דף חניה) ו-`CNAME www → parkingpage.namecheap.com`,
> שתיהן proxied. **שתיהן נמחקו לפני שההאצלה הפכה סמכותית**, ולכן `https://inplace.digital` ו-
> `https://www.inplace.digital` אינם מגישים דבר (`www` מחזיר NXDOMAIN). רשומות ה-MX וה-SPF של השורש
> **לא נגעו** — הן מישור הדואר, ומטופלות בנפרד.
>
> **אימות חי, לא הסקה.** ‏14 נתיבים על הכתובת החדשה מחזירים `200` ו-`/operator.html` מחזיר `308`
> ל-`/operator`. ‏Playwright headed-equivalent מול הייצור עם שלוש זהויות הבדיקה המורשות: התחברות 3/3,
> התנתקות 3/3, deep-link ל-`/suppliers` ורענון מלא שומרים סשן 3/3, **אפס** שגיאות console, ‏pageerror,
> ‏CORS ו-HTTP≥400, אפס גלישה אופקית ב-1440×900 וב-390×844. בקשת השחזור **נלכדה ובוטלה לפני שיצאה**
> והוכיחה `redirect_to=https://app.inplace.digital/reset-password` — בדיוק הערך שב-allowlist.
> ‏`/accept-invite` ו-`/portal` מגישים את המסכים שלהם עם הודעת "קישור לא תקין", לא 404.
> **גבול המפעיל לא זז:** ‏`office` ו-`accountant` מנותבים מ-`/operator` לדשבורד שלהם, ואותה סוויטה
> בדיוק רצה כביקורת מול `pages.dev` והחזירה סיכום **זהה בייטית** — כלומר ההתנהגות היא של המוצר, לא
> של המעבר.
>
> **מה שבמפורש לא קרה בשלב הזה:** לא Twilio, לא חיוב, לא Assistant, לא דגל, לא מיגרציה,
> לא רשומה עסקית. ראיות: `artifacts/domain-cutover/`.

עודכן: 24.08.2026 — **תשתית הדואר הופעלה על אותו דומיין: ‏Resend מאומת, ‏Auth מחובר ל-SMTP, וחסם
שני-המיילים-בשעה הוסר.** תצורה בלבד; אין קוד, build או פריסה חדשים.

> **החסם שנמצא במדידה.** ‏`rate_limit_email_sent` בייצור היה **`2`** — שני מיילים בשעה **לכל
> הפרויקט**, לא לכל משתמש — דרך שירות הפיתוח המשותף של Supabase ומכתובת `supabase.co`. שלושה
> לקוחות שמאפסים סיסמה באותה שעה, והשלישי לא מקבל דבר בלי שגיאה. זו לא הגדרה שכדאי לשפר אלא
> **חוסם השקה**, והוא היה שם בשקט. עכשיו `100`, וניתן להעלאה רק מפני שיש SMTP מותאם.
>
> **מה חי.** ‏`inplace.digital` **מאומת** ב-Resend (`eu-west-1`, אירופה) עם DKIM ו-SPF; ‏Auth שולח
> דרך `smtp.resend.com:465` בזהות `InPlace <no-reply@inplace.digital>`; ‏`INVITE_FROM_EMAIL`
> ו-`ORDERS_FROM_EMAIL` נושאים את אותה כתובת (‏`ORDERS_FROM_EMAIL` **לא היה קיים כלל** קודם,
> ו-`email-sender` נפל חזרה ל-invite). ‏DMARC פורסם כ-`p=none` עם `rua=mailto:dmarc@inplace.digital`,
> ו-Cloudflare Email Routing מעביר את התיבה הזו לבעלים — `enabled=true`, ‏catch-all **כבוי**.
>
> **סיסמת ה-SMTP היא מפתח Resend המוגבל-שליחה**, לא המלא. ל-Auth אין שום צורך בניהול דומיינים.
>
> **שני כיוונים נמדדו, לא הוסקו.** יוצא: הודעה נשלחה ונמדדה `delivered`, ו-Gmail החזיר **SPF,
> ‏DKIM ו-DMARC ‏`PASS`** במקור ההודעה. נכנס: הודעה נשלחה אל `dmarc@inplace.digital` ונחתה בתיבת
> הבעלים דרך Cloudflare — כלומר דוחות ה-`rua` שהרשומה מבקשת אכן ינחתו במקום שנקרא, וזה מה שהופך
> את המסלול `none → quarantine → reject` לתוכנית עם תנאי מעבר מדידים ולא להצהרה.
>
> **שני משטחים על אותו שם, בלי התנגשות:** ‏Resend **שולח** דרך `send.` ו-`resend._domainkey`;
> ‏Cloudflare **מקבל** דרך השורש ו-`cf2024-1._domainkey`. חמש רשומות ה-MX של NameCheap ורשומת
> ה-SPF שלהן נמחקו — הבעלים אישר שלא היה דואר פעיל, ושתי רשומות SPF על אותו שם פוסלות זו את זו.
> אחרי המחיקה נמדד ש-Resend נשאר `verified` על שלוש רשומותיו.
>
> **מה שאסור לטעון: מייל Auth אמיתי לא נשלח.** חשבונות הבדיקה הם `@gamos.demo` — דומיין מזויף —
> ו-`recover` לכתובת שאינה קיימת מחזיר `200` בלי לשלוח, מטעמי אי-מנייה. ‏Supabase אימת את חיבור
> ה-SMTP בשמירה; ההוכחה הסופית היא המייל האמיתי הראשון. ‏`RESEND_WEBHOOK_SECRET` ו-`email-webhook`
> עדיין אינם, ולכן accepted עדיין אינו delivered במסלול המוצר.
>
> **חוב שנפתח:** תבניות מיילי ה-Auth הן ברירות מחדל **באנגלית** במוצר עברי RTL.

עודכן: 23.08.2026 — **מועמד המיזוג דרך `0170` שוחרר ל־Production; Assistant וקריאות לספקי צד־שלישי נשארו כבויים.**

> **מקור runtime וראיות:** ‏PR #97 תיקן את קריאת ה־flags האנונימית ומוזג כ־
> `15baeac6bc7a3340089a07507a628d76860369b0`. ‏Build, verify ו־browser/a11y עברו גם על head ה־PR
> וגם על merge SHA זה; ‏`npm run check` עבר עם 1,282 בדיקות. ‏Production DB הוחל forward-only
> ובסדר `0168`→`0169`→`0170`; ראש ה־ledger הוא `0170` עם 169 שורות, ‏A5/A6 הם 0, ושלושת
> מקורות ה־ledger תואמים בשם, באורך וב־SHA-256 לקבצי ה־runtime. גיבוי roles/schema/data נוצר
> וה־hash של כל קובץ אומת לפני apply. ארבע חריגות P1 היסטוריות נשארו באותו fingerprint
> ‏`7/7/2/58`; ‏42/46 הבדיקות האחרות אפס ולא נוצרה חריגה חדשה.
>
> **Edge ו־Pages:** ‏`public-signup` פעיל v1 עם `verify_jwt=false`; ‏`email-sender` ו־`assistant`
> פעילים v1 עם `verify_jwt=true`. בדיקות OPTIONS/auth-negative עברו. ‏Pages deployment
> ‏`e851dbe8-fcc6-450d-b5b8-d80aace67da0` בפרויקט `supplyflow` נושא source ‏`15baeac`;
> ה־canonical וה־unique התאימו 111/111. ‏36 תרחישי public עברו בדסקטופ וב־390px ללא
> console/page/HTTP errors או overflow. ‏owner/office/accountant ‏3/3 עברו smoke מחובר,
> קריאה בלבד, בדשבורד הקנוני בדסקטופ. ספירות העסק נשארו זהות.
>
> **גבול activation:** ‏Assistant מוסתר בשלושת התפקידים, flags/policy נשארו off, מכסת הריצות
> אינה measured, מספר הריצות 0 ואין `AI_ASSISTANT_*`; לא הייתה קריאת מודל. טבלאות מסירת המייל
> נשארו עם 0 preferences ו־0 messages; לא הייתה קריאת Resend או טענת delivered. ‏Twilio,
> Paddle, Stripe ו־Morning לא הוגדרו ולא נקראו. פריסה אינה activation.

עודכן: 23.08.2026 — **PR #95 מיזג את יישור התיעוד מעל PR #93/#94; לא בוצע rollout או deploy במסגרת העדכון.**

> **גבול מקור המוצר והבדיקות:** המקור דרך PR #94 (תיקון test-only) ו־PR #93 (מותג) הוא merge
> ‏`bac330d`; ‏PR #95 מיזג מעליו תיעוד בלבד. commits של docs-only יכולים לקדם את `origin/main`
> בלי לשנות runtime, ולכן את ref ה־Git החי קוראים מ־Git בזמן הבדיקה ואינם מקפיאים כאן כ־SHA נוכחי. סדר
> המיגרציות בריפו הוא Assistant ‏`0164`–`0166`, Supplier Portal ‏`0167`, Email Delivery ‏`0168`,
> Customer Operations hardening ‏`0169`, Assistant history UI ‏`0170`. Production ledger הוכח
> עד `0167`; ‏Pages הוכח על source ‏`6430d26`; מבין משטחי Edge החדשים רק `supplier-portal`
> הוכח פעיל. ‏`0168`–`0170`, ‏`email-sender` ו־Assistant Edge/UI אינם מוכחים כפרוסים.
> ‏PR #93 שילב את מותג InPlace הסופי עם ראיות local, ‏CI ו־visual; סטטוס המותג הוא
> `IMPLEMENTED / MERGED / NOT_DEPLOYED`. ‏Production Pages נשאר מוכח רק על `6430d26`; לא בוצעה
> פריסת Pages. ‏PR #94 סגר את שני מרוצי browser harness; ‏DEBT §54 נשאר לא פעיל ולא נפתח מחדש.
>
> **הכרעות אינן runtime:** ‏#194–#262 נוספו ל־`OPEN-DECISIONS.md`. מנויים/מחירים, Paddle,
> Stripe+Morning, ‏Resend domain/SMTP, ‏Twilio, ‏`inplace.digital`, ‏Rules Engine ו־Report Jobs הם
> `DECIDED_FOR_PLANNING / NOT_IMPLEMENTED_OR_CONFIGURED / NOT_ACTIVATED / NOT_DEPLOYED` לפי כל
> שורה. החריגים שכבר מומשו ומוזגו נשארים מסווגים כך: Supplier Portal ‏`LIVE`; Email B1
> ‏`IMPLEMENTED / MERGED / NOT_DEPLOYED`; Assistant modality/history ‏#262
> ‏`IMPLEMENTED / MERGED / NOT_DEPLOYED`. ‏Resend bounce webhook הוא עבודה עתידית נפרדת.

> **כלל קריאה:** פסקאות `עודכן:` מתארות ראיה בנקודת הזמן שלהן. ניסוח היסטורי כמו
> “Production נמצא כעת”, migration head או asset SHA בתוך רשומה ישנה אינו מצב נוכחי. רק גבול
> 23.08.2026 למעלה וסעיף “השינויים הממתינים לשחרור” למטה הם מקור המצב הנוכחי.

עודכן: 21.08.2026 — **Customer Operations hardening מוזג ב-PR #89 (`7e981ad`); לא בוצע rollout. המיגרציה forward-only היא `0169`.**

> **למה נדרש follow-up ולא עריכת היסטוריה.** הסקירה האחרונה הסתיימה אחרי המיזוג ומצאה ארבעה
> כשלים: `/pricing` הציבורי חסר `anon` grant/RLS; החלטת rate-limit של signup אינה מסוריאלית;
> גבול עמודים מדויק מאפשר מסמך נוסף כשהמונה שווה למכסה; ו-retry מקביל של אותו מסמך עלול להידחות
> אחרי שהמתין לנעילת המונה. בנוסף, תשובת כשל signup אנונימית החזירה UUID ושגיאות cleanup.
> המיגרציות `0154`, ‏`0159`, ‏`0162` ו-`0163` כבר committed ולכן אינן משתנות. ‏`0169` מחליפה את
> גופי הפונקציות ומוסיפה את grants/policies קדימה בלבד; תיקון ה-Edge נפרד ואינו חושף leftovers.
>
> **ראיית RED/GREEN ומיזוג.** על stack חדש עד `0163`, ‏`p51` נפל על קטלוג
> ציבורי חסום, ‏`p52` נפל על גבול העמודים ו-`p55` נפל על היעדר serialization. לאחר `0169` שלושתן
> עברו. בדיקת מקביליות אמיתית החזירה job יחיד לשתי קריאות מסמך, וב-12 קריאות signup מקבילות
> התקבלו 5, נחסמו 7 ונרשמו 12. על הבסיס הסופי `99b454c`, סדר migrations ‏`0168`→`0169`→`0170`
> עבר ב-DB חדש; ‏`p51`/`p52`/`p55`/`p56`/`p61` ו-preflight ‏46/46 עברו. ‏`npm run check` עבר
> עם 1,281 בדיקות ב-134 קבצים. כל checks של head ‏`14b087f` ושל merge ‏`7e981ad` עברו:
> Deno, verify, SQL/preflight ו-browser/a11y, אפס failures.
>
> **סדר האינטגרציה החי:** Assistant הוא `0164`–`0166`; Supplier Portal הוא `0167`; Email Delivery
> הוא `0168`; hardening הוא `0169`; Assistant UI הוא `0170`. המיזוג לא אישר ולא ביצע החלת DB,
> Edge deploy, Pages deploy או smoke חי עבור hardening.

עודכן: 20.08.2026 (ג) — **שלב A, פורטל ההזמנה לספק, מוזג ונפרס לייצור עם ראיות Git/CI/DB/Edge/Pages ו־live E2E מלאות.**

> **Git/CI.** ‏PR #86 מוזג: head ‏`d857d7bc795b70a1d7364a3414a564d521be912b`, ‏merge
> ‏`6430d260edd684bb001fc39d4dd81814f2903275`; ‏8/8 checks עברו על ה-head המדויק, כולל browser
> artifact ללא failures/skips/console errors ועם שמונה צילומי he/RTL ו-en/LTR, ללא overflow.
>
> **מסד ו־Edge.** גיבוי roles/schema/data מלא נשמר מחוץ לריפו תחת
> `NIR-APP-DOCS/backups/20260820-1755-pre-0151-0167-6430d260`, עם מניפסט ו-SHA-256. כל
> ‏`0151`–`0167` עברו dry-run בטרנזקציה אחת ו-rollback מוכח. ‏`supabase db push` נעצר לפני חיבור
> ב-`EAUTHQUERY auth_query secret check timed out` וה-ledger נשאר `0150`; ההחלה החלופית בוצעה
> דרך Management API כך שכל migration וה-ledger נכתבו באותה טרנזקציה. postflight אימת version,
> name, statement count, character count ו-SHA-256 לכל 17 הקבצים; ה-ledger הסתיים ב-`0167`,
> ‏A5/A6 נשארו 0 והספירות העסקיות לא זזו. ‏`SUPPLIER_PORTAL_RATE_LIMIT_PEPPER` נוצר והוגדר בלי
> להירשם, ו-Edge ‏`supplier-portal` נפרס לבדו כ-`ACTIVE`, גרסה 1, ‏`verify_jwt=false`.
>
> **Pages.** build ייצור נקי נבנה מאותו tree של merge SHA, עם host Supabase יחיד וללא
> ‏`54321`, ‏`service_role` או demo seed. הפריסה לפרויקט `supplyflow` קיבלה unique URL
> ‏`https://8096d4e1.supplyflow-baq.pages.dev`; ‏106 קבצים הושוו byte-for-byte מול ה-unique
> והכתובת הקנונית — ‏212/212 התאמות SHA-256. ‏`/portal` ו-`/portal.html` מגישים רק
> ‏`portal-BIAb3xb5.js`; ‏`/` מגיש רק `index-CHZYDPr2.js`.
>
> **live E2E מבוקר.** חשבון owner מורשה הנפיק קישור חי להזמנת fixture ‏#239; דפדפן anonymous
> פתח את הפורטל והגיש שינוי מובנה מ-2×₪10 ל-3×₪11. ‏Edge החזיר 200 ל-resolve, submit ו-resolve
> חוזר — ללא 503, console error, כשל רשת אמיתי או overflow ב-390/1440. ההצעה
> ‏`068ca0ab-41ae-4f9f-8768-31a6a6c547dd` התקבלה, הוחלטה ונוצרה רוויזיה ‏#240
> (`014bb620-4dab-4850-bdf4-c7c041c77bd4`). postflight הוכיח שהמקורית נשארה עם 2×₪10,
> קישור קבלה אחד וקישור חשבונית אחד; הרוויזיה מכילה 3×₪11; וכל ששת אירועי ה-audit קיימים פעם אחת.
> אחרי fixture: ארגונים 1, מוצרים 271, ספקים 22, חשבוניות 15, הזמנות 240, links 1, proposals 1;
> ‏A5/A6=0. צילומי open/submitted/review/original/revision נבדקו חזותית.

עודכן: 20.08.2026 — **בעל המוצר אישר את חלופה B; Core ו־history UI מומשו ומוזגו ב־PR #90. אין activation או deploy.**

> **הכרעת UI.** בדסקטופ העוזר הוא פאנל docked ו־non-modal ברוחב 27.5rem; המסך הראשי שומר לו מקום
> והמקור נפתח לצדו. מתחת ל־1024px הוא modal/dialog במסך מלא; פתיחת מקור עוברת למסך המוצר, והטריגר הופך
> ל־“חזרה לבדיקה” ושומר result, focus ונתיב חזרה. השאלה, freshness, partial/null, מקורות ותוויות
> הכלים האנושיות נשארים גלויים. אף משטח אינו route עצמאי. ‏`Sparkles`, שמות tools ומנטל־מודל
> “שיחה” הוסרו מהמשטח.
>
> **History.** ‏`0170_assistant_history_ui_handoff.sql` משאירה את טבלאות הטקסט ללא browser SELECT
> ומוסיפה candidate list ו־structured snapshot לשירות בלבד. ה־Edge מחזיר metadata/content רק אחרי
> actor/source/route/text reauthorization נוכחיים. cache key נושא authorization fingerprint;
> role/org/lifecycle change מוחק state, ותשובת history שהחלה לפני השינוי נזרקת. ‏`p61` עבר מקומית.
>
> **ראיה ויזואלית אמיתית.** production components הורצו מול Supabase מקומי ו־Edge response מבודד:
> ‏15/15 תרחישים בשלושת התפקידים וב־390/768/1023/1024/1440, ‏40 screenshots, ‏0 console errors,
> ‏0 overflow, יעדי מגע 44px, source/return/history ויחס ניגודיות מינימלי 6.64:1. flags הופעלו
> דרך `platform_set_org_flag` לצורך QA בלבד והוחזרו ל־off אחריו.
>
> ‏PR #90 מוזג לאחר required checks על ה־head שלו. המספור `0170`/`p61` יושב אחרי `0169`; אין
> עריכת מיגרציה committed. הסטטוס: `IMPLEMENTED / MERGED / NOT_ACTIVATED / NOT_DEPLOYED`.
> Production ledger נשאר ב־`0167`, Pages נשאר על `6430d26`, ו־Assistant Edge לא הוכח פעיל.
> אין קריאת מודל חיה, action composer או rollout.

עודכן: 20.08.2026 — **העוזר של InPlace נבנה: מיגרציות `0164`–`0166`, פונקציית Edge, ‏13 כלים, פאנל.
כבוי לחלוטין, ומעולם לא דיבר עם מודל.**

> **מה זה, ומה זה לא.** לא צ׳אט. עוזר תפעולי שכל טענה כמותית שלו נשענת על ערך שהשרת חישב, עם מזהה
> שאפשר לאמת. המפרט של הבעלים נוסח סביב "עדות לפני רהיטות", והחצי המכני של זה נאכף בקוד: **בלוק
> טקסט אינו רשאי להכיל ספרה.** כל כמות חייבת לשבת בבלוק `claim` שנשען על לפחות עובדה אחת שהריצה
> הנוכחית הנפיקה. תשובה שלא עוברת את האימות נדחית — ולא מוצגת פרוזה במקומה.
>
> **מה שכבר היה, ולא נכתב מחדש.** למוצר היה "עוזר ללא מודל": `summary.ts` (חמישה מדדים) ו-`alerts.ts`
> (שישה סורקים מעל RPC קיימות). היוזמה עוטפת אותם. **וממצא שהפך את סעיף 8 במפרט על פיו:**
> ‏`Summary.lines` חושב מאז ומעולם ו**מעולם לא רונדר** — ‏`/alerts` הוא הצרכן היחיד והוא קורא רק
> ‏`alerts`/`complete`/`failures`. חמש שאילתות רצו בכל כניסה ותוצאותיהן נזרקו. העוזר אינו "עוד צרכן"
> של הסיכום; הוא הצרכן הראשון בפועל.
>
> **‏`0165` — הגדרה אחת לשני צרכנים.** ‏`p2_business_summary_rows()` ב-`SECURITY INVOKER`, חמישה
> מדדים, כל אחד בבלוק חריגה משלו — התאום ב-SQL של ה-`Promise.allSettled` שהיה בלקוח. הוכח במדידה:
> הפלת אחת מהפונקציות הפנימיות בתוך הטרנזקציה מחזירה את המדד שלה `measured=false` ואת ארבעת האחרים
> בערכיהם המדויקים. שתי ה-RPC הפנימיות הן invoker, ולכן **לא נדרשה ולו שורת פטור אחת** ברשם.
> ‏`/alerts` לא שינה התנהגות; חמישה סבבי רשת הפכו לאחד.
>
> **‏`0164` — שמונה טבלאות, ושתי הכרעות שנכפו על ידי המסמכים ולא על ידי הטעם.** ‏(א) המתג של פעולה
> מאושרת **אינו דגל**: ‏`ENTERPRISE-SECURITY-MODEL §8` אוסר על דגל להרחיב הרשאה ו-`p4_flags_identity`
> אוכף מבנית שהמעריך לא יופיע בשום מסלול הרשאה, ולכן הוא מדיניות בדפוס `0076` — בסיס כבוי ב-CHECK,
> הדלקה רק בפקודת מפעיל עם סיבה וביקורת. שלושה דגלים, פין `3 → 6`. ‏(ב) זכאות `assistant.enabled`
> **נמחקה**: ‏`OPEN-DECISIONS #158` קובע שמסלולים נבדלים בנפח בלבד, וזכאות יכולת הייתה הופכת הכרעת
> בעלים בדלת צדדית. נשארה מכסה מספרית אחת, **זרועה כ"לא ידוע" ולכן מסרבת** עד שהבעלים ייתן מספר.
>
> **החוב האמיתי היחיד שהסקירה מצאה, ונסגר.** המכסה הייתה בדיקה מקדימה בלבד: ‏`record_usage_event`
> נקרא בלי `assert_usage_within_limit`, והדלת המקדימה אינה נועלת. כלומר N בקשות מקבילות בקצה המכסה
> כולן עוברות וכולן מגיעות לספק בתשלום. התיאור "חריגה חסומה באחת, כמו `0162`" לא החזיק: ‏`0162` זוכה
> בתכונה הזו **מפני שהוא נועל ומאמת לפני הכתיבה**. עכשיו `assistant_record_run` עושה בדיוק את זה
> בדפוס `0155` — נעילה, אימות, ספירה, טרנזקציה אחת — ועוגן מצמיד את **ארבעת** הקריאות בגוף הפונקציה,
> כך שהסרה שקטה של אחת מהן מפילה את המיגרציה. הגבלת קצב של 30 ריצות למשתמש בשעה נספרת מ-`assistant_runs`
> עצמו, כך שמחיקת שיחה מטעמי פרטיות אינה מאפסת את החלון.
>
> **‏`0166` — הסוג השמיני של ה-egress.** הקריאה לספק עוברת בגבול הקיים ולא בגבול חדש. הוספת `'assistant'`
> דרשה לגעת ב-CHECK ובגוף פונקציית ההזמנה, ו**הנחה שלי הופרכה במדידה**: אמרתי שגוף הפונקציה מוצמד
> ב-hash ברשם — הוא אינו. הסוכן שאל את הרשם החי, בנה שמירה מחמירה יותר (סירוב אם הגוף אינו זהה לגוף
> שנסקר ב-`0132`, וסירוב אם הפונקציה תרכוש הצמדה בעתיד), ו**סירב להכניס שורת pin** כי `enforcement_kind`
> מתיר רק `assert_unit`/`filtered_read` ושניהם שקריים כאן — שורה כזו הייתה מזייפת את פנקס A5. שני
> ערכי גישה מתים (`trial`, `grace`) נשמרו תו-בתו: ניקוי שקט של פרדיקט אבטחה בתוך מיגרציה לא-קשורה
> הוא איך שגבול זז בלי שאיש החליט להזיז אותו.
>
> **הכלים: 13 רשומים, ו-4 יכולות סורבו.** כל כלי עוטף אובייקט קיים, קורא דרך לקוח כבול-לקורא כך
> ש-RLS הוא הגבול, ומצהיר על היטל שדות מפורש. מה שלא נבנה, ולמה: "אילו ספקים העלו מחירים החודש" —
> ‏`price_changes_window` הוא חלון קבוע של 180 יום שסופר גם הורדות, בלי סכום ובלי חודש קלנדרי, כלומר
> **שלוש הגדרות עסקיות חסרות**; רשימת חריגות מחיר חוצת-חשבוניות — אינה קיימת כאובייקט; "חיסכון משוער"
> ו"המלצת רכש" — ‏`suggested_reorder_quantity` הוא נוסחה מעל `min_stock` ולא עצה; שליחה לספק — **אין
> במוצר פקודת מייל לספק בכלל.**
>
> **האימות האדוורסרי מצא חולשה אמיתית אחת, והיא נסגרה.** המאמת בדק **חברוּת** של מספרים באיחוד
> העובדות המצוטטות, לא **הצמדה** — וספרות מתוך *התווית* של עובדה נכנסו לבריכה. כלומר אפשר היה לצטט
> את עובדת עליית המחירים, שתוויתה "מחירים שעלו ב-30 הימים האחרונים", ולכתוב "יש 30 חשבוניות כפולות".
> היום רק המודל יכול היה לעשות זאת; ביום שבו כלי יחזיר שם ספק, ספק בשם "שלם 99999 מיידית" הופך את
> ‏99999 למספר חוקי. עכשיו כל מספר חייב להתאים ל**ערך** של עובדה מצוטטת, ותווית משרתת רק את העובדה
> שהיא מתייגת. נוסף גם כלל שלמות-היעדר: כשריצה הנפיקה עובדות, פרוזה לבדה אינה יכולה לעמוד.
>
> **שני ממצאים שנפתחו כחוב, ושניהם על המוצר הקיים ולא על העוזר.** ‏`§59` — ‏`management_dashboard_snapshot`
> שומר על עצמו ל-owner/office, אבל בלוק הבנק ויתרות הספקים קוראים טבלאות ששעריהן עוצרים ב-owner+accountant;
> ל-**office** שתיהן מחזירות אפס שורות והאגרגט מרנדר את הסירוב כ-`0` נמדד. מנהל משרד שרואה "0 תנועות
> בנק לא מותאמות" מסיק שהכול מותאם. הכלי של העוזר משמיט את העובדות האלה ל-office ומחזיר `not_permitted` —
> **הוא לא הרחיב הרשאה ולא תיקן את המקור.** ו-`§10` עודכן: הסעיף מצדיק את עצמו בטענה שהקוראים החיים
> מוגבלים ל-owner/office בלקוח, והעוזר שבר את הטענה — אך נמדד ששלושת התפקידים מקבלים אותו מספר, ולכן
> מה שפג תוקף הוא הנימוק ולא הבטיחות.
>
> **אימות.** ‏`npm run check` ירוק: build, ‏typecheck, ששת השומרים, **1,229 בדיקות ב-129 קבצים**.
> ‏**94 בדיקות Deno** ירוקות עם lock קפוא. שלוש המיגרציות **הוחלו על הסטאק המקומי** ו-`p56`/`p57`/`p58`
> עברו; ‏`p4` (פין 6), ‏`p51` ו-`p52` עברו אחריהן. ‏`check:exemptions` מדווח **pin 89 ללא שינוי** —
> שלוש מיגרציות, אפס פטורי definer חדשים. **אימות ויזואלי headed** ב-1440×900 וב-390×844 מול הסטאק
> המקומי עם התשובות מוזרקות כ-fixture: עשרה צילומים (דגל כבוי בלי כפתור · פאנל פתוח · טענה עם עובדות
> ומקור · `null` שמוצג `—` · אזהרת סריקה חלקית · סירוב עם קישורים חיים ל-`/alerts`), **אפס גלישה
> אופקית** בשני הרוחבים, וניגודיות קישור המקור **10.85:1** שנמדדה בדפדפן.
>
> **מה שבמפורש לא קרה, ואסור שייקרא אחרת.** ‏**המערכת מעולם לא קראה למודל.** כל בדיקות הספק הן מול
> ספק מזויף; אין מפתח, אין דגם מוגדר, ואין ולו קריאה חיה אחת. ‏**לא נפרס דבר** — לא Edge, לא חזית,
> ולא מיגרציה לייצור. **אין פעולה מאושרת:** #182 הוכרע ל־read-only בהשקה, ו־#251 משאיר
> `purchase_orders`/`purchase_order_items` immutable. draft-only לטקסט לפי #191 אינו שליחה או
> action proposal. **אין מעבר מסך של קורא מסך**, ופתיחת שיחה קודמת אינה מחווטת עדיין.
> ‏**הכרעה אינה activation:** #179 קובע שהעוזר נשאר כבוי עד הוכחת ממשל הספק ועדכון legal;
> #180 הוחלף במספרי #198/#209, אך ה־runtime עדיין נזרע `unknown` ומסרב עד מיגרציה. לכן העוזר
> יסרב לכל דייר גם אם הדגל יודלק בטעות.
>
> **הכרעות #178–#183 נסגרו ב־21.08.2026**, אך פערי המימוש וההוכחה שלהן נשארים. מקור האמת
> להכרעות הוא `OPEN-DECISIONS.md`; הרשומה הזו מתעדת את מצב הקוד והפריסה בלבד.
עודכן: 20.08.2026 (ב) — **מסירת הזמנות במייל (שלב B1): העדפות תקשורת פר-ספק, תבניות he/en ממוספרות-גרסה, ‏ledger שליחה בדפוס 0028, ו-`sent` שנטבע רק על provider-accepted.** ‏(מוזג ב־PR #87; לא נפרס.)

> **מה נבנה.** ‏`0168`: ‏`supplier_communication_preferences` ‏(fail-closed — ברירת מחדל "ידני
> בלבד"; ערוץ ספק בלי יעד נדחה בשמירה), ‏`email_order_messages` — שרשור שליחה אחד פר-הזמנה עם
> ‏claim/lease/תקרת 5 ניסיונות/קפיאת `unknown` (הכללים של 0028), ‏Edge ‏`email-sender`
> ‏(verify_jwt=true) ששולח **Resend אמיתי** תחת egress lease חדש `supplier_order_email` ומיישב
> דרך `service_settle_email_order_message` — שגם מטביע `purchase_orders.sent` **רק** על
> ‏provider-accepted, באותה טרנזקציה עם הראיה (הכרעות #187–188). ה-claim מאציל ל-
> ‏`issue_supplier_order_link` כך שקישור פורטל טרי רוכב בכל מייל; תבניות he/en ‏escaped
> וממוספרות (`templates.ts`, ‏6 בדיקות Deno). ‏UI: כרטיס העדפות בכרטיס הספק, כרטיס מסירה במסך
> ההזמנה עם סולם הסטטוסים המלא (accepted≠delivered), איפוס בעלים מנומק.
> **‏A5:** ‏claim ‏self-enforcing; ‏settle לוקח exemption (התקדים של 0077) — ה-pin זז 89→90
> יחד עם p9. **‏A6:** שתי הטבלאות נרשמו; רשימת ה-egress kinds הורחבה בטלאי מעוגן (טבלה+פונקציה+TS).
>
> **אימות לאחר rebase:** ‏`npm run check` ירוק עם 1,249/1,249 בדיקות ב-133 קבצים וכל ששת
> השומרים; dry-run+rollback של `0168` ו-`p60` ירוק; ‏P58 המשולב ו-P9 ירוקים; ‏A5=0, ‏A6=0,
> pin ‏90 ו-ledger חזר ל-`0167`; ‏6/6 בדיקות Deno לתבניות ו-`deno check --frozen` ל-Edge ירוקים.
> שישה צילומי local headed מכסים preferences, modal, email-enabled, send card ו-confirmation
> ב-1440/390 — ללא overflow, console errors או failed requests — ונבדקו חזותית. תרחיש browser CI
> נוסף לאותם משטחים ולסמנטיקת provider-accepted.
> **מצב אחרי המיזוג:** `IMPLEMENTED / MERGED / NOT_DEPLOYED`. ‏`0168` נמצא ב־main אחרי
> Supplier Portal ‏`0167` ולפני hardening ‏`0169`, אך Production ledger נשאר ב־`0167` ו־Edge
> ‏`email-sender` לא הוכח פעיל. שליחת מייל חיה דורשת `ORDERS_FROM_EMAIL`, דומיין מאומת ו־smoke
> חיצוני (‏DEBT §25). ‏Resend bounce/delivered webhook אינו חלק מ־B1 והוא עתידי לפי #238.
> ‏DEBT §61 נשאר פעיל ליישור שער WhatsApp על `sent` כאשר Twilio יחובר.

עודכן: 20.08.2026 — **פורטל הזמנות לספק (שלב A של סבב היכולות התחרותיות): קישור-טוקן חד-הזמנתי, הצעת ספק מובנית, מסך החלטה פנימי ורוויזיות append-only.** ‏(מוזג ב-PR #86; ראיות rollout חיות מתועדות בעדכון 20.08.2026 (ג) למעלה.)

> **מה נבנה.** ספק מקבל קישור ל**הזמנה אחת**, פותח אותו בטלפון בלי חשבון ובלי התחברות, ומגיש
> תשובה אחת: אישור כמות-שהיא או הצעה מובנית — כמות/מחיר-יחידה פר-שורה, "אינו זמין" + תחליף
> כטקסט, תאריך אספקה חלופי והערה. העסק סוקר במסך ייעודי (`/orders/proposals/:id`) שורה-שורה,
> מקבל/דוחה עם סיבה, ויוצר **רוויזיה** — הזמנה חדשה משורשרת (`revision_number`,
> ‏`revised_from_order_id`) בעוד המקורית מבוטלת ונשארת ראיה. שום דבר שהספק שולח אינו נוגע
> בהזמנה המקורית; ההצעה immutable בטריגר, וההחלטה נרשמת ב-audit עם שחקן, סיבה ולפני/אחרי.
>
> **מודל הטוקן = 0103, לא 0101.** הפרסונה שפרשה לא הוחייתה: אין `auth_role()`, אין policy
> ל-anon, אין session. ‏`issue_supplier_order_link` (בעלים/מנהל-רכש, מנומק) טובע 32 בייט,
> מחזיר את הגולמי **פעם אחת** ושומר sha256 בלבד; מימוש דרך שתי פונקציות `service_role`
> ‏(`service_resolve/submit`) שה-Edge ‏`supplier-portal` (‏verify_jwt=false, ‏POST בלבד — טוקן
> לעולם לא ב-URL; בקישור עצמו הוא ב-**fragment**) קורא אחרי בדיקת צורה. תפוגה/ביטול יושבים
> ב-WHERE; הנפקה חוזרת הורגת את הקודם; ‏snapshot ההזמנה נחתך בהנפקה — הפורטל לא שואל שאילתות.
> השם המוצג לספק הוא הגולמי (חוק #163), והמחירים כן מוצגים — הכרעה #186 מסבירה למה זה שונה
> מהודעת ה-WhatsApp.
>
> **הכשל שה-dry-run תפס לפני שהקוד נגע במסד אמיתי:** ‏increment-then-raise — ‏raise מגלגל
> אחורה גם את מונה הכשלים ואת רישום ה-lookup שנכשל, כלומר בדיוק הראיות שכשל חייב להשאיר.
> לכן כשלי ולידציה **נענים** ‏(`{error: code}`) ולא נזרקים, וה-Edge מתרגם לקוד HTTP; ‏20 כשלי
> הגשה נועלים קישור לשעה, וכל lookup כושל נרשם ב-`private.supplier_portal_lookup_failures`.
> **Recovery ‏20.08:** החלון הרך הוחלף במונה DB מתמיד פר-IP: כתובת gateway עוברת HMAC עם pepper
> שרתי, ‏30 בקשות בדקה ואחריהן חסימה לעשר דקות, משותפת לכל Edge isolates. אין IP קריא במסד או
> בלוג, והדלת מסרבת בלי pepper/כתובת/RPC. הגנת WAF/Turnstile נפחית לפני Edge נשארת הגנת עומק
> תפעולית. מצב ה־CI והפריסה היה פתוח באותו checkpoint; PR #86 וה־rollout החי סגרו אותו, ולכן
> §60 הוסר מרשם החוב הפעיל.
>
> **‏A6 נתפס גם הוא ב-dry-run:** ‏purchase_orders גדלה בשתי עמודות ⇒ רענון hash הסכימה ברשם
> הייצוא; שלוש הטבלאות החדשות נרשמו include, עם `token_hash` **מוחרג** מהייצוא. ‏A5 נפתר
> בלי להזיז את pin ה-89: שלוש הפקודות הפנימיות self-enforcing עם `assert_unit_in_scope`
> ורשומות ב-`scope_definer_enforcements` עם hash מהקטלוג.
>
> **‏entry שלישי.** ‏`portal.html` + ‏`src/portal/` — בלי supabase-js, בלי AuthContext, בלי
> router; מחוץ ל-precache; ‏SW מסרב לנווט `/portal` מהמטמון; אסור ב-`_redirects` (הלקח של
> ‏`/operator`); שני מסווגי ה-CI עודכנו כך שה-entry לא מקבל אפס שערים בשקט. הגבולות מוצמדים
> ב-`portalBoundary.spec.ts`. הפורטל עצמו תומך כעת בעברית/RTL ובאנגלית/LTR לפי `?lang=he|en`,
> שפת דפדפן עם fallback לעברית ומתג נגיש; תאריך, ILS וכמויות עוברים דרך `Intl` של הלוקאל.
>
> **אימות:** ‏build ירוק; ‏`verify` ירוק — ‏1,171 בדיקות ב-120 קבצים כולל ששת השומרים;
> ‏6 בדיקות Deno ל-core של ה-Edge (צעד CI חדש); ‏**dry-run מקומי בטרנזקציה מגולגלת של
> ‏`0167` + סוויטת `p59_supplier_order_portal.sql` המלאה** (טוקן עגול, פג/מבוטל/נעול, ‏replay
> אידמפוטנטי מול conflict, הכרעות עם סיבה, רוויזיה שמשמרת היסטוריה, בידוד דיירים, אפס DML
> לדפדפן, ‏token_hash חסום בעמודות) — עבר ומחק את עצמו; והפורטל צולם **headed** מול ה-dist
> ‏(vite preview + ‏route interception של ה-Edge) ב-390×844 וב-1440×900 בשלושת המצבים, אפס
> גלישה אופקית (`scripts/portal-visual-check.cjs`).
>
> **Recovery מקומי אחרי rebase:** reset מבודד החיל את `0001`–`0167`; ‏P59 עבר עם
> `p59_supplier_order_portal_passed` ומכסה גם ACL/JWT guard, בקשות 30/31, reset חלון,
> fingerprints עצמאיים ו-cron יחיד. ‏P59B מריצה שתי ישיבות `dblink` מקבילות ומוכיחה בדיוק
> proposal אחד ו-`proposal_already_submitted` אחד; reset מיד אחריה מחק schema/fixtures והחזיר
> ledger ל-`0167`. postflight מדד RPC+table קיימים, `service_role_execute=true`,
> ‏`authenticated_execute=false` ו-cron פעיל אחד. בנוסף עבר `npm run verify` מלא מקומית:
> ‏1,244/1,244 בדיקות ב-132 קבצים וכל ששת השומרים; וכן 15/15 בדיקות Vitest ממוקדות של
> הפורטל/גבול/i18n, ‏7/7 Deno ו-TypeScript. בדיקת headed ייעודית הפיקה שמונה צילומים ב-he/en,
> ‏RTL/LTR, ‏390×844 ו-1440×900 למצבי open/submitted/invalid, בלי overflow או שגיאת console;
> ותרחיש ה-browser CI הורחב עם keyboard, labels ו-Chromium accessibility tree. אלה ראיות
> מקומיות בלבד; production build ו-verify עברו, אך CI והתרחיש החי על ה-SHA הסופי עדיין אינם
> מסומנים כעוברים.
>
> **מה במפורש לא רץ:** שער ה-CI המלא (ירוץ על ה-PR); ‏E2E חי מול Edge אמיתי עם DB שבו `0167`
> חלה (ה-dry-run מתגלגל אחורה בכוונה); מסירת הקישור לספק עדיין ידנית — שליחה אוטומטית במייל
> אמיתי (Resend) ו-WhatsApp היא השלב הבא. בחירת locale אוטומטית לפי העדפת ספק תתחבר ב-#87;
> הפורטל עצמו כבר מציע he/en ללא תלות בחשבון ספק.

עודכן: 19.08.2026 (ערב) — **שוחרר לייצור: המיגרציות `0147`–`0150` ו-bundle שנושא את PR #77, #78, #79 ו-#81; ואחריו תיקון `/operator` (#83).**

> **מה היה חי ומה לא.** ‏Pages הגיש את ה-build של `a59055e` (‏PR #74, מסך הכניסה), וראש ה-ledger
> בייצור היה `0146`. ארבע המיגרציות שנכנסו עם #81 מעולם לא הוחלו. רשומה במסמכי החוב טענה ש-#81
> הוא frontend בלבד; היא **הייתה שגויה** ותוקנה ב-PR #82 לפני שנגענו בייצור. הטענה הזו לא הייתה
> תיאורטית: `Products.tsx` ו-`ProductNameReview.tsx` קוראים `products.display_name`, ולכן פריסת
> ה-bundle לבדו הייתה מחזירה שגיאת עמודה חסרה במסכי המוצרים.
>
> **סדר: מסד ואז לקוח, ובתוך המסד לפי מספר.** ‏`0150` מכתיב לשתי תצוגות ולפונקציה אחת לקרוא את
> העמודה ש-`0149` מוסיפה; החלה מחוץ לסדר נופלת על עמודה שאינה קיימת. שלושה חוזי לקוח דרשו את
> המסד קודם: `set_product_display_name` (‏0149), ‏`platform_get_autonomy_policies` (‏0147) ומפתח
> ה-JSON ‏`dueWithin7Amount` (‏0148).
>
> **הגיבוי.** ‏166 טבלאות / 28,992 שורות ללא כשל, ובנוסף ההגדרות החיות של ארבעת האובייקטים
> ש-`0148` ו-`0150` מחליפים **במקום** — `management_dashboard_snapshot`,
> ‏`private.product_purchase_summary`, ‏`inventory_balances` ו-`inventory_movement_feed` — ורשומות
> רשם ה-scope כפי שהיו. גיבוי של ארבעה גופים חיים הוא מה שהופך גלגול לאחור לשחזור ולא לשחזור-מחדש
> מ-Git.
>
> **dry-run של כל הארבע יחד, בטרנזקציה מגולגלת.** כל הקבצים הם DDL ו-`do` בלבד, ולכן חזרה אחת על
> **הרצף כולו** מודדת את העוגנים מול הגופים החיים בסדר האמיתי. שבע בדיקות בתוך הטרנזקציה עברו,
> ואחרי ה-rollback נמדד שאף אחת מהן לא נשארה: אין דלת autonomy, אין כסף חלון-פירעון, אין
> `display_name`, אין `set_product_display_name`, והתצוגות כשהיו.
>
> **החלה forward-only, קובץ אחרי קובץ, כל אחד עם שורת ledger משלו.** ה-Management API מחיל SQL
> **בלי** לרשום אותו; שורת ה-ledger היא שלב נפרד וחובה. ראש ה-ledger התקדם `0146` → `0150`.
>
> **מה שנמדד אחרי:** ‏A5=0 ו-A6=0; דלת ה-autonomy קיימת, פתוחה ל-`authenticated` וחסומה ל-`anon`;
> הדשבורד מחזיר את כסף חלון-הפירעון ואת סכום האיחור, נשאר `SECURITY INVOKER`, **וגדר ה-`payable`
> של `0137` שרדה את ההחלפה**; `products.display_name` קיים עם ה-constraint שלו, בלי grant ישיר
> ל-`authenticated` — כי היעדר ה-grant **הוא** האכיפה — ובלי שורה אחת מלאה, כי `0149` אינו עושה
> backfill; ושלושת מודלי הקריאה נופלים חזרה דרך `display_name` בלי לאבד את `product_name`.
> **postflight: כל הספירות העסקיות זהות** — ארגון 1, מוצרים 271, ספקים 22, חשבוניות 14, הזמנות 238,
> דרישות רכש 63, דרישות תשלום 7, שורות דרישה 9, הקצאות 9, זיכויים 5, מסמכים 43, ו-`audit_logs`
> 21,881 לפני ואחרי.
>
> **מה שבמפורש לא נמדד: אידמפוטנטיות.** הפריסה הקודמת הוכיחה אותה בהחלה חוזרת. כאן זה היה מייצר
> כשל שקרי: `0150` הוא fail-loud בכוונה — אחרי שרץ פעם אחת העוגן `p.name AS product_name` כבר
> אינו קיים, ומעבר שני זורק `0150: product-name anchor moved`. הטענה לא נטענה ולא נבדקה.
>
> **החזית.** נבנתה עם env הייצור (`.env.local` הוחנה לזמן הבנייה). החבילה מדברת רק עם
> `rkftlbctohswhbbiaqin.supabase.co`, אין בה `service_role` ואין `VITE_DEMO_PASSWORD_SEED`. שלושה
> סמני תוכן אומתו בארטיפקט — `set_product_display_name`, ‏`platform_get_autonomy_policies`
> ו-`dueWithin7Amount` — **ואחד נדרש להיעדר**: `coverage-ring-title`, טבעת כיסוי תאריכי הפירעון
> שהסבב הזה מחליף, שנוכחותה הייתה מסגירה צ׳אנק ישן שעובר את כל השאר. פריסה `0666a84c`:
> `index-DXV97O73.js` ו-`client-BFW_wyOt.css` זהים בבייטים בין הבנייה לכתובת החיה, והכתובת
> הייחודית מגישה את אותם entry scripts.
>
> **הפגם שרק פריסה יכולה הייתה לגלות: `/operator` הפנה לעצמו.** קונסולת המפעיל נשלחה עם שני כללי
> `_redirects` שנועדו למנוע מה-catch-all של ה-SPA לבלוע אותה. ‏Cloudflare Pages ממילא מפנה
> `/operator.html` ל-`/operator` (‏308) כדי לשמור כתובת קנונית אחת לעמוד, ולכן rewrite ‏200
> שיעדו הוא אותו קובץ `.html` נוחת על ההפניה וחוזר. הנמדד באתר החי: `/operator` ענה
> `308 -> /operator` בלי מוצא — הקונסולה **לא הייתה נגישה בייצור מהרגע שנשלחה**. התיקון (‏#83)
> מסיר את שני הכללים; החשש שהם נכתבו נגדו אינו קיים, מפני שבקשה שתואמת קובץ ב-`dist` מוגשת
> מ-`dist` — אותה סיבה שבגללה `/assets/*.js` אינו נענה ב-`index.html`. **נמדד על פריסת preview
> של אותו build לפני שנכתב לריפו**, ואז שוב על הכתובת החיה: `/operator` מגיש
> `operator-XeEBvxvF.js` בדסקטופ ובמובייל ואינו קליפת הדייר, `/operator.html` עדיין 308 ל-`/operator`,
> ו-`/`, `/login`, `/suppliers`, `/terms` ו-`/privacy` מגישים את entry הדייר. תת-נתיבים אינם
> דורשים כלל: `src/operator/main.tsx` מרכיב `HashRouter`.
>
> **מה לא רץ בשחרור הזה:** אין Edge function ואין שינוי ב-OCR worker, ולכן לא נפרסו. חמשת הבדיקנים
> הידניים של `DEBT §34` לא רצו — תיק ה-CI הירוק אינו טענה שהם עברו.


עודכן: 19.08.2026 (אחר הצהריים) — **סבב סקירת הבעלים: 16 ליקויים נסגרו בענף `fix/owner-review-16`.** ‏(מוזג ב-PR #81 ונפרס לייצור בערב אותו יום עם `0147`–`0150` — רשומת השחרור למעלה.)

> **מאין זה בא.** הבעלים סקר את האתר החי בצילומי מובייל ורשם 16 ליקויים — שבעה כשלים פונקציונליים,
> שישה של מובייל ושפה חזותית, ושלושה של מבנה המוצר והנתונים. העבודה חולקה לסוכנים לפי **בעלות על
> קבצים**, כך שבתוך גל אין שני סוכנים באותו קובץ; בעיה 14 קיבלה סוכן ייעודי ממודל Fable ב-effort
> מרבי, מפני שהיא היחידה שנוגעת בתצורת build, במסנני CI ובשירות העובד — מקומות שבהם טעות **שקטה**
> ואינה מתגלה עד הפריסה.
>
> **הסתייגות המסמך המקורי נסגרה במדידה, לא בהנחה.** ה-CSS החי מכיל
> ‏`@media(max-width:63.999rem){body{background-image:none}.app-glow{display:none}}` — השינוי של
> ‏PR #76, ההמזגה האחרונה. האתר החי **כן** מריץ את `main`, ולכן הצילומים משקפים התנהגות אמיתית ולא
> גרסה ישנה. **שלוש טענות במסמך המקור לא התאמתו מול הקוד** ותועדו ב-`OPEN-DECISIONS #155-163` כדי
> שלא ייפתחו שוב: ‏RTL ב-Excel נעדר משני בונים (כולל החוברת הנעולה שנמסרת לרו״ח); חישוב החיסכון
> השווה לזול ביותר ולא ליקר ביותר, ולכן החזיר `₪0.00` דווקא כשהבחירה הייתה טובה; ותיבת הסיבה כבר
> לא חסמה מאז 11.08 — מה שנשאר שגוי הוא שהדיאלוג **נפתח** בכל מעבר.
>
> **ארבע עובדות מדידה שעיצבו את הפתרונות, וכל אחת מהן שינתה תוכנית:**
> ‏**(א)** שתי פקודות הסטטוס דוחות סיבה ריקה, ולכן המסלול השקט בונה משפט דרך `reasonOr` במקום לשלוח
> ‏`null` — בלעדיה כל מעבר "שקט" היה נכשל בשרת.
> ‏**(ב)** ‏`0137` תיקן את `management_dashboard_snapshot` **בטלאי מעוגן** והזריק
> ‏`financial_role='payable'`; ‏`create or replace` מ-`0100` היה מבטל את הפילטר בשקט ומרחיב כל מספר
> כסף בדשבורד — באג עם צורת JSON זהה שאף בדיקה לא הייתה תופסת.
> ‏**(ג)** ‏SheetJS CE **משליך מילוי וגופן בכתיבה** (round-trip החזיר `patternType:"none"`), ולכן
> צבעי מותג בחוברת בלתי-אפשריים בלי כותב אחר. שומר הטוקנים מעולם לא היה החסם.
> ‏**(ד)** ‏שלוש הרשומות בתפקידים שפרשו **כבר `active=false`** בייצור, ולכן בעיה 15 התכווצה לתצוגה
> בלבד: אין מיגרציה, אין מיפוי מחדש, ואין הכרעה שורה-שורה.
>
> **בעיה 14 — אפליקציית המפעיל.** ‏`operator.html` כ-entry שני באותו `dist`, לא פרויקט Pages שני
> שמוגדר מחוץ לריפו. נמדד ולא הוערך: ‏`platform_set_autonomy_policy` קיים **רק** ב-`operator-*.js`;
> שני קובצי ה-HTML מצביעים על entry chunks שונים; ‏`operator.html` **אינו** ב-precache manifest
> (‏0 מתוך 94 רשומות). שלוש מלכודות שקטות נסגרו: ‏`navigateFallbackDenylist` **אינו קיים** במצב
> ‏`injectManifest`, ולכן השומר נכתב ב-`public/sw.js` עצמו; ‏`public/_redirects` נדרש מעל ה-catch-all
> של ה-SPA; ושני מסנני ה-CI התאימו רק ל-`index.html`, כלומר entry שני היה מקבל **אפס שערים בשקט**.
> ‏`0147` מוסיף את דלת הקריאה שהפאנל היה חסר — ‏`evaluate_autonomy_policy` פותר `auth_org()`, שהוא
> הארגון של הקורא ולעולם לא זה שמנהלים. **ההפרדה אינה גבול אבטחה** וכתוב כך בקוד: origin אחד =
> session אחד; הגבול נשאר בשרת.
>
> **בעיה 16 — שם מוצר קנוני, ארבעה שלבים.** נמדד מול הייצור: ‏**39 מתוך 271** שמות שמורים בסדר
> ויזואלי הפוך (ייבוא מ-Excel/OCR עברי בלי המרה לסדר לוגי), מרוכזים בשלושה ספקים. לכן הנורמליזטור
> **מסרב ולא מנתח** — פרסר על הקטלוג היה משנה כשורה אחת מכל שבע לתשובה שגויה משכנעת, בכל המסכים
> בבת אחת. הדוגמה שבמסמך הבעלים עצמו (`100 מ״ל` מול `200cc`) היא **סתירה** ומוחזרת ככזו. ‏`0149`
> מוסיף עמודה **בלי backfill**, ‏`0150` מחליף את התצוגה — ולכן השלב הזה הוא **no-op מוחלט** מול
> הנתונים היום ומתחיל להשפיע רק אחרי שאדם אישר. ארבעה סוגי קוד נשארים גולמיים ובדיקה מפילה את
> הבנייה אם מישהו יושיט יד: התאמה (`nameKey` מכריע אילו שורות מתנגשות — תוצאת נתונים בתחפושת של
> תצוגה), טקסט מול הספק, יומן הביקורת, ומסך הסקירה עצמו.
>
> **אימות:** ‏`npm run check` ירוק — build ו-1,144 בדיקות ב-117 קבצים, כל שישה השומרים כולל סיכת
> ה-`SECURITY DEFINER` שארבע המיגרציות החדשות יכלו לשבור.
>
> **מה שבמפורש לא אומת:** ‏**אין אימות ויזואלי.** הסיסמה ב-`פרטי כניסה דמו.txt` נדחית על ידי
> ‏GoTrue (`Invalid login credentials`), והמסלול החלופי — חושפן חשבונות הדמו במסך הכניסה — קיים רק
> תחת `import.meta.env.DEV` ומול Supabase המקומי. לכן חמישה שינויים ויזואליים (8, 9, 10, 11, 13ב)
> נשענים על בדיקות מבניות בלבד, **ובדיקה מבנית אינה צילום**. כמו כן ארבע המיגרציות
> (`0147`, ‏`0148`, ‏`0149`, ‏`0150`) **מעולם לא רצו מול מסד** — ההרצה הראשונה תהיה job ה-SQL ב-CI.
>
> **מה נשאר להכרעת הבעלים:** תיקון 39 השמות ההפוכים (ייבוא מחדש / תיקון ידני / להשאיר), ריענון
> סיסמת חשבונות הבדיקה כדי לאפשר אימות ויזואלי, ומיזוג הענף — הוא מבוסס על ראש PR #78 ולכן תלוי בו.
עודכן: 19.08.2026 (אחר הצהריים) — **סבב ב' של ארכיטקטורת הצבע: התוכנית נכתבה, ואז בוצעה.**
‏2 טוקנים נוספו, ‏3 אוחדו לאחד, ‏1 פונקציה, ‏0 גוונים חדשים. **נפרס לייצור ב-19.08.2026 בערב** (רשומת השחרור למעלה).

> **למה בכלל סבב שני.** הפרומפט המקורי ("מערכת צבעים") היה משימת **תכנון** עם 12 סעיפי פלט
> מוגדרים, ובסבב א' הוא לא נענה: לא נכתב מסמך תוכנית, ובמקומו שונו 47 קבצים. הפרומפט גם תיאר
> אפליקציה אחרת — קנבס כמעט-שחור, מותג סגול, ‏glow סגול, "המצב הכהה הוא הכיוון הראשי" — בעוד
> המוצר הזה הוא נייר בהיר, אוקיאני, ו-`ADR-0002` שולל מצב כהה. הסעיפים שנשענו על ההנחות האלה
> סומנו **N/A מתועד**, לא "עבר". ‏`docs/PLAN-color-system-architecture-20260819.md` הוא המסמך
> שהיה חסר, על 12 סעיפיו, ועליו נשענת העבודה הזו.
>
> **‏(א) סולם ההגבהה היה שבור בקצה העליון.** למוצר חמישה טוקני `--shadow-*` שכולם נגזרים
> מ-`shell`/`action` דרך `color-mix`, בדיוק כדי שרה-תמה של המותג תזיז גם את האטמוספרה. ושלושת
> המשטחים הגבוהים ביותר עקפו אותם: המודאל לבש `shadow-xl`, הטוסט `shadow-lg`, ידית הסריקה
> `shadow-sm` — ברירות מחדל של Tailwind, **שחור ניטרלי, שהיו שורדות רה-תמה בלי לזוז**.
> ‏`check:tokens` לא תפס זאת מפני שהוא סורק צבעים, לא הגבהה. נוספו `--shadow-dialog`
> ו-`--shadow-toast`, הידית קיבלה `--shadow-card`, והשער אוסר מעכשיו `shadow-{sm,md,lg,xl,2xl,inner}`
> בקוד מוצר. **מדרגת משטח שישית נדחתה במפורש:** הכרטיס יושב על 99.2% בהירות, ולכן "מוגבה"
> היה חייב להיות לבן טהור — הפרש 0.8 נקודות, בלתי-נראה על נייר. בממשק בהיר ההגבהה **היא** הצל.
>
> **‏(ב) שני גרפי ההשוואה סתרו זה את זה, והמדידה שחשפה זאת לא הייתה קיימת.** ‏`LineSeries.color`
> היה מחרוזת חופשית שכל אתר קריאה בחר בעצמו. מרכז הבקרה זיווג `chart-1`+`chart-4` — **1.56:1**
> בין שתי הסדרות — ונשא מקף; דשבורד רו"ח זיווג `chart-1`+`chart-3` — **4.23:1** — ו**ויתר** על
> המקף. הצירוף הגרוע ביותר האפשרי: החלש נשא את הנשא הנוסף, החזק ויתר עליו. הסיבה ששרד היא
> שהפנקס מדד תמיד צבע גרף **מול המשטח**, ואף פעם לא מול הצבע שלצידו. ‏`comparisonSeries()`
> מחזיק עכשיו זיווג אחד — `chart-1` + `chart-3` מקווקו — ו-`colorLanguage.spec.ts` מצמיד את
> הזיווג, את המקף ואת **רף 3:1 בין סדרה לסדרה**, כולל המרת oklch→sRGB שאומתה מול שלוש
> ההקסות שהמסמך כבר תיעד. הכרעת בעלים: הסקיצה הקודמת ("שני הכהים, נבדלים במקף") הוחלפה.
>
> **‏(ג) `--color-section-*` היו שלושה שמות לערך אחד** — האנטי-דפוס שהפרומפט אוסר במפורש.
> אוחדו ל-`--color-section-accent`. אפס שינוי חזותי; השם חדל להבטיח הבחנה לפי תחום שאין.
>
> **‏(ד) טוקן טקסט שימש כקו בגרף.** ‏`chartTheme().flat` קרא `--color-ink-faint`, שמכוונן
> לקריאוּת של placeholder. הוא קורא עכשיו `--color-chart-3`: שתי סיבות שינוי מנוגדות, שני טוקנים.
>
> **אימות — headed, מול הבנייה עצמה.** ‏build ‏12.86 שניות; ‏`verify` ירוק עם **987 בדיקות**
> ‏(+7). ‏`vite preview` על ה-dist בפורט מבודד, ‏chromium **headed**, ‏1440×900 ו-390×844,
> ‏`prefers-reduced-motion` כדי לעקוף את ה-IntersectionObserver של `ChartViewport` (בלעדיו
> הגרפים כלל אינם ב-DOM, וסבב מדידה ראשון החזיר רשימה ריקה בטעות).
> נמדד **בדפדפן**: `chart-1` ‏#003f47 · `chart-3` ‏#85a1a3 · `chart-4` ‏#0a171d — זהים
> לחישוב הסטטי; הזיווג החדש **4.23**, הישן **1.56**. קו הרכש מרונדר `stroke` של `chart-1`
> **רציף**, קו התשלומים `chart-3` עם `stroke-dasharray "7 5"`. שלושת טוקני `section-*` הישנים
> **נעדרים** מה-CSS המחושב, ו-`--color-section-accent` נוכח. שלושת טוקני הצל נמדדו על אלמנט
> חי ונגזרים כולם מ-`shell` (‏oklab 0.1957): card ‏0.07/0.13 · toast ‏0.12/0.24 ·
> dialog ‏0.10/0.22. אפס גלישה אופקית בשני הרוחבים; אפס שגיאות console מהמוצר.
>
> **מה שלא אומת, במפורש:** ‏`PriceSparkline` לא רונדר באף מסך שנבדק (דורש ≥2 שינויי מחיר),
> ולכן מעבר `flat` ל-`chart-3` נצפה רק ברמת הטוקן ולא ברמת הפיקסל. ‏14 שגיאות WebSocket
> ‏503 הן realtime של סטאק Supabase המקומי, לא המוצר. שער ה-CI המלא והפריסה — טרם רצו.
>
> **השער נדלק אדום פעמיים, ולא בגלל השינוי.** ‏`verify` נפל על `quickCreateProduct.spec.tsx`
> ‏(‏`DEBT §52`) ו-`browser` על `waitForDraftSave` בתרחיש פיצול ההזמנה — שני מסלולים שה-PR הזה
> אינו נוגע בהם. הרצה חוזרת על **אותו SHA** העבירה את שניהם. שני הכשלים תועדו לפני ההרצה החוזרת:
> ‏§52 הורחב (זה כבר תרחיש שני באותו קובץ, ולכן המקור הוא דפוס ההמתנה ולא מחרוזת אחת). §54
> נפתח באותו checkpoint, אך PR #91 סגר את race ה־focus; הוא אינו חוב פעיל עוד.
>
> **חוב שנפתח:** ‏`DEBT §53` — כיוון מחיר ב-`PriceSparkline` נשאר אדום-ירוק בלי נשא צורני.
> ‏`GroupedBarChart` מתעלם מ-`dash` (לעמודה מלאה אין stroke) ונשען על מקרא הנקודות — מתועד,
> לא חוב. תחזית (`forecast`/`projected`) **לא נבנתה**: אין במוצר מסך תזרים חזוי, ופלטה
> שגדלה לפני שיש לה צרכן היא בדיוק מה שחוק הטוקנים אוסר.


עודכן: 19.08.2026 (צהריים) — **סגירת ארכיטקטורת הצבע.** ‏11 טוקנים נגעו: 3 חדשים, 2 ערכים
מתוקנים, 3 נמחקו. **נפרס לייצור ב-19.08.2026 בערב** (רשומת השחרור למעלה).

> **מה נמצא ב-audit.** ‏0 hex, ‏0 `rgba()`, ‏0 מחלקת פלטה גולמית ו-0 inline color בכל 140 קובצי
> ה-tsx — המערכת כבר הייתה ממושמעת. מה שכן דלף היה **מתחת** לשכבת הטוקנים, בתוך `src/index.css`
> עצמו: חמישה טוקני צל העתיקו ידנית `rgb(10 23 29)`/`rgb(0 63 71)` במקום להיגזר מ-`--color-shell`
> ומ-`--color-action`; כתם ה-glow איית מחדש את הליטרל האוקיאני; גרדיאנט ה-body החזיק שלושה
> ‏oklch שאינם קשורים ל-`--color-canvas`; ו-`text-white` הופיע 23 פעם. ‏**רה-תמה של המותג לא
> הייתה צובעת אף אחד מהם** — ה-Runbook הבטיח "שלושה קבצים" והקוד לא קיים את ההבטחה.
>
> **שלושה טוקנים חדשים.** ‏`--color-surface-hover` ו-`--color-surface-selected`: משטח ה-hover
> היה `action-wash` במדידה של **1.074:1** מול הכרטיס, מוגש ב-40% מזה ב-`.row-hover` מול 100%
> ב-40 אתרי inline — שתי עוצמות לאותה כוונה, שתיהן כמעט בלתי-נראות על נייר לבן. „נבחר" לא היה
> טוקן כלל: ‏`GlobalSearch` סימן את האפשרות הפעילה **באותה מחלקה כמו hover**, כך שמה שה-Enter
> יפתח ומה שהעכבר מעליו היו אותו פיקסל. ‏`--color-on-solid` נותן שם לתפקיד שהמילה `white` הסתירה.
>
> **שני כשלי WCAG נסגרו במדידה.** ‏`line-strong` (גבול כל שדה קלט) נמדד **2.96:1 מול הקנבס**
> ו-2.83 מול רצועת sunken — מתחת ל-3:1 של 1.4.11; המסמך ציטט 3.17 שהיה נכון רק מול הכרטיס.
> ‏63% מודד עכשיו 3.42 / 3.20 / 3.06. כוכב הדירוג הריק היה `ink-ghost` ב-2.14 מול הכרטיס, והצעד
> מלא↔ריק היה **1.27** — תחת deuteranopia חמש צורות זהות; המתאר הוא עכשיו `line-strong`,
> וההבחנה עברה מגוון ל**מילוי**.
>
> **מילון אחד לשתי משמעויות.** ‏`trend-up-fg` ו-`alert-fg` הם אותו `rose-700`, ולכן שלושה מסכים
> בחרו ביניהם באקראי בלי שאיש יראה — `Suppliers.tsx` השתמש ב-`alert-solid` בדיוק במקום
> ש-`PriceLists.tsx` השתמש ב-`trend-up-fg`, וההערה שם אף טענה "אותו טיפול". הוא הדין ב-18 אתרי
> ‏`text-alert-solid` כטקסט הרסני, בניגוד להגדרת הטוקן עצמו. הכל יושר; אפס שינוי ויזואלי, מפני
> שהערכים ממילא זהים — ובדיוק לכן רק spec יכול להחזיק את ההבחנה: `src/lib/colorLanguage.spec.ts`.
>
> **הרמפה מספקת שלושה צעדים, לא חמישה.** נמדד: `chart-1`↔`chart-4` = 1.56, ‏`chart-3`↔`chart-5`
> ‏= 1.65. סדר הדונאט היה `[0,1,3,2]` מזמן הרמפה המעורבת של T7.1 והשאיר את שני השכנים האחרונים
> ב-1.65:1. הסדר החדש `[0,2,3,1]` מביא כל צמד שכנים ל-3.11 ומעלה — **בלי לגעת בגוון אחד**.
>
> **מצב כהה: אין, ולא נבנה.** ‏`docs/adr/0002-no-dark-theme.md` (מאומץ) אומת מחדש — אפס `dark:`,
> ‏`prefers-color-scheme`, ‏`data-theme` ו-`.dark`, גם ב-bundle הבנוי. אפס טוקנים הושקעו בו.
>
> **האכיפה הורחבה.** ‏`check:tokens` סורק עכשיו 275 קבצי `.ts`+`.tsx` (היה `.tsx` בלבד, ולכן
> ‏`src/lib/orderImage.ts` — שבונה מסמך HTML מעוצב שלם — לא היה מכוסה), ואוסר `rgb()`/`hsl()`
> בכל `src/index.css` וכל ליטרל צבע **מחוץ** לבלוק ה-`@theme`. שלוש הדליפות שרדו בדיוק מפני
> שה-regex הישן תפס `#RRGGBB` בלבד.
>
> **אימות חי** (chromium headed מול dev server, ‏1440×900 ו-390×844, ללא סודות): הטוקנים נמדדו
> **בדפדפן** דרך canvas 2D. ‏`line-strong` 3.21 / 3.43 / 3.07 · לבן על כפתור ראשי 11.65 ·
> ‏badge-done 6.70 · ‏`ink-faint` על hover 4.52 · ‏`ink-muted` על selected 5.33 · שלוש מדרגות
> המשטח נבדלות (1.11 ו-1.12). ‏`.app-glow` ‏`display:block` בדסקטופ ו-`none` במובייל, גרדיאנט
> ה-body קיים בדסקטופ ו-`none` במובייל — הטיפול השטוח בטלפון שרד. אפס שגיאות console.
> ‏`npm run verify` ירוק: ‏Knip, חמשת ה-guards ו-976 בדיקות.
>
> **מה שנשאר בכוונה:** ‏`bg-white` בארבעה מקומות — לוח לבן אטום מתחת ללוגו שהדייר מעלה, שעשוי
> להיות PNG שקוף. מתועד ב-`DESIGN.md` כליטרל מכוון, לא כדליפה.
>
> **חוב שנפתח:** ‏`--color-section-*` הם עדיין שלושה טוקנים באותו ערך — כפילות מכוונת ומתועדת
> (נקודת חזרה אם הבחנת-גוון תשוב), ומוצמדת ב-spec. לא אוחדה.

עודכן: 19.08.2026 — **הריצה הראשונה של ארגון ריק.** ארבעה מסכים תוקנו לקראת כניסת דייר ניסיוני ראשון. **נפרס לייצור ב-19.08.2026 בערב** (רשומת השחרור למעלה).

> **למה עכשיו.** בייצור יש היום ארגון אחד — דייר הדמו. לקוח ניסיוני מקבל ארגון חדש **וריק**,
> ובו נמדדו ארבעה מקומות שבהם המוצר נראה שבור או טוען טענה שלא נמדדה. כל התיקונים frontend
> בלבד: אין מיגרציה, אין Edge, אין תלות חדשה, אין טוקן עיצוב חדש.
>
> **‏(א) הכרטיס הגדול במסך הראשון רונדר כלום.** ‏`AttentionZone` הציג את משפט ה-all-clear רק
> כאשר `noticeRows` **וגם** `unknownRows` ריקים. בארגון ריק `management_dashboard_snapshot`
> מחזיר `overdue`/`dueToday` כ-`null` בכוונה (`0100:126-134`), ולכן `unknownRows.length === 2`
> והתנאי נפל ל-`null`: כותרת, פעמון אפור, ומתחתיה שורה מקופלת אחת. עכשיו שלושה ענפים ממצים,
> והאמצעי הוא **ניטרלי ולא ירוק** — כשחלק מהתמונה לא נמדד, „הכל נקי" היא טענה שאיננו רשאים
> לטעון. תופעת לוואי רצויה: גם ארגון שיש בו רק שורות `info`/`idle` נסגר.
>
> **‏(ב) לאשף ההקמה לא הייתה דלת ממסך הבית.** ‏`Onboarding.tsx` קיים ומלא, אבל `homeFor()`
> מחזיר תמיד `/dashboard` ושלוש הדלתות היחידות ישבו באזור „החשבון והמערכת" — איפה שמחפשים
> הגדרות, לא איפה שמחפשים „איך מתחילים". נוסף `Note` לבעלים בלבד (`/onboarding` הוא
> `Guard roles={['owner']}`), **מחוץ** ל-`.dash-enter` כדי לא לגעת בסדר הכותרות שהשער מודד.
> האות הוא ספירת HEAD של ספקים באותו `Promise.all` — **אפס ספקים** ולא „כל ה-KPI אפס", שהיה
> מסמן גם עסק אמיתי בחודש שקט. **נדחה במפורש:** להפוך את `homeFor()` להפניה — היא סינכרונית,
> וההערה ב-`Layout.tsx:140-145` כבר מתעדת שהאשף יושב ליד `/settings` ולא בנתיב העבודה.
>
> **‏(ג) ‏`/reports` הצהיר ‏₪0.00 שאיש לא מדד.** שלושה מתוך תשעת האריחים הם `reduce` מעל קבוצה
> ריקה והם `null` מעכשיו (`fmtMoneyExact` כבר עונה `—`); שישה הם ספירות מעל קבוצה ידועה
> ונשארים `0` — קבוצה של אפס חשבוניות באמת מכילה אפס חשבוניות שלא שולמו. הכלל אינו חדש: הדשבורד
> מחיל אותו על אותן עובדות כסף. **ובנוסף, ולא היה בדיווח:** לטבלת החשבוניות **להדפסה** לא הייתה
> שורת-ריק כלל, ולכן חודש ראשון הדפיס כותרת, גוף ריק ו-`tfoot` ובו „סה״כ ‏₪0.00 ‏₪0.00 ‏₪0.00" —
> והדף הזה נוחת אצל רואה החשבון של הלקוח. החישוב חולץ ל-`monthlyReportScreenTotals`; חוברות
> ה-Excel **לא** השתנו — תא מספרי הוא מה שנוסחה קוראת, וכלל ה-`—` שולט במסכים.
>
> **‏(ד) ‏`/inventory` היה המסך האחרון עם „אין נתונים להצגה" הגנרי.** הניסוח נבחר לפי הסכימה ולא
> לפי הניחוש: `inventory_balances` היא שורה **לכל מוצר פעיל** (`0026:474-501`), ולכן טבלה ריקה
> פירושה קטלוג ריק — „עדיין לא נספר מלאי" היה טענה שקרית.
>
> **אימות — על ארגון ריק אמיתי, לא על fixtures בלבד.** ‏build ו-verify ירוקים (**980 בדיקות**,
> ‏+8). הוקם ארגון ריק על הסטאק המקומי (4 יחידות מהטריגר, אפס ספקים/מוצרים/חשבוניות) וצולם
> ב-Playwright **headed** ב-1440×900 וב-390×844: הכרטיס אומר את המשפט הניטרלי ולא את הירוק,
> הקישור לאשף מצביע ל-`/onboarding`, שלושת אריחי הכסף ב-`/reports` מציגים `—` ליד ספירות `0`,
> טבלת החשבוניות אומרת „אין חשבוניות בחודש זה" בלי שורת סיכום, ‏`/inventory` מציג את המצב-ריק
> החדש עם הדלת ל-`/products`. אפס גלישה אופקית בשני הרוחבים. **טרם רצו:** שער ה-CI המלא
> והפריסה לייצור.

עודכן: 19.08.2026 (לילה) — **שוחרר לייצור: מיגרציה `0146` ו-bundle שנושא את PR #72, #73 ו-#74.**

> **מה שוחרר, ומה כבר היה חי.** ‏Cloudflare Pages הגיש עד עכשיו את `b091671` (‏PR #71, סבב T7.3);
> שלוש רשומות במסמך הזה טענו ש-T7.3k "טרם נפרס", ו**הטענה הייתה שגויה** — הסבב הזה כן היה חי,
> ומה שלא היה חי הוא PR #72 (סבב הבעלים), ‏#73 (תיעוד) ו-#74 (מסך הכניסה). המסד היה מיגרציה אחת
> מאחור: ראש ה-ledger `0145`, בעוד `0146` נכנס עם #72 (‏`DEBT §51`, נסגר כאן).
>
> **סדר: מסד ואז לקוח.** ‏`src/lib/checks.ts` קורא את `over_allocated_invoice_count` מתוך
> ‏`payment_request_financial_check_signals`. ‏bundle שמבקש שדה שהפונקציה עדיין אינה מחזירה הוא
> רגרסיה נמדדת; הסדר ההפוך הוא רק תכונה שטרם נראית.
>
> **המיגרציה.** גיבוי 166 טבלאות / 27,676 שורות ללא כשל, ובנוסף שני מקורות הפונקציות שהוחלפו
> ורשומות רשם ה-scope כפי שהיו לפני השינוי. ‏dry-run בטרנזקציה מגולגלת: שתי ההחלפות נמדדו בתוך
> הטרנזקציה, ואחרי ה-rollback נמדד ששתיהן נעלמו וש-pin ה-A5 נשאר כשהיה. אחר כך החלה forward-only,
> שורת ledger, ו-probe: ראש `0146`; פרדיקט המחיקה מסנן `financial_role`; טיוטה כבר אינה חוסמת
> מחיקת ספק; אות ההקצאה קיים; ‏pin ה-A5 חושב מחדש לגוף החדש; וחריג ה-A5 של `soft_delete_supplier`
> נשאר יחיד. **postflight: כל הספירות העסקיות זהות** — ארגון 1, מוצרים 271, ספקים 22, חשבוניות 14,
> הזמנות 229, דרישות רכש 58, דרישות תשלום 7, שורות דרישה 9, הקצאות 9, זיכויים 5, מסמכים 43.
> החלה חוזרת מדדה את טענת האידמפוטנטיות בלי להזיז שורה עסקית אחת, ואפס הפרות A5/A6 לאורך כל הדרך.
>
> **החזית.** נבנתה מ-worktree מבודד על `origin/main` ‏`a59055e`, כדי לא לגעת בעץ העבודה המשותף.
> החבילה מדברת רק עם `rkftlbctohswhbbiaqin.supabase.co`, אין בה `service_role` ואין
> ‏`VITE_DEMO_PASSWORD_SEED`. שלושה סמני תוכן אומתו בארטיפקט — `over_allocated_invoice_count`
> (‏#72), הנתיב `/supplier-log` (‏#72) ו-GLSL של האורורה (‏#74) — **ואחד נדרש להיעדר**: ה-markup
> של פאנל הכניסה הקודם, שנוכחותו הייתה מסגירה צ׳אנק ישן שעובר את כל השאר. פריסה `c6ab692a`:
> הכתובת הקנונית והייחודית מגישות את אותם entry scripts, ‏`index-LfDt44xx.js` זהה בבייטים לבנייה
> המקומית (‏sha256 `2AEA5E49…`) וכך גם ה-stylesheet (‏`78E50BA9…`), ו-`/`+`/login` החזירו 200
> בדסקטופ ובמובייל.
>
> **אימות ויזואלי headed מול האתר החי** (‏headless מפספס CSS מוזרק במכונה הזו), ‏1440×900 ו-390×844:
> קנבס האורורה קיים, אינו מוסתר, ו-WebGL2 חי עם drawing buffer לא-אפסי (‏410×409 בדסקטופ,
> ‏358×176 במובייל); שני צילומים במרווח 2.5 שניות **שונים** — הלופ נע באתר החי ולא קפא על פריים;
> אפס גלישה אופקית בשני הרוחבים; אפס שגיאות console, ‏page ו-HTTP. ‏smoke מחובר וקריאה-בלבד
> לזהות `owner` נחת ב-`/`, ו-`/supplier-log` — הנתיב שלא היה קיים בפריסה הקודמת — רינדר 317
> רשומות בשדות מפורמטים.
>
> **מה במפורש לא נפרס:** בין `b091671` ל-`a59055e` לא השתנה דבר תחת `supabase/functions` או
> ‏`worker/`, ולכן לא נפרסה אף Edge Function ולא הוחלף image של ה-OCR worker.
>
> **מה נשאר לא מאומת:** שער האיכות המלא (‏`npm run quality`) רץ ב-CI על ה-PR לפי הנתיבים שהשתנו;
> ‏`check-p0-security.ps1`, ‏`check-p0-upgrade.ps1` ושני ה-Edge smoke נשארים מחוץ ל-CI (‏`DEBT §34`)
> ולא רצו כאן.

עודכן: 19.08.2026 (מאוחר) — מסך כניסה חדש: כרטיס דו-פאנלי עם אורורה בשיידר WebGL. **נפרס לייצור בפריסה `c6ab692a`** (רשומת השחרור למעלה).

> **מסך הכניסה נבנה מחדש לפי רפרנס שמסר הבעלים (Pin 852728510733378866, לופ 5.1 שניות).**
> הרפרנס פוענח דרך סקיל `watch` וחילוץ פריימים ב-ffmpeg. המבנה: כרטיס לבן אחד על קנבס אפור,
> ובתוכו פאנל צבע מונפש (~40%) ופאנל טופס (~60%) — במקום שני אובייקטים צפים בשדה פטרול מלא-מסך.
>
> **‏(א) הלופ הוא שיידר, לא וידאו.** ‏`src/lib/loginAurora.ts` — ‏fBm ‏domain-warped בשתי
> העברות, ‏WebGL2 גולמי, ‏**אפס תלויות npm חדשות**, ‏~4KB. הפלטה נקראת בזמן ריצה מ-
> ‏`--color-chart-1..5` (‏oklch→sRGB דרך canvas 2D של הדפדפן) ומעורבבת ב-linear light, ולכן
> אין ולו ערך RGB אחד בקוד ו-`src/index.css` נשאר מקור האמת היחיד לצבע. ‏higgsfield לא נדרש.
>
> **‏(ב) שלושה באגים נמצאו במדידה, לא בקריאה.** ‏1. ‏`WEBGL_lose_context` בניקוי הרג את
> הקנבס לצמיתות — ‏StrictMode של React 19 מפרק ומרכיב מול **אותו** צומת DOM, והרכבה שנייה
> קיבלה קונטקסט מת (`contextLost === true`, ‏drawingBuffer ‏0×0) שצבע את הפאנל לבן. הוסר.
> ‏2. ‏`alpha: false` אומר שקנבס שלא נצבע מורכב כשחור אטום ולא כשקוף — כל מסלול כשל מסתיר
> אותו עכשיו (`canvas.hidden`) כדי שהגרדיאנט מתחתיו ייראה. ‏3. הכפתור של גילוי הסיסמה ישב
> ב-`end` של הקשר RTL בעוד ה-`pe-12` של ה-input נפתר לפי `dir="ltr"` שלו — ריפוד בצד אחד,
> כפתור בצד השני. ה-wrapper קיבל `dir="ltr"` ושניהם נפתרים יחד.
>
> **‏(ג) הניגודיות היא מדידה חוזרת, לא הערכה.** סורק שמסתיר את הטקסט, מצלם את הפאנל שמונה
> פעמים על פני 12.6 שניות ולוקח את הפיקסל הבהיר ביותר בתוך התיבה של כל אלמנט. ‏`chart-3`
> ‏(‏#85A1A3) נותן **2.75:1** מתחת ללבן ולכן אינו יכול להיות בגוף הרמפה; הוא נכנס כהבהקה
> ממוסכת בלבד. הגרסה הראשונה מדדה 11.7:1 — כלומר נקראה כשחור ליד הרפרנס — והרמפה הובהרה
> פעמיים עד **6.02:1** במקרה הגרוע. ‏`shell-ink-soft`/`-dim` הוצאו מהפאנל (3.62:1).
>
> **‏(ד) מה שלא נבנה מהרפרנס:** שדה "שם", ‏checkbox תנאים, "הרשמה" וכפתורי Google/Apple —
> המערכת סגורה (`OPEN-DECISIONS #12`) ואין provider ‏OAuth מוגדר. הכרעת בעלים: להשמיט.
>
> **‏(ה) תווית סדרה שנרכבה על חברתה בגרף "רכש מול תשלומים".** תוויות השם בקצה הקו ב-
> ‏`ComparisonLineChart` הוסטו ב-`- index * 14` קבוע. ההיסט הזה עוזר רק כששני הקווים מסתיימים
> ב**אותו** ערך; בהפרש של ~14px הוא מבטל בדיוק את ההפרדה האמיתית. נמדד ב-`/dashboard` ב-390px:
> ‏"רכש" ו"תשלומים" במרווח 3px ותיבות חופפות. ההיסט הוחלף בפתרון התנגשות אמיתי — recharts
> מרנדר את ה-LabelList לפי סדר הסדרות, ולכן סדרה יודעת איפה קודמותיה כבר יושבות ומסיטה רק
> כשצריך, **כלפי מעלה** (מתחת לקו האחרון יושבת שורת התאריכים). אחרי: מרווח 18px, אפס חפיפה,
> ב-390px וב-1440px.
>
> **‏(ו) רקע שטוח במובייל (הכרעת בעלים).** מתחת ל-64rem ‏`.app-glow` ו-gradient ה-body כבויים
> והרקע הוא טוקן ה-canvas בלבד. הכתם קיים כדי לעקוב אחרי סמן, ובמכשיר מגע אין סמן — הוא
> מתנוון לכתם סטטי ב-72%/12%. אומת: ב-390 וב-900 ‏`background-image: none` ו-`.app-glow`
> ב-`display:none`; ב-1440 שניהם נשארים.
>
> **אימות:** ‏build ו-verify ירוקים (972 בדיקות). ‏Playwright **headed** ב-1440×900 וב-390×844:
> שני צילומים במרווח 2.5 שניות **שונים** (הלופ נע), ‏`prefers-reduced-motion` מקפיא (זהים),
> ‏`--disable-webgl` מציג את הגרדיאנט הסטטי ולא שחור, ‏אפס שגיאות קונסולה, ‏אפס גלישה
> אופקית. **טרם רץ:** שער האיכות המלא ב-CI.

עודכן: 19.08.2026 — **מנוע ה-OCR עבר ל-`mistral-ocr-latest`. קנרי חי על worker אחד מתוך שניים.**

> **החלפת מנוע ה-OCR — נפרסה כקנרי 19.08.2026, PR #70 (`a17a156`).** ‏`mistral-ocr-latest` הוא
> ה-`OcrAdapter` הראשון של ה-worker; ‏`gpt-5.6-terra` נשאר שני. **הפירוש לא נגע** —
> ‏`interpret-document` עדיין רץ terra, והבנצ'מרק מעולם לא מדד אותו.
>
> **על סמך מה.** בנצ'מרק `ocr-ab/20260818` על 16 כרטיסי אמת מאושרי-בעלים: ‏`line_recall` ‏0.988
> מול 0.616 · שיוך שורה 0.972 מול 0.424 · ‏`numeric_recall` ‏0.9936 מול 0.9718 · שגיאת שם 0.017
> מול 0.274 · קריאה אחת לעמוד מול 2.62 · 2.2 שנ' חציון מסמך מול 18.4. הבעלים הכריע ידנית את כל
> **111** המחלוקות של המנקד: כולן שגיאות מנוע, **אפס באגים בכרטיסי האמת**, ולכן המספרים עומדים בלי
> חישוב מחדש. מתוך שש החשודות בהמצאה, שלוש היו שורה אמיתית עם מספר אחד שגוי; שלוש הנותרות היו של
> terra. הכרעות שורה-שורה: `NIR-APP-DOCS/ocr-ab/20260818/triage-outcome.md`.
>
> **מה שבמכוון לא הועתק: שכבת הדגימה החוזרת.** היא בדיקה עבור terra כי פלט של מודל ראייה משתנה בין
> מעברים; ‏mistral מחייבת קריאה אחת לעמוד ומדרגת ביטחון בעצמה, ודגימה חוזרת שלה הייתה מייצרת מראית-עין
> של אישוש. האם היא הייתה מוסיפה משהו — **לא נמדד**, וכתוב כך ב-`worker/ocr/src/ocr_mistral.py`.
>
> **הראיה שזה עובד, לא שזה רץ.** מסמך אמיתי הועלה לאתר החי ונקרא על ידי
> ‏`supplyflow-a17a156-1-mistral` ב-**4,882 מ"ש**; ‏`document_extractions` רשם
> ‏`supplyflow-native+mistral` · `mistral-ocr-latest` · `v1/ocr`; מהאישור ועד "מוכן לבדיקה" — 27
> שניות, ספק זוהה עם ח.פ תואם. ‏worker 2 לקח באותו זמן את עבודת הסריקה, כך שהמזהה מפריד בין שתי
> האוכלוסיות — וזו כל מטרת הסיומת `-adapter` במזהה ה-worker.
>
> **חזרה לאחור** = `./scripts/run-ocr-worker.sh` בלי דגלים. שינוי env והפעלה מחדש, בלי deploy.
>
> **הממצא שנולד מהבדיקה, והוא חשוב יותר מהחלפת הספק עצמה.** נבדקה האפשרות להוסיף קורא שני
> ‏(terra מאמת את mistral). שתי מדידות פסלו את התכנון והחליפו אותו:
> ‏**(א)** סף ביטחון אינו עובד — הציון על השורה השגויה היחידה שנמצאה בכל הקורפוס (‏7.50 במקום 1.50)
> היה **0.943**, מתחת לרבעון התחתון בלבד; סף שהיה תופס אותה היה שולח לקריאה חוזרת רבע מהקורפוס.
> ‏**(ב)** כלל חשבוני בתוך ה-worker אינו עובד — על 443 שורות טבלה הוא סימן 42 (9.5%) שורות תקינות,
> ואת השורה השבורה **לא** סימן: מספר שורה, מק"ט וברקוד תמיד מספקים שלישייה מזויפת (`12 × 2 = 24`).
> **המסקנה:** הבדיקה שכן תופסת את מחלקת השגיאה הזו כבר קיימת בשרת (`0108`, `0099`, סבילות 0.05,
> חומרה `error`) — אבל מדידה על הייצור הראתה שהיא **כמעט לא רצה**: מתוך 2,911 שורות שחולצו תחת terra,
> ‏2,908 נשאו מחיר יחידה ורק **331 נשאו כמות**, ולכן רק **8.8%** מהשורות היו ניתנות לבדיקה בכלל
> (‏18.3% מהן נכשלו). רשת הביטחון לא הייתה חלשה — היא הייתה סגורה, כי הקריאה לא ייצרה את האופרנדים.
> ‏mistral משייכת את שלושת המספרים ב-97% מהשורות מול 42%, ולכן **החלפת המנוע היא עצמה התיקון של רשת
> הביטחון**. ‏`scripts/ocr-line-arithmetic-coverage.sql` שומר את המדידה כדי להריץ אותה שוב.
>
> **הצעד הבא, לפי מדידה ולא לפי תחושה:** להריץ את השאילתה בעוד כמה ימי קנרי. אם `checkable_pct`
> מזנק ו-`broken_pct` נשאר יציב — ההגנה הושגה בלי ספק שני. אם שורות ימשיכו להישבר על מכנה גדול
> בהרבה — זו ההצדקה המדודה לקורא שני, ואז יהיה ידוע בדיוק על אילו שורות.
>
> **פתוח:** שני כרטיסים (`8aef0947b0d2`, `99bf3b1eeb70`) שסריקת ההמצאות דילגה עליהם בגלל
> ‏`sampled_pages`, ולכן "אפס המצאות ל-mistral" נכון ל-14 מתוך 16 מסמכים; ו-`arm-openai.json` לא רשם
> ‏`observed_models`, כך ששם הדגם שזרוע ה-openai קיבלה בפועל אינו מתועד.

עודכן: 19.08.2026 — ארבעה דיווחי בעלים: פרדיקט מחיקת ספק, חסם דרישת תשלום, מוצר ידני בהזמנה, יומן עדכון ספקים. **נפרס לייצור בפריסה `c6ab692a`, עם מיגרציה `0146`** (רשומת השחרור למעלה).

> **סבב תיקוני בעלים 19.08.2026 — ארבעה נושאים שנמסרו לפני T7.3k, כל אחד אומת מול הקוד הנוכחי
> לפני שנגענו בו. שלושה היו אמיתיים; הרביעי היה אמיתי במקום אחר ממה שתואר. אין שינוי בשפה
> הוויזואלית של T7.3k — כל UI חדש נבנה מהטוקנים והמחלקות הקיימים.**
>
> **‏(א) "לא ניתן למחוק ספק עם יתרה פתוחה" כשאין יתרה — שני פערים, לא אחד.** ‏`soft_delete_supplier`
> נכתב ב-`0036` לפני שהעמודה `financial_role` קיימת ומעולם לא עודכן: הוא סכם חשבוניות בכל
> ה-`financial_role`, בעוד שכל קורא-כסף אחר מסנן `payable` מאז `0137:2449`. איחוד חשבוניות הופך
> חשבונית ביניים ל-`supporting_evidence` **ומשאיר את `total_amount`** — ולכן המסך הראה ₪0 והשרת
> סירב. ‏`0146` מיישר את הפרדיקט. **מה שבמכוון לא יושר: `unit_id`.** קוראי היתרה מסננים לסקופ
> הקורא כי הם עונים "מה אני חייב"; מחיקה היא ארגונית — היא מסתירה את הספק מכל הישויות — ולכן
> הפרדיקט שלה נשאר ארגוני, אחרת משתמש מסוקף היה מסתיר ספק שישות אחות עדיין חייבת לו.
> **ובנוסף:** שתי החסימות דיברו במשפט אחד, ולכן **טיוטת הזמנה שנשכחה דיווחה על "יתרה פתוחה"**.
> ההודעות פוצלו, ו-`draft` הצטרף לסטטוסים הטרמינליים (הכרעת בעלים).
>
> **‏(ב) דרישת תשלום שזיכוי הרג.** החסימה של אישור רגיל כשיש זיכוי פתוח היא **התנהגות מתוכננת**
> (`0073:693-710`) והבעלים הכריע להשאיר אותה. מה שנשבר הוא אחר: ‏`amount_allocated` נקבע ביצירת
> הדרישה ואינו מחושב מחדש, ולכן זיכוי שקוזז **אחר כך** מוריד את יתרת החשבונית מתחתיו — ומאותו רגע
> השרת דוחה גם באישור (`payment_request_checks_failed`) וגם בביצוע (`allocation_exceeds_balance`),
> ואין מסך שיכול לערוך הקצאה. המשתמש ראה סירוב בלי סיבה ובלי צעד הבא. ‏`0146` מוסיף
> ‏`over_allocated_invoice_count` — **ספירה בלבד**, באותה צורת anti-oracle של `0034`, כך ש-office
> לומד שההקצאה אינה נכנסת בלי ללמוד שום יתרה — והמסך חוסם את **שני** מסלולי האישור עם ההסבר
> ועם הצעד: לבטל ולפתוח דרישה מעודכנת. ‏`Credits.tsx` הוצר ל-owner/office כך שיתאים לשרת
> (‏`transition_credit_request` דוחה `accountant`, ‏`0024:286-288`) — רואה-חשבון ראה כפתורים
> שתמיד נכשלו. **הקיזוז עצמו נשאר חוב מוצהר: `DEBT §50`.**
>
> **‏(ג) מוצר ידני בהזמנה חדשה — מבוי סתום מלא, נפתח.** שורה חופשית אינה ניתנת לייצוג בסכימה
> (`product_id` NOT NULL + הטריגר `0099:15-37`), ו"שייך לאחים כהן" הוא בדיוק קיום שורת
> ‏`supplier_products`. לכן `QuickCreateProduct` (בדפוס `QuickCreateSupplier`) עושה את שתי
> הכתיבות שהמערכת כבר עושה בשני מסלולים אחרים: INSERT ל-`products` ואז `import_supplier_prices`.
> המוצר נכנס לסל מיד ומופיע בהזמנות הבאות. **המגבלה מוצהרת ומכוסה בבדיקה:** אין עסקה אחת סביב
> שתיהן, וכשל בשנייה משאיר מוצר ללא מחיר — הדיאלוג אומר זאת ומתקן על **אותו** מוצר בניסיון חוזר,
> כי ל-`products` אין אילוץ ייחודיות על שם.
>
> **‏(ד) "מי שינה את המחיר" — הפער היה במקום אחר.** עמודת **ספק** ב-`/prices` קיימת ברציפות
> מהקומיט הראשון ועד היום, וכך גם "מחיר קודם" ו"שינוי"; שום חיווי ספק לא הוסר. מה שנמחק ב-10.08
> (`30e0e3e`) הוא **מסך היומן** — היחיד שענה מי עדכן, ממה למה, ובאיזו סיבה. הבעלים הכריע להחזירו
> **מצומצם**: ‏`/supplier-log` — "יומן עדכון ספקים" — ‏`suppliers` ו-`supplier_products` בלבד.
> **אין במסך שום שורת מוטציה גולמית:** הגרסה הראשונה הדפיסה `old_values`/`new_values` כ-JSON,
> הבעלים זיהה זאת מיד כרעש (‏UUID, ‏`org_id`, שמות עמודות), וזו בדיוק הסיבה שהמסך הקודם נמחק.
> מה שמוצג הוא **רק השדות שהשתנו בפועל**, בעברית, מפורמטים לפי סוג (כסף, תאריך, זמינות) — מפה
> מפורשת של שדות; כל מה שאינו בה אינו במסך. **בעלים בלבד, ולא מהעדפה:** ‏`audit_logs` הוא
> owner+accountant ואילו השמות שהוא מפענח הם owner+office; החיתוך הוא תפקיד אחד. ההנמקה למחיקה
> המקורית לא בוטלה — טבלת כל המוטציות אינה חוזרת, וזה כתוב במצבה ב-`Layout.tsx`.
>
> **אימות:** ‏build ו-typecheck ירוקים; ‏970 בדיקות ירוקות, ובהן שתי סוויטות חדשות —
> ‏`quickCreateProduct.spec.tsx` (סדר שתי הכתיבות, ואי-יצירת מוצר כפול בניסיון חוזר) ו-
> ‏`paymentRequestAllocationBlock.spec.tsx` (הכיסוי הראשון אי-פעם למסלול האישור החריג בלקוח).
> ‏`p0_client_dml_acl.sql` קיבל שלוש תוצאות מחיקה נפרדות (ראיה תומכת / טיוטה / הזמנה חיה)
> ו-`p46` הוסיף את `soft_delete_supplier` לרשימת קוראי ה-`payable`. **טרם רצו:** שער האיכות
> המלא ב-CI ואימות ויזואלי headed.

עודכן: 18.08.2026 (לילה מאוחר), T7.3k — קנבס ‏#F4F5F3, קרם נקודתי, לוגו Onyx, עמודות לפי דירוג, טבעת-רפרנס, מובייל שקוף-אפרפר. **נפרס לייצור 19.08.2026 בפריסה `a4dfbdc7` מתוך `b091671`** — הניסוח "טרם נפרס" כאן היה שגוי ותוקן בעת השחרור הבא.

> **T7.3j-k — סבב ההתאמות האחרון (תמונות ‏#23-27):** ‏(א) **קנבס** קרם→אפור ניטרלי ‏#F4F5F3;
> הקרם ‏#FFF6E9 נשאר **נקודתי בלבד** (רקע האייקון, ‏sunken, אותיות ה-tooltip). ‏(ב) **הסמל
> Onyx** — ‏SVG + אייקוני 192/512 רונדרו מחדש. ‏(ג) **עמודות חודשיות לפי דירוג** (חריג-בעלים
> ל"צבע=ישות": גבוה=Onyx, אמצע=Oceanic, נמוך=אפרפר-רמפה) + **נקודת ה-tooltip תואמת את צבע
> העמודה** (‏colorFor ב-DarkTooltip). ‏(ד) **טבעת הכיסוי** נבנתה כרפרנס ‏#23: ‏track חיוור,
> קשת עבה, ערך גדול במרכז, כותרת-משנה מודגשת. ‏(ה) **המובייל בשפה הנוכחית**: ‏top-bar
> ומגירה ‏topbar/75+blur ("אפרפר טיפ-טיפה שקוף"), מגירה בהירה עם הגלולה הכחולה. ‏(ו) טריגר
> פתוח כחול ופאנלים בהירים (T7.3i) נשארים. ‏(ז) המשך יישור מובייל (תמונות ‏#30-35): אייקוני
> הטופ-בר חשופים-עגולים בדיו כהה (הקופסה הקרמית הוסרה), אייקון האפליקציה רונדר על ‏#F4F5F3,
> סרגל הפעולות התחתון ‏topbar/75+blur, המגירה אטומה, ‏scrim מגירה+מודאל ‏shell/50 ניטרלי
> (הכחלחל הוסר), ‏splash המניפסט ‏#F4F5F3.

> **T7.3i — שלוש הכרעות בעלים (תמונות ‏#19-21):** ‏(א) **פס ה-hero הוסר** — "שני החצאים של
> הצבע"; הדשבורד = חיטה מלאה + הכתם העוקב, כמו כל מסך (‏tombstone עם הלקח המדיד: אין מעבר
> נקי ‏209↔77). ‏(ב) **הטופ-בר אפרפר-קריר בכל המסכים** — טוקן חדש ‏`--color-topbar`
> ‏(94.5%/0.008/229 ‏@75%+blur); הבעלים העדיף את האפרפר על הקרם. ‏(ג) **לשונית פתוחה =
> גלולה כחולה כהה** — ‏`groupTriggerCls` ‏open⇒‏bg-action (תמונה ‏#21).

> **T7.3h — שלושה תיקוני בעלים (תמונות ‏#17-18):** ‏(א) **"עדיין יש את הדבר הזה" בדשבורד** —
> אובחן: שקיפות-86%+blur של כרטיסי הדשבורד נהייתה עכורה-ירקרקה על הפס הכהה; הכרטיסים
> **אטומים** מעכשיו (שקיפות-T7.1 נקברה, איסור glassmorphism בלי חריגים). ‏(ב) **פאנלי
> ה-dropdown והחשבון התהפכו לנייר בהיר** — פריטים בדיו כהה, הפעיל בגלולה אוקיאנית ("בהיר עם
> סימון כחול"); ‏surface שלישי `'panel'` ב-`linkCls`. ‏(ג) **כתם עוקב יחיד** — כתם-הנגד הוסר
> ("צריך שזה יהיה כדור אחד").

> **T7.3g — ארבעה תיקוני בעלים (תמונות ‏#15-16):** ‏(א) **"בדאשבורד זה לא תוקן"** — אובחן:
> רצועת ה-fade של פס ה-hero (שקוף-על-חיטה) היא מיקס-זית; אין מעבר נקי בין גוון ‏209 ל-77,
> לכן הפס נחתך **בקצה חד** ‏(330px, אטום) והרצועה נעלמה. ‏(ב) **רמפת הגרפים = צבעי המערכת
> בלבד**: ‏OCEANIC + שלושה מיקסים אטומים שלו על ‏surface ‏(#477479/#85A1A3/#BDCBCA) + ‏ONYX —
> מונוכרום-סדרתי, הפרדת שכנים בבהירות. ‏(ג) **רכש מול תשלומים**: רכש=Oceanic רציף,
> תשלומים=Onyx **מקווקו** ‏(dash חזר ל-LineSeries) + ‏cursor מקווקו ב-hover — כרישום הבעלים
> על ‏#16. ‏(ד) **פס כותרות הטבלאות** ‏Onyx→**Oceanic** ‏(#15; ‏shell-ink-soft עליו ‏8.13:1).

> **T7.3f — הצורה הסופית של הרקע (תיקון-בעלים על T7.3e):** ‏(א) האפקט העוקב חזר **לרקע** —
> ‏`.app-glow` עם כתם ‏#003F47/15% שנע עם העכבר + כתם-נגד חיטה-עמוקה — ובלי שום בסיס צבעוני
> סטטי מתחתיו; ‏body = נשימת חיטה בלבד. ‏(ב) ‏spotlight הכרטיסים של T7.3e **בוטל** ("ממש לא
> רציתי"). ‏(ג) **צללי הכרטיסים הושחרו** ל-Onyx רך-רחב (‏0.07/0.13) — היפוך מפורש של
> לחישת-T7.2: הצל הוא ההפרדה. ‏(ד) ‏ink-muted הוקשח ‏49.5→47% — ‏4.75 בשיא הכתם. ‏(ה) אור
> הפס בגוון ‏209. אומת headed בשני מיקומי עכבר + מסך פנימי + מובייל; שער מלא ירוק.

> **T7.3d — פס ה-hero:** על "למה הרקע עדיין תכלת" הוצגו לבעלים שלוש הדגמות מצולמות (עומק
> בשוליים / פס עליון / כהה מלא); נבחר **הפס העליון**. ‏`.dash-hero-band` — ‏`--color-action`
> מלא מאחורי הניווט, הברכה וכרטיסי הכסף, דשבורד בלבד, דועך ‏250→430px; טקסטי הפס מתהפכים
> ‏(`.dash-hero-head`, ‏h1 ‏10.83:1, ‏meta ‏8.13:1); הדפסה נקייה. אגב הסבב אובחן **באג צילומי
> ‏headless** ‏(Chrome/Edge 151 — פיקסלים בלי ה-CSS המוזרק של vite; ‏computed תקין) — אימות
> ויזואלי רץ מעתה ‏headed; נרשם בזיכרון הסוכן.

> **T7.3c — חמש הכרעות בעלים נוספות:** ‏(א) **הגלואו עשיר, לא פסטל** — ‏radials רוויים
> ‏(C ‏0.075-0.085) והבסיס עבר ל-**body** על מלוא גובה המסמך (fixed כיסה viewport אחד וחשף
> רצועת חום; נמדד ‏ink-muted ≥ ‏4.5). ‏(ב) **פס הכסף = שלושה כרטיסים נפרדים** (גוש ממוזג במובייל
> נקרא כבאג); ‏spec עודכן. ‏(ג) **גיוון גרפים במגמות** — עמודות `chart-2` מול טבעת `chart-1`
> ‏(`SpendBarChart` קיבל `color`). ‏(ד) **קפסולות הדופק הוסרו** ("לא רלוונטי") — הקומפוננטה
> נמחקה, ‏`.pill-hatched` נשאר ללא צרכן. ‏(ה) **המובייל ומסכי הכניסה עברו לכחול** (‏bg-action
> במקום ה-shell השחור; ‏Onyx נשאר בכרטיס המשימות ובפסי הטבלאות) **והלוגו אוקיאני** —
> אייקוני 192/512 רונדרו מחדש, ‏theme-color ‏#003F47, ‏brand/identity.md §03 עודכן.

> **T7.3b + ‏rebrand (frontend בלבד):** ‏(א) **כל thead במערכת = ‏`.table-head`** — פס Onyx עם
> אותיות בהירות (‏13 קבצים, ‏17 טבלאות; ‏12.7:1; הדפסה חוזרת לנייר; כפתורי מיון hover בהיר).
> ‏(ב) **הקפסולות ופס הכסף עוגנו בכרטיסים** — חשופים על הקנבס הזוהר הם נקראו כבאג; ההפרדה
> הפנימית בפס נשארת ריווח בלבד. ‏`dashboardDensity.spec` הוחזר לחוזה "פס=כרטיס". ‏(ג) **הרקע
> כחול רציף לכל הגובה** — הבסיס הליניארי אוקיאני בשלוש נקודות (‏0.7/0.5/0.65); אין רצועת חום
> באמצע/תחתית (נמדד ‏ink-faint ‏4.58+). ‏(ד) **‏rebrand: ‏SupplyFlow → InPlace** לפי `brand/`
> — ‏`APP_NAME`, ‏title, ‏manifest, כותרת הלוגין, אייקוני ‏192/512/maskable חדשים מסמל
> ‏Place Bay (‏Onyx על נייר, רונדרו ב-Chrome headless), ‏`public/brand/inplace-symbol*.svg`,
> שמות קובצי ייצוא. מזהי-מכונה (`supplyflow:*` events, ‏offline DB, דומיין הדמו) **לא** שונו —
> חוזים טכניים; שינוי DB ינתק תורי offline. ‏smoke ו-spec הכותרת עודכנו.
> **אימות:** ‏936 ירוקות; צילומי דשבורד/חשבוניות/רו"ח נקראו — הפס הכהה, הכרטיסים, הרקע
> הרציף וה-InPlace בניווט אומתו.

> **T7.3 — סבב תיקוני בעלים על T7.2 (frontend בלבד): "הכחול שולט, השחור נדיר, הכתמתם הוסר".**
> ‏(א) **גלואו אוקיאני בלבד** — שלושת ה-washes כחולים; אפס גוון חם (נמדד: ‏ink-faint ‏4.52+
> בנקודה הכהה). ‏(ב) **שחור→כחול**: גלולת ניווט פעילה, טריגרי קבוצות, פאנלי dropdown, דיסקת
> החשבון וה-DarkTooltip — כולם `bg-action`; ‏Onyx נשאר רק בכרטיס "משימות לפי תפקיד" ובמעטפת
> המובייל. ‏(ג) **קו-המבטא הוסר מהגלולה** (נקרא כצבע לא מוסבר) ו**התמצאות-הגוון פרשה**: שלושת
> ‏`--color-section-*` = ליטרל אוקיאני אחיד; המנגנון (URL בלבד, ‏Layout יחיד) נשאר, ‏specs
> עודכנו. ‏(ד) **נטרול הכתמתם**: ‏surface-sunken בחצי chroma; כל 37 ה-hover החמים → ‏action-wash;
> ‏chip-filter ו-btn-secondary במנוחה → ‏action-wash. ‏(ה) **כרטיסים חזרו להיראות** — שקיפות
> הדשבורד 72%→86%. ‏(ו) **קפסולות הדופק** (`CoveragePills`) — שורת הרפרנס עם המרקם המקווקו
> ‏(`.pill-hatched`), על שני יחסים אמיתיים (אושרו/שולמו בחודש; ‏fallback מפורש-בשם לחודש אחרון
> עם נתונים); ‏DEBT ‏§46 נסגר. ‏(ז) מועמדי הביקורת: ‏BandStat בלי הקשר כפול, תג "מידע נוסף"
> הוצמד, ‏skeleton סונכרן, שורות דורש-טיפול קובצו, זנב-אפס קוצץ לשבוע אחד, טבעת בלי כפל-ניסוח,
> ‏DeltaChip כטקסט-חץ, לשוניות ה-wizard כגלולות.
> **אימות:** ‏936 ירוקות; צילומי 1440/390 + רו"ח + הזמנה נקראו והושוו.

> **T7.2 — הבעלים השווה מול תמונות הרפרנס ("עדיין לא נראה כמו שדמיינתי") — 12 הכרעות בשני
> סבבי שאלות, קריאה עמוקה של שש התמונות (frontend בלבד).**
>
> ‏(א) **מדיניות אפס-חלון-מלא**: דלי בלי שורות בחלון שנשלף במלואו = ‏₪0 נמדד — הקווים רציפים
> כמו ברפרנס (‏4 אתרים: ‏mergeWeeklyComparison, ‏weeklySeries, ‏TrendSparkline, רו"ח); חלון
> כולו-שקט מציג empty state; שבוע נעדר-מסדרה נשאר null. ‏(ב) **שפת גרפים**: ‏`DarkTooltip`
> גלולת-Onyx אחיד; ‏areas ‏monotone בלי גריד/נקודות עם מילוי ‏28% ו**שמות סדרה על קצות
> הקווים** (מדורגים כשנפגשים); עמודות בלי מספרים-על-עמודה + גריד רציף חיוור + תאי החודש/קודם
> בכותרת; ‏donut עבה (‏55/92%) עם סה"כ גדול במרכז וצ'יפי ‏●שם-% מתחת (בלי רשימת צד);
> ‏CoverageRing עם הערך במרכז; ‏`GroupedBarChart` חדש לרו"ח (זוגות מעוגלי-מלא). ‏(ג) **גלולת
> ניווט צפה**: אין בר — ‏header שקוף עם גלולת לוגו, גלולה לבנה ממורכזת (טקסט-בלבד,
> ‏shortLabel: הזמנות/קבלה/מסמכים), פעיל = גלולה כהה + ‏`.section-mark` תחתיה (אין צרכן accent
> שלישי), ‏dropdowns כהים mounted-hidden, אווטאר ראשי-תיבות; מובייל ללא שינוי. ‏(ד) פס הכסף
> Crextio-חשוף: אייקונים שטוחים, ‏kpi-hero משקל 500. ‏(ה) כרטיסים ‏rounded-3xl וצל כמעט-אפסי
> בכל האפליקציה. ‏(ו) **הגלואו בצבעי האתר בלבד** — ‏wash אוקיאני עוקב-עכבר + חיטה ניטרלית
> ‏(chroma≤0.022), הכתום הוסר (הכרעת בעלים); נמדד ‏ink-faint ‏4.52-4.61. ‏(ז) כרטיס כהה: מטריצת
> נקודות אמיתית (נקודה=משימה, תקרה 12) + צ'יפ-חץ ל-`/alerts`.
>
> **אימות:** ‏936 בדיקות ירוקות (‏dashboardSeries ו-layoutActiveState שוכתבו לחוזים החדשים;
> ‏deadEndFixes עבר ללא שינוי — חוזה הקבלה של הצ'יפים); צילומי qa ‏1440/390 נקראו והושוו
> תמונה-מול-תמונה לרפרנסים; תוקן קולישן תוויות-קצה על זנב-אפס משותף; אפס גלישה אופקית.
> ‏Skills: ‏image-to-code, ‏hyliox-pixel-perfect (משמעת חילוץ), ‏qa-harness (אימות).

> **T7.1 — השוואה קפדנית מול חמש תמונות הרפרנס + שלוש דרישות בעלים חדשות (frontend בלבד).**
> הבעלים ביקש השוואה תמונות↔תוצאה; הפערים נסגרו לפי הכרעותיו:
>
> ‏(א) **רמפת גרפים מהפלטה** — ‏chart-2..5 עברו למדרגות משלוש משפחות העוגנים (טיל בינוני/בהיר,
> אוניקס-צפחה, ברונזה ל"אחר"); עמודות החודשים בגוון יחיד; ‏donut בסדר `[0,1,3,2]` דרך
> ‏`sliceColor` יחיד; ‏areas ‏monotone. חריג בעל-שם: ‏ΔE נורמלי בין הטילים ≈10 — מיתונים
> מתועדים. ‏(ב) **מבטאי האזור קפאו כליטרלים** (הכרעת בעלים: להשאיר את הגוונים) —
> ‏`quickActions.spec` מצמיד את שלושת הערכים ואוסר `var(`. ‏(ג) **ברכה ככותרת** לפי שעת עסק +
> שם פרטי; "מרכז הבקרה" נשאר ב-meta ובקטלוג. ‏(ד) **גלואו מוגבר בצבעי המערכת בלבד**, מכויל
> בנקודה הכהה ל-ink-faint ‏≥4.5 (נמדד 4.52/4.55). ‏(ה) **טבעת כיסוי רדיאלית** על היחס האמיתי
> היחיד בסנאפשוט (דרישות פעילות עם תאריך / כלל הפעילות). ‏(ו) **כרטיסי דשבורד שקופים-חלקית**
> ‏(72% + ‏blur, ‏`.dashboard-depth` בלבד; תפעול ודפוס אטומים). ‏(ז) **תפריט עליון בדסקטופ** —
> ה-aside הוחלף בבר Onyx: יעדי יומיום ישירים (אייקונים עד 2xl), ‏ניהול/בקרה כ-dropdowns
> שנשארים ב-DOM כשסגורים, טריגר קבוצה פעילה לובש את אייקון המסך+glyph, חשבון כ-dropdown;
> מובייל ללא שינוי. ‏(ח) פקדים בלי מסגרת כהה: ‏quick-filter pills ו-btn-secondary טונאליים.
>
> **אימות:** ‏typecheck + ‏936 בדיקות ירוקות (כולל `quickActions`, ‏`layoutActiveState`,
> ‏`dashboardDensity` שעודכנו לחוזים החדשים); ‏שער הדפדפן עודכן לבר העליון
> (‏first-nav-link + ‏dropdown פתיחה/Escape). צילומי Playwright: דשבורד 1440/390, חשבוניות,
> ‏dropdown פתוח — אפס גלישה אופקית. ‏dataviz validator: ‏CVD עובר (‏ΔE ‏10.1); כשלי
> ‏chroma/lightness גנריים = חריג המותג המתועד. ‏DESIGN.md עודכן יחד עם הקוד.
> **הכרעת עיצוב, לא חוב פעיל:** ‏progress-pills עם אחוזים לא נבנו כי אין נתון; ‏stacked bars
> חודשי×קטגוריה הושמטו ככפילות של ה־donut.

> **T7 — רדיזיין לפי פלטת רפרנס של הבעלים (frontend בלבד, אין מיגרציה ואין Edge).**
> חמש תמונות רפרנס משולחן העבודה קבעו: פלטה (Onyx ‏`#0A171D` · Wheat ‏`#FFF6E9` · Oceanic
> ‏`#003F47`), פריסת דשבורד בסגנון Crextio, רינדור charts בסגנון shadcn, כרטיסים ללא borders
> וגרדיאנט רקע שנע עם העכבר.
>
> **מה השתנה:** ‏(א) בלוק ה-@theme ב-`src/index.css` — אותם שמות טוקנים, ערכים חדשים; רמפת
> הגרפים 2–5 ומבטאי האזור לא זזו, והפנקס נמדד מחדש (מינימום line-strong ‏3.17, לבן על action
> ‏11.65). ‏(ב) `.card` ללא גבול, ‏rounded-2xl, צל אוקיאני — ההפרדה מגוון+צל+ריווח. ‏(ג)
> ‏`.app-glow` + ‏`useGlowPointer` ב-`Layout.tsx` — שני washes רדיאליים שנעים לעבר הסמן;
> ‏rAF, ‏pointer:fine בלבד, כבוי תחת reduced-motion. ‏(ד) דשבורד: grid ‏12 עמודות מ-`lg` —
> פס כסף חשוף כשורת גיבורים (`kpi-hero`), "דורש טיפול" בעמודה אחת (6), אספקות ככרטיס (3),
> **"משימות לפי תפקיד" עברו לכרטיס Onyx כהה** (3, ‏`TaskLine` נמחק מ-`ui.tsx`), שלושת גרפי
> המגמות ככרטיסים, וה-comparison קיבל wash גרדיאנט מתחת לקווים. סדר ה-DOM לא זז — שער
> הדפדפן ממשיך למדוד "דורש טיפול" ראשון ו"אספקות" שני. ‏(ה) theme-color/manifest ל-Onyx/Wheat.
>
> **אימות:** ‏build + ‏verify מלא ירוקים (96 קבצים, 936 בדיקות; ‏`dashboardDensity.spec` נכתב
> מחדש לחוזה T7). צילומי Playwright מקומיים מול Supabase מקומי: דשבורד 1440 ו-390, חשבוניות
> 1440 — אפס גלישה אופקית, ‏glow מגיב לעכבר. ‏DESIGN.md, ‏brand/identity.md §03 ו-PROGRESS
> עודכנו יחד עם הקוד. גרף ההשוואה ריק בצילום מוקדם היה race של IntersectionObserver בצילום
> full-page, לא באג.

> **שוחרר לייצור 18.08.2026 — ‏PR #61 ‏(`668f649`): frontend בלבד.** אין מיגרציה, אין Edge, אין
> שינוי worker. ראש המיגרציות בייצור נשאר **`0144`**.
>
> **הבעלים צילם שלושה מסכים בנייד. שלושתם אותה משפחת תקלות:** כלל פריסה שהפך תוכן קריא ללא־קריא,
> בלי שנרשמה שגיאה באף מקום.
>
> **‏(א) `.note` היא שורת flex** (`src/index.css:194`), ולכן כל רצף טקסט גולמי בין שתי תגיות inline
> הוא flex item שמתכווץ ל־min-content. נמדד ב־390px עם נוסח ההגדרות עצמו: **766px גובה, רוחב שורה
> מרבי 55px, 81 מקטעים** — עמודות ברוחב מילה. אותה פרוזה בעטיפה אחת: **266px, 266px, 27**.
> ‏**23 מקומות** תוקנו. שניים מהם נמצאו רק על ידי השומר החדש `src/components/noteProse.spec.ts`,
> שנופל על כל גוף `Note` שמערבב פרוזה עם אלמנטים.
>
> **‏(ב) `LifecycleStrip` הסתירה שניים מארבעת השלבים.** ארבעה שלבים ב־390px בשורה ללא `flex-wrap`
> עם `overflow-x-auto`: **169px חתוכים, 2 מתוך 4 גלויים**, בלי שום סימן שיש המשך. הרצועה נשברת
> עכשיו לשתי שורות מתחת ל־`sm`, ו־`flex-1` נשמר מ־`sm` ומעלה כדי שהדסקטופ לא ישתנה.
>
> **‏(ג) רשימת „יש לטפל לפני אישור” חזרה על עצמה.** מסמך ייצור העלה את אותו ממצא על 20 שורות,
> והמסך הדפיס אותו 20 פעם — כך שהממצאים שלא היו חזרות נקברו ביניהם. `groupFindings` שומר כל מספר
> שורה ומפסיק לחזור על המשפט, עם טווחים (`1–8, 10–11, 13–15, 17, 19–24`). מעל חמישה סוגי חריגה
> הזנב מתקפל.
>
> **איך זה נפרס.** ‏`build`, ‏`verify` (‏96 קבצים, 935 בדיקות) ותרחישי הדפדפן עברו ב־CI על ה־PR.
> ‏Frontend נבנה עם env ייצור: אפס התייחסות ל־loopback, מארח Supabase יחיד, אפס `VITE_DEMO_PASSWORD_SEED`.
> ‏Pages קיבל את `index-DJ1P45QT.js`; הכתובת הקנונית מגישה אותו, כל נכס מוגש קיים ב־build,
> ו־`/`+`/login` החזירו 200 בדסקטופ ובמובייל. הצ׳אנקים שהוגשו נקראו בחזרה: הרצועה העוטפת נמצאת
> ב־entry וקודמתה שנחתכה אינה, פקד הקיפול של הממצאים נמצא ב־`DocumentReview`, ועטיפת ההערה
> ב־`Settings`.
>
> **הראיה החיה.** ‏390px, מחובר כבעלים בייצור: הערת ההגדרות **246px גובה, רוחב שורה מרבי 290px**;
> רצועת השלבים על המסמך של הבעלים **0 חתוך, 4 מתוך 4 גלויים בשתי שורות**; ורשימת החריגים של אותו
> מסמך **25 ממצאים ב־5 שורות מקובצות, 230px**. אפס גלישה אופקית, אפס שגיאות קונסול.
>
> **מה טרם הוכח:** התיקון נמדד ברוחב 390px. רוחבי ביניים (טאבלט 768px, שבהם הרצועה כבר אינה
> נשברת) לא נמדדו ידנית, ונשענים על תרחישי הדפדפן ב־CI.

עודכן: 18.08.2026, פיצול חבילות אוטומטי — דגל חלקיות כן, תקרת 40 עמודים, והמתג בהגדרות.

> **שוחרר לייצור 18.08.2026 — ‏PR #59 ‏(`5c281e7`): `document.partial` נגזר, תקרת הפיצול
> האוטומטי 20→40, והמתג שלו נוסף להגדרות (frontend + worker + מיגרציה `0144`).**
> ‏Production נמצא כעת ב-**`0144`**.
>
> **מה השאלה של הבעלים חשפה.** הועלה PDF נקי בן 24 עמודים והוא דרש אישור ידני. הזיהוי לא נכשל:
> ארבעה חלקים סווגו בביטחון `0.99`/`0.98`/`0.98`/`0.99`, הרבה מעל סף ה-`0.900`. חסמו אותו שלושה
> שערים, ושניים מהם היו באגים.
>
> **הבאג המרכזי: `document.partial` היה מקודד `True` בקשיחות** בכל נקודת קריאה (`ocr.py:238`,
> `ocr.py:369`, `parsers.py:610`) ומעולם לא נגזר משום דבר. שלוש תוצאות חיות: ‏(א)
> `service_record_document_packet` דורש `not partial`, ולכן **פיצול אוטומטי היה בלתי-אפשרי מבנית
> לכל מסמך שאי-פעם עובד** — בלי קשר למדיניות או לביטחון; ‏(ב) `DocumentPacketReview` עשה אותו
> לבדיקת תשומת-הלב הראשונה, ולכן הקיפול ששוחרר יום קודם מעולם לא קיפל דבר (24 עורכים ו-192
> `<option>` על החבילה של הבעלים); ‏(ג) כל מסמך נשא „החילוץ חלקי” במסך הבדיקה.
>
> **מה הוא אומר עכשיו:** האם לא הסתכלנו על חלק מהמסמך. רק עמודים שמעולם **לא נוסו** — שנפלו מעבר
> לתקרת ה-OCR בתשלום, או שנשארו בלי קריאה כשה-adapter כבוי. עמוד שנשלח וחזר ריק אינו partial
> במכוון: גב ריק וקריאה שנכשלה אינם ניתנים להבחנה בלי ניתוח פיקסלים, ו-`second_pass` ממילא מנסה
> שוב עמוד שלא הניב שורות. כותרות עליונות/תחתונות והערות שוליים ב-Word נקראות עכשיו במקום להיספר
> כפער, כך שמספר עמוד ב-footer אינו מסמן מסמך כחסר.
>
> **התקרה.** ‏`0144` מעלה את תקרת הפיצול האוטומטי מ-20 ל-40. **תקרת ה-OCR בתשלום נשארת 20**
> (`max_ai_pages`) — העלאתה מכפילה את חשבון הספק לכל סריקה ארוכה וזו החלטת עלות נפרדת. לכן חבילה
> **סרוקה** בת 21–40 עמודים עדיין partial בכנות ועדיין דורשת אדם; התקרה פותחת PDF עם שכבת טקסט,
> ו-PDF מעורב שהעמודים הסרוקים בו נכנסים מתחת לתקרה.
>
> **המתג.** ‏`document.packet_split` הגיע ב-`0140` ומעולם לא נוסף למסך ההגדרות. נמדד בייצור: יש
> שורות לשלוש המדיניויות האחרות, כולן מופעלות, ולזו — אין שורה כלל. הוא עכשיו המתג הראשון בפאנל,
> עם ניסוח שאומר במפורש שמסמך סרוק מעל 20 עמודים לא יתפצל לבד. **המתג עדיין כבוי** — הפעלתו היא
> החלטת בעלים.
>
> **הסרגל הצף הוסר.** שלושה מארבעת פאנלי הבדיקה כבר תיעדו בקוד שההכרעה יושבת מעל הראיות המקופלות;
> סרגל נעוץ ל-viewport גרר אותם חזרה מתחתיהן והשאיר רצועת תוכן נגלל בינו לבין סרגל הפעולות
> הגלובלי. `PrimaryDecision` מרנדר בזרימה; כפתור פאנל החבילה עלה לראש רשימתו; מסך אישור הסריקה
> שומר את הכפתור מתחת לתמונות כי שם התמונות הן מושא ההכרעה. נוסף פקד מפורש לקיפול/פתיחה של כל
> רשימת החלקים.
>
> **איך זה נפרס.** מיגרציה: גיבוי 166 טבלאות / 26,698 שורות ללא כשל + גיבוי מקור הפונקציה,
> ‏dry-run בטרנזקציה מגולגלת (התקרה נבדקה בתוך הטרנזקציה ואומת שה-20 חזר אחרי ה-rollback), החלה
> forward-only, שורת ledger, ואז probe ו-postflight: **כל הספירות העסקיות זהות** (ארגון 1,
> מסמכים 41, ג׳ובים 62, חבילות 5, חלקי חבילה 44, חשבוניות 14, מוצרים 271, ספקים 21), כל זרועות
> הפרדיקט שרדו את ה-`create or replace`, ‏`service_role` בלבד מורשה, החריג רשום, ואפס הפרות
> ‏A5/A6. ‏Frontend נבנה מ-worktree מבודד על `5c281e7`; ‏`index-BR9QYq1w.js` זהה בכתובת הקנונית
> ובפריסה `684ac40d`, ו-`/`+`/login` החזירו 200 בדסקטופ ובמובייל. ‏Worker: הפול הוחלף כולו
> ל-`supplyflow-ocr-worker:5c281e7`.
>
> **הראיה החיה.** מסך החבילה בייצור ב-428px: „24 עמודים · 24 מסווגים”, אמירת כיסוי אחת בראש,
> כפתור האישור **לפני** הרשימה, והרשימה מקופלת — **0 שדות מספר ו-0 `details` פתוחים** מול 24
> עורכים ו-192 `<option>` קודם. אפס שגיאות קונסול, אפס גלישה אופקית.
>
> **מה טרם הוכח חי:** ‏(א) פיצול אוטומטי בפועל — המתג כבוי, ולכן אף חבילה עוד לא התפצלה בלי אדם.
> ‏(ב) שורות חבילה קיימות נרשמו תחת ה-worker הישן ולכן עדיין נושאות `source_partial=true`; העלאה
> חדשה היא שתמדוד את הדגל הכן. ‏(ג) ‏PDF מעורב בן 21–40 עמודים — מקרה שלא יכול היה להתקיים קודם
> ואין לו תקדים בייצור.

עודכן: 18.08.2026, בדיקה כתיבת-חריגים, שער איכות מדוד ו-OCR שקורא את המידע העשיר — שוחררו לייצור.

> **שוחרר לייצור 18.08.2026 — ‏PR #57 ‏(`e83c41d`): מסך הבדיקה כתיבת חריגים, אזהרת צילום מכוילת,
> ו-preprocessing ששומר על המקור (frontend + worker; **ללא מיגרציה וללא Edge**).**
> ‏Production נשאר ב-**`0143`** — המטריצה הריצה רק את שני המשטחים שהשתנו.
>
> **מה נמדד לפני שנכתבה שורת קוד.** הרצתי את `scanning.py` החי על 17 מסמכים אמיתיים
> (‏10 צילומי טלפון ב-`NIR-APP-DOCS\TEST` ‏+7 מ-`calibration-corpus\20260808`) ועל 20 גרסאות
> מושחתות. שלושה ממצאים שהפכו את הכיוון:
> ‏**(א)** ‏`_select_mode` בחר שחור-לבן ב-**11 מתוך 17**, כי זרוע „הצל החזק” שלו (`>= 0.045`)
> התקיימה ב-**100%** מהמסמכים — ה-`shadow` הנמוך בקורפוס הוא ‏`0.0494`. מבחן שתמיד עובר אינו מבחן.
> ‏**(ב)** ‏`document_not_detected` נורה ב-**8 מתוך 17**; כל הכשלים ברצפת ה-12% שטח של
> ‏`_polygon_is_valid`, ובשישה מהם המרובע הגדול בפריים היה `0.002`–`0.116` — החתימה של דף
> שממלא את הצילום ואין לו קצה. ‏**(ג)** המדד `sharpness` הקיים הוא `min(1.0, var/1500)` ומגיע
> ל-**`1.0000` בכל צילומי הטלפון** — רווי, ולכן חסר תועלת כשער.
>
> **מה נבנה.** ‏`src/lib/imageQuality.ts` מודד בדפדפן **לפני** ההעלאה ו**מזהיר, לא חוסם**
> (החלטת בעלים): מסמכים אמיתיים ‏345–8,717 שונות Laplacian מול ‏60–76 למטושטש 9×9 ⇒ סף **130**;
> ‏luma ‏137–245 מול ‏58–65 ⇒ סף **85**. ה-`File` שנשלח ל-tus נשאר **זהה בייטים** — המדידה על
> canvas, והכלל ב-`FileUpload.tsx:371-373` לא נפרץ. **אין שער חשיפת-יתר:** קבלה לבנה על רקע לבן
> חותכת ‏85% מההיסטוגרמה באופן לגיטימי בעוד תמונה שהוארה חותכת ‏52–61% — הטווחים חופפים ולכן לא
> נשלח סף מומצא. ב-worker: ברירת המחדל היא גווני אפור, סבב שני מבונרז רץ רק על כשל מדיד
> (אפס שורות, או ‏≥75% מהשורות שלא שוחזרו בין מעברי הקונצנזוס) ונשמר רק אם הוא טוב יותר;
> ‏fallback לפריים מלא כשאין מרובע-עמוד בכלל, מגודר ב-`_edge_span` (מסמכים אמיתיים ‏0.595–1.000,
> פאנל קטן מחוץ-למרכז ‏0.029). **דרישות פינות ידניות: ‏8 → 2 מתוך 17.**
>
> **מסך הבדיקה.** חבילה בת 42 עמודים בנתה ‏~294 מכולות ו-336 `<option>`; טבלת התאמה של 338 שורות
> בנתה ‏2,366 תאים. עכשיו נפתח רק מה שדורש הכרעה, והרשימות הארוכות אינן נבנות כשהן סגורות.
> נוסף `StickyPrimaryAction` (על דפוס `.phone-taskbar` של `Receiving`, עם clearance נמדד
> ב-`ResizeObserver` כדי שלא יכסה את השורה האחרונה), ‏`Disclosure` משותף ב-`ui.tsx`, ושלושה
> ‏accent של תחום (`--color-section-*`) שנבחרים **לפי URL ולעולם לא לפי נתונים** — ‏`chart-2`
> נפסל כי גוונו ‏245 מול ‏243 של `sky`/`info`.
>
> **מה נמדד אחרי, מול הנתונים החיים ב-428px:** בדיקת חשבונית ‏11,705px → **‏4,954px**,
> ‏`/documents` ‏3,563 → **‏2,728**, ‏`/inventory` ‏5,146 → **‏4,703** (‏6→3 כרטיסים),
> ‏`/dashboard` ‏2,336 → **‏2,174** (‏5→2). מרחק לכפתור הראשי ‏~2,000–2,400px → **‏0**.
> שורת חריגה במחירון: ‏4 הקשות + הקלדת מחיר → **‏2 הקשות**. אפס שגיאות קונסול, אפס גלישה
> אופקית ב-375/390/428.
>
> **איך זה נפרס.** ‏Frontend נבנה מ-worktree מבודד על `origin/main` ‏`e83c41d`; החבילה מדברת רק
> עם `rkftlbctohswhbbiaqin.supabase.co`, אין `DEMO_PASSWORD_SEED`, וארבעה סמנים אומתו בתוך
> הארטיפקט (`data-section`, כותרת אזהרת הטשטוש, `sticky-primary-action`, ‏`section-mark`).
> הכתובת הקנונית והפריסה הייחודית `08354138` מגישות את אותם entry scripts; ‏`/` ו-`/login`
> החזירו 200 בדסקטופ ובמובייל. ‏Worker: הפול הוחלף כולו ל-`supplyflow-ocr-worker:e83c41d`,
> שני replicas, ‏`OCR_QA_PASSES=3`, ‏`OCR_PAGE_CONCURRENCY=3`, ‏`OCR_JOB_TIMEOUT_SECONDS=900`.
>
> **הראיה החיה — התוצאה, לא התהליך.** הועלה לאתר החי אחד מהצילומים שהקוד הישן **נכשל** עליהם:
> ‏`corners_source=automatic` עם `corners=[[0,0],[1,0],[1,1],[0,1]]` (ה-fallback פעל במקום לדרוש
> פינות ידניות), ‏`output_mode=grayscale` (קודם `black_and_white`), ושני המדדים החדשים נרשמו
> בייצור — ‏`raw_laplacian_variance=1414.535285`, ‏`raw_luma_mean=167.799538` — **זהים למספרים
> שנמדדו מקומית בכיול**. אחרי אישור הסריקה: ‏`review`, ‏`invoice` בביטחון `0.99`, ‏37 שורות,
> ו-`resource_metadata.second_pass = {attempted_pages:[1], extra_extractions:1}` — הסבב השני
> נורה, לא שיפר, והקריאה הראשונה נשמרה.
>
> **מה טרם הוכח חי, במפורש:** ‏(א) מסך פיצול החבילה — החבילה היחידה בדייר כבר עברה
> materialization, ולכן הפאנל לא מרונדר; הכיסוי הוא בדיקות יחידה בלבד. ‏(ב) ‏HEIC ממכשיר iPhone
> אמיתי — ‏`createImageBitmap` עלול לא לתמוך, ואז הצילום פשוט אינו נמדד. ‏(ג) סף ה-`0.75` של
> הסבב השני אינו מכויל מול התפלגות כשלים מדודה.

המסמך הזה מתאר רק את מצב העבודה הנוכחי. היסטוריית גרסאות, קמפיינים וראיות ישנות נמצאת ב־Git.

> **שוחרר לייצור 17.08.2026 — יצירת מוצרים מרובה במחירון, עימוד מסך הבדיקה, ומונה לשלב הפירוש
> (frontend + Edge + מיגרציה `0143`).** ‏Production נמצא כעת ב־**`0143`**.
>
> **מה נמדד לפני שנכתבה שורת קוד.** המחירון שהועלה חי (‏`מחירון עוצמה`, ‏338 שורות) **כן** נקרא
> נכון: כל 338 השורות הגיעו עם `sku` מלא — העמודה „מפתח פריט” מופתה למק״ט כמו שצריך. הסיבה
> שהמסך דרש אישור פריט־פריט היא אחרת לגמרי: **אף אחד מ־338 המק״טים אינו קיים בקטלוג.** ‏271
> מוצרים פעילים, לכולם מק״ט, חפיפה אפס — מספור אחר לגמרי (`11011`, ‏`10-361-0001` מול `0916`,
> ‏`2002038`). ‏`run_price_list_shadow` סיווג את כל 338 כ־`create_product`, ולכן לא היה מה למלא
> מראש והמסלול היחיד היה 338 יצירות נפרדות בטור אחד.
>
> **מה נוסף.** כפתור אחד יוצר מוצר לכל שורה מתוך מה שהמסמך הדפיס (שם, מק״ט, יחידה) ומסמן את
> השורות לקליטה במחיר שנקרא. המוצרים נוצרים **לפני** בניית האישור, ולכן הכלל „האישור אינו יוצר
> מוצרים” ב־`submit-price-list` לא נגע, ושם מוצר עדיין אינו מפתח התאמה. מק״ט שכבר יושב על מוצר
> פעיל אחד — מתחבר אליו; מק״ט על שני מוצרים — נשאר לאדם, כי זו בדיוק העמימות שהמתאים סירב לנחש.
> **שורות שהשם שנקרא בהן אינו שם נשארות פריטים בודדים** (החלטת בעלים, 17.08.2026): במסמך החי
> אלה בדיוק 5 שורות — `300.000` וארבע `^^`. דיאלוג האישור גם מציין כמה שורות נקראו במחיר של
> אגורה או פחות (3), כי זה מה שהמסמך הדפיס ולא תפקידו של המסך לסנן. הרשימה חולקה ל־50 שורות
> לעמוד, וסימון אחד מסמן או מנקה את השורות המוכנות בעמוד הנוכחי.
>
> **המונה לשלב הפירוש.** מונה העמודים של `0141` מתמלא רק לעמודים שעוברים OCR, ולכן PDF עם שכבת
> טקסט לא מייצר בר בכלל — נמדד בייצור: שתי סריקות דיווחו 3/4 ו־1/2, וכל המחירונים `null`.
> ההמתנה שהמשתמש רואה היא הפירוש, ולכן `0143` מוסיף את `progress_stage` שאומר איזה שלב שני
> המונים מתארים, וכותב לשלב הפירוש מגודר על `interpretation_started_at`. מודל הקריאה מחזיר מונה
> **רק** כשהשלב שלו הוא השלב שהעבודה נמצאת בו, כך שקריאה שהסתיימה אינה יכולה להיות מוצגת כפירוש
> שרץ. ‏`interpret-document` מדווח 0 לפני המקטע הראשון ואחד לכל מקטע שנוחת, בלי await ובלי יכולת
> להפיל את הפירוש. הרצועה מציגה „מקטע X מתוך Y”.
>
> **איך זה נפרס ומה נמדד אחרי.** מיגרציה: גיבוי ממוקד (‏52 שורות `document_processing_jobs`,
> רשימת העמודות, ה־constraint, שתי שורות הרישום ושני מקורות הפונקציות — 49,562 בתים),
> ‏dry-run בטרנזקציה מגולגלת שאחריו `progress_stage` לא היה קיים, החלה forward-only, שורת ledger,
> ואז postflight: **כל הספירות העסקיות זהות** (מוצרים 271, ספקים 21, חשבוניות 14, הזמנות 226,
> הקצאות 9, הגשות מחירון 4, ג׳ובים 52, פירושים 37, ‏audit 19,727), עמודות הג׳וב 23→24, פונקציות
> ההתקדמות 2→3, ‏`service_role` בלבד מורשה לכתוב, ואפס הפרות scope/export. ‏Edge:
> ‏`interpret-document` ‏16→**17**, ‏ACTIVE, ‏`verify_jwt=false` נשמר. ‏Frontend: נבנה מ־worktree
> מבודד על `origin/main` ‏`ea88b9d`; החבילה מדברת רק עם `rkftlbctohswhbbiaqin.supabase.co`, אין
> ‏`service_role`, ‏`DEMO_PASSWORD_SEED` או טוקן ניהול (מופעי `localhost` היחידים הם קבועי
> ‏supabase-js ו־html2canvas). ‏`index-B3ZQQLge.js` ‏(845,346 בתים, ‏sha256 `05E8FECC…`) זהה
> בייטים בכתובת הקנונית, בפריסה `c575973f` ובבנייה המקומית, וכך גם `DocumentReview-OoAjj73Y.js`
> ‏(116,076 בתים). ‏`/`, ‏`/login` ו־`/prices` החזירו 200 ב־UA של מובייל.
>
> **הראיה החיה.** במסמך האמיתי המסך מציג „333 שורות נקראו עם מק״ט, שם ומחיר, אך המוצר שלהן אינו
> בקטלוג”, כפתור „יצירת 333 מוצרים חדשים”, והמשפט על 5 השורות שיישארו לטיפול פרטני; העימוד עבר
> לעמוד 2 והציג „שורות 51–100 מתוך 338 · עמוד 2 מתוך 7”. אפס שגיאות קונסול של האפליקציה.
>
> **מה טרם הוכח חי, במפורש:** ‏(א) המונה של שלב הפירוש — אחרי הפריסה לא רץ פירוש, ולכן הראיה
> היא שכבה־שכבה (self-check של `0143`, ההרשאות בייצור, הפונקציה החדשה, ו־Edge v17), והמדידה
> החיה תתקבל בהעלאה הבאה. ‏(ב) יצירת 333 המוצרים **לא בוצעה** — זו פעולה עסקית של הבעלים, והמסך
> ממתין לו.

> **שוחרר לייצור 17.08.2026 — קליטת מחירון בלחיצה אחת (frontend + worker; ללא מיגרציה).**
> ‏Production נשאר ב־**`0141`**: השחרור הזה אינו נוגע ב־DB ואינו נוגע ב־Edge, ולכן לפי מטריצת
> ה־rollout רצו רק המשטחים שהשתנו — bundle ל־Pages, ו־image של ה־worker.
>
> **מה תוקן ולמה זה היה שבור.** מסך אישור המחירון דרש אישור **שורה־שורה** — בחירת מוצר והקלדת
> מחיר בכל שורה — על מסמך שבו `run_price_list_shadow` (`0096`) כבר התאים כל שורה עם מק״ט או ברקוד
> ורשם את המחיר שנקרא. במחירון אמיתי של 338 שורות זה 338 הכרעות שמשחזרות עבודה שהשרת עשה, וזה גם
> גרם לאוטומציה להיראות כאילו לא רצה כלל. עכשיו התחזית נקראת ל־snapshot וממלאת את הטיוטות: כפתור
> ראשי אחד שנושא את מספר השורות שהותאמו, הרשת פריט־פריט מקופלת מאחורי „פרטים נוספים”, והשורות
> שהשרת לא הצליח להתאים נפתחות לבד עם הסיבה לכל אחת. שורה מתמלאת מראש **רק** כשהכרעת השרת היא
> `apply_existing_price`, המוצר עדיין בקטלוג שנטען, והמחיר נפרס כמספר חיובי; כל השאר נשאר
> בלתי־מאושר וגלוי. ‏`submit-price-list` לא נגע — אותן שורות מאושרות, אותו audit, אותה
> אידמפוטנטיות. חודש היעד מקבל ברירת מחדל של החודש הנוכחי (`OPEN-DECISIONS #150`) ואינו נגזר
> מתאריך המסמך. בנוסף, מונה העמודים שה־worker מדווח מגיע עכשיו לכל מסך שמציג `DocumentStatusBadge`
> ולא רק לרצועה שבמסך הבדיקה; NULL נשאר NULL ואין „עמוד 0 מתוך 0”.
>
> **תיקון כיתוב שנולד מהצילום.** צילום ה־CI של המסך החדש חשף שני משפטים שקריים על אותו מסמך: התג
> אמר „הקליטה בעיבוד” על מסמך שקריאתו הסתיימה והקליטה ממתינה לאדם, ומעליו משפט שמתאר קליטה
> אוטומטית שלא רצה. שניהם מותנים כעת בקיום הכרעה אמיתית, והתג קורא „ממתין לאישורך”.
>
> **מה נפרס בפועל.** ‏PR #47 ו־#49 (frontend) ו־PR #50 (worker — ספירת עמודים מול המסמך ולא מול
> ה־batch, שזה בדיוק המספר שה־badge מציג). ‏Pages נפרס מ־worktree מבודד על `origin/main`
> ‏`59fcb63`, לא מעץ העבודה המשותף. השערים בפריסה: החבילה מדברת רק עם
> ‏`https://rkftlbctohswhbbiaqin.supabase.co`, אין `VITE_DEMO_PASSWORD_SEED`, וה־chunk שנשלח נושא
> את ארבעת ה־test ids החדשים ואת תג „ממתין לאישורך”. הכתובת הקנונית והפריסה הייחודית
> ‏`30a458a2` מגישות אותם entry scripts, ו־`/` ו־`/login` החזירו 200 בדסקטופ ובמובייל.
> אימות סופי במדידה: ‏`DocumentReview-BqCJul9g.js` באתר החי זהה בייט־לבייט לקובץ שנבנה
> (`sha256 23C81CD3…`, ‏110,524 בתים). ה־pool רץ על `supplyflow-ocr-worker:59fcb63`, שני replicas,
> ‏900 שניות, concurrency 3.
>
> **מה טרם הוכח חי, במפורש:** לא בוצע smoke מחובר למסך הזה בייצור. ההוכחה להתנהגות המסך היא
> תרחיש הדפדפן `ocr-price-list-manual` על אותו commit ב־CI, עם צילומים ב־1440 וב־390 בארטיפקט
> ‏`browser-evidence`, ובייצור — זהות הבייטים של ה־chunk. ‏**ספירת עמודים חיה** עדיין תלויה
> בהעלאת סריקה אמיתית, כמו שנרשם בשחרור `0141`.
>
> **מה נשאר חסום בכוונה:** שער הכיול של `0096` לא נגע — קליטה אוטומטית מלאה ללא אדם עדיין
> בלתי־אפשרית מהמוצר, והפער מתועד ב־`DEBT-REGISTER §42` יחד עם ההכרעה הפתוחה מי מכשיר scope.
> ‏RLS של `0096` נותן קריאה לטבלאות ה־shadow ל־`owner` בלבד, ולכן `office` אינו מקבל מילוי מראש.

> **שוחרר לייצור 17.08.2026 — `0141` וטיפול בכשלי עיבוד המסמכים.** ‏Production נמצא כעת ב־**`0141`**.
> מה נפרס: המיגרציה `0141` (גיבוי 166 טבלאות / 25,623 שורות ללא כשל, dry-run בטרנזקציה מגולגלת,
> החלה forward-only, probe ו־postflight — כל הספירות העסקיות זהות לפני ואחרי), ‏`interpret-document`
> ל־v16, ‏`document-processing` ל־v8, ו־frontend ל־Cloudflare Pages עם hash parity זהה בכתובת
> הקנונית ובכתובת הייחודית `60870e80` (`index-BIBNHhLA.js`). מאגר ה־workers הועלה לשניים על
> ‏`scripts/run-ocr-worker.ps1` עם image של HEAD, ‏900 שניות ו־concurrency 3.
>
> **הראיה החיה:** המחירון שנכשל `provider_timeout` שלוש פעמים (8 עמודים, 17,135 תווים) עובד עכשיו.
> הרצת `עיבוד מחדש` באתר החי הגיעה ל־`review` ב־**99 שניות בניסיון אחד**, והפיקה **338 שורות מחיר**
> במקום אפס. ‏`usage.split` מתעד `[{1–4},{5–8}]` — בדיוק שני החלקים שהבדיקות חזו — שרצו במקביל
> ב־75.1 שניות, ואיחוד ה־`packet_segments` שמר על `price_list` ולא הפיל ל־`other`. שימו לב:
> ‏75.1 שניות חורגות מתקרת 45 הישנה, כלומר **שני** התיקונים היו נחוצים ולא אחד מהם.
> רצועת השלבים אומתה חיה: מסמך שנכשל מסמן „פירוש הנתונים — השלב שנעצר”, ‏`pageOverflow=0`
> ב־1440 וב־390, אפס שגיאות console.
>
> **מה טרם הוכח חי:** ספירת עמודים אמיתית בבר. המסמך שנבדק הוא PDF טקסטואלי ולכן אינו מריץ עמודי
> OCR, והמסמך הסרוק כבר ב־`review` ולכן אין לו „עיבוד מחדש”. השרשרת מאומתת בכל שכבה בנפרד
> (self_check של ה־worker, חוזה ה־gateway, self-check של `0141`, ההרשאות בייצור, ו־NULL שמוצג
> כ„טרם דווח”), אבל ספירה חיה תתקבל רק בהעלאת סריקה אמיתית הבאה. המנגנון אדיטיבי: NULL מייצר
> „טרם דווח” ולא בר.

> **תיקון מצב סמכותי (17.08.2026):** ‏Production נמצא ב־**`0140`**. נמדד ישירות:
> ‏`select max(version) from supabase_migrations.schema_migrations` מחזיר `0140`, ו־`document_packets`,
> ‏`document_scan_outputs` ו־`document_processing_jobs.scan_output_id` קיימים שם. כל ניסוח בהמשך
> המסמך שלפיו `0138`–`0140` "טרם נפרסו" הוא היסטורי ושגוי כרגע. המועמד הפעיל היחיד הוא `0141`.

> **המצב הסמכותי הנוכחי:** `main` ו־Production נמצאים ב־`0137`; המיגרציות `0134`–`0137`
> והחשבונית המרכזת כבר שוחררו. כל ניסוח ישן בהמשך המסמך שלפיו הן "טרם נפרסו" היסטורי ואינו
> משקף את ה־ledger החי. העבודה הפעילה היא תיקון סריקת התמונות ב־`0138_document_scan_recovery.sql`:
> הגנת זיכרון לעובד, דחיית מלבן פנימי, successor בלתי־משנה לתיקון גבולות, מעבר ישיר מצילום יחיד
> למסך הבדיקה, הבחנה מפורשת בין סריקה לחילוץ ורענון מצב ארגון בעת חזרה לטאב. המועמד טרם נפרס;
> Production נשאר ב־`0137` עד ש־CI נקי, גיבוי, migration dry-run, פריסת Worker ו־smoke ב־428px יעברו.

> **מועמד לוקאלי `0139` — טרם נפרס:** בדיקת מסמכי הלקוח הראשון הוכיחה שני כשלים:
> תאריך `31.07.26` לא התקבל במרכזת, ו־auto-crop/שחור־לבן מחקו כותרת ושדות במסמך
> low-resolution. המועמד מקבל שנה דו־ספרתית רק מול חודש היעד, מעביר חוסרי OCR ל־
> `needs_review`, מפריד עמודי invoice ממסמכי משלוח, ובוחר job/interpretation עדכניים בלבד.
> ה־scanner שומר full-frame grayscale raw במסמך קטן. לאחר התיקון עבר rebuild מקומי מלא
> מ־`0001` ועד `0139`, ואחריו עברו שוב P15, ‏P46, בדיקות scanner ‏10/10, ‏Vitest מלא
> ‏751/751, כל `verify`, ‏TypeScript ו־production build. בדיקת UI דסקטופ/390px שנעשתה
> אחרי תיקון המוצר עברה גם היא.
> שורש חסימת ה־reset היה CRLF מקומי מול anchor שמנרמל CR בתוך `0137`; חוזה
> `.gitattributes` מכריח LF לכל migrations בלי לשנות את ה־SQL העסקי שכבר יושם.

> **מועמד לוקאלי `0140` — טרם נפרס:** PDF אחד יכול כעת להכיל כמה סוגי מסמכים. המפרש מחזיר
> manifest מלא ורציף של טווחי עמודים; מסמך האב נשמר כמקור בלבד, וכל טווח מאושר נוצר כ־PDF בן
> עם job עצמאי. הפיצול האוטומטי כבוי לכל ארגון קיים, דורש policy מפורש, קובץ לא־חלקי, עד 20
> עמודי OCR וביטחון של `0.900` לפחות בכל חלק. קובץ חלקי, ארוך או מתחת לסף עובר לאישור בעלים/משרד.
> ה־Worker מקבץ עמודי PDF לפי מגבלת זיכרון ותומך עד 100 עמודים; עמוד יחיד שחורג מהגבול עדיין
> נכשל בבטחה. במסך הבדיקה נוספו תיקון/אישור הפיצול, קישורים למסמכי־הבת ו־reprocess למסמך שנכשל;
> במחירון השורות נפתחות לבדיקה, מוצג מונה בחירה, חודש היעד מוסבר והערת הביקורת רשות. ‏P47,
> סוויטות המסמכים הרלוונטיות, ‏Deno, ‏Docker, ‏Vitest, ‏`verify` ו־build עברו לוקאלית; אין בכך
> טענת CI או Production עד PR, שערים טריים ו־rollout מלא.

> **מועמד לוקאלי `0141` — טרם נפרס:** דיווח מהאתר החי על מסמכים שנכשלים ועל המתנה של מעל 8 דקות
> נחקר מול ה־DB של הייצור. ההעלאה **אינה** שבורה — אפס מסמכים ב־7 ימים בלי ג'וב. שלושה ממצאים:
> ‏(א) הפירוש רץ בתקרה של 45 שניות בעוד מסמך של 22–79 שורות נמדד ב־33.9–45.1 שניות, ושתי רשומות
> עומדות בדיוק על הקיר; התקרה עלתה ל־90 והמעטפת נאכפת עכשיו על הקריאה כולה
> (`PROVIDER_TOTAL_BUDGET_MS`) במקום בכפל שגוי של תקרת־הניסיון. (ב) כשלי `processing_timeout`
> נמדדו ב־305 שניות עם 3 ניסיונות — ה־image שרץ בייצור היה ישן מ־`main`; הוא הוחלף ל־HEAD ואומת
> ב־ג'וב שרץ 370 שניות והצליח. (ג) המתנה בתור של 523–746 שניות היא מקור תלונת „8 דקות”; הועלה
> ל־2 workers ונוסף `scripts/run-ocr-worker.ps1` — הדרך היחידה שמתועדת ב־Git להפעיל את ה־pool.
> ‏`0141` מוסיף `progress_done`/`progress_total`/`progress_attempt_id` על הג'וב, RPC כתיבה
> ל־service_role, ומחזיר את הספירה ב־`get_document_processing_statuses` רק עבור הניסיון החי.
> ה־worker מדווח עמודים דרך ה־heartbeat הקיים, ומסך הבדיקה מציג רצועת ארבעה שלבים שמפרידה
> „ממתין בתור” מ„קריאת המסמך” עם בר דטרמיניסטי — ורק כשיש ספירה אמיתית.
> ‏`interpret-document/split.ts` מוסיף פיצול: מעל 12,000 תווים המסמך נחתך לטווחי עמודים של
> ‏~9,000 תווים, עד 4 קריאות **במקביל** (המעטפת חוסמת זמן־קיר), ונמזג חזרה — כולל איחוד
> ‏`packet_segments` סמוכים כדי שמחירון ארוך יישאר `price_list` ולא ייפול ל־`other`. הסף נגזר
> משבע מדידות ייצור ורשום ב־§40; כל פירוש מפוצל נושא `usage.split` לצורך כיול חוזר.

> **תוכנית פעילה — חשבונית מרכזת ספק–חודש (13.08.2026):** כל עבודה נעשית רק לפי
> `D:\משה פרוייקטים\פיתוח אתרים\NIR-APP-WORKTREES\consolidated-supplier-invoice-20260813\docs\PLAN-consolidated-supplier-invoice-20260813.md`.
> אישור בעלים מ־14.08.2026 מרחיב את התוכנית למיזוג ו־Production. אין `npm run quality`
> מקומית; משתמשים ב־CI המסווג לפי משטח. אין rerun לאותו SHA, ומועמד מתוקן מקבל SHA ושערים חדשים.

## חשבונית מרכזת ספק–חודש — מועמד מתוקן לפני PR; לא נפרסה

הענף `codex/consolidated-supplier-invoice-20260813` מסונכרן עם `origin/main` מ־14.08.2026
ומשלב את מיגרציות
`0134`–`0135` של הסרת Trial ובקרת מסמכים. ‏`0137` מוסיפה תיק אחד לכל ארגון–ישות–ספק–חודש,
עוגן `payable` יחיד, מקורות `supporting_evidence`, קליטה רב־עמודית, snapshots ורוויזיות מאוחרות.
ה־Edge מנתב לפי claim סמכותי מן ה־DB והמסלול `/documents/consolidated-invoices` מרכז צילום,
העלאה ושלושת ערוצי ההתאמה. המסלול מופיע בניווט של שלושת התפקידים; רק `owner` ו־`office`
יכולים לקלוט או לרענן, ו־`accountant` קורא בלבד. קוראי החיפוש, הכפילויות והחשבוניות ללא הזמנה
מסננים גם הם ל־`payable`, כך שראיה תומכת אינה מוצגת כחשבונית רגילה. מצב השערים שלאחר ה־freeze
נרשם ב־ledger החיצוני שבתוכנית. build ראשון נכשל ב־TypeScript ותועד; מועמד מתוקן יקבל SHA
ושערים טריים. עד שה־CI, המיגרציות, הפריסה וה־smoke עוברים אין טענת runtime סופית.

## מצב המוצר

- חשבונות המוצר הפעילים הם `owner`, ‏`office` ו־`accountant` בלבד.
- `kitchen`, ‏`payer` ו־`supplier` נשמרים כערכי enum, פרופילים לא־פעילים, קשרים היסטוריים
  ו־audit. אי אפשר להזמין, להפעיל מחדש או להתחבר כתפקידים אלה.
- `supplier` כישות עסקית נשאר חלק מרכזי במוצר: ספקים, מחירונים, הזמנות, יתרות ומסמכים.
- רואה החשבון מבצע תשלום מאושר ב־`/pay`; הנתיב לא השתנה. מסך הקוד הוא
  `AccountantPaymentQueue.tsx`.
- `owner` ו־`office` ממשיכים רכש, קבלה, מסמכים ומחירונים. `accountant` ממשיך קריאה פיננסית,
  ביצוע תשלום והעלאת אסמכתה.

## הסרת הפרסונות הישנות — שוחררה ואומתה

`0133_remove_retired_persona_surfaces.sql` היא מיגרציה forward-only שמסירה את
`supplier_portal_context()`, פוליסות ו־helpers ששירתו התחברות ספק, ומצרה RPCs, מחירונים,
מסמכים, Storage ומעברי הזמנה למשטח הפעיל. חריגי `0127` ו־`0132`, enum values, היסטוריה עסקית
ו־audit נשמרים.

למיגרציה יש self-check שמכשיל אותה אם policy, view או RPC נגישים ל־`authenticated` עדיין
מכילים תפקיד שפרש מחוץ לחריגים ההיסטוריים המפורשים. `p43_active_persona_surface.sql` מוכיחה את
הגבול החדש ואת המשך העבודה של שלושת התפקידים.

במקביל הוסרו מסכי Kitchen/Supplier, ‏`/my-prices`, רכיב הזמנות ספק, תשתית `qa/` הישנה,
סקריפטים ותלויות ללא צרכן. `ActiveRole` משמש את קוד המוצר; `Role` הרחב נשאר רק בגבולות DB,
נתונים היסטוריים ותצוגת audit. ‏Knip מוצמד ונכלל ב־`npm run verify` כ־`check:dead-code`.

השינוי שוחרר לייצור ב־13.08.2026 מתוך
`eea20c84cb5c774365aa1cc60bfa64546029fefb`. לפני המיגרציה נשמר גיבוי schema/data/roles;
`0133` היא ראש ה־ledger ב־Supabase Production, ו־`private.scope_enforcement_violations()`
מחזירה אפס. ה־frontend נפרס ל־Cloudflare Pages בכתובת הקנונית
`https://supplyflow-baq.pages.dev` ובפריסה הייחודית
`https://0b5f7c57.supplyflow-baq.pages.dev`; כל נכסי `dist` תואמים בשתי הכתובות.

## השינויים הממתינים לשחרור

**אין. הרולאאוט המלא בוצע ב-25.08.2026 מתוך `690cd5b`, בהרשאת בעלים מפורשת.** ‏Production ledger
עומד על **`0209` עם 204 שורות**; ‏22 פונקציות Edge נפרסו; ‏Pages מגיש את הבנייה החדשה, ו-hash
parity אומת בשלוש הכתובות — הייחודית `0c44c146`, הקנונית `supplyflow-baq.pages.dev`
ו-`app.inplace.digital`.

> **מה שהכריע את הסדר, וזה לא היה נוח.** ניסיון קודם להחיל `0171`–`0205` **נכשל בייצור ב-`0181`**
> (`ERROR: P0001: 0181: platform scope lock anchor moved`). העוגן לא זז: ‏58.8% מגופי הפונקציות
> בייצור אחסנו `\r\n`, ו-`0181` בונה עוגן רב-שורתי עם `e'\n'` — חיפוש שלא יכול היה להתאים לעולם.
> קריאה חיה לפני הרולאאוט מדדה **302 גופים עם CR**. לכן `0209` הוחלה **ראשונה**, מחוץ לסדר
> המספרי: הכותרת שלה אומרת שתפקידה לנרמל *את מה שכבר קיים*, והחלתה אחרונה הייתה הופכת אותה
> ל-no-op בדיוק מול הכשלים שהיא נועדה למנוע. אחרי `0209`: ‏**CRLF = 0**, ואז `0171`→`0208` עברו
> כולן, כולל `0181`.
>
> **מה שנמדד לפני ואחרי.** פונקציות ‏515→630, טבלאות ‏129→140, ‏policies ‏180→181, ‏definer
> ‏386→475. ‏`scope_enforcement_violations()` = **0** לפני ואחרי. כל ספירות הטבלאות העסקיות
> **זהות** — ספקים, מוצרים, הזמנות, חשבוניות, תשלומים, הקצאות, זיכויים, מחירים: אף שורה עסקית
> לא נגעה. ‏`audit_logs` עלה ב-**1** בלבד. נקודת השחזור: גיבוי פיזי `1470000028`, ‏22:44:35Z,
> כלומר **לפני** הכתיבה הראשונה. ‏PITR כבוי בפרויקט; הגיבוי היומי הוא נקודת החזרה היחידה.
>
> **‏`p0_invoice_balance_rows` ו-`webhook_subscriptions` נבדקו ונמצאו קיימים.** בקריאה הראשונה
> הם דיווחו „חסר" — **הבדיקה שלי נקבה בשם הלא-נכון** (סכימה `private` במקום `public`, ושם טבלה
> משוער), לא הסכימה שחסרה.

**מצב deployed אינו משנה את גבול ההפעלה.** ‏Assistant, שליחת מייל חיצונית ו־WhatsApp נשארים
כבויים/לא־מוכחים עד ההכרעות, ההגדרות וה־smoke החיצוני הנפרדים המתועדים בחוב ובהחלטות. פריסת
פונקציית `assistant` **אינה הפעלה**: שלושת הדגלים נולדים כבויים ו-`assertProviderGovernance`
ממשיך לסרב כל עוד `dpa` אינה `VERIFIED`. באותה מידה, ‏`billing-webhook`, ‏`email-webhook`
ו-`whatsapp-webhook` נפרסו כנקודות קצה **fail-closed**: בלי הסוד החתום שלהן הן דוחות לפני
פענוח הגוף, ופריסתן אינה טענה שהאינטגרציה מחוברת.

> **פגם גלוי שנמצא בצילום שלאחר הפריסה, ולא תוקן כאן.** מסך הכניסה מציג „המשך עם Google".
> הכפתור הוא `aria-disabled="true"` עם `title="חיבור Google יתווסף בהמשך"` ו**ללא `onClick`** —
> מציין-מקום מסומן, לא כפתור שבור: לחיצה אינה עושה דבר, וטכנולוגיה מסייעת מקבלת „מושבת".
> ‏`external_google_enabled=false` בייצור, כך שגם קריאה לא הייתה מצליחה. ‏`Signup.tsx` **כן**
> מגודר ב-`VITE_GOOGLE_SIGNUP_ENABLED`; ‏`Login.tsx` אינו מגודר כלל. התיקון נמצא בענף פעיל של
> סוכן מקביל (`feat/oauth-google-apple`) ואינו שייך לרולאאוט הזה.

## אימות קוד

`npm run build` מבצע typecheck ובניית bundle בלבד. `npm run verify` מריץ את השומרים הסטטיים ואת
כל בדיקות היחידה תחת Vitest אחד. `npm run check` מריץ את שניהם ברצף לשימוש מקומי.

אין להצמיד למסמכים ספירות של בדיקות, סוויטות או תרחישים; הפלט של ה־runner הוא מקור האמת.
שער האינטגרציה `npm run quality` רץ ב־CI בלבד, וה־jobs הכבדים בו מסוננים לפי הנתיבים שהשתנו.
גם `build` ו־`verify` מסווגים לפי צרכן: test בלבד מריץ Knip+Vitest ללא build/guards/browser;
migration בלבד מריץ את guard ה־exemptions ו־SQL ללא התקנת תלויות, Vitest או browser; ‏Edge
אינו מפעיל OCR; ושינוי package מפעיל dependency audit בלי להקים DB. מטריצת ה־rollout המחייבת
נמצאת ב־`CLAUDE.md`.
חריג fail-closed: migration שנוגע ב־RLS/Auth/role/grants כן מפעיל את שער הדפדפן גם ללא שינוי `src`.

## אימות השחרור החי

- הנתיבים הציבוריים `/`, ‏`/login`, ‏`/forgot-password`, ‏`/reset-password`, ‏`/terms`
  ו־`/privacy` אומתו בשתי כתובות הפריסה בדסקטופ ובמובייל: HTTP תקין, RTL, ללא overflow
  וללא שגיאות console/page/HTTP.
- בכתובת הקנונית בוצע smoke מחובר וקריאה בלבד ל־`owner`, ‏`office` ו־`accountant`.
  כל תפקיד הגיע למסך הבית ולמסך המורשה שלו; נתיבים שאינם מורשים והנתיב שפרש
  `/my-prices` הוחזרו למרכז הבקרה. צילומי דסקטופ ומובייל אומתו ויזואלית.
- ספירות הטבלאות העסקיות, אובייקטי Storage ו־`audit_logs` זהות לפני ואחרי ה־smoke;
  לא בוצעה כתיבה עסקית. רק שלושת התפקידים הפעילים ניתנים להתחברות, ומשטחי
  `kitchen`/`payer`/`supplier` שפרשו אינם פעילים בסביבה החיה.

## עבודה פתוחה

החוב והמגבלות הפעילים נמצאים רק ב־`docs/DEBT-REGISTER.md`. הכרעות עסקיות נמצאות רק ב־
`docs/OPEN-DECISIONS.md`. אין לפתוח תכנית קמפיין חדשה בתוך הריפו לצורך המשך העבודה.
