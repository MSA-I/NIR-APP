# DEBT-REGISTER — כל מה שנדחה, בכוונה, במקום אחד

> **למה הקובץ הזה קיים.** קמפיין יסודות ה-Enterprise (גלים 0–10) דחה דברים אמיתיים, וכל דחייה
> תועדה — אבל **פוזרה** בין `PROGRESS.md`, ‏`OPEN-DECISIONS.md`, מסמכי תכנון, מסמכי handoff והערות
> בקוד. הביקורת הסופית (`Plans/10-FINAL-AUDIT.md`, ‏Finding 8) מצאה שאף דחייה אינה מתועדת במקום
> אחד בלבד ואף אחת אינה מוצהרת כעובדת — אבל שתיים תועדו **דק**, ובאחת יש סתירה בטון. זהו התיקון:
> טבלה אחת שסוכן עתידי מוצא **ראשון**, לא אחרון.
>
> **הקובץ הזה אינו מכריע דבר.** כל שורה כאן היא חוב מוצהר או מגבלה מדודה. שאלה עסקית פתוחה
> יושבת ב-`OPEN-DECISIONS.md` ורק שם; המספרים כאן מפנים לשם.

**איך קוראים שורה:** *מה* (העובדה, בלי ריכוך) · *למה נדחה* (ההיגיון בזמנו) · *איפה ההוכחה*
(קובץ:שורה, סוויטה או מדידה — לא זיכרון) · *הצעד הזול הבא* (המימוש הקטן ביותר שסוגר את זה).

**לפני שמסמנים שורה כסגורה:** מוסיפים/מעדכנים טענה בשער. שורה שנסגרה בלי הוכחה מוחזרת לכאן.

**היקף:** ‏§1–§15 הן חוב קמפיין יסודות ה-Enterprise (גלים 0–10). ‏**§16–§21 הן מגל 11** (אוטונומיית
המסמכים והניווט, ‏07.08.2026) — הקובץ אינו נעול לקמפיין אחד, וכל גל שמשאיר משהו פתוח מוסיף כאן שורה.
‏**§22 היא מ-07.08 אחה"צ**, מהסבב שהוריד את פנימיות ה-OCR מהמסך ובנה למתג האוטונומיה את הקורא
הראשון שלו. ‏**§24 היא מקליטת המחירונים האוטומטית באותו ערב.** ‏**§25 היא מחבילה 1 של קמפיין "מוצר מוגמר"
(‏09.08.2026) — מגבלת מסירת המייל בהיעדר דומיין מאומת. ‏§26 היא מחבילה 5 — האימות החי של שלושת
התפקידים, שנשאר פתוח כי הוא דורש חשבונות פרודקשן שאין לסוכן.** ‏**§27–§28 הן מקליטת תעודות
המשלוח (‏`0090`), ו-§29–§31 הגיעו עם נרמול קמפיין ה-codex ב-10.08.2026** (הן היו §25–§27 בענף
הקמפיין; מוספרו מחדש כי `main` כבר החזיקה §25–§28 משלה).

## מצב עדכני — קמפיין 09.08.2026

הטבלה הזאת גוברת על ניסוחי ההיסטוריה שבהמשך. סעיף שסומן "נסגר" נשמר למטה כדי להסביר מדוע
נבנה, אך אינו משימה עתידית. הצלחה בייצור עדיין תלויה בשערי השחרור המתועדים בדוח הקמפיין.

| סעיף | מצב נוכחי | ראיה |
|---|---|---|
| §1 אגרגציית dashboard | **צומצם, לא נסגר כולו.** מדדי ההנהלה שהוכרעו עברו ל־snapshot שרתי אחד; גרפים ורשימות מפורטים עדיין כוללים קריאות נפרדות | `0100`, `p21_dashboard_snapshot.sql` |
| §2 app shell offline | **נסגר בקוד.** Service Worker שומר רק HTML/assets מאותו origin; API ונתונים פיננסיים אינם נשמרים | `public/sw.js`, `serviceWorkerCachePolicy.spec.ts` |
| §4 תמונות offline | **נסגר בקוד.** צילום נכתב כ־Blob ל־`pending_photos`, נשמר בסגירה, עולה לאחר חיבור ומוחק מקומית רק אחרי הצלחה | `FileUpload.tsx`, `offlinePhotoQueue.spec.ts` |
| §5 הוכחת webhook חיצוני | **DEFERRED בהחלטת בעלים.** אין יעד או credentials מאושרים; אין להמציא אותם. בדיקות outbox/signing/retry מקומיות נשארות חובה | `OPEN-DECISIONS` #137 |
| §7 marker של A5 | **הפרצה נסגרה; הרשם לא רוקן.** parser מסיר comments/literals, דורש קריאה executable ומצמיד body hash והוכחת scope | `0095` |
| §24 כיול מחירון | **צומצם, עדיין פתוח נתונית.** קיימים shadow ledger, corpus review, metrics ו־rollback; 50 מחירונים אמיתיים טרם נבדקו ולכן הסף נשאר לא־מכויל | `0096`, `p18_document_automation_calibration.sql` |

> **‏§18 השתנתה באותו סבב.** ההכרעה שהיא ביקשה — "האם מסך הסוקר מדבר בשפת המשתמש או בשפת
> ה-pipeline" — **נפלה: שפת המשתמש.** ‏`DocumentReviewWorkspace` אינו מציג עוד קטעי טקסט, סימונים,
> דרגות זיהוי, קואורדינטות או תיבות bbox מעל המסמך. שלושת המשטחים היומיומיים שהיא מונה
> (‏`AttachmentsPanel`, ‏`FileUpload`, ‏`UploadCenter`) **עדיין פתוחים.**

---

## 1. חוב אגרגציית הדשבורד — צומצם ב־0100, היסטוריית המקור נשמרת

| | |
|---|---|
| **מה היה** | לפני הקמפיין חמשת מסכי הדשבורד ביצעו 34 קריאות `fetchAll`. זו מדידה היסטורית, לא ספירת המצב הנוכחי. |
| **מה נסגר** | `management_dashboard_snapshot()` מרכז את מדדי ההנהלה שהוגדרו: `unmatched` בנפרד מ־`suggested`, שתי שורות אישור, ו־overdue רק כשקיים `due_date`; הוא שומר null כ־unknown. |
| **מה נשאר** | גרפים, רשימות מפורטות ודשבורדים תפקידיים אחרים עדיין נטענים בחלקם בנפרד. אין להעביר מדד נוסף לשרת לפני שהגדרתו הוכרעה. |
| **איפה ההוכחה** | `0100_management_dashboard_snapshot.sql` · `p21_dashboard_snapshot.sql` · `OPEN-DECISIONS` #135. |
| **מה זה כן ולא** | ‏`fetchAll` **אינו חותך בשקט** — הוא בלתי-חסום וזורק בכשל (`src/lib/supabasePaging.ts:26-38`). זהו חוב **השהיה**, לא חוב של מספר שגוי. אימות זה היה חלק מהביקורת, כי חסימה שם **הייתה** הופכת אותו למספר שגוי. |
| **הצעד הזול הבא** | למדוד מחדש round trips לאחר פריסה, ואז להעביר מסך אחד בכל פעם רק אם latency מצדיק זאת והגדרות המדדים סגורות. |

## 2. ‏precache של קונכיית האפליקציה (#101) — ✅ **נסגר 09.08.2026 (חבילה 5)**

> ‏`vite-plugin-pwa` ב-`injectManifest` על `public/sw.js` הקיים, כפי שהשורה הזאת עצמה ביקשה —
> ובלי workbox בכלל: הפלאגין רק ממלא `self.__WB_MANIFEST` בזמן build, והלוגיקה כתובה ביד.
> ‏push, ‏`skipWaiting`/`clients.claim` וחוזה ה-`controllerchange` נשמרו; ‏API לעולם לא נכנס
> ל-cache. הכותרת המילולית של sw.js **שונתה בכוונה** — כותרת שמכחישה את ה-cache שמתחתיה הייתה
> השקר הגרוע יותר; המסמכים שציטטו אותה מתארים את העולם שלפני חבילה 5 (‏`handoff/08` §5 עודכן).
> **טענה בשער:** תרחיש `an offline reload keeps the app shell` — ‏reload לא-מקוון אמיתי.

| | |
|---|---|
| **מה היה** | בגל 8 קונכיית האפליקציה לא נשמרה; התור ב־IndexedDB בלבד שרד. זו החלטה היסטורית שהוחלפה. |
| **מה נסגר** | `vite-plugin-pwa` מזריק manifest build-time ל־`public/sw.js`. ה־worker שומר HTML ונכסים immutable של אותו build; navigation נופל ל־shell האחרון, ו־Supabase/API לעולם אינם נכנסים ל־cache. אין Workbox runtime שמחליט אילו נתונים לשמור. |
| **איפה ההוכחה** | `vite.config.ts` · `public/sw.js` · `serviceWorkerCachePolicy.spec.ts` · תרחיש browser `an offline reload keeps the app shell`. |
| **מה נשאר** | שער browser אמיתי עם refresh כשהרשת מנותקת עבר ב־09.08.2026. לא נשאר חוב קמפיין בסעיף זה. |

## 3. ‏Workflow engine ו-report jobs (#103)

| | |
|---|---|
| **מה** | אין מנוע workflow ואין מנוע חוקים; אין report jobs (‏Edge + cron + XLSX בשרת). |
| **למה נדחה** | שש מכונות המצב עובדות ומבוקרות, ו-`docs/INTEGRATION-ARCHITECTURE.md:164-172` מבטיח **מתאם Temporal עתידי, לא מנוע**. הפער האמיתי היה מטריצה משוכפלת בדפדפן — נסגר ב-`read_allowed_transitions()` (#105). ל-"reporting" יש **מילה אחת** של מפרט בכל החבילה, ושלושה משטחי ייצוא נכונים כבר קיימים. |
| **איפה ההוכחה** | ‏`OPEN-DECISIONS.md` #103 (7 מקומות בשני הריפו — הדחייה המתועדת ביותר) · ‏`supabase/tests/p9_five_domains.sql` מריץ את ארבע הפקודות האמיתיות על כל זוג סטטוסים. |
| **הצעד הזול הבא** | תוכנית שמנמקת **צרכן אמיתי** לכל תחום שנדחה. **לא** למלא מספר מיגרציה פנוי. |

## 4. ‏`pending_photos` — נסגר בקוד בקמפיין 09.08

| | |
|---|---|
| **מה היה** | החנות וה־API נוצרו בגל 8 ללא כותב. זו היסטוריה, לא המצב הנוכחי. |
| **מה נסגר** | `FileUpload` שומר צילום offline כ־Blob עם entity, שם, סוג מסמך ו־resume state; הסנכרון מחדש משתמש באותו מסלול upload/enqueue, משמר שגיאת resume ומסיר Blob רק אחרי הצלחה. |
| **איפה ההוכחה** | `src/components/FileUpload.tsx` · `src/lib/offlineDb.ts` · `src/components/offlinePhotoQueue.spec.ts`. |
| **מה נשאר** | תור הקבלה, סגירת tab וחיבור מחדש עברו בשער browser; bytes/resume/lease/CAS של תמונות עברו בבדיקות IndexedDB ממוקדות. הוכחת tus/resume מול Storage מנוהל נשארת חוב נפרד בדוח הקמפיין. |

## 5. הוכחת מסירת webhook — DEFERRED בהחלטת בעלים

| | |
|---|---|
| **מה** | הוכחת המסירה של ה-outbox היא **loopback**; מסירה אמיתית ל-URL חיצוני מוכחת רק בסביבה מנוהלת. |
| **למה נדחה** | לא אושר ספק, endpoint, tenant או credentials. המצאת יעד לצורכי proof אסורה. |
| **איפה ההוכחה** | `OPEN-DECISIONS` #137 · `docs/INTEGRATION-ARCHITECTURE.md`. |
| **הצעד הבא** | סבב נפרד אחרי בחירת יעד חיצוני אמיתי והרשאה מפורשת; עד אז רק בדיקות outbox/signing/retry/dead-letter מקומיות. |

## 6. אסימטריית tus — מקומי מול ענן

| | |
|---|---|
| **מה** | הוכחת ה-resume של העלאה גדולה נעשית מול **FileStore מקומי**. התנהגות הענן אינה זהה ואינה מוכחת. |
| **למה נדחה** | הוכחת resume אמיתית בענן דורשת חשבון וסביבה שהשער המקומי אינו מחזיק. |
| **איפה ההוכחה** | ‏`PROGRESS.md:158-159` · ‏`Plans/PLAN-07:51-52` · ‏`src/lib/tusUpload.ts:17-20`. **סתירה בטון, יחידה בקמפיין:** ‏`docs/ENTERPRISE-UI-PLAN.md:170` מנסח כקריטריון קבלה בלתי-מסויג — "ניתוק רשת באמצע העלאת 8MB מתחדש מאותה נקודה" — בלי לציין שההוכחה היא FileStore-מקומי בלבד. גם `handoff/6b-upload-contract.md` אינו מזכיר את האסימטריה. |
| **הצעד הזול הבא** | הערת-הסתייגות אחת ב-`ENTERPRISE-UI-PLAN.md:170` ושורה אחת ב-`handoff/6b-upload-contract.md`. **תיעוד, לא קוד.** |

## 7. רשם החריגים של שכבת ה-definer (#83) — **78 שורות היום** — ‏marker הוקשח; ריקון הרשם נשאר חוב

| | |
|---|---|
| **מה** | פונקציות SECURITY DEFINER הנוגעות בטבלה אכופה (לפי הפרדיקט של A5 עצמה, ‏`0057:321-350`) — **המפקד החי האחרון, 06.08.2026: ‏63 נוגעות · ‏7 מקיימות במסמן** (`auth_scopes` / `assert_unit_in_scope` בגוף) **· ‏56 מקיימות דרך שורת חריג**, ורשם בן **57** שורות. ‏**מאז לא רץ מפקד נוסף:** ‏`0077`, ‏`0080`, ‏`0081`, ‏`0084` ו-`0090` הוסיפו תשע פונקציות definer שנוגעות בטבלאות אכופות, ולכן המספרים הנגזרים־מהפנקסנות (ולא נמדדים־מחדש) הם **72 נוגעות · 7 במסמן · 65 בחריג**, רשם **67** — השורה ה-67, כמו ה-57, היא רישום-כנות: ‏`organization_row_write_guard` של `0092` אינו נוקב בשם טבלה אכופה ולכן A5 אינה רואה אותו, והוא נרשם בכל זאת. **‏67 הוא מוצמד ונגזר, לא נקרא חי** — ‏`p9_five_domains.sql:267` מצמיד 67 ו-`check:exemptions` מאשר את האריתמטיקה בכל `npm run build`; הקריאה החיה האחרונה של `count(*)` הייתה בשער שעבר ב-07.08, כשהרשם עמד על 59. שאר הפירוק בשורה הזאת הוא אריתמטיקה על טקסט המיגרציות, ומי שצריך אותו מדויק מריץ את שאילתת המפקד שוב. הסקירה הפותחת של הקמפיין קראה לדליפת הסקופ בשכבת ה-definer "הממצא בעל הערך הגבוה ביותר בכל הסקירה" (`Plans/01-ADVERSARIAL-REVIEW-FINDINGS.md:8-22`). **המספרים ההיסטוריים בשורה הזאת היו 60 / 2 / 59, והם נכונים לגל 1** — הפער אינו הידרדרות אלא עבודה: ‏`0073` ניקז שלוש פונקציות תשלום לבדיקות סקופ אמיתיות, ומספר הפונקציות שקוראות בפועל ל-`assert_unit_in_scope(` עלה מ-**1** (`save_saved_view` בלבד) ל-**6**. |
| **למה נדחה** | הצהרה-מחדש של ~6,000 שורות בגל אחד הייתה **מוקש silent-revert** גדול בהרבה מהדליפה עצמה. ההנחה המקורית ("רק 2 פונקציות + 28 RPCs") **הופרכה במדידה** — ‏`PROGRESS.md:296-299` אומר זאת בקול. |
| **איפה ההוכחה** | **נמדד 06.08.2026 מול `supabase_db_supplyflow-p0`** (שאילתת מפקד שמריצה את הפרדיקט של `private.scope_enforcement_violations()` עצמו, לא ספירה מהזיכרון): ‏`private.scope_definer_exemptions` = **57** שורות · ‏63 definers נוגעים · ‏7 מקיימים במסמן · ‏56 מכוסים בחריג · ‏**‏A5 violations = 0**. ‏**7 + 56 = 63; השורה ה-57 אינה מיותמת** — היא `execute_emergency_payment_request`, שנרשמה **בכוונה** ב-`0057:255-257` "for honesty and for the latch count", כי A5 אינה יכולה לראות wrapper שמאציל בלי לנקוב בטבלה בטקסט. **הרשם עומד היום על 67** — מוצמד ב-`p9_five_domains.sql:267` ונגזר מטקסט המיגרציות ע"י `scripts/check-exemption-pin.ts` (‏`pin 67 matches 8 migration(s) (+11 / -3 over 0057's seed of 59)`). הבריח: ‏`p1_preflight.sql` (`multi_unit_org_with_open_exemptions`) · ‏`OPEN-DECISIONS.md` #83. **המספר 59 שהופיע כאן קודם, וגם ב-`0070:107`, התיישן ולא עודכן** — שתי השורות נכתבו כשהוא היה נכון בפעם הראשונה; ואז הוא היה נכון שוב **מסיבה אחרת**, וזה בדיוק סוג הצירוף שגורם לקורא להסיק שהמספר לא זז. **והוא זז שוב:** ‏`0080`, ‏`0081` ו-`0084` הוסיפו ארבעה חריגים והעלו את הרשם ל-**63**, בעוד השורה הזאת נשארה על 59 עד ש-`check:exemptions` הוצלב מולה ב-09.08.2026 — **ואז שוב:** ‏`0090` הוסיף שלושה והעלה ל-**66**, והשורה הזאת נשארה על 63 עד סקירת הקוד של 10.08.2026. פעמיים אותו דפוס באותו שבוע: הרשם וההצמדה זזים בזמן אמת, והשורה הזאת מתעדכנת רק כשמצליבים אותה. |
| **אחת-עשרה התוספות שהתקבלו, וכולן בהחלטה ולא בסחף** | ‏59 (גל 9) **−3** (`0073:993` ניקז שלוש פונקציות תשלום) = 56, **+1** (`0075:464`) = 57, **+2** (`0077:1773`, ‏`0077:1793`) = 59, **+1** (`0080:122`) = 60, **+2** (`0081:1012`, ‏`0081:1021`) = 62, **+1** (`0084:246`) = 63, **+3** (`0090:982`, ‏`0090:991`, ‏`0090:1000`) = 66, **+1** (`0092:165`) = **67**. ‏**(א) ‏`rescue_document_from_archive(uuid,text)`** — הפיכתה ל-invoker מחייבת grant של UPDATE על `document_filings` ל-`authenticated`, וסקירת האבטחה של B1 הדגימה ב-probe שדפדפן היה יכול אז ל-PATCH את `reverted_at` ישירות בלי סיבה, ובכך לרוקן את דרישת-הסיבה המבנית שהכרעת הבעלים קבעה (#110). ‏**(ב) ‏`apply_document_interpretation(uuid,uuid,uuid)`** — היא רצה **בלי JWT בכלל** (ה-Edge Function קורא לה במפתח השירות), ולכן `auth_scopes()` ריקה וכל קריאה מסוננת-סקופ הייתה מחזירה אפס שורות ומכבה את שכבת ההחלטה **בשקט בזמן שהיא נראית בריאה** — אותה מחלקת כשל ש-C1 מצאה ב-`evaluate_autonomy_policy`. גבול הדייר מוצמד במקום זאת מפורשות: `org_id` של הפירוש ומפתחות זרים מרוכבים על כל שורה שהיא כותבת. ‏**(ג) ‏`revert_document_auto_action(uuid,text)`** — חצי-הביטול של אותה שכבה, ובדיוק במראה: גרסת invoker הייתה דורשת UPDATE ל-`authenticated` על `document_auto_actions`, ‏`document_filings` **וגם** `invoices.deleted_at`, כלומר PATCH ישיר שמבטל רשומה כספית שמכונה כתבה — בלי סיבה ובלי מחיקה רכה. לשלושתן אותה תרופה רשומה בשדה ה-`reason`: לסנן את `documents` על `auth_scopes()` ברגע שלמסמך יש `unit_id` בעל משמעות (מסמך ב-inbox הוא `unit_id` ‏NULL במכוון, ‏`0055:112`). ‏**(ד) ‏`p1b_submit_supplier_price_list_internal(...)`** (`0080:122`) — overload פנימי שאין לאף תפקיד `EXECUTE` עליו, ומקבל actor מפורש; כל מפתחותיו מרוכבים־דייר והוא מאציל לכותב של `0048` בלי לשנותו. ‏**(ה) ‏`private.claim_document_interpretation_jobs(integer,integer)`** (`0081:1012`) — אין `EXECUTE` לאף תפקיד ועוטף ה-cron הוא הקורא היחיד; כל מועמד גוזר `org_id` משרשרת job/document/extraction אחת בלתי־משתנה, והפונקציה מחזירה מזהה עבודה בלבד. ‏**(ו) ‏`apply_price_list_interpretation(uuid,uuid,uuid)`** (`0081:1021`) — אותה מחלקה של (ב): היא רצה תחת `service_role` בלי JWT, ולכן `auth_scopes()` ריקה. זהות הדייר מוצמדת ב-`org_id` של הפירוש ובמפתחות מרוכבים־דייר של מסמך, עבודה, actor, ספק והגשה — לפני כל כתיבת מחיר או מוצר. ‏**(ז) ‏`sync_document_kind_from_interpretation()`** (`0084:246`) — טריגר; ה-FK של הפירוש מצמיד מסמך וארגון, והוא מעדכן רק את שורת המסמך שהפעילה אותו. ‏**(ח) ‏`private.resolve_delivery_note_order(uuid,uuid,numeric,uuid[])`** (`0090:982`) — פנימית בלבד, אין `EXECUTE` לאף תפקיד; כל הזמנה מועמדת מסוננת ב-`org_id` וב-`supplier_id` שהפקודה גוזרת משורת הפירוש, והיא מחזירה מזהה הזמנה בלבד. ‏**(ט) ‏`apply_delivery_note_interpretation(uuid,uuid,uuid)`** (`0090:991`) — אותה מחלקה של (ב) ו-(ו): רצה תחת `service_role` בלי JWT, ‏`auth_scopes()` ריקה; זהות הדייר מוצמדת ב-`org_id` של הפירוש ובמפתחות מרוכבים־דייר של מסמך, עבודה, actor, ספק והזמנה לפני כתיבת טיוטת הקבלה. ‏**(י) ‏`revert_delivery_note_receipt(uuid,text)`** (`0090:1000`) — רצה כאדם המבטל (‏`auth_org`/`auth.uid`/`auth_role` נאכפים ראשונים) אך חייבת למחוק שורת `goods_receipts` ש-`0023:167-168` שלל מ-`authenticated`, ולכן הכתיבה אינה יכולה להיות invoker. **ארבע השורות של 0080–0084 נוספו לרשם ולהצמדה של `p9` בזמן אמת, אבל לא לכאן** — הרשם הוא ההנהלה החשבונאית הגלויה, וזו בדיוק הדרך שבה הוא מתיישן; שלוש השורות של `0090` חזרו על אותו דפוס ונסגרו כאן רק בסקירת 10.08.2026. ‏**(יא) ‏`private.organization_row_write_guard()`** (`0092:165`) — בריח הקריאה-בלבד של מחזור החיים (‏#15): טריגר על כל טבלת `org_id` שחייב להיות definer כדי לגבור גם על `service_role` ועל definers אחרים; קורא רק את `org_id` של שורת ה-NEW/OLD שהפעילה אותו, ורשום כרישום-כנות — A5 אינה מסוגלת לראות אותו. **הבריח שאוכף זאת:** ‏`supabase/tests/p9_five_domains.sql` — מיגרציה שמוסיפה חריג ולא מזיזה את ההצמדה **נכשלת**; זה קרה על ארבעה גלים רצופים, תמיד בדקה התשע-עשרה, ולכן גל 11 הוסיף את **`check:exemptions`** ל-`npm run build` (ראה §9). **פער ידוע בשורה הזאת:** ראיית ה-probe של B1 מצוטטת כאן ובהערת `p9` מהסקירה, בלי `קובץ:שורה` — הגל הבא לוקח אותה על אמון. סגירתו = הצמדת המיקום כשמאתרים אותו. |
| **שלוש הערות מבניות שהביקורת הוסיפה** (`Plans/10-FINAL-AUDIT.md` Finding 3) | ‏**(א) הבריח הוא `select`, לא אילוץ** — זרוע read-only בשער **מקומי וידני** בלי CI. בייצור **שום דבר אינו מעריך אותו**, ואין בסכימה דבר שמונע יחידה אחות שנייה בזמן שהרשם לא ריק. ‏**(ב) הגדר האמיתי נמצא במקום אחר, והוא חזק מהבריח:** ל-`org_units` יש ל-`authenticated` **SELECT בלבד**, חוקה אחת (`org_units_select`), והכותב היחיד הוא טריגר הזריעה `p0_seed_org_units` שאינו ניתן להרצה ע"י `authenticated` — יצירת יחידה אחות דורשת `service_role` או מיגרציה. **גדר אמיתית שאף אחד לא תיעד כ*הגדר*, וגל האפשור הרב-יחידתי יסיר אותה בכוונה.** ‏**(ג) בדיקת הסמן של A5 היא חיפוש-מחרוזת בגוף הפונקציה** (`0057:345-346`: ‏`prosrc !~ 'assert_unit_in_scope' and prosrc !~ 'auth_scopes'`) — ‏definer עתידי שגופו מכיל את ההערה `-- auth_scopes is not needed here` מקיים את A5 **באפס אכיפה, ובלי להופיע ברשם החריגים** שהוא ההנהלה החשבונאית הגלויה היחידה של הפונקציות הנדחות. לא מנוצל נכון למפקד האחרון (‏06.08 — **שבע** מסומנות, וכולן משתמשות ב-`auth_scopes()` ב-WHERE אמיתי; ‏"שתיים" שהופיע כאן היה המספר של גל 1 ולא עודכן), אך צורת ה-bypass-הבלתי-נראה חיה. **‏(ג) נסגרה בקמפיין:** ‏`0095` החליף את חיפוש-המחרוזת ב-**lexer** שמסיר comments ו-literals לפני הבדיקה, דורש קריאה **executable** (ולא אזכור בהערה), ומצליב body hash מול `private.scope_definer_enforcements`. ‏definer שגופו מכיל רק את ההערה `-- auth_scopes is not needed here` **נכשל** עכשיו. ‏(א) ו-(ב) נשארות פתוחות. |
| **הצעד הזול הבא** | לתעד את גדר ה-`org_units` **כגדר** (שורה ב-`ENTERPRISE-SECURITY-MODEL`) — ‏**הקשחת סמן A5 כבר בוצעה ב-`0095`**, ולכן מה שנשאר כאן הוא שורת התיעוד בלבד. ריקון הרשם עצמו = גל האפשור הרב-יחידתי, פונקציה-פונקציה, עם actor/tenant/unit/tables/reason/audit/proof לכל אחת. אין לחזור לחיפוש-מחרוזת או להצהרה-מחדש גורפת. |

## 8. ההוכחה המבנית "אף פקודה לא שונתה" מכסה פקודה אחת מתוך שמונה

| | |
|---|---|
| **מה** | ‏`supabase/tests/p5_domain_events.sql:58-62` מוכיח שאף פקודה אינה מזכירה `domain_events` — מול **`set_invoice_review_status` בלבד**. הטענה עצמה **נכונה היום** (בקטלוג החי רק ארבע פונקציות מזכירות `domain_events`, וכולן תשתית מישור-האירועים, אפס פקודות) — אבל ההנחה של ADR-0009 היא שגופי שמונה הפקודות חיים **רק ב-`pg_proc`** ולכן הם מוקשי silent-revert. גל עתידי שיזריק emit ישיר ל-`execute_payment_request` **עובר את השער הזה**. |
| **למה נדחה** | לא נדחה במפורש — זו **פרצה בכיסוי**, לא החלטה. נמצאה בביקורת (`Plans/10-FINAL-AUDIT.md` Finding 7). |
| **איפה ההוכחה** | ‏`supabase/tests/p5_domain_events.sql:58-62`. |
| **הצעד הזול הבא** | להכליל את הטענה מ-`proname = 'set_invoice_review_status'` לקבוצת שמונה הפקודות המבוקרות. **שינוי של שורה אחת.** |

## 9. בלוק ה-re-assert של A1/A3/A5 נאכף בזיכרון, וכבר פספס פעם אחת

| | |
|---|---|
| **מה** | ‏`0057:13-14` טוען שהטענות "מפילות את המיגרציה הזו — וכל מיגרציה עתידית". מבנית הן לא: הערבות היא בלוק `do $$ … raise exception … $$` שכל מחבר מיגרציה צריך **לזכור להדביק**. ‏13 מ-14 המיגרציות שאחריה מחזיקות אותו; **`0067` חסר** (יש לו re-assert — אבל על צורת האינדקס, לא על `private.scope_enforcement_violations()`). |
| **למה נדחה** | ‏`0067` יצר אינדקס חלקי ולא טבלה/חוקה/definer, והטענה המחווטת ב-`p3_org_scope.sql:294-296` בודקת את הסכימה הסופית — ולכן הפספוס **בלתי-מזיק במקרה הזה**. הממצא הוא **האינווריאנט**, לא המיגרציה: טענה מסוג "כל מיגרציה עתידית" שנאכפת בזיכרון כבר פגה פעם אחת, גל אחד אחרי שנכתבה. ‏`0071` (גל 10) משלם את החוב ומחזיק את הבלוק. |
| **איפה ההוכחה** | ‏`Plans/10-FINAL-AUDIT.md` Finding 10 · הרצף, **נספר מחדש 07.08.2026** על כל קובץ מעל `0057` (חיפוש `scope_enforcement_violations` בטקסט): ‏`0057 0058 0059 0060 0061 0062 0063 0064 0065 0066 __ 0068 0069 0070 0071 0073 0074 0075 0076 0077` — ‏**19 מתוך 20 מחזיקים, ו-`0067` עדיין הפער היחיד.** שלוש המיגרציות של גל 11 מחזיקות. |
| **הצעד הזול הבא — ‏עדיין פתוח, ואל תתבלבל עם `check:exemptions`** | ‏גל 11 הוסיף סקריפט `check:*` לשער **לאינווריאנטה אחרת**: ‏`scripts/check-exemption-pin.ts` מצמיד את **מספר שורות רשם החריגים** (§7), לא את נוכחות בלוק ה-re-assert. הצעד שהשורה הזאת מבקשת עדיין לא נכתב: סקריפט שמאשר שכל קובץ ב-`supabase/migrations/` מעל `0057` מכיל `scope_enforcement_violations`. **בדיקת טקסט אחת, אפס עלות ריצה** — ועכשיו יש לה תקדים בקובץ שכן נכתב, כולל היכן לחבר אותה ב-`package.json`. |

## 10. ‏`p2_invoice_without_order_count()` מתהפך תחת RLS — והבקרה היחידה היא קבוע תפקיד בלקוח

| | |
|---|---|
| **מה** | הפונקציה היא `SECURITY INVOKER` ומוענקת ל-`authenticated`, וה-`not exists (… invoice_order_links)` שבה **מתהפך** תחת RLS: ל-`payer` חוקת `iol_select` מחזירה אפס שורות, ולכן ה-negation נכון **לכל** חשבונית והמונה מחזיר את **כל** החשבוניות שה-payer רואה, בתווית "N חשבוניות ללא הזמנת רכש מקושרת" (`src/lib/alerts.ts:116-127`). זה **מספר שגוי**, לא דליפה. |
| **למה זה לא חי היום** | ‏`scanAlerts()` מגיע רק דרך `buildSummary()` (`src/lib/summary.ts:95`), שנצרך רק ב-`src/pages/Alerts.tsx:6`, שמוגן ב-`FINANCE = ['owner','office']` (`src/App.tsx:102,254`) — ולהם המונה **נכון**. הבקרה היחידה היא **קבוע תפקיד בלקוח**, במערכת שכל התזה שלה היא אכיפה בשרת. |
| **מה נעשה בגל 10** | הסריקה המבנית הורחבה: ‏`src/pages` נסרק **רקורסיבית** (הקודמת לא ירדה ל-`src/pages/dashboards/`, שם חי `PayerDashboard`), ונוספה זרוע ל-`src/lib` שמצמידה את **מספר הקוראים ל-1** ואת המדריך ל-`FINANCE` ללא `payer`/`supplier` (`src/pages/serverListScreens.spec.tsx`). **הספק לא הוחלש ולא הוצהר תקין** — הזרוע מצמידה את הבקרה שקיימת, כדי שקורא שני או הרחבת `FINANCE` ייפלו בבדיקה. |
| **איפה ההוכחה** | ‏`Plans/10-FINAL-AUDIT.md` Finding 9 · ‏`src/pages/serverListScreens.spec.tsx` (שתי הזרועות) · שאר ששת ה-`p2_*` נסקרו והם אגרגטים פשוטים ש**מחסירים** לתפקיד מוגבל — פגם אחר, פחות מטריד. ההיפוך יחיד. |
| **הצעד הזול הבא** | **החלטת תכולה, לא תיקון:** או שער תפקיד בשרת בתוך הפונקציה (`if auth_role() not in ('owner','office') then raise`), או להפוך אותה ל-`SECURITY DEFINER` עם סינון סקופ מפורש כמו שתי פונקציות היתרה. שתי האפשרויות משנות חוזה RPC ולכן דורשות הכרעה. |

## 11. יצירת ספק עם פרטי בנק — ‏INSERT בלי step-up ובלי סיבה (#106) — ✅ **נסגר 09.08.2026 (חבילה 4)**

> **ההכרעה:** בעלים, אפשרות (ב) — ‏step-up וסיבה כשפרטי הבנק אינם ריקים ביצירה. **המימוש חזק
> מהניסוח:** ‏`0088` ביטל את grant ה-INSERT על העמודה כולה (מקבילת `0061` ל-UPDATE), כך שהאכיפה
> בסכימה ולא בלקוח; היצירה כותבת שורה בלי בנק וערך לא-ריק עובר את `update_supplier_bank_details`
> המנומקת. **טענות בשער:** ‏`p0_client_dml_acl.sql` (שני הכיוונים ברמת הפריווילגיה) ·
> ‏`p1_financial_commands.sql` (מוטציה פונקציונלית: נדחה עם בנק, עובר בלי) ·
> ‏`supplierBankDetails.spec.tsx` (מסלול הלקוח). השורה נשארת כאן כתיעוד היסטורי בלבד.

| | |
|---|---|
| **מה** | ‏`0061:469` ביטל את grant ה-**UPDATE** על `suppliers.bank_details`; ‏**INSERT שרד בכוונה מנומקת** (`0061:463-468`), והלקוח משתמש בו (`src/pages/Suppliers.tsx:244`). לכן בעל/משרד יכול ליצור שורת ספק **חדשה** עם פרטי בנק מוחלפים — בלי step-up, בלי סיבת מפעיל ובלי `security_events`. |
| **למה נדחה** | תדירות יצירת ספק והשאלה אם אונבורדינג הוא מסלול "רגיש" הן שאלות **עסקיות**, לא שאלות סכימה. **נרשם, לא הוכרע.** |
| **איפה ההוכחה** | ‏`OPEN-DECISIONS.md` **#106** · ‏`Plans/10-FINAL-AUDIT.md` Finding 4 · ‏`information_schema.column_privileges`: ל-`authenticated` יש INSERT/REFERENCES/SELECT על `bank_details`, **בלי UPDATE**. ‏(נבדק ונקי: ‏`domain_event_map` נותן ל-`supplier_bank_details_changed` ‏`payload_keys` ריק ול-`supplier.created` רק `{name,status}` — פרטי בנק אינם נכנסים ל-`domain_events`.) |
| **הצעד הזול הבא** | להכריע #106, ואז מיגרציה יחידה: או ניתוב INSERT דרך פקודה מנומקת, או step-up **רק** כאשר `bank_details` אינו ריק בעת היצירה. |

## 12. הרוכב הקנוני אינו InitPlan — המדידה מפריכה את `0057:25-28`

| | |
|---|---|
| **מה** | ‏`0057:25-28` טוען שהחזרת `uuid[]` שומרת על `auth_scopes()` כ-InitPlan שמוערך **פעם אחת לכל statement**. **המדידה מפריכה זאת:** ‏EXPLAIN מראה את הקריאה בתוך `Filter:` בלי צומת InitPlan, וספירת קריאות נותנת **קריאה אחת לכל שורה שבה `unit_id` אינו NULL** (‏46 / 451 / 4,501 קריאות ב-50 / 500 / 5,000 שורות כאלה). זה נסבל היום **רק** כי זרוע `unit_id IS NULL` מקצרת ~95% מהשורות — כלומר **הוא מתדרדר בדיוק כשדיירים מאמצים יחידות**. |
| **למה זה חוב ולא באג** | קיים תיקון **מדוד**: עטיפה כ-`(select auth_scopes()) @> array[unit_id]` מחזירה InitPlan אמיתי ומורידה COUNT על 100k שורות מ-**548ms ל-22ms** בנראות זהה. אבל היא **משנה את מחרוזת הרוכב הקנונית**, שמוצמדת ע"י טענת **A3** (`0057:318-319`, השוואת `pg_get_expr` מדויקת) וע"י מודל האבטחה. זו **החלטת חוזה, לא תיקון**. |
| **איפה ההוכחה** | ‏`Plans/10-MEASUREMENTS.md`. **‏הסתייגות שנוסעת עם כל המספרים בסעיפים 12–15:** הסוכן המודד לא הצליח לטעון את דייר הדמו (אין משתמשי auth לדמו, ויצירת חשבונות אינה פעולה של סוכן), ולכן **כל** המספרים מגיעים מ-fixture ב-`pg_temp` פר-סשן שמשחזר את ה-DDL האמיתי — לא מדייר דמו טעון. |
| **הצעד הזול הבא** | **לא לגעת בלי הכרעה.** ריקון החוב = לעדכן **יחד**: ‏A3 (`0057`), ‏`ENTERPRISE-SECURITY-MODEL` והמחרוזת הקנונית, ו-`supabase/tests/p3_org_scope.sql` (שמצמיד את אותה מחרוזת). שלושה קבצים בקומיט אחד, או שהשער נופל. |

## 13. ‏`global_search` אינו משתמש ב-pg_trgm בכלל

| | |
|---|---|
| **מה** | חמש עמודות בשרשרת ה-OR חסרות אינדקסי trgm (`suppliers.phone`, ‏`suppliers.tax_id`, ‏`suppliers.email`, ‏`invoices.notes`, ‏`products.barcode`), ו-BitmapOr דורש ש**כל** זרוע תהיה מאונדקסת — ולכן **אף זרוע אינה משתמשת באינדקס**. הוכחה: מונח שמתאים לאפס שורות עולה כמו מונח שמתאים ל-6.7% (‏1,148ms מול 1,088ms ב-389k חשבוניות). הוספת האינדקסים החסרים **לבדה אינה משנה דבר** (1,087ms), כי זרוע החשבוניות מסננת על `suppliers.name` המצורף. |
| **מה זה מכריע** | ‏`docs/INTEGRATION-ARCHITECTURE.md:170` הגדיר את התנאי המקדים להחלפת pg_trgm במנוע חיפוש. **התנאי נענה, והתשובה היא: לא להחליף — לעצב מחדש את השאילתה.** צמצום ה-OR לעמודות מאונדקסות נמדד **130x** (‏1,088ms → 8.34ms). |
| **איפה ההוכחה** | ‏`Plans/10-MEASUREMENTS.md` (עם הסתייגות ה-fixture מסעיף 12) · ‏`0069` הזיז את שער סוגי-התוצאה לשרת ו**לא נגע** ב-pg_trgm בכוונה, בהמתנה למדידה הזו. |
| **מה נסבל בפועל** | עד ~10k חשבוניות/דייר — נסבל. ‏100k — מורגש. ‏389k — לא נסבל. |
| **הצעד הזול הבא** | לצמצם את שרשרת ה-OR לעמודות המאונדקסות (או להוסיף אינדקס ולפרק את זרוע החשבוניות מהצירוף ל-`suppliers.name`). **בלי מנוע חיפוש חדש.** |

## 14. ‏`invoice_has_duplicate` כ**מסנן** אינו מדיד-לגודל

| | |
|---|---|
| **מה** | כ**מסנן**: ‏1,232ms ב-100k חשבוניות, ‏5,460ms ב-387k — ו-`serverList` שולח תמיד `count:'exact'`. כ**עמודה מוצגת** הוא שטוח ותקין (‏0.52ms). |
| **למה זה חוב** | לא נדחה במפורש; זו **מגבלה מדודה** על כל מסך שחושף את מסנן הכפולים. |
| **איפה ההוכחה** | ‏`Plans/10-MEASUREMENTS.md` (עם הסתייגות ה-fixture מסעיף 12). |
| **הצעד הזול הבא** | לפני חשיפת המסנן במסך נוסף: או אינדקס תומך, או `count:'planned'` על המסלול הזה, או ויתור על המסנן והשארת העמודה. |

## 15. ‏`count:'exact'` הוא 99.6% מעלות הבקשה בקנה מידה

| | |
|---|---|
| **מה** | היחס בין בקשה עם `count:'exact'` לבקשה בלעדיו: ‏**3x / 22x / 282x / 837x** ב-1k / 10k / 100k / 389k שורות. ב-389k זה **99.6%** מהעלות. |
| **למה נדחה** | הקמפיין **קיבל זאת ביודעין** — ספירה מדויקת היא חלק מחוזה ה-pagination של גל 1b ("N מתוך M", ועמוד שלא קיים נאמר בשמו). עכשיו **יש לזה מספר**. |
| **איפה ההוכחה** | ‏`Plans/10-MEASUREMENTS.md` (עם הסתייגות ה-fixture מסעיף 12). |
| **הצעד הזול הבא** | ‏`count:'planned'` מעל רף שורות מוגדר, **עם שינוי מפורש בטקסט המסך** — "כ-M" ולא "M". שינוי טקסט הוא חלק מהתיקון, לא נספח לו: מספר משוער שמוצג כמדויק הוא בדיוק הפגם שהקמפיין נוצר כדי למנוע. |

## 16. סף האוטונומיה ‏0.90 אינו מכויל — והוא המספר שמתיר למודל לכתוב רשומה כספית (#109)

| | |
|---|---|
| **מה** | ‏`private.autonomy_policy_definitions.baseline_min_confidence = 0.900` הוא **ניחוש מתועד**. הוא לא נמדד מול קורפוס אמיתי: איש לא הריץ גוף מסמכים אמיתי והשווה את ההחלטה האוטומטית להכרעת אדם. אותו מספר יחיד חל על `document_type_confidence` **וגם** על `supplier.confidence`. מעליו, לדייר שנדלק, המערכת רשאית ליצור רשומה כספית בלי אישור אדם (הכרעת הבעלים, אפשרות ב'). **זו המגבלה האחת שהגל הזה משאיר פתוחה בכוונה.** |
| **למה נדחה** | כיול דורש **נתונים**, לא קוד: גוף מסמכים אמיתי של הלקוח והכרעת אדם לכל אחד מהם. אין דרך להמציא אותו, והמצאת מספר "מדויק" יותר בלי מדידה הייתה בדיוק ההמצאה שהקמפיין אוסר. לכן נבחר להצהיר על הניחוש בקול — בכותרת המיגרציה, בתגובת הפונקציה, ב-#109 וכאן — במקום להסתיר אותו מאחורי ברירת מחדל שנראית סמכותית. |
| **מה מגודר בכל זאת** (הנוסח הזה תוקן אחרי הסקירה — שתי השורות הקודמות היו **רחבות מהמדידה**) | ‏(א) **ברירת המחדל היא כבוי לכל דייר**, ואף מיגרציה אינה מדליקה — הטענה הראשונה ב-`p13_document_autonomy_config.sql`. ‏(ב) **הכתיבה היא פקודת מפעיל מנומקת ומבוקרת בלבד** (`platform_set_autonomy_policy`) — ‏**לאף תפקיד שאינו superuser אין INSERT/UPDATE/DELETE**, לא ברמת טבלה ולא ברמת עמודה: לא ל-`anon`, לא ל-`authenticated` ו**לא ל-`service_role`** (‏`service_role=rDxtm/postgres` אחרי ה-revoke; קודם היה `arwdDxtm` וזה היה חור אמיתי). הכותב היחיד הוא ‏`SECURITY DEFINER` בבעלות `postgres`. ‏(ג) **הסף אינו יכול להיות `null`/`0`/שלילי/`>1`** — נדחה בשם בפקודה ושוב ב-`CHECK` על השורה, **וגם בתשובת המעריך** (`and c.min_confidence is not null`), כי הגדר על השורה לבדה לא כיסתה את הענף הלא-מוגדר. ‏(ד) **חוק החמרה-בלבד:** דייר רשאי לדרוש יותר ביטחון, לעולם לא פחות — ‏**וזה נכון רק כל עוד (ב) נכון**, כי הרצפה נאכפת בגוף הפקודה בלבד. ‏(ה) ‏**kill switch** שמכבה אוטונומיה בכל הדיירים, **בלי מקבילה של "הדלקה בכוח" — כאילוץ סכימה** (`autonomy_definitions_baseline_off`), לא כהבטחה בהערה. |
| **איפה ההוכחה** | ‏`OPEN-DECISIONS.md` **#109** · ‏`supabase/migrations/0076_document_autonomy_config.sql` (כותרת + ‏`comment on function evaluate_autonomy_policy`) · ‏`supabase/tests/p13_document_autonomy_config.sql` — **נספר מחדש 07.08.2026: ‏44 קריאות `select pg_temp.p13_assert(` + ‏7 בלוקי `do $$` שכולם מרימים `raise exception` = ‏51 אתרי טענה** (השורה הזאת אמרה "32" בטיוטה, ואז "42 + 7 = 49"; שתי הפעמים המספר לא נספר מחדש אחרי שנוספו טענות — בדיוק הכשל ש-`CLAUDE.md` מזהיר מפניו, בקובץ שנועד להיות מקור אמת. **הפעם רשומה גם שיטת הספירה, כדי שהבא יריץ אותה במקום לסמוך על התוצאה.**) **שלוש הוכחות מוטציה:** הסרת רצפת ההחמרה ⇒ ‏0.80 נכנס (הרצפה היא המגן **היחיד** שם); הפלת ה-`CHECK` ⇒ סף 2.0 נשמר; והפלת `autonomy_definitions_baseline_off` + הדלקת הבסיס ⇒ התשובה **עדיין** כבויה, ואז החזרת גוף המעריך הישן ⇒ המצב המסוכן חוזר, מה שמוכיח איזה שומר בדיוק מחזיק. **מה שאין:** אף מדידה של הסף. אפס מסמכים אמיתיים נבדקו. |
| **שני חורים שהסקירה מצאה אחרי המסירה הראשונה, ונסגרו** | ‏**(1) ‏`baseline_enabled` היה מנוף force-on** — הקובץ הבטיח "אין מקבילה של הדלקה בכוח", אבל העמודה ישבה באותה טבלה, שורה והרשאה כמו ה-kill switch בלי `CHECK`. ‏`update … set baseline_enabled = true` יחיד הדליק אוטונומיה לכל דייר שלא הוגדר — כלומר לכולם — בלי סיבה ובלי audit. **נמדד לפני התיקון:** ‏`configured=false, enabled=TRUE, min=NULL`. גרוע מכך, המצב הזה הוא בדיוק "דלוק בלי סף": ‏`NOT NULL` מגן על ה**שורה**, לא על ה**תשובה**, ובעוד `confidence >= null` נכשל-סגור ב-SQL, ‏`0.42 >= null` ב-TypeScript אחרי PostgREST הוא **true** (null נכפה ל-0). נסגר בשתי גדרות: ‏`constraint autonomy_definitions_baseline_off check (baseline_enabled = false)` ו-`and c.min_confidence is not null` בתוך המעריך. ‏**(2) ‏`service_role` החזיק DML מלא** (‏`service_role=arwdDxtm/postgres`, ברירת המחדל של Supabase לכל טבלה חדשה) — ‏`set role service_role` ואז INSERT פשוט אחסן **0.050**, מתחת לרצפה, עם **אפס** שורות audit מנומקות. זה הפריך שתי טענות שנכתבו כאן: "הכתיבה היא פקודה מנומקת ומבוקרת **בלבד**" ו"דייר רשאי לדרוש יותר, **לעולם לא פחות**" — הרצפה חיה **רק** בגוף הפקודה. נסגר ב-`revoke insert, update, delete … from service_role`; ה-SELECT נשאר. **התיקון כאן חינם** כי הכותב הלגיטימי היחיד הוא `SECURITY DEFINER` בבעלות `postgres`, שכותב כ-postgres בלי קשר להרשאות הקורא. **הלקח שנרשם:** סריקת ההרשאות בסוויטה בדקה רק `anon`/`authenticated` בזמן שהודעתה טענה "הכותב היחיד" — פרדיקט צר מהטענה שמעליו הוא איך החור הזה עבר. הסריקה כוללת עכשיו `service_role`. |
| **הצעד הזול הבא — ‏חלקו בוצע 09.08.2026 (חבילה 3): הסף נמדד לראשונה, ואושרר** | ‏✅ **מדידה ראשונה רצה** — לא על מסמכי הלקוח אלא על קורפוס אינטרנט, בהכרעת בעלים מפורשת (סוכן אסף 25 מסמכים; ‏10 שופטו מול ריצות מודל אמיתיות של `gpt-5.6-terra` + ‏v8, דרך `core.ts` עצמו). **התוצאה: ‏0.900 בתוך הרצועה הבטוחה הנמדדת [0.85, 0.95] — ‏FP יחיד (דוח מחירים ממשלתי שפורש כמחירון ב-0.90/0.80) נחסם ברצפה הנוכחית והיה נכנס ב-0.80; ‏0.99 מפיל חשבונית נכונה בלי רווח.** הדוח המלא, כולל כל מה שהמדידה *לא* אומרת: ‏`docs/calibration/2026-08-09-internet-corpus.md`. ‏🔲 **מה שנשאר, בדיוק כקודם:** ‏50 מסמכים **של הדייר החי** עם הכרעת אדם-בעל-עסק — הנחת ההעברה מקורפוס גנרי מוצהרת ופתוחה. **אין להוריד את הרצפה** — המדידה חיזקה את הסיבה, לא החלישה אותה. |

## 17. פריט שלא הוזמן מוצג תחת תווית שאינה מתארת אותו — ‏`receipt_mismatch` כפשרת ביניים

| | |
|---|---|
| **מה** | כשהאוטומציה (`apply_document_interpretation`, ‏0077) מוצאת בחשבונית פריט שאינו בהזמנה המקושרת, היא פותחת חריג מסוג **`receipt_mismatch`**, שתוויתו בעברית היא **"פער קבלה מול חשבונית"**. המצב האמיתי הוא **"התקבל פריט שלא הוזמן"** — קרוב, ולא זהה. אין קבלה בסיפור הזה בהכרח. הכותרת של החריג **כן** אומרת את האמת (`'התקבל פריט שלא הוזמן — חשבונית ' \|\| מספר`), והקוד המדויק יושב ב-`details.code = 'item_not_ordered'` יחד עם `order_id`, ‏`order_number`, ‏`interpretation_id` ורשימת הפריטים — אבל **עמודת הסוג** ב-`/exceptions`, זו שמסננים לפיה ושסופרים אותה, מציגה תווית מקורבת. |
| **למה נדחה** | ‏`exception_type` הוא **enum סגור** בן תשעה ערכים (`0001_init.sql:18`), ואף אחד מהם אינו מתאר פריט לא-מוזמן. הוספת ערך היא `alter type exception_type add value` — ב-PostgreSQL 17.6 היא מותרת בתוך טרנזקציה, אבל **הערך החדש אינו שמיש באותה טרנזקציה שהוסיפה אותו**, ולכן מיגרציה אחת שמוסיפה ומשתמשת תיפול. **נמדד על המסד המקומי, לא נזכר מהזיכרון:** ‏`alter type … add value 'item_not_ordered'` ואז שימוש באותה טרנזקציה מחזיר ‏`ERROR: unsafe use of new value "item_not_ordered" of enum type exception_type`. בנוסף היא דורשת תווית עברית ב-`src/lib/status.ts:118-128` — קובץ frontend שמשימה C2 לא החזיקה, ובזמן שסוכן אחר ערך קבצי frontend באותו גל. הפשרה שומרת את האמת בשני מקומות (כותרת + `details.code`) בעלות **אפס** שינוי סכימה, ומשאירה את התיקון זול. |
| **מה זה כן ולא** | זו **אינה** טענה שגויה על המציאות — החריג נפתח על אירוע אמיתי, מול ההזמנה הנכונה, עם הפריט המדויק. זהו חוב **תווית**: קטגוריה מקורבת שתשרוד לנצח אם איש לא ירשום אותה. ההתאמה עצמה שמרנית במכוון — היא רצה **רק על מק"ט או ברקוד**, אף פעם לא על תיאור חופשי, כי "עגבניות שרי 500 גרם" ו"עגבניות שרי" הם אותו מוצר לאדם ומחרוזות שונות ל-Postgres. שורה בלי מזהה **מדולגת** ולא מנוחשת: התראת-שווא מאמנת את המנהל להתעלם מהחריג, וזה גרוע מלא להתריע. |
| **איפה ההוכחה** | ‏`supabase/migrations/0077_apply_document_interpretation.sql` (בלוק ה-`insert into public.exceptions` וההנמקה מעליו) · ‏`supabase/tests/p14_apply_interpretation.sql` סעיף (l) — שלוש טענות: החריג נפתח עם `details.code='item_not_ordered'` ועם המק"ט; השורה שכן הוזמנה **אינה** מדווחת; והשורה בלי מזהה **מדולגת** (`jsonb_array_length(details->'items') = 1`) · ‏`src/lib/status.ts:127` (התווית) · ‏`0001_init.sql:18` (ה-enum). |
| **הצעד הזול הבא — ‏חציו בוצע 09.08.2026 (חבילה 2)** | ‏✅ **שלב (1) בוצע:** ‏`0086` הוסיפה את הערך במיגרציה משלה, ‏`status.ts` נושא את התווית `'פריט שלא הוזמן'`, והפקודה הידנית החדשה `open_manual_exception` (‏`0087`, ‏#116) משתמשת בסוג הכן מהיום הראשון. ‏🔲 **מה שנשאר:** המסלול ה**אוטומטי** (‏`apply_document_interpretation`) עדיין פותח `receipt_mismatch` עם `details.code='item_not_ordered'` — ההחלפה היא הזרקה לגוף החי של 0077 (**בעוגני-אב, לא הצהרה-מחדש**) + עדכון שלוש הטענות בסעיף (l) של p14, **בקומיט אחד**. עד אז שני חריגים על אותו אירוע-עולם עשויים לשאת שתי תוויות — הידני מדויק, האוטומטי מקורב. |

## 18. ארבעה משטחים עדיין מציגים את שבעת שלבי ה-OCR — כולל המשטח שהתלונה נקבה בשמו

| | |
|---|---|
| **מה** | משימה D1 קיפלה שבעה שלבי pipeline לארבעה מצבים אנושיים (נקלט · בבדיקה · שויך/נקרא · לא נקרא) **בתיקיית המסמכים בלבד** (`src/pages/DocumentsInbox.tsx`). **ארבעה משטחים אחרים ממשיכים להדפיס את התוויות ההנדסיות** לאותם משתמשים: ‏`src/components/AttachmentsPanel.tsx:226` (קבצים מצורפים לחשבונית/קבלה) · ‏`src/components/FileUpload.tsx:558` (רשימת הקבצים אחרי צירוף) · ‏`src/components/UploadCenter.tsx:544` (מגש ההעלאה) · ‏`src/components/document-review/DocumentReviewWorkspace.tsx:31` (מסך הבדיקה). התוצאה: אותו מסמך קורא **נקלט** בתיקייה ו**"ממתין לפירוש"** במסך שאליו התג עצמו מקשר, בקליק אחד. |
| **למה זה חמור מ"חוסר עקביות"** | ‏(א) ‏**`UploadCenter` הוא המשטח שהבעלים תיאר מילולית** — *"כשאני מעלה מסמך אנחנו לא אמורים לראות את כל הprocess של ה-OCR"*. הוא נשאר בשפה הישנה. ‏(ב) ‏`DocumentReviewWorkspace` הוא **יעד הקישור** של התג החדש, כך שהסתירה מוצגת ברצף פעולה אחד ולא בשני מסכים מרוחקים. ‏(ג) שתי תוויות שונות לאותה עובדה מלמדות שאי אפשר לסמוך על אף אחת מהן. |
| **למה נדחה** | ‏D1 הוקצתה לשני קבצים (`useDocumentProcessing.ts`, ‏`DocumentsInbox.tsx`) ולספק חדש, בזמן שסוכנים אחרים החזיקו `src/components/document-review/*` באותו גל. עריכת קבצים בבעלות סוכן אחר באמצע גל היא התנגשות מובטחת. **`DocumentReviewWorkspace` הוא גם המקרה היחיד שבו התווית ההנדסית אולי נכונה:** סוקר מול job תקוע צריך לדעת איזה שלב נעצר — וזו הכרעת מוצר פתוחה, לא החלה מכנית. |
| **איפה ההוכחה** | ארבעת ה-file:line למעלה · ‏`src/lib/useDocumentProcessing.ts` (‏`DOCUMENT_USER_STATE_META`, ‏`documentUserState`) · ‏`src/lib/documentStage.spec.tsx` (‏36 בדיקות: המיפוי, חוזה ה-`data-stage`, וארבע האפשרויות של הבקרה) · אף שער אינו נשען על התוויות בארבעה המשטחים: ‏`data-document-processing-status` נקרא **רק** בתוך `AttachmentsPanel` ו-`FileUpload` עצמם, ולא ב-`scripts/` או ב-`qa/`. |
| **הצעד הזול הבא** | שורה אחת בכל אחד משלושת המשטחים היומיומיים: ‏`DOCUMENT_USER_STATE_META[documentUserState(stage)]` במקום `DOCUMENT_PROCESSING_STAGE_META[stage]`. **‏`UploadCenter` דורש מחשבה ולא החלפה עיוורת** — ‏`displayMeta` שם **ממזג מצב העלאה עם מצב עיבוד**, ומוציא את `unprocessed` מהכלל בכוונה ("נרשם — העיבוד לא החל"), כלומר קיפול עיוור יבלע הבחנה אמיתית. ‏**`DocumentReviewWorkspace` דורש הכרעת בעלים תחילה** — האם מסך הסוקר מדבר בשפת המשתמש או בשפת ה-pipeline. |

## 19. אין ולו ראיה אחת שהמודל מציית לפרומפט — ‏`interpret-document-v4` נאכף בדיג׳סט, לא בהתנהגות

| | |
|---|---|
| **מה** | ‏`SYSTEM_PROMPT` נעול ל-`PROMPT_VERSION = "interpret-document-v4"` וכל שינוי בטקסט **חייב** לעדכן את הדיג׳סט ב-`PROMPT_DIGESTS`, אחרת `core.test.ts` נופל. זו הצמדה חזקה — והיא מוכיחה רק ש**הפרומפט שנשלח הוא הפרומפט שנרשם**. ‏**היא אינה מוכיחה דבר על מה שהמודל עושה איתו.** אף בדיקה בריפו אינה מריצה מודל אמיתי, ואין קורפוס מסמכים לבדוק מולו. כל הבדיקות של שכבת הפירוש רצות מול mock. |
| **למה נדחה** | הרצת מודל בשער היא לא-דטרמיניסטית, עולה כסף לכל ריצה, ודורשת מפתח חי בסביבה שכל התזה שלה היא ריצה מקומית מנותקת. חשוב מכך: אין מה להריץ אותו **על** — קורפוס מסמכים אמיתי של הלקוח הוא בדיוק הנתון החסר גם ב-§16. |
| **איפה ההוכחה** | ‏`supabase/functions/interpret-document/core.ts:34` (הגרסה) ו-`core.ts:379` (ההערה שקובעת את מדידת השדה) · ‏`core.test.ts:669-737` (הצמדת הדיג׳סט) · ‏`core.test.ts:497-509` — **תיקון מתועד בקובץ עצמו:** נוסח מוקדם טען ש-`0077` דוחה fixture מסוים ב-`amounts_unreconciled`, וזו הייתה טענה על התנהגות מודל שאיש לא הריץ. |
| **הסגירה היחידה שקיימת, והיא מדידת שדה ולא בדיקה** | ‏**שיעור הפירושים שנעצרים ב-`amounts_unreconciled`, מושווה בין `prompt_version` ‏`interpret-document-v3` ל-`interpret-document-v4`.** אם נקיבת המפתחות בפרומפט עבדה — השיעור יורד. אם לא — שום דבר בריפו לא היה מספר לנו. |
| ✅ **נסגר חלקית 07.08 אחה"צ — ‏`reason_code` היא עכשיו עמודה** | ‏`0079` הוסיפה `document_filings.reason_code` והזריקה אותה לגוף החי של `apply_document_interpretation` (‏`pg_get_functiondef` + שני עוגנים, כל אחד חייב להופיע **בדיוק פעם אחת**, ושש זרועות מוצמדות בשם אחרי ההזרקה). **המדידה שסעיף זה קרא לה בלתי-ניתנת-להרצה — ‏שיעור ה-`amounts_unreconciled` בין `prompt_version` ‏v3 ל-v4 — היא כעת שאילתה אחת:** ‏`select reason_code, count(*) from document_filings f join document_interpretations i on i.id = f.interpretation_id group by 1, i.prompt_version`. **מה שעדיין פתוח:** אין עדיין נתונים להשוות (‏v4 בפרודקשן מ-07.08, שתי שורות בלבד), ו**שתי שורות קיימות אינן מבוססות למפרע** — הסיבה שלהן לא נרשמה מעולם, והמצאתה הייתה בדיוק הפבריקציה שהחוקה אוסרת. |
| 🔴 **פגם שנמצא בתיעוד גל 11: המדידה הזאת, כפי שנוסחה, אינה ניתנת להרצה מול המסד** | ‏`core.test.ts:504-511` מנסח את הסגירה כך: *"compare the rate of `document_filings` rows carrying `reason_code = 'amounts_unreconciled'` … **Both columns already exist and both are already populated on every row** … and the filing carries the reason"*. ‏**‏`document_filings` אין בה עמודת `reason_code`.** ‏`0075:216-260` מגדירה ‏`id · org_id · document_id · category · supplier_id · interpretation_id · confidence · decided_by · decided_at · reverted_at · reverted_reason` ותו לא, ו-`0077` נוגעת בטבלה רק כדי להחליף את `document_filings_reversal_shape`. ‏`reason_code` הוא **משתנה מקומי ב-plpgsql ומפתח ב-jsonb שהפקודה מחזירה** — לא עמודה. **והחמור מכך, למוצא הרלוונטי:** ‏`auto_applied` ו-`archived` כן משאירים שורת `audit_logs` שנושאת את הסיבה, אבל ‏`queued_for_review` — שהוא המוצא של `amounts_unreconciled`, ובמצב המשלוח (מתג כבוי) גם המוצא של **כל** מסמך — **אינו כותב שורת audit כלל.** כלומר הסיבה הזאת אינה נשמרת בשום מקום במסד; היא קיימת רק בשורת ה-`console.log` של ה-Edge Function (‏`index.ts:376-384`). **המדידה אפשרית היום רק מלוגים, לא משאילתה.** ‏**זה נרשם ולא תוקן** — התיקון (עמודת `reason_code` על `document_filings`, או שורת audit גם ל-queued) הוא מיגרציה ושינוי חוזה, ולא עבודת תיעוד. **אל תצטט את המשפט ההוא כאילו הוא נכון.** |

## 20. הסניטייזר לאחסון נשאר denylist — סיכון שיורי מוצהר, לא סגור

| | |
|---|---|
| **מה** | ‏`private.document_text_key` (מפתח ההשוואה לכפילות) הפך ל**allowlist** ולכן שלם בהגדרה. ‏`private.document_text_sanitize` (הטקסט **הנשמר**) נשאר **denylist** במכוון: המטרה שם היא נאמנות למסמך, ולכן "שמור אלא אם ידוע כרע". המשמעות: תו בלתי-נראה ש-Postgres מסווג שגוי ושהרשימה אינה נוקבת בשמו **ישרוד לתוך מספר חשבונית שמור** ויקשה למצוא אותו בחיפוש. |
| **למה זה נסבל ולא באג** | האסימטריה מכוונת ומדודה: כפילות נבדקת על ה-key, לא על הטקסט השמור, ולכן תו כזה **אינו יכול לגרום לתשלום כפול** — הנזק הגרוע ביותר במערכת. ‏`p14` טוענת כ**תכונה** שכל מה שהפונקציה פולטת הוא גרפי או רווח פשוט, ושלוש הטווחים שנוקבים בשם הם בדיוק החורים ש**נמדדו** בסיווג של Postgres עצמו (‏U+3164 ‏HANGUL FILLER מדווח `graph=true` **וגם** `alnum=true`; ‏U+FE0F מדווח `graph=true`). |
| **איפה ההוכחה** | ‏`0077:242-338` (שתי הפונקציות וההנמקה) · ‏`p14_apply_interpretation.sql:169-183`,`:1171-1216` — **סחיפת הסקירה: כל קוד-פוינט ב-Cf, Zs, Zl, Zp, Cc ו-Default_Ignorable_Code_Point, ‏4,289 מועמדים; ‏413 שרדו; ‏7 אומתו קצה-לקצה כותבים חשבונית חיה שנייה תחת מספר אחד** (‏U+FE0F, ‏U+0600, ‏U+E0041, ‏U+206F, ‏U+3164, ‏U+FFF9, ‏U+E0100). טענת התכונה בשער סורקת **67,583** קוד-פוינטים (כל ה-BMP פחות ה-surrogates, ועוד מישור U+E0000). |
| **הצעד הזול הבא** | אין. **זו מגבלה מוצהרת ולא משימה** — אלא אם יוחלט שנאמנות לטקסט המקורי פחות חשובה מיכולת החיפוש, ואז `document_text_sanitize` מתהפכת ל-allowlist בדיוק כמו ה-key. **ההיפוך הזה משנה מה נשמר על רשומה כספית, ולכן הוא הכרעה ולא ניקיון.** |

## 21. היסטוריית ביקורת מבואות סתומים — חלק מהממצאים נסגרו מאז

| | |
|---|---|
| **מה** | ‏`docs/DEAD-ENDS-AUDIT.md` תיעד snapshot היסטורי ל־07.08.2026. מאז נמסרו recovery עצמי, סמנטיקת ספק לא פעיל וכרטיס ספק פיננסי ייעודי; אין להשתמש בספירת 13 כמדידת המצב הנוכחי בלי להריץ את הביקורת מחדש. |
| **למה נדחה** | **הפרדת האיתור מהתיקון הייתה תנאי המשימה**, ומהסיבה שהסבב הקודם (`docs/persona-ux-audit/final/`) נכשל בדיוק שם: מי שמתקן תוך כדי מפסיק לחפש. הביקורת הזאת מדדה **משימות**, לא מסכים — הסבב הקודם מדד מסלולים קיימים ונתן 4/5 ל"קלות תפעול" למשימה שהיא עצירה מוחלטת. |
| **איפה ההוכחה** | ‏`docs/DEAD-ENDS-AUDIT.md` — כל ממצא נושא `קובץ:שורה` ואת "התיקון הזול ביותר" שלו. **הביקורת מצהירה גם על הפערים של עצמה** (סעיף "מה לא יכולתי לבדוק"): לא הורצה אפליקציה, לא נבדק מובייל אמיתי, ושלושת התפקידים שנושאים את רוב החסימות (`kitchen`, ‏`payer`, ‏`supplier`) נבדקו בקריאה בלבד — הפער שהיא עצמה מסמנת כמסוכן ביותר. |
| **הצעד הזול הבא** | להריץ מחדש את 22 המשימות לאחר שער הדפדפן; להשאיר ברשם רק חסימות שניתנות לשחזור בקוד הנוכחי. |

## 22. מתג האוטונומיה ניתן להגדרה לארגון המפעיל בלבד — אין קורא חוצה-דיירים

| | |
|---|---|
| **מה** | ‏`AutonomyPolicyPanel` (‏`/admin`) הוא **הקורא היחיד** של `platform_set_autonomy_policy`, והוא מוגבל ל**ארגון של המפעיל עצמו**. הפקודה מקבלת `p_org_id` שרירותי, אבל **הקריאה** אינה: ‏`org_autonomy_policies` נקראת דרך `org_id = auth_org()` (‏`0076:200-201`), ו-`evaluate_autonomy_policy` פותרת דרך `auth_org()` גם היא (‏`0076:437`). מנהל פלטפורמה שמסתכל על דייר אחר **אינו יכול לראות את מצב המתג שלו**. |
| **למה נדחה** | מתג שאינו יכול להציג את מצבו הוא בדיוק "מסך שטוען משהו שהוא לא יודע" — האיסור המפורש בחוקה. עדיף פאנל שמצהיר על הגבול שלו מאשר טבלת דיירים עם מתגים ריקים. הסגירה דורשת RPC קריאה חדש (‏`SECURITY DEFINER`, מגודר ב-`is_platform_admin()`), כלומר **מיגרציה** — ולא היה לה צורך אמיתי כל עוד יש דייר אחד. |
| **איפה ההוכחה** | ‏`src/components/AutonomyPolicyPanel.tsx` (כותרת הקובץ, סעיף SCOPE) · ‏`src/pages/Admin.tsx` (‏`{org && <AutonomyPolicyPanel …>}`) · ‏`0076:200-203` (ה-RLS וה-grant) · ‏`0076:423-442` (המעריך ו-`auth_org()`). **נמדד בפרודקשן 07.08.2026:** ‏`platform_admins` = ‏1 שורה, ‏`org_autonomy_policies` ‏= 0 שורות, ‏`authenticated` מחזיק `SELECT` בלבד. |
| **הצעד הזול הבא** | ‏`platform_autonomy_policies()` — ‏`SECURITY DEFINER`, זורקת `not_platform_admin` בדיוק כמו ה-setter, ומחזירה שורה לכל ארגון. אז הפאנל הופך לעמודה בטבלת הארגונים. **מיגרציה אחת ושינוי מסך אחד** — ולא לפני שיש דייר שני. |

## 23. שם קובץ עברי לא הגיע לאחסון — נסגר, והמנגנון המדויק לא אותר

| | |
|---|---|
| **מה** | העלאת מחירון בשם `אחים כהן מחירון.pdf` נכשלה בהודעה גנרית. ‏`reserve_supplier_price_document_upload` **הצליחה** (שורת הזמנה קיימת, ‏07.08 ‏11:43:48Z), ו**אף אובייקט לא נוצר**. הנתיב היה `{org}/supplier/{s}/{d}/אחים כהן מחירון.pdf` — ‏`0048:370` בנה אותו **מהשם הגולמי**. מסלול התיבה הנכנסת מעולם לא נתקל בזה כי `FileUpload.tsx:220` כבר מחטא (`[^\w.\-]+` → `_`, ו-`\w` ב-JavaScript הוא ASCII). |
| **מה נסגר** | ‏`0078`: ‏`private.storage_key_segment()` מחטא את **מפתח האחסון בלבד**; ‏`file_name` נשאר עברית ומוצג כך. אילוץ הנתיב משווה מול הצורה המחוטאת (קודם הוא **חייב** את השם הגולמי במפתח — כלומר מפתח ASCII היה **בלתי-ניתן-לייצוג בסכימה**). |
| **מה לא נקבע, ונאמר במפורש** | **מחרוזת השגיאה לא אותרה.** ‏`edge_logs` לא הראה ולו בקשה אחת לנקודת ההעלאה בחלון הרלוונטי. שלושה מנגנונים עולים בקנה אחד עם הראיות — ולידציית המפתח של Supabase Storage, ‏round-trip של `Upload-Metadata`, או `supplier_price_upload_reservation_active` שאינו מתאים כי שם האובייקט אינו זהה-בית לנתיב השמור. הם נבדלים ב**איפה** הסירוב קרה ומסכימים על הסיבה ועל התרופה. **המיגרציה מתקנת את המפתח ואינה טוענת איזו שכבה סירבה.** |
| **מלכודת שנמדדה ושווה לזכור** | ‏**`select 'א' ~ '\w'` מחזיר `true` בפוסטגרס.** העתקת הרג'קס מ-JavaScript ל-SQL הייתה no-op שנראה נכון. המחלקה כתובה במפורש (`[^a-zA-Z0-9._-]`), ו-`0078` **טוענת את זה בבלוק `do $$`** כדי שהיום שבו זה ישתנה יפיל מיגרציה במקום להשתיק תיקון. |
| **פגם שנתפס בדרך, ושייך לרשומה הזאת** | הטיוטה הראשונה של `0078` הצהירה-מחדש את הפונקציה מ-**`0048`** ונימקה זאת בבדיקת `pg_proc` שהפרדיקט שלה (`prosrc like '%concat_ws(%'`) נכון גם ל-`0065`. ‏`0065` שכתב את הפונקציה (חלון-חסד של שעה ב-sweep), ולכן ההצהרה-מחדש הייתה **מחזירה בשקט** את המחיקה ל-`now()` ופותחת מחדש את המרוץ שחידוש-בתעופה יכול להפסיד. ‏`p6b_upload_reservations.sql` תפס זאת מקומית, בשם. **פרדיקט צר מהטענה שמעליו — הפעם השלישית שהקובץ הזה מתעד את אותה מחלקה, והראשונה שבה היא נתפסה לפני פרודקשן.** |

## 24. סף המחירון `0.900` אינו מכויל — תשתית המדידה נמסרה, הכיול הנתוני נשאר פתוח

| | |
|---|---|
| **מה** | מדיניות `price_list.intake` משתמשת באותו סף התחלתי לא-מכויל של §16. במחירון, טעות אחת עשויה לחזור על מאות שורות; ובהכרעת הבעלים החדשה, שורה עם שם ומק״ט/ברקוד שאינה תואמת מוצר קיים יכולה גם ליצור מוצר חדש. אין עדיין קורפוס מחירונים אמיתי שהושווה להכרעת אדם. |
| **הכרעת הבעלים 07.08.2026** | הבעלים הורה להפעיל את המדיניות לדייר החי גם לפני כיול 50 מחירונים, כדי ששורות בטוחות ייקלטו ומוצרים מזוהים ייווצרו בלי הקמה ידנית. זו קבלת סיכון מפורשת לדייר הזה בלבד; בסיס המוצר נשאר כבוי לדיירים אחרים. |
| **מה מגודר** | המדיניות נפרדת ונשלחת כבויה; שם מוצר לעולם אינו מפתח התאמה; כפילות ממתינה; מחיר לא תקין ממתין; הקליטה חלקית ואידמפוטנטית. `0096` מוסיפה shadow immutable, החלטות אדם בגרסאות, פילוח ספק/format/version, drift read model ו־rollback batch מנומק ואידמפוטנטי. שינוי מבני חדש בלבד מעביר ל־Shadow; מדדים מספריים אינם מפעילים threshold אוטומטי. |
| **איפה ההוכחה** | `0081`, ‏`0093`, ‏`0096`, ‏`p15_automatic_price_list_intake.sql` · `p18_document_automation_calibration.sql` · `docs/calibration/2026-08-09-internet-corpus.md` · `OPEN-DECISIONS` #119–#122 ו-#138. שער האיכות המלא עבר ב־09.08.2026; הכיול הנתוני עצמו נשאר פתוח. |
| **הצעד הזול הבא** | מדידת האינטרנט N=10 היא מדידה מקדימה לסיווג בלבד ואינה מכיילת התאמת שורות או מאשרת אוטומציה. יש למדוד את 50 המחירונים האמיתיים הבאים מול הכרעת אדם, עם ספירה נפרדת של התאמה שגויה, מוצר שגוי ומחיר שגוי. רק המדידה הזאת רשאית לשנות את הרצפה; ההפעלה לדייר החי אינה כיול. |

## 25. מסירת מייל בלי דומיין מאומת — הזמנה מגיעה רק לכתובת בעל החשבון, ואיפוס רוכב על המיילר המובנה

| | |
|---|---|
| **מה** | אין דומיין שליחה מאומת (הכרעת בעלים 09.08.2026: "אין דומיין"). ‏Resend על `onboarding@resend.dev` מוסר מייל **רק לכתובת בעל חשבון ה-Resend** — הזמנת עובד/ספק לכל כתובת אחרת נוצרת במסד ואינה מגיעה. מייל "שכחתי סיסמה" (חבילה 1) יוצא מהמיילר המובנה של Supabase — מגיע לכל כתובת, אבל מוגבל-קצב ולא ממותג. |
| **למה נדחה** | אימות דומיין הוא פעולת DNS של הבעלים, ואין כרגע דומיין לאמת. שני המסלולים במוצר נבנו כך שהשדרוג הוא קונפיגורציה בלבד — ‏`INVITE_FROM_EMAIL` ל-invites, ‏SMTP מותאם ב-Auth לאיפוס — בלי לגעת בקוד. |
| **איפה ההוכחה** | ‏`docs/NIR-ROADMAP.md:50` · ‏`OPEN-DECISIONS.md` #114 · תרחיש `passwordRecovery` בשער הדפדפן מוכיח את מסלול האיפוס **בלי SMTP** (‏admin generate_link — אותו טוקן שהמייל היה נושא). **האימות החי המלא של הזמנה → מייל לכתובת שאינה של הבעלים → ‏`/accept-invite` → כניסה לא רץ מעולם** (‏`NIR-DEV-CLOSEOUT` 3.2) ולא יכול לרוץ עד שיהיה דומיין. |
| **הצעד הזול הבא** | דומיין מאומת ב-Resend → עדכון `INVITE_FROM_EMAIL` → ריצה חיה אחת של מסלול ההזמנה המלא. לאיפוס: חיבור SMTP מותאם (Resend SMTP על אותו דומיין) ב-Supabase Auth, שמסיר גם את מגבלת הקצב. |

## 26. אימות חי של שלושת התפקידים בשטח (kitchen/payer/supplier) — נשאר פתוח: דורש הרשאות שאין לסוכן

| | |
|---|---|
| **מה** | ‏`DEAD-ENDS-AUDIT` מסמן כפער המסוכן ביותר את זה ששלושת התפקידים שנושאים את רוב החסימות נבדקו **בקריאה בלבד**. חבילה 5 תוכננה לסגור זאת בהרצה חיה במובייל (390px) — **וזה לא בוצע**: כניסה חיה לפרודקשן בתפקידים האלה דורשת חשבונות אמיתיים של הדייר החי, שאינם ואסור שיהיו בידי הסוכן. |
| **מה כן קיים** | שער הדפדפן המלא מריץ את שלושת התפקידים ב-390px מול stack חי מקומי (login אמיתי, RLS אמיתי): ‏`role and viewport matrix`, ‏`supplier portal uses the RLS-safe projection`, תרחישי הקבלה, ‏`payer` בחיפוש ובניווט. זה מכסה את המסלולים — לא את סביבת הפרודקשן ולא מכשיר אמיתי. |
| **הצעד הזול הבא** | הרצה אחת של הבעלים (או בנוכחותו): שלוש כניסות במובייל אמיתי — kitchen מריץ קבלה עם פריט פגום, ‏payer פותח את `/pay`, ‏supplier מגיש מחירון — עם צילומי מסך. פרוטוקול מוכן: המסלולים המרכזיים כבר ממופים ב-`DEAD-ENDS-AUDIT` ממצאים 5–7, 11, 12. |

## 27. קליטת תעודת משלוח נשענת על "ההזמנה הפתוחה היחידה" — ההיוריסטיקה ש-`0077` פוסל לחשבוניות

| | |
|---|---|
| **מה** | ‏`private.resolve_delivery_note_order` (‏`0090`) בוחרת הזמנה בשלוש דרגות ראיה. הדרגה השלישית — "לספק יש בדיוק הזמנה פתוחה אחת" — היא בדיוק ההיוריסטיקה ש-`0077:1134-1141` פוסל **בשמו**: תעודה ששייכת להזמנה אחרת תיקשר להזמנה הפתוחה היחידה בכל פעם שיש כזו. **ארבע תעודות המשלוח שהמשתמש העלה ב-09.08.2026 לא נשאו מספר הזמנה כלל**, כך שזו אינה מקרה קצה תיאורטי אלא המסלול הצפוי. |
| **הכרעת הבעלים 09.08.2026** | הבעלים בחר במפורש "פריטים קודם, ואם לא הכריעו — הזמנה פתוחה יחידה", אחרי שהוצג לו ש-`0077` פוסל את הדרגה הזאת לחשבוניות. קבלת סיכון מפורשת. |
| **מה מגודר** | התוצר הוא **טיוטה** בלבד: אפס תנועות מלאי, אפס שינוי ב-`received_qty` וב-`purchase_orders.status`, אפס בקשות זיכוי, אפס חשבוניות — ‏`p16` מצמיד את הטביעה הפיננסית כמחרוזת אחת ומשווה אותה אחרי **כל** תרחיש. הדרגה שהכריעה נשמרת ב-`order_matched_by`, מודפסת ללוג ה-Edge, **ומוצגת בכרטיס ההזמנה** עם "ההזמנה נבחרה כיחידה הפתוחה — כדאי לוודא". ביטול מנומק מוחק את הטיוטה ומחזיר את המסמך לתיבה; אחרי שאדם השלים את הקבלה הביטול מסרב. המדיניות `delivery_note.receiving` נשלחת **כבויה**. |
| **איפה ההוכחה** | `0090_automatic_delivery_note_receiving.sql` · `supabase/tests/p16_automatic_delivery_note_receiving.sql` · `OPEN-DECISIONS` #125–#126. |
| **הצעד הזול הבא** | למדוד: אחרי 30 טיוטות אוטומטיות אצל הדייר החי, לספור כמה מהן שויכו ב-`single_open_order` וכמה מאלה תוקנו ידנית להזמנה אחרת. ‏`delivery_note_interpretation_decisions.order_matched_by` כבר נושא את הנתון — השאילתה היא דוח, לא מיגרציה. אם שיעור התיקון גבוה, מבטלים את הדרגה השלישית ומשאירים שתיים. **הסף `0.900` למפתח החדש אינו מכויל, בדיוק כמו §16 ו-§24.** |

## 28. קבלה (`payment_confirmation`) עדיין ללא יעד אוטומטי — התלונה המקורית כללה אותה

| | |
|---|---|
| **מה** | המשתמש דיווח ב-09.08.2026 על "חשבונית **וקבלה**" מצילום שלא עובדו. הבדיקה מול הפרודקשן הראתה שארבעת המסמכים בפועל היו תעודות משלוח, והמשתמש אישר זאת — ולכן `0090` נבנתה לתעודת משלוח בלבד. ‏`payment_confirmation` ממשיך לעצור ב-`not_an_invoice` (`0077:976`), בדיוק כמו ה-`ADTV receipt` מ-07.08 שרשום כך בבסיס הנתונים. |
| **למה נדחה** | היעד העסקי של קבלה הוא רשומת תשלום, וכתיבת תשלום אוטומטית מזיזה יתרות ספק — הרבה מעבר לטיוטה שאינה נוגעת בכסף. זו הכרעה עסקית נפרדת שלא נשאלה. |
| **איפה ההוכחה** | `document_filings` בפרודקשן: `payment_confirmation` עם `reason_code = 'not_an_invoice'` · `model.ts` ‏`paymentConfirmationFacts` כבר מציג התאמה ידנית במסך הבדיקה. |
| **הצעד הזול הבא** | לשאול את הבעלים מה קבלה אמורה לעשות לבד — להתאים לתשלום קיים (קריאה בלבד, זול ובטוח) או ליצור רשומת תשלום (כתיבה כספית). התשתית של `0090` — מפתח מדיניות, טבלאות ledger, ביטול מנומק — היא תבנית מוכנה לנתיב רביעי. |
## 29. חשבונית שאושרה ואחר כך הועברה ל־investigation ממשיכה לצרוך כמות

| | |
|---|---|
| **מה** | `invoice_three_way_approval_snapshots` שומרת התחייבות היסטורית immutable. ה־assessment המצטבר משתמש ב־snapshot האחרון של כל חשבונית שאושרה; שינוי מאוחר ל־`investigation` אינו משחרר את הכמות בשקט. |
| **למה נשאר פתוח** | שחרור כמות הוא שינוי בחוזה פיננסי: צריך להכריע אם investigation לבדו מספיק, או שנדרשת פעולת reversal/תיקון/אישור מחדש ומה נכתב ב־audit. לא הומצאה פעולה כזאת. |
| **מה מגודר** | ברירת המחדל fail-closed: הכמות נשארת צרוכה, שני אישורים מקבילים ננעלים לפי ארגון+ספק, והמיפוי ההיסטורי אינו תלוי ב־SKU/barcode חי. |
| **הצעד הבא** | הכרעת owner ב־`OPEN-DECISIONS` #139 ולאחריה פקודת reversal סמכותית, מנומקת ואידמפוטנטית אם תידרש. |

## 30. מחיקת retention סופית לאחר offboarding עדיין דורשת job ציות ייעודי

| | |
|---|---|
| **מה** | החלטה #140 מומשה ב־`0103`: בקשת owner עם step-up, read-only מיידי, ביטול בתוך 30 יום, הפעלה מחדש עד 120 יום, ו־export מלא עמיד של CSV+JSON+מסמכי מקור באמצעות manifest וקישור מתווך בר־ביטול ל־7 ימים. אין עדיין job שמבצע purge סופי. |
| **למה נשאר פתוח** | מחיקה פיזית מרובת טבלאות/Storage/גיבויים דורשת הוכחת scope, legal hold, סדר שימור לפי סוג מידע ואימות שניתן export תקין. הרצה אוטומטית לפני שכל התנאים ניתנים לבדיקה הייתה מסוכנת יותר משימור fail-closed. |
| **מה מגודר** | offboarding אינו נועל מידע: צפייה וייצוא נשארים. אין מחיקה שקטה; הקישורים ניתנים לביטול, סודות אינם מיוצאים, וה־export מתחדש מאותה generation בלי לשכתב חלק שהושלם. הנתונים הפיננסיים נשמרים לפחות עד סוף שנת המס ועוד 7 שנים, security logs לפחות 24 חודשים, ו־`legal_hold` חוסם purge. |
| **הצעד הבא** | לממש retention executor מפורש שמפיק dry-run, דורש אישור Platform Admin, מאמת export וגיבוי, מטפל בגיבויים לפי תקנה 15, ומבצע מחיקה/אנונימיזציה בקטעים עם audit ויכולת עצירה. |

## 31. lease של העלאת תמונת קבלה offline אינו מתחדש במהלך upload ארוך במיוחד

| | |
|---|---|
| **מה** | claim אטומי ב־IndexedDB מונע משני טאבים להתחיל אותה תמונה, אך ה־lease קצוב ל־15 דקות ואינו מקבל heartbeat בזמן העלאה פעילה. upload שחורג מכך עלול להיתבע מטאב אחר. |
| **מה מגודר** | `clientUploadId` קבוע מייצר אותו object key בכל retry, update/delete דורשים את owner ה־lease, וה־API נשאר אידמפוטנטי. לכן הסיכון השיורי הוא עבודה כפולה/מצב תור שדורש התאוששות, לא שני מסמכים פיננסיים שונים. |
| **למה לא חוסם** | העלאת תמונה רגילה קצרה משמעותית מהחלון; crash/tab-close מתאוששים בכוונה לאחר expiry. זה P2 תפעולי ולא חולשת tenant/finance. |
| **הצעד הבא** | renewal תקופתי בעל ownership check ותרחיש browser של upload שנמשך מעל חלון ה־lease. |

---

## מה אין כאן

- **‏`0071`–`0072` "מוקצות ובלתי-מנוצלות"** — ‏`0071` **נוצלה בגל 10** (שער ה-grant האחרון). ‏`0072` נשארת מוקצית וריקה. **מספר מיגרציה אינו מכסה שיש למלא** (#103).
- **מצב כהה** — לא חוב. ‏`docs/adr/0002-no-dark-theme.md` — לא ייבנה.
- **‏MFA** — הוכרז out-of-scope בגל 4; המסמכים אינם מבטיחים אותו.
- **‏ESLint / Prettier** — לא בריפו במכוון. השער הוא `npm run build`.
