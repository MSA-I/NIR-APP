# SupplyFlow — תוכנית יסודות Enterprise

> **מסמך עוגן.** כאן נקבע מה המצב היום, מה חסר, מה מותקן, מה רק מוכן דרך מתאם, ומה נדחה במפורש.
> המסמכים הנלווים: [`ENTERPRISE-SECURITY-MODEL.md`](ENTERPRISE-SECURITY-MODEL.md) ·
> [`ENTERPRISE-UI-PLAN.md`](ENTERPRISE-UI-PLAN.md) · [`INTEGRATION-ARCHITECTURE.md`](INTEGRATION-ARCHITECTURE.md) ·
> [`OFFLINE-SYNC-DESIGN.md`](OFFLINE-SYNC-DESIGN.md) · [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md) · [`adr/`](adr/)

---

## 1. הכיוון

המערכת נבנית כך שתוכל לשרת ארגונים גדולים — הרבה סניפים, מחסנים, ספקים, מוצרים, מסמכים, חשבוניות,
משתמשים ואינטגרציות. **זה אינו שכתוב.** רכיב עובד אינו מוחלף רק משום שקיימת ספרייה אחרת; הוא מוחלף
או מורחב רק כשיש רווח טכני או רווח בקנה מידה שאפשר לנמק.

**מה שאינו נתון למשא ומתן:** החוויה העברית RTL · התהליכים העסקיים הקיימים · בידוד הדיירים ·
`service_role` לעולם לא בדפדפן · פעולה פיננסית נאכפת בשרת (RLS, אילוצי DB, RPC מאובטח או Edge Function) ·
הסתרת UI אינה גבול אבטחה · כל טבלה דיירית חדשה נושאת סקופ ארגוני ו-RLS · כל פעולה מיוחסת ניתנת לביקורת ·
כל פעולה חיצונית הניתנת לניסיון חוזר היא אידמפוטנטית · אין העתקת קוד GPL/AGPL/קנייני/source-available.

---

## 2. המצב היום — מדוד, לא משוער

> **הטבלה נמדדה בגל 0 (04.08.2026) ואינה נמדדת מחדש בכל גל.** שני תאים תוקנו מאז כי סתרו מדידה
> מאוחרת יותר: ספירת המיגרציות (גלים 1b–4 הוסיפו `0053`–`0061`; הבאה בתור `0062` לפי
> `02-MIGRATION-ALLOCATION.md`) וספירת צמדי (טבלה, חוקה) — המדידה של גל 3
> (`ENTERPRISE-SECURITY-MODEL.md §1`) מצאה **118** לפני `0054` (‏109 ב-`public`, ‏9 ב-`storage`);
> "172" המוקדם לא שיקף מדידה.

| ממד | מספר |
|---|---|
| מיגרציות | בגל 0: ‏52 (`0001`–`0052`) · אחרי גל 4: ‏61; הבאה בתור `0062` |
| טבלאות יישום | ~55 |
| שורות `create policy` | צמדי (טבלה, חוקה) נבדלים: **118** לפני `0054`, ‏120 אחריה (מדידת גל 3) |
| Edge Functions | 6 |
| נקודות קריאה ל-`.rpc(` בלקוח | 68, ב-55 שמות נבדלים |
| טריגר audit גנרי | מחובר ל-23 טבלאות |
| סוויטות SQL בשער | 13 (~460KB) |
| תרחישי דפדפן / טענות P0 | 22 / 266 |
| בדיקות יחידה | קובץ אחד (`node --test`) |

**מה כבר עובד ואין לבנות מחדש:** צנרת OCR מלאה עם אישור אנושי חובה (`0045`–`0052`) · פורטל ספקים
והגשות מחירון immutable · פקודות פיננסיות אטומיות עם נעילה, סיבה ו-audit באותה עסקה · N:M להקצאות ·
יתרות מחושבות · מסירת WhatsApp עם webhooks ו-guards · Web Push · חיפוש גלובלי · גלריית מסמכים ·
סביבת סקירת מסמכים עם שכבת bbox אינטראקטיבית לתמונות.

### 2.1 ארבע הנחות שהופרכו באימות ישיר

1. **אין מצב כהה.** אפס מופעים של `dark:` / `prefers-color-scheme` / `data-theme` ב-`src`.
   `DESIGN.md` מגדיר ערכה אחת. **החלטה: לא נבנה** (ראה `OPEN-DECISIONS.md` #79).
2. **סביבת סקירת המסמכים כבר קיימת** — `src/components/document-review/`, שישה רכיבים. תמונות מקבלות
   שכבת bbox מלאה עם מסלול מקלדת נגיש. הפער האמיתי: **PDF מרונדר ב-`<iframe>` חשוף**
   (`DocumentSourceViewer.tsx:133`), ולכן בורר העמוד (`:150-154`) מסנן רק את שכבת החילוץ ואינו מזיז
   את התצוגה — במסמך רב-עמודי המקור החזותי והעמוד הנבחר מנותקים.
3. **חיפוש גלובלי כבר קיים** — `global_search()` (`0011:113`, `security invoker` בכוונה, pg_trgm+ILIKE
   כי לעברית אין מילון PostgreSQL) מאחורי `GlobalSearch.tsx`.
4. **PWA כבר קיים** — אך `public/sw.js` מצהיר במפורש שאין בו מטמון: *"a cached stale balance is a wrong
   balance — the network is the source of truth, always."* תמיכת offline היא **חריג מתוחם** להחלטה הזו,
   לא ביטולה. ראה `OFFLINE-SYNC-DESIGN.md`.

### 2.2 מה באמת חסר

אפס `domain_events` / `integration_outbox` / `idempotency_keys` / `webhook_subscriptions` גנרי ·
אפס `docs/adr/` (עד מסמך זה) · אפס ESLint · אפס runner בדיקות מלבד `node --test` על קובץ אחד
(‏Vitest נוסף בגל 1) · **אפס CI** (`.github/` אינו קיים).

*נסגר מאז גל 0:* עץ היחידות (`org_units`, גל 3, ‏`0054`) · ‏feature flags (גל 4, ‏`0059`) ·
תשתית SSO — סכימה ופקודות בלבד, בלי מסלול התחברות (גל 4, ‏`0060`). ‏MFA נשאר לא-קיים במוצהר
(‏OPEN-DECISIONS ‏#88).

---

## 3. הפער שקובע את הסדר — השליפה, לא הטבלה

כל מסך רשימה טוען היום את **הטבלה כולה** ללקוח דרך `fetchAll` (`src/lib/supabasePaging.ts:7`), שמלפף
`.range()` בדפים של 1000 עד שנגמר, ורק אז מסנן/ממיין/מעמד בזיכרון (`Invoices.tsx:64-68`,
`DocumentsInbox.tsx:293-296`, `AttachmentsPanel.tsx:54-61`). ב-`DocumentsInbox.tsx:333-334` כבר רשום
TODO מפורש: *"Move filters themselves server-side when client filtering becomes slow."*

בנוסף **אין מטמון בכלל**: `useQuery` (66 שורות) מחזיק state ב-`useState` פר-מופע, כך ששני רכיבים
שקוראים לאותה שאילתה שולפים פעמיים. אין key, dedup, `staleTime`, `enabled` או retry. ביטול-תוקף בין
רכיבים נעשה היום ב-DOM CustomEvents ובמאזיני `focus`.

**לכן שכבת השליפה קודמת לכל שאר הפאזות** — היררכיית סניפים מעל מסך שמוריד 50,000 שורות לדפדפן אינה
פותרת דבר.

---

## 4. הגלים

| גל | תוכן | DB | מיגרציות |
|---|---|---|---|
| 0 | ביקורת ותיעוד (המסמך הזה ונלוויו) | ✗ | — · **הושלם** `140f650` |
| 1 | רתמת בדיקות · יסוד מטמון + מעטפת תאימות | ✗ | — |
| **1b** | **שליפה בצד שרת** | **✓** | **0053** |
| 2 | מנוע טבלה ארגונית | ✗ | — |
| 3 | **היררכיית ארגון + RLS מסוקף + בורר סקופ** | ✓ | 0054–0058 |
| 4 | Feature flags + זהות ארגונית (תשתית SSO/step-up; ‏MFA אינו בתוכנית — ‏#88) | ✓ | 0059–0061 |
| **4b** | **מזהי קורלציה מקצה לקצה** | **✓** | **0062** |
| 5 | אירועי דומיין + transactional outbox | ✓ | 0063–0064 |
| 6 | PDF בסקירת מסמכים | ✗ | — · **מקביל לגל 5** |
| **6b** | **Upload Center + tus** | **✓** | **0065** |
| 7 | מתאמי אינטגרציה | ✓ | 0066 |
| 8 | קליטת סחורה offline + פיילוט ברקוד | ✓ | 0067 |
| 9 | **העדפות התראה · שער סוגי-החיפוש · מדיניות אישור (סכימה) + קורא המעברים** — ‏workflow, מנוע חוקים ו-report jobs **נדחו במפורש** (‏#103) | ✓ | **0068–0070** · ‏0071–0072 **מוקצות ובלתי-מנוצלות** |
| 10 | ביקורת אבטחה, מדידות ביצועים, נגישות, דוח | ✗ | — |

> **ההקצאה הסמכותית של מספרי המיגרציה היא `02-MIGRATION-ALLOCATION.md` בחבילת התוכניות**, לא הטבלה
> הזו. תוכנית אינה בוחרת מספר — היא מקבלת אותו משם.

**סטייה מכוונת אחת מסדר המימוש שהוגדר בבריף:** ההיררכיה מוקדמת אל לפני feature flags ואירועי הדומיין.
הבריף עצמו מגדיר ש-flags מתמקדים לפי `branch`/`warehouse` ושכל אירוע נושא *"relevant scope IDs"* —
בנייתם לפני שהסקופ קיים היא בנייה כפולה. הבריף מתיר התאמה כזו במפורש.

**שלוש חלוקות שנוספו בביקורת אדוורסרית, ומתקנות טענות מוקדמות של המסמך הזה:**

1. **שליפה בצד שרת היא גל בפני עצמו (1b), והיא כן דורשת מיגרציה.** ‏גרסה מוקדמת של המסמך קבעה
   ש"גל 2 נשלח ללא מיגרציה" — **זה היה שגוי.** ‏`searchFn` ו-`sortValue` הם JS שרירותי, ופילטרים
   כמו `attention` בחשבוניות מחושבים בזיכרון; עימוד שרתי בלי predicates ואינדקסים שרתיים היה הופך
   כל אחד מהם לפועל **על דף אחד בלבד** — כלומר "5 חשבוניות" כשבפועל 40 תואמות. **מספר שגוי במסך
   כספי הוא מחלקת הכשל הגרועה ביותר כאן.**
2. **מזהי קורלציה עוברים לפני גל האירועים (4b).** אחרת `domain_events.correlation_id` הוא backfill
   של `NOT NULL`.
3. **‏Upload Center מופרד מ-PDF (6b) והוא כן נוגע ב-DB.** ‏`0048` מקציב להזמנת ההעלאה **15 דקות**
   (`:222`), מטאטא אותה (`:353`), ובודק את תוקפה **בתוך מדיניות ה-INSERT של Storage** (`:303,463`) —
   ולכן חידוש tus אחרי ניתוק ארוך נדחה, ונדרש RPC לחידוש הזמנה.

**מה שנשאר נכון:** "תצוגות שמורות" נדחות לגל ההיררכיה כי הבריף מסקף אותן לפי `optional branch` ודורש
audit; עד אז העדפות עמודה ב-`localStorage` — **זמני ומתועד**, לא מוצג כתכונה ארגונית
(`OPEN-DECISIONS.md` #80).

---

## 5. מה מותקן מול מה שרק מוכן דרך מתאם

| יכולת | הכרעה |
|---|---|
| מטמון נתונים | **מותקן** — TanStack Query |
| מנוע טבלה | **מותקן** — TanStack Table, בתוך `DataTable` הקיים |
| רתמת בדיקות | **מותקן** — Vitest + MSW + Testing Library |
| מציג PDF | **מותקן** — react-pdf / pdfjs-dist |
| העלאה מתחדשת | **מותקן** — tus-js-client (אחרי spike מול מדיניות ה-Storage) |
| offline + PWA | **מותקן חלקית** — Workbox/IndexedDB רק למסלול קבלת הסחורה |
| ברקוד | **מותקן מאחורי flag** — ZXing Browser |
| feature flags | **פנימי** — מגובה Supabase, תואם OpenFeature ברעיון; אין שירות חיצוני |
| ספק חיפוש | **מתאם** — המימוש הוא PostgreSQL FTS/trigram הקיים. Meilisearch לא נפרס בלי מדידה. ‏גל 9 (`0069`) העביר את **שער סוגי-התוצאה** לשרת; ‏pg_trgm ללא שינוי |
| מנוע workflow | **נדחה בגל 9 (‏#103), לא "מתאם שקיים".** שש מכונות המצב החיות הן המימוש; מה שנמסר הוא `read_allowed_transitions()` (‏`0070`) — הגרף כדאטה, במקום מטריצה משוכפלת בדפדפן. Temporal לא נפרס |
| מנוע חוקים | **מתאם** — פונקציות PostgreSQL ורשומות חוק מובנות. לא בדפדפן. ‏גל 9 מסר סכימת **מדיניות אישור + מעריך, אפס צרכנים** (`0070`); חיווט = הכרעת #2 מחדש |
| ספק התראות | **מתאם** — המימוש הוא מערכת ההתראות הקיימת. Novu לא נפרס. ‏גל 9 (`0068`) הוסיף **העדפות פר-משתמש** שמסננות מסירה בלבד |
| ERP / חשבונאות / WMS | **מתאם + mock בלבד** — אין קוד ספק |
| ספק חילוץ מסמכים | **מתאם** — המימוש הוא `gpt-5.6-terra` הקיים + שכבת QA |
| ספק זהות | **מתאם** — דפוסי Supabase; SSO ארגוני מאחורי flag |
| read replica | **הפשטת ניתוב בלבד** — לא מופעל בלי מדידה |

---

## 6. שינויי מסד, סיכונים ו-rollback

**עקרון:** כל מיגרציה היא צעד אחד שאפשר לעבור עליו בשער בנפרד. הרחבה לפני כיווץ; ברירת מחדל תואמת
לאחור לפני אכיפה.

- **גל 3 הוא הסיכון הגבוה ביותר בכל התוכנית.** `auth_org()` נשארת **ללא שינוי** כדי שאף אחד
  מ-118 צמדי (טבלה, חוקה) הקיימים לא יישבר (המדידה — `ENTERPRISE-SECURITY-MODEL.md §1`; "172"
  המוקדם לא שיקף מדידה); `auth_scopes()` נוספת לצדה. חלוקת התפקידים כפי שמומשה בפועל: ‏`0054`
  אוצר-מילים + **טריגרי הזרעה** (לא backfill חד-פעמי — ‏`[db.seed]` רץ אחרי המיגרציות, ו-backfill
  חד-פעמי היה משאיר את ה-seed, הדמו וכל פיקסטורה בלי שורש); ‏`0055` עמודות ואינדקסים; ‏`0056`
  ‏backfill לשורות שקדמו ל-`0054`; ‏`0057` אכיפה (רוכבים + ‏A1/A3/A5) **עם טענה שמוכיחה שלא
  נשכחה טבלה**; ‏`0058` תצוגות שמורות. זהו התנאי לכך שכל 22 תרחישי הדפדפן, 266 טענות ה-P0 והמסע
  המשולב ימשיכו לעבור ללא שינוי.
- **rollback:** מיגרציה שמוסיפה טבלאות ועמודות nullable ניתנת לנטרול בהסרת החוקות החדשות בלי לגעת
  בנתונים. מיגרציה שהופכת עמודה ל-`NOT NULL` או משנה חוקה קיימת **אינה** נסיגה זולה — לכן היא תמיד
  צעד נפרד ומאוחר, אחרי שה-backfill אומת.
- **גיבוי לפני כל מיגרציה בפרודקשן**, כפי שנעשה לפני `0052`. הסתייגות שנרשמה שם עומדת: אומת שקובצי
  הגיבוי מכילים את הנתונים; **לא אומת שהם נטענים בחזרה**. אין לטעון שהגיבוי עובד עד שבוצעה בדיקת שחזור.

### 6.1 ⚠️ חוזה תחזוקת השער

**אין CI.** השער הוא ריצה ידנית של PowerShell + Docker, ובתוכו קבועים מקודדים שנשברים בשקט:

| מה | היכן | הפעולה |
|---|---|---|
| `p1_preflight.sql` חייב להחזיר בדיוק **39** שורות (38 עד גל 6; 33 עד גל 4; 28 עד גל 3; 20 היה נכון לגל 0) | `check-quality-gates.ps1` — שלושת הליטרלים ב-`Invoke-Preflight` (התווית, בדיקת הספירה והודעת המעבר) | בדיקה שנוספה/הוסרה → לעדכן את **שלושת** המספרים |
| רשימת 24 נתיבי ה-OCR המקודדים (`$requiredFiles`) | `Assert-OcrPrerequisites`, ‏`:665-690` | קובץ שנוסף/הועבר → לעדכן |
| מפת `$functionJwt` | `:696-701` | **Edge Function חדשה אינה מכוסה** |
| `Wait-LocalEdgeReady` קודים `400/401/401` | `:432` (ההשוואה ב-`:441`) | פונקציה חדשה → לעדכן |
| `demo_seed.sql` | `supabase/demo/` | עמודת `NOT NULL` בלי default שוברת את ה-seed |
| `demo_verify.sql` | `supabase/demo/` | **טבלה דיירית חדשה אינה מכוסה בטענות בידוד אלא אם נוספה במפורש** — הכשל השקט המסוכן ביותר |
| `audit_row_change()` על טבלה בלי `org_id` | `0009:184-193` | זורק בשם בזמן המיגרציה — התנהגות רצויה |

---

## 7. השלכות ביצועים

- `auth_scopes()` תיקרא בתוך RLS על כל שורה. היא חייבת להיות `stable`, מוגבלת לסקופים של המשתמש
  הנוכחי, ולהיתמך באינדקס על עמודת הסקופ בכל טבלה מסוקפת. עלות ההרחבה נמדדת מול הסט הסינתטי לפני שהיא
  נחשבת מקובלת.
- שליפה בצד שרת מחליפה הורדת-טבלה-מלאה. את הספירות המדויקות יש להשאיר תואמות למשמעת הקיימת של
  `readExactCount` (`queryResult.ts`) — ספירה שאינה זמינה זורקת, ולעולם לא מוצגת כאפס.
- וירטואליזציה מופעלת רק היכן שנמדד צורך, לא כברירת מחדל.
- **אין להצהיר על מוכנות ארגונית בלי בדיקות, מדידות ומגבלות מתועדות.** נתונים סינתטיים בקנה מידה
  (הרבה סניפים/ספקים/מוצרים/מחירונים גדולים/יומני audit גדולים/הרבה מסמכים/חשבוניות/משתמשים מקבילים)
  נבנים בגל 10, והתוצאות נרשמות כמדידה ולא כהערכה.

---

## 8. החלטות נדחות

נרשמות ב-[`OPEN-DECISIONS.md`](OPEN-DECISIONS.md) עם ברירת מחדל מתועדת ומקום שינוי — לא כניחוש שקט
בקוד. הפתוחות הבולטות בכניסה לתוכנית זו: אכיפת `trial_ends_at` (#15, נשמר ואינו נאכף) · לוגו פר-דייר
(#24, לא מומש) · `bank_allocations` ללא אילוץ יעד-יחיד בשל 7 שורות היסטוריות (`0023:20-23`) ·
זיהוי ספק אוטומטי ממסמך (#77, בלתי אפשרי במסלול הניתן לאישור).

**מגבלה תפעולית שיש לומר במפורש ולא לעקוף:** Resend מוגדר עם `onboarding@resend.dev`
(`NIR-ROADMAP.md:28,50`), ולכן **הזמנות עובדים מגיעות היום רק לתיבת בעל החשבון**. אין להציג "הזמנות
עובדים עובדות" עד לאימות דומיין.
