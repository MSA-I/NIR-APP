# תוכנית — Report Jobs

נכתב: 23.08.2026. תוכנית מצומצמת לפי `DEBT-REGISTER.md` §3 ו־`OPEN-DECISIONS.md` #259.

**מצב:** `WITHDRAWN / NOT_BUILT` — **הכרעת בעלים 23.08.2026**

> **השער של הבעלים:** *Report Jobs מחכה לחוזי הדוחות הפיננסיים.*
> **מסקנת התוכנית:** השער אכן סגור — אבל **יש שער קודם לו שלא היה ידוע**: אין היום ולו דוח אחד
> שמצדיק תור אסינכרוני. ‏#259 מתיר תור ל"דוחות קיימים וכבדים"; המדידה מראה שהכבדים אינם כבדים
> מהסיבה שתור מתקן, ושהיחיד שכן — **כבר בנוי**.
>
> **הכרעת הבעלים על סמך הממצא: אין הצדקה — מוותרים.** ‏#259 אינו מבוטל כהכרעה עקרונית, אלא
> **נסגר כפריט פעיל**: אין לבנות תור דוחות עד שתימדד הצדקה. הפריט חוזר לשולחן רק אם מתקיים
> אחד משני התנאים ב‏§8.

## 0. מה עדיין תקף במסמך הזה אחרי הוויתור

הוויתור נוגע **לתור בלבד**. שלושה חלקים נשארים פעילים ואינם תלויים בו:

1. **§2.2 — שני ליקויי הכסף.** קיימים בין אם ייבנה תור ובין אם לא, ומשפיעים על **הדוח שהרו״ח מקבל.**
   **זהו הפריט הדחוף ביותר במסמך.**
2. **§2.4 + `PLAN-tenant-operational-readiness-20260823.md` §5.5 — סדר המיזוג.** ‏`0172`/`0173`/`0174`
   מתקנות גופי פונקציות לפי עוגני מחרוזת; מיזוג מחוץ לסדר מפיל אותן ב‏`apply`.
3. **§3 — חוזה הדוח בלי עמודות.** נשאר עובדה ארכיטקטונית מתועדת, וכל דיון עתידי בדוחות מתחיל ממנה.

**§2.3 הוכרע:** מחיקה קשה ב‏`0171:356-358` — **מאושררת על ידי הבעלים 23.08.2026.** הנימוק מתקבל:
מטרת `0171` היא לא לשמר מספר חשבון גולמי, והאודיט שומר `{shape, country_code, last_four, fingerprint}`
וסיבה. אין לרשום זאת כחריגה פתוחה מכלל "מחיקה רכה"; זו החרגה מנומקת ומאושרת.
**‏`0171:124-127` הוכרע 23.08.2026 (בעלים): להוסיף שורת `audit_logs` אחת מסכמת** לפעולת ה‏backfill —
סיבה מפורשת וספירת השורות שאופסו, בטרנזקציה של המיגרציה. לא שורה לכל ספק: הפעולה היא **מיגרציה
אחת**, ו‏N שורות היו מרעישות את יומן הביקורת בלי להוסיף מידע. הנתונים כבר נשמרים ב‏
`private.supplier_bank_detail_migration_queue` לפני האיפוס עם טענת ספירה — כלומר זו **הוספת עקבה,
לא שינוי התנהגות**.

---

## 1. השער הקודם: אין דוח שמצדיק תור

### 1.1 כל דוח במוצר נבנה בדפדפן

| דוח | מיקום | תפוקה |
|---|---|---|
| דוח חודשי לרו״ח | `src/pages/Reports.tsx:197-237`, בונה `src/lib/monthlyReport.ts:146-190` | `.xlsx`, ‏5 גיליונות, `XLSX.writeFile` בדפדפן |
| דוח סופי נעול — הורדה | `Reports.tsx:239-252`, ‏`monthlyReport.ts:220-360` | `.xlsx` מתוך snapshot שכבר נטען |
| ריכוז הוצאות | `src/pages/Expenses.tsx:186-233` | `.xlsx` |
| סיכום רכישות מוצרים | `src/pages/ProductPurchaseSummary.tsx:119-163` | `.xlsx` |
| תבנית רו״ח מותאמת | `src/lib/reportTemplateExport.ts:160-205` | חוברת הדייר עם תאים ממופים |
| הדפסה / PDF ×5 | `Reports.tsx:343`, ‏`Expenses.tsx:273`, ‏`Orders.tsx:290,421`, ‏`InvoiceDetail.tsx:280`, ‏`Invoices.tsx:325` | דיאלוג הדפסה — **המוצר אינו מייצר ארטיפקט** |

**אין במוצר דוח שמייצר PDF או קובץ בצד השרת.** הארטיפקט היחיד שנוצר בשרת הוא ייצוא offboarding.

### 1.2 שני המועמדים, ושניהם נופלים

| מועמד | למה כבד | למה לא לבנות עכשיו |
|---|---|---|
| **ייצוא offboarding לדייר** (`0103` + `supabase/functions/tenant-export/`) | מעתיק כל שורה בכל טבלה + כל אובייקט בשלושה דליים (`0103:3025,3084,3934`); ארטיפקט רב־חלקי עם checksum פר־חלק | **כבר בנוי במלואו** — סטטוסים `export_building/ready/failed` (`0103:36-39`), מפתחות אידמפוטנטיות (`0103:40-41`), resume (`tenant-export/index.ts:45-46`), retry למפעיל (`Admin.tsx:157`). יעד **שימוש חוזר**, לא בנייה |
| **`create_monthly_report_snapshot`** (`0074:201`) | ‏`INSERT` יחיד עם **14 CTE ב־`materialized`** (`0074:256-594`), מכוון כ־snapshot בפקודה אחת (`0074:252-255`), מאחורי `pg_advisory_xact_lock` (`0074:233`) | **לא נמדד מעולם**, ‏**אינו מייצר קובץ**, ו‏#259 אוסר להמציא לו ארטיפקט. הפיכתו לג'וב היום היא ה־refactor הספקולטיבי ש‏#260 פוסל |

### 1.3 מה שכבד — כבד מסיבה שתור לא מתקן

| משטח | עלות | סיבה אמיתית |
|---|---|---|
| דשבורד | ‏**≥11 סבבי רשת** לטעינה: ‏9×`fetchAll` + RPC + `count:'exact'` head (`Dashboard.tsx:479-508`) | N+1. ‏DEBT §1, וצעדו הבא הוא מילולית **"למדוד latency"** (`DEBT-REGISTER.md:77`) |
| טעינת הדוח החודשי | ‏≥10 סבבים, ‏`invoice_balances` ב־chunks של 150 מזהים (`Reports.tsx:128-131`) | N+1 |
| ריכוז הוצאות | שתי מניפות chunk מקוננות (`Expenses.tsx:141-155`) | N+1 |
| `supplier_metrics` | חלון `lag()` על **כל** ה־`price_history` של הארגון, בלי חסם תאריך (`0031:355-362`) | חסם/אינדקס חסר |
| `global_search` · מסנן כפילויות · `count:'exact'` | ‏DEBT §13 · §14 · §15 | אינדקס חסר · ספירה לא־חסומה |

**תור מזיז את הכאב לעובד; הוא אינו מסיר אותו.** אף אחד מארבעת פריטי החוב האלה אינו נפתר על ידי ג'וב.

### 1.4 אין ולו מדידה אחת של דוח

כל המספרים המדודים בריפו הם על צנרת ה־OCR: ‏99 שניות (`PROGRESS.md:1057`), ‏75.1 (`:1059`),
‏33.9–45.1 (`:1105`), ‏305 ו־370 (`:1108-1109`), ‏745 חציון (`DEBT-REGISTER.md:249`).
**אפס מדידות של דוח או אגרגט.** ‏#260 קובע: איחוד רק לאחר מדידה, ורק לקריאות יקרות מוכחות.

---

## 2. השער שהבעלים ציין: חוזי הדוחות הפיננסיים

### 2.1 מצב החוזים

שש מיגרציות יושבות ב־`codex/financial-data-contracts-20260823`, **כולן לא־מקומיטות ולא־מוחלות**.
ראש הפנקס בייצור הוא `0170`.

| # | מה מגדיר | שלמות |
|---|---|---|
| `0171_financial_bank_contracts` (‏692 שורות) | `supplier_bank_accounts`, ‏views מוסתרי־4־ספרות, ‏`update_supplier_bank_details(uuid,jsonb,text)`, חוזה ייבוא XLSX מוצמד | שלמה — ‏RLS+force, רשמי scope/export, בדיקה עצמית בת 7 סעיפים |
| `0172_approved_credit_note_intake` | קליטת זיכוי ממסמך, resolver, טלאים מעוגנים ל־`get_document_review_assessment` ו־`apply_reviewed_document` | שלמה — ‏8 עוגנים שזורקים אם הגוף המארח זז |
| `0173_partial_credit_allocations` | `credit_request_balance_rows()`; **משכתב את `credited` ב־`p0_invoice_balance_rows()` מסטטוס־מחזור ל־`sum(payment_allocations.amount)`** | שלמה |
| `0174_invoice_approval_consumption_reversal` | ביטול צריכת אישור תלת־כיווני, חריג חד־פעמי בשומר האי־שינויות | שלמה |
| `0175_legal_entity_audit_scope` | טקסונומיית scope ל־`audit_logs` (‏#255), ‏`audit_log_read_model` בן 15 עמודות | שלמה |
| `0176_supplier_metrics_90_days` | ‏`supplier_metrics` ב־17 עמודות, חלון 180→90 יום | שלמה |

מגובות בשלוש סוויטות — `p62`, ‏`p63` (כולל מרוץ `dblink` דו־סשן אמיתי), ‏`p64`.

### 2.2 שני ליקויי כסף ש‏`0173` יוצר והגל לא תיקן — **חוסמים מיזוג, לא רק Report Jobs**

1. **זיכוי חלקי נספר בפנקס ונעדר מהדוח לרו״ח.**
   אחרי `0173:64-71` הסכום המזוכה הקנוני הוא `sum(payment_allocations.amount)`, ו־`0173:172-187`
   משאיר זיכוי שנוצל חלקית בסטטוס `received`. אבל `src/lib/reportTemplateExport.ts:38-40` עדיין
   מסנן `status === 'offset' || status === 'closed'`.
   **התוצאה: `invoice_balances.credited_amount` סופר את הזיכוי, והדוח החודשי לרו״ח מחסיר אותו.**
   הקובץ הזה אינו בדיף כלל.
2. **`0176` מנפח יתרת זיכוי פתוחה.**
   `0176:51-53` — ‏`open_credits_amount` סוכם את `cr.amount` **המלא** עבור `('open','requested','received')`.
   אחרי `0173`, זיכוי שנוצל חלקית נשאר `received`, כלומר הסכום כולל את החלק שכבר הוקצה.
   ‏`0176` שכתב בדיוק את ה־view הזה ולא תיקן זאת.

### 2.3 שני חריגים מכללי הברזל ב‏`0171`

- **`:356-358` — מחיקה קשה** של שורת `supplier_bank_accounts` כשמעדכנים ל־null; לטבלה אין `deleted_at`.
  יש נימוק אמיתי (מטרת `0171` היא לא לשמר מספר חשבון גולמי), והאודיט שומר `{shape, country_code, last_four, fingerprint}` וסיבה. **הכרעת בעלים, לא מעבר ברור.**
- **`:124-127` — `update suppliers set bank_details = null`** על כל השורות **בלי שורת audit**. הנתונים
  נשמרים בתור פרטי קודם עם טענת ספירה, כלומר ניתנים לשחזור, אך המוטציה עצמה אינה מתועדת.

### 2.4 סיכון רצף חמור יותר ממספור

`0172`, ‏`0173` ו־`0174` **מתקנות גופי פונקציות חיות לפי עוגני מחרוזת** (`0172:145-147,172-174,179-181`;
‏`0173:73-75,90-92`; ‏`0174:65,73,81`). כל מיזוג מחוץ לסדר שנוגע ב־`apply_reviewed_document`,
‏`execute_payment_request`, ‏`p0_invoice_balance_rows` או `invoice_three_way_raw` — **מפיל אותן בזמן apply**.
זו תלות רצף אמיתית, לא רק מספור.

---

## 3. השער האמיתי: אין חוזה דוח עם עמודות

זהו הממצא שסוגר את Report Jobs גם אחרי שהחוזים הפיננסיים ייסגרו.

**יש חוזה בעל שם וגרסה:**
- שלושה מפתחות דוח מוצמדים ב־CHECK: `('accountant_monthly_report','owner_expense_summary','product_purchase_summary')` — ‏`0123:47-58`, ‏`0126:156-159`
- גרסאות אמיתיות: `document_export_template_versions` תחת advisory lock (`0126:167-196`), שומר אי־שינויות עם עדכון אחד חוקי לפני אישור (`0126:223-249`)
- `read_active_export_template` מחזיר `{template_id, version_id, version, contract, workbook_path, workbook_bytes}` (`0126:332-352`) — **מספיק לג'וב להצמיד template+version+hash, בדיוק כפי ש‏#259 דורש**

**ואין לו עמודות — במכוון.** ‏`0126:20-21` אומר מילולית: החוזה הוא
`{schema_version, name, format, scope:{export_key}}` — *"אין עמודות. עמודות של דוח מוגדרות על ידי הדוח."*
נאכף ב־`document_export_template_contract_valid` (`0126:60-62`) וחוזר בהערת הפונקציה (`0126:80-82`).

- קבוצת העמודות קיימת **רק ב־TypeScript**: `EXPORT_DEFINITIONS` ב־`src/lib/exportTemplates.ts:50-91`
- הערכים מחושבים **בדפדפן**: `reportTemplateExport.ts:26-52`, ‏`:61`, ‏`:77`, וטיפוס ההחזרה הוא
  `Record<string, string|number|null>` (`:6`) — שק placeholder לא־מוקלד, לא צורת שורה קבועה
- מקור הנתונים הוא `select('*')` בתוך הדף: `Reports.tsx:104-114`

**עובד ג'ובים יוכל לפתור *איזו* תבנית וגרסה — אבל אין פונקציית שרת שמפיקה את שורות הדוח.**
הוא ייאלץ לממש מחדש את `reportTemplateExport.ts`, וזה בדיוק מה ש‏#259 אוסר: *"אינו ממציא דוחות או שדות חדשים."*

**הפונקציה היחידה בריפו שהיא דוח בצורת שרת:** `p2_business_summary_rows() → (metric_key, value, measured)`
(`0165:33-34`) — והיא `SECURITY INVOKER` באפס פטורים.

---

## 4. ארבעת התחומים ש‏§3 דורש — תקדים לכל אחד

### 4.1 Worker

**לשימוש חוזר מילולי** (`0129_document_processing_claim_circuit_breaker.sql`):
- שער `service_role` בלבד → `42501` (`0129:40-42`)
- ‏`loop` + `for update skip locked limit 1` (`0129:47,57-58`) עם `continue` לכל מועמד שנפסל
- מיון `priority desc, created_at, id` (`0129:56`) עם אינדקס חלקי תואם (`0045:434-436`)
- גדר דיירות/offboarding `private.organization_write_allowed_fenced(j.org_id)` (`0129:55`, מוגדר `0103:851`)
- **מפסק זרם:** תקרה `8` בפונקציה immutable מנוטרלת מכל תפקיד (`0129:7-17`); בהגעה — `failed` + שורת audit **בתוך אותה טרנזקציה** (`0129:99-124`)
- מסווג תקיעות: הבעלים הוא **`0132:37-65`** (לא `0130`) — הוא הוסיף חסד 5 דקות ל־lease
- לולאת עובד סדרתית עם backoff חסום ו־heartbeat (`worker/ocr/src/worker.py:751-808`), וכלל
  **שתיקת heartbeat אבוד** (`worker.py:716-720`) — עובד שאיבד בעלות אינו כותב לג'וב
- ‏retry/dead-letter/replay: `0064:25,81,337-351,377`

**לא לשימוש חוזר:** בלוק שחזור עדות ה־egress (`0129:64-96`) — ייחודי לקריאות LLM חיצוניות;
שדות מקור המסמך (`storage_path`, ‏`input_checksum`); הספים 2 שעות / 30 דקות — מכוילי OCR, לא SLA כללי.

**REUSE:** `0129:40-58,99-139` · `0132:37-65` · `worker.py:751-808` · `0064:337-351,377`
**חדש:** כיול ספי תקיעות לדוחות, **והכרעה איפה עובד הדוחות רץ** — היום אין מארח כזה.

### 4.2 Storage

- **כלל `{org_id}/` מאומת:** `0005_saas_hardening.sql:10-12,18-21` — ‏`(storage.foldername(name))[1]`
  נקרא מהנתיב ומושווה, **לעולם לא נסמך** (`0123:194-196`). ‏clamp עומק `array_length(...) = 1` (`0122:147`, `0123:154`)
- **תקדים מסירת ארטיפקט — `tenant-exports`:** דלי **בלי שום מדיניות דפדפן, במכוון** (`0103:813-814`).
  הזרימה: `createSignedUrl` ב־Edge עם `service_role` (`tenant-export/index.ts:1517-1521`) →
  **אימות מחדש של הקורא מול שורת הבקשה** (`:1524-1533`, ‏`404` אלא אם `true`) → audit, וכשל
  ב־audit מפיל את ההורדה ב־`503` (`:1535-1545`) → ‏`302` עם `no-store` ו־`no-referrer` (`:1547-1554`).
  חוזה מוצר: קישור חתום ל־7 ימים (`0103:7-8`)

**מלכודת שחייבת להיכנס לכל תוכנית — `0125_feedback_upload_policy_metadata.sql:1-67`:**
1. `metadata` הוא **null** כשמדיניות ה־INSERT רצה — תנאי `metadata->>'mimetype'` היה תמיד false,
   כלומר **כל העלאה בפיצ׳ר נדחתה, תמיד**. הפתרון: `storage.buckets.allowed_mime_types`
2. `owner`/`owner_id` נכתבים גם הם אחרי השורה — ‏`(null or null)`. **התנאי עדיין קיים בדליים אחרים** (`0122:149`, `0123:155`)
3. `insert ... returning *` דורש **מדיניות SELECT** — דלי עם INSERT בלי SELECT הוא דלי שאי אפשר
   לכתוב אליו כלל. מוצמד כטענה ב־`0125:139-143`

**REUSE:** `0103:801-814` · `tenant-export/index.ts:1517-1554` · `0005:18-21`
**חדש:** דלי `reports` אינו קיים; יש ליצור אותו **בלי מדיניות דפדפן** עם מינוי `service_role`.

### 4.3 Retention

**שמירה במוצר הזה = תאריכים רשומים וגבולות שנכשלים סגור, לא מחיקה.**
- ‏`0103:11-12`: *"purge הרסני אינו מנוחש. תאריכי שמירה תפעוליים, לוגי־אבטחה ופיננסיים נרשמים
  בנפרד כדי ש‏purge worker עתידי יוכל להיכשל סגור לפי מחלקת נתונים."*
- חמישה שעונים + `legal_hold boolean not null default true` (`0103:53-62`)
- **‏#261 (`OPEN-DECISIONS.md:611`):** אין purge עיוור. דוח מועמדים; ‏Platform Admin מאשר batch רק
  אחרי export מוכן, גיבוי, זכאות ובדיקת legal_hold. **"ההכרעה אינה מרשה purge כעת."**
- **ה־purge היחיד שעובד בכל המערכת** הוא של העוזר: `private.purge_assistant_history(90)`
  (`0164:1602`), מנוטרל מכל תפקיד כולל `service_role` (`0164:1667`), ב־pg_cron (`0164:1680`),
  והערך 90 מוצמד בתוך המיגרציה (`0164:1985-1987`)
- **אין ולו cron אחד שמשחרר אובייקט אחסון.**

**REUSE:** `0103:53-62,88-90` · `0164:1582-1592,1602,1680`
**חדש:** ‏**הכרעה פ-3** — האם ארטיפקט דוח מתיישן בשעון (מודל #181) או דורש אישור Platform Admin (מודל #261).

### 4.4 Permissions

- ‏`user_role` **קפוא** בשש ערכים (`0001_init.sql:5`); הנימוק — ‏77 חוקות RLS (`0006:6-8`).
  סמכות פלטפורמה היא **ציר נפרד**, לעולם לא תפקיד
- ‏`auth_org()` הוא **נקודת החנק היחידה** (`0006:79-86`). ‏`0006:46-64` מסביר למה תיקון 77 החוקות
  היה **לא־שלם**: ל־views מסוג definer אין RLS כלל והם מגנים על עצמם בקריאה ל־`auth_org()`,
  ומדיניות האחסון משווה `auth_org()::text`
- ‏`is_platform_admin()` (`0006:21-23`); חברות ניתנת **מחוץ למסלול בלבד** — המדיניות היחידה היא
  `select`, כדי ש־admin לא יוכל להנציח את עצמו (`0006:27-31`)
- **תקדים `platform_orgs()` (`0006:138-154`):** מחזיר **אגרגט בלבד**, והשער הוא `where` בגוף השאילתה
  ולא `raise` — *"לא־מפעילים מקבלים אפס שורות, לעולם לא שגיאה"* (`0006:152`). הנימוק (`0006:135-137`):
  מתן `SELECT` על `profiles` היה מוסר לכל מפעיל את רשימת העובדים של כל דייר, טלפונים כלולים
- **`0006:159-162`:** ל־Platform Admin יש `INSERT` בלבד על `audit_logs`, **לעולם לא `SELECT`** —
  *"כתיבה בלבד מספיקה לתעד פעולת מפעיל; קריאת הספרים של הדייר אינה חלק מהפעלת הפלטפורמה"*

**מי רשאי, לפי התקדימים הקיימים:**

| פעולה | קבוצת תפקידים | ראיה |
|---|---|---|
| הזמנת ג'וב | `owner`/`office`/`kitchen` | `0045:599-601` |
| צפייה בסטטוס | `owner`/`office`/`kitchen`; אגרגט תפעולי — **בעלים בלבד** | `0130:251-253`; `0130:58,341` |
| הורדת ארטיפקט | או מדיניות דלי המונה תפקידים (`0123:162-169`), או דלי חסר־מדיניות + מינוי Edge (`0103:813-814`) | — |

הקבוצה הרחבה ביותר שמדיניות דלי כלשהי העניקה אי־פעם היא `('owner','office','kitchen','accountant','payer')`
(`0005:20-21`) — ‏**`supplier` אינו מופיע בשום מדיניות אחסון בריפו.**

**REUSE:** `0006:21-23,79-86,138-154,159-162` · `0130:251-253` · `0123:162-169`
**חדש:** ‏**הכרעה פ-2** — האם `accountant`/`payer` רשאים להזמין או להוריד דוח. שניהם כשירי־קריאה
ב־`0005:20-21` אך אינם מופיעים בשום תקדים של הזמנה או סטטוס. **תוכנית אינה רשאית להרחיב בלי הכרעה.**

---

## 5. רשומת הג'וב — שש התכונות של ‏#259

הטבלה הקרובה ביותר: `public.document_processing_jobs` (`0045:398-428`).

| תכונה ב‏#259 | תקדים | מקור |
|---|---|---|
| **org** | ✅ | `org_id ... references organizations(id) on delete restrict` (`0045:400`) + אידיום `unique (org_id, id)` (`0045:420`) |
| **scope** | ❌ **אין** | לג'וב יש `document_id` — מצביע ישות יחידה, לא scope. אוצר המילים חי ב־`auth_scopes()` ובצורה המוצמדת `((unit_id IS NULL) OR (unit_id = ANY (auth_scopes())))` (`0057:20,139`) |
| **template** | ⚠️ חלקי | הקרוב הוא `document_export_template_versions` (`0123:130-146`). **אין טבלת ג'וב שמפנה לתבנית** |
| **version** | ✅ | `contract_version text not null default '1' check (= '1')` (`0045:406`) |
| **status** | ✅ | CHECK על `text` (לא enum, במכוון — `0136:80-89` מפיל ומוסיף מחדש), + שני אילוצי צורה ל־lease ולשגיאה (`0045:424-427`) |
| **hash** | ✅ כ־checksum | `input_checksum` עם CHECK צורה (`0045:405`), **נגזר בשרת** (`0045:610-612`) — ‏#72: *"eTag חסר חוסם enqueue; הלקוח אינו מספק checksum סמכותי"*. תקדים sha256 נפרד: `0103:69` |
| **retry אידמפוטנטי** | ✅ | אינדקס ייחודי חלקי מעל סטטוסים פעילים (`0136:91-93`) + לכידת `unique_violation` (`0045:614-647`) |

**חדש:** ‏`scope` ו־`template` בלבד. ‏`scope` חייב להתיישב עם צורת המדיניות המוצמדת ב־`0057:139`.

---

## 6. הכרעות — מצב אחרי 23.08.2026

| # | שאלה | מצב |
|---|---|---|
| **פ-1** | האם בכלל לבנות Report Jobs, בהינתן §1 | **הוכרע: לא.** אין הצדקה — מוותרים |
| **פ-2** | האם `accountant`/`payer` רשאים להזמין או להוריד דוח | **מוקפא** עם פ-1 |
| **פ-3** | מודל שמירה לארטיפקט דוח: שעון (#181) או אישור batch (#261) | **מוקפא** עם פ-1 |
| **פ-4** | האם `create_monthly_report_snapshot` יימדד | **נשאר פתוח כפריט זול.** מדידה אחת סוגרת את השאלה אם אי־פעם תהיה הצדקה, ומונעת ויכוח חוזר |
| **פ-5** | האם החוזה בעל־העמודות עובר ל־SQL | **נשאר פתוח.** אינו תלוי בתור — הוא שאלה על מקור האמת של דוח, וגם §2.2 נובע ממנו |
| **פ-6** | ‏`0171:124-127` — backfill הרסני בלי audit | **הוכרע: שורת `audit_logs` אחת מסכמת** עם סיבה וספירה. §0 |

---

## 7. מה התוכנית אוסרת במפורש

- **אין להמציא דוח או שדה.** ‏#259 מפורש.
- **אין להעביר דוח קיים לתור לפני שנמדד.** אין ולו מדידה אחת של דוח בכל הריפו (§1.4).
- **אין לבנות תור כדי לפתור N+1, אינדקס חסר או `count:'exact'`.** אלה §1, §13, §14, §15 — ותור אינו התרופה.
- **אין דלי עם מדיניות INSERT בלי מדיניות SELECT** (`0125:139-143`).
- **אין מדיניות אחסון מול `metadata`, ‏`owner` או `owner_id`** — הם null בזמן ריצת המדיניות.
- **אין מיגרציה מכוח התוכנית הזו.**

---

## 8. מה נותר לעשות, ומתי הפריט חוזר

**נותר לעשות — אינו תלוי בוויתור:**

1. **שני ליקויי הכסף של §2.2.** הפריט הדחוף ביותר במסמך. משפיע על הדוח שהרו״ח מקבל.
2. **סדר מיזוג `0171`–`0176` לפני `0177`+**, בגלל §2.4.
3. **‏`0171:124-127`** — להוסיף שורת audit ל‏backfill, או להכריע שאינה נדרשת (פ-6).
4. **מדידה אחת של `create_monthly_report_snapshot`** — זולה, סוגרת את פ-4 לתמיד.

**התנאים שמחזירים את Report Jobs לשולחן — שניהם, לא אחד מהם:**

- **מדידה** שמראה דוח קיים שזמנו חורג ממה שמסך יכול להחזיק, **ושהסיבה אינה N+1, אינדקס חסר או
  ספירה לא־חסומה** (§1.3).
- **חוזה דוח בעל עמודות בצד השרת** (פ-5), כך שעובד לא ייאלץ לממש מחדש את `reportTemplateExport.ts`
  ולהפר את #259 (§3).

עד אז: **דוח כבד שנמצא — נפתר במקום שבו הוא כבד**, לא בתור.

**עדכון נדרש ברשומות ההחלטה:** ‏#259 ו‏`DEBT-REGISTER.md` §3 צריכים לשקף את הוויתור. ראו §10.

---

## 10. נוסח מוצע לרשומות ההחלטה

**‏`OPEN-DECISIONS.md` #259 — תוספת לעמודת ההכרעה:**

> **עודכן 23.08.2026 (בעלים): נסגר כפריט פעיל.** מדידת המצב הראתה שאין דוח קיים שמצדיק תור
> אסינכרוני: כל דוח מייצר `.xlsx` בדפדפן, המשטח הכבד היחיד עם ארטיפקט (‏ייצוא offboarding) כבר
> בנוי ב‏`0103`, ולא נמדד ולו דוח אחד בכל הריפו. הפריט חוזר רק בהתקיים שני התנאים ב‏
> `docs/PLAN-report-jobs-20260823.md` §8. מצב: `DECIDED / WITHDRAWN / NOT_BUILT`.

**‏`DEBT-REGISTER.md` §3 — החלפת המשפט האחרון:**

> **הצעד הבא:** ‏Rules Engine — תוכנית `docs/PLAN-rules-engine-20260823.md`, הוכרעה 23.08.2026
> (כותב = מפעיל, צרכן = שלושת הספים ב‏`alerts.ts`). ‏Report Jobs — **נסגר כפריט פעיל**, ראו
> `docs/PLAN-report-jobs-20260823.md`. ‏Workflow Engine — נדחה סופית (#257).
> **אין מיגרציה או מימוש מכוח הכרעה בלבד.**

---

## 9. ראיות

`OPEN-DECISIONS.md` #259, #260, #261, #181, #72 · `DEBT-REGISTER.md` §1, §3, §13, §14, §15, §30 ·
`0045:398-434,599-601,610-647` · `0057:20,139` · `0064:337-351,377` · `0074:201,233,252-594` ·
`0103:7-12,53-62,801-814` · `0123:47-58,130-146,162-169,194-196` · `0125:1-67,139-143` ·
`0126:20-21,60-62,80-82,156-159,167-196,332-352` · `0129:40-58,99-139` · `0130:58,251-253,341` ·
`0132:37-65` · `0164:1602,1667,1680,1985-1987` · `0165:33-34` · `0005:10-21` · `0006:6-8,21-31,79-86,135-162` ·
`tenant-export/index.ts:1517-1554` · `worker/ocr/src/worker.py:716-720,751-808` ·
`src/lib/exportTemplates.ts:50-91` · `src/lib/reportTemplateExport.ts:6,26-52,38-40` · `src/pages/Reports.tsx:104-114`
