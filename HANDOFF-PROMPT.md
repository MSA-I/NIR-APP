# פרומפט מסירה — קליטה נכנסת, חוזה עמודות הדוח, וחוליית החיוב

העתק את כל מה שמתחת לקו לסוכן המבצע.

---

אתה מממש תוכנית שכבר עברה חקירה מול הבעלים וחמישה סבבי ביקורת יריבה מול מודל אחר.
**אל תתכנן מחדש. אל תשפר את הארכיטקטורה. אתה מבצע מפרט נעול.**

## מה לקרוא, בסדר הזה, לפני שאתה נוגע בקוד

1. `CLAUDE.md` — חוקת הפרויקט. כללי הברזל שם מחייבים.
2. `docs/PROGRESS.md` ואז `docs/DEBT-REGISTER.md`.
3. **`PLAN.md`** — התוכנית הנעולה.
4. `docs/plans/2026-08-31-inbound-intake-and-billing.md` — המפרט המלא (982 שורות).
5. `docs/research/2026-08-31-inbound-intake-claudex-research.md` — עובדות הספקים.
6. `PLAN-REVIEW-LOG.md` — **קרא אותו.** הוא מסביר *למה* כל החלטה נראית כך, ומונע ממך
   "לתקן" משהו שנכתב במכוון.

**כלל הכרעה:** במקום סתירה בין `PLAN.md` למפרט — **`PLAN.md` גובר.** במקום סתירה בין
`PLAN.md` ל-`CLAUDE.md` — **`CLAUDE.md` גובר, ואתה עוצר ומדווח.**

## תנאי-קדם חוסמים — אף שורת קוד מוצר לפניהם

1. **‏preflight טרי.** סרוק את **כל** ה-refs ועצי העבודה והקצה מספרי מיגרציה וסוויטה
   **עכשיו**. ‏`0268`–`0270` ו-`p94`/`p95` **תפוסים**. ‏`PLAN.md` מפנה לשמות לוגיים
   (`M-INGEST`, `M-WA`, `M-MAIL`, `M-COLUMNS`, `M-BILLING-LINK`, `M-ENABLE`) בכוונה — אל תניח
   מספרים מהמסמך.
2. **רישום ההכרעות.** ‏`#309`–`#318` נכתבות ל-`docs/OPEN-DECISIONS.md`. הרישום הקנוני מסתיים
   ב-`#308`, ושתיים מההכרעות **הופכות** את `#241` ואת `#259` — כלומר עד שהן נכתבות, הרישום
   עדיין אומר „לא", ואסור שקוד יקדים אותו.
3. **`guardedDownload`.** ‏`supabase/functions/webhook-verify/ssrf.ts` **אינו יכול להוריד קובץ**
   (`:490` — „The body is intentionally absent"). בלי מוריד בטוח, שני ערוצי הקליטה חסומים.
4. **ספירת מסמכים חסרי-מעלה** לפני כל backfill של `source` — ראו מלכודת 6 להלן.
5. **גשוש `received_for`** — חוסם את `M-MAIL` בלבד, לא את גל 0.

## שבע מלכודות שנמדדו — כל אחת הייתה מגיעה לקוד

הן מפורטות ב-`PLAN.md`. אל תגלה אותן מחדש בדרך הקשה:

1. מספרי המיגרציות שהמפרט הקצה **תפוסים כולם**.
2. **`document_scan_jobs.requested_by` הוא `not null`** (`0136:100`) **ו**התובע מצמיד
   `j.requested_by = d.uploaded_by` (`0246:480`). שחרור נעילה אחת לא פותח את הצינור —
   תמונה לא תיכנס לסריקה.
3. **`ssrf.ts` מחזיר `Response(null)`** — אינו מוריד תוכן.
4. **סדר „raw → verify → parse" אינו ישים ב-Twilio** (`whatsapp-webhook/index.ts:52-72`).
   ‏`PLAN.md` נושא טבלת סדר פר-ספק. האינווריאנט האמיתי: **אף תופעת לוואי לפני אימות.**
5. **ל-`profiles` אין עמודת email** (`0001:31-38`). היא ב-`auth.users`.
   ‏**ו-`profiles.backup_email` (`0255`) אינה כתובת ההתחברות** — אל תשתמש בה.
6. **`documents.uploaded_by` nullable מאז `0001:368`.** ‏backfill גורף + אכיפת
   `browser ⇒ actor not null` **מפיל את המיגרציה בייצור**. ספור קודם; תוצאה שאינה אפס עוצרת.
   **לעולם אל תמציא שחקן.**
7. **`auth_scopes()` חסרת-ארגומנטים ונשענת על JWT** (`0054:321-328`), **ו-`assert_unit_in_scope`
   מקדימה יציאה כ„עבודת שירות מהימנה" כשאין JWT** (`0054:334-336`). בדיקת היקף בעבודת cron
   שמשתמשת בהן **עוברת בלי לבדוק כלום.** השתמש ב-`private.scopes_for_user(profile_id, org_id)`
   מעל `user_scope_closure`.

## סדר העבודה

לפי `## Approach` ב-`PLAN.md`. **גל 0 (`M-INGEST`) ראשון** — הוא חוזה שחקן-המכונה וכל השאר תלוי בו.
‏`PR-3` (הכנסת שלושת `core.test.ts` של ה-webhooks לשער) אינו תלוי בכלום ויכול לרוץ במקביל.

**כל PR בנפרד, ועל בסיס `main`.** ‏PR שהבסיס שלו אינו `main` מקבל **אפס בדיקות** ונראה כמו הצלחה
(`DEBT §65`) — אם אתה חייב שרשרת, הרץ `gh workflow run quality-gate.yml --ref <branch>` וקשר
לתוצאה בהערה.

## מה חסום ואסור להתחיל

- **‏checkout** — עד שיש Paddle sandbox. בלי מזהי מחיר אמיתיים אין צורה לכתוב אליה.
- **‏`M-ENABLE`** (הדלקת ספק חיוב) — **נכתבת, נסקרת, ואינה נמזגת.** ולא ממזהי sandbox.
- **מפת המחירים** — נשארת ריקה.
- **מיזוג הענף אינו הפעלת גבייה** ואינו יכול להפוך לכזו בהגדרה.

## משמעת

- **אל תשנה את מסלול הדפדפן בשום תו.** ‏`documents_insert` ו-`smart_document_source_checksum`
  הקיימות אינן נגעות.
- **`org_id` לעולם לא מה-payload, ולעולם לא מארגומנט של קורא `service_role`** — הוא נגזר מרשומת
  הניתוב במסד.
- **אל תדווח „בוצע" על מה שלא נמדד.** שער ירוק שלא רץ אינו ראיה; `Up` אינו ראיה; HTTP 200 אינו ראיה.
- **שינוי ויזואלי — צילום מסך של התוצאה בפועל.**
- **שני כשלים רצופים באותו שער — עצור ודווח.** אל תיכנס ללולאת ניסיונות. כל retry דורש השערה חדשה.
- **אל תריץ פקודות git שמשנות מצב** (`reset`, `checkout -f`, `stash pop`, `rebase`) — יש סוכנים
  אחרים על אותו ריפו, ו-stash משותף בין כל עצי העבודה.
- **אל תיגע בעצי עבודה אחרים.**
- **תקשר עם משה בעברית, בשפה פשוטה ולא טכנית.** הוא אינו מתכנת.
- **אל תיתן הערכות זמן בימים.** מדוד היקף: כמה קבצים, כמה אתרי קריאה, כמה בדיקות.
- **סודות:** אתה לא נוגע במפתחות ולא מבקש אותם. אם נדרש סוד — אמור למשה מה השם ואיפה מתקינים,
  והוא יעשה זאת בעצמו.

## שאלות פתוחות שאתה עשוי לפגוש

- **‏ט-1:** גודל מייל וצרופה מרביים ב-Resend **אינם מתועדים**. אל תנחש — מדוד או שאל את הספק.
- **‏מצב `RESEND_WEBHOOK_SECRET`** לא נמדד. אל תטען לגביו לשום כיוון.
- **ארבעת הממצאים האחרונים של Codex יושמו ולא נסקרו מחדש** (הלולאה נעצרה בתקרה של 5 סבבים).
  הם ב-`PLAN.md`: `private.scopes_for_user`, מפתח מסירה כפול, מדיניות bounce, ושתי כותרות.
  אם משהו שם נראה לא עקבי — **דווח, אל תתקן לבד.**

**ולבסוף:** אם אתה מוצא שהתוכנית שגויה במשהו — **עצור ודווח למשה.** אל תממש מפרט שאתה יודע שהוא
שגוי, ואל תשנה אותו בשקט. שבע המלכודות למעלה נמצאו בדיוק כך.
