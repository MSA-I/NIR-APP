# The operator console is a second HTML entry in the same deploy. Named explicitly and ABOVE
# the SPA catch-all so /operator can never be swallowed into the tenant shell by the rewrite.
/operator /operator.html 200
/operator/* /operator.html 200
/* /index.html 200
